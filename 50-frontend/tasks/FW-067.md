---
id: FW-067
legacy_id:
title: WIP rejection dialog
status: not-started
status_confirmed: true
owner:
jira:
mvp: 1
phase: "7"
stream: FE
streams: [FE]
priority: high
hours: 20
sprint: S2
depends_on: [FW-134, FW-174, FW-254]
blocked_by: [OI-100, OI-84, G79, G82]
has_plan: false
started:
completed:
---

# FW-067 - WIP rejection dialog

> **No implementation plan has been written for this story yet.**
> The card below is the contract from `[TB]`. Before starting, replace this notice
> with the sections in the task template: *What to build* / *Context you need* /
> *Build order* / *Decisions made here* / *Verification* / *Handoff*.

## 1. What to build

**Hours:** 20 h FE · **Priority:** High · **Sprint:** S2 · **Phase:** 7 · **Stream:** FE

**As an** operator,
**I want** to flag suspect material with its context already filled in,
**So that** a hold is recorded in seconds and the supervisor is alerted.

**Acceptance Criteria:**
- [ ] Built as a `MatDialog` from `../mockups/wip_rejection.js` — `openWipRejection(ctx)`, CSS scoped under `.fwwip`
- [ ] **Raised from five places**, each supplying its own material context: the active-run monitors, the pre-check-in station (failed staging inspection), the SPC checkpoint (*suspend material*), the resume dialog, and the More Options tile. **Nothing navigates to it**
- [ ] **Context contract:** `materialAlpha`, `stage`, `runId`, `footagePosition`, `trigger`, and on the staging path `payoff`
- [ ] **On the pre-check-in path `runId` and `footagePosition` are both `null`** — the rod never ran
- [ ] Context auto-populated (alpha, stage, footage, time); group → reason dropdowns; details (measured / target / deviation)
- [ ] Disposition **Suspend (→ HOLD)** / **Scrap (→ SCRAP)** / **Rework (→ return stage)**; **observation required for Suspend**
- [ ] Submit → status set, WIP-Held queue entry, `AlertRaised` to Dashboard 1
- [ ] ✅ **The reason list arrived 1 Sep 2026 — 72 in scope, 64 inherited and 8 new** (`A5` closed). Read them from **`GET /reasons/wiprejection`** ([`FW-254`](../../40-backend/tasks/FW-254.md)), never compiled in. [`wip_rejection.js`](../mockups/wip_rejection.js) has already been reworked to the new list and **needed only a data swap** — its quick-chips-over-two-selects pattern was already right, and [`FW-071`](FW-071.md) has now adopted it
- [ ] ⛔ **`G79` — every group value is ours.** The client's sheet is a **flat list with no grouping at all**, while `RejectionGroup` was `NOT NULL` across five groups. `CK_WipRejection_Group` is **dropped** and the group moved onto the `WipRejectionReason` row ([`FW-251`](../../30-database/tasks/FW-251.md)); all 72 assignments were made by us on 2 Sep 2026 and **are not client-signed-off**. The dialog must not present them as if they were
- [ ] ⛔ **`G82` — no threading reason exists**, and the client's answer 4 is explicit that *"we will definitely need to record the threading as a WIPREJ/Scrap."* The footage comes from the Material Loss table, **whose every value is empty pending trial**. So the transaction is mandated, the reason code is missing and the quantity is unknown — do not invent any of the three
- [ ] ⚠ **24 reasons were deliberately excluded** as coil-form and side-scrap reasons that do not exist on wire (*Angel Wings*, *Excess Side Scrap*, *Coil Set*, …). A reason absent from the list is absent on purpose

**Rate-card basis:** modal/dialog 12 h + five call sites and the context contract 8 h = 20 h (§2). ⚠ **Not re-priced** — the mockup took a data swap, and [`FW-258`](../../60-delivery/tasks/FW-258.md) owns any movement
**Dependencies:** FW-134, FW-174, **[FW-254](../../40-backend/tasks/FW-254.md)** *(the reason list)*
**Blockers:** **OI-84** *(WIP reason list — ⚠ **narrowed, not closed**: the 72 reasons arrived 1 Sep 2026, the **groups did not**, and it stays `Open` in the register)* · **`G79`** *(all 72 group values are ours)* · **`G82`** *(no threading reason)* · **OI-100** *(valid rework stages)*

---
