---
id: FW-133
legacy_id:
title: Shared composite controls
status: not-started
status_confirmed: false
status_note: "🟡 **`gauge-trace-chart`'s foundation is built** — `lib-chart-canvas`, `lib-trace-panel` and `buildTraceConfig` are in `projects/shared` and consumed by DB3. ⛔ **Still owed:** the runtime-switchable `isLive` contract, the inline-SVG profile mode, the REST profile query, the point window, and the other five controls. 🔴 **The hours are deliberately not restated** and now overstate what is left — see the banner"
owner:
jira:
mvp: 1
phase: "1A"
stream: FE
streams: [FE]
priority: critical
hours: 120
sprint: S0
depends_on: [FW-130]
blocked_by: [G18]
has_plan: true
started:
completed:
---
# FW-133 · Shared composite controls — the critical path

**Project:** United Aluminum (UAL) — Flat Wire Mill Module
**Last Updated:** September 4, 2026 — Change history is in [`../../CHANGELOG.md`](../../CHANGELOG.md)
**Document Type:** Implementation plan for a single backlog story
**Status:** Ready to build once `FW-130` lands — **wave 2**. 🔴 **120 h, 45 % of the layer, and every screen depends on it**
**Owner:** Frontend (Angular) stream
**Audience:** The Angular developers building `FW-133` — plural, deliberately; see §2
**Shortcode:** — *(implementation plan, derived from the specifications; **not citable as a requirement**)*
**Depends on:** [`FW-130`](FW-130.md) — **tokens only**
**Unblocks:** every screen story in Phases 3–14, and `FW-081` wires `gauge-trace-chart` live
**Part of:** `ProjectPlan/Frontend/tasks/` — index: [Orchestration.md](Orchestration.md) · shared context: [Phase-01A-ImplementationPlan.md](Phase-01A-ImplementationPlan.md)

---

> ### 🟡 `gauge-trace-chart`'s foundation is already built — and the hours here are deliberately unchanged
>
> Two `shared` components and a config builder now exist, and this story composes them rather than
> starting from a canvas:
>
> | Built | What it owns |
> |---|---|
> | **`lib-chart-canvas`** (`projects/shared`) | the canvas and the whole Chart.js lifetime — create, update in place while live, resize with the box, **destroy before rebuild**, release on teardown |
> | **`lib-trace-panel`** (`projects/shared`) | the reading header, the chart and the statistics row, as one unit |
> | **`buildTraceConfig`** (`projects/shared`) | the line, target line and tolerance band, with `Chart.register` inside the builder |
> | **`lib-nav-rail`** (`projects/shared`) | ⚠ **not one of this story's six** — the collapsible icon rail, listed here because it is the fourth thing now in `projects/shared` and no story owns it. Consumed by both flat wire screens **and** `slitter-traveler-landing` |
>
> ⛔ **Still this story's, and unchanged:** the **runtime-switchable `isLive`** contract, the profile
> mode that renders **inline SVG rather than Chart.js**, the REST profile query, the ~500-point window
> (still to be chosen and written down), and the other five controls. Live wiring and maximise remain
> `FW-081`'s.
>
> ⚠ **The 120 h and the 20 h are NOT restated, on purpose.** The published metrics are already
> calculated and re-costing them here would disturb figures other documents depend on. The
> consequence to know: **the remaining hours now overstate the work left for `gauge-trace-chart`**,
> because its canvas, lifetime and config builder are done. Whoever reconciles the estimate should
> read this note, not infer effort from the number.
>
> ⚠ The placement is the one exception to `[CMP §5.1]`: these are consumed by **slitter-interface as
> well as flat wire**, so they live in `projects/shared` — see *Where a reusable control lives* in the
> repository `CLAUDE.md`.

> **Read [`[P1A §2]`](Phase-01A-ImplementationPlan.md) first.** Decisions: **`F-08`** (the 95 %
> coverage gate lands here hardest), **`F-14`** (the canvas), **`F-15`** (the mockups are
> authoritative for content, not composition).
>
> **This is the critical path.** `FW-N03 (24) → FW-130 (16) → FW-133 (120)` = 160 h, and this story
> is three quarters of it. Nothing on the path is blocked — the constraint is not a decision anyone
> owes, it is that **one story gates every screen in the programme.**
>
> This plan is derived from the specifications and **loses to every one of them.**

---

> ### 🎨 Building UI? Read [`[UIC]`](../UIConventions.md) first
>
> **The mockups are the content authority, not the look authority** — their `--color-*` palette
> exists nowhere in this repository. `[UIC]` carries the class inventory, the card idiom and the
> rules a built screen produced: **badge text colour follows the badge background**
> (`bg-dark-subtle` → `text-black`; `bg-secondary` → leave it white), a card-header subtitle keeps
> the header's `fw-bold`, Bootstrap's `.badge` renders at 10.5 px so every badge takes `fs-14`, and
> the app forces every `.btn` to `#343a40` with `!important`.
>
> **Create no new class, style or colour.** If a requirement genuinely needs one, say so explicitly
> in this plan — what was created and why.

---

## 1. The story

From `[TB §7]`:

> ###### FW-133 · Shared composite controls
> **Hours:** 120 h FE · **Priority:** Critical · **Sprint:** S0 · **Phase:** 1A · **Stream:** FE
>
> **As a** developer,
> **I want** the six shared composite controls built once from the approved mockups,
> **So that** no phase reimplements them and every screen looks like one system.
>
> **Acceptance Criteria:**
> - [ ] `pass-schedule-table` — read-only component/state table, honouring `State ∈ {Active, Bypass, Skip}`
> - [ ] `payoff-weight-bar` — consumption bar with colour thresholds
> - [ ] `gauge-trace-chart` — Chart.js streaming shell with target line, tolerance band, green/red points and weld markers
> - [ ] `tolerance-viz` — SPC marker on a band; **replaces the retired inline-SVG progress ring**
> - [ ] `tab-wizard` — progressive-unlock step strip backing Dashboard 2's six steps
> - [ ] `action-bar` — line-mode configurable, **intent-grouped** (Run events · Go to · Run control)
> - [ ] All six built fresh from `../Mockups/*.html`; **none derived from `shop-floor-common`, `checkin-precheckin` or any existing UI library**
>
> **Rate-card basis:** 6 × shared composite control @ 20 h = 120 h
> **Dependencies:** FW-130 (tokens)
> **Blockers:** **G18**

---

## 2. Not trimmable, but divisible — and those are different claims

`[TRP §1.4]` says *"It is not trimmable"*, meaning **no control can be cut**. It does **not** say one
developer must build all six. The six are **independent of one another** and share only `FW-130`'s
tokens, so this is where a second and third developer go.

⚠ **Start with `gauge-trace-chart`.** It is the only member with a live-streaming contract behind it
(`FW-081`, `[SIG §5.6]`) and the only one whose **`isLive` input must be runtime-switchable, not
mount-time** — Phase 8's FL2 Live/Profile control and a hybrid FL3 run both require flipping it on a
mounted component.

**FE compresses better than any other stream, and that cuts both ways.** `[TRP §1.4]` prices FE
retention at **0.62** against RT's 0.75–0.90, so the most AI-assisted leverage in the programme sits on
this path. It is also the stream `[DE §4.3]` warns *"is not the constraint"* — compressing this story
does not shorten the programme; it stops this story from holding up eleven phases of screens.

---

## 3. The six controls

### 3.1 `gauge-trace-chart` — build this first

| | |
|---|---|
| **Mockups** | `dashboard_3_active_run.html` (live) · `_fl2.html` (profile) · `_fl3.html` (hybrid) |
| **Consumers** | DB1 live gauge · DB3 all three variants · `FW-081` live wiring · Phase 8's FL2 variant |
| **Technology** | **Chart.js**, updated in place with `update('none')` — bounded redraw under the ~10 Hz feed (`[CMP §5.5]`) |

**One component with an `isLive` flag, not two components** (`[CMP §5.5]`).

- ⚠ **`isLive` is runtime-switchable.** Not a constructor input, not mount-time.
- **Live mode:** target line, tolerance band, green/red points, weld markers, a fixed window of ~500 points.
- **Profile mode:** the FL2 historical trace is a **REST query** (`GET /run/{runId}/gaugetrace`), and the mockup's profile is **hand-crafted inline SVG, not Chart.js** — so profile mode renders SVG, not a Chart.js dataset.
- ⚠ **FL2 sends `null` live gauge and width, forever, by design** (`FR-120`, `[SIG §5.3]`). A component that waits for `GaugeReading` on `FL2Data` is a defect, not a slow loader.
- **The ~500-point window is now a choice.** At 1920 the same window renders at ~1.5× horizontal pixels per point, or the same density across ~750 points. `[SIG §4]` fixes it at ~500 and `G9` gives no cadence target to derive better from — **pick one deliberately and write it down** (`F-14` rule 3).
- Available already: `chart.js` 4.5.1 with the **annotation**, **zoom** and **datalabels** plugins — the target line and tolerance band are annotation-plugin work, not hand-drawn.
- ✅ **The target line, the tolerance band and the whole Chart.js lifetime are built** — `buildTraceConfig` and `lib-chart-canvas` in `projects/shared`. **Compose them; do not write a second canvas.** What is left on this control is `isLive`, the SVG profile mode, the REST query and the point window.
- ⚠ **Live wiring, maximize and the runtime source toggle are `FW-081`, not this story.** Build the shell and its inputs. ✅ Maximise itself is **already built** as a popup on DB3 ([`[UIC §3.14]`](../UIConventions.md)).

### 3.2 `pass-schedule-table`

Read-only component/state table. **Honours `State ∈ {Active, Bypass, Skip}`** — never a boolean
(`FW-132`'s enum, `[API §2]`). Grey = bypassed or offline, per the token semantics. Consumers: DB2's
wizard, DB5, and the acknowledgement step at check-in.

### 3.3 `payoff-weight-bar`

Consumption bar with colour thresholds. **Used directly by Dashboard 1 and as the Payoffs card's
content on Dashboard 3** — `[TB §7]` is explicit that it *"survives as the Payoffs card's content, not
as a sibling of the status panel"*, which is a change to DB3's composition and **not** a deletion.
Feeds off `payoffWeight$`.

### 3.4 `tolerance-viz`

Track + marker + min/center/max labels. **Replaces the retired inline-SVG progress ring** — the
`- Old.html` Dashboard 2 variant is retired and deleted; do not resurrect it. Consumers: DB2 step 4,
DB6's SPC dialog.

### 3.5 `tab-wizard`

Progressive-unlock step strip backing **Dashboard 2's six steps**. The approved DB2 is
**`dashboard_2_rod_checkin.html`**, the guided six-step wizard (`D-08`). ⚠ **That file inlines its own
app bar and omits `flat-wire-topbar.js`** — so when you need a page skeleton, **clone Dashboard 12's,
not Dashboard 2's** (`[SCR §5.6]`).

### 3.6 `action-bar`

Line-mode configurable and **intent-grouped**: Run events · Go to · Run control. The per-line button
sets are fixed requirements, and **FL1 differs from the other two**:

| Line | Buttons | Source |
|---|---|---|
| **FL1** | six — Log Weld Event, Die Change, SPC Checkpoint, Pause Run, WIP Reject, Complete Run. ⛔ **no Roll Adjust, no edger controls** | `FR-107` |
| **FL3** | the six above **plus Roll Adjust** | `FR-108` |
| **FL2** | omits Weld and Die Change (no drawing dies); includes **Roll Adjust** and **Complete Coil** | `FR-109` |

⚠ **Roll Adjust is FL2 and FL3 only.** `OI-11` is
still carried as open in the register although `FR-107`–`FR-109` answer it.

---

## 4. Rules that apply to all six

- **Built fresh from [`../Mockups/`](../Mockups/).** ⛔ **None derived from `shop-floor-common`, `checkin-precheckin`, `common-grid`, `wip-rejection` or `slitter-*`** — `D-06`, and `[ARC §2.2]` calls these *"the rules most likely to be broken by a developer working from habit."*
- ⛔ **`--color-*` is defined nowhere in the repository, and no stylesheet ships it.** **No new styles are created** — use the application's `src/styles/_colors.scss` SCSS variables, and write a new style or class only when a requirement calls for one. `--fw-*` does not exist either (`G18`).
- **`OnPush` everywhere**, and the trace component must not re-enter the Angular zone per reading.
- ⚠ **Component `.scss` stays under 10 kB** — `anyComponentStyle` is a build **error**. ⛔ **There is no library stylesheet to move shared bulk into**; keep each component's styles genuinely small, and if a shared sheet becomes a requirement it follows `planning-styles.scss`.
- **14 px floor**, including any form control a control hosts. The documented exception is axis labels inside the three compressed DB3 charts — and ⚠ **1080 does not relieve it** (`F-14` rule 1).
- ⚠ **Composition at 1920 × 1080 is new design work with no mockup behind it** (`F-15`). These controls are content-authoritative from the mockups; how they sit on a 16:9 panel is not.

---

## 5. Verification

```bash
ng build shared                 # MANDATORY FIRST if you touch projects/shared — jest resolves the
                                # `shared` alias to dist/, not source, so a stale bundle fails silently
npm run build   # compiles flat-wire from source via the wrapper — there is no `ng build flat-wire`
ng lint flat-wire
npm run test:shared             # anything added to projects/shared lands here, not in flat-wire
npm run test:flat-wire          # 95 % on components — see below
npm start                       # a harness route rendering all six — add it as a CHILD of the shell
                                # route in flat-wire-routing.module.ts
```

⚠ **Two of the six now belong in `projects/shared`, not `projects/flat-wire`** — anything
slitter-interface also consumes does (see *Where a reusable control lives* in the repository
`CLAUDE.md`). That moves their tests to `npm run test:shared` and makes `ng build shared` a
precondition of every dependent test run.

| AC | Proof |
|---|---|
| 1–6 · the six controls | each renders from its mockup, in light and dark, at 1920 × 1080 |
| `gauge-trace-chart` | `isLive` flipped **at runtime** on a mounted component switches live ↔ profile; a mock stream at ~10 Hz drives it with **no change-detection storm** under OnPush |
| `action-bar` | the three per-line button sets match `FR-107`–`FR-109` exactly, with **no Roll Adjust on FL1** |
| `pass-schedule-table` | renders all three `State` values distinctly |
| 7 · built fresh | ⚠ **reviewable only by reading the diff** — no test proves provenance. **`D-06` reviewer: TBD**, and it must be someone who knows what `shop-floor-common` looks like |

**Test cases:** ⛔ **None.** Phase 1A has **no test cases in `[TCS]`** — measured 28 Aug 2026 against its **405 defined cases** (the ids run to `TC-799`, but 47 are cited and never defined), nothing covers the Angular library, the shell, the canvas, the guards or the mock hub. This story's verification is this plan plus Jest, and nothing else. → `[TCS]`, `[P1A §6.13]`

⚠ **`F-08` bites hardest here.** `collectCoverageFrom` globs `*.component.ts`, so all six carry a
**95 %** obligation on statements, branches, functions and lines — and `phase-01a` asks only for
"smoke tests". That difference is real effort against `[TB §7]`'s 120 h, and it is flagged rather than
re-estimated because `[CE]` owns the figure.

---

## 6. Blockers and open items

| Item | Effect |
|---|---|
| `G18` | ⚠ **a trap, not a gate** — `--fw-*` is retired and there is nothing to migrate |
| ⚠ `[SIG §4]`'s ~500-point window vs `G9` | no cadence target to size the window against; choose and record |
| ⚠ `OI-11` | Roll Adjust line applicability — **answered** by `FR-107`–`FR-109`, still open in the register |
| ⚠ `F-15` | composition at the new canvas has no mockup |
| ⛔ no owner for scale-to-fit | you cannot review your own layout 1:1 — `[P1A §6.3]` |

---

## 7. Handoff

**Every screen story in Phases 3–14 consumes these six.** Three things to hand on:

1. **`gauge-trace-chart`'s `isLive` contract** — runtime-switchable, and `FW-081` wires it live.
2. **Which window size you chose**, and why.
3. **The eight controls in `[CMP §7.6]` that are *not* in this story** — `option-card`, `consequence-box`, `footer-stamp`, `info-table`, `machine-status-panel`, `skid-tracker`, `source-traceability-table`, `coil-label`. They belong to the screen story that first needs them, and that is a **costing** boundary, not a licence to reimplement.
