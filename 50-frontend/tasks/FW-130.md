---
id: FW-130
legacy_id:
title: Shell layout and the 1920×1080 shopfloor canvas
status: in-progress
status_confirmed: false
status_note: "🟡 **Partially done.** The DB1 Line Status screen is built as `supervisor-dashboard.component` — left nav, two context bars, three `col-4` line cards, alerts bar — and it is the **reference implementation** for [`[UIC]`](../UIConventions.md). ⛔ **Not done: the Payoffs section and the Components / die-life / SPC section** of the line card. ⚠ The screen itself is **not phase-1 scope** — it belongs to a future supervisor-desktop story"
owner:
jira:
mvp: 1
phase: "1A"
stream: FE
streams: [FE]
priority: critical
hours: 16
sprint: S0
depends_on: [FW-N03]
blocked_by: [G18, G23]
has_plan: true
started:
completed:
---
# FW-130 · Shell layout and the 1920 × 1080 shopfloor canvas

**Project:** United Aluminum (UAL) — Flat Wire Mill Module
**Last Updated:** September 4, 2026 — Change history is in [`../../CHANGELOG.md`](../../CHANGELOG.md)
**Document Type:** Implementation plan for a single backlog story
**Status:** 🟡 **Partially done** — the DB1 Line Status screen is built as `supervisor-dashboard.component` and is the **reference implementation** for [`[UIC]`](../UIConventions.md). ⛔ **The Payoffs and Components / die-life / SPC sections of the line card are not implemented.** ⚠ **The screen is not phase-1 scope** — see §1
**Owner:** Frontend (Angular) stream
**Audience:** The Angular developer building `FW-130`
**Shortcode:** — *(implementation plan, derived from the specifications; **not citable as a requirement**)*
**Depends on:** [`FW-N03`](FW-N03.md)
**Unblocks:** [`FW-133`](FW-133.md) · [`FW-134`](FW-134.md) — **for the tokens only**
**Part of:** `ProjectPlan/Frontend/tasks/` — index: [Orchestration.md](Orchestration.md) · shared context: [Phase-01A-ImplementationPlan.md](Phase-01A-ImplementationPlan.md)

---

> **Read [`[P1A §2]`](Phase-01A-ImplementationPlan.md) first.** Decisions this story rests on:
> **`F-03`** (the token stylesheet and stylelint), **`F-14`** (the canvas), **`F-15`** (what the
> mockups are and are not authoritative for).
>
> **This story is on the critical path.** `FW-N03 → FW-130 → FW-133` is the 160 h path, and
> `FW-133` cannot start without this story's tokens.
>
> This plan is derived from the specifications and **loses to every one of them.**

---

## 1. The story

From `[TB §7]`:

> ###### FW-130 · Shell layout and the 1280×1024 shopfloor canvas
> **Hours:** 16 h FE · **Priority:** Critical · **Sprint:** S0 · **Phase:** 1A · **Stream:** FE
>
> **As an** operator,
> **I want** a consistent shell with header, navigation and an alert slot on a fixed shopfloor canvas,
> **So that** every dashboard renders identically on the panel at arm's length.
>
> **Acceptance Criteria:**
> - [ ] Shell layout component: header (line context + operator + clock), sidebar nav to all dashboards, `alert-banner` slot
> - [ ] Fixed **1280×1024** canvas; renders in both light and `prefers-color-scheme: dark`
> - [ ] Consumes the existing semantic design-token system in `../mockups/flat-wire-shopfloor.styles.scss` **as-is**
> - [ ] **No `--fw-*` token appears anywhere** — that prefix is stale (**G18**)
> - [ ] Minimum text size **14px** throughout, including form controls
>
> **Rate-card basis:** shared composite control 20 h, **discounted to 16 h — the token system is consumed, not authored**
> **Dependencies:** FW-N03
> **Blockers:** **G18** · **G23**

⚠ **The canvas criterion is superseded.** `F-14` sets **1920 × 1080**, which decides `G23`. The
figure in the card above and in `phase-01a` is the old one; `[VAL §7.5]` — the row every other
document derives from — was corrected on 27 Aug 2026.

### 1.1 In scope

✅ **Done** — two screens, both on the 1920 × 1080 canvas at the 14 px floor, plus the
default-child redirect that makes `#/flat-wire` resolve:

| Screen | Component | Route |
|---|---|---|
| **DB3 Active Run Monitor** — the landing screen, and the reference [`[UIC]`](../UIConventions.md) is drawn from | `flat-wire-landing.component` | `flat-wire-landing` *(the default)* |
| **DB1 Line Status** | `supervisor-dashboard.component` | `supervisor-dashboard` |

⚠ **DB3 belongs to [`FW-062`](FW-062.md)** and DB1 to a future supervisor-desktop story. Both were
built here as UI references; the data paths are theirs.

⛔ **Not done — two sections of the line card**, both present in
[`../mockups/dashboard_1_line_status.html`](../mockups/dashboard_1_line_status.html) and both
skipped: the **Payoffs** section and the **Components / die-life / SPC** section. Dark mode is also
unverified.

⚠ **This screen is not phase-1 scope.** It belongs to a future **supervisor-desktop** story — a
supervisor checking machine status across the mill — and lives at `#/flat-wire/supervisor-dashboard`.
It stays because it is the **reference implementation** for [`[UIC]`](../UIConventions.md): it
established the card idiom, the palette and the six rules every flat wire screen now follows.
**Phase 1's own default screen layout is a separate piece of work.**

⛔ **No token stylesheet, no library style file and no app `styles` array entry** — step 2 is
withdrawn, so there is no global sheet to contain either. ✅ **Verified after the build: no `.scss`
exists in `projects/flat-wire/`.** The screen adds three generically named classes to
`src/styles/styles.scss` — `.floating-popover`, `.floating-popover-scale` and `.live-dot` — all
recorded in
[`[UIC §2]`](../UIConventions.md). Anything that earns a class belongs there, under a generic name,
never in a library sheet.

⚠ **Two utility classes had to be defined, not just used.** `min-w-0` and `min-h-0` were being
referenced by this screen — and by `slitter-traveler-landing` before it — but
`_dynamic-classes.scss` generates from **1**, so neither existed and both were silent no-ops. That
was a **horizontal scrollbar at every resolution**. They are now defined, and the height-budget
rule that depends on them is [`[UIC §4.1]`](../UIConventions.md).

✅ **The screen has been reviewed against the standards** — `.claude/code-review-guidelines/` and
`.claude/instructions/` — and the findings are fixed. What was caught, and the two deviations kept
on purpose, are listed in [`[UIC §3.20]`](../UIConventions.md) so the next screen does not repeat
them. ⚠ **`ng lint flat-wire` passed before and after**: every finding was a convention ESLint
cannot check.

### 1.2 Out of scope

| Not here | Owner |
|---|---|
| Any control the shell hosts | `FW-133` / `FW-134` |
| `alert-banner` itself — this story provides the **slot** | [`FW-134`](FW-134.md) |
| The scale-to-window behaviour (`flat-wire-fit.js`'s Angular equivalent) | ⛔ **no story owns it** — `[P1A §6.3]` |
| Sidebar *targets* for screens not yet built | the phase-3+ screen stories |

---

## 2. The canvas — a change of shape, not of size

| | was | **now** | change |
|---|---|---|---|
| Width | 1280 | **1920** | **+640 px · ×1.50** |
| Height | 1024 | **1080** | **+56 px · ×1.05** |
| Aspect | 5:4 (1.25) | **16:9 (1.78)** | **the shape changes** |

⚠ **1920 × 1080 at 100 % scaling is 16:9 by definition** — there is no scaling factor to reason
about and no ambiguity left in the ratio. Every statement below that reads *"5:4"* is describing
the **mockups**, which were composed at 1280 × 1024, not the target panel.

**Six rules, and the first is the one that gets missed.**

1. ⚠ **The re-layout is horizontal. Nothing gets taller.** +56 px is one row of 14 px type with its padding, so **every vertically-constrained decision stays constrained** — including `[VAL §7.5]`'s sub-14 px exception for axis labels on `dashboard_3_active_run` / `_fl2` / `_fl3`. **1080 does not relieve the chart compression.**
2. **Spend the width on columns, not on stretching.** A 1280-composed panel widened to 1920 has 640 px of slack; filling it by scaling turns 14 px type into 21 px and a dense screen into a sparse one. Two columns → three; stacked → side by side.
3. **The 14 px floor and ≥ 48 px tap targets are physical rules** — `[VAL §7.5]` derives both from reading at arm's length with gloves, so they track **dpi, not resolution**. `F-14` carries them forward on a stated assumption: the new panel is **physically larger** of the same dpi class. ⚠ **If it is the same panel at higher density, every size scales ×1.5 and the floor becomes 21 px.** The diagonal is recorded nowhere — `[P1A §6.10]`.
4. **Fixed canvas, not responsive.** One target box, centred, letterboxed below it. It is a panel, not a browser.
5. ⚠ **You will not see it 1:1 while building it.** A 1920 × 1080 laptop loses ~100 px of height to browser chrome. The Angular scale-to-fit that would solve this **has no owner** (`[P1A §6.3]`) — until it does, review at reduced zoom and check type sizes against a rendered 1:1 reference.
6. **The mockups remain authoritative for content, not composition** (`F-15`). **18 of the 19 are laid out for 5:4.** ⚠ **`dashboard_3_active_run_ual.html` no longer exists in the folder** — it was the 1920×1080 comparison build, and `[P1A §6.16]` still cites it. What is there now is **`mockups/ual-app.styles.css`**, the compiled application CSS (Bootstrap 5.3.8 plus the app's own, 15,460 lines), so a mockup can be previewed in the real styling. Three things the comparison build established by measurement, which still hold: a **two-column split** (traces left in `col-8`, state rail right in `col-4`) is what spends the width without stretching; the app’s CSS needs a **35-token shim plus explicit 14 px containment** (§2a–§2c, `[P1A §6.16]`); and ⚠ **Bootstrap’s `.badge` renders at 10.5 px and the app forces every `.btn` to `#343a40` with a cyan icon on hover, both with `!important`** — neither is obvious until a screen is built on it.

---

## 3. Build order

### Step 1 — the screen is DB1 Line Status, and it is built in the **landing**, not the shell

> 🟡 **This step is done** — DB1 renders as `supervisor-dashboard.component` and DB3 as
> `flat-wire-landing.component`, both as **UI skeletons on placeholder data** (§1.1). The step is
> kept in the imperative because it is still the instruction for the two line-card sections that
> remain, and because the next screen is built by reading it.

⛔ **The UI does not go in `flat-wire.component`.** Measured: `slitter-interface.component` is *only*
`lib-header` / `router-outlet` / `lib-footer` / `lib-global-spinner` / `lib-toast`, and **every piece
of UI — sidebars, context bars, cards — lives in its child `slitter-traveler-landing`.**
`flat-wire.component` is already that same frame, so it is **untouched**; the dashboard is
`flat-wire-landing.component`. `slitter-traveler-landing` is the UI reference throughout.

**Composition, top to bottom** — `dashboard_1_line_status.html` supplies the content, not the look:

| Region | Content | Note |
|---|---|---|
| **Left nav** | collapsible icon rail — Shift Summary · WIP Rejections · Pass Schedules · Spool Queue · OEE | ⚠ **Added by us.** The mockup has no left nav; it carries these as header buttons. Entries for unbuilt screens are **inert, not absent** (§1.2). ✅ **Consume `lib-nav-rail` from `projects/shared`** — do not write rail markup ([`[UIC §3.22]`](../UIConventions.md)) |
| **Context bar 1** | *Flat Wire Mill* · *Line Status* chips, shift badge, live badge | mockup `.panel.header` |
| **Context bar 2** | Lines active `1 / 3` · Lbs this shift + progress · Orders completed · Avg shift util · Shift end | mockup `.panel.shift-summary` |
| **Line cards** | **three cards, `col-4` each, no fixed width** — FL1 Running · FL2 Idle · FL3 Offline *(hybrid)*, each with status pill, info grid, order progress, metrics, SPC line | mockup `.cards` |
| **Alerts bar** | a card holding the alert rows and the active count | mockup `.panel.alerts` |

⛔ **No right action rail** — `slitter-traveler-landing` has one; flat wire does not.

`ChangeDetectionStrategy.OnPush`. Static placeholder data — **no API**, which is `FW-132`'s.

⚠ **Default load.** `slitter-interface` needs no default child because the shop-floor menu navigates
to `shop-floor/slitter/traveler`. **Nothing navigates to flat wire**, so the routing module carries
`{ path: '', pathMatch: 'full', redirectTo: 'flat-wire-landing' }` as its first child — otherwise
`#/flat-wire` renders the shell with an empty outlet. `login-routing.module.ts` is the precedent.

⚠ **Keep the spec green as the layout grows.** `collectCoverageFrom` globs `*.component.ts` against
a **95 %** gate (`F-08`).

#### 1a. Colours and classes — see [`[UIC]`](../UIConventions.md), which this screen produced

**The mockup's palette is not used.** Colours come from `slitter-traveler-landing`:
`bg-semi-dark-blue` card headers and context bar · `bg-dark-subtle` chips, nav buttons and status
pills · `bg-body-secondary` page · `bg-light-grey` inner panels · `text-white` headings.

| Need | Class | Defined by |
|---|---|---|
| frame, rails, content column | `h-91` · `w-16` · `w-5` · `w-100` · `min-w-0` · `menu-sidebar` · `menu-content` · `menu-icon-btn` · `extra-big` · `bigger` | `_dynamic-classes.scss` generates `w-`/`h-`/`fs-`; the rest are in `styles.scss` |
| colours | `bg-semi-dark-blue` · `bg-light-grey` · `bg-dark-subtle` · `bg-body-secondary` | `_colors.scss`'s `$colors` map → `.bg-<name>` and `.<name>` generated for **every** entry |
| type sizes | `fs-14` · `fs-15` · `fs-5` | generated in px |
| cards, grid, progress, alerts | `card border-0 shadow-sm` · `card-header … justify-content-between` · `row g-2` / `col-4` · `progress` / `progress-bar` · `alert alert-warning` / `alert-success` | Bootstrap 5.3.8 |
| status dots | `<i class="fa-solid fa-circle text-success">`, plus `live-dot` when it stands for something live | Font Awesome for the dot — ⚠ **deliberately an icon, not a CSS dot**; only its **pulse** needed a class ([`[UIC §3.18]`](../UIConventions.md)) |

✅ **No `.scss` file exists in `projects/flat-wire`.** Verified after the build. The classes the
screen did create — `.floating-popover`, `.floating-popover-scale` and `.live-dot` — live in
`src/styles/styles.scss` under generic names
([`[UIC §2]`](../UIConventions.md)); everything else on the screen is an existing class.

**Card header idiom** — [`scaffold-form §8`](../../../../UALUADEV/.claude/commands/scaffold-form.md)'s
structure with `slitter-traveler-landing`'s colours: a `badge bg-secondary p-2` holding a `fa-2x`
icon, then a `fw-bold fs-5 text-white` title. ⛔ **Only the card layout from that command — none of
its modal layout.**

⚠ **Two traps from §2 bite here.** Bootstrap's `.badge` renders at **10.5 px** in this app, so every
badge carrying text takes an explicit `fs-14`; and the app forces every `.btn` to `#343a40` with a
cyan hover icon (`!important`), so nav buttons carry `bg-dark-subtle`, as slitter's do.

#### 1b. ⚠ Prefer `:host` over `ViewEncapsulation.None`, and know why the spec offers both

`[CMP §7.4]` says *"`ViewEncapsulation.None` or `:host` scoping **so the tokens resolve**."* ⚠ **The
stated reason does not hold, and picking the first option because of it is actively harmful.** CSS
custom properties are **inherited** — Angular's emulated encapsulation adds attribute selectors to
component styles and creates no style boundary, and custom properties pierce even a real shadow
boundary by design. **`var(--color-*)` resolves under the default encapsulation with nothing switched
off.**

⚠ **What `ViewEncapsulation.None` would actually do here is leak step 2c's 138 bare global selectors
out of the shell into every UAL module in the app.** Use **`:host`**, which `[CMP §7.4]` permits
equally. **This plan does not overrule `[CMP]`** — the reason attached to the choice is raised for it
to correct; the choice itself is already this plan's to make.

### Step 2 — styling: use what the application already has

**No stylesheet is shipped and no new styles are created.** Flat wire uses the **existing
application styles** — Bootstrap 5.3.8, `src/styles/styles.scss` and its `_colors.scss` SCSS
variables, and `shared`'s components. **A new style or class is written only when a requirement
calls for one**, and then as a component `.scss` beside its component.

⛔ **The mockups' `flat-wire-shopfloor.styles.scss` is not copied into the repository**, so there is
no `src/lib/styles/` folder and no entry in the app `styles` array. ⚠ **If a requirement ever asks
for a global sheet**, `planning-styles.scss` is the pattern — one of four library sheets already in
that array — and these four measurements apply the moment it lands:

| Measured on that stylesheet | Consequence |
|---|---|
| It carries **no `/* stylelint-disable */`** and yields **856** stylelint errors | ⛔ **Prepend the comment before the first lint run.** `npm run lint:styles` runs with **`--fix`**, so one un-disabled run rewrites the file **29,788 → 30,406 bytes** and still leaves **64** errors (55 `color-no-hex`). With the comment: **zero**, file untouched |
| It is **14 sections**, not a token file — §1 is tokens, §2–§14 are base component styles, §13 is the retired progress ring | Consuming it wholesale imports far more than a palette |
| §2/§3 **hard-code a 1280 canvas** | Directly contradicts `F-14`'s 1920 × 1080 |
| It declares **138 bare top-level class selectors**, including **`.btn`**, `.panel`, `.field`, `.input`, `.header`, `.footer` | ⛔ Registered globally in an app that `@use`s **Bootstrap 5.3.8** and hosts 30 other libraries, **`.btn` collides with Bootstrap's own**. The precedent `shop-floor-common.styles.scss` declares **exactly one** bare selector |

⚠ **`--color-*` is defined nowhere in this repository** — the application's colour system is the
SCSS variables in `src/styles/_colors.scss` (`$crimson`, `$green`, `$navy-blue` …), consumed as
`@use 'colors' as colors;`. Those tokens exist only in the mockups' stylesheet.

### Step 3 — global-CSS containment

⚠ **This is a real step, and skipping it produces a bug that looks like "the mockup was wrong."**

The host `src/styles/styles.scss` sets:

```scss
body { font-family: Verdana, ...; font-size: 12px !important; }
html, body { text-align: center; }
```

and Bootstrap 5.3.8's reboot is global. A **direct** declaration on the shell root beats an
**inherited** one — including an inherited `!important` — so the shell re-declares font family, size
and text alignment for its own subtree. **Verify in the browser, not by reasoning.**

### Step 4 — the 14 px floor

Set `input, select, textarea, button, option` **explicitly**. Form controls do not inherit the body
font and the browser default is **13.333 px** (`[VAL §7.5]`).

### Step 5 — dark mode

`@media (prefers-color-scheme: dark)`, which the stylesheet already implements. Verify **every**
`--color-*` group resolves in both schemes.

### Step 6 — keep component styles small

⚠ **`anyComponentStyle` is a 10 kB *error*, not a warning** — a component `.scss` past it **fails the
production build**. Shared bulk belongs in the library stylesheet from step 2, never in a component.

### Step 7 — two lint traps the reverted `FW-N03` build hit, so you do not have to

Both are `eslint` rules on the TypeScript, and both cost time on first contact. Measured during the
28 Aug 2026 build that was **reverted on 31 Aug 2026** — they are properties of the repository's
lint configuration, not of that build, so they still bite:

1. ⛔ **Never put a `/** … */` JSDoc block immediately above `@Component` or `@NgModule`.**
   `@stylistic/padding-line-between-statements` forbids the blank line that `jsdoc/lines-before-block`
   requires, so the two rules are **circular** — `ng lint --fix` reports
   `ESLintCircularFixesWarning` and leaves an error whichever way it goes.
2. ✅ **Use `/* … */` block comments there instead.** `multiline-comment-style` separately forbids
   consecutive `//` lines, so **a block comment is the only form that passes.** JSDoc on *methods* is
   still required (`jsdoc/require-jsdoc: error`) and is fine — the conflict is specific to a doc block
   sitting between a statement and a decorator.

---

## 4. Verification

```bash
ng build shared                            # MANDATORY FIRST - jest resolves `shared` to dist/, not source
npm run build                              # compiles flat-wire from source via the wrapper
ng lint flat-wire
npm run test:flat-wire                     # 95 % gate - currently 25 tests at 100 % on all four
grep -r '\-\-fw-' projects/flat-wire       # must return nothing
npm start                                  # #/flat-wire, both colour schemes
```

⚠ **The browser half of this story cannot be skipped.** The canvas, dark mode and the 14 px
computed-style checks have **no compile-time equivalent** — they are only observable in a browser,
which makes `npm start` part of this story's verification and not an optional extra.

🟡 **Where the browser half stands:** the layout, the 1920 × 1080 canvas and the standards review are
done (§1.1). ⛔ **Dark mode is still unverified**, and the 14 px computed-style check has not been
run against a live browser — those two are what remain of this block.

⚠ **Use `npm run build`, not `ng build flat-wire`** — the library has no bundle of its own; the app
build compiles it from source through its wrapper. `npm run build` runs `build:base` first, which
matters: skipping it once produced a misleading failure where `dist/shared` was stale from 23 Jul and
`build:shop-floor` broke inside `shop-floor-common`, **looking like flat wire breaking the chain**
([`FW-N03 §6.4`](FW-N03.md)).

| AC | Proof |
|---|---|
| 1 · shell regions | header, sidebar and alert slot render; the slot accepts projected content |
| 2 · canvas + both schemes | renders at **1920 × 1080** (`F-14`) in light and dark |
| 3 · no `--fw-*` | the grep above returns nothing |
| 4 · 14 px floor | computed style on an `<input>`, a `<select>` and a `<button>` is ≥ 14 px |

**Test cases:** ⛔ **None.** Phase 1A has **no test cases in `[TCS]`** — measured 28 Aug 2026 against its **405 defined cases** (the ids run to `TC-799`, but 47 are cited and never defined), nothing covers the Angular library, the shell, the canvas, the guards or the mock hub. This story's verification is this plan plus Jest, and nothing else. → `[TCS]`, `[P1A §6.13]`

⚠ **Exit criterion 2 of `phase-01a` still says 1280 × 1024.** Verify against **1920 × 1080** and
record the discrepancy rather than the old figure — `[P1A §6.10]`.

---

## 5. Blockers and open items

| Item | Effect |
|---|---|
| `G23` | ✅ **Decided — 1920 × 1080** (`F-14`). ⚠ The register row still reads 1280×1024 and is still `Open` |
| `G18` | ⚠ **a trap, not a gate.** `--fw-*` is retired; there is nothing to migrate. If it resurfaces from an older commit it is wrong |
| ⛔ **the panel diagonal** | unrecorded. The 14 px floor rests on it — `[P1A §6.10]` |
| ⛔ **no owner for scale-to-fit** | affects reviewing this story's own output — `[P1A §6.3]` |
| ⚠ **the rate-card basis does not describe the work** | 16 h is a discounted 20 h priced as *"the token system is consumed, not authored"*, and **neither happens** — no sheet is consumed and none is authored. **Flagged, not re-estimated** — `[CE]` owns the figure and this plan restates no hours |

---

## 6. Handoff

**This story unblocks `FW-133` and `FW-134`** — both depend on it *for the tokens only*
(`[TB §7]`: *"Dependencies: FW-130 (tokens)"*). Tell those developers:

1. **There is no token system to consume.** `--color-*` is defined nowhere in the repository and no stylesheet ships it — use the application's `src/styles/_colors.scss` SCSS variables. `--fw-*` does not exist either (`G18`).
2. **Component styles stay under 10 kB** or the production build fails.
3. **The canvas is 1920 × 1080 and the width is where the room is** — compose in columns.
4. **Nothing global is registered**, so `FW-133`/`FW-134` inherit no `.btn` and collide with nothing.
5. ⚠ **What you actually observed in the browser.** Dark mode and the 14 px floor must be **observed**, not asserted from source.
