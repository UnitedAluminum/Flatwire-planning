---
id: FW-204
legacy_id:
title: Minimal landing route — the entry point when Dashboard 1 is out of scope
status: not-started
status_confirmed: true
status_note: "⚠ **`/flat-wire` now resolves, but not to this story's screen** — it redirects to `#/flat-wire/flat-wire-landing`, which is **DB3** (`FW-062`). The two-tile line picker this story owns does not exist. ⚠ **Its retirement condition also needs re-deciding**: DB1 now exists as a skeleton (`FW-060`), so whether the trial still needs a separate front door is an open question, not an automatic yes"
owner:
jira:
mvp: 1
phase: "3"
stream: FE
streams: [FE]
priority: high
hours: 8
sprint: S1
depends_on: [FW-N03, FW-130, FW-164]
blocked_by: []
has_plan: false
started:
completed:
---

# FW-204 - Minimal landing route — the entry point when Dashboard 1 is out of scope

> **No implementation plan has been written for this story yet.**
> The card below is the contract from `[TB]`. Before starting, replace this notice
> with the sections in the task template: *What to build* / *Context you need* /
> *Build order* / *Decisions made here* / *Verification* / *Handoff*.

## 1. What to build

**Hours:** 8 h FE · **Priority:** High · **Sprint:** S1 · **Phase:** 3 · **Stream:** FE

> **New 14 Aug 2026 for the six-screen trial run** ([`TrialRunPlan.md`](TrialRunPlan.md) §1.5). **Dashboard 1 was
> removed from trial scope on client direction**, and it was the target of **every** mockup's back button plus
> Dashboard 5's Cancel — so the trial had no entry point and no way back into a running line. This is the smallest
> thing that closes that hole. It is **not** a reduced Dashboard 1: no live panels, no alert bar, no payoff bars.
> `FW-060` is unaffected and still owns the real board. Hours are **additional to `[CE §3b]`**.

**As an** operator arriving at a terminal,
**I want** to pick my line and land on the right screen for its current state,
**So that** I can start work without knowing which screen to open.

**Acceptance Criteria:**
- [ ] Route `/flat-wire` — two tiles, **FL1** and **FL2**, each showing line name and current state only — ⚠ **the route resolves, but to DB3.** `flat-wire-routing.module.ts` carries `{ path: '', pathMatch: 'full', redirectTo: 'flat-wire-landing' }`, and `flat-wire-landing.component` is the **Active Run Monitor**. This story's tiles do not exist, and building them means **changing where `/flat-wire` lands**
- [ ] **Idle** routes to that line's check-in screen — Dashboard 2 for FL1, Dashboard 5 for FL2. **Running** routes to Dashboard 3 in that line's mode
- [ ] State is read from **`GET /run/active?line=`** (`FW-164`, already in scope). ⚠ **Adds no endpoint** — in particular it must **not** pull in `GET /lines/status` (`FW-154`), which left trial scope with Dashboard 1
- [ ] Becomes the target of every screen's back control and of Dashboard 5's Cancel, **replacing `dashboard_1_line_status.html`** for the trial
- [ ] Carries the `FR-119` / `NFR006` reconnect banner over cached state like every other screen — it is a route in the shell, not an exception to it
- [ ] ⚠ **Retired, not extended, when `FW-060` lands.** Dashboard 1 is the entry point in full MVP-1; leaving both in place gives the operator two front doors — ⚠ **re-decide this before building.** `FW-060`'s DB1 already renders as `supervisor-dashboard.component`, so the condition that retires this story is partly met and building the tiles now may be redundant work

**Rate-card basis:** screen variant @ **8 h** (§2) — two tiles and a state-conditional route, well under a new screen's 24 h. **5 h AI-assisted** at `[DE §1]`'s 0.62 FE factor, which is the figure `[TRP §4]` schedules
**Dependencies:** FW-N03, FW-130, FW-164
**Blockers:** —

---
