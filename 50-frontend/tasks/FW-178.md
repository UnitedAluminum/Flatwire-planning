---
id: FW-178
legacy_id:
title: Active Run — the FL2 material-flow card and Spool Information grid
status: not-started
status_confirmed: true
status_note: "⬜ **Not started — 8 h, and the real deliverable is an empty state.** ⛔ **RESCOPED 9 Sep 2026 — the empty state this story existed to build is retired.** It read *"FL2 broadcasts `null` live gauge and width by design (`[SIG §5.3]`, `FR-120`), so this variant owes an explicit no live trace state plus the historical profile in inline SVG."* **FL2 now broadcasts live gauge and width at 4 s** (`A3` retired, `FR-120` superseded, `[SIG §5.3]` withdrawn), so there is **no *no live trace* state and no inline-SVG profile row** — FL2 renders the same live trace component as FL1 and FL3 (`D-55`). ✅ **What survives is smaller and still needed:** a **no-measurement** state for a dropped feed on **any** line, which must never draw a flat line at target. ⛔ **Treating the absence of `GaugeReading` as a fault is a defect**, not a fallback. ⚠ It depends on `FW-162` and `FW-163`, **both of which already render as inline markup on DB3** and are owed as controls — so this story inherits whichever shape they land in. ⛔ The null-gauge contract itself is `FW-181` (not started)."
owner:
jira:
mvp: 1
phase: "8"
stream: FE
streams: [FE]
priority: high
hours: 8
sprint: S3
depends_on: [FW-062, FW-162, FW-163, FW-070]
blocked_by: [OQ-18]
has_plan: false
started:
completed:
---

# FW-178 - Active Run — the FL2 material-flow card and Spool Information grid

> ### ⭐ Narrowed 7 September 2026 by `D-55`
>
> **Four of the six criteria below dissolve, and none of them was really FL2-specific:**
> the **Live/Profile toggle** (the trace treatment is now uniform on all three lines), the
> **Order Information weight field** (one shared column set for all three, `Q3`), the **three FM2
> rows with edgers on S2/S3** (that is **pass-schedule data**, not a screen variant — the same card
> shows draw boxes on FL1 without a line branch) and the **info-grid chrome** (shared).
>
> ✅ **What remains is genuinely FL2, and it is two things:** the **material-flow card's content
> model** — spool in / coil out with skid and coil-of-N, which is a different *shape* from two
> payoff rows rather than different labels on the same one — and the **Spool Information** grid
> subject. Both are rows 1 and 2 of `D-55`'s four-row profile.
>
> ⚠ **The null-gauge half moves to `FW-181`** as a rule on the shared trace path.
> ⛔ **Do not build an FL2 copy of the screen.** `FW-062` owns one component for three lines.

> **No implementation plan has been written for this story yet.**
> The card below is the contract from `[TB]`. Before starting, replace this notice
> with the sections in the task template: *What to build* / *Context you need* /
> *Build order* / *Decisions made here* / *Verification* / *Handoff*.

## 1. What to build

**Hours:** 8 h FE · **Priority:** High · **Sprint:** S3 · **Phase:** 8 · **Stream:** FE

**As an** FL2 operator,
**I want** the run monitor to show spool-in and coil-out rather than payoffs,
**So that** the screen matches what my line actually does.

**Acceptance Criteria:**
- [ ] **Configures Phase 5's shell — does not reimplement it.** The differences are enumerable and are configuration, not a fork:
  - Middle status card → **Material flow** (spool in: alpha, source rods, load time, consumption bar, lb remaining/consumed · coil out: output alpha, take-up, fill bar, lb, ft/ft target, skid + coil-of-N badge)
  - Info grid subject → **Spool Information** (+ a width column FL1's omits, because material arriving here is already flattened)
  - Order Information → `Max Wgt of Spool` replaced by **`Coil Min–Max Wgt`**, the completion basis
  - Trace source → **Live / Profile toggle**, **Profile authoritative**
  - Action clusters → **no Die Change, no Weld**; **SPC Checkpoint is present** (FM2's S3 output is a specified checkpoint site)
- [ ] Components card shows **exactly three** FM2 rows with edgers on S2 and S3 only, and **no separate "8″ Roller" row**
- [ ] Check Out Rod disabled on a running line; **Complete Coil** is the only true navigation left
- [ ] Raises `fw-spc-checkpoint-dialog`, `fw-wip-rejection-dialog`, `fw-pause-dialog`/`fw-resume-dialog` and **`fw-roll-adjust-dialog`** (Phase 6), supplying **FL2's stand set**

**Rate-card basis:** screen variant of an existing screen 8 h (§2)
**Dependencies:** FW-062, FW-162, FW-163, FW-070
**Blockers:** **OQ-18** *(which order field carries the coil min–max weight range)*

---
