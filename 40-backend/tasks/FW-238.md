---
id: FW-238
legacy_id:
title: Register flat wire with OPCConnection
status: in-progress
status_confirmed: true
status_note: "⚠ **Scripts written and complete. The two schema-only scripts have RUN; the two that write rows have NOT.** ⛔ **`11_` is now technically runnable — every guard passes and §4a binds — but must not be run until `FW-236`/`G94` merges** (`P-319`). `11_` seeds four `CommonDB` tables — `OPCModules` (id 6, `ConnectionType` 21317), `OPCServerApplicationMapping` (4 rows), `OPCTags` (41 = 39 data + 2 system-error) and `OPCTagApplicationMapping` (41) — for FL1 and FL2 only, and **reads** `OPCServers`, which is a lookup it never writes (`D-48`). The endpoints are the pair at `OPCServersIdx` **1 and 2**, both lines onto both, addressed **by id** — on `DEV00164-001` those ids are `UAOPC3`/`UAOPC4`, not `UAOPC1`/`UAOPC2` (`D-49`). ✅ **`G100` IS CLEARED on `DEV00164-001`** — `dbo.OPCModules` was behind `ual-database` (4 columns against 6); the two were added by hand on 5 Sep 2026 and captured as `08_CommonDB_OPCModules_ColumnDrift.sql`, and `11_` §4a now binds clean. `G100` stays **Open** on its other half — the deployed `GetOPCServerAndTagDetails` — which does **not** block this story, because flat wire writes through `WriteTag`, not the SignalR hub. ⛔ **ONE gate is left, and it is not this story's artefact to fix:** `G94`/`FW-236`, which must **merge before activation** or every push writes to namespace 0 reporting 200. ⚠ Closing this story does **not** turn the write path on — the simulate flags stay `true` (`P-320`). ⚠ FL3 is deliberately unregistered (`D-47`/`G99`). ✅ **Two of the four scripts have now been RUN on `DEV00164-001`.** `09_CommonDB_OPCTables_Constraints.sql` — the five OPC tables were pure heaps (no PK, no unique index, no FK), which is why every idempotency guard in `11_` is hand-written — is applied, all 18 objects measured present. `08_CommonDB_OPCModules_ColumnDrift.sql` is applied too, and is idempotent. ⛔ **`10_` and `11_` remain unrun.** ⚠ **`11_`'s drift guard was rewritten, not just re-sited: as first written it could never fire**, sharing a batch with §4a where column names bind at batch compile time. It is now §0, in a batch of its own, and proven to abort before the seed batch is sent."
owner:
jira:
mvp: 1
phase: "14"
stream: BE
streams: [BE, DB]
priority: critical
hours: 12
sprint: S3
depends_on: [FW-003, FW-144, FW-151, FW-241]
blocked_by: [G60, G94]
has_plan: true
started:
completed:
---

# FW-238 · Register flat wire with `OPCConnection`

**Project:** United Aluminum (UAL) — Flat Wire Mill Module
**Last Updated:** September 5, 2026 (third pass) — ➕ **A fourth script, [`08_CommonDB_OPCModules_ColumnDrift.sql`](../../30-database/scripts/08_CommonDB_OPCModules_ColumnDrift.sql), and `G100` is off this story's `blocked_by`.** `dbo.OPCModules` on `DEV00164-001` carried four columns against `ual-database`'s six; the two were added there **by hand** and `08_` is the record of that change — guarded per column, idempotent, **writes no row**. `11_` §4a now binds clean, measured with a `SET NOEXEC ON` compile pass returning zero messages. ✅ **`08_` and `09_` have both been RUN on that instance**; `10_` and `11_` have not. ⛔ **`11_` is now gated on `FW-236`/`G94` alone.** ⚠ **`11_`'s drift guard was rewritten, not merely re-sited — as first written it could never fire**, sharing a batch with §4a where column names bind at *batch compile* time; it is now §0, in a batch of its own, and proven to abort before the seed batch is sent. ⛔ **`G100` stays Open on its other half**, and the fix created a new hazard: adding the columns removed the interlock that made a `GetOPCServerAndTagDetails` refresh impossible, while `OPCModulesIdx` 1-4 are `NULL` and `OPCInfo` maps both as non-nullable `int` — **backfill before refreshing**, or four other modules throw. *(previously September 5, 2026 (second pass) — ➕ **A third script, [`09_CommonDB_OPCTables_Constraints.sql`](../../30-database/scripts/09_CommonDB_OPCTables_Constraints.sql), and it is the only part of this story that can be run today.** The five `CommonDB` OPC tables are pure **heaps** — no PK, no unique index, no FK, no check constraint — while their `united_db` predecessors carry PKs and two FKs, lost in the migration and never re-created. ⛔ **Every hand-written idempotency guard in `11_`, and three of its eight fault queries, exist only because of that** (new §2.1c). `09_` restores 5 PKs, 6 uniques, 4 FKs, 1 check and 2 indexes, writing **no row**; it is new §3 step `7a` and `[DEP §4.2]` step `1a`. ✅ **`G100` does not gate it.** *(previously — **current state, not a history.** `G97` is **resolved** (`D-49` — endpoint pair `OPCServersIdx` 1 and 2, both lines onto both), `OPCServers` is a **lookup** this story never writes (`D-48`), and `11_` carries no placeholder. ⛔ **Three gates remain — `G100`, `G94`/`FW-236` and `FW-241`'s sign-off — and nothing has been run.** ⚠ *The four nested `previously` blocks this header used to carry were an in-document change log; removed 5 Sep 2026, and the passes they recorded are in [`CHANGELOG.md`](../../CHANGELOG.md).*)*)*
**Document Type:** Implementation plan for a single backlog story
**Status:** ⚠ **Scripts delivered and complete. `08_` and `09_` have been RUN on `DEV00164-001`; `10_` and `11_` have not** (§3 steps `7a`/`7b`, §2.1c). The DB 8 h is spent and ✅ **`G97` is answered (`D-49`), so `11_` carries no placeholder** — the BE 4 h is now waiting on the environment, not on a person. ✅ **`G100` is cleared on `DEV00164-001`** — `dbo.OPCModules` carries all six columns and `11_` §4a binds clean. **Two gates left, neither this story's own artefact to fix** — `FW-236`/`G94` (the identity fix a `21317` module needs, which must merge **before** activation) · `FW-241` (the shared-reference-data **sign-off** — ⚠ its `machines` rows already exist on `DEV00164-001`, §5.2)
**Owner:** Backend (BE) and Database (DB) streams — 4 h and 8 h of one card
**Audience:** The developer executing `FW-238`, and whoever owns `CommonDB` provisioning (`G100`'s remaining half — the stale `GetOPCServerAndTagDetails` and the backfill it needs — is theirs, not this story's)
**Shortcode:** — *(implementation plan, derived from the specifications and the scripts; **not citable as a requirement**)*
**Part of:** `40-backend/tasks/` — index: [Orchestration.md](Orchestration.md)

---

> **Why this document exists.** The story is unusual in three ways, and each one changes how it
> should be worked.
>
> **The deliverable is already written.** `11_` and `16_` are authored, complete and in the folder
> manifest, and no answer is outstanding from anyone. What is *not* written is the sequence: the
> remaining work is **running things in a fixed order against an environment that is not ready**,
> and getting that order wrong produces silent failure rather than an error (§3).
>
> **Second: everything here fails silently.** ⛔ A line with no system-error tag row answers
> **empty**, indistinguishable from not being registered at all (`G95`). A `machines` row in any
> status but `1` vanishes the same way. A module on `ConnectionType` 21317 before `FW-236` merges
> writes to namespace **0** and reports `200` (`G94`, `G58`). **Not one of these three raises an
> error anywhere.** The pre-flight guards and the §5 verification block in `11_` exist because of
> that, and they are the story's real content.
>
> **Third, and it is the one to hold on to:** ⚠ **finishing this story does not turn the write path
> on.** `SimulatePLCTagPush` stays `true`, `SimulateOpcFeed` stays `true`, and `G59` still has no
> service identity for a background write. `FW-238` makes the resolve-and-write path *possible*;
> it does not make it *live* (`P-320`).

---

## 1. What to build

**Hours:** 12 h (BE 4 · DB 8) · **Priority:** Critical · **Sprint:** S3 · **Phase:** 14 · **Stream:** BE + DB

> ⛔ **`G60` — nothing registers flat wire with `OPCConnection`, so `GetOPCInfo` cannot succeed and
> no tag can ever be written.** Two halves are missing: `UAL.Constants.GlobalConstants.OPCModules`
> has five members and **no flat wire one**, while `GetConfigurationsQueryValidator` requires one;
> and `CommonDB` has no flat wire rows in `OPCModules` / `OPCServers` / `OPCTags` /
> `OPCTagApplicationMapping`. `MachineId`, `OPCServers`, `ConnectionType` and `IsReadonly` are
> `CommonDB` state and **cannot be hand-built** (`P-104`).
>
> ⚠ **Not a build blocker today** — simulate short-circuits before the resolve, and `FW-N05` and
> `FW-151` both reproduce `G60` by name at boot and stay up. But the resolve-and-write path is
> **dead code until this lands**.
>
> ✅ **`OI-A` is decided — `D-44`, 4 Sep 2026: the tag paths live in the `CommonDB` registration,
> not in `appsettings`.** This story therefore owns the `OPCTags` rows outright, and the
> *"decide `OI-A` first or the rows get written twice"* caveat is discharged.
>
> ✅ **`G93` IS WITHDRAWN — `D-45`, 5 Sep 2026. There is no `TagKey` column and none is needed.**
> Flat wire registers in `OPCTags` in the same shape as every other module: `TagName` only. The
> logical-name layer stays where `FW-144` already put it — `TagNames.cs` plus the `appsettings` map
> behind the **unchanged** `ITagPathResolver` seam — which is what the ecosystem does anyway
> (`UnifiedSlitterService` addresses **313** tags off this schema with no logical-name column).
> **No shared `ALTER`, no other team, off the Phase-14 critical path.**
>
> ⛔ **A different prerequisite replaces it: `FW-236` / `G94` must merge first.**
> `ConnectionType = 21317` makes flat wire the **first module ever to select `ual-api`'s
> `OPCUAManager`** — `G94` measured all four existing `OPCModules` rows as `21316` (OPCDA), so that
> code path has never run in production. Until `G94`'s identity fix lands, `ReadTag` and `WriteTag`
> address namespace **0** — a different node — and every push writes nowhere.
>
> ✅ **The endpoint question is settled, and it settled into a shape the question did not have.**
> `OPCServers` is a **lookup** this story never writes (`D-48`), and the lines connect through the
> **pair** at `OPCServersIdx` **1 and 2** with **both lines mapped to both** (`D-49`). So the
> registration writes **four** shared tables and reads a fifth,
> [`11_`](../../30-database/scripts/11_CommonDB_Insert_OPCRegistration_FlatWire.sql) addresses
> endpoints **by id** rather than by name, and `G97` is closed. **§2.1a and §2.1b are the two
> halves**, and `P-322` / `P-323` are why each is written down rather than just done.

**Acceptance Criteria:**
- [ ] ✅ **The five `CommonDB` OPC tables carry keys** — they are pure **heaps** in source control (no PK, no unique index, no FK, no check constraint), which is the sole reason `11_`'s idempotency is hand-written (§2.1c). Delivered as [`09_`](../../30-database/scripts/09_CommonDB_OPCTables_Constraints.sql): **5 PKs · 6 unique constraints · 4 FKs · 1 check · 2 indexes**, writing no row. ⛔ **Run it before `11_`** — that is what turns `11_`'s guards from load-bearing into redundant. ⚠ **No FK on `Machineidx`** — `dbo.machines` is a cross-database view and the rule is not expressible
- [ ] `CommonDB` rows for **FL1 and FL2 only** in **four** tables — `OPCModules`, `OPCServerApplicationMapping`, `OPCTags` and `OPCTagApplicationMapping` — as a **numbered, reversible script** in `30-database/scripts/` — delivered as [`11_`](../../30-database/scripts/11_CommonDB_Insert_OPCRegistration_FlatWire.sql) with reverse [`16_`](../../30-database/scripts/16_CommonDB_Delete_OPCRegistration_FlatWire.sql)
- [ ] ⛔ **`OPCServers` is a LOOKUP table and takes no `INSERT` and no `UPDATE`** (`D-48`). `11_` reads it and aborts — printing the endpoints that do exist — if either id is missing; `16_` deletes nothing there. ⚠ A missing endpoint is a request to whoever owns `CommonDB`'s OPC estate, **never** a reason to add an `INSERT` to `11_`
- [ ] **The endpoint pair is `OPCServersIdx` 1 and 2, addressed BY ID** (`D-49`), giving **4** `OPCServerApplicationMapping` rows — each line on **both**, `ConnectionSequence` 1 and 2. ⛔ **Not by name**: ids 1 and 2 are `UAOPC3`/`UAOPC4`, and `UAOPC1`/`UAOPC2` are ids 3 and 4 — a different pair serving three other modules. ⛔ **Not two rows**: all 37 existing `(module, machine)` pairs carry exactly two endpoints, and one per line looks healthy until the primary drops (§2.1b)
- [ ] ⚠ **`OPCServersIdx` re-checked per environment**, exactly as the module id is (`G96`) — the numbering is an artefact of insertion order, not a convention, so the right numbers elsewhere can be the wrong pair
- [ ] **41 `OPCTags` rows**: 39 data (FL1 17 · FL2 22) + **2 system-error** (`G95`). ⚠ **Do not reconcile 41 against `FW-144 §8.2`'s 72 bound paths** — both are correct and count different things: 72 is the `appsettings` map across three lines, 41 is what has a machine to attach to
- [ ] ⛔ **The two system-error rows are not optional** — `GetOPCServerAndTagDetails`'s first result set INNER JOINs `IsSystemErrorTag = 1`, so without them `GetOPCInfo` returns an **empty list** and the whole line is invisible, a symptom identical to `G60` itself (`G95`)
- [ ] ⛔ **FL3 (machine 127) is NOT registered** — `D-47` / `G99`. It has no controller of its own, so its tags *are* FL1's and FL2's. `GetOPCInfo` answering empty for FL3 is **correct**, and `11_`'s verification block asserts 127 is absent. Do not "fix" it by seeding 127
- [ ] **`OPCModuleId = 6`**, forced with `SET IDENTITY_INSERT` and a pre-flight abort if occupied — **not `5`**, which the six slitters use in production even though the enum stops at 4 (`G96`). Re-check the free id per environment
- [ ] **`ConnectionType = 21317`** (`Lookups.OPCUA`), and `IsReadOnly` / `OPCEventType` / `EventDurationSeconds` all set **non-NULL** — they are nullable in the table but non-nullable on the `OPCInfo` DTO, so a NULL throws at `GetOPCInfo`
- [ ] Every path stored as a **bare** path in the `.PLC.` form (`D-46`) — `FL1.PLC.AGC.Gauge`, never `ns=2;s=…`. `OPCConnection` applies the node-id form at the QuickOPC boundary, and `G94` measured 0 of 496 existing rows carrying a prefix
- [ ] The 39 registered data paths **equal** FL1's and FL2's `appsettings` values exactly — the config-vs-rows diff in `[DEP §5]`. `D-45` accepts that the strings live in two places; this is what keeps them in step
- [ ] Sequenced with `FW-241`: both write shared reference data, and the `machines` rows `FW-003` creates are what an OPC registration keys on. ⚠ Read the ids from `[DEP §4.2]` step 2, not by re-typing them
- [ ] `GetOPCInfo` returns a usable `OPCInfo` for **FL1 and FL2**, verified against the real service — this is what retires `G60` in `FW-151`'s and `FW-N05`'s boot logs
- [ ] ⚠ `G33`, `G32` and the **namespace-index** follow-up still have to be settled in the same commissioning window; this story closes none of them. ⚠ **The namespace index travelled with `G97` and is not closed by it** (`11_` `D8`) — `G97` closing is exactly when it gets forgotten

~~- [ ] A flat wire member added to `UAL.Constants.GlobalConstants.OPCModules`~~ — **struck 5 Sep 2026.** Every `OPCModules` reference in the FlatWire codebase is a comment or a log string; no line evaluates `(int)GlobalConstants.OPCModules.FlatWire`, and `ResolveOpcInfoAsync` sends `options.OpcModuleId`, a plain config int. `ual-window-service` does not reference the enum at all. Adding `Slitter = 5` and `FlatWire = 6` is **worth doing so the enum stops lying, but it gates nothing** and is not this story's blocker.
~~- [ ] `ConfigurationTagPathResolver` replaced by a `CommonDbTagPathResolver`~~ — **struck by `D-45`.** Without a logical-name column nothing in the table can map `Gauge` to a path, so the resolver stays as built and the seam is untouched. `FW-151` keeps overwriting `GetOPCInfo`'s `Tags`.
~~- [ ] `appsettings` holds no tag path; a `grep` returns zero hits~~ — **struck by `D-45`.** `appsettings` **is** the resolution map. The 72 entries stay.
~~- [ ] `G93` must land first — ship the column before the rows~~ — **struck: `G93` is withdrawn.**

**Rate-card basis (§2):** four-table shared registration script with a reverse script, priced as a stored-proc-class deliverable @ 8 h (DB 8) · configuration and verification (BE 4) = **12 h**. ⚠ **Re-confirmed 5 Sep 2026 and left at 12 h.** `D-45` removed the `CommonDbTagPathResolver` that would have grown it, and the row count fell from a notional 72 to 41 because FL3 is not registered — so the two movements since `D-44` cancel out. The `Hours` cell feeds three client `.xlsx` generators and the capacity totals are quoted in ~20 files, so a re-price belongs in an additive pass that owns the arithmetic, not in this edit. ⚠ **`09_` does NOT move the number either, and that is a decision rather than an oversight.** It is a third script and real DB work, but it is *schema hardening of tables this story merely uses* — the same 8 h that priced a four-table registration with a reverse script absorbs it. ⛔ **If it is ever to be priced separately it belongs on its own card, not added here**, for exactly the propagation reason above.
**Dependencies:** FW-003, FW-144, FW-151, FW-241
**Blockers:** ✅ **`OI-A` decided (`D-44`) and `G93` WITHDRAWN (`D-45`)** — no shared-schema change and no other team. ⛔ **`G94` — `FW-236`'s OPC UA identity fix must merge before a `21317` module is activated** *(written but uncommitted on `feature/UADEV-23146`; `FW-236` is still `not-started` and needs `OPCConnection`'s owner to accept a response-shape change)*. ✅ **`G97` RESOLVED (`D-49`)** — `OPCServersIdx` 1 and 2, both lines onto both; `11_` has no placeholder left. ⛔ **`G100` is now what stops `11_` running**: the target `CommonDB` is behind source control (`§7`) · shared reference-data sign-off, as for `FW-241`

---

## 2. Context you need

### 2.1 Five tables, four of them written, and the order between them is the whole design

`11_` touches five `CommonDB` tables in dependency order, because each one is keyed by something the
previous one produces. ⚠ **Only four are written** — `OPCServers` is a **lookup** and is read
(`D-48`, §2.1a). The counts below are the script's, and `11_`'s §5 verification block asserts every
one of them.

| # | Table | Rows this story writes | Keyed by |
|---|---|---|---|
| 1 | `OPCModules` | **1** — `OPCModulesIdx` **6**, `ModuleName` `FlatWire` | Nothing. The id is *forced*, not allocated (§2.4) |
| 2 | `OPCServers` | **0 — READ ONLY.** Two existing endpoints are read, never written | ✅ `OPCServersIdx` **1 and 2** — `G97`, answered by `D-49` |
| 3 | `OPCServerApplicationMapping` | **4** — each line onto **both** endpoints, `ConnectionSequence` 1 and 2 (§2.1b) | the module, the two servers, and `machines` 125 / 126 |
| 4 | `OPCTags` | **41** — 39 data + **2 system-error** | `TagName`, unique because every path begins `FL1.PLC.` or `FL2.PLC.` |
| 5 | `OPCTagApplicationMapping` | **41** — **18** on machine 125, **23** on 126 | tag × machine × module |

⛔ **The order was enforced by the script and by nothing else — and as of `09_` that is no longer
true.** As written, `OPCTagsIdx` carried **no foreign key anywhere in source control**, `OPCTags` had
no primary key and no unique index, and `OPCServersIdx` had no foreign key either — so idempotency
here was `11_`'s own guards, not the schema's. That is also why `16_` deletes mappings **before** the
rows they point at: reversed, the mapping rows are orphaned with no error, the natural key `11_`
matches on stops resolving, and the next run **inserts duplicates instead of skipping**. It is why
`11_`'s §5 fault queries gained a **dangling-endpoint** check: nothing in the schema would notice a
mapping pointing at an endpoint that is not there.

✅ **[`09_`](../../30-database/scripts/09_CommonDB_OPCTables_Constraints.sql) closes that hole, and
it is ordered before `11_` for exactly this reason** — §2.1c. ⚠ **Read the paragraph above as the
*pre-`09_`* state**, which is still the state of every environment where `09_` has not been run. The
guards in `11_` are **not** removed by it: they stay as the belt to `09_`'s braces, and they are what
makes `11_` re-runnable on an instance that is still unconstrained.

### 2.1a ⛔ `OPCServers` is a lookup — the endpoint is chosen, not created (`D-48`)

The endpoints are **site infrastructure shared by every module that connects**, not per-module data.
`CommonDB.dbo.OPCServers` already holds `UAOPC1`–`UAOPC4`, all of class `SWToolbox.TOPServer.V6`,
and flat wire becomes **one more consumer of two of them** (§2.1b) rather than the author of a
fifth row. Four things follow, and the fourth is the one worth reading twice:

1. **`11_` performs no `INSERT` and no `UPDATE` against `OPCServers`.** §4b reads the rows and
   prints them, so the run records which endpoints it used.
2. **`11_` carries no `@OPCServerName` and no `@OPCServerClass`.** Nothing it does needs either,
   and a variable for either one invites exactly the insert this decision forbids. It addresses
   endpoints by id instead — §2.1b (a) is the second, sharper reason for that.
3. **Guards replace them**, outside the transaction: the endpoints must already exist (the abort
   **prints the ones that do**, so the reader gets the answer with the error).
4. ⛔ **`16_`'s old *"delete the endpoint if no module still maps it"* step was WRONG, not merely
   unnecessary.** An endpoint nothing currently maps is still infrastructure; deleting it on the
   strength of *our* rollback would have removed somebody else's lookup row. That step is gone.

⚠ **This does not by itself say *which* endpoint** — only that the answer has to be a row that
already exists. §2.1b is the answer, and what it turned out to mean.

### 2.1b ✅ `G97` answered — and neither half of the answer was the shape of the question (`D-49`)

**The lines use the OPC UA endpoint pair at `OPCServersIdx` 1 and 2, and both lines map to both.**
Client direction. Two things about that sentence were checked against the instance before anything
was written, and **both checks changed the script**.

**(a) The ids do not match the names — so the endpoint is addressed by id.**

| `OPCServersIdx` | Name | | `OPCServersIdx` | Name |
|---|---|---|---|---|
| **1** | **`UAOPC3`** | | 3 | `UAOPC1` |
| **2** | **`UAOPC4`** | | 4 | `UAOPC2` |

⛔ *"`OPCServersIdx` 1 and 2"* is therefore the **`UAOPC3`/`UAOPC4`** pair — **not** `UAOPC1`/
`UAOPC2`, which is what the names invite you to assume and which is **a different pair, serving
`CoilReceiving`, `Handheld Service` and `Furnace Scheduling`**. ⛔ **A name-based selector gets this wrong silently**: with an
`@OPCServerName`, *"1 and 2"* is transcribed as `UAOPC1`/`UAOPC2` and the lines are pointed at the
wrong endpoints **with no error at any layer** — the guard passes, the mapping writes, `GetOPCInfo`
answers. `11_` §1 therefore carries **ids**, because ids are what the instruction gives *and* what
`OPCServerApplicationMapping` stores; §4b prints the names so a reader can still see which rows
they are. ⚠ **Re-check per environment** (`G96`'s rule, applied to a second column): the
numbering is an artefact of insertion order, so the right numbers elsewhere may be the wrong pair.

**(b) It is a primary/failover pair, not a per-line split — so §4c writes four rows, not two.**

`11_` `D9` asked whether FL1 and FL2 sat behind *one* endpoint or *two*. That framing has no
correct answer, and the table says why:

| Measured on `DEV00164-001`, 5 Sep 2026 | |
|---|---|
| `(module, machine)` pairs in `OPCServerApplicationMapping` | **37** |
| …carrying **exactly two** endpoint rows, `ConnectionSequence` 1 and 2 | **37 — all of them** |
| …carrying one | **none** |
| `GetOPCServerAndTagDetails` result set 2 | `ORDER BY MachineId, ConnectionSequence` |

So the sequence is **consumed, not decorative**: the procedure hands the client an *ordered* server
list per machine — primary first, failover second. ⛔ **A one-row-per-line mapping would have made
flat wire the only machine in `CommonDB` with no failover, and would have looked like a working
registration right up until the primary endpoint dropped.** §4c is now a `CROSS JOIN` of the two
lines with the two endpoints, and two fault queries were added: a line without a full pair, and a
duplicated `ConnectionSequence`.

⚠ **One thing measurement could not settle, and it is left as a named assumption.** `11_` makes id
**1** the primary, taking *"1 and 2"* in the order it was given. **The precedent points the other
way**: `Hole Detection` — the only other module on this same pair — uses id **2** as its primary on
all three of its machines. Flipping the two `DECLARE`s in `11_` §1 is the entire change if that is
what is wanted; nothing else depends on which is which.

### 2.1c ✅ `09_` — the five tables are heaps, and every guard in `11_` exists because of it

**All five `CommonDB` OPC tables have no primary key, no unique constraint, no foreign key, no check
constraint and no index of any kind.** Measured on `DEV00164-001`, 5 Sep 2026:

| Table | Rows | Pages | PK | Unique | FK | Index |
|---|---|---|---|---|---|---|
| `OPCModules` | 4 | 1 | none | none | none | none |
| `OPCServers` | 4 | 1 | none | none | none | none |
| `OPCTags` | 496 | 4 | none | none | none | none |
| `OPCServerApplicationMapping` | 74 | 1 | none | none | none | none |
| `OPCTagApplicationMapping` | 882 | 5 | none | none | none | none |

⛔ **This is a migration defect, not a design choice.** The legacy `united_db` predecessors —
`OPC_Tags`, `OPC_Servers`, `OPC_Tag_Application_Mapping`, `OPC_Server_Application_Mapping` — **do**
carry primary keys and two foreign keys, under `Databases\united_db\Tables\OPC_*\Constraints\`. The
constraints were dropped when the tables were migrated into `CommonDB` and never re-created; git
history shows no key ever existed on the `CommonDB` copies.

**Read `11_`'s own header against that table and the connection is exact** — every hand-written
guard in it is standing in for a missing constraint:

| `11_` guard, in its own words | What `09_` replaces it with |
|---|---|
| *"`OPCTags`: No primary key and no unique index of any kind exists on this table in source control. Idempotency is therefore enforced by this script's own guards, not by the schema"* | `PK_OPCTags` · `UQ_OPCTags_TagName` |
| *"Guard: `ModuleName` has no unique index, so a duplicate would make the id lookup ambiguous"* | `UQ_OPCModules_ModuleName` |
| *"A duplicate (machine, module, tag) mapping. The table has no unique index to prevent it"* (fault query) | `UQ_OPCTagApplicationMapping_Module_Machine_Tag` |
| *"A duplicated `ConnectionSequence` on one line … No unique index prevents it"* (fault query 8) | `UQ_OPCServerApplicationMapping_Module_Machine_Seq` |
| *"A server mapping pointing at an endpoint that does not exist … `OPCServersIdx` carries no foreign key"* (fault query 6) | `FK_OPCServerApplicationMapping_OPCServers` |

`09_` adds **5 clustered primary keys, 6 unique constraints, 4 foreign keys, 1 check constraint and
2 indexes**. It **writes no row and alters no column** — it only `ALTER`s five tables.

⚠ **It is an integrity fix, not a performance one — and measurement after the run proved that
harder than intended.** `SET STATISTICS IO` for `GetOPCServerAndTagDetails`, same 11 machines and
module 4, across all five tables:

| | Logical reads |
|---|---|
| Before `09_` (all heaps) | **21** |
| After `09_` | **269** |

⛔ **The plan predicted "at or below 21" and was wrong — reads went up ~12×.** The cause is not a
missing index: `GetOPCServerAndTagDetails` builds `@machines` as a **table variable**, which the
optimiser estimates at **1 row**. With only heaps it had to hash-join over one scan of each tiny
table; with seekable indexes available it switches to **nested loops and repeats a seek per
machine**, and on 1–5 page tables repeated seeks cost more than a single scan.
`OPCServerApplicationMapping` went 1 → 70 reads and `OPCServers` 1 → 68.

⚠ **This does not argue for reverting anything, and no covering index would fix it** — the problem
is cardinality estimation, not access paths. In absolute terms 269 logical reads against five
fully-cached 1–5 page tables is sub-millisecond, and **the results are unchanged and still correctly
ordered** (result set 2 by `MachineId, ConnectionSequence` — verified after the run). If it is ever
worth removing, the lever is `OPTION (RECOMPILE)` on the procedure's statements, **not** an index —
and that is a change to a shared procedure, outside this story.

⛔ **No foreign key on `Machineidx`, and it is not an omission.** The `dbo.machines` both mapping
tables join is a **`CommonDB` VIEW** over `united_db..machines` (§5.2 finding 7). SQL Server supports
neither cross-database foreign keys nor foreign keys to a view. All 74 + 882 values were verified
valid against `united_db.dbo.machines` — **0 orphans** — but the rule is not expressible. This
matches `LabelButtonMachineMapping` and `OptionButtonMachineMapping`, structurally identical mapping
tables that likewise carry no FK on their machine column.

⚠ **The mapping-table natural keys lead with `OPCModuleId`, not `Machineidx`.** Column order does not
change what a unique constraint *enforces*, but it decides whether it is usable as an index —
and `OPCModuleId` alone is the predicate in all three result sets of `GetOPCServerAndTagDetails`
**and** in both `DELETE` statements of `16_`.

✅ **Nothing blocks `09_`, and it has now been applied to `DEV00164-001`** — 5 Sep 2026 13:09:49.
Verified independently after the run: **0 heaps · 5 PKs · 6 uniques · 4 FKs · 1 check · 2 indexes ·
0 untrusted FKs**, and row counts **unchanged** at 4 / 4 / 496 / 74 / 882, which is the proof it
wrote no data. Its pre-flight passed **16 checks, 0 conflicts**, so every object was created with no
cleanup. It touches only columns present on **both** that instance and source control, so `G100`
never applied to it.

⚠ **§2.1 above now describes history on this instance only.** It remains the live state of `test1`,
staging and production — all unmeasured, the last two marked *fill* in `[DEP §2]` — so `09_` has to
be re-run per environment, exactly as `08_` and the module id do.

### 2.1d ⚠ `08_` closed the half of `G100` that blocked `11_` — and opened a trap in its place

`dbo.OPCModules` on `DEV00164-001` carried **four** columns against source control's **six**. The two
missing ones — `OPCEventType`, `EventDurationSeconds` — are exactly what `11_` §4a inserts, so its
**first** insert died on `Msg 207`. They were added by hand on 5 Sep 2026 and captured as
[`08_`](../../30-database/scripts/08_CommonDB_OPCModules_ColumnDrift.sql), which is the re-runnable
record of that change for the environments still carrying the drift.

✅ **Measured after the fact: `dbo.OPCModules` now has all six columns, and `11_` §4a binds.**

⛔ **But adding the columns removed an interlock, and this is the part to carry forward.** The four
pre-existing modules are **`NULL` in both new columns** — measured, not assumed:

| `OPCModulesIdx` | `ModuleName` | `OPCEventType` | `EventDurationSeconds` |
|---|---|---|---|
| 1 | `Hole Detection` | **NULL** | **NULL** |
| 2 | `CoilReceiving` | **NULL** | **NULL** |
| 3 | `Handheld Service` | **NULL** | **NULL** |
| 4 | `Furnace Scheduling` | **NULL** | **NULL** |

`UA.APIDTO.OPCInfo` maps both as **non-nullable `int`** through Dapper (§2.3 is the same hazard, one
row over). While the *deployed* `GetOPCServerAndTagDetails` does not select the two columns, those
NULLs are harmless — and **it does not**, verified. The **source-controlled** procedure does
(`:26-27`). Before the columns existed, `CREATE OR ALTER` on that procedure was impossible; now it
succeeds.

⛔ **So refreshing that procedure without backfilling ids 1–4 first makes `GetOPCInfo` throw for four
other modules** — hole detection, coil receiving, handheld service and furnace scheduling. That is a
bigger blast radius than anything else in this story, and **flat wire is not among the casualties**:
`11_` sets both columns explicitly on its own row (§2.3). ⚠ **Their correct values are not this
project's to invent** — `08_` deliberately does not guess them, and §7 carries the item.

⚠ **A second finding came out of `08_`, and it is a lesson about T-SQL rather than about data.**
`11_`'s original drift guard **could never have fired**: it shared a batch with §4a, and column names
bind at **batch compile** time, so `Msg 207` killed the guard along with the batch it was meant to
protect. It is now `11_` **§0**, in a batch of its own. ⚠ `SET NOEXEC ON` does not rescue this, and
`:on error exit` is SQLCMD-mode-only — **pass `-b`** so a failing batch actually stops the run.

### 2.2 ⛔ The two system-error rows are the difference between a registered line and an invisible one

`GetOPCServerAndTagDetails`'s **first** result set — the `OPCInfo` header carrying `MachineId`,
`ConnectionType`, `IsReadonly` and the servers — is an INNER JOIN through `OPCTagApplicationMapping`
to `OPCTags` filtered `IsActive = 1 AND IsSystemErrorTag = 1`
(`GetOPCServerAndTagDetails.sql:41-42`). **No such row, no header** — and `ContextRepository` builds
the servers and the tags off that header, so the entire line disappears (`G95`).

**The symptom is identical to `G60` itself:** an empty list either way. A full 39-row registration
with the two system-error rows missing looks exactly like no registration at all, and would have
surfaced for the first time at `C1`. The two rows — `FL1.PLC._System._Error` (machine 125) and
`FL2.PLC._System._Error` (126) — were supplied by the client on 5 Sep 2026 and are recorded in
`[PLC §5.2.1]` / `[PLC §5.2.2]`.

⚠ **The same result set has a second silent filter.** It joins `CommonDB.Machines`, which filters
`STATUS = 1`. A `machines` row in any other status vanishes with no error at all — which is why
`11_`'s pre-flight asserts `status = 1` on both 125 and 126 rather than merely asserting they exist.

### 2.3 Four columns that are nullable in the table and non-nullable on the DTO

`OPCModules.IsReadOnly`, `ConnectionType`, `OPCEventType` and `EventDurationSeconds` are all
NULLable in `CommonDB` and all **non-nullable on the `UA.APIDTO` `OPCInfo` DTO** that Dapper maps
them into. A NULL in any of the four throws at `GetOPCInfo`. `11_` sets all four explicitly:

| Column | Value | Why |
|---|---|---|
| `IsReadOnly` | **0** | Flat wire writes at check-in. `WriteTag` echoes the tags back **unwritten** when this is `1`, and `P-104` already treats that as a hard failure rather than a skip |
| `ConnectionType` | **21317** (`Lookups.OPCUA`) | The lines are OPC UA. ⛔ This is also what makes flat wire the **first** module ever to select `ual-api`'s `OPCUAManager` — see §2.7 |
| `OPCEventType` | **0** | ⚠ **A placeholder, and labelled as one** (`11_` D6). It drives the SignalR timed-event flush, which flat wire does not use |
| `EventDurationSeconds` | **1** | Same — chosen only to satisfy the DTO |

### 2.4 The module id is live data, not an enum member — and the enum is already lying (`G96`)

Three sources give three different pictures of which module ids are taken, and **only the live table
is authoritative**:

| Source | Says | Reality |
|---|---|---|
| `UAL.Constants.GlobalConstants.OPCModules` | stops at `FurnaceScheduling = 4`, so `5` looks free | **Documentation, and stale** |
| `ual-window-service` config | all six slitters run on `OpcModuleId: 5` | **In production use.** `UnifiedSlitterService` never registered in the enum because it does not consume that package |
| `CommonDB.OPCModules` on `DEV00164-001` | four rows, all `ConnectionType` **21316** (`OPCDA`) | Measured 4 Sep 2026 by `G94` |

**Flat wire takes `6`**, clear of all three, forced with `SET IDENTITY_INSERT` because
`OPCModulesIdx` is an `IDENTITY` column, and guarded by a pre-flight `RAISERROR` if `6` is occupied
by anything but `FlatWire`. ⚠ **Re-check the free id per environment before seeding** — `test1`,
staging and production can differ and the enum will not warn you.

### 2.5 Two homes for one string, on purpose — and the diff is the only thing keeping them level

`D-45` left `appsettings` as the **resolution map** (logical name → path, behind `FW-144`'s
unchanged `ITagPathResolver`) and `CommonDB.OPCTags` as the **registration** (path only, so
`GetOPCInfo` can answer). The same 39 strings therefore exist twice with nothing keying them
together (`P-315`). That is accepted; what it costs is one recurring check.

✅ **Measured 5 Sep 2026 while writing this plan — the two copies agree exactly.** The
`FlatWireOpc:Lines:{FL1,FL2}:Tags` values in `FlatWire.API/appsettings.json` on
`feature/UADEV-23146`, compared as a set against `11_`'s `@Tags` data rows:

| | Count | In `appsettings`, not in `11_` | In `11_`, not in `appsettings` |
|---|---|---|---|
| FL1 | 17 | — | — |
| FL2 | 22 | — | — |
| **Total data paths** | **39 = 39** | **0** | **0** |

⚠ **Do not reconcile that 39 against `FW-144 §8.2`'s 72 bound paths.** Both are correct and they
count different things: 72 is the whole `appsettings` map across three lines; 39 is what has a
machine to attach to. FL3's 33 paths are bound, unregistered, and deliberately still in their
pre-`D-46` form (§2.6).

### 2.6 FL3 is absent by design, and three independent things enforce it

`D-47` answers `PLC-Q08`: FM1 and the die blocks belong to the **FL1** controller, FM2 to the
**FL2** controller, and there is **no `FL3.PLC.*` namespace on any machine**. FL3's tags *are* FL1's
and FL2's. The two-controller push that implies is undesigned (`G99`), so FL3 is left to fail
loudly rather than made to half-work (`P-317`):

1. **`appsettings` gives FL3 no `MachineId` key at all**, so it binds to `0` and
   `PLCTagService.ResolveOpcInfoAsync` fails at its **second** guard — *"`Lines:FL3:MachineId` is
   not configured (gap `G60`)"* — before any HTTP call is made. `IngestLines` is `["FL1","FL2"]`
   for the same reason.
2. **`11_` writes no machine-127 row** in either mapping table.
3. **`11_`'s §5 verification block asserts 127 is absent**, so a later well-meaning edit is caught
   rather than discovered on a line.

⛔ **Do not "fix" this by seeding 127.** `GetOPCInfo` answering **empty** for FL3 is the designed
answer. A 127 registration would make an FL3 push reach a controller nobody designed the write for.

### 2.7 ⛔ Why `ConnectionType = 21317` is a prerequisite and not just a value

`G94` measured all four existing `OPCModules` rows as `21316` (OPCDA) on `DEV00164-001`.
`OPCUAManager` is therefore **selected by nothing today and has never run in production**. Flat wire
at `21317` is the first module to select it — and in that manager `ReadTag` (`:207`) and `WriteTag`
(`:359`) pass the **bare** tag name, which QuickOPC parses as namespace **0**, a different node,
while only `Subscribe` (`:247`) uses the `ns=2;s=` form. `G58`'s swallow then reports `200`.

**So a registration activated before `FW-236` merges produces a system that looks completely
healthy and writes nowhere.** That is the single most expensive failure available in this story, and
the only defence is ordering (§3, `P-319`).

⚠ **The rows themselves are right either way:** `OPCTags` stores the **bare** path and the
`ns=2;s=` form is applied only at the QuickOPC boundary (`P-316`). `G94` measured 0 of 496 existing
rows carrying a prefix; a prefixed row would be double-wrapped. `11_`'s verification block fails on
any row matching `ns=%`.

### 2.8 What is already delivered

| | State |
|---|---|
| [`08_CommonDB_OPCModules_ColumnDrift.sql`](../../30-database/scripts/08_CommonDB_OPCModules_ColumnDrift.sql) | ✅ **Written, complete and RUN on `DEV00164-001`, 5 Sep 2026.** Adds `OPCEventType` and `EventDurationSeconds` (`int NULL`) to `dbo.OPCModules`, matching `ual-database`'s `CreateTable.sql`. **Writes no row.** It is the record of a change made by hand, and is idempotent — on a level instance it reports *present* twice and exits `0`. Closes the half of `G100` that stopped `11_` §4a. ⛔ **Does not backfill** `OPCModulesIdx` 1-4; §4 audits the `NULL`s it leaves and points at `G100` |
| [`09_CommonDB_OPCTables_Constraints.sql`](../../30-database/scripts/09_CommonDB_OPCTables_Constraints.sql) | ✅ **Written, complete and RUN on `DEV00164-001`, 5 Sep 2026** — all 18 objects measured present. Adds the 5 PKs, 6 unique constraints, 4 FKs, 1 check and 2 indexes the five OPC tables have never had (§2.1c). Writes no row. Pre-flight validates all 16 conditions and aborts before creating anything; sections 2–5 are one transaction; §6 re-lists every object and asserts no table is left a heap. ✅ **`PARSEONLY`-checked clean.** ✅ **Pre-flight measured on `DEV00164-001` — 16 checks, 0 conflicts.** ⛔ **`G100` does not apply to it.** ⚠ Reversible DEV-only **by hand** — no reverse script |
| [`11_CommonDB_Insert_OPCRegistration_FlatWire.sql`](../../30-database/scripts/11_CommonDB_Insert_OPCRegistration_FlatWire.sql) | ✅ **Written and complete — no placeholder left** (`D-49`). Pre-flight guards all outside the transaction, one transaction, **4** server-mapping rows + **41** tag rows into four tables with `OPCServers` **read** (`D-48`), and a §5 verification block asserting the counts, the endpoint pair, the system-error rows, the `ns=` prefix, the `PLC` element, the absence of 127, a dangling endpoint, a line without a pair and a duplicated `ConnectionSequence`. ✅ **`PARSEONLY`-checked clean**, all six batches |
| [`16_CommonDB_Delete_OPCRegistration_FlatWire.sql`](../../30-database/scripts/16_CommonDB_Delete_OPCRegistration_FlatWire.sql) | ✅ **Written** — DEV only, deletes by exact value, mappings first, and **nothing at all from `OPCServers`** (`D-48`). ✅ `PARSEONLY`-checked clean |
| [`30-database/scripts/README.md`](../../30-database/scripts/README.md) | ✅ **All four** scripts in the manifest with sign-off state and reversibility — the folder is now twelve. ⚠ The reversibility note's *"`OPCTagsIdx` carries no foreign key"* is marked **conditional on `09_`** |
| `FlatWire_Scripts_RunAll.sql` | ✅ Excludes **`08`, `09`, `10`, `11` and `16`** deliberately — `08` and `09` because they `ALTER` shared tables four other modules read, which should not be one keystroke inside a runner |
| `[DEP §4.2]` steps `1a` / `1b` | ✅ **`09_` and `08_` added to the cross-folder sequence, both ahead of step `2a`.** ⚠ Lettered suffixes, not a renumber — `P-153`, the same idiom that made `11_` step `2a` |
| `FlatWire.API/appsettings.json` | ✅ `OpcModuleId` **6** · `Lines:FL1:MachineId` **125** · `Lines:FL2:MachineId` **126** · **no** FL3 `MachineId` · all 39 FL1/FL2 paths in the `.PLC.` form (`D-46`) |
| `[DEP §1.1]` component 4, `[DEP §4.5]`, `[DEP §5]` | ✅ Retargeted to the real script; the config-vs-rows diff added |
| **Running any of it** | ⚠ **`08_` and `09_` HAVE been run on `DEV00164-001`** (5 Sep 2026) — both are schema-only and wrote no row. ⛔ **`11_` and `16_` have not, and `11_` is the one that matters**: it is now technically runnable — every guard passes and §4a binds — but **must not be** until `FW-236`/`G94` merges (§3 step 6, `P-319`) |
| `UAL.Constants.OPCModules` | ⚠ Untouched, and **gates nothing** (`P-321`) |

### 2.9 Out of scope

| Concern | Story |
|---|---|
| The `machines` rows and WIP stations this keys on | [`FW-241`](../../30-database/tasks/FW-241.md) / `FW-003` — `10_`. **A prerequisite, not a co-authorship** |
| Per-tag write status, and the `G94` identity fix | [`FW-236`](FW-236.md) — **must merge before activation** |
| A service identity for a background OPC call (`G59`) | [`FW-237`](FW-237.md) |
| Resolving a logical name to a path | [`FW-144`](FW-144.md) — built, and **unchanged** by `D-45` |
| Writing tags at all | [`FW-151`](FW-151.md) — built; this story is what its resolve path has been waiting for |
| Reading tags as a feed | [`FW-N05`](FW-N05.md) |
| Confirming the paths are the ones the controller has | `[PLC]` `C1` / `C11` — `G33`, `G32`. **This story closes neither** |
| Designing FL3's two-controller push | `G99` — raised by this planning, owned by nothing yet |

---

## 3. Build order

**Steps 1–5 are done.** Steps 6–9 are a sequence of gates, and ⛔ **the order is not a preference —
every wrong order in it fails silently** (§2.2, §2.7). ⚠ **What is left is no longer environmental either**: `G100` is fixed on `DEV00164-001` (step `7b`,
`08_`), so nobody is waiting on an answer *or* on a database, and `11_` will run the moment `FW-236`
merges — which is precisely why step 6 matters more than it used to.

✅ **Two exceptions, and both are already done: steps `7a` and `7b`.**
`09_` and `08_` are gated by nothing — not `FW-236`, not `FW-241`'s sign-off — because neither writes
a row and neither activates anything. They are listed at `7a`/`7b` for ordering (both must precede
`11_`), **not** because they have to wait for steps 6 or 7. Both have been run on `DEV00164-001` and
both are idempotent, so a re-run reports *present* and changes nothing.

1. ✅ **Read the ids from `[DEP §4.2]` step 2, not from the script and not from memory** — 125 / 126
   / 127. `FW-241 §2` is explicit that re-typing them here is how the registration and the insert
   drift.
2. ✅ **Author `11_`** — five tables in dependency order (**four written**, `OPCServers` read), one
   transaction, idempotent on natural keys, `SET IDENTITY_INSERT` for the module id, and every
   pre-flight guard **outside** the transaction so an abort changes nothing.
3. ✅ **Author `16_`** — mappings before rows, by exact value, DEV only.
4. ✅ **Manifest and runner** — both in `scripts/README.md`; both excluded from
   `FlatWire_Scripts_RunAll.sql`.
5. ✅ **`G97` answered (`D-49`)** — `OPCServersIdx` **1 and 2**, both lines onto both; `11_` §1
   carries the ids and no placeholder remains. This step was going to ask a controls engineer three
   things and **two are now settled**: the endpoint, and `D9`'s *one or two*, which was the wrong
   question (§2.1b). ⛔ **The third is NOT, and it no longer has a conversation to ride on**: the
   **namespace index** is still `DefaultNamespaceIndex = 2`, a compile-time constant nothing has
   confirmed (`OPCTag.cs:14`), which `FW-236`'s final acceptance criterion defers. **`G97` closing
   is exactly when this gets forgotten** — carry it into the `G33`/`G32` commissioning ask.
6. ⛔ **Wait for `FW-236` / `G94` to merge.** Not "start after" — *merge*. §2.7 is why.
7. ⛔ **Run `10_` first** (`FW-241`, under sign-off). `11_` aborts if 125 and 126 are not both
   present in `united_db..machines` at `status = 1`, which is the correct behaviour and also means
   **this ordering cannot be got wrong by accident**.
   **— STEP `7a` · ✅ DONE — [`09_`](../../30-database/scripts/09_CommonDB_OPCTables_Constraints.sql),
   run on `DEV00164-001` 5 Sep 2026 13:09:49 —** it adds the primary keys, unique constraints and
   foreign keys the five OPC tables have never carried (§2.1c), which turns `11_`'s hand-written
   duplicate guards into schema guarantees on its **first** run rather than its second. Verified
   after the run: **0 heaps · 5 PKs · 6 uniques · 4 FKs · 1 check · 2 indexes · 0 untrusted FKs**,
   row counts unchanged. ⛔ **Gated by nothing** — not `G100`, not `FW-236`, not `FW-241`: it writes
   no row and activates nothing, so it needed neither the merge in step 6 nor the `machines` rows in
   step 7. It is numbered `7a` **only** to fix its position relative to `11_`. ⚠ **Still owed on
   every other environment** — `test1`, staging and production are unmeasured. ⚠ If its pre-flight
   aborts there, that instance holds data the constraints reject; a finding about the environment,
   not a reason to weaken the script.

   **— STEP `7b` · ✅ DONE — [`08_`](../../30-database/scripts/08_CommonDB_OPCModules_ColumnDrift.sql),
   the `OPCModules` column drift —** the two columns `11_` §4a inserts were added by hand on
   `DEV00164-001` and captured as this script. ✅ **Verified: six columns, `11_` §4a now binds**, and
   that is the half of `G100` which blocked the run. **Independent of `7a`** — neither touches what
   the other checks, so either order works; both must precede step 8. ⛔ **It does NOT backfill:
   `OPCModulesIdx` 1–4 are `NULL` in both columns**, which is safe *only* while the deployed
   `GetOPCServerAndTagDetails` does not select them (verified: it does not). **Backfill before
   refreshing that procedure** or `GetOPCInfo` throws for four other modules — §2.1d, §7.

   ⚠ **Lettered steps, not new numbers** — *"steps 5–9"* and *"step 7"* are cited in this document
   and in `P-318` / `P-319`, so renumbering would invalidate them; same idiom as `[DEP §4.2]`'s
   `1a` / `1b` / `2a`.
8. **Run `11_` by hand on the target instance.** ✅ **Every guard now passes on `DEV00164-001`** —
   machines 125/126 at `status = 1`, `OPCModulesIdx` 6 free, endpoints 1 and 2 present, no flat wire
   tags yet, and §4a binds since `7b`. ⛔ **That makes it runnable, not cleared to run** — step 6 is
   still open, and `P-319` puts the merge first. ⚠ **Pass `-b`** so a failing batch stops the run;
   `:on error exit` is SQLCMD-mode-only and `SET NOEXEC ON` does not rescue a bind failure (§2.1d).
   ⚠ **Nothing is left to fill in** (`D-49`) — §1
   carries `OPCServersIdx` 1 and 2, and there is no `@OPCServerName` or `@OPCServerClass` to set.
   ⛔ **Re-check those two ids on that instance first** (§2.1b (a)): the guard proves they *exist*,
   which on a differently-numbered instance could be the wrong pair with the right numbers. Then
   **run its §5 verification block and read all four results** — the module row and the endpoint
   pair (§5a / §5a2, expect **4** mapping rows), the counts (39 / 2 / 41 and 41 tag mappings split
   18 / 23), every registered path, and the **eight** fault queries that must each return zero
   rows.
9. **Prove it against the real service, then close `G60`.** `GetOPCInfo` for FL1 and FL2 returns a
   populated `OPCInfo` with `ConnectionType == 21317`; for **FL3 it must still fail by name at
   `Lines:FL3:MachineId`** without a call being made. `FW-151`'s and `FW-N05`'s boot logs stop
   naming `G60` for FL1/FL2 — that is the observable that retires the gap.
10. ⚠ **Optional, unbundled, and gating nothing** — propose `Slitter = 5` and `FlatWire = 6` to
    `UAL.Constants`' owner so the enum stops lying (`P-321`). **Never make this a dependency.**

> ⛔ **What is deliberately NOT in this list: flipping a flag.** `SimulatePLCTagPush` and
> `SimulateOpcFeed` stay `true` when this story closes (`P-320`).

---

## 4. Decisions made here

> `P-##` is continuous across this folder. `P-315`–`P-317` were minted on 5 Sep 2026 when the
> scripts were written and are restated here because they are this story's; **new decisions in this
> plan mint at `P-318`**, and the next story mints at `P-324`+.

### Already minted — `P-315` · `P-316` · `P-317`

| Id | Decision |
|---|---|
| `P-315` | **The tag paths are kept in two places and the copies are *checked*, not trusted** (§2.5). `D-44`'s `TagKey` column cost a shared `ALTER` and another team's schedule to buy a property the ecosystem has never needed. Fallback if drift ever bites: `OpcSubModuleTags` is the precedent for a **side table, not an `ALTER`** |
| `P-316` | **`OPCTags` stores the BARE path**; the `ns=2;s=` node-id form is applied only at the QuickOPC boundary (§2.7) |
| `P-317` | **FL3 is left to fail loudly rather than made to half-work** (§2.6) |

### `P-318` — the story has an **authored** half and an **activated** half, and only the second closes `G60`

The DB 8 h bought a script; a script that has never run registers nothing. But holding the card open
in S3 waiting on a controls engineer misreports where the work is, and closing it on the file
existing misreports what the system can do.

**So the card is worked as two halves with different exit conditions.** *Authored* — `11_`, `16_`,
the manifest, the config, the deploy documentation — is **complete**, and is what the 8 h priced.
*Activated* — steps 5–9 — is a **commissioning-window event** gated on three outside answers, and it
is what closes `G60`. ⚠ The story's `status` stays `in-progress` until the activated half lands:
`G60`'s cost was never the typing, it was that *"discovering the registration is absent inside the
40 h window spends commissioning time on data entry"*, and that cost is only avoided if the run is
scheduled rather than assumed.

### `P-319` — the activation order is fixed, and each step's tripwire is named

Steps 5–9 of §3 in that order, because **three of the four possible mis-orderings fail silently**:

| Wrong order | What you see | What is actually true |
|---|---|---|
| Run `11_` before `10_` | ⛔ **Nothing — it aborts loudly.** The one safe mis-ordering | The guard did its job |
| Activate `21317` before `FW-236` merges | Every push answers `200` | Every write lands in **namespace 0** (`G94` + `G58`) |
| Seed without the two system-error rows | `GetOPCInfo` returns an empty list | Looks exactly like `G60`, unregistered (`G95`) |
| Seed with a `machines` row at any status but `1` | `GetOPCInfo` returns an empty list | `CommonDB.Machines` filtered it out |

**Only the first of those four is self-reporting.** That asymmetry — one guard, three silent
failures — is why the order is written down as a decision rather than left to the runbook.

### `P-320` — closing `FW-238` does **not** turn the write path on, and the flags do not move here

`SimulatePLCTagPush` and `SimulateOpcFeed` stay `true`. Three separate reasons, and all three
outlive this story:

- **`G59` is untouched.** `RestClient` takes its bearer from the current `HttpContext`; a hosted
  service has none, so a background `GetOPCInfo` fails with a `NullReferenceException` **before the
  network**, registration or no registration. `FW-237` owns that.
- **`G33` is untouched.** A registered path is not a *confirmed* path — `[PLC]` is v1.3 with no
  `[CONFIRMED]` tag by design, and only `C1`/`C11` settle it.
- **`[PLC §7.1]`** is explicit that a push configures the machine, and `[DEP §4.5]` that tag-push
  verification happens **on a stopped line only**.

⚠ **Stated as a decision because the opposite is the intuitive reading**: `G60` was the reason the
flag could not usefully be turned off, so retiring `G60` reads like permission to turn it off. It
is not.

### `P-321` — the `UAL.Constants` enum change is proposed as documentation, and never becomes a dependency

`G60`'s original text named a missing `OPCModules` member as half the gap. Measured 5 Sep 2026, it
is not: **no line in the FlatWire codebase evaluates the enum** — every occurrence is a comment or a
log string — `ResolveOpcInfoAsync` sends `options.OpcModuleId`, a plain config int, and
`ual-window-service` does not reference the package at all.

**Adding `Slitter = 5` and `FlatWire = 6` is still worth doing**, because an enum that omits a
production module id is how `G96` happened. But it is a **shared-constants change owned by another
team**, and this story must not acquire that team's schedule as a blocker for something that gates
nothing. Propose it; ship without it.

### `P-322` — a shared **lookup** is joined, never seeded, and the guard that enforces it hands back the answer

`D-48` settles that `CommonDB.dbo.OPCServers` takes no `INSERT` and no `UPDATE` from flat wire
(§2.1a). Recorded as a decision here for two reasons that outlive the edit.

**First, it is the same call as `D-32`, one table down.** `D-32` bound the `coils` / scheduling
schema as *read and written as it stands and never altered*; `D-48` says the OPC endpoint register
is read as it stands too. The test that separates the two halves of the registration is **who owns
the row**: `OPCModules` id 6, the two mapping tables and the 41 tag rows are **ours** — nothing else
in the ecosystem refers to them, which is exactly why `16_` can reverse them on a DEV instance.
The endpoint is **not ours** under any reading, and the giveaway was already in `G97`'s own
measurement: four endpoints existed and were serving other modules before flat wire was designed.

**Second, the abort is a better instrument than the variable it replaced.** An `@OPCServerClass`
looks like a field to fill and is really a field to *not fill*, so it makes a wrong action available
at the exact moment someone is under commissioning-window pressure. What replaces it is a guard that
**prints the endpoints and stops** — the failure hands the reader the list rather than an empty
column to guess at. ⚠ **The rule that guard protects:** an endpoint that is not there is a request
to whoever owns `CommonDB`'s OPC estate. It is never a reason to add an `INSERT` back to `11_`.

### `P-323` — a two-number answer that had two wrong readings, and measurement caught both

`G97` was answered as *"`OPCServersIdx` 1 and 2 will be used for OPCUA"* (`D-49`). Eleven words,
and **both of the obvious readings of them were wrong** (§2.1b). Recorded because the near-miss is
the reusable part, not the ids.

**The name/id reading is silently wrong.** A name is legible and an id is not, so selecting the
endpoint by `@OPCServerName` reads like the better design. But ids 1 and 2 are `UAOPC3`/`UAOPC4`,
so under a name selector *"1 and 2"* is transcribed as `UAOPC1`/`UAOPC2` — **ids 3 and 4, the pair
three other modules use** — and nothing raises an error anywhere: guard passes, mapping writes,
`GetOPCInfo` answers, lines point at the wrong server. ⚠ **The lesson is narrower than "prefer
ids"**: when an instruction arrives in a particular key, resolve it in that key. Translating ids
into names on the way in adds a step whose only possible contribution is an error.

**The one-or-two reading is wrong later, which is worse.** The lines do not split across two
endpoints; they **share a primary/failover pair**, which is what all 37 existing `(module, machine)`
mappings do and what `GetOPCServerAndTagDetails`' `ORDER BY … ConnectionSequence` exists to serve.
⛔ Two rows instead of four **registers successfully, verifies successfully, and works** — until the
primary endpoint drops, at which point flat wire is the only machine in `CommonDB` with nowhere to
fail over to.

**What both have in common** is that the answer was checked against the table before being typed
into the script, at a cost of two queries. ⚠ **It is the same move that found `G100`**, and the same
move behind `D4`'s *"single words"* correction. **Three for three on this story: every claim about
`CommonDB` that was reasoned rather than measured has turned out wrong.**

⚠ **The residue.** One assumption is left and it is not measurable: which of the pair is the
primary. `11_` takes the order given (1, then 2); `Hole Detection` does the reverse on the same
pair. Named in §2.1b, in `11_` `D14`, and in §7 — flipping two `DECLARE`s is the whole change.

---

## 5. Verification

**No automated tests** — `[TS §1.2]` (all automated backend test levels withdrawn 15 Aug 2026). The
checks below split the same way `P-318` does: **§5.1** is verifiable from the artefacts today,
**§5.2 was actually run** against the live instance, and **§5.3** waits on the activation steps
(§3, 6 onward).

### 5.1 Verifiable now — the authored half

| Check | Expected | State |
|---|---|---|
| **Config-vs-rows diff** | The 39 `appsettings` FL1/FL2 paths and `11_`'s 39 data rows are **equal as sets, both directions** | ✅ **Measured 5 Sep 2026 — 39 = 39, zero difference** (§2.5) |
| Row shape | 41 tag rows = 39 data + 2 system-error; 41 mappings = 18 (125) + 23 (126) | ✅ In the script and asserted by its §5 block |
| **No `ns=` prefix** | Zero rows match `ns=%` (`P-316`) | ✅ Fault query 2 |
| **Every path carries `PLC`** | Zero rows fail `LIKE 'FL_.PLC.%'` (`D-46`) | ✅ Fault query 3 |
| **A system-error row per line** | Zero lines returned by the "no system-error tag" query (`G95`) | ✅ Fault query 4 |
| ⛔ **Machine 127 is absent** | Zero rows (`P-317`) | ✅ Fault query 5 |
| Guards run **before** the transaction | An abort leaves no table half-seeded | ✅ Every one of them is outside `BEGIN TRANSACTION` |
| Idempotency | A second run inserts **nothing** and prints so | ✅ Natural-key guards on all four written tables; `OPCServers` has nothing to be idempotent about, being read only (`D-48`) |
| ⛔ **Nothing is written to `OPCServers`** | No `INSERT` and no `UPDATE` against it anywhere in `11_`; §4b is a `SELECT` (`D-48` / `P-322`) | ✅ And guarded — both ids must already be rows, and must differ |
| **The endpoint pair, by id** | `OPCServersIdx` **1 and 2**, `ConnectionSequence` 1 and 2, **4** mapping rows (`D-49`) | ✅ §1 and §4c. ⚠ Ids 1/2 are `UAOPC3`/`UAOPC4` — §2.1b (a) |
| **A dangling endpoint is caught** | Zero rows from the mapping-with-no-`OPCServers`-row query. `OPCServersIdx` carries no FK | ✅ Fault query 6 |
| **A line without a full pair is caught** | Zero rows — each line must have exactly **2** endpoints (`D-49` / `P-323`) | ✅ Fault query 7 |
| **A duplicated `ConnectionSequence` is caught** | Zero rows — two "primaries" would make the ordered server list arbitrary | ✅ Fault query 8 |
| `16_` deletes in the right order | Mappings before the rows they point at, and **nothing at all from `OPCServers`** | ✅ §"ORDER IS NOT OPTIONAL" and §SCOPE |
| All three excluded from the runner | `FlatWire_Scripts_RunAll.sql` skips `09`, `10`, `11`, `16` | ✅ Its header says so, and gives the reason for each |
| ✅ **`09_` parses** | All batches compile | ✅ `PARSEONLY`-checked clean |
| ✅ **`09_`'s pre-flight passes on the live instance** | 16 checks, 0 conflicts — every constraint creatable with **no data cleanup** | ✅ **Measured read-only on `DEV00164-001`, 5 Sep 2026** (§2.1c) |
| ⚠ **`09_` writes no row** | No `INSERT`, `UPDATE` or `DELETE` against any of the five tables; only `ALTER` and `CREATE INDEX` | ✅ By inspection — the only `SELECT`s are its own validation |
| ⛔ **`09_` never drops an existing constraint** | Every object behind `IF NOT EXISTS`, so a re-run is a no-op | ✅ Deliberately unlike the per-table files in `ual-database`, which use drop-then-create |
| ⚠ **`appsettings` gives FL3 no `MachineId`** | The key is absent, so it binds `0` and the tripwire fires by name | ✅ Verified in the bound file |

### 5.2 What executing this plan actually established — 5 Sep 2026, on `DEV00164-001`

**Read-only, against the live shared instance** — the one `[DEP §2]` names as the `test1` database
server. **Nothing was written anywhere**; `11_` was not run and could not be. Seven things were
confirmed, one was narrowed, and **one new blocker was found**.

| # | Check | Result |
|---|---|---|
| 1 | `[DEP §4.2]` **step 0** — co-location | ✅ All six databases on one instance: `commondb` · `united_db` · `proddb` · `Slitterdb` · `wiplogdb` · `FlatWireDB` |
| 2 | `11_` guard 5 — the `machines` rows | ✅ **125 `FL1`, 126 `FL2`, 127 `FL3`, all `status = 1`.** ⚠ **They already exist**, so on this instance the guard **passes** and §3 step 7 is already satisfied — see §7 |
| 3 | `11_` guards 2–4 — the module id (`G96`) | ✅ **`OPCModulesIdx` 6 is FREE**; identity seed is `4`. ⚠ **There is no row `5` on this instance at all**, so the slitters' `OpcModuleId: 5` points at a row that is not here — consistent with `G96`'s *"three different pictures"*, and **6 is still the right choice** |
| 4 | **`G60`, live** | ✅ **Confirmed.** 496 `OPCTags` rows, **0** beginning `FL1.`/`FL2.`; no `FlatWire` module row. Nothing registers flat wire |
| 5 | **`G94`, live** | ✅ **Confirmed exactly as measured 4 Sep.** Four `OPCModules` rows — `Hole Detection`, `CoilReceiving`, `Handheld Service`, `Furnace Scheduling` — **all `ConnectionType` 21316** (OPCDA), and **0 of 496** tags carry an `ns=` prefix |
| 6 | **`G95`, in the deployed procedure** | ✅ **Confirmed in the object itself**, not inferred: `GetOPCServerAndTagDetails`'s first result set INNER JOINs `OPCTags` on `IsActive = 1 AND IsSystemErrorTag = 1`. No system-error row, no header, no line |
| 7 | The `STATUS = 1` silent filter | ✅ **`CommonDB.dbo.Machines` is a VIEW** — `SELECT … FROM united_db..machines WHERE m.STATUS = 1`. So `11_`'s warning is right **and** its guard is pointed at the correct object (`united_db`, checking `status`) |
| 8 | **`G97` — NARROWED, then narrowed again, then ANSWERED** | ⚠ `OPCServers` **already holds four endpoints**, all class **`SWToolbox.TOPServer.V6`**: `UAOPC1`–`UAOPC4`. So `OPCTag.cs`'s *"TOP Server v6"* hint is confirmed as what this site runs and **the class value is known**. ✅ **`D-48` then closed the question's shape**: those four are a **lookup**, flat wire picks from them, and the measurement above was the evidence — endpoints existed and were serving other modules before flat wire was designed. ✅ **`D-49` answered it: `OPCServersIdx` 1 and 2, both lines onto both.** ⛔ **Two follow-up measurements changed the script** — the ids do not match the names, and all 37 existing mappings are pairs (§2.1b). ✅ **The `varchar(56)` sub-risk is WITHDRAWN with the insert that created it** |
| 9 | ✅ **`G100` — FOUND HERE, and its blocking half now CLOSED** *(the columns were added to `DEV00164-001` by hand 5 Sep 2026 and captured as `08_`; `11_` §4a binds clean. `G100` stays Open on the stale `GetOPCServerAndTagDetails`, which does not block this story.)* | **`11_` cannot run on this instance, for a reason unrelated to `G97`.** `dbo.OPCModules` here has **four** columns; `ual-database` declares **six**. Section 4a inserts `OPCEventType` and `EventDurationSeconds`, so the run dies at the **first** insert on `Msg 207 Invalid column name` |

> ⛔ **`G100` is the finding that justifies executing rather than reading.** All five of `11_`'s
> original guards **pass** on `DEV00164-001`, so the script reports itself ready and then fails on
> its first write — with a message that reads as a typo in the script rather than a stale database.
> ✅ **`11_` is correct and the database is behind**: the live `GetOPCServerAndTagDetails` does not
> select the two columns either, while the source-controlled copy does (`:26-27`), so the instance
> is internally consistent and simply old. ✅ **Scope was measured, not assumed** — the other four
> tables were compared column by column and **match exactly**, so `OPCModules` is the only drifted
> object of the five. A **sixth pre-flight guard** now aborts by name before the transaction opens.

⚠ **Four claims in the delivered artefacts were corrected against measurement.** Three are minor:
`11_` `D4` said the four existing `ModuleName` values *"are single words"* — **three of the four
contain spaces**, so there is no convention to match; `D7` was narrowed by finding 8; and the
module-id note now records that `5` is free here too.

⛔ **The fourth is load-bearing, and it is finding 8 read properly.** The measurement recorded four
existing endpoints as *context*; they are the **answer** — a lookup (`D-48`), whose ids are the key
(`D-49`). `11_`'s §4b insert was never the right shape. §2.1a and §2.1b are the re-cut.

#### 5.2a What running `08_` and `09_` then established — same instance, same day

**Both are schema-only and neither wrote a row.** Re-measured independently afterwards, not taken
from the scripts' own output:

| # | Check | Result |
|---|---|---|
| 10 | `09_` applied | ✅ **0 heaps · 5 PKs · 6 uniques · 4 FKs · 1 check · 2 indexes · 0 untrusted FKs.** Row counts **unchanged** at 4 / 4 / 496 / 74 / 882 — the proof it wrote no data |
| 11 | `08_` applied | ✅ `dbo.OPCModules` now carries **all six** columns, so `11_` §4a binds. `G100`'s blocking half is closed |
| 12 | ⛔ **NEW — the backfill trap** | `OPCModulesIdx` **1–4 are `NULL`** in both new columns, and `OPCInfo` maps them as non-nullable `int`. ✅ **Currently harmless — verified the deployed `GetOPCServerAndTagDetails` does not select them.** ⛔ It is the *source-controlled* one that does, and `CREATE OR ALTER` on it is now possible where it previously failed. **Backfill before refreshing** (§2.1d) |
| 13 | ⛔ **NEW — the IO regression** | `GetOPCServerAndTagDetails` went **21 → 269 logical reads**. ⛔ **The plan's "at or below 21" was wrong.** Cause is the `@machines` **table variable**'s 1-row estimate flipping the plan to nested-loop seeks; on 1–5 page tables that is worse than a scan. ⚠ Results unchanged and still correctly ordered; all pages cached, so the absolute cost is sub-millisecond. **No index fixes it** — see §2.1c |
| 14 | `11_`'s guards, re-run | ✅ **All pass**: machines 125/126 at `status = 1` · `OPCModulesIdx` 6 free · endpoints 1 and 2 present · 0 flat wire tags · §4a binds. ⛔ **`11_` is runnable and still must not be run** — step 6 is open |
| 15 | ⚠ **A second, unrelated drift** | The **deployed** procedure returns an `OPCTagId` column in result set 3 that source control does **not** have. So the drift is **bidirectional**, and a blind refresh would drop it. `UA.APIDTO.OPCTag` has no such property so Dapper would ignore it, but a repo-wide search for other consumers **did not complete** — treat as unverified |

⚠ **Finding 13 is the one to carry.** *"Adding the right indexes made it slower"* is counter-intuitive
enough that it will be re-discovered by someone else otherwise, and the reflex fix — add a covering
index — is the wrong one.

### 5.3 Verifiable only after activation — the half that closes `G60`

| Check | Expected |
|---|---|
| ⛔ **`FW-236` merged first** | Confirm on the branch, **before** `11_` is run — not after (`P-319`) |
| `10_` has run | `united_db..machines` 125 and 126 both present at `status = 1`. `11_`'s guard proves it |
| ✅ **`09_` has run** | `09_` §6 reports **0 heaps · 5 PKs · 6 uniques · 4 FKs · 1 check · 2 indexes · 0 untrusted FKs**. ⚠ **Not a hard gate on `11_`** — `11_` runs correctly either way, because its own guards duplicate what the constraints enforce. Running `09_` first is what makes those guards redundant rather than load-bearing |
| ⛔ **`11_` still succeeds under the new constraints** | `11_` writes parents before children (`OPCModules` → server mappings → `OPCTags` → tag mappings) and `16_` deletes children before parents, so both already satisfy the four FKs. ⚠ **A second `11_` run must still insert nothing** — under `09_` a broken guard would now raise a duplicate-key error instead of silently doubling the rows, which is the point |
| The module row | `OPCModulesIdx` 6 · `ModuleName` `FlatWire` · `ConnectionType` **21317** · **none of the four columns NULL** (§2.3) |
| ⛔ **The endpoints were joined, not created** | `11_` §5a2 returns **4** rows — each line on **both** endpoints, sequences 1 and 2 — every one naming a row that **pre-existed the run**; and `dbo.OPCServers` holds the **same number of rows after as before** (`D-48` / `D-49`). ⚠ Count it before running; the script prints what it used, but only the instance can prove nothing was added |
| ⚠ **The pair is the one intended** | §5a2's `OPCServerName` column reads **`UAOPC3`** (seq 1) and **`UAOPC4`** (seq 2) on `DEV00164-001`. ⛔ **If it reads `UAOPC1`/`UAOPC2`, the ids meant something else on this instance — stop** (§2.1b (a)) |
| Counts, read from `11_` §5b | Data **39**, SystemError **2**, Total **41**; mappings 125 → **18**, 126 → **23** |
| **`GetOPCInfo` for FL1 and FL2** | A populated `OPCInfo`, `MachineId > 0`, `ConnectionType == 21317`, `IsReadonly` false. **This is what retires `G60`** |
| ⛔ **`GetOPCInfo` for FL3** | **Fails at `Lines:FL3:MachineId` naming `G60`, with no HTTP call made** — the designed answer, not a defect (`G99`). ⚠ If FL3 ever returns a populated `OPCInfo`, machine 127 was seeded and `P-317` was broken |
| Boot logs | `FW-151` and `FW-N05` stop naming `G60` for FL1/FL2 and **keep** naming it for FL3 |
| `IsReadonly` still fails loudly | Set the module read-only → the push fails `PLC_PUSH_FAILED` (`P-104`, already harness-proven against a stand-in) |
| ⚠ **The flags did not move** | `SimulatePLCTagPush` and `SimulateOpcFeed` both still `true` (`P-320`) |
| `[DEP §4.5]` step 5 | Its six OPC/PLC checklist items become checkable for the first time |
| ⚠ **Tag push on a STOPPED line only** | `[DEP §4.5]` `V12`. Never on a running line — the push configures the machine |

> ⛔ **What none of this establishes.** A registered path is not a confirmed path. `G33` — a wrong
> path reads back consistently wrong — is untouched, `G32`'s FM2 station names are unsigned, and
> `[PLC §10.3]` is explicit that *"a tag push actually configures the machine"* cannot be proven
> before `C1`/`C11`. This story makes the write path **reachable**; it does not make it **correct**.

---

## 6. Handoff

`FW-151`'s `PLCTagService` resolve-and-write path stops being dead code and gets its first exercise
against the real `OPCConnection`. `FW-N05`'s `OpcIngestService` gains a `GetOPCInfo` that can answer
— though `G59` still stops it calling one. `FW-217`'s E2E fixture no longer needs the
`OpcConnectionDouble` to stand in for a registration that does not exist. `[PLC]`'s `C1` and `C11`
gain their precondition.

**Six items handed on, none of them this story's to fix:**

| To | Item |
|---|---|
| **`[DEP]`** | ✅ **DONE 5 Sep 2026 — raised here and fixed the same day.** `11_` was component 4 in `[DEP §1.1]` and **absent from `[DEP §4.2]`'s cross-folder sequence**, the table that calls itself *"this is its only home"* — so a deployer following the chain would have skipped it and met `G60` at commissioning. Added as **step `2a`**, immediately after the `machines` rows it keys on and aborts without. ⚠ **A suffix, not an eleventh step**: the chain is cited as *ten steps* in five files and `P-153` makes *"step 2"* the record of `FW-003`'s decided values, so a renumber would have invalidated all of it. The runner-exclusion sentence now reads *steps 2 and 2a*, the teardown paragraph records `16_` as the one shared-row reversal that exists, and §1.1's *"step 1's `10_` script"* is corrected to step 2. ➕ **Also `09_`, added as step `1a`** — a second lettered suffix for the same `P-153` reason, placed ahead of `2a` because the constraints protect the insert |
| **Whoever owns `CommonDB`** | ⚠ **`09_` changes the schema of five tables four other modules read** — `Hole Detection`, `CoilReceiving`, `Handheld Service` and `Furnace Scheduling` all resolve through them. It adds only keys and indexes and writes no row, and `GetOPCServerAndTagDetails` is unaffected (its result sets and ordering are unchanged), but it is **not** a flat-wire-local change and should be run with that owner's knowledge. ⚠ The per-table files live in `ual-database` under `Tables\OPC*\{Constraints,Indexes}\`; **`09_` and those 18 files must stay in step** |
| **`FW-236`** | ⛔ **The namespace-index follow-up is now ORPHANED and must be picked up explicitly.** `DefaultNamespaceIndex = 2` is a compile-time constant nothing has confirmed (`11_` `D8`). It was going to ride along with the `G97` ask; **`G97` has been answered without it**, so there is no longer a conversation scheduled that carries it |
| **`G99`** | FL3's two-controller push, its two-domain compensation, the `ITInhibit` **pair**, and the re-pointing of FL3's 33 configured paths into the FL1/FL2 namespaces. ⚠ **Also rewrite the commissioning test** — *"one acknowledgement configures FM1 and FM2"* passes under either topology, which is how `PLC-Q08` stayed open so long |
| **`UAL.Constants`' owner** | `Slitter = 5` and `FlatWire = 6` (`P-321`) |
| **`[TB]` / `[CE]`** | The card's hours are **left at 12 h and re-confirmed**, not re-derived: `D-45` removed the `CommonDbTagPathResolver` that would have grown it, and the row count fell from a notional 72 to 41, so the two movements cancel. ⚠ The `Hours` cell feeds three client `.xlsx` generators and the capacity totals are quoted in ~20 files — a re-price belongs in an additive pass that owns the arithmetic |

---

## 7. Open items

| Item | Effect here |
|---|---|
| ✅ **The five OPC tables were heaps — CLOSED on `DEV00164-001` by `09_`** *(found and fixed 5 Sep 2026)* | No PK, no unique index, no FK, no check constraint on any of the five, while their `united_db` predecessors carry PKs and two FKs (§2.1c). ⛔ **Every hand-written guard and three of the eight fault queries in `11_` existed only because of this.** ✅ [`09_`](../../30-database/scripts/09_CommonDB_OPCTables_Constraints.sql) **is applied there** — 5 PKs, 6 uniques, 4 FKs, 1 check, 2 indexes, 0 heaps, 0 untrusted FKs, row counts unchanged. ⚠ **Still owed on `test1`, staging and production**, all unmeasured. ⚠ **`Machineidx` is deliberately left without a FK** — `dbo.machines` is a cross-database view, so the rule is not expressible. ⚠ The per-table source-control home is `ual-database` under `Tables\OPC*\{Constraints,Indexes}\`; **keep the two in step** |
| ⚠ **`GetOPCServerAndTagDetails` reads went UP after `09_`, not down** *(measured after the run, 5 Sep 2026)* | **21 → 269 logical reads** for the same 11 machines on module 4. ⛔ **The plan predicted "at or below 21" and was wrong.** Cause is cardinality, not access paths: `@machines` is a **table variable** estimated at 1 row, so with indexes available the optimiser switches from hash-join-over-scan to **nested loops repeating a seek per machine** — and on 1–5 page tables that costs more than one scan. ⚠ **Not a defect and not worth reverting**: all five tables are fully cached, results are unchanged and result set 2 is still correctly ordered. **No covering index would help.** If it is ever worth removing, the lever is `OPTION (RECOMPILE)` on the procedure — a shared-procedure change, outside this story |
| ⚠ **`G100`** *(found by executing this plan, 5 Sep 2026; blocking half CLOSED the same day — see `08_`)* | **`DEV00164-001`'s `CommonDB` is behind `ual-database`**: `dbo.OPCModules` has four columns against source control's six, so `11_` dies at its first insert on `Msg 207`. ⛔ **One of the two gates left, and the only one that is about the environment rather than the design — so it is the one thing standing between `11_` and a clean run.** ✅ Guarded in `11_` (`D11`) so it aborts by name; ⛔ the guard does not fix the instance — refresh `CommonDB`, **the table *and* `GetOPCServerAndTagDetails`**, before running |
| ⛔ **`G94` / `FW-236`** | **Flat wire is the first module ever to select `OPCUAManager`.** Until the identity fix merges, `ReadTag` and `WriteTag` address namespace **0** and every push reports `200` while writing nowhere (§2.7). ⚠ `FW-236` is `not-started` and needs `OPCConnection`'s owner to accept a response-shape change |
| ✅ **`G97` — RESOLVED** *(`D-49`, 5 Sep 2026)* | **The lines use the endpoint pair at `OPCServersIdx` 1 and 2, both lines onto both.** `11_` carries real values and no placeholder; **this no longer gates anything**. ⚠ **Two corrections came with the answer** (§2.1b): the ids are `UAOPC3`/`UAOPC4`, not `UAOPC1`/`UAOPC2`, so the script addresses endpoints **by id**; and it is a primary/failover **pair**, so §4c writes **4** rows. ⛔ **Re-check the ids per environment** — the guard proves they exist, not that they are the same pair |
| ⚠ **Which of the pair is primary** *(new, and not measurable)* | `11_` makes `OPCServersIdx` **1** the primary, taking *"1 and 2"* in the order given. ⛔ **`Hole Detection`, the only other module on this pair, uses id 2 as its primary** on all three of its machines. Low cost either way — it decides which endpoint is tried first — but it is an **assumption, not a finding**. Flipping the two `DECLARE`s in `11_` §1 is the entire change |
| ⛔ **The namespace index** *(orphaned by `G97` closing)* | `DefaultNamespaceIndex = 2` is a compile-time constant and **nothing proves the flat wire PLCs publish there** (`11_` `D8`). ⚠ **It travelled with `G97` and was never part of it** — `G97` closing is exactly when this gets forgotten. `FW-236`'s final acceptance criterion still defers it |
| ⚠ **`FW-241` / `10_`** | ⚠ **Superseded in fact on this instance, and only there.** The plan said the `machines` rows *"have never been written"*; measured 5 Sep 2026, **125/126/127 all exist at `status = 1` on `DEV00164-001`**, so `11_`'s fifth guard passes and §3 step 7 is already satisfied. ⛔ **This is not `FW-241` closing** — the story's content is the `machine_type` / station-set / `StationType` **sign-off** and the missing reverse script, and neither is discharged by rows existing. **Re-check per environment** |
| ⛔ **`G99`** *(raised by this planning)* | FL3 cannot run at all until its two-controller write is designed. **FL1 and FL2 are unaffected** |
| **`G59` / `FW-237`** | A background OPC call still has no identity. ⚠ **This is why closing `FW-238` does not let the flags move** (`P-320`) |
| **`G33` / `PLC-Q05`** | A wrong path fails silently. `P-105`'s confirm read narrows it; only `C1`/`C11` close it |
| **`G32` / `PLC-Q04`** | FM2 station names (`FL2.FM2.S1/S2/S3`) pending sign-off; `C11` records what the controller accepted |
| ✅ **`11_` D9 — one endpoint or two?** | **CLOSED, and the question was the wrong shape.** Neither: **both** lines sit behind **both** endpoints as a primary/failover pair, which is what all 37 existing `(module, machine)` mappings do (`D-49`, §2.1b (b)) |
| ⚠ **`11_` D6 — the two placeholder columns** | `OPCEventType` and `EventDurationSeconds` exist only to satisfy the DTO. Revisit if flat wire ever takes the SignalR path |
| ⚠ **Module id per environment** | `6` is free on `DEV00164-001`. `test1`, staging and production can differ and the enum will not warn you (`G96`) |
| ✅ **`OI-A`** | **CLOSED — `D-44`**, then re-answered by `D-45`: the paths live in **both** places by decision (§2.5) |
| ✅ **`G93`** | **WITHDRAWN — `D-45`.** No shared `ALTER`, no procedure change, no other team on the Phase-14 critical path |
