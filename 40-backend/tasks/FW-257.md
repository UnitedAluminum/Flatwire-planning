---
id: FW-257
legacy_id:
title: Re-point the built ITInhibitService at the ItInhibitReason lookup
status: not-started
status_confirmed: true
owner:
jira:
mvp: 1
phase: "4"
stream: BE
streams: [BE]
priority: high
hours: 8
sprint: S2
depends_on: [FW-205, FW-254]
blocked_by: [G80, G81]
has_plan: false
started:
completed:
---

# FW-257 - Re-point the built `ITInhibitService` at the `ItInhibitReason` lookup

> **No implementation plan has been written for this story yet.**
> The card below is the contract from `[TB]`. Before starting, replace this notice
> with the sections in the task template: *What to build* / *Context you need* /
> *Build order* / *Decisions made here* / *Verification* / *Handoff*.

## 1. What to build

**Hours:** 8 h BE · **Priority:** High · **Sprint:** S2 · **Phase:** 4 · **Stream:** BE

**As** the run-block interlock,
**I want** my inhibit reasons read from the seeded lookup,
**So that** the client's eight reasons and `[PLC §8.2]`'s five are one list rather than two.

> ⛔ **[`FW-205`](FW-205.md) is `done`, and the vocabulary it was built against has changed.** It
> implements `[PLC §8.2]`'s five set conditions. The client's list of eight, supplied 1 Sep 2026,
> **shares exactly one of them** — *no coil or rod is checked in*. `ItInhibitReason` is now seeded
> with **12** rows: 8 client-active and 4 §8.2-only, inactive.
>
> ⚠ **`G80`: nobody has said the five are superseded, so this is additive or it is a
> contradiction — it is not a replacement.** §8.2's five are not loose prose: they are `FR-008` /
> `FR-009` with alternate flows `ALT002`–`ALT005` / `DAT009` and **five P1 test cases
> `TC-011`–`TC-015`**.

**Acceptance Criteria:**
- [ ] `ITInhibitService` evaluates its conditions against **`ItInhibitReason` rows**, not a compiled-in set of five, reading them through `GET /reasons/itinhibit`'s source
- [ ] The two genuinely new client reasons are evaluable: **`No Bundle/Spool is Checked In`** (which is `[PLC §8.2]` condition 1 in the client's words) and **`Next Bundle Not Welded`**
- [ ] **`TC-011`–`TC-015` still pass.** §8.2's other four conditions — no active material-tracking identifier, feet data unavailable, feet data invalid, two or more consecutive missing recordings — remain **live inhibits**, seeded inactive only because the client's sheet omitted them. ⛔ **Do not delete a condition the client merely failed to list**
- [ ] **`TC-015a`, `TC-015b`, `TC-015c`, `TC-015e` and `TC-015f` pass**; `TC-015d` stays **blocked with `G81` named on it**
- [ ] ⛔ **`No Qualified Operators Are Logged In` is not implementable and must not be stubbed.** `G81`: `[SEC §8]`'s six roles contain neither **Leadman** nor **Helper**, and there is no qualification matrix at all. The row stays seeded and **inactive**; a predicate that silently returns false would make an inhibit the client marked as applying look implemented
- [ ] ⚠ **`Supervisor Monitor` is not resolved here.** `D-38` is a flat *"No"* to a dedicated Supervisor Monitor **screen**, yet the reason is marked as applying and appears live on both real screenshots. `Q20` owns what sets it; this story seeds and reads it without inventing a setter
- [ ] ⚠ **A `Call Supervisor` action exists on both live inhibit pop-ups and in no requirement** in this repository. Out of scope, recorded on the card so it is not lost
- [ ] ⚠ **The client's 8-row sheet is a curated subset of what these machines actually inhibit on.** The screenshots carry periodicity, torch/conductivity and mandrel-state conditions the sheet drops — evidence the union is larger than either list

**Rate-card basis (§2):** not a rate-card unit — a rework of a **built** service from a literal condition set onto a seeded vocabulary, priced at one command-endpoint-class change plus the predicate set = **8 h**
**Dependencies:** [FW-205](FW-205.md) *(built — this amends it)* · [FW-254](FW-254.md)
**Blockers:** **`G80`** *(the two lists share one of thirteen)* · **`G81`** *(the qualification matrix does not exist)*

---
