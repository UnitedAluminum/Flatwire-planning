---
id: FW-230
legacy_id:
title: FL1 segment alpha — one namespace
status: in-review
status_confirmed: false
status_note: "⛔ **Blocked on [`FW-231`](FW-231.md).** Built alone it **reissues `R00001A` on every spool**"
owner:
jira:
mvp: 1
phase: "8"
stream: DB
streams: [DB, BE]
priority: high
hours: 0
sprint: S2
depends_on: []
blocked_by: []
has_plan: true
started:
completed:
---
# FW-230 · FL1 segment alpha — one namespace

**Project:** United Aluminum (UAL) — Flat Wire Mill Module
**Last Updated:** August 29, 2026 — Change history is in [`CHANGELOG.md`](../../CHANGELOG.md)
**Document Type:** Implementation plan for a single backlog story
**Status:** ⛔ **Blocked on [`FW-231`](FW-231.md).** Built alone it **reissues `R00001A` on every spool**
**Owner:** Backend (.NET) + Database streams
**Audience:** The developer building `FW-230`
**Shortcode:** — *(implementation plan, derived from the specifications and the built code; **not citable as a requirement**)*
**Part of:** `ProjectPlan/Backend/tasks/` — index: [Orchestration.md](Orchestration.md)

---

> **Why this document exists.** Fourteen hours, and **four details decide whether it is right —
> the first of which is that its own first acceptance criterion is WITHDRAWN.**
>
> **⛔ The ignore list no longer exists.** Change `[N]` (26 Aug 2026) withdrew it: the mint passes
> **`''`**, and `GenerateCoilAlpha`'s **own sweep** finds prior segments because
> [`FW-231`](FW-231.md) registers every one in `proddb..coils`.
> **The struck text is still on the card**, as superseded history.
> **⛔ Built alone, this reissues `R00001A` on every spool** (`OI-138` / `G54`). **Ship with
> `FW-231` or stay blocked.**
> **`ChildAlpha` is OPAQUE** — never parsed, never rebuilt, and **no stored letter index**.
> **Single letter for a segment, double for a coil part.** Segments take one trailing letter off
> the **rod**; coil parts take a **double** off the **segment**.

---

## 1. The story

From `[TB §7]` — verbatim. ⚠ **Compact `FW-225`–`FW-231` card variant**, and its first bullet is
**struck text retained as history**.

> ###### FW-230 · FL1 segment alpha — one namespace
> **Hours:** 14 h (DB 4 · BE 10) · **Priority:** High · **Sprint:** S2 · **Phase:** 8 · **Stream:** DB + BE
>
> > **`Q57`.** FL1 segment alphas and FL2 coil identities are the same strings off the same six-character
> > root, and `CommonDB.dbo.GenerateCoilAlpha` cannot see `FlatWireDB` — so both are minted through that one
> > function. `SpoolTraceability.ChildAlpha` is already applied.
> >
> > ⚠ **Change `[N]` (26 Aug 2026) answers the *"cannot see `FlatWireDB`"* clause differently.** Rather than
> > compensating with an ignore list, **`FW-231` makes the alphas visible** by registering them in
> > `proddb..coils`. Segments keep a **single** trailing letter off the rod; coil parts take a **double** off
> > the segment. Design of record: `[RodOrderAllocation.md §2.4/§2.8]`.
>
> - [ ] ⛔ **WITHDRAWN 26 Aug 2026 (change `[N]`) — there is no ignore list.** *Superseded: "Ignore list = **every** prior segment alpha for that rod, read from `SpoolTraceability` — not just this transaction's. Cap `VARCHAR(500)`."* The mint passes **`''`**; `GenerateCoilAlpha`'s own sweep finds prior segments because **`FW-231` registers every one in `proddb..coils`**. `F11`'s 500-char cap and `F10`'s 2048 → 500 truncation stop applying to flat wire
> - [ ] ⚠ **Depends on `FW-231`.** Built alone, a blank-list mint **reissues `R00001A` on every spool** (`OI-138` / `G54`). Ship them together or keep this story blocked
> - [ ] Replicate the caller's guards: the `' '` blank return and the `UPDLOCK, HOLDLOCK` re-check
> - [ ] `ChildAlpha` is **opaque** — never parsed, never rebuilt, and no stored letter index
> - [ ] ⚠ **Cannot be tested on LocalDB** — it has no `CommonDB`
> - [ ] Reference loop: `PlanningDB.dbo.GetCoilAlpha`'s `CONCAT_WS` accumulation — **cite it, do not call it** (it uses PlanningDB's divergent copy)
>
> **Residual:** **`Q59`** — a third-party caller cannot see `FlatWireDB` and can be issued an alpha a segment holds. Accepted and monitored; the `UNIQUE` index makes it loud

### 1.1 Out of scope

| Concern | Owner |
|---|---|
| Registering the alphas in `proddb..coils` | ⛔ [`FW-231`](FW-231.md) — **the gate** |
| The spool-completion transaction | [`FW-202`](FW-202.md) |
| The FL2 **output coil** alpha | [`FW-185`](FW-185.md) / `FW-219`, Phase 9 |
| `GenerateCoilAlpha` itself | ⛔ **`CommonDB`, shared** — cited, not modified |
| `PlanningDB.dbo.GetCoilAlpha` | ⛔ **Cite, do not call** — a divergent copy (§2.4) |

### 1.2 What already exists

Verified on 29 Aug 2026.

| Thing | State |
|---|---|
| `CommonDB.dbo.GenerateCoilAlpha` | ✅ Exists, shared — ⚠ **cannot see `FlatWireDB`** |
| `SpoolTraceability.ChildAlpha` | ✅ **Applied** — built 22 Aug 2026 |
| `SpoolTraceability` + `SpoolOrder` | ✅ Built |
| **`SpoolSegmentsMustNotOverlapRule`** | ✅ Built and **invoked** at `SpoolProcessing.cs:188` |
| The six alpha value objects | ✅ Built by `FW-141` (`D-29`) — validating constructors; `G14`'s format half **verified at startup** |
| `Q89`'s `UX_CoilTraceability_ChildAlpha` | ✅ Built 26 Aug 2026 — ⚠ **the `UNIQUE` index that makes `Q59` loud** |
| **The mint call** | ⛔ **Absent.** This story |
| **`proddb..coils` segment rows** | ⛔ **Absent** — `FW-231` |

---

## 2. The four details

### 2.1 ⛔ AC 1 is withdrawn — read the strike-through, not the sentence inside it

The card's first bullet is **struck text preserved as history**. The superseded design was an
**ignore list** of every prior segment alpha for the rod, read from `SpoolTraceability`, capped
at `VARCHAR(500)`.

**What change `[N]` (26 Aug 2026) does instead:** the mint passes **`''`**, and
`GenerateCoilAlpha`'s **own sweep** finds prior segments — because
[`FW-231`](FW-231.md) puts them in `proddb..coils` where the sweep
can see them.

⚠ **Two constraints stop applying to flat wire as a result** — `F11`'s 500-char cap and `F10`'s
2048 → 500 truncation. ⛔ **A developer implementing the struck bullet would reintroduce both**,
and would also build a list the function no longer needs.

### 2.2 ⛔ Alone, this is a silent duplicate-name generator

`GenerateCoilAlpha`'s sweep reads the **shared** coil master. With no FL1 segment rows there, a
blank-list mint sees **no prior sibling** and returns the **first** letter every time —
`R00001A`, on every spool, for the same rod.

⚠ **The failure is delayed and displaced.** The mint succeeds; the duplicate surfaces later when
`UX_CoilTraceability_ChildAlpha` rejects an insert — *"the `UNIQUE` index makes it loud"* — at
which point the bad name has already been printed on a label.

⛔ **`FW-231` is not a nice-to-have ordering preference.** `P-229` records the same rule from the
gating side.

### 2.3 `ChildAlpha` is opaque, and the temptation is to parse it

*"Never parsed, never rebuilt, and no stored letter index."*

⚠ **Every part of the scheme invites parsing it**: the six-character root is a prefix, the
trailing letter is a sequence, and *"single letter for a segment, double for a coil part"* looks
like a grammar.

⛔ **It is a name, not a structure.** Reasons the rule exists:
- The **function** owns the format; a parser in `FlatWireDB` is a second implementation that
  drifts when `CommonDB`'s changes.
- **A stored letter index** would be a second source of truth for "which letter is next", and
  the whole 26 Aug change is about having exactly one (the sweep).
- ⚠ **`G14`'s format half is enforced by value objects with validating constructors** — that
  validates **shape**, and is not licence to decompose.

### 2.4 The reference loop is a citation, not a call

*"`PlanningDB.dbo.GetCoilAlpha`'s `CONCAT_WS` accumulation — **cite it, do not call it** (it uses
PlanningDB's divergent copy)."*

⛔ **PlanningDB holds a divergent copy** of the alpha logic. Calling it would mint from a
different implementation than the one `FW-231` registers against — producing names that collide
with nothing today and everything later.

**Read it for the accumulation pattern; call `CommonDB.dbo.GenerateCoilAlpha`.**

---

## 3. Build order

1. ⛔ **[`FW-231`](FW-231.md) ships with this or before it** (§2.2).
2. Call **`CommonDB.dbo.GenerateCoilAlpha`** with a **blank** ignore list (§2.1).
   ⛔ **Do not build the withdrawn list.**
3. **Replicate the caller's guards**: the `' '` blank return and the `UPDLOCK, HOLDLOCK` re-check.
   ⚠ Same `THROW 51011` pattern `FW-231` uses — **one transaction across mint and insert.**
4. Store the result in `SpoolTraceability.ChildAlpha` — ⛔ **opaque** (§2.3).
5. ⛔ **Single letter for a segment**; the double belongs to the coil part, off the segment.
6. ⚠ **Test on a shared instance** — LocalDB has no `CommonDB` (§1.2, and `FW-231` §2.5).
7. Record `Q59`'s residual as **accepted and monitored** — the `UNIQUE` index is the alarm.

---

## 4. Decisions this plan makes

> `P-##` is continuous across the repository; `P-01`–`P-230` precede this story.

### `P-231` — the withdrawn ignore list is not built, and the strike-through is load-bearing

§2.1. The card preserves the superseded design **inside** its first bullet, so a developer
skimming acceptance criteria reads a specification for a thing that must not exist — **and
building it reintroduces `F10`/`F11`'s caps** that change `[N]` retired for flat wire.

⚠ **This is the clearest case in the whole pass of a card whose text says the opposite of its
intent**, and it is why §1 quotes the strike-through rather than paraphrasing.

### `P-232` — `ChildAlpha` is written and compared, never decomposed

§2.3. Extends `D-29`'s value-object stance: the constructor validates **shape**, and nothing
reads meaning out of the string. **No letter index is stored**, because the sweep is the single
source of "what comes next".

**Fallback:** none. A second source of sequence is exactly the defect `G54` describes.

---

## 5. Verification

**No automated tests** — `[TS §1.2]`. ⚠ **Cannot be tested on LocalDB.**

| Check | Expected |
|---|---|
| **No reissue** | Two consecutive spools on one rod → **`R00001A` then `R00001B`** (`G54`, §2.2) |
| **With `FW-231`** | The second mint's sweep finds the first segment **in `proddb..coils`** |
| ⛔ **Alone** | Demonstrate the failure once: without `FW-231`'s rows, the mint **repeats** — so the dependency is evidenced, not asserted |
| **No ignore list** | The call passes `''`; ⛔ `grep` finds no list construction and no 500-char cap (`P-231`) |
| Guards | `' '` blank return handled; `UPDLOCK, HOLDLOCK` re-check present |
| Atomicity | Mint and insert in **one** transaction; a crash leaks no name |
| **Opaque** | ⛔ No parsing, no rebuilding, **no stored letter index** (`P-232`) |
| Letter width | **Single** for a segment; double is the coil part's, off the segment |
| **Not PlanningDB** | ⛔ `PlanningDB.dbo.GetCoilAlpha` is **not called** (§2.4) |
| `Q59` | Recorded as accepted and monitored; the `UNIQUE` index is the alarm |
| Instance | Tested against real `CommonDB`, ⛔ never LocalDB |

---

## 6. Handoff

[`FW-231`](FW-231.md) is the gate and ships with this.
[`FW-202`](FW-202.md) owns the spool-completion transaction the mint runs
inside. [`FW-185`](FW-185.md) mints the **coil part** alpha off the
segment — **double letter** — in Phase 9. The design of record is
`[RodOrderAllocation.md §2.4/§2.8]`, which is **rationale, not a citable requirement**.

---

## 7. Open items

| Item | Effect here |
|---|---|
| ⛔ **`OI-138` / `G54`** | `FW-231` gates this. **Alone, it reissues names** (`P-229`) |
| ⛔ **AC 1 is withdrawn** | The struck text is history. **Do not build it** (`P-231`) |
| **`Q59`** | A third-party caller may be issued an alpha a segment holds — **accepted and monitored** |
| **`OI-139`** | Can an FL2-standalone spool arrive with no segment at all? |
| **`Q57`** | The one-namespace decision this implements |
| ⚠ **LocalDB** | No `CommonDB` — untestable there |
