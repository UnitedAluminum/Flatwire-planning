---
id: FW-164
legacy_id:
title: GET /run/active, GET /run/{runId}/gaugetrace and RunQueryService
status: in-review
status_confirmed: true
status_note: "✅ **DE-STUBBED, 29 Aug 2026 — staged on `feature/UADEV-23146`, not committed.** `RunService.cs` goes 86 L / 6 throws → **841 L / 1**, and the throw that remains is `FW-166`'s. ⛔ **`P-254`'s CODE half landed and its CONTRACT half did not** — `[API §4.7a]` and the built DTO now disagree by eleven fields. ⚠ AC 3 is built in SHAPE only: `GetOrderContextAsync` is a documented `OI-33` seam returning null"
owner:
jira:
mvp: 1
phase: "5"
stream: BE
streams: [BE]
priority: high
hours: 12
sprint: S2
depends_on: [FW-138, FW-141]
blocked_by: [Q18]
has_plan: true
started:
completed:
---
# FW-164 · `GET /run/active`, `GET /run/{runId}/gaugetrace` and `RunQueryService`

**Project:** United Aluminum (UAL) — Flat Wire Mill Module
**Last Updated:** August 31, 2026 — ✅ **THE DE-STUB IS WRITTEN, and the outstanding work is now the CONTRACT, not the code.** `RunService.cs` is **staged on `feature/UADEV-23146` at 841 L / 1 throw** (`HEAD` still holds the 86 L / 6-throw stub), so this document stops planning the two service bodies and starts recording them. ⛔ **The correction that changes what a developer does: `P-254` landed by halves.** Its code half is built — the Order Information block **and** the `outOfSpec` thresholds are on `ActiveRunResponse`, populated, and annotated in code with `P-254` by name — while `[API §4.7a]` was never widened, so the published contract and the built DTO **disagree by eleven fields** (§2.1). That is `[API §7]`'s stub-first rule broken in the one direction `P-254` itself named as forbidden, and closing it is the only build step left. ⚠ **AC 3 is built in SHAPE only** — `GetOrderContextAsync` is a documented `OI-33` seam returning null, which is `P-42` working as designed. ✅ `routeMode` populated, the `204` served, `GetRunWeldEventsAsync` still throwing and correctly `FW-166`'s. ⚠ **`G51` is not "new here"** — it is registered, owned by `FW-245` on its `SpcMeasurement` half, and already cited by `[API §4.7a]`; what is new is that the API/DTO half has **three** sites and no owner. Change history is in [`CHANGELOG.md`](../../CHANGELOG.md)
**Document Type:** Implementation plan for a single backlog story — now also the record of its de-stub
**Status:** ✅ **DE-STUBBED, 29 Aug 2026 — staged on `feature/UADEV-23146`, not committed.** `RunService.cs` goes 86 L / 6 throws → **841 L / 1**, and the throw that remains is `FW-166`'s. ⛔ **`P-254`'s CODE half landed and its CONTRACT half did not** — `[API §4.7a]` and the built DTO now disagree by eleven fields. ⚠ AC 3 is built in SHAPE only: `GetOrderContextAsync` is a documented `OI-33` seam returning null
**Owner:** Backend (.NET) stream
**Audience:** The .NET developer building `FW-164`
**Shortcode:** — *(implementation plan, derived from the specifications and the built code; **not citable as a requirement**)*
**Part of:** `ProjectPlan/Backend/tasks/` — index: [Orchestration.md](Orchestration.md)

---

> **Why this document exists.** Two reads, and each carries something that is not obvious
> from its name.
>
> **`GET /run/active` is the trial's landing route.** DB1 left trial scope on 14 Aug, so
> `[TRP §1.4]` names this endpoint as what the app opens on instead of `/lines/status`. It is
> load-bearing for the trial in a way its 12 h suggests it is not.
>
> **Its DTO reaches across databases.** The Order Information block — customer, due date,
> tolerances, setup width/gauge, finish, OD min–max, weights — lives in the **shared
> order/scheduling schema**, and it is served **in one round trip** rather than a second call.
> ⚠ Those columns are **unmapped** (`OI-33`).

---

## 1. The story

From `[TB §7]` — verbatim:

> ###### FW-164 · `GET /run/active`, `GET /run/{runId}/gaugetrace` and `RunQueryService`
> **Hours:** 12 h BE · **Priority:** High · **Sprint:** S2 · **Phase:** 5 · **Stream:** BE
>
> **As a** client developer,
> **I want** an active-run snapshot and a paged historical trace,
> **So that** the monitor resumes correctly and FL2 can show a finished run's profile.
>
> **Acceptance Criteria:**
> - [ ] `RunController GET /run/active?line=` returns the active-run DTO (payoffs, weldEvents, components)
> - [ ] `GET /run/{runId}/gaugetrace` returns the gauge-trace DTO (readings, weldMarkers, limits), paged by `from` / `to` / `resolution` via Dapper
> - [ ] **The active-run DTO carries the Order Information fields** — customer, due date, tolerances, setup width/gauge, finish, OD min–max, weights — served in one round trip rather than a second call. These live in the **shared order/scheduling schema, cross-database**, on the same unenforced-link basis as the rod-alpha references
> - [ ] Out-of-spec detection thresholds surfaced to the client as configuration
> - [ ] Weld markers sourced from `WeldEvent`
> - [ ] Any authenticated role may read
>
> **Rate-card basis:** 2 query endpoints @ 4 h = 8 h + `RunQueryService` / Dapper paging 4 h = 12 h (§2)
> **Dependencies:** FW-138, FW-141
> **Blockers:** **`Q18`**

### 1.1 Out of scope

| Concern | Story |
|---|---|
| `sp_GetGaugeTrace` itself | [`FW-165`](../../30-database/tasks/FW-165.md), DB — ✅ **built** |
| The DB3 screen | [`FW-081`](FW-081.md), `FW-062`, `FW-163`, FE |
| Live telemetry | [`FW-150`](FW-150.md) — **this is the historical/REST half** |
| Weld **writes** | [`FW-166`](FW-166.md) — deferred with DB2A |
| `GET /run/{runId}/weldevents` | [`FW-166`](FW-166.md) — ⚠ **it is one of six methods on the same `IRunService`**; do not de-stub it here |

⚠ **`IRunService` carries six methods and this story owns two.** The interface is shared, so the
staged diff on `RunService.cs` is **not** FW-164's alone — read it against this split before
reviewing it:

| Method | Owner | State |
|---|---|---|
| `GetActiveRunAsync` `:123` · `GetGaugeTraceAsync` `:248` | **`FW-164`** | ✅ **this story** |
| `GetRunWeldEventsAsync` `:311` | `FW-166` | ⛔ **still throwing, correctly** — deferred with DB2A |
| `PauseRunAsync` `:335` · `ResumeRunAsync` `:398` | `FW-170` | de-stubbed in the same pass |
| `GetOutstandingPromptsAsync` `:468` | `FW-149` / `FW-225` | ⚠ returns an **empty success**, deliberately — already recorded by [`FW-149`](FW-149.md); **do not re-raise it here** |

### 1.2 What the de-stub built

Read off the working tree on 31 Aug 2026. ⚠ **Staged on `feature/UADEV-23146`, not committed** —
`HEAD` still holds the 86-line, 6-throw stub, and `[TRP §9.2]`'s rule applies: *uncommitted work
is not delivered work*.

| Thing | Where | State |
|---|---|---|
| `RunController` endpoints **17** / **18** | `FlatWire.API/Controllers/Run/` | ✅ Built (`FW-138`) |
| `IRunService` + `StubRunService` + named-throw shell | `FlatWire.Domain/Services/`, `…/Infrastructure/Services/` | ✅ Built (`FW-140`, `P-64`) |
| `ActiveRunResponse` · `GaugeTraceResponse` · `RunPayoff` · `WeldMarker` · `ComponentStateItem` | `RunContracts.cs:15`, `:206`, `:161`, `:175`, `:192` | ✅ Built |
| **`ActiveRunResponse.RouteMode`** | `RunContracts.cs:39` | ✅ Built ([`FW-181`](FW-181.md) `P-49`) **and now populated** — `RunService.cs:196` |
| **`204 No Content` for no active run** | `RunService.cs:148` | ✅ Contracted **and served** — *"the normal idle state, not an error"* |
| `sp_GetGaugeTrace` | `FlatWireDB` | ✅ Built ([`FW-165`](../../30-database/tasks/FW-165.md)) — ⛔ `G9` leaves its decimation parameter untunable |
| `RunReading` | `04_Runs` | ✅ Table built (`G3`'s table half) and written by **raw SQL, absent from the EF model** (`P-12`) — as §2.2 says, there is no repository to reach for |
| ⭐ **The Order Information block** | `RunContracts.cs:91-118` | ✅ **BUILT — ten nullable fields**, under a comment citing `P-254`, `P-42`, `OI-33` and `Q18` by name. ⛔ **But `[API §4.7a]` was NOT widened with it** — §2.1 |
| ⭐ **`OutOfSpecThresholds OutOfSpec`** | `RunContracts.cs:132`, class `:151` | ✅ **BUILT and populated** (`RunService.cs:214`) from `IOptionsMonitor`. ⛔ **Also absent from `[API §4.7a]`** — §2.4 |
| **`IContextRepository.GetOrderContextAsync`** | `ContextRepository.cs:234` | ✅ **Signature built, body returns `null`** — a documented `OI-33` **seam**, not an unfinished method. This is `P-42`'s first half **declared**, and its own remark says the view lands here and nowhere else |
| **The two service bodies** | `RunService.cs:123`, `:248` | ✅ **WRITTEN.** This is no longer the deliverable |

---

## 2. What each read owes

### 2.1 `GET /run/active?line=` — the trial's landing route

Returns payoffs, weld events, components **and the Order Information block**.

> **One round trip, deliberately.** The alternative — a second call for the order — doubles
> latency on the screen an operator opens first.

⛔ **`[API §4.7a]` does NOT specify the combined shape — found 29 Aug 2026.** Its `200` response
is `{ runId, lineId, orderId, alloy, passScheduleId, targetGauge, gaugeTolerance, targetWidth,
widthTolerance, routeMode, status, startedAt, pausedAt, footageFt, payoffs[], weldEvents[],
components[] }`. **Customer, due date, finish, OD min–max, setup width/gauge and weights appear
nowhere in it.** So AC 3 was never "join a view and fill fields that exist" — adding them is a
**contract change** (`P-254`).

⛔ **`P-254` then landed by HALVES, and that is the live defect — corrected 31 Aug 2026.** The
de-stub built the code side: the order block **and** `outOfSpec` are on `ActiveRunResponse`
(`RunContracts.cs:91-118`, `:132`), populated at `RunService.cs:221` and `:214`, with `P-254`
cited in the type's own comment. **`[API §4.7a]` was not widened.** So the drift now runs the
other way — *the built DTO returns eleven fields the published contract does not describe* —
which is exactly `[API §7]`'s stub-first rule broken in the direction `P-254` named as forbidden.
**The fields exist; the contract is what is missing.**

**The delta, exactly — eleven fields, the last an object:**

> `customer` · `dueDate` · `setupGaugeIn` · `setupWidthIn` · `finish` · **`temper`** · `odMinIn` ·
> `odMaxIn` · `coilWeightMinLb` · `coilWeightMaxLb` ·
> `outOfSpec { consecutiveReadings, autoPromptEnabled }`

⚠ **`temper` is in the built DTO and in NEITHER AC 3's list NOR `P-254`'s.** The widening must
enumerate what **shipped**, not what was planned — writing AC 3's list into `[API §4.7a]` would
leave the contract one field short and re-open the same drift.

⚠ **The order block is a cross-database read** on the same unenforced-link basis as rod
alphas, and **`OI-33` leaves `planning_routings`' columns unmapped**. Follow `[DBD §6.6]`'s
convention: `united_db..alloys` is surfaced as a **view** in the consuming databases, *"one
place to absorb mismatches rather than repeating them at every call site."* ✅ **That seam is
already declared** — `ContextRepository.GetOrderContextAsync` returns `null` today and its remark
records that when `OI-33` closes the change lands there **and at no call site**. So every order
field is null on the wire now, and the screen degrades to *not available* exactly as `P-42`
intends.

### 2.2 `GET /run/{runId}/gaugetrace` — paged, via Dapper

Readings, weld markers and limits, paged by `from` / `to` / `resolution`.

- **Dapper, not EF** — `[SVC §3.3]` puts high-volume reads on Dapper, and this is the
  heaviest query in the service. Backed by **`sp_GetGaugeTrace`** (`FW-165`).
- **Weld markers come from `WeldEvent`**, not from the readings.
- ⚠ **`RunReading` is in no aggregate** ([`FW-207 §2.1`](FW-207.md)) and has
  **no repository** ([`FW-141 §2.1`](FW-141.md)) — it is an append-only
  write model read by Dapper. Do not reach for a repository that does not exist.

### 2.3 Two consumers, two shapes — and FL2 is why

| Line | Live gauge | Profile |
|---|---|---|
| FL1 / FL3 | streamed | available |
| **FL2 standalone** | **`null`** — `FR-120` | **the value of record** |

`[TRP §7]` calls the FL2 empty state *"the single most likely thing to ship wrong."* This
endpoint is what makes Profile possible: on FL2 the trace is a **REST query over the
incoming spool's FL1 history**, not a live stream. See
[`FW-181`](FW-181.md).

> ⚠ **Profile must not be re-rendered or re-sampled by the live tick** — it is static.
> A paging contract that assumes a live cursor breaks that.

### 2.4 Thresholds are configuration, not code

Out-of-spec detection thresholds are **surfaced to the client as configuration**, so
Dashboard 3's N-consecutive-out-of-spec auto-prompt is tunable without a deploy. Bound through
[`FW-144`](FW-144.md) — ✅ built, and read per request from `IOptionsMonitor` rather than captured
once, which is what makes an `appsettings` edit take effect **without an app-pool recycle**.

⚠ **They ride THIS response** — `ActiveRunResponse.OutOfSpec` (`RunContracts.cs:132`, populated
`RunService.cs:214`), for the same one-round-trip reason as the order block: DB3 already makes
this call on load and on resume, and a separate endpoint for two numbers would be a second trip
on the trial's landing route. ⛔ **So `[API §4.7a]` owes this block too** (§2.1) — it is the
eleventh field of the widening, not a separate concern.

⛔ **Detection thresholds, NOT tolerance bands — do not merge the two.** The band (what counts as
out of spec at all) is `AlloyProperty`'s and is server-held; this is only **how many
already-judged readings in a row** raise the operator's prompt. `autoPromptEnabled` off
suppresses the **prompt**, never the verdict.

---

## 3. Build order — ✅ done except step 8

Recorded rather than planned. Steps 1–7 are in the working tree; **only step 8 is outstanding.**

1. ✅ The two `RunController` actions existed on [`FW-138`](FW-138.md)'s shell — endpoints **17**
   and **18** of `[API §3.2]`. `GetActiveRunAsync` and `GetGaugeTraceAsync` de-stubbed;
   **`GetRunWeldEventsAsync` left throwing** — `FW-166`'s (§1.1).
2. ✅ Dapper paging over the **built** `sp_GetGaugeTrace` — `ContextRepository.GetGaugeTraceAsync`,
   two result sets in one round trip, so a weld cannot land between the readings and their markers.
3. ✅ Active-run DTO populated, **`routeMode`** included (`RunService.cs:196`), and widened for the
   order block per `P-254` — ⛔ **the DTO half only**; see step 8.
4. ✅ Weld markers from `WeldEvent`.
5. ✅ Thresholds from configuration (§2.4).
6. ✅ **Any authenticated role may read** — bare `[Authorize]`, no policy, `RunController.cs:24`.
7. ✅ Pagination per `[API §1.5]` where the trace is paged — [`FW-138`](FW-138.md) owns the
   convention.
8. ⛔ **OUTSTANDING — widen `[API §4.7a]`** with the eleven fields of §2.1. Non-breaking under
   `[API §8]`. Until it lands the published contract and the built DTO disagree, and `FW-132`
   has no source to mirror from.

> ➕ **`P-258` binds this service and the plan did not say so.** *"No MediatR command; the service
> owns its unit of work"* — [`Orchestration.md`](Orchestration.md) names `RunService` explicitly,
> and the built code follows it: the controller calls `IRunService` directly. ⚠ **This matters
> because the repo offers the opposite precedent next door** — `FW-154`'s sibling read goes
> through MediatR (`GetLinesStatusQuery`, `P-61`). Do not "harmonise" the two; the difference is
> deliberate.

---

## 4. Decisions this plan makes

> `P-##` is continuous across the repository; `P-01`–`P-41` preceded `P-42` when it was minted on
> 15 Aug 2026, and **`P-254` was added by the 29 Aug re-review**. ⚠ **Do not read a next-free id
> off this page** — the pointer has moved twice since, and
> [`Orchestration.md`](Orchestration.md) owns it.

### `P-42` — read the order block through a view, and let the DTO carry nulls

`OI-33` leaves the `planning_routings` column mapping unknown, and this DTO must carry
customer, due date, tolerances, setup width/gauge, finish, OD min–max and weights out of it.

**Resolution, in two parts.** Surface the shared-schema read as a **view in `FlatWireDB`**,
per `[DBD §6.6]`'s established convention — so when `OI-33` closes, the mapping changes in
one object rather than at every call site. And make **every order field nullable in the DTO**,
rendering as *not available* rather than blank or zero.

Reason: this is the trial's landing route. A missing order field must degrade the screen, not
fail the request — and `Q18` (which order field carries coil min–max weight) is a second,
independent unknown in the same block. **`[TRP §6]` lists `Q18` as second-tier: it stops no
build.**

### `P-254` — the order block is a **contract addition**, made once and made nullable

`P-42` assumed the response shape existed and only its mapping was unknown. It does not (§2.1).

**So: widen `[API §4.7a]`'s `200` shape and `ActiveRunResponse` with the order fields, in one
pass, all nullable** — non-breaking under `[API §8]` (adding a response field), mirrored in the
TypeScript contract when `FW-132` authors it, and **served from the same view `P-42` establishes**.

⛔ **Do not serve the block by widening `orderId` into a nested object the contract does not
describe, and do not add a second call.** The first makes the built DTO and the published
contract disagree — the exact drift `[API §7]`'s stub-first rule exists to prevent; the second
undoes the one-round-trip decision AC 3 was written for.

⚠ **The trial can ship without the widening** — every field is nullable and the screen degrades to
*not available* — so this is a contract task to schedule, **not a blocker on the de-stub**.

> ⛔ **Status, 31 Aug 2026 — `P-254` landed by halves, which is the one outcome it forbade.** The
> **DTO half is built** (`RunContracts.cs:91-118`, `:132`); the **`[API §4.7a]` half is not**. *"In
> one pass"* was the whole point: a single-sided widening produces exactly the built-DTO ⇄
> published-contract disagreement the paragraph above rules out, only with the two sides swapped.
> **Closing it is §3 step 8**, and the field list is §2.1's — eleven, including `temper` and
> `outOfSpec`, neither of which this decision originally named.

---

## 5. Verification

**No automated tests** — `[TS §1.2]`. Verified in the QA0 walkthrough.

| Check | Expected |
|---|---|
| **De-stub, not a rebuild** | The controller is untouched and the contracts are **widened, not re-created**. ⚠ **Do not assert "two `NotImplementedException`s removed"** — `RunService.cs` went **6 throws → 1** and only two of the five are this story's; `FW-170` and `FW-149`/`FW-225` took the rest in the same pass (§1.1). `RunContracts.cs` **is** modified, by `P-254`'s DTO half |
| ⛔ **Contract ⇄ DTO parity** | `[API §4.7a]`'s `200` enumerates **every** field the built `ActiveRunResponse` returns. **FAILS TODAY by eleven** — §2.1. This is the one check the story does not yet pass |
| `GET /run/active?line=` | Active-run DTO with payoffs, weld events, components **and** the order block in **one** round trip |
| No active run | **`204 No Content`** — the contracted idle state; not a `404`, not an error, not an empty `200` |
| **`routeMode` populated** | Present and correct on FL3 hybrid runs — an unpopulated field is worse than an absent one *(`FW-181` `P-49`)* |
| `gaugetrace` paging | `from` / `to` / `resolution` honoured; Dapper, via `sp_GetGaugeTrace` |
| Weld markers | Sourced from `WeldEvent`; **empty array is legitimate** in trial scope, where no weld is captured |
| **FL2** | Profile served from the incoming spool's FL1 history; **static** |
| **`outOfSpec` served** | Present and **non-null** — the client always has a rule to apply. An `appsettings` edit changes it **without an app-pool recycle** (§2.4) |
| Authorization | Any authenticated role; anonymous → `401` |
| Order fields absent | DTO carries nulls, request still `200` *(`P-42`)* — the expected state until `OI-33` closes |

### 5.1 Acceptance criteria against the staged code — 31 Aug 2026

| AC | State |
|---|---|
| **1** active-run DTO — payoffs, weldEvents, components | ✅ `RunService.cs:123` |
| **2** paged `gaugetrace` via Dapper | ✅ `:248`, over `sp_GetGaugeTrace` |
| **3** order block, one round trip | ⚠ **shape ✅ / data null.** The ten fields are on the DTO and filled by `FillOrderBlockAsync` (`:693`), but `GetOrderContextAsync` is an `OI-33` seam returning `null`. **Met as designed** — `P-42` requires it degrade, not fail. ⛔ Uncontracted until §3 step 8 |
| **4** thresholds as configuration | ✅ `:214` |
| **5** weld markers from `WeldEvent` | ✅ empty array legitimate in trial scope |
| **6** any authenticated role may read | ✅ bare `[Authorize]`, `RunController.cs:24` — ⚠ **the inverse of [`FW-157`](FW-157.md)**, whose AC 7 *fails* on that same bare attribute because it needs a policy. Same code, opposite verdict; do not "fix" this one to match that one |

> ⚠ **Build the weld-marker layer even though it renders empty in the trial** (`[TRP §7]`).
> With no `WeldEvent` rows in scope the array is empty — *"a legitimate state, not a
> defect."* **Deleting the layer is rework when weld capture returns.**

---

## 6. Handoff

`FW-062` / `FW-163` (FE) render both. [`FW-181`](FW-181.md) binds the
Live/Profile toggle. `FW-165` (DB) supplies `sp_GetGaugeTrace`. `FW-204` (FE) is the minimal
landing route that calls `GET /run/active`.

⚠ **`FW-132` is a handoff this plan did not name, and §3 step 8 gates it.** It authors the
TypeScript mirror of these contracts, and `P-254` names it as the mirror site — so it now has
**eleven more fields** to carry. Mirroring from the built DTO while `[API §4.7a]` says something
else would put the drift into a third place.

---

## 7. Open items

| Item | Effect here |
|---|---|
| **`Q18`** *(blocker)* | Which order field carries coil min–max weight. Second-tier per `[TRP §6]` — **stops no build**. Surfaces as `coilWeightMinLb` / `coilWeightMaxLb`, null until it closes |
| **`OI-33`** | `planning_routings` columns unmapped — `P-42`. ✅ The seam is declared: `ContextRepository.GetOrderContextAsync` returns `null`, and closing `OI-33` changes **that body and no call site** |
| ⛔ **Contract behind the DTO** *(new 31 Aug 2026)* | `P-254` landed by halves. The built `ActiveRunResponse` returns **eleven fields `[API §4.7a]` does not describe** — the ten order fields plus `outOfSpec`. §3 step 8; §2.1 has the list |
| **`G51`** *(recorded here, not minted here)* | `gaugeTolerance` / `widthTolerance` are **single-±** on this response while `Q22` decided **min/max pairs**. ⚠ **`G51` is an existing register row** — it is the `SpcMeasurement.InSpec` computed column, and [`FW-245`](../../30-database/tasks/FW-245.md) **owns that half**. `[API §4.7a]`'s own note already cites it here. ⛔ **The API/DTO half has no owner and THREE sites**: `ActiveRunResponse.GaugeTolerance` / `.WidthTolerance`, and `GaugeTraceResponse`'s derived `UpperLimit` / `LowerLimit` (`RunService.cs:285-286`, whose comment says the same). Derived in one place so it moves with `FW-245` |
| **`G17`** | Cross-DB logical FKs, unenforced by design |
| **`G3`** | `RunReading` is the store this reads — ✅ **table half closed 26 Aug 2026** |
| **`G14`** | Footage is `DECIMAL(10,2)` on `RunReading` but `INT` on event tables — **a fractional footage does not round-trip through an event endpoint** |
