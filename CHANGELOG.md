# Flat Wire Planning — Change Log

**Project:** Flat Wire Mill Implementation
**Document Type:** Consolidated change log — the single home for document change history
**Last Updated:** August 13, 2026
**Status:** Active — append here, never to an individual document

---

## How this file works

**This is the only change log in the repository.** On 12 Aug 2026 the per-document
`## Change Log` sections were removed from all 63 documents that carried one and their
entries moved here **verbatim**. Do not add a `## Change Log` section to any document —
append a row to the relevant section below instead.

| Rule | Detail |
|---|---|
| **Where entries go** | Under the `##` heading naming the document they describe. Add the document's section if it has none yet, in repository path order. |
| **Cross-document changes** | Under [Repository-wide](#repository-wide), not repeated under each file touched. |
| **Entries are verbatim** | Dates, wording and column shapes are exactly as migrated. Several entries deliberately cite **superseded** numbering, values and filenames — that is the audit trail, not an error. Read each against the reading note in its section. |
| **Document headers still apply** | Each document keeps its **Project / Last Updated / Status** header block. Update `Last Updated` on the document *and* append the entry here. |

**Client-facing `## Document Change History` blocks are deliberately NOT here.** The
client-review specifications in `MVP-1/RequirementDocuments/` and
`MVP-2/RequirementDocuments/` keep their own `Version | Date | Description` block near the
top of the file, because it is **deliverable content** — it renders into the `.docx` the
client signs off against. Those record document *versions*; this file records the internal
audit trail. `RollAdjust.md` §6 *Change History* and the *Change History Panel* section of
the dashboard design reference are likewise untouched — the first is a numbered section of
a client deliverable, the second describes a screen.

> **Load-bearing content lives in this file.** Three things that other documents cite by
> name are change-log entries, and their citations now resolve here:
> the **`Q##` old→new renumbering map** (12 Aug 2026) and the **`[PLC]` v1.0 section and
> `PLC-Q##` mapping tables** (4 Aug 2026), both under
> [`Analysis/FlatWireOpenQuestions.md`](#analysisflatwireopenquestionsmd); and the
> **superseded table-count history** under
> [`LatestDocument/FlatWire_MasterSpecification.md`](#latestdocumentflatwire_masterspecificationmd).

---

## Index

| Document | Entries |
|---|---|
| [Repository-wide](#repository-wide) | 8 |
| [CLAUDE.md](#claudemd) | 1 |
| **Analysis** | |
| &nbsp;&nbsp;[Analysis/FlatWireDecidedQuestions.md](#analysisflatwiredecidedquestionsmd) | 6 |
| &nbsp;&nbsp;[Analysis/FlatWireEndToEndProcess.md](#analysisflatwireendtoendprocessmd) | 3 |
| &nbsp;&nbsp;[Analysis/FlatWireOpenQuestions.md](#analysisflatwireopenquestionsmd) | 54 |
| &nbsp;&nbsp;[Analysis/FlatWirePlan.md](#analysisflatwireplanmd) | 4 |
| &nbsp;&nbsp;[Analysis/FlatWireProcessWalkthrough.md](#analysisflatwireprocesswalkthroughmd) | 4 |
| &nbsp;&nbsp;[Analysis/FlatWireShopfloorDashboards.md](#analysisflatwireshopfloordashboardsmd) | 19 |
| **BaseDocuments** | |
| &nbsp;&nbsp;[BaseDocuments/ClientCall_2026-07-23_SyncPlan.md](#basedocumentsclientcall_2026-07-23_syncplanmd) | 2 |
| &nbsp;&nbsp;[BaseDocuments/ClientCall_2026-07-30_SyncPlan.md](#basedocumentsclientcall_2026-07-30_syncplanmd) | 3 |
| &nbsp;&nbsp;[BaseDocuments/ClientCall_2026-08-06_SyncPlan.md](#basedocumentsclientcall_2026-08-06_syncplanmd) | 5 |
| **LatestDocument** | |
| &nbsp;&nbsp;[LatestDocument/FlatWire_MasterSpecification.md](#latestdocumentflatwire_masterspecificationmd) | 13 |
| &nbsp;&nbsp;[LatestDocument/ProjectPlanPrompt.md](#latestdocumentprojectplanpromptmd) | 1 |
| **MVP-1** | |
| &nbsp;&nbsp;[MVP-1/DBChanges/GapAnalysis.md](#mvp-1dbchangesgapanalysismd) | 1 |
| &nbsp;&nbsp;[MVP-1/DBChanges/Schema/FlatWireSchema_Lookup.md](#mvp-1dbchangesschemaflatwireschema_lookupmd) | 5 |
| &nbsp;&nbsp;[MVP-1/DBChanges/Schema/FlatWireSchema_Mapping.md](#mvp-1dbchangesschemaflatwireschema_mappingmd) | 4 |
| &nbsp;&nbsp;[MVP-1/DBChanges/Schema/FlatWireSchema_Materials.md](#mvp-1dbchangesschemaflatwireschema_materialsmd) | 2 |
| &nbsp;&nbsp;[MVP-1/DBChanges/Schema/FlatWireSchema_QualityOutput.md](#mvp-1dbchangesschemaflatwireschema_qualityoutputmd) | 4 |
| &nbsp;&nbsp;[MVP-1/DBChanges/Schema/FlatWireSchema_Runs.md](#mvp-1dbchangesschemaflatwireschema_runsmd) | 7 |
| &nbsp;&nbsp;[MVP-1/ProjectPlan/APIContracts.md](#mvp-1projectplanapicontractsmd) | 8 |
| &nbsp;&nbsp;[MVP-1/ProjectPlan/CapacityAndEffortModel.md](#mvp-1projectplancapacityandeffortmodelmd) | 6 |
| &nbsp;&nbsp;[MVP-1/ProjectPlan/CheckinImplementationPlan.md](#mvp-1projectplancheckinimplementationplanmd) *(deleted)* | 3 |
| &nbsp;&nbsp;[MVP-1/ProjectPlan/CheckinImplementationPrompt.md](#mvp-1projectplancheckinimplementationpromptmd) *(deleted)* | 3 |
| &nbsp;&nbsp;[MVP-1/ProjectPlan/DevelopmentEffortModel.md](#mvp-1projectplandevelopmenteffortmodelmd) | 3 |
| &nbsp;&nbsp;[MVP-1/ProjectPlan/FlatWireJiraStories.md](#mvp-1projectplanflatwirejirastoriesmd) | 4 |
| &nbsp;&nbsp;[MVP-1/ProjectPlan/FlatWireTables.md](#mvp-1projectplanflatwiretablesmd) | 1 |
| &nbsp;&nbsp;[MVP-1/ProjectPlan/PLCTagImplementation.md](#mvp-1projectplanplctagimplementationmd) | 1 |
| &nbsp;&nbsp;[MVP-1/ProjectPlan/Development/StaffedSprintPlans.md](#mvp-1projectplandevelopmentstaffedsprintplansmd) | 2 |
| &nbsp;&nbsp;[MVP-1/ProjectPlan/Development/TrialRunPlan.md](#mvp-1projectplandevelopmenttrialrunplanmd) | 6 |
| &nbsp;&nbsp;[MVP-1/ProjectPlan/REVIEW.md](#mvp-1projectplanreviewmd) | 4 |
| &nbsp;&nbsp;[MVP-1/ProjectPlan/RodPreCheckinUxReviewPrompt.md](#mvp-1projectplanrodprecheckinuxreviewpromptmd) | 2 |
| &nbsp;&nbsp;[MVP-1/ProjectPlan/ShopfloorAndRealTimePlan.md](#mvp-1projectplanshopfloorandrealtimeplanmd) | 16 |
| &nbsp;&nbsp;[MVP-1/ProjectPlan/ShopfloorPlan/back-matter.md](#mvp-1projectplanshopfloorplanback-mattermd) | 4 |
| &nbsp;&nbsp;[MVP-1/ProjectPlan/ShopfloorPlan/phase-01-core-platform-setup.md](#mvp-1projectplanshopfloorplanphase-01-core-platform-setupmd) | 1 |
| &nbsp;&nbsp;[MVP-1/ProjectPlan/ShopfloorPlan/phase-12-yield-cost-ledger-scrap.md](#mvp-1projectplanshopfloorplanphase-12-yield-cost-ledger-scrapmd) | 1 |
| &nbsp;&nbsp;[MVP-1/ProjectPlan/TechStackRecommendation.md](#mvp-1projectplantechstackrecommendationmd) | 1 |
| &nbsp;&nbsp;[MVP-1/ProjectPlan/Tools/README.md](#mvp-1projectplantoolsreadmemd) | 6 |
| &nbsp;&nbsp;[MVP-1/ProjectPlan/Tools/build_coverage_matrix.py](#mvp-1projectplantoolsbuild_coverage_matrixpy) | 1 |
| &nbsp;&nbsp;[MVP-1/ProjectPlan/Tools/build_development_plan_xlsx.py](#mvp-1projectplantoolsbuild_development_plan_xlsxpy) | 2 |
| &nbsp;&nbsp;[MVP-1/ProjectPlan/Tools/DevelopmentPlanContent.md](#mvp-1projectplantoolsdevelopmentplancontentmd) | 2 |
| &nbsp;&nbsp;[MVP-1/ProjectPlan/Tools/build_trial_run_xlsx.py](#mvp-1projectplantoolsbuild_trial_run_xlsxpy) | 1 |
| &nbsp;&nbsp;[MVP-1/ProjectPlan/Tools/TrialRunContent.md](#mvp-1projectplantoolstrialruncontentmd) | 1 |
| &nbsp;&nbsp;[MVP-1/ProjectPlan/WeldEventPopupPlan.md](#mvp-1projectplanweldeventpopupplanmd) | 4 |
| &nbsp;&nbsp;[MVP-1/ProjectPlan/Development/YieldCostAndScrapStories.md](#mvp-1projectplanyieldcostandscrapjirastoriesmd) | 1 |
| &nbsp;&nbsp;[MVP-1/ProjectPlan/YieldCostAndScrapSheet.md](#mvp-1projectplanyieldcostandscrapsheetmd) | 3 |
| &nbsp;&nbsp;[MVP-1/ProjectPlan/00-README.md](#mvp-1projectplan00-readmemd) | 3 |
| &nbsp;&nbsp;[MVP-1/ProjectPlan/01-VisionAndScope.md](#mvp-1projectplan01-visionandscopemd) | 2 |
| &nbsp;&nbsp;[MVP-1/ProjectPlan/02-SRS.md](#mvp-1projectplan02-srsmd) | 7 |
| &nbsp;&nbsp;[MVP-1/ProjectPlan/03-HLD-and-ERDiagram.md](#mvp-1projectplan03-hld-and-erdiagrammd) | 5 |
| &nbsp;&nbsp;[MVP-1/ProjectPlan/04-APIContract.md](#mvp-1projectplan04-apicontractmd) | 3 |
| &nbsp;&nbsp;[MVP-1/ProjectPlan/05-SprintPlanAndBacklog.md](#mvp-1projectplan05-sprintplanandbacklogmd) | 3 |
| &nbsp;&nbsp;[MVP-1/ProjectPlan/06-TestPlanAndTestCases.md](#mvp-1projectplan06-testplanandtestcasesmd) | 5 |
| &nbsp;&nbsp;[MVP-1/ProjectPlan/07-DeploymentRunbookAndRollback.md](#mvp-1projectplan07-deploymentrunbookandrollbackmd) | 1 |
| &nbsp;&nbsp;[MVP-1/README.md](#mvp-1readmemd) | 3 |
| &nbsp;&nbsp;[MVP-1/RequirementDocuments/ActiveRunMonitor.md](#mvp-1requirementdocumentsactiverunmonitormd) | 1 |
| &nbsp;&nbsp;[MVP-1/RequirementDocuments/ClientQuestionsContent.md](#mvp-1requirementdocumentsclientquestionscontentmd) | 1 |
| &nbsp;&nbsp;[MVP-1/RequirementDocuments/DieChangeAndManagement.md](#mvp-1requirementdocumentsdiechangeandmanagementmd) | 4 |
| &nbsp;&nbsp;[MVP-1/RequirementDocuments/LineStatusOverview.md](#mvp-1requirementdocumentslinestatusoverviewmd) | 2 |
| &nbsp;&nbsp;[MVP-1/RequirementDocuments/OutputCoilCompletion.md](#mvp-1requirementdocumentsoutputcoilcompletionmd) | 3 |
| &nbsp;&nbsp;[MVP-1/RequirementDocuments/PartialRodReCheckin.md](#mvp-1requirementdocumentspartialrodrecheckinmd) | 4 |
| &nbsp;&nbsp;[MVP-1/RequirementDocuments/RocCheckin.md](#mvp-1requirementdocumentsroccheckinmd) | 5 |
| &nbsp;&nbsp;[MVP-1/RequirementDocuments/RodCheckout.md](#mvp-1requirementdocumentsrodcheckoutmd) | 3 |
| &nbsp;&nbsp;[MVP-1/RequirementDocuments/RodPreCheckin.md](#mvp-1requirementdocumentsrodprecheckinmd) | 6 |
| &nbsp;&nbsp;[MVP-1/RequirementDocuments/RollAdjust.md](#mvp-1requirementdocumentsrolladjustmd) | 1 |
| &nbsp;&nbsp;[MVP-1/RequirementDocuments/SPCCheckpoint.md](#mvp-1requirementdocumentsspccheckpointmd) | 3 |
| &nbsp;&nbsp;[MVP-1/RequirementDocuments/Spool.md](#mvp-1requirementdocumentsspoolmd) | 6 |
| &nbsp;&nbsp;[MVP-1/RequirementDocuments/SpoolCompletionNotification.md](#mvp-1requirementdocumentsspoolcompletionnotificationmd) | 3 |
| &nbsp;&nbsp;[MVP-1/RequirementDocuments/SpoolQueue.md](#mvp-1requirementdocumentsspoolqueuemd) | 2 |
| &nbsp;&nbsp;[MVP-1/RequirementDocuments/WeldEvent.md](#mvp-1requirementdocumentsweldeventmd) | 4 |
| &nbsp;&nbsp;[MVP-1/RequirementDocuments/WipRejection.md](#mvp-1requirementdocumentswiprejectionmd) | 1 |
| **MVP-2** | |
| &nbsp;&nbsp;[MVP-2/Analysis/FlatWireOpenQuestions-MVP2.md](#mvp-2analysisflatwireopenquestions-mvp2md) | 1 |
| &nbsp;&nbsp;[MVP-2/DBChanges/README.md](#mvp-2dbchangesreadmemd) | 2 |
| &nbsp;&nbsp;[MVP-2/DBChanges/Schema/FlatWireSchema_Schedule.md](#mvp-2dbchangesschemaflatwireschema_schedulemd) | 2 |
| &nbsp;&nbsp;[MVP-2/DevelopmentPlan/FlatWireJiraStories-MVP2.md](#mvp-2developmentplanflatwirejirastories-mvp2md) | 1 |
| &nbsp;&nbsp;[MVP-2/DevelopmentPlan/PassScheduleGenerationSpecPrompt.md](#mvp-2developmentplanpassschedulegenerationspecpromptmd) | 1 |
| &nbsp;&nbsp;[MVP-2/DevelopmentPlan/README.md](#mvp-2developmentplanreadmemd) | 1 |
| &nbsp;&nbsp;[MVP-2/DevelopmentPlan/ShopfloorPlan/phase-11-mvp2-shift-summary.md](#mvp-2developmentplanshopfloorplanphase-11-mvp2-shift-summarymd) | 1 |
| &nbsp;&nbsp;[MVP-2/DevelopmentPlan/ShopfloorPlan/phase-13-mvp2-die-management.md](#mvp-2developmentplanshopfloorplanphase-13-mvp2-die-managementmd) | 1 |
| &nbsp;&nbsp;[MVP-2/ProjectPlan/00-README.md](#mvp-2projectplan00-readmemd) | 1 |
| &nbsp;&nbsp;[MVP-2/ProjectPlan/02-SRS-MVP2.md](#mvp-2projectplan02-srs-mvp2md) | 1 |
| &nbsp;&nbsp;[MVP-2/ProjectPlan/04-APIContract-MVP2.md](#mvp-2projectplan04-apicontract-mvp2md) | 1 |
| &nbsp;&nbsp;[MVP-2/ProjectPlan/05-Backlog-MVP2.md](#mvp-2projectplan05-backlog-mvp2md) | 1 |
| &nbsp;&nbsp;[MVP-2/ProjectPlan/06-TestCases-MVP2.md](#mvp-2projectplan06-testcases-mvp2md) | 1 |
| &nbsp;&nbsp;[MVP-2/README.md](#mvp-2readmemd) | 2 |
| &nbsp;&nbsp;[MVP-2/RequirementDocuments/DieManagement.md](#mvp-2requirementdocumentsdiemanagementmd) | 1 |
| &nbsp;&nbsp;[MVP-2/RequirementDocuments/PassScheduleManagement.md](#mvp-2requirementdocumentspassschedulemanagementmd) | 4 |
| &nbsp;&nbsp;[MVP-2/RequirementDocuments/ShiftSummary.md](#mvp-2requirementdocumentsshiftsummarymd) | 1 |
---

## Repository-wide

Changes that span the repository rather than one document.

| Date | Changed By | Description |
|------|-----------|-------------|
| Aug 14, 2026 | Plan team | **The trial run is now a generated workbook — `Development/FlatWire_TrialRunPlan.xlsx`, ten sheets, 61 work items, 778 hours.** [`Tools/build_trial_run_xlsx.py`](MVP-1/ProjectPlan/Tools/build_trial_run_xlsx.py) is the repository's **fifth** script and **third** workbook builder, following the standing rule that each duplicates its helpers rather than importing them so a change to one guard cannot silently change another's. Sheets: *Read Me · Effort Summary · Effort by Phase and Discipline · Platform Detail · Work Items · Sprint Plan · Sprint Allocation · Blockers · Deferred Items · Removal Impact*. **Everything numeric is parsed from `[TRP]`**, titles from `[TB]`, and only the plain-language *what it delivers* per item is authored — in the new [`Tools/TrialRunContent.md`](MVP-1/ProjectPlan/Tools/TrialRunContent.md). Effort figures reach about twenty files in this repository, so a hand-kept second copy of the trial numbers was the likeliest way they would drift.<br><br>**It is the first generator to write outside `MVP-1/SRS/`, and that placement is the design.** That folder holds **leakage-guarded** client deliverables; this workbook carries story identifiers, hours and gap identifiers on purpose, so it is **internal, shared with the client**, and lives in `Development/`. Putting it in `MVP-1/SRS/` would place an unguarded file among guarded ones. Two further departures from the development-plan workbook are recorded in the script header so none is "fixed" back: **story identifiers are present and there is no leakage guard** (`FW-\d` is in *that* script's fatal `LEAKS` list, and it numbers its rows 1..N instead), and **effort is in hours, not days**.<br><br>**An abbreviation guard takes the leakage guard's place, on client direction that no abbreviation appear in any cell.** The reason it had to be a build-time transformation rather than a style note is that `[TRP]` writes `FE`/`BE`/`DB`/`RT`/`T1`/`1A`/`DB5`/`FL2` freely as prose, **a filtered spreadsheet column has no surrounding sentence, and `DB` is genuinely ambiguous — it is both the Database discipline and the Dashboard prefix.** A `LABELS` map expands every abbreviation at write time; a guard refuses the build if a bare one survives. Two properties keep the expansion safe: **single pass** over one longest-first alternation, so a replacement's output cannot be re-matched; and **no replacement contains its own key**, which is what makes it idempotent and why phrase entries exist. The guard's token set is **deliberately wider** than the expander's — `EF`, `DI`, `PWA` and `JWT` appear only inside phrases, so a bare one in new prose is flagged rather than expanded into something clumsy. `Phase 1A` and `Sprint 1` are exempt as identifiers, and code identifiers are untouched because `\bPLC\b` does not match `PLCTagService`.<br><br>**Five fatal guards, and every one was proved live by breaking it**: reconciliation (*"block table: computed 779, the plan prints 778"*), coverage, title resolution, drift, and abbreviation. Each run named the offending location and left no workbook on disk. Two findings worth keeping: the tables' **footer rows are excluded from their own sums on purpose** — counting them double-counts the total, which is why testing reconciliation means changing a *data* row; and injecting a bare `RT` did **not** trip the guard because the expander correctly handled it, so the real test uses a token the guard covers and the expander deliberately does not. |
| Aug 14, 2026 | Plan team | **The client requested six screens for a trial run finishing 30 Sep 2026, and scoping them found a 26-requirement subsystem priced as a 4 h story.** The request — DB2 rod check-in · DB3 active run FL1 · DB6 SPC checkpoint · DB8 WIP rejection · DB5 spool check-in · DB3 active run FL2, with **UAT signed off inside the window** and PLC commissioning moving to October — is planned in the new [`Development/TrialRunPlan.md`](MVP-1/ProjectPlan/Development/TrialRunPlan.md) `[TRP]`: **778 h, four sprints from 17 Aug, feature-complete 18 Sep at five developers for T1 and four thereafter.** It is a **scoped subset of `[SSP]`, not a replacement**; `[SSP]` remains the staffed plan for all of MVP-1 development.<br><br>**The headline is that this is a platform problem, not a screen problem.** The six screens are **88 h** of Angular work; **Phase 1 — not started, its hard gate 14 Aug and not met — is 423 h, 54 % of the trial.** Cutting screens is the weakest lever available; shortening Phase 1 is the strongest, and it is the one place extra people convert into calendar because 1A/1B/1C parallelise while everything after them is the sequential `4→5→6→7` chain.<br><br>**Two screens were then removed on client direction the same day — `DB1` Line Status and `DB2A` Pre-Check-in + weld capture, −107 h — and neither was a clean subtraction.** `[TRP §1.5]` records what each takes with it, because the consequences are decisions rather than arithmetic. **DB1 (−42 h, +5 h back):** every one of the six mockups' back button targeted `dashboard_1_line_status.html`, so a **minimal landing route** replaces it, reusing `GET /run/active` and needing no new endpoint; `FR-079a`'s *Acknowledge → DB2A* becomes *→ DB3*, which `OI-109` had left unconfirmed anyway; the SPC dialog's **read-only mode loses its only caller**; and **`AlertRaised`/`AlertCleared` lose their only consumer**, so the WIP rejection's supervisor alert has no visible destination and is verifiable only on the wire — the same class as `G31`. Two things in the old Phase 3 row are **not** DB1's and stay: the reconnect banner (`FR-119`/`NFR006`, required on every screen) and the `FlatWireRun(LineId, Status)` index. **The real-time backbone is untouched** — hub, OPC ingest and broadcast loop are Phase 1B, and reading *"Phase 3 removed"* as *"real-time removed"* is wrong. **DB2A + weld (−70 h):** it takes **two blockers out of trial scope** — **`G21`**, which blocked the Phase-4 schema freeze and is a defect *in `RodStaging`*, and **`G26`**, the cross-phase weld write — both of which **stay open for MVP-1**. What it costs is larger: **no weld markers on either gauge chart** (build the layer, it renders empty — an empty `weldMarkers[]` is a legitimate state, and deleting it is rework), **a single-rod spool only**, and therefore **no multi-rod genealogy and no exercise of the `CoilTraceability` chain the welding-wire customer certificates are produced from** (`NFR012`). **75 test cases leave trial scope, 25 of them the suite `[TCS §5.6]` heads *"the contractual suite"*.** That is the largest thing the removals give up and `[TRP §9]` says so.<br><br>**The position improved but the shape did not.** Three developers now finish development around **1 Oct** rather than 7–9 Oct, and **one additional developer very nearly closes the whole trial** — 4 × 24 days = 768 h against 778, ten hours short — where before it was 117 h short. Phase 1 is still 54 % of the work, UAT still cannot overlap feature work (`[SP §1.4]`), and neither removal touched the platform. The recommended shape carries **70 h of slack**, the first version of this plan with real margin.<br><br>**Gap `G37` is the substantive finding, and it was found by asking what DB5 checks in.** `FR-130`–`FR-155` specify FL1 spool completion in 26 requirements with **25 test cases (`TC-160`–`TC-184`, eleven P1)**, an owning specification, two hub events, and a **built mockup component loaded at `dashboard_3_active_run.html:1396`** — so it is inside the approved mockup of a screen the client explicitly asked for, not adjacent scope. The only costed work was **`FW-N02`, 4 h RT**. Re-priced on `[CE §2]`'s card at **98 h** and minted as **`FW-202`**; `FW-N02` retains Part A (`FR-130`–`FR-136`), which is `Should` and explicitly advisory. **The consequence that stops a demo:** `phase-05` routes *Complete Run* to `POST /coil/complete`, which is Phase 9's FL2 **coil** endpoint — **nothing in the plan writes the FL1 `Spool` row**, so DB5 has nothing to check in and the FL1→FL2 journey cannot be demonstrated. Part B is also the harder half: a server-owned prompt state machine (edge-triggered once per stop, configurable dwell, weight latched at the PLC stop timestamp, surviving a refresh) plus a scale-vs-calculated weight basis whose ±2 % override must **never** disable commit.<br><br>**Two consequential edits followed.** `[SIG]` **promoted `SpoolCompletionPromptDue` and `SpoolCompletionPromptResolved` into the published hub contract** as events 11 and 12 — they had sat in §5.5 marked *"not yet in the published contract"* while `FR-144` made durability a requirement **on the transport itself**; `PP-04`'s count is restated 10 → 12 and `OI-32` is half-closed. And **`FW-202`'s hours are deliberately kept out of Phase 8's published 118 h** and out of `[CE §3b]`, with an explicit instruction not to fold them in — effort figures propagate to roughly twenty files, so new scope is restated additively, never edited in place.<br><br>**One correction made in the open, twice.** `FW-202` was first priced at 52 h and DB2A+weld at ~24 h; pricing each deliverable against the rate card gave **67 h** and **64 h**. The `FW-202` re-estimate consumed **half of T3's buffer (32 h → 17 h)**, which is recorded in `[TRP §2.3]` as the reason both remaining reserves are now treated as date risk rather than absorbable — and as the plan's own worked example of `[DE §7]`: the retention factors assume a settled specification. **No published per-phase hour, phase total or MVP-1 total was changed by this pass.** |
| Aug 13, 2026 | Plan team | **`MVP-1/ProjectPlan/` is now the single source of truth for development, testing and deployment** — phase C, the last. `RequirementDocuments/`, `Mockups/` and `DBChanges/` were absorbed and no longer exist as siblings; **`MVP-1/` is now `ProjectPlan/` + `README.md` + `SRS/`**. The 13 screen specifications are `Business/Screens/`, `PLCTagSpecification.md` sits beside `PLCCommunication.md` in `Architecture/`, the 31 mockups are `Frontend/Mockups/` **flat and intact** (~90 bare-filename asset references make any subfoldering an all-at-once break), and the DDL is `Database/Schema/SQL/`.<br><br>**The folder is complete but not supreme.** Everything needed to build, test and ship MVP-1 is inside it; `FlatWire_MasterSpecification.md` remains the arbiter, and **where the two disagree the master specification wins and this folder is corrected up to it**. That is a standing re-derivation obligation nothing enforces — stated in `README.md` and recorded as a risk.<br><br>**Proved rather than asserted: the schema was torn down, deployed and re-run from its new location**, returning **25 tables · 33 FKs · 1 procedure · 1 trigger** with zero errors on the second run. The SQLCMD `:r` includes survived the move, and the counts confirm the ruling that `[DEP §4.2]` had been **rejecting correct deployments** by asserting 27 and instructing the deployer to stop on 25.<br><br>**Two cross-scope dependencies that broke the SSoT claim were closed.** The **Operations Manager role** existed only in `MVP-2/RequirementDocuments/PassScheduleManagement.md` §3.3–§3.4 while `FR-212` enforces it on **DB11 Roll Adjust, an MVP-1 screen** — copied into `[SEC §8.9]`. The **`.docx` branding template** was read from `MVP-2/SRS/`; copied to `Tools/template.docx`, which `CLAUDE.md` had warned could be deleted as inert MVP-2 content and break the MVP-1 renderer. **This is the one deliberate duplicate the work creates**, and it is binary, so no diff will show it drifting.<br><br>**27 cross-scope mockup references were rewritten** — 19 inbound from all five MVP-2 screens, which load their stylesheet, app bar and fit script from MVP-1, plus 5 outbound and the topbar's own tile targets, which resolve from `script.src` rather than the host page. None is visible to a link checker and all fail silently. **Five MVP-2 SQL scripts** carried the MVP-1 path in run-order comments; also rewritten.<br><br>**The ER documentation was absorbed into `[DBD]` and deleted**, correcting the six defects `GapAnalysis.md` **E1** records rather than relocating them; only its query patterns (§6.10) and build order (§6.11) were genuinely new. **E2's corrections landed on `FlatWireSchema_Mapping.md`**: `RodStaging` and `PayoffPosition` were **missing from the table inventory entirely**, the total was wrong twice over, "43 FKs as built" was the full-design figure against a script that builds 33, and `RodCheckout` was missing **Mode P**. The **alloy reference data** — MVP-1 seed data that had been sitting inside an MVP-2 screen section in `Analysis/` — is now `Database/ReferenceData.md`, with its `Spring-back factor` column flagged per master spec §10.5.<br><br>**Link check: 0 unresolved across 1,168 links**, against 54 when the work began. |
| Aug 13, 2026 | Plan team | **`MVP-1/ProjectPlan/` was restructured into eight subject folders** — phase B. `Business/` · `Architecture/` · `Database/` · `Backend/` · `Frontend/` · `Development/` · `Testing/` · `Operations/`, plus `Tools/`, the workspace file and **`README.md`, which is now the only map of the set**.<br><br>**The seven numbered documents were split by section, and section numbers were preserved exactly.** `BusinessRequirements.md` opens its requirements at §5; `DatabaseDesign.md` numbers the data model §6; `APIs.md` runs §1–§4 then §7–§10. Numbering inside a file is **deliberately non-contiguous**, and that is what kept **every `§n` citation in the repository resolving** — the migration reduced to a shortcode substitution routed by section number. A per-source audit confirmed **no section number was lost** (80 → 108, 60 → 82, 64 → 75, 36 → 36, 62 → 62, 40 → 40 across their destinations), and a coverage audit refused to run while any top-level section was claimed by no destination. **Never renumber a section to close a gap.**<br><br>**Documents are now cited by shortcode**, declared in each header. `[SRS]`, `[HLD]`, `[TP]` and `[DR]` are retired and **342 citations were rerouted by section** — `[SRS §5.14]` → `[REQ §5.14]`, `[HLD §6.3]` → `[DBD §6.3]`. Three codes were chosen to avoid live collisions: **`[SIG]` not `[RT]`** and **`[DBD]` not `[DB]`**, because `RT` and `DB` are the real-time and database *delivery-stream* codes and `DB##` is the dashboard numbering; **`[GAP]` not `[G]`**, because `G##` is the gap-id format. The `**Companion documents:**` header line, which listed the other six documents, **does not scale to thirty and was replaced** by `**Shortcode:**` + `**Part of:**` on every file.<br><br>**Three files in `ShopfloorPlan/` were not phase specs and were dissolved.** `00-foundations.md` — **all four of its sections duplicated a ProjectPlan section**, verified before deletion: §0.2's reference map and its six numbered decisions were already `[ARC §2.2]` and `[ARC §13.1]` `D-01`–`D-08` in more detail; §0.3's domain cheat-sheet was already `[BR §3]`, which additionally carries the line-state vocabulary and the FL1-no-edger rule; §0.4 was `[SIG §4]`; §0.5 was `[API §7]`, which names the real environment flag. **Nothing was migrated** — 119 citations were rerouted instead. `back-matter.md`'s **`G1`–`G36` register was promoted to `Development/GapsRegister.md`** as a first-class document, its dependency mapping went to `[SP §6.5]`, its roadmap to `[RM]`, its Appendix B to `[SCR]`, and Appendix C was deleted as a self-declared derived view. The `phase-01` roll-up index went too — it declared itself "not the build spec". The **15 phase specs** keep their filenames, because SQL comments cite them.<br><br>`PLCTagImplementation.md` was absorbed into `Architecture/PLCCommunication.md`; **the anti-drift rule now covers the whole file and was verified — zero tag path strings.** `build_coverage_matrix.py` was repointed and still exits 0 on identical figures. **Link check: 0 unresolved across 1,160 links** (54 before this work began), 1 dead anchor, pre-existing. |
| Aug 13, 2026 | Plan team | **`MVP-1/DevelopmentPlan/` was consolidated into `MVP-1/ProjectPlan/` and no longer exists** — phase A of making `ProjectPlan/` the single home for development, testing and deployment material. The two folders sat at equal depth under `MVP-1/`, so the move itself preserved every outbound relative link, the `../../../MVP-2/` links from `ShopfloorPlan/`, the three `Tools/*.py` `ROOT` calculations and the `.code-workspace` paths; **108 inbound links and 9 redundant self-referential forms were rewritten**.<br><br>**Four April-dated documents were absorbed into the documents that own their subjects and deleted** — closing the "split-brain by design" convention: **`APIContracts.md` → [04-APIContract.md](MVP-1/ProjectPlan/Backend/APIs.md)**, which gained four MVP-1 endpoint sections it had *indexed but never specified* (`POST /staging/rod/unstage` §4.5a, `POST /checkin/spool` §4.6a, `GET /spools` §4.6b, `GET /run/active` §4.7a) plus worked request/response examples on 16 endpoints; **`FlatWireJiraStories.md` → [05-SprintPlanAndBacklog.md](MVP-1/ProjectPlan/Development/SprintPlan.md)** §4/§6/§7/§8, leaving **one** backlog of 116 stories — 05's section *numbers* were preserved because `[SP §1]`, `§1.4`, `§2`, `§4`, `§6`, `§7`, `§10`, `§10.3` and `§11` are all cited elsewhere, and §1/§3/§5/§10.2 became pointers rather than four more restatements of the capacity model; **`TechStackRecommendation.md` → [03-HLD-and-ERDiagram.md](MVP-1/ProjectPlan/Architecture/Architecture.md) §14**, carrying only what §1–§13 lacked; **`FlatWireTables.md` → [FlatWireSchema_Mapping.md](MVP-1/ProjectPlan/Database/Schema/FlatWireSchema_Mapping.md)**'s appendix, retained because it is the **only** inventory of the legacy `FlatLineProcessing`/`FlatLineSetup` columns and `OI-31`/`G8` record that no legacy migration deliverable exists. `WeldEventPopupPlan.md`'s `Q-W#` register moved to [REVIEW.md](MVP-1/ProjectPlan/Development/REVIEW.md)'s appendix — **`Q-W2` is open and that document stated it exists nowhere else.**<br><br>**Seven reconciliation rulings, all counted rather than quoted** (comments stripped from the scripts): the **MVP-1 build is 25 tables · 33 FKs · 41 index statements · 1 procedure · 1 trigger**, against a full design of 28 tables / 43 FKs. ⚠ **[07-DeploymentRunbookAndRollback.md](MVP-1/ProjectPlan/Operations/Deployment.md) §4.2 asserted 27 tables and told the deployer to STOP on 25 — as written it rejected a correct deployment.** `03` §6.2 said "28 tables" above a table summing to 27 and omitted `Dancer`, which `01_Lookup` creates. `PP-01`'s index count is **41**, not 44/46/"40+1" — four sources tabulated with what each was actually measuring, since a *deployed* database reports more than script 07 creates. `02-SRS` claimed 366 requirements and `00-README` 363; the document has **279 rows, 263 in MVP-1 scope**, and both older figures predate the MVP-2 split and were unreproducible. Master spec: 9 hub events → **10** (`PP-04`). Roadmap table: Phase 5 **221 h → 154 h**. `00-foundations` decision 3: 27/24 → **28/25**, the figure `[HLD §6.3]` had already flagged as the stale side.<br><br>**`REVIEW.md` deliberately keeps the old file names** and carries an old→new map in its header: its findings are statements about those documents *as they stood*, so substituting the new names would have made it assert that `04-APIContract.md` carries the defects its own §2.3 corrects. Same convention as this file. **Verified:** no `FW-###` story id lost, `build_coverage_matrix.py` still exits 0, and the link check ended **better than baseline** — 51 unresolved against 54 before the work, 1 dead anchor against 2. |
| Aug 13, 2026 | Plan team | **The backlog's authority moved, and eight documents were repaired in the same pass.** [05-SprintPlanAndBacklog.md](MVP-1/ProjectPlan/Development/SprintPlan.md) was rewritten as the sprint-wise MVP-1 backlog (**116 stories / 3,292 h** — see its own section) and promoted from *"a points cross-check, not a schedule"* to **authoritative**. That promotion has blast radius, and the ripple was treated as part of the work rather than as follow-up. **`05-SprintPlanAndBacklog.md` gives up §7/§7.2/§7.3** — its 44 shopfloor rows and twelve `[NEW]` `FW-N##` stories — and **§4.2's `S0`–`S4` cadence is marked retired**; both carry banners, and **the rows are retained, not deleted**, because §11's `FR-###` coverage matrix keys on them. **§11 itself stays there and still resolves**, since story ids are frozen. **`back-matter.md` Appendix C became a derived view**: correct for the 45 rows it maps, explicitly not complete, and marked *do not extend*. **`G4` stays closed and is closed harder** — the rewrite proves coverage at source by walking every phase's `Stories:` trailer, which is what makes `REVIEW.md` #57's class of defect (a live story reaching no summary) structurally impossible rather than merely fixed. **`CapacityAndEffortModel.md` keeps its §3 cross-check but loses its denominator**: the h/point ratios are marked historical with an instruction not to re-derive them, and **its finding 1 is recorded as closed by the rewrite** — Phase 1 went from seven database-only stories to 33. Its Related-Documents row now describes a document that **consumes** the model rather than one that offers a rival estimate. **`YieldCostAndScrapJiraStories.md` reduced to a pointer** after its four story bodies were absorbed; **`YieldCostAndScrapSheet.md` is untouched and remains the Phase-12 hours authority**, its 33/49/28/67 ladder split still reconciling on both axes. **`FlatWireJiraStories-MVP2.md` keeps its own point values** — MVP-2 was not re-derived — but is marked no longer comparable with MVP-1's sizing, and the `147 + 11 + 31 = 189` reconciliation is retired. **`ShopfloorAndRealTimePlan.md` §0.1 and `CLAUDE.md`** both still described *"12 epics / 58 stories / 184-point cross-check"*, stale on every figure; both now describe the current backlog. **`REVIEW.md` #33, #51 and #57 were re-closed on stronger grounds and #51 marked moot** — it concerned a wrong epic point total, and there are no longer epics or points to be wrong about. **The convention on retired figures is the reusable part: nothing was deleted to make the new numbers true.** Every superseded total — 147, 189, 31, 11, 184, 220, 224, 226, 231, the per-epic split and the retired epic ids — is preserved in the backlog's *Appendix A*, so a citation written against any of them still resolves. |
| Aug 13, 2026 | Clean-up | **The two check-in documents deleted, and their 26 inbound citations repaired across 17 files — the reversal of the decision recorded in the entry below.** Earlier the same day both were *reduced to records* precisely because deleting them would orphan live traceability; on the request to remove them outright, the orphaning was fixed rather than accepted. **The method is the reusable part: three facts were cited *from* those documents rather than *about* them, and each was moved to a document that owns the rule before the source was removed.** The **stub-first delivery contract** became `ShopfloorPlan/00-foundations.md` **§0.5** — new, and stated more completely than the original, since it now also names the DI-level `useStub` swap, the DB seed as the one canonical fixture set, the switchover criterion, and the fact that the check-in stub routes around `OQ-3`/`OQ-14`/`OQ-15` on a tracked de-stub debt. The **`0.003″` ovality datum** was restated at all four *"supersedes the hard-coded value"* sites as **per-alloy reference data, not a constant**, with `AlloyProperty.RodOvalityMaxIn` named as its only home — the provenance pointer was what depended on the file, not the value. **`G14`** moved into `back-matter.md`'s register row, which now carries the *"every check-in insert fails as specified"* statement, and `REVIEW.md` #6 was rewritten from *"inside one doc"* to the live **DDL-four-NOT-NULL vs contract-three** form. **`G14` remains open and still blocks Phase 4.** The remaining citations were converted to past tense naming the deleted document (`--fw-*`/`G18`, the bare-`ControllerBase` finding, the `FW-S3-###` ids, the DB5 fixture and inline-SVG mockup notes, the master spec §8.2 and `03-HLD` §warnings, both Appendix A.3 rows struck), so the record of *what those documents got wrong* survives — the instructions are still in git history and in any April copy, which is the reason to keep saying so. **Files touched: 17.** `ClientCall_2026-07-30_SyncPlan.md` was deliberately not swept, on the same reasoning that excludes it from the numbering sweeps: it is a dated record of what was written that day. **Zero dangling links.** |
| Aug 13, 2026 | Clean-up | **`MVP-1/DevelopmentPlan/` cleaned of irrelevant content — 12,700 words removed, about 11 % of the folder, with no requirement, hours figure or decision lost.** Four documents accounted for nearly all of it: three **reduced to records** and one **deleted** (see their own sections). **The headline finding is that three of the four could not simply be deleted.** A reference map run first found **50 inbound references across 18 files, nine of them outside this folder** — the master specification, `02-SRS.md`, `03-HLD-and-ERDiagram.md`, `04-APIContract.md`, `FlatWireSchema_Lookup.md`, `Analysis/FlatWireOpenQuestions.md`, `ProjectPlanPrompt.md`, the 30 Jul client-call ledger and `MVP-2/DevelopmentPlan/README.md` — and several cite those documents **as evidence**, not merely as further reading: the ovality datum `RodOvalityMaxIn` supersedes, `G18`'s stale-token evidence, the bare-`ControllerBase` finding, the stub-first delivery model, and gaps `G25`/`G26`/`G28`. Deleting them would have orphaned live traceability in governing documents or forced a rewrite of nine files outside the requested scope; in every case the **body** was the irrelevant part while the **file's existence** was load-bearing, so the bodies went and the cited facts stayed. **Two things were nearly lost and are now recorded:** `Q-W2`, a live `Medium` open question with no home outside `WeldEventPopupPlan.md`, and the per-stream QA-contingency correction, which had no entry in this log. **Two of the four requested categories turned out to be near-empty, and no deletions were manufactured to fill them.** *Out-of-scope residue*: the MVP-2 and upstream mentions that remain are **deliberate scope markers** added by the 11 Aug split and the 13 Aug backlog reduction — removing them would restore the ambiguity they were added to fix. *Duplicated content*: phase files barely restate `00-foundations.md`; the real defect there is the opposite one, that **13 of 16 carry no `reference context (do not restate)` pointer at all**, which is an addition and so out of scope here. **The in-document history sweep was also smaller than the marker count implied** — of 471 candidate markers, most are either **semantic state** (`~~POST /staging/rod/mark-welded~~ RETIRED`, `~~422~~ to 201`, `~~2~~ to Phase 2`), which prevents implementing the wrong thing, or **rationale for the current rule**, which prevents reverting to the old one. Only `CapacityAndEffortModel.md` carried substantial pure narrative. **`REVIEW.md`'s findings and `back-matter.md`'s gap register were deliberately left intact** — dated history is their content, not clutter in it. |
| Aug 12, 2026 | Analysis team | **One question-numbering scheme, repo-wide — 630 citations rewritten across 83 files.** The third renumbering of the open register (`Q1`–`Q33`, after 23 questions were withdrawn as ours to answer rather than the client's) was propagated with a sweep that, unlike the two before it, **included the bare `Q##` form**. That is what repaired the drift the earlier passes left: they rewrote only the canonical `OQ-##` form outside the registers, stranding ~200 bare citations in ~55 files on two older schemes — every client-facing specification in `MVP-1/RequirementDocuments/`, the DB2A/DB5/DB5A/DB8 mockups, four schema and DDL files, and several plan documents. Each was resolved **by subject** rather than by arithmetic, because the first renumbering was a reassignment in document order and no single offset recovers it. **Two corruptions caused by the earlier sweeps were undone** — `Q4 2026`, a calendar quarter, had become `Q51` in four places, and `PSG-Q34` had become `Q17`. **Exclusions, both deliberate:** this file, whose entries keep their original numbers as the audit trail, and `ClientCall_2026-07-30_SyncPlan.md`, verified old-numbering and never swept. Detail, including all three maps, under [`Analysis/FlatWireOpenQuestions.md`](#analysisflatwireopenquestionsmd). |
| Aug 12, 2026 | Documentation owner | **Change logs consolidated into this file.** The per-document `## Change Log` section was removed from all **63** documents that carried one and every entry moved here verbatim, grouped by owning document; the master specification's `## Appendix B — Change Log` moved with it and its table-of-contents entry was removed. **No entry text, date or author was altered, and nothing was dropped** — including entries that cite pre-renumbering `Q##` numbers, retired component names and deleted filenames. The three load-bearing entries called out above (the `Q##` renumbering map, the `[PLC]` v1.0 section/`PLC-Q##` mapping tables, the table-count history) moved with their sections, and the citations pointing at them were repointed here. Client-facing `## Document Change History` blocks were **left in place** as deliverable content. The `Last Updated` header of the 63 documents was **not** touched: removing a change log changes no requirement, and bumping 63 dates would misreport 63 content revisions. |
| Aug 13, 2026 | Gap remediation | **A propagation-discipline sweep: three structural changes had each left their own checklist unfinished.** The 11 Aug MVP-1/MVP-2 split reached `05-SprintPlanAndBacklog.md` and `06-TestPlanAndTestCases.md` but **not `back-matter.md`**, whose Appendix C still listed six MVP-2 stories as *"In scope"* and closed on a combined-scope coverage figure. The 1 Aug Dashboard-4 retirement propagated everywhere **except step S7**, leaving `phase-06` citing gap **`G26`** against no register entry for twelve days. The 12 Aug questions-workbook toolchain (`ClientQuestionsContent.md` → `build_questions_xlsx.py` → `FlatWire_ClientQuestions.xlsx`) entered **no inventory at all** — not `MVP-1/README.md`, not `CLAUDE.md`, not `Tools/README.md`. Each is corrected under its own document below. **`G25` is reused**, which the register's own note authorised (*"the next free gap ID remains G25"*): the weld-history gap that first held the id was withdrawn on 1 Aug 2026 **before it was ever registered**, so no citation to the old meaning exists — but any `G25` reference dated on or before 12 Aug 2026 means the withdrawn weld-history gap, not the coverage gap. Also added: a repository **`.gitignore`** (there was none) covering `__pycache__/`, `*.pyc` and the Word lock artifacts, and `Tools/__pycache__/build_questions_xlsx.cpython-314.pyc` was untracked. **Generated client deliverables are deliberately still tracked** — the rule is *edit the source and re-render*, not *do not track the render*. |
| Aug 13, 2026 | Reconcile-up | **The 26 Jul 2026 roadmap rewrite was finally propagated into `MVP-1/DevelopmentPlan/`.** Four documents dated 29–30 April had never been reconciled with it — they still carried the *"May–June 2026 / Trial Jul 1 / Production Aug 1"* timeline and a 5-sprint delivery model against the roadmap's 14 phases and 17 Aug – 30 Sep window, and `REVIEW.md` recorded that most of its own findings descended from that one un-propagated split. **A second gap sat on top:** the 11 Aug MVP-1/MVP-2 split had reached almost none of the folder — nine of twelve top-level documents contained **zero** occurrences of "MVP-2", including the API contract carrying 16 `/passschedule` references now owned by another track. All four April documents are now scope-marked, re-baselined and banner-ed with what supersedes them; a **sprint→phase crosswalk** makes the old `S1`–`S5` and `FW-S3-###` tags resolvable. **Three instructions in `REVIEW.md`'s own worklist were found to be wrong and were struck rather than executed** — dropping the `Rod` table (superseded by `D-04`; `G12` closed on it), fixing the `/passschedule/generate` example (out of MVP-1 scope), and a stale table count — and a fourth figure, #51's story-point correction, was overtaken by `FW-124`. Six new findings are recorded as `REVIEW.md` **Tier 8**. |
---

## [CLAUDE.md](CLAUDE.md)

| Date | Changed By | Description |
|------|-----------|-------------|
| Aug 13, 2026 | Gap remediation | **Four inventory statements corrected against the tree.** `MVP-1/RequirementDocuments/` is **17 files, not 16** — `ClientQuestionsContent.md` arrived 12 Aug 2026 and entered no inventory, so it is now described as the **third** non-specification in the folder (with `Spool.md` and `PartialRodReCheckin.md`) and its role as the sole authored source of the questions-workbook prose is stated, along with the strict structure-from-registers / prose-from-content split and the four fatal build guards. `MVP-1/SRS/` is **3 deliverables, not 2** — `FlatWire_ClientQuestions.xlsx` is generated output like `PLCTagSpecification.docx`, and the row now says which two of the three are generated. `MVP-1/DevelopmentPlan/` holds **18 phase files, not 16**. The **`MVP-2/` row listed three subfolders where the tree has seven** — `Analysis/`, `DBChanges/`, `DevelopmentPlan/` and `ProjectPlan/` were missing, the last being where the deferred backlog rows and test cases live that MVP-1's own §11 and §10 cite. **The schema deployment block still pointed at `LatestDocument\DBChanges\Schema\SQL`**, a path that stopped existing on 11 Aug 2026 (`MVP-1/DBChanges/GapAnalysis.md` **E7**); it now matches the path in `FlatWire_DDL_RunAll.sql`'s own header. |

---

## [Analysis/FlatWireDecidedQuestions.md](Analysis/FlatWireDecidedQuestions.md)

| Date | Changed By | Description |
|------|-----------|-------------|
| Aug 11, 2026 | Analysis team | **File created by splitting the question register.** [FlatWireOpenQuestions.md](Analysis/FlatWireOpenQuestions.md) had grown to hold both open and closed items; it now carries **open items only** and this file carries the **24 decided questions with their answers**, verbatim. **The two files share one numbering space and nothing was renumbered**, so every inbound `OQ-##` reference still resolves against whichever file holds its question. Ten partly-answered questions (`Q30`, `Q31`, `Q47`, `Q49`, `Q57`, `Q60`, `Q66`, `Q71`, `Q72`, `Q74`) stayed in the open register **with their decided portions**, because they are not closed — that is the one place a partial answer is not in this file. Pre-decision deliberation was **not** carried across; where a decision left a genuine residual it is stated and points at the open register (`Q67`→ the `FlatwireQueue`/reqsum writes, `Q75`→**Q81**, `Q77`→`WLD011`, `Q65`→**OI-25**). |
| Aug 11, 2026 | Analysis team | **`ClientQuestionsEmail.md` merged in and removed.** The 27 Apr 2026 outbound e-mail was the request for these decisions; it is superseded as a live document (its 57-question count, its July 1 trial / August 1 production deadlines and its `Q4` entry are all stale). What it uniquely held is preserved here: its **provenance** — date, sender, recipient list, subject, content at send — in the *Origin* section, and the **client-facing wording of each question** as an *As put to the client* line on the fifteen decisions it covered. Its open-question text was checked line by line against the open register first: the register's wording is equal or richer in every case bar two, and those two details were ported across (`Q37` weld scrap as a fourth scrap category; `Q31`'s question statement, which the register had lost, keeping only its status). Recoverable at `d79ce78`. |
| Aug 11, 2026 | Decision | **`Q15` closed and moved here from the open register — the FL2 spool check-in identifier is the spool alpha (`SP-#####`).** It confirms the recommendation that stood against the question, so nothing in the design changes. **Both halves of the question are answered:** the identifier is the alpha, and the coil linkage it asked about is already implemented as `SpoolCheckin.SpoolAlpha` (NOT NULL) plus `CoilTraceability.SpoolAlpha` (NULL, added 6 Aug 2026), both FKs to `Spool.Alpha`. Recorded on the body is the reason the coil→spool edge is on `CoilTraceability` rather than a `CoilOutput` header column — a header column is wrong the moment one spool runs out mid-coil and a second finishes it. Counts: **25 decisions · 22 `Shopfloor` · 3 `Other`**; the open register drops to **72 open · 48 Shopfloor**, and `Q15` leaves its `Critical` row. **Attribution not recorded:** the answer came through the project team on 11 Aug 2026 and no confirming client name was given — add one if this was Tim O. or Jaspreet, since every other entry in this file names its source. |
| Aug 12, 2026 | Renumbering | **Decided questions renumbered `Q61`–`Q85`; open questions took `Q1`–`Q60`.** The pair is now contiguous with no holes, numbered in document order. Every inbound `OQ-##` citation in the repository — 500 across 54 files — was rewritten in the same pass, and citations to removed questions were retargeted to their `OI-##` equivalents. **The full old→new map is in the open register's Change Log**, and entries above this row keep their original numbers because they record what happened under the old scheme. |
| Aug 12, 2026 | Pointer | **The open register dropped to `Q1`–`Q59`** — `Q60` (planning grid versus a dedicated flat-wire view) was removed as out of scope and its number was not reused. Decided numbering is unaffected: `Q61`–`Q85` stands. |
| Aug 12, 2026 | Residual absorbed | **`Q73` item 6 now carries the no-welding branch in full.** It was tracked as its own open question until 12 Aug 2026; that question was removed and has **no `OI-##` mirror**, so this entry is the only record that the branch is unresolved. Both readings and the one-sentence form of the question are stated inline. The open register is now `Q1`–`Q57`. |

---

## [Analysis/FlatWireEndToEndProcess.md](Analysis/FlatWireEndToEndProcess.md)

| Date | Changed By | Description |
|------|-----------|-------------|
| Apr 23, 2026 | Plan team | Initial document created from source SRS, meeting notes, and process diagrams |
| Apr 28, 2026 | MOM — Planning & Shopfloor meeting | **Stage 2 updated:** alpha generation at planning time (not execution); stop calculation system-driven; planner inputs weight only; three allocation scenarios confirmed; "assign as-is" stock option added; pattern picture replaced with tabular grid. Open questions reference updated to 59 items. |
| Aug 11, 2026 | Terminology | **"Transformer line" removed.** The term was used as the precedent for the 2-coils-per-skid packaging rule but is **defined nowhere in this repository or the wider UAL document tree** — it appeared 11 times across 7 files as assumed plant context. The packaging rule itself is unchanged; only the undefined attribution is gone. See `Q16` / `OI-67`, which now ask which existing convention is being inherited. |

---

## [Analysis/FlatWireOpenQuestions.md](Analysis/FlatWireOpenQuestions.md)

> **Reading note.** Entries dated **before 12 Aug 2026 cite the pre-renumbering `Q##` scheme by design**, and entries written *during* 12 Aug cite whichever scheme was current when they were written. **The register was renumbered three times that day and the three maps compose in order** — the contiguous reassignment, then the mid-register decrement when `Q19` was removed, then the withdrawal of 23 questions and the reassignment to `Q1`–`Q33`. All three rows are below; read them in sequence, oldest first, and do not apply one alone. These entries also record superseded values and withdrawn answers that the question bodies no longer carry; the bodies are the live rule, this log is the audit trail.

| Date | Changed By | Description |
|------|-----------|-------------|
| Apr 23, 2026 | Plan team | Initial register created from FlatWirePlan.md; 46 questions captured |
| Apr 27, 2026 | Analysis team | Added Q47–Q57 from RodCheckout.md, PassScheduleManagement.md, Spool.md, and DieChange.md analysis; total now 57 |
| Apr 28, 2026 | Analysis team | Updated Q51 and Q52 to reflect dashboard changes: selection mechanism (attribute-based lookup + confirm bar) now shown in dashboards 2, 5, FL3; unified pass schedule approach now implied by FL3 check-in. Residual open points scoped: Q51 = no-match notification path; Q52 = FL2 check-in validation for hybrid-origin spools. Q53 and Q54 remain fully open. |
| Apr 28, 2026 | MOM — Planning & Shopfloor meeting | **Q32 Decided**: max finished coil weight = 1,000 lb (TKUP-2 limit); orders split into multiple stops with alphas generated at planning time. Added **Q58** (OD → weight conversion formula, Tim O. to share) and **Q59** (planning grid vs new flat-wire section, team to decide internally). Key decisions recorded: alpha generation at planning (not execution); stop/calculation logic system-driven; pattern visualization replaced with tabular order → spool → weight grid; three spool-to-order allocation scenarios confirmed; "assign as-is" stock checkbox added; routing retains Add Operation with no child coil hierarchy; fully digital traveler (printing disabled for flat wire). Total questions: 59. |
| May 4, 2026 | Analysis team — Tim O. review | **Q27 Decided**: five-case alpha-handling rules confirmed (same-spec swap = single alpha; size change / edge type / roll gap to new target = new child alpha; AGC roll gap adjustment = single alpha, no change). **Q28 Decided**: four-step pass schedule override flow confirmed; operator read-only except one-for-one same-size die swap. **Q29 Decided**: unplanned component bypass event confirmed as distinct transaction; alpha split, supervisor acknowledgment, and pre-bypass material disposition all required. **Q30 In Progress**: roll gap validation approach deferred pending engineering confirmation. **Q31 In Progress**: OD/ID limits from both UA equipment and customer; Tim to provide specifics. **Q32 Updated**: TKUP-2 capacity revised to 1,100 lb (up from 1,000 lb stated Apr 28). **Q33 Decided**: no separator capability; no interleave field required. **Q39 Decided**: camber SPC feature conditional on customer specification. **Q40 Decided**: edge burr not currently measured; no implementation required. **Q41 Decided**: system-level die footage tracking confirmed; unique die IDs; threshold deferred until failure data available. **Q44 Decided**: line speeds unknown; determined by trial; table-driven design. **Q45 Decided**: FL1/FL2 independent simultaneous operation confirmed; throughput ratio 3:1; FL3 blocks FL1 and FL2. **Q47 In Progress**: multiple partial spool alphas confirmed needed; weigh-at-payoff scale question open (Scott/Bob/Shannon); carry-forward design deferred. **Q48 Decided**: supervisor approval required for mid-run checkout. **Q49 In Progress**: PLC tag behavior deferred pending engineering confirmation. **Q50 Decided**: supervisor must approve partial-run disposition; notification-driven remote approval model confirmed. **Q54 Decided**: pass schedule data not on label; logged for technical traceability. **Q55 Decided**: anneal modifies existing alpha; no FL1 re-pass capability for spools. **Q56 Decided**: thread mode allowed until SPC complete post die change; system routes to SPC Checkpoint screen for gauge drift and size change cases. **Q57 In Progress**: spool unique IDs confirmed (like furnace plates); tracking workflow FL1→furnace→cooling→FL2 confirmed; full state machine transitions still TBD. **Q10 In Progress**: FL3 cannot run if orders scheduled on FL1 or FL2 (partial decision from Q45). |
| Jul 28, 2026 | Analysis team | Added **Q60–Q62** from the spool completion alert requirement ([SpoolCompletionNotification.md](MVP-1/ProjectPlan/Business/Screens/SpoolCompletionNotification.md)): target weight source and over-target behavior, applicability to finished coils at TKUP-2, supervisor mirroring and acknowledgement audit persistence. Total questions: 62. |
| Jul 29, 2026 | Analysis team | Q60 updated with the assumed **2,000 lb** default target spool weight. Added **Q63–Q65** from the machine-stop confirmation requirement (Part B of [SpoolCompletionNotification.md](MVP-1/ProjectPlan/Business/Screens/SpoolCompletionNotification.md)): `FL{n}.LineState` vocabulary + stop dwell + pause-reason suppression, supervisor visibility and multi-operator arbitration of the prompt, and the short-close-below-target path. Total questions: 65. |
| Jul 29, 2026 | Analysis team | Added **Q66** — scale-vs-calculated spool weight reconciliation on the completion step: variance tolerance (±2 % proposed), which weight is the default basis, whether an out-of-tolerance variance needs supervisor approval rather than just a reason, and whether a scale exists at the take-up at all (overlaps Q47, feeds Q58). Total questions: 66. |
| Jul 29, 2026 | Client direction | **Q66 part 3 Decided**: an out-of-tolerance spool weight must not block spool creation — the completion is authorised by a **supervisor override** (reason + badge/ID + PIN, remote-approval fallback) and the override is recorded on the spool. Q66 now In Progress; tolerance value, default basis, scale availability and PIN validation source remain open. |
| Jul 29, 2026 | Analysis team | Added **Q67–Q70** from the pre-check-in / payoff-staging build ([RodPreCheckin.md](MVP-1/ProjectPlan/Business/Screens/RodPreCheckin.md)): coil status at staging (`INFLAT` per SRS §4.2 vs `STAGED` per the process walkthrough, and what reverses it — **Critical**, blocks the Phase 4 staging build), whether pre-check-out needs supervisor approval (contrast Q48 for mid-run), staging against a future order and its effect on weld selection, and the scope of `RodSeqno`. Also noted: pre-check-out has **no SRS requirement ID** — §4.17 covers only post-check-in removal, so a new `PCI`-series block is needed. Total questions: 70. |
| Jul 29, 2026 | Analysis team | Added **Q71–Q73** from the Dashboard 2A mockup review: rod diameter tolerance (`CHK007`) has **no column anywhere in the schema** — `AlloyProperty` carries only gauge/width, which are flat-wire *output* dimensions, so the mockup had hard-coded a single 0.005" that is wider than every value in the standards table; whether a failed staging inspection persists a `RodStaging` row at all, given that the UI/API `Blocked` bay state is derivable (`Staged` + any `Fail`) but nothing currently writes it; and which WIP station FL3 pre-check-in posts to, since the schema/DDL/API all scope staging to FL1 **and FL3** while only `FL1PO` is seeded and no `FL3PO` exists. Total questions: 73. |
| Jul 29, 2026 | Client requirement | **Free rod processing order.** Planned rod sequence is explicitly **not enforced** — the operator may process rods in any order; staging validates only current-order membership and availability, and must never block because an earlier-planned rod is unprocessed. Both sequences are retained: planned for planning/reporting, actual in the transaction history for traceability. **Q70 partly resolved** — reframed from "what is `RodSeqno`'s scope" to "which of two sequences is it"; `RodSeqno` is now the *actual* sequence and a new `RodStaging.PlannedSeqno` snapshots the planned one. **Q69 leaning "no"** — validation is scoped to the *current* order, which makes a future-order rod a non-candidate; not closed, because continuous feed then cannot cross an order boundary and that consequence needs confirming. |
| Jul 29, 2026 | Client direction | **Q74 added, part-decided.** Rod→order resolves from **`planning_routings`** at pre-check-in / check-in, so the order is *revealed* by the scan rather than chosen — which makes the first rod on a cold line validatable and removes the need for an order chooser. A rod whose order is scheduled on **another line** is **not refused**: the operator is notified and a **supervisor override** (reason + badge/ID + PIN, remote-approval fallback — the Q66 pattern) authorises it, with the override, supervisor and reason recorded on `RodStaging`. Still open: PIN validation source (shared with Q66), whether the mid-order cross-order case should also be overridable given the weld/genealogy constraint, applying the same panel at Dashboard 2, whether scheduling gets corrected, and whether the other line's operator is notified. |
| Jul 30, 2026 | Analysis team | Added **Q75** — whether multiple rod bundles can be **stacked on a single VPS payoff position**. Every artifact assumes one bundle per bay (`UX_RodStaging_Bay` enforces it as a filtered unique index), but nothing states whether that is the equipment's limit or a modelling assumption, and eye-to-sky is the geometry in which stacking is standard practice. The resolving fact — received bundle gross weight — is stated **two incompatible ways** in the delivered contracts: **8,690–8,840 lb** in `/payoff/status`, `/staging/queue` and `/staging/rod` versus **2,000 lb** in `/rod/{alpha}` and `/checkin/rod`, against a 9,000 lb position rating. That contradiction matters independently of stacking, because the payoff weight bar and the weld alerts (warn 3,000 lb / critical 2,000 lb) are calibrated to it. Recorded the four schema consequences if stacking is real, and flagged **`CK_WeldEvent_PayoffDiff`** as worth relaxing pre-emptively — it rejects an in-stack weld outright, and the real invariant is that the rod *alphas* differ, not the bays. Total questions: 75. |
| Jul 30, 2026 | Analysis team | **Scoped the register to shopfloor.** Added a `Scope` column to the Quick Reference table and a **Shopfloor Scope — Filtered Index** section at the top: **48 of 75** questions relate to Flat Wire Mill shopfloor changes (35 Open/In Progress, 13 Decided); the other 27 belong to adjacent modules and are marked `Other`. **Nothing was deleted** — this Change Log cites question numbers throughout, and `OQ-##` references point inbound from the phase files, `REVIEW.md` and the master specification, so the numbering must stay contiguous. Filter rule: `Shopfloor` if a Dashboard 1–14 screen reads, validates against or writes it — which keeps reference data and equipment limits in scope (Q32, Q36, Q38, Q41) and leaves scheduling representation (Q10), pre-scheduling validation (Q53) and order-entry behaviour (Q9, Q11, Q12) out. Also corrected the **Q4** Quick Reference row, which still read `Open` although its detail entry records it **Decided May 15, 2026**. |
| Jul 31, 2026 | Analysis team | **Q72 items 1–2 decided; Q76–Q77 added** from the Dashboard 2A UX review (`Dashboard2A_UXReview.md`, deleted 1 Aug 2026 — in git history at `2a0426b`). **Q72:** `Blocked` confirmed **derived**, and pre-check-in now **commits the `RodStaging` row before the inspection gate** — `POST /staging/rod` returns `201` with `state: "Blocked"` instead of `422`-and-write-nothing. The deciding argument is physical: a bundle is not unbanded until it is on the payoff, so a rod that fails inspection is already in the bay, and writing no row made `GET /payoff/status` report an occupied position as `NotStaged` and let the next rod be staged into it. `CHK010` unchanged (no bypass). Item 3 — *what releases a blocked row* — is now the blocking residual: `Status` has no value for a WIP-rejection outcome and inventing one would move the row outside `UX_RodStaging_Bay`'s filter, freeing a bay that is not free. **Q76:** `UX_RodStaging_Bay` is keyed `(LineId, PayoffPosition)` while `CK_RodStaging_LineId` admits both `FL1` and `FL3` — so if the two lines share one physical VPS (the assumption everywhere, and why `FL3PO` was never seeded, Q73), **two rods can be staged on one physical bay with every constraint satisfied**. Not covered by G20, which settled payoff-position vocabulary rather than uniqueness scope; logged as **G21**. **Q77:** a welded rod could be un-staged from the queue — welded is `IsWelded` on a `Staged` row, so every "staged" control matched it — despite being physically welded to the rod in the mill; the control is removed, but `WLD011` (supervisor reversal of a weld) remains unspecified. Total questions: 77. |
| Jul 30, 2026 | Client direction | **Q74 extended — out-of-planned-sequence now requires supervisor authorisation.** The operator is notified when the rod is not the one planning expects next, and a supervisor override is required to depart from the planned sequence — same credential block as the off-schedule override (reason + badge/ID + PIN, remote-approval fallback), recorded as `RodStaging.OutOfSequenceOverride` + `ExpectedRodAlpha`. Two deviations can co-occur and share one sign-off. **This supersedes the earlier free-processing-order requirement** (Q70), which had the operator re-sequencing at will with no warning and no override; both sequence columns are retained regardless, so the deviation is now authorised as well as recorded. |
| Jul 30, 2026 | Client direction | **Eleven answers from the 30 Jul call — six questions closed, three reversals.** **Q75 Decided:** rods **cannot be stacked**; only two rods at a time, one per payoff, as on the mills — the weight contradiction inside it is re-homed as **Q81**. **Q74 off-schedule REVERSED:** no blocking message and no supervisor override — the system **auto-selects the correct station** (FL3-planned rod scanned on the FL1 tab switches the tab), at both pre-check-in and check-in. Out-of-sequence override stays, provisionally, with a re-review committed. **Q72 item 3 Decided:** a failed staging inspection is captured as a rejection with a reason on the rejection screen and the rod goes to **`HOLD`** — that is what releases the row and frees the bay. **Q67 Decided (Critical):** `INFLAT` is set **only at check-in**, no intermediate status for welded-but-not-checked-in — reverses the interim SRS-following design and unblocks Phase 4; the reqsum / `wip_coil_orders` residual stays open. **Q71 shape Decided:** min/max tolerances exist for **gauge, width, diameter and ovality**, held in the lookup, applied at both pre-check-in and check-in — values to follow by e-mail, so nothing is to be seeded. **Q69 Decided:** a rod may carry **more than one order**, which inverts the "current order only, else refuse" rule; sequencing deferred to **Q79**, with a possible deferral out of this delivery floated on the call. **Q68 Decided:** pre-check-out needs supervisor approval **only when the rod is welded**, where it is a rejection to `HOLD` — which also **decides Q77** and reverses the Jul 31 removal of the welded-rod Unstage control. **Q65 Decided:** short close is an **unplanned stop** on the mill 10-90 pattern, graded against the **customer min/max weight**, outside-range needing supervisor override + hold or a concession offer, with the spool run off regardless and a mid-run coil break restarting the stop **from zero**. **Q60 basis Decided:** the 2,000 lb default target is withdrawn in favour of the customer weight range. Added **Q78** (rod scheduled on neither FL1 nor FL3 — not covered on the call), **Q79** (multi-order sequencing / MVP scope), **Q80** (panel resolution 1280×1024 vs 1920×1080). Total questions: 81. |
| Aug 1, 2026 | Project decision | **`OffSchedule*` columns dropped.** With Q74's off-schedule case becoming a navigation behaviour, `RodStaging.OffScheduleOverride`, `ScheduledLineId` and `CK_RodStaging_OffSched` are removed and `CK_RodStaging_Override` reverts to keying on `OutOfSequenceOverride` alone. The analysis recommendation had been to retain them unwritten against **Q78**; the project decision is removal, so Q78 re-adds a column group if it ever needs one. **`OverrideBy` / `OverrideAt` / `OverrideReason` survive** — they are shared with the out-of-sequence override, and dropping them would delete the surviving override's audit trail. Propagation across the schema, contracts, mockups and the July 30 project-plan set is sequenced in [ClientCall_2026-07-30_SyncPlan.md](BaseDocuments/ClientCall_2026-07-30_SyncPlan.md). |
| Aug 1, 2026 | Project decision | **Weld quality is captured at the weld; `Q24` widened.** Dashboard 2A's Mark as welded dialog now requires a **Pass/Fail** result with a reason mandatory on Fail (`WLD013`), using the same six reasons as Dashboard 4 — so one physical join is documented identically wherever it is captured. Quality was the only NOT NULL `WeldEvent` column that dialog lacked, so **`POST /weldevent` becomes the single weld write** and `POST /staging/rod/mark-welded` is **retired**; the `RodStaging` weld columns are set in the same transaction, **on a Pass only**. This closes `WeldEventPopupPlan` **Q-W1** ("is a quality result at weld time acceptable on the floor?" — yes) and confirms its decision **D-A**; it needs **no schema change**. **A failing weld does not mark the rod welded** — the join did not hold, so the position keeps reading *not yet welded*, the station states the failure and its reason, and the operator remakes the weld. That last rule **widens Q24** rather than adding a question: a remade weld leaves **several `WeldEvent` rows for one physical join**, which is the re-weld-on-certificate question Q24 already asks — now reachable two ways (a weld that breaks mid-run, and a weld that fails its quality check before anything runs through it). Footage attribution across the two boundaries belongs to **Q22**. Total questions: 81. |
| Aug 1, 2026 | Build decision | **DB6, DB8 and DC converted from screens to dialogs.** SPC checkpoint, WIP rejection and die change are now popups (`spc_checkpoint.js`, `wip_rejection.js`, `die_change.js`) raised over the screen the operator is already on, rather than pages navigated to. The reason is context: WIP rejection alone is reached from five places and, as a page, could describe only one — its material banner was hard-coded to *R00042 at 8,220 ft*, and the **Q72 item 3** pre-check-in path (rejection releases the bay, rod to `HOLD`) could not be represented at all. It now is: on that path `runId` and `footagePosition` are `null`, the dialog states on screen that submitting releases the bay, and it reports `releasesBay` back to the staging station. Two hand-offs the spec always described also start working — a gauge-drift/size-change die change opens the SPC checkpoint it mandates (**Q56**), and an out-of-spec checkpoint's *suspend material* opens the rejection with the failing reading carried over. **No dialog scrolls**: an oversized popup is scaled to fit the window, since an operator in gloves cannot drag a scrollbar to reach *Confirm*. Added **Q82** — DB1's "WIP Rejection**s**" nav item reads as a list screen that does not exist. Total questions: 82. |
| Aug 1, 2026 | Build decision | **Dashboard 2A's weld-readiness strip removed; every control moved onto the bay it acts on.** The 96px band between the payoff cards and the traveler queue is gone. Its narrative was already duplicated by the cards in every branch but one — weight and percentage on the payoff bar, all four weld states in the bay alert, cold start in the empty-bay text — and the exception, *induction-weld tail to head*, became the staged card's alert while a rod is running. **Mark as welded** moved to the **staged** card (`PCI008` defaulting; all five disabled-tooltips kept, since they are the only statement of *why* it is unavailable) and **Welds this run · N** to the **active** card. The reclaimed space went to the queue, which is ~108px taller. Also removed: the **Open active run** link on the active card (`FR-051b`) — the run monitor is reachable from the app bar and Line Status, and this station's job is staging the next rod. The queue gained a standing **"Rods In Queue"** heading. Separately, Dashboard 2's *Acknowledge & Begin Check-in* now returns to **Dashboard 2A** rather than Dashboard 3 (`FR-079a`). Added **Q83** (at cold start *Welds this run* is now absent rather than shown-and-greyed — supersedes **TC-068e**) and **Q84** (whether the operator should land on the staging station or the run monitor after check-in). New requirements `FR-050a`, `FR-051a`, `FR-051b`, `FR-079a`; new cases TC-068f/g/h/i and TC-079a. **Note for G27:** the queue heading reuses the name *Rods In Queue* from the retired Dashboard 4 accordion but is **not** that control — it is a read-only table with no drag and no undo, so the re-sequencing capability remains homeless. Total questions: 84. |
| Aug 2, 2026 | Client direction | **Q57 part-decided — FL2 has two spool statuses and runs one spool at a time.** FL2 has no space to stage material, so a spool is either waiting for the line or on it: the operator-visible vocabulary is fixed at **`Ready for FL2`** (`RECEIVED`) and **`Checked in`** (`INFLAT`), and **`STAGED` is never set at FL2** — the "At TPO" status is withdrawn from the spool queue, staging remaining the FL1 concept per `PCI002`. **Check-in is exclusive:** while any spool is checked in, **no spool offers a check-in action** — not the others and not the checked-in one — and it returns only on checkout. Applied to [dashboard_5a_spool_queue.html](MVP-1/ProjectPlan/Frontend/Mockups/dashboard_5a_spool_queue.html) (lock bar, checked-in row sorted first with a *View run* action, `HOLD`/`COMPLETE`/`SCRAP`/`STAGED` pills removed) and [SpoolQueue.md](MVP-1/ProjectPlan/Business/Screens/SpoolQueue.md) v1.1 §3.5, rules **SQ-7**–**SQ-10**. **Q57 stays In Progress** — this fixes what the FL2 operator *sees*, not what is stored, and **OI-06**'s two rival vocabularies are still unmapped. **Two residuals with no owner yet:** exclusivity has **no backing constraint** (`dbo.Spool` has no filtered unique index on `Status` and **no `LineId` column at all**, so "one checked-in spool per line" is not currently expressible — `POST /checkin/spool` must return `409` on the second attempt); and a **quality-held spool now has nowhere to appear** on a two-status queue, raised to the client as SpoolQueue.md open item 6. Total questions: 84. |
| Aug 4, 2026 | Client direction | **HMI/SCADA descoped — `Q4` superseded.** The client confirmed that **SCADA Trends (DB14)**, the **HMI Line Schematic (DB13)** and the **Machine View tab** on the active run monitor are **not required**. `HMIAndSCADALayout.md` and both mockups are deleted; `FR-111`, `FR-112`, `FR-114`, `FR-440`–`FR-451` and `FR-460`–`FR-470` are marked withdrawn rather than renumbered. **`Q4` is superseded, not deleted** — its chart-layout half is moot because there is no chart layout to own, but its *other* subject, "machine tags for flat wire", survives: those tags are still read by the run monitor, the line status board, the rod-checkout gatekeeper, the spool stop prompt and the die-life counter. They are now specified in [PLCTagSpecification.md](MVP-1/ProjectPlan/Architecture/PLCTagSpecification.md) and remain unconfirmed as **`PLC-Q02`**. **Raised but not asked:** DB14 was also the answer to the legacy .NET **SCADA Report** in [FlatWirePlan.md](Analysis/FlatWirePlan.md) §Reporting Suite — whether that report is also descoped is a separate client decision. |
| Aug 4, 2026 | Analysis team | **Added `Q85`–`Q88` from the PLC tag-surface consolidation.** The surface existed in six partial, mutually contradictory copies; reading them as one document surfaced four questions no single copy could raise. **`Q85`** — speed is pushed as a **target** in four sources and a **limit** in two, including inside `02-SRS.md` itself (§9.1 vs `FR-073`); a setpoint and a safety clamp are different tags with opposite failure modes, so `FR-073`'s wording is deliberately left unfixed until this closes. **`Q86`** — edge type is in the push payload in four sources and absent from a fifth, and separately **no edger tag path exists anywhere in the repo**: the only edger-adjacent tag is `FL1.EdgeSet.Status.Active`, on the one line that has no edger. **`Q87`** — every map addresses FM2 as `FL2.FM2.*` even inside the FL1 map, so it is undetermined whether FL3's single-batch push crosses a controller boundary; commissioning test **C5** passes either way and cannot distinguish them. **`Q88`** — units are in the path for `Speed.FPM` and `Weight.Lb` but not for gauge, width, roll gap or die diameter, where inches are assumed and never stated; a controller reporting gauge in mils would pass every structural check and produce out-of-spec wire. Logged as **`G29`**/**`G30`** in [back-matter.md](MVP-1/ProjectPlan/Development/GapsRegister.md) where they carry schedule impact. Total questions: 88. |
| Aug 4, 2026 | Analysis team | **Register index reconciled.** The counts had not been updated when `Q82`–`Q84` were added on Aug 1: the header read *"81 · Shopfloor 54"* and the Open/In-Progress heading read *(35)* against 38 actual entries, and `Q82`–`Q84` were missing from the Quick Reference table entirely. Now **88 total · 61 Shopfloor** (42 Open/In Progress · 18 Decided · 1 Superseded), with `Q82`–`Q88` added to the Quick Reference table. The filtered-index scope note also read *"Dashboards 1–14+"*, corrected to **1–12+** with the DB13/DB14 descope. |
| Aug 4, 2026 | Analysis team | **Added `Q89`–`Q92` from the audit of the PLC tag deliverables.** All four are items the tag specification had marked `[CLIENT INPUT REQUIRED]` **without a register entry**, breaching its own stated rule that *"nothing is tracked only here."* **`Q90` is the serious one:** confirmation of every machine tag path has been an open action since **15 May 2026** and has never been tracked anywhere — it lived as an untagged row on the sign-off sheet of `HMIAndSCADALayout.md`, and when that file was deleted on 1 Aug it survived only as prose. It is **Critical**: every published path follows a proposed convention, none has been read off a controller, and a wrong path fails silently. **`Q89`** surfaced a contradiction the consolidation exposed — assumption A2 (rescued from the deleted file, its only home) says take-up load cells are fitted, while [SpoolCompletionNotification.md](MVP-1/ProjectPlan/Business/Screens/SpoolCompletionNotification.md) says the completion weight is *derived* from footage × cross-section, and **the tag map publishes no take-up weight path at all** — so the machine-stop prompt, the completion transaction and the printed label all read a value with no published source. **`Q91`** (ordinal convention: `DB1`/`Payoff2` vs `FM2.Stand6S1` — R7 and R8 contradict each other) and **`Q92`** (`ITInhibit` is unprefixed in all six sources while every other tag is line-scoped; if it is genuinely plant-level, one line’s unmet prerequisite blocks all three) complete the set. Total questions: 92. |
| Aug 4, 2026 | Client direction | **`Q93` decided on arrival: FM2 has three stands — `S1` 8″, `S2` 6″, `S3` 6″.** The client corrected FL2's roller sizes. This is **not a digit swap**: the repo had modelled a separate 8″ roller upstream of three 6″ stands — **four components** — because the May 21 note was recorded as *"FM2 has three 6″ stands"* in `00-foundations.md` §0.3 and read that way. **The 8″ roller is S1**, and the fourth stand never existed. The misreading had propagated into ~50 files, the `Stand` seed data, two SQL `CHECK` constraints, the `ComponentName` enum, the PLC tag grammar and eight mockups. Three things pin the correction: the client's **published PLC map has exactly three FM2 stations**, all `[CONFIRMED]` by observation; **every seeded pass schedule has exactly three FM2 component rows** with a descending gap chain; and **`FM2_6inS3` never had a tag path or a seed row** — its absence was itself logged as `OI-36`/`G29`. **Two open items close, neither having been a real defect. `OI-04`** — *"is the mandatory stand `FM2_6inS2` or `6″ S3`?"* — both named **the same physical stand**; the phantom made one answer look like two. **`OI-36`** — *"the final stand has no tag path"* — the map was complete. **Component names become position-only** (`FM2_S1`/`FM2_S2`/`FM2_S3`) with roll diameter moved into a new `Stand.RollDiameterIn` column, because diameter inside an identifier is what let this survive ten weeks. **No gap or gauge value is recomputed** — the seeded chains were always right, and `FR-387`'s multipliers just move from diameter labels to positions, which fixes a defect where the final stand had no gap formula. **One real physics consequence:** roll radius feeds the generation engine, so S1 admits **~1.33×** the draft of a 6″ stand and develops **~1.16×** the force at equal draft — `F_max` and mill modulus become per-stand (`PSG-D10`, `PSG-D12`), and §3.3.5's allocation illustration is recomputed at `k` = 3 (**5.4% / 9.8% / 13.5%**). **Added `Q94`** (`PLC-Q04`, Critical) — the PLC station rename departs from observed `[CONFIRMED]` strings and needs the controls engineer's sign-off; gap **G32**. Total questions: 94. |
| Aug 4, 2026 | Client direction | **`Q92` decided: `ITInhibit` is line-scoped — `FL1.ITInhibit` and `FL2.ITInhibit`.** One tag per line, so a line blocked from running blocks **only itself**. The plant-level reading — in which an idle FL1 with no rod checked in would have stopped a scheduled FL2 — is excluded. All six pre-consolidation sources had recorded the tag bare, without its line prefix. **The answer reaches further than one tag.** Rule **R2** of the naming convention (*"the first segment is always the line; there is no plant-level tag"*) was `[PROPOSED]` **only because of this single counterexample** and is now `[CONFIRMED]` — which matters because the specification asks the client to confirm **the grammar** rather than ~60 individual strings, and every `[PROPOSED]` path derived from that grammar now rests on a convention with no exception in it (**Q90**). Commissioning test **C7** also becomes a per-line test: set each of the five conditions on one line, assert the other two still run. **One residual, on FL3 only** — whether the hybrid line carries `FL3.ITInhibit` or asserts both controllers' interlocks follows from **Q87** / `PLC-Q08`, the same question that decides whether FL3's single-batch push crosses a controller boundary; FL3 is the one line where a block legitimately implicates a second, the two being one physical thread of material. Applied to [PLCTagSpecification.md](MVP-1/ProjectPlan/Architecture/PLCTagSpecification.md) v1.3 as decision **D15** (§2.2 R2, §2.3, §2.4, the three §3.2 tag maps, §3.4 item 4, **§5.1 — a new per-line tag table**, §12, §13.1, and Part A/Part B of the sign-off sheet), and re-rendered to `MVP-1/SRS/PLCTagSpecification.docx`. `PLC-Q18` closed. **Register index also reconciled:** `Q93` (decided Aug 4) and `Q94` (Critical, open) had been added without updating the filtered index — `Q93` is now listed under Decided and `Q94` under Critical/Open. Counts: **46 Open/In Progress unchanged** — less `Q92`, plus `Q94`, so 8 Critical · 24 High · 13 Medium · 1 Low — and **20 Decided** *(from 18, adding `Q92` and `Q93`)* · 1 Superseded. Total questions: 94. |
| Aug 4, 2026 | Analysis team | **The tag surface's measure segment reshaped, and all old/new content stripped from the client deliverable. Added `Q95` (Critical).** Six corrections were applied and generalised across all three lines: `Speed.FPM`→`Speed`, `Payoff{n}.Weight.Lb`→`Weight`, `Die.ActiveDiameter`→`Diameter`, `Status.Active`→`Status.IsActive`, `Status.Fault`→`Status.IsFaulted`, and `.Current` dropped from roll gap, gauge, width and footage — **41 paths, FL1 15 · FL2 14 · FL3 23**. The measure now splits two ways: **analogues are a single bare segment; booleans keep a `Status` group and take an `Is` prefix.** Element and station segments are untouched, so `FM2.S1/S2/S3` stands. **`Q91` resolved without either side losing** — the digit-suffix rule and the station-segment rule govern different things (the *n*th instance of a piece of equipment versus a station inside an assembly), so they never competed; recorded as `D16`. **`Q88` part 1 specified** (units never appear in the path) while part 2 — *what unit are the values in* — is now the whole of it and was raised to **High**, because with `.Lb` and `.FPM` gone **no tag declares a unit at all** and there is nothing left in an address to catch a misconfigured scale. **The cost is `Q95`:** thirteen FL1 rows were `[CONFIRMED]` by observation — every confirmed string on the surface bar `LineState` and `ITInhibit` — and all thirteen are now `[PROPOSED]`, so where **Q94** departs from three observed strings this departs from thirteen and **C1** fails across the whole map rather than on isolated rows if the reading is wrong. **Second half of the change:** neither `PLCTagSpecification.md` nor `MVP-1/SRS/PLCTagSpecification.docx` may contain old/new content, so `[PLC §4.3]`'s as-published→as-specified table, the nine `observed as FM2.Stand8` footnotes, §2.3's *"inconsistencies in the observed set"* framing and `PLC-Q04`'s two-column sign-off wording were **all removed** — **this register is now the only record of the superseded strings** (`Q94`, `Q95`). Withdrawing two rules rather than striking them renumbered `[PLC §4.2]` to **R1–R8** (old R7→R5, old R8→R6, old R9→R7, new R8); `Q92`'s R2 citation is unaffected. Also **added the missing `D15` row** to `[PLC §12]`, which cited it three times without ever listing it, and added `D17`. Applied to `PLCTagSpecification.md` **v1.4**, re-rendered to `MVP-1/SRS/PLCTagSpecification.docx`, with `G31` restated and **`G33`** raised in [back-matter.md](MVP-1/ProjectPlan/Development/GapsRegister.md) and the config keys realigned in [PLCTagImplementation.md](MVP-1/ProjectPlan/Architecture/PLCCommunication.md). Counts: **47 Open/In Progress** *(9 Critical · 24 High · 13 Medium · 1 Low)* · 20 Decided · 1 Superseded. Total questions: 95. |
| Aug 4, 2026 | Analysis team | **Tag specification reissued as v1.0 — a first issue, not a consolidation. `[CONFIRMED]` retired; both registers renumbered.** The document had accumulated three tells that it was a consolidation of six sources revised four times, and all three are gone. **(1) `[CONFIRMED]` is withdrawn entirely** — the legend row is deleted and the reading convention is now two tags. Tracing the status is what forced this: **no client artifact contains a single tag path** (all 14 `BaseDocuments/` files searched for every tag token returned one hit, *"~1800–2000 FPM"*, a speed figure), the strings trace to our own `HMIAndSCADALayout.md` §6.2 — deleted 4 Aug, which itself said *"they follow a proposed naming convention, not a verified map"* — and `[CONFIRMED]` was manufactured by the convention's own second limb, *"or stated consistently across the source specifications."* Eight items claimed it; the count is now **zero** (**Q90**). The dated form `[CONFIRMED — August 4, 2026]` was **removed, not downgraded**, on the equipment heading, so the deliverable does not label the client's own correction as our proposal (**Q93**). **(2) The decision log `§12` (D1–D17, Apr→Aug 2026) is deleted** — a revision history in disguise, whose every rule was already normative in the body and four of whose rows were purely editorial. `ITInhibit`'s line-scoping survived as prose in `[PLC §8.1]` with **C7** tightened to prove it per line; `D14`/`D15` citations were removed from four files and **this register is now the audit trail for both** (**Q92**, **Q93**). **(3) Restructured by subject** — equipment merged with per-line differences into §2, the one-trigger rule promoted to §3, line state reduced to what the client owns (§6, dropping the internal three-way naming collision), and the three separate lists of open questions consolidated into §13. **A live defect closed on the way:** `PLC-Q18` was listed as requiring client input although it was decided — deleted, not renumbered. **Two renumberings, both recorded here because the deliverable carries no history.** Sections: `§1.4`→`§2.1`, `§1.6`→`§3`, `§2`→`§4`, `§2.5`→`§4.3`, `§3`→`§5`, `§3.2*`→`§5.2*`, `§4`→`§7`, `§5`→`§8`, `§6`→`§9`, `§7`→`§2.2`, `§8`→`§6`, `§8.5`→`§6.2`, `§9`→`§10`, `§10`→`§11`, `§11`→`§12`, `§12`→**deleted**, `§13`–`§15` unchanged. Open items, now **numbered in priority order** (5 Critical, 10 High, 2 Medium): `Q03`→`Q02`, `Q04`→`Q03`, `Q19`→`Q04`, `Q20`→`Q05`, `Q16`→`Q14`, `Q14`→`Q16`, `Q18`→**retired**; `Q01`, `Q06`–`Q13`, `Q15`, `Q17` unmoved. **134 citations retargeted across 15 files** — 97 `[PLC §n]` section refs and 37 `PLC-Q` ids, the heaviest being [PLCTagImplementation.md](MVP-1/ProjectPlan/Architecture/PLCCommunication.md) (59 section refs). Re-rendered to `MVP-1/SRS/PLCTagSpecification.docx`. No question was added or closed by the reissue: totals stay **95 · 47 Open/In Progress · 20 Decided · 1 Superseded**. |
| Aug 6, 2026 | Client direction | **Five outcomes from the 6 Aug client call: `Q79` decided, `Q96`–`Q99` added.** **`Q79` DECIDED** — the rod consumption sequence is a three-tier rule: **full coils → partials → multi-order coils last** when welding is involved, operators working a **pick list in planned order** as on the mills, and an operator **may not jump a multi-order coil to the head of the line**. Full coils of the same order in the middle may be taken in any order. **The validation applies at pre-check-in *and* at check-in**, and it is **in MVP-1** — the deferral floated on 30 Jul was not taken. **The rule was stated three ways before it settled** — Srikanth gave full→partial→multi-order, Tim said partials first and elaborated on it, then corrected himself to Shray's reading at 12:30 — so the exchange is recorded in the entry to stop the wrong half being quoted. This **resolves `G22`**, which has stood since 1 Aug as a knowingly-wrong staging rule waiting on exactly this answer. **`OI-13`'s flow was decided in the same call** (recorded against the master spec, not here): a wire break that is welded keeps the **same alpha** — Bob: *"if it's not actually splitting and you're welding back together, it's the same alpha, no new alpha"* — with the weld recorded at its footage position and the material flagged in SCADA; only the persistence target is still missing, raised as **`G34`**. Added **`Q96`** (High) — the no-weld-customer break, decided in principle as **Z-mill coil-break logic** with a supervisor disposition on both pieces, open on the vocabulary, the gate and the persistence. Added **`Q97`** (High) — **FM2 carries two dancers with two modes** (compensating speed control and tension), which nothing in the repository models and whose tension mode **contradicts `PSM012`**, *"tension is derived from speed, never entered manually"*; gap **`G35`**. Added **`Q98`** (High) — **edging is vertical profiling roll-forming, not a knifing cut**, so it needs its own formulas treated as their own operation; the coefficients land as `PSG-D24`–`D26` in the generation spec and this row is the pointer. Added **`Q99`** (Medium) — does multi-order-last survive **without** welding? Both readings are on the recording and they differ by one validation branch. Also corroborated without change: **`Q93`** (FM2 `S1` = 8″, `S2`/`S3` = 6″, re-stated by Tim from the generation spec) and **`Q60`** (FL1 output in ~1,800 lb multiples, FL2 in 800/900). Full record in [ClientCall_2026-08-06_SyncPlan.md](BaseDocuments/ClientCall_2026-08-06_SyncPlan.md). Counts: **50 Open/In Progress** *(9 Critical · 27 High · 13 Medium · 1 Low)* · 21 Decided · 1 Superseded. Total questions: 99. |
| Aug 6, 2026 | Analysis team | **Gap audit of `PassScheduleGenerationSpec.md` — twelve defects fixed, five algorithmic; specification at v1.4.** No `Q##` opens or closes, but **`Q98` is widened** and five new `PSG-` items are owed by the client. The five that mattered: **(1)** FL2 standalone had **no path through the engine** — §6.2 began at *“area reduction, rod to flattening entry”* and every branch exited via FL1, FL3 or FL1→anneal→FL2, while the section's own note claimed a standalone FL2 schedule was producible; the tree now branches on **input form** and a flat spool enters at Step 9. **(2)** §4.2 had **no incoming flat-section inputs**, and `Prior cold work` — optional — is now **mandatory on a spool input**, since the anneal gate otherwise starts from zero on already-worked material. **(3)** `no_draw_threshold` gated the whole drawing sequence but was **never defined** anywhere — now **`PSG-D28`**, with `FR-383`'s 2% explicitly *not* adopted pending review. **(4)** **Die snapping could violate the bound it snapped from**: §3.3.3 calls the zero-elongation entry diameter a *lower bound* whose use as an answer produces under-width wire, yet Step 6 snapped to nearest and the worked example snapped **down**. The final die now snaps **up** from a bound — and the down-snap had passed every reduction check, because those test reduction, not width. **(5)** Step 9A's partition function was **undefined** (`Δt = φ · f(…)`), so **`PSG-D25` asked for a coefficient to a function that had not been stated**; it is now volume-constant strain decomposition tagged `[RECOMMENDED DEFAULT]`, with the shape itself raised as `PSG-Q31`. Also closed two **dangling validations** — `V33` had no calculation behind it (new Step 12A; over-capacity **splits the order**, not fails it) and `V16` had no method (**`PSG-D29`**) — and corrected §7's rule tally, which claimed 12/14/4 against a table holding 16/13/1. New client items: **`PSG-D28`**, **`PSG-D29`**, **`PSG-D30`**, **`PSG-Q30`**, **`PSG-Q31`**. **`PSG-D30` overlaps `Q36`** (footage-to-weight, Critical) and must resolve to one factor shared with spool completion and the coil label. Re-rendered to `PassScheduleGenerationSpec.docx`. Totals unchanged: 99 · 50 Open/In Progress · 21 Decided · 1 Superseded. |
| Aug 6, 2026 | Analysis team | **Second gap audit of `PassScheduleGenerationSpec.md` — eighteen defects, seven algorithmic; specification at v1.5.** Again no `Q##` opens or closes, but **`Q97` and `Q98` are both widened** and **eight more `PSG-` items** are owed. The recurring cause was a change made in one section between 1 and 6 Aug and never carried into the sections that consume it. **The seven that mattered: (1) width was designed at one point on the line and nowhere else** — the entry diameter was solved from target width, then nothing computed width through FM2, although two later steps required it. New §3.3.12 and Step 9B do, and they surface a consequence no artifact had stated: **there is no edger at the mill exit, so final width leaves S3, which spreads** — the last edger must be set *narrow* of target or every FL2/FL3 coil runs over-width. **`PSG-D08` is therefore not an FL1-only item**, and S3's exit must be instrumented in the same trial campaign as FM1's. **(2)** Steps 9/9A/9B are **mutually dependent** and were written sequentially — now one iterative solve. **(3)** Step 9 had **no form for the FL2 path v1.4 had just added**, solving for an FM1 exit gauge that does not exist on FL2; split into rod and spool directions, with `k_bulk = 0` handled and a **pre-anneal** branch added, since *"intermediate anneal"* is meaningless with no upstream stage. **(4) An undersize rod passed silently** — a rod smaller than the required entry gives a *negative* area reduction, which satisfied the no-draw branch, so both draw boxes bypassed and a warning-free schedule issued for a product that cannot be made. **(5) The bite condition was the wrong relation on the wrong coefficient** — `PSG-D04` asked one number for die/wire *and* roll/material contact, and the limit is **quadratic in μ**; split, with rolling friction as **`PSG-D31`** (High). The flat-entry relation also **rejects the spec's own nominal example** when applied to FM1's round entry (it would need μ ≥ 0.176), so it is excluded and the real criterion asked as **`PSG-Q32`** (High). **(6)** Drive power, tension and `Q_p` were **required but not calculable** — no torque relation existed behind an Error-severity check, `R34` was tagged `[INDUSTRY STANDARD]` with no relation named, and `Q_p` had no source and no id. **(7) Cold work under-reported** — the area ratio equals the *length* strain alone, so v1.3's edger term was understated ~2×; equivalent strain now used for rolling and edging, and `ε_prior` added, which the formula had omitted although §4.2 makes it mandatory. **Also:** Step 12A split orders against take-up **ratings** rather than the **planned output multiple** this same call corroborated (`D8` — ~1,800 lb FL1, 800/900 FL2), roughly halving the reported unit count — now **`PSG-D32`**; §8 gained a third outcome (*not evaluated*), since a strict Error/Warning rule means no schedule could be approved at go-live; §8.7 held **fifteen rows against forty validations** and is completed, including `V27`, an **Error** blocked on `PSG-D26` and previously unlisted. New client items: **`PSG-D31`**–**`PSG-D35`**, **`PSG-Q32`**–**`PSG-Q34`**. **Separately, the audit found `FR-381`/`384`/`385`/`386`/`387` contradict this specification on five counts of physics** — recorded as `FlatWire_MasterSpecification.md` **§10.5** and a `REVIEW.md` Tier 1 callout, **not** in the client deliverable, which carries no requirement IDs. Re-rendered to `PassScheduleGenerationSpec.docx`. Totals unchanged: 99 · 50 Open/In Progress · 21 Decided · 1 Superseded. |
| Aug 11, 2026 | Scope reduction | **Register reduced to MVP-1, and reduced to live content.** Two changes in one pass. **(1) Scope.** All 99 questions were classified against the deferred screen set — pass schedule authoring and its list screen, the supervisor shift summary, die management, and pass-schedule generation. **Only three touched it, and only one was unambiguously outside MVP-1: `Q1` (Pass Schedule UI vs. table management), which was removed** — pass schedule authoring is outside MVP-1, so it gated nothing here. **Two questions were retained at their in-scope halves, each banner-marked on its body:** **`Q41`** (per-tool die-life tracking and the die master table are out; the **60/85 % bands are in**, read at die-**size** granularity from `Drawer`) and **`Q28`** (the `PassScheduleChangeLog` table and the schedule-editing flow are out; the **Active Run Monitor alert, its acknowledgement and the continue-on-previous-PLC-values rule are in**). **(2) Live content only.** Superseded values, withdrawn answers and audit-trail blocks were removed from the question bodies — the surviving decision is stated as a live rule with no record of what it replaced. **`Q4` (SCADA chart layout) was removed entirely:** its answer was withdrawn on 4 Aug with the DB13/DB14 descope, its chart-layout half is moot, and its surviving machine-tags half is already carried by **`Q90`** / `PLC-Q02`. That also removes the register's only `Superseded` row, so the status vocabulary is now `Open` · `In Progress` · `Decided`. **This Change Log is deliberately the one exception** — it is the record of what changed and keeps its dated entries whole, including the values the live sections no longer carry. **Two numbering holes and four dangling citations.** Numbering runs `Q2`–`Q99` with `Q1` and `Q4` absent; nothing was renumbered, so every other inbound `OQ-##` still resolves. **`OQ-1` is still cited by four MVP-1 documents and no longer resolves** — [05-SprintPlanAndBacklog.md](MVP-1/ProjectPlan/Development/SprintPlan.md) (the UAT gate list at ~L1405 and the OQ index at ~L1470), [phase-14](MVP-1/ProjectPlan/Development/Phases/phase-14-integration-testing-plc-commissioning-golive.md) (the UAT Critical-OQ list) and **`OI-88`** in [FlatWire_MasterSpecification.md](LatestDocument/FlatWire_MasterSpecification.md); the first three list it as a **Critical blocker for UAT sign-off**, which is now a gate on a question that does not exist. `OQ-4`'s two external citations already describe it as superseded and need no change. **Worth recording for anyone expecting a larger deferred set:** four questions that read as pass-schedule questions are **in scope** — **`Q51`**, **`Q52`**, **`Q54`** and **`Q27`** — because MVP-1 *reads* a schedule at check-in and persists a snapshot; it simply never authors one. **`Q42`** (edger blades — used by every FL2/FL3 run) and **`Q62`** (surfaces on Dashboard 1) are in scope for the same class of reason. Counts: **97 total · 70 Shopfloor** — 49 Open/In Progress *(8 Critical · 27 High · 13 Medium · 1 Low)* · 21 Decided. |
| Aug 11, 2026 | Register split | **Decided questions moved out; this register now holds open items only.** The 24 answered questions went to [FlatWireDecidedQuestions.md](Analysis/FlatWireDecidedQuestions.md) with their decisions carried across verbatim — `Q27`, `Q28`, `Q29`, `Q32`, `Q33`, `Q39`, `Q40`, `Q41`, `Q44`, `Q45`, `Q48`, `Q50`, `Q54`, `Q55`, `Q56`, `Q65`, `Q67`, `Q68`, `Q69`, `Q75`, `Q77`, `Q79`, `Q92`, `Q93`. **The two files share one numbering space and nothing was renumbered**, so every inbound `OQ-##` reference still resolves against whichever file holds its question. **Ten `In Progress` questions stayed here with their decided portions** — `Q30`, `Q31`, `Q47`, `Q49`, `Q57`, `Q60`, `Q66`, `Q71`, `Q72`, `Q74` — because a partly-answered question is still open; that is the one place a decision is not in the decided file. Three section headings were left empty by the move and removed (`B6. Scheduling Capacity Inputs`, `D2. Pass Schedule Traceability`, `Section F — Die Change and SPC`); the former `Section G` is now **Section F**. Counts: **73 open** — 49 Shopfloor *(8 Critical · 27 High · 13 Medium · 1 Low)* · 24 `Other`. |
| Aug 11, 2026 | Merge | **`ClientQuestionsEmail.md` merged into the decided-questions file and removed** (recoverable at `d79ce78`). The 27 Apr 2026 e-mail was the outbound request for these decisions and had gone stale in three ways — a 57-question count, July 1 trial / August 1 production deadlines, and a live `Q4` entry. Its provenance and the client-facing wording of the fifteen questions it covered are preserved in that file. **Its open-question text was checked line by line against this register before deletion:** the register's wording is equal or richer in every case bar two, and both were ported here — **`Q37`** gains *weld scrap* as a fourth scrap category, and **`Q31`** regains its question statement, which this register had lost while keeping only its status line. |
| Aug 11, 2026 | Status consolidation | **`In Progress` retired — every question in this register is now `Open`.** The eleven rows carrying it were set to `Open` (`Q10`, `Q30`, `Q31`, `Q47`, `Q49`, `Q57`, `Q60`, `Q66`, `Q71`, `Q72`, `Q74`), along with the matching body headers, the `*(IP)*` markers in the filtered index, the status legend and the *Open / In Progress* heading. **The status vocabulary is now a single value**, which is what a register of open items should carry. **What the status no longer tells you, the body still does:** those eleven all carry a partly-decided portion — a basis, a shape, or some of their numbered items — and the inline qualifiers that say so were kept verbatim on `Q57`, `Q60`, `Q71` and `Q72`. A note at the head of the filtered index names all ten shopfloor ones so the partial answers are not lost to the flattened status. **This also fixed a latent inconsistency:** `Q66`'s table row read `In Progress` while its body header read `Open`; both now read `Open`. Historical `In Progress` references in this Change Log are left as written. **All 73 body headers were also normalised to carry `· `Open``** — 36 had no status marker at all, a pre-existing inconsistency that the flattening would otherwise have made conspicuous. Counts unchanged: **73 open** — 49 Shopfloor · 24 `Other`. |
| Aug 11, 2026 | Recommendations added | **All 73 open questions now carry a `Recommendation:`** at the foot of their body — **our proposed answer, explicitly not a decision and carrying no client authority.** The purpose is to make a client review a confirm-or-correct exercise rather than a blank page, and to give the build a defensible default while an answer is outstanding. **Where a body already held a proposal, leaning or preferred option, the recommendation crystallises that rather than inventing a new one** — `Q30` Option 2, `Q49`'s proposed checkout behaviour, `Q63`'s 5-second dwell, `Q66`'s ±2 % and scale-wins, `Q72`'s reuse-`Unstaged` note, `Q88` part 1, `Q91`'s two-rule resolution and `Q84`'s third option are all restatements, not new positions. **Confirming a recommendation closes the question** and moves it to [FlatWireDecidedQuestions.md](Analysis/FlatWireDecidedQuestions.md) with the date and the confirming name. **Nine recommendations deliberately converge rather than standing alone:** the seven PLC items (`Q85`, `Q86`, `Q87`, `Q88`, `Q91`, `Q94`, `Q95`) all recommend closing inside the single controls-engineer session `Q90` asks for — they are one conversation blocked on one person; `Q73` and `Q76` recommend one answer between them, because the station-versus-`LineId` index fix follows directly from the one-VPS reading; and `Q97` is to be closed from `PSG-Q29` rather than asked twice. **Three recommendations are deliberately "do less than asked":** `Q19` (no location tracking), `Q43` (no roll lifecycle in MVP-1) and `Q82` (rename the nav item rather than build the register) — each says so with its reason, because an unstated descope reads as an oversight. **Two flag a wrong-in-the-unsafe-direction risk rather than a preference:** `Q85` recommends implementing against a clamp because the wrong guess then fails loudly instead of starting a threading line at full speed, and `Q88` recommends declaring the unit in configuration because with `.Lb`/`.FPM` gone nothing in an address can catch a 1000× mils error. Header note added above the filtered index. No question opened, closed or changed status. |
| Aug 11, 2026 | Decision | **`Q15` closed and moved to [FlatWireDecidedQuestions.md](Analysis/FlatWireDecidedQuestions.md) — the FL2 spool check-in identifier is the spool alpha (`SP-#####`).** It confirms the recommendation that stood against the question, so no design changes. Both halves are answered: the identifier is the alpha, and the coil linkage the question also asked about is already built as `SpoolCheckin.SpoolAlpha` (NOT NULL) and `CoilTraceability.SpoolAlpha` (NULL, added 6 Aug 2026), both FKs to `Spool.Alpha`. `Q15` leaves the `Critical` row. Counts: **72 open · 48 Shopfloor**. |
| Aug 11, 2026 | Scope removal | **`Q17`, `Q18` and `Q19` removed as out of shopfloor scope.** The three rod-receiving items — receiving label format, rod inventory type, and system-side storage location — were all `Scope = Other`, and none is read, validated against or written by a flat wire screen. Section **`A4. Rod Receiving`** was left empty by the removal and deleted with them. Counts: **69 open — 48 Shopfloor · 21 `Other`**. **Nothing was renumbered**, so the numbering holes are now `Q1`, `Q4`, `Q17`, `Q18`, `Q19`.<br><br>**The substance is not lost — it is re-homed.** All three already exist in the master specification's own register: **`OI-69`** (rod receiving label, incl. the tolling-label question), **`OI-49`** (inventory type) and **`OI-85`** (storage location tracking). Track them there.<br><br>**⚠ Seven inbound citations no longer resolve here, and `OQ-18`'s are load-bearing.** `OQ-18` is cited as a **`PROVISIONAL` annotation on the `InventoryType` column** in both [FlatWire_MasterSpecification.md](LatestDocument/FlatWire_MasterSpecification.md) §1486 and [FlatWireSchema_Materials.md](MVP-1/ProjectPlan/Database/Schema/FlatWireSchema_Materials.md) L30 — a schema column whose classification is explicitly pending this question — and it appears in the **Critical UAT gate lists** in [05-SprintPlanAndBacklog.md](MVP-1/ProjectPlan/Development/SprintPlan.md) (L1405, plus the `FW-020` mapping at L1474) and [phase-14](MVP-1/ProjectPlan/Development/Phases/phase-14-integration-testing-plc-commissioning-golive.md) L29. `OQ-17` and `OQ-19` are cited only by their `OI-##` mirrors, which is harmless. **Retarget the `OQ-18` citations to `OI-49`** — do not simply delete them, because the `InventoryType` column is genuinely still provisional and the UAT gate is genuinely still unmet. |
| Aug 11, 2026 | Scope removal | **Section `A6. Web Application` removed in full — `Q2`, `Q9`, `Q11`, `Q12`.** Pricing auto-population, the Finish field lock, Bundle Width range determination and the Edge critical-attribute default were all `Scope = Other`: they are Orders/Quotes and IQR screen behaviour, and no flat wire shopfloor screen reads, validates against or writes any of them. The section heading went with them, leaving the A-lettering to jump `A3 → A5 → A7` — deliberately not renumbered, on the same principle that keeps `Q##` stable. Counts: **65 open — 48 Shopfloor · 17 `Other`**. Numbering holes are now `Q1`, `Q2`, `Q4`, `Q9`, `Q11`, `Q12`, `Q17`, `Q18`, `Q19`.<br><br>**All four were already carried outside this register as a single item — `OI-81`** in [FlatWire_MasterSpecification.md](LatestDocument/FlatWire_MasterSpecification.md), which covers the web-application details together. Track them there; nothing is lost.<br><br>**Inbound citations are light, unlike `OQ-18`'s.** Only `OQ-2` is cited outside its `OI-81` mirror — [05-SprintPlanAndBacklog.md](MVP-1/ProjectPlan/Development/SprintPlan.md) L628 (*"pricing auto-population to be confirmed (OQ-2) — placeholder for now"*) and [REVIEW.md](MVP-1/ProjectPlan/Development/REVIEW.md) L146 (FW-050 pricing in the load-bearing-TBD list). Retarget both to `OI-81`. `OQ-9`, `OQ-11` and `OQ-12` have no citations at all.<br><br>One internal cross-reference was cleaned: **`Q98`'s Related line cited `Q12`** (edge as a critical attribute) and no longer does. |
| Aug 11, 2026 | Terminology | **All references to "transformer line" removed, repo-wide — 11 of them across 7 files.** The term was the stated precedent for the coreless-coil packaging rules (2 coils per skid, the label fields, the alpha assignment, and the guided packaging prompts), but it is **defined nowhere in this repository or in the wider UAL document tree** — it was used as assumed plant context, including inside the client-facing [OutputCoilCompletion.md](MVP-1/ProjectPlan/Business/Screens/OutputCoilCompletion.md). **Every substantive rule is unchanged** — `FR-335` still enforces exactly two coils per skid, and the rule survives in 25+ places. What changed is only the attribution: where the *inheritance* claim was load-bearing it became **"UA's existing coil packaging"** (`FlatWirePlan.md`, `OutputCoilCompletion.md` §D2 context, master spec `OI-67`, and `Q16` here); where it was decoration it was simply dropped (`FlatWireEndToEndProcess.md`, `FlatWireProcessWalkthrough.md` step 42, `FlatWireShopfloorDashboards.md`, the master spec glossary entry for *Skid*). **`Q16`'s recommendation gained a question rather than losing one:** it now also asks the client **which existing line's convention is being inherited**, since the artifacts asserted a precedent for four months without ever naming a definable one. |
| Aug 12, 2026 | Renumbering | **All questions renumbered contiguously — open `Q1`–`Q60`, decided `Q61`–`Q85`.** Thirteen removals over 11–12 Aug had left the sequence with holes at `Q1`, `Q2`, `Q4`, `Q6`, `Q7`, `Q9`, `Q11`, `Q12`, `Q13`, `Q17`, `Q18`, `Q19`, `Q42` and `Q43`. Numbers were reassigned in **document order**, so they now ascend as the file is read; the Quick Reference table follows the same order. **This reverses the register's standing no-renumbering rule**, which is why [CLAUDE.md](../CLAUDE.md) was updated in the same pass — leaving it unchanged would have made the repo's own guidance contradict its primary register.<br><br>**Every inbound citation was rewritten mechanically in the same pass — 500 `OQ-##` references across 54 files.** Renumbering without that would have been worse than a dangling reference: each citation would have silently resolved to a *different* question. **Citations to the thirteen removed questions were retargeted, not left to rot** — `OQ-1`→`OI-88`, `OQ-2`/`OQ-9`/`OQ-11`/`OQ-12`→`OI-81`, `OQ-6`→`OI-82`, `OQ-7`/`OQ-13`→`OI-83`, `OQ-17`→`OI-69`, `OQ-18`→`OI-49`, `OQ-19`→`OI-85`, `OQ-42`/`OQ-43`→`OI-77`, and `OQ-4`→`Q51` (the machine-tags half that survived it). That mattered because those numbers are now live again and pointing at different questions.<br><br>**Two safeguards, one of which caught a real defect.** Bare `Q##` was rewritten **only inside these two registers**; elsewhere only the canonical `OQ-##` form was touched, because `Q4 2026` is a calendar quarter in fourteen places and a blanket rewrite corrupted every one of them on the first attempt. They were restored and verified at 15 occurrences. `PSG-Q##` and `PLC-Q##` are separate series and were excluded by the pattern throughout — 361 of them are untouched.<br><br>**Change Log entries above this row keep their original numbers.** They record what happened under the old scheme, and rewriting them would falsify the audit trail — read them against the map below.<br><br>**Open register, old→new (document order):** 30→1 · 10→2 · 14→3 · 16→4 · 3→5 · 5→6 · 8→7 · 20→8 · 46→9 · 21→10 · 22→11 · 23→12 · 24→13 · 25→14 · 26→15 · 38→16 · 34→17 · 31→18 · 35→19 · 36→20 · 37→21 · 47→22 · 49→23 · 51→24 · 52→25 · 53→26 · 57→27 · 60→28 · 61→29 · 62→30 · 63→31 · 64→32 · 66→33 · 70→34 · 71→35 · 72→36 · 73→37 · 74→38 · 76→39 · 78→40 · 80→41 · 81→42 · 82→43 · 83→44 · 84→45 · 85→46 · 86→47 · 87→48 · 88→49 · 89→50 · 90→51 · 91→52 · 94→53 · 95→54 · 96→55 · 97→56 · 98→57 · 99→58 · 58→59 · 59→60<br><br>**Decided file, old→new (document order):** 27→61 · 28→62 · 29→63 · 54→64 · 56→65 · 44→66 · 45→67 · 67→68 · 68→69 · 69→70 · 75→71 · 77→72 · 79→73 · 48→74 · 50→75 · 15→76 · 32→77 · 55→78 · 65→79 · 33→80 · 39→81 · 40→82 · 41→83 · 92→84 · 93→85 |
| Aug 12, 2026 | Scope removal | **`Q60` removed as out of scope, and the `Section F — Planning & UI Decisions` heading removed with it.** `Q60` asked whether flat wire orders belong in the existing planning grid or a dedicated view — a planning-screen question, `Scope = Other`, read or written by no flat wire shopfloor screen. Its substance is already carried as **`OI-87`** in the master specification, and its single inbound citation resolves there. **`Q59` was kept and rehomed**, not removed: the OD → weight conversion formula for a spool is spool-lifecycle content, so with the Section F heading gone it now sits under **Section E — Spool Lifecycle**, which is where it belonged. It is also load-bearing — `Q33` and `Q50` both cite it, and its recommendation ties it to `Q20`'s single footage-to-weight factor. **No renumbering was needed:** `Q60` was the last number, so the open register remains contiguous at **`Q1`–`Q59`**. Counts: **59 open — 47 Shopfloor · 12 `Other`**. |
| Aug 12, 2026 | Scope removal | **`Q57` (edger roll-forming coefficients) and `Q58` (multi-order-last without welding) removed; `Q59` renumbered to `Q57`** so the register stays contiguous at **`Q1`–`Q57`**. Both were `Shopfloor` scope, unlike the earlier removals, so each was checked for what it was holding before it went.<br><br>**`Q57` was a pointer entry and its substance is mirrored.** It existed to make the owed edger data visible outside the generation specification — the coefficients themselves are `PSG-D24`–`D26`, `PSG-D31`–`D35`, `PSG-Q27`, `PSG-Q28` and `PSG-Q32`–`Q34` in `PassScheduleGenerationSpec.md`, which carries 105 such items. It had **no inbound `OQ-57` citations**, so removal is clean. **The one consequence:** that specification is an MVP-2 client deliverable, so the owed edger coefficients are no longer visible from any MVP-1 document — including the finding that the **final S3 pass is unedged**, which makes free spread set the delivered width of every FL2/FL3 coil.<br><br>**`Q58` had no `OI-##` mirror and a decided rule depended on it**, so it was folded rather than deleted. `Q73` item 6 read *"where no welding is involved the sequence is free — subject to `Q58`"*; deleting the question would have left that as an unqualified "the sequence is free", which is **reading B** — one of the two competing readings the question existed to arbitrate. Both readings, both transcript quotations and the one-sentence form of the question are now stated inline in `Q73` item 6, with a residual note on the entry saying it is the only surviving record. **A decided entry now carries an open branch**; that is deliberate.<br><br>**One inbound citation was retargeted:** master spec **`OI-66`** cited `OQ-59` for the OD → weight formula and now cites `OQ-57`. Counts: **57 open — 45 Shopfloor · 12 `Other`**. |
| Aug 12, 2026 | Client input + build | **23 Jul 2026 meeting summary ingested; `Q56` corrected and a dancer element authored.** The summary is filed as [ClientCall_2026-07-23_SyncPlan.md](BaseDocuments/ClientCall_2026-07-23_SyncPlan.md) — sixteen clarifications mapped to register IDs, twelve action items, seven decisions. It **predates** the 30 Jul and 6 Aug calls but was ingested after both, which is why it opens with a precedence problem.<br><br>**`Q56` item 1 was wrong and is corrected.** It read *"there is no dancer on FM2 anywhere"* — written to mean *"nothing models one"*, but reading as an equipment claim. **FM1 carries one dancer and FM2 two**, between S1/S2 and S2/S3 (`D-28`, master spec §234). Item 2 (*no tag element*) was accurate and is now **partly closed**: a **read-only** element is published in `[PLC]` v1.1 — `Dancer` on FM1, `Dancer1`/`Dancer2` on FM2, ordinal per `R6` so the paths do **not** inherit the `PLC-Q04` station-rename risk — with new open item **`PLC-Q18`** and commissioning test **C12**. A `Dancer` lookup table and three seeded rows land in the schema, taking it to **28 tables, 25 of them MVP-1**.<br><br>**⚠ The recommendation on `Q56` is now contested and was deliberately not rewritten.** It argues the mode is a **pass schedule parameter**; the 23 Jul summary records tension control as **machine-driven**, set through process settings. The two client statements are tabled against each other in the body with their dates. **Read-only was chosen because it is the half that holds either way** — if the mode turns out to be written, `[PLC §7.2]` gains a row, which is an addition rather than a correction. **A second question rides on it:** if dancers *remove* rather than *apply* tension, `PSG-D27`'s substitution may model behaviour the equipment does not have, and it feeds separating force and roll gap through `F/K`. Both are sent back to Tim O. / Engineering; **neither is resolved here**. `G35` narrowed, not closed. No question opened or closed: **57 open**, unchanged. |
| Aug 12, 2026 | Editorial | **Stale references corrected and revision narrative stripped from the question bodies.** No question opened, closed or changed status; **57 open**, unchanged. **Four references were broken and two of them resolved silently to the wrong question** — the 12 Aug renumbering rewrote bare `Q##` only where the old number appeared in its map, so citations to *removed* questions were left pointing at whatever now holds that number. Fixed: **`Q6`**'s recommendation cited "the `Q6` throughput rates" (a self-reference; the throughput rates are **`OI-82`**), **`Q47`**'s recommendation cited `Q42` for edger blade profiles (`Q42` is now rod bundle gross weight; the profiles are **`OI-77`**), **`Q33`**'s recommendation cited `Q59` (renumbered to **`Q57`** on 12 Aug), and `Q42`/`Q47` carried *"formerly `Q6`"* / *"formerly `Q42`"* tags whose numbers are live again. **`Q5`'s `Q6`/`Q7` citation was checked and is correct** — it was rewritten from old `Q5`/`Q8`. Also **retargeted two dangling `OQ-60` citations** in [dashboard_3_active_run_fl2.html](MVP-1/ProjectPlan/Frontend/Mockups/dashboard_3_active_run_fl2.html) and [dashboard_3_active_run_fl3.html](MVP-1/ProjectPlan/Frontend/Mockups/dashboard_3_active_run_fl3.html) to **`OQ-28`**; they were HTML comments and were missed by the 12 Aug sweep, which found no others repo-wide.<br><br>**Two superseded values removed from live bodies:** the priority legend still gated `High` on **July 1 trials** and `Medium` on **August 1 production**, both superseded by the 17 Aug → 30 Sep window (now UAT late Sep / go-live Q4 2026); and `Q33` asked for the variance spread on a *"~2,000 lb spool"* against the withdrawn default (spools are sized ~**1,800 lb** per the Jul 30 decision recorded on `Q28`). `Q28`'s completed propagation instruction — *"must be removed from Q79, SpoolCompletionNotification.md and the mockup"* — was dropped, the removal having been done.<br><br>**Revision narrative removed where the live rule stands without it**: the re-homing note on `Q42`, the *"from the same consolidation"* provenance chain repeated across `Q46`–`Q52` (raised-date and `PLC-Q##` id kept), `Q51`'s eleven-weeks-invisible archaeology and the eight-items history of the retired `[CONFIRMED]` tag (**the rule that it must not be reintroduced is kept**), `Q53`'s two-things-changed-on-Aug-4 framing, `Q54`'s intermediate-form note for strings written nowhere, and `Q56`'s *"this item previously read…"* self-correction. **Nothing flagged as a sole record was touched** — `Q53`'s observed `Stand8`/`Stand6S1`/`Stand6S2` strings, `Q54`'s old→new measure table and `Q52`'s R-rule renumbering all survive, each now carrying an explicit ⚠ marker. **Section lettering closed up** (`A5`→`A4`, `A7`→`A5`), the holes being a legacy of the retired no-renumbering rule; a stray duplicate separator before this log was removed. **This log is unchanged apart from this row and a reading note at its head.** |
| Aug 12, 2026 | Scope removal | **`Q19` (expected metallic yield per route) removed as out of shopfloor scope, and the register renumbered to stay contiguous at `Q1`–`Q56`.** The question asked for target metallic yield per route (rod → flat direct, rod → round wire → flat, flat → flat re-pass). It was `Scope = Other` and already listed under *Out of shopfloor scope*: no flat wire screen reads it, validates against it or writes it, so it failed the register's own filter rule. **Its substance is not lost — `OI-60`** in [FlatWire_MasterSpecification.md](LatestDocument/FlatWire_MasterSpecification.md) was already its mirror and is now restated as the **sole tracking home**, carrying the route list, the owner (Tim O. / Jeff G.) and the open question of whether the flat → flat re-pass route is real at all. Section `B4. Yield Loss & Planning Inputs` **survives** — the renumbered `Q19` and `Q20` remain under it. Counts: **56 open — 45 Shopfloor · 11 `Other`**; shopfloor is unchanged because the removal was an `Other` row.<br><br>**Unlike the 12 Aug removals of `Q57`/`Q58`/`Q60`, this one was mid-register**, so contiguity cost a renumber rather than nothing: **`Q20`→`Q19` … `Q57`→`Q56`, a uniform decrement of one across the whole range 20–57** — 38 questions. Nothing outside that range moved: `Q1`–`Q18` are untouched and the decided file keeps `Q61`–`Q85` intact, so the 58–60 gap between the two files is unchanged.<br><br>**554 citations were rewritten mechanically in one pass across 49 files**, the heaviest being this register (187), [ClientQuestionsContent.md](MVP-1/ProjectPlan/Tools/ClientQuestionsContent.md) (86), [FlatWire_MasterSpecification.md](LatestDocument/FlatWire_MasterSpecification.md) (52), [FlatWireDecidedQuestions.md](Analysis/FlatWireDecidedQuestions.md) (30) and the 6 Aug ledger (27). A single-pass lookup-table transform was used rather than 38 sequential replacements, because a cascade can shift one number twice. **The rewrite mattered more than a dangling reference would have:** with the range shifting down by one, every missed citation would have resolved silently to a *different, real* question rather than failing visibly.<br><br>**Four exclusions, all deliberate.** This change log keeps its original numbers, as always. **[ClientCall_2026-07-30_SyncPlan.md](BaseDocuments/ClientCall_2026-07-30_SyncPlan.md) was left alone — it is verified old-numbering**, written pre-renumber and never swept (its `Q19` is the min/max tolerance item and it counts `OQ 77 → 81`); the **23 Jul and 6 Aug ledgers were swept**, both having been put on the new scheme on 12 Aug. `PSG-Q##` and `PLC-Q##` are separate series and were excluded by a lookbehind that rejects a preceding hyphen — the 80 `PSG-Q20`–`PSG-Q57` occurrences in [PassScheduleGenerationSpec.md](MVP-2/RequirementDocuments/PassScheduleGenerationSpec.md) are untouched, and that file shows zero changes as the proof. Bare `Q##` was rewritten only in the two registers, the client-content file, the MVP-2 mirror and the two live ledgers; elsewhere only the canonical `OQ-##` form was touched. The `Q4 2026` calendar-quarter trap did not apply, the range starting at 20, and the count is unchanged at 15.<br><br>**⚠ Six citations collided and were retargeted to `OI-60`, not deleted.** Because footage-to-weight moved *into* 19, six lines ended up reading `OQ-19` twice or citing `OQ-19` for metallic yield: [05-SprintPlanAndBacklog.md](MVP-1/ProjectPlan/Development/SprintPlan.md) (the `FW-041` open-questions note, the Critical UAT gate list, and the OQ index row), [phase-12](MVP-1/ProjectPlan/Development/Phases/phase-12-yield-cost-ledger-scrap.md) (the scope call and the OQ-blockers line), [phase-14](MVP-1/ProjectPlan/Development/Phases/phase-14-integration-testing-plc-commissioning-golive.md) (the UAT Critical-OQ list) and [CapacityAndEffortModel.md](MVP-1/ProjectPlan/Development/CapacityAndEffortModel.md) §Phase 12. They were **retargeted rather than dropped**, on the `OQ-18`→`OI-49` precedent of 11 Aug: the yield figures are still owed, so Phase 12's estimate is still provisional and the UAT gate is still unmet. Every *other* `OQ-19` in the repository now correctly means footage-to-weight. |
| Aug 12, 2026 | Decision | **`Q19` (footage-to-weight conversion factor) carries no recommendation — the first question in the register without one.** Removed from both homes: the `Recommendation:` paragraph in this register and the `Recommended answer:` field in [ClientQuestionsContent.md](MVP-1/ProjectPlan/Tools/ClientQuestionsContent.md). The question, its background and the `OI-45` dimensional-basis pointer all stand; what is gone is our proposed answer. **The reasoning is recorded in the register above the filtered index**: the open part is the dimensional basis — nominal or measured gauge and width, and whether the round edge is corrected for — which is a measurement question United Aluminum must answer from its own practice, and a proposed default on the most widely depended-on number in the build risks being adopted as the basis rather than confirmed. **Two header claims were amended** rather than left false: the `Recommendations:` legend line and the paragraph stating that every question carries one now name the exception and say why.<br><br>**This required a tool change.** `Recommended answer` was in `REQUIRED_OPEN` in [build_questions_xlsx.py](MVP-1/ProjectPlan/Tools/build_questions_xlsx.py), where a missing required field is a **fatal** build error, so the deletion alone would have broken the client workbook. The field moved to the optional set beside `Options` and `Already agreed`; it stays in `FIELDS_OPEN`, so the workbook column still renders for the other 55 open questions. The generator's coverage and drift guards are unaffected and both still pass. |
| Aug 12, 2026 | Scope reduction | **23 questions withdrawn as ours to answer rather than the client's, and the register renumbered to `Q1`–`Q33`.** Unlike the 11–12 Aug removals, these did not fail the shopfloor filter — they fail a different test: **each is a decision the build team owns.** Internal design calls (the stop-prompt arbitration, `RodSeqno` scope, the FL3 WIP station, bay uniqueness, the WIP-rejection list screen, *Welds this run* at cold start, the post-check-in landing screen), data defects (rod bundle gross weight), adjacent-module items (costing codes, standard times, the WIP REJ report columns, anneal rules, furnace capacity, tolled cert liability, coil OD/ID limits, tolerance bands, the rework-weld certificate rule, scale-vs-calculated spool weight) and the **five tag-surface items that already go to the controls engineer on `[PLC]`'s own `PLC-Q##` sign-off sheet** (tag-path confirmation, the FM2 station rename, measure names, units, the ordinal convention). Asking the client twice, in two documents, was the defect. **Withdrawn: `Q5`, `Q6`, `Q7`, `Q8`, `Q9`, `Q13`, `Q15`, `Q16`, `Q18`, `Q31`, `Q32`, `Q33`, `Q36`, `Q38`, `Q41`, `Q42`, `Q43`, `Q44`, `Q48`, `Q50`, `Q51`, `Q52`, `Q53`** (pre-withdrawal numbering; bodies recoverable at `d79ce78`).<br><br>**Nothing was deleted before it had a home, and each home was checked for the question, its decided portion and our recommendation.** Eighteen already existed and were **extended where thin** — `OI-56` (+ `OI-38` for the PIN source), `OI-57`, `OI-59`, `OI-64`, `OI-65`, `OI-68`, `OI-72`, `OI-75`, `OI-84`, `OI-86`, `OI-97`, `OI-26`, gap `G21`, and `PLC-Q02` / `PLC-Q04` / `PLC-Q05` / `PLC-Q15` / `PLC-Q17`. **Three were new** — **`OI-107`** (is a WIP-rejection *list* screen in scope), **`OI-108`** (*Welds this run* absent or disabled at cold start), **`OI-109`** (where the operator lands after *Acknowledge & Begin Check-in*) — added to master spec §11.3. **`OI-75` and `G21` were the two that genuinely lacked the content** and now carry the sub-questions and the recommendation; `G21` also gained the FL1/FL3 toggle defect, which the index fix does not cover. **The withdrawal table in the register lists all 23 with their homes**, and says in terms that withdrawn is not answered: the bay-uniqueness gap still blocks the Phase-4 schema freeze, the tolerance bands still gate every SPC control limit, and three tag confirmations are `Critical`.<br><br>**Two §10.3 decision rows lost their key, not their decision** — the spool-weight part 3 authority and the rod-sequencing partial answer are now keyed by subject with a pointer to `OI-56` / `OI-72`, as is the superseded SCADA row (`PLC-Q02`). **Two section headings went empty and were deleted** (`A5. Yield, Cost & Reporting`, `B3. Packaging & Coreless Coil Limits`), and `B4` re-lettered to `B3`. Counts: **33 open — 28 Shopfloor · 5 `Other`** *(4 Critical · 22 High · 2 Medium shopfloor; the `Low` row is gone with the WIP REJ report question)*. |
| Aug 12, 2026 | Renumbering | **Third renumbering of the day: survivors reassigned in document order to `Q1`–`Q33`.** Decided `Q61`–`Q85` did not move. **Open register, old→new:** 1→1 · 2→2 · 3→3 · 4→4 · 10→5 · 11→6 · 12→7 · 14→8 · 17→9 · 19→10 · 20→11 · 21→12 · 22→13 · 23→14 · 24→15 · 25→16 · 26→17 · 27→18 · 28→19 · 29→20 · 30→21 · 34→22 · 35→23 · 37→24 · 39→25 · 40→26 · 45→27 · 46→28 · 47→29 · 49→30 · 54→31 · 55→32 · 56→33.<br><br>**Read the three 12 Aug maps in order** — this one composes on top of the mid-register decrement, which composes on top of the contiguous reassignment. **Footage-to-weight moved for the second time in one day, `Q19`→`Q10`**, so the note that *"every surviving `OQ-19` means footage-to-weight"* is retired: it is now `Q10`, and it remains the register's only question with no recommendation. `Q4` (skid labelling) is the second, by the client's own edit to the workbook.<br><br>**Every inbound citation was rewritten in the same pass — 630 substitutions across 83 files** — and this time **bare `Q##` was included**, which is what repaired the drift below. `CHANGELOG.md` and `ClientCall_2026-07-30_SyncPlan.md` are the two deliberate exclusions. |
| Aug 12, 2026 | Correction | **The citation drift left by the two earlier sweeps was repaired, and the repository is now on one scheme.** Both earlier passes rewrote **only the canonical `OQ-##` form** outside the registers — the stated reason being that `Q4 2026` is a calendar quarter — which left roughly **200 bare `Q##` citations in ~55 files stranded on two older schemes**: every client-facing specification in `MVP-1/RequirementDocuments/` (`WeldEvent`, `SPCCheckpoint`, `ActiveRunMonitor`, `RodCheckout`, `RollAdjust`, `PartialRodReCheckin`, `RodPreCheckin`, `RocCheckin`, `SpoolCompletionNotification`, `WipRejection`, `LineStatusOverview`, `PLCTagSpecification`, `Spool`, `SpoolQueue`, `DieChangeAndManagement`), the DB2A/DB5/DB5A/DB8 mockups and their scripts, four DDL and schema files, `04-APIContract.md`, `06-TestPlanAndTestCases.md`, `MVP-1/README.md`, `WeldEventPopupPlan.md` and `MVP-2/RequirementDocuments/ShiftSummary.md` and `DieManagement.md`.<br><br>**Resolved by subject, not by arithmetic.** The first renumbering was a reassignment in document order, not a shift, so no single offset recovers it; the map in the 12 Aug *Renumbering* row above was composed with the later two and each file's scheme was **detected from citations whose subject is unambiguous** — a paired `OI-##` is a stable id and identifies the question outright. A per-citation keyword rule was tried and abandoned: several citations share one line, so a context window picks up its neighbours' subjects. **The DB5 mockups needed twelve explicit overrides** — their `OQ-50`/`OQ-51` flags predate even the pre-12-Aug scheme, and only the paired `OI-46`/`OI-47` identify them as the no-match path and the hybrid-origin guard.<br><br>**Two mechanical corruptions were undone.** `Q4 2026` had itself been rewritten to **`Q51`** in [FlatWire_MasterSpecification.md](LatestDocument/FlatWire_MasterSpecification.md) (×2), [ProjectPlanPrompt.md](LatestDocument/ProjectPlanPrompt.md) and [01-VisionAndScope.md](MVP-1/ProjectPlan/Business/VisionAndScope.md) — the very failure the bare-form exclusion was meant to prevent, in the one place the exclusion did not apply — and **`PSG-Q34`** had become **`Q17`** in [ClientCall_2026-08-06_SyncPlan.md](BaseDocuments/ClientCall_2026-08-06_SyncPlan.md), a `PSG-Q32`–`Q34` range written with the prefix elided. Both are restored.<br><br>**Also corrected on the way:** gaps `G32` and `G33` in [back-matter.md](MVP-1/ProjectPlan/Development/GapsRegister.md) cited the FM2 rename and the measure grammar one number too high (they now cite `PLC-Q04` / `PLC-Q05`), and the stale register inventory line in [FlatWireEndToEndProcess.md](Analysis/FlatWireEndToEndProcess.md) *("59 items")* was rewritten rather than remapped. |
| Aug 12, 2026 | Rescue | **The `[PLC §4.2]` R-rule renumbering map is preserved here, its last home having been withdrawn.** The register entry for the ordinal-naming question was the only place it was recorded, because `[PLC]` carries no history by rule. When two rules were **withdrawn rather than struck** on 4 Aug 2026, `[PLC §4.2]` renumbered to **R1–R8**: **old R7→R5, old R8→R6, old R9→R7**; **old R5 and R6 withdrawn**; **R1–R4 unmoved** (R4 rewritten); and a **new R8** states the bare-analogue rule. **An R-number cited in any document older than 4 Aug 2026 reads against the old scheme** — use this map, not the current section. The question itself is now `PLC-Q17`. |

---

## [Analysis/FlatWirePlan.md](Analysis/FlatWirePlan.md)

| Date | Changed By | Description |
|------|-----------|-------------|
| Apr 23, 2026 | Plan team | Initial document created |
| Apr 28, 2026 | MOM — Planning & Shopfloor meeting | **Section 5 updated:** alpha generation moved to planning time; stop calculation is fully system-driven; pattern replaced with tabular order→spool→weight grid; spool-to-order allocation (3 scenarios) confirmed; "assign as-is" stock checkbox added. **Traveler:** fully digital traveler confirmed — printing disabled for flat wire. **Q32:** maximum finished coil weight decided at 1,000 lb. Open questions register reference updated to FlatWireOpenQuestions.md (59 questions). |
| May 4, 2026 | Analysis Team — Tim O. review | **Q32 revised** to 1,100 lb (TKUP-2 capacity — revised from 1,000 lb; customer defines limit in orders/quotes). **Q27, 28, 29 Decided:** mid-run alpha handling (5 cases), pass schedule override flow, component failure protocol. **Q33, 39, 40, 41 Decided:** no interleave, camber conditional, burr not measured, system die life tracking. **Q44, 45 Decided:** line speeds TBD by trial; FL1/FL2 independent operation confirmed, 3:1 throughput ratio, FL3 blocks FL1/FL2. **Q48, 50 Decided:** supervisor approval for mid-run checkout and partial-run disposition. **Q30, 47, 49 In Progress.** |
| Aug 11, 2026 | Terminology | **"Transformer line" replaced with "UA's existing coil packaging".** The term was the stated precedent for the 2-coils-per-skid rule but is **defined nowhere in this repository or the wider UAL document tree**. The inheritance claim is kept because it is load-bearing here; the undefined line name is not. The rule is unchanged. `Q16` / `OI-67` now ask which existing convention is actually being inherited. |

---

## [Analysis/FlatWireProcessWalkthrough.md](Analysis/FlatWireProcessWalkthrough.md)

| Date | Changed By | Description |
|------|-----------|-------------|
| July 28, 2026 | Analysis Team | Initial document — sequential 45-step walkthrough from rod procurement to shipment, covering pre-check-in staging, Dashboard 2 check-in gate, in-run events, both route branches, FM2 finishing, coil completion and packing. Equipment facts aligned to the May 21, 2026 client corrections; April-source conflicts catalogued. |
| July 29, 2026 | Analysis Team | Step 8 (pre-check-in) corrected: staging is now a `RodStaging` row set on **Dashboard 2A**, not the retired `Rod.StagedPayoffPosition`/`IsWelded` columns. The `RECEIVED → STAGED` status claim is flagged as **Q67** — SRS §4.2 has pre-check-in committing the coil to `INFLAT` instead, and the conflict is unresolved. See [RodPreCheckin.md](MVP-1/ProjectPlan/Business/Screens/RodPreCheckin.md). |
| Aug 1, 2026 | Client sync (30 Jul call) | **Step 8's coil status resolved in this document's favour.** The client confirmed `INFLAT` is set **only at check-in**, with no welded-not-checked-in status — so the original `RECEIVED → STAGED` wording stands and the SRS §4.2 `PCI` data note is superseded for the status (**Q67**, Critical, unblocks Phase 4). The reqsum / `wip_coil_orders` half of that note is still open. |
| Aug 11, 2026 | Terminology | **"Transformer line" removed.** The term was used as the precedent for the 2-coils-per-skid packaging rule but is **defined nowhere in this repository or the wider UAL document tree** — it appeared 11 times across 7 files as assumed plant context. The packaging rule itself is unchanged; only the undefined attribution is gone. See `Q16` / `OI-67`, which now ask which existing convention is being inherited. |

---

## [Analysis/FlatWireShopfloorDashboards.md](Analysis/FlatWireShopfloorDashboards.md)

| Date | Changed By | Description |
|------|-----------|-------------|
| Aug 11, 2026 | Scope division | **Divided by MVP scope — tagged in place, not split, and one table rescued first.** The inventory gained an **`MVP`** column and all fifteen dashboard sections gained a scope badge: **DB9, DB9A and DB10 are MVP-2**; everything else is MVP-1, **including DB7 and DB7B**, which returned with Phase 9 on the same day. **The alloy lookup table was lifted out of the Dashboard 9 section first** and promoted to the top-level **Alloy Reference Data** — it is MVP-1 reference data (cited by the master specification and by `phase-13`, whose alloy-lookup admin is the non-deferrable half of that phase) that had been buried inside an MVP-2 screen; tagging DB9 without moving it would have carried MVP-1 data out of scope, the same seam class as `CoilCompletionService`. Its `Spring-back factor` column is flagged, not removed — master spec **§10.5** arbitrates springback as a machine stiffness misfiled as a material property, but `AlloyProperty` carries the column and the generation that consumes it is MVP-2. **Two things stayed whole deliberately:** the **Screen Navigation Map**, which spans both scopes and gained an MVP legend instead of being halved (*"a half-ER is worse than none"*), and this **Change Log**. **A note was added under DB7 recording that DB7B has no section here and never had one** — which is precisely how it reached 11 Aug 2026 with no owning specification. |
| Aug 11, 2026 | Consolidation | **This document ceased to be a requirements source.** The last five screens without an owning requirement document — **DB3** (Active Run Monitor), **DB7** (Output Coil Completion & Label), **DB8** (WIP Rejection), **DB10** (Supervisor Shift Summary) and **DB11** (Roll Adjust) — were consolidated into new client-facing specifications in `../MVP-1/RequirementDocuments/`, completing the migration begun on 1 Aug 2026. An authority banner was added at the head, the **Dashboard Inventory gained an "Owning specification" column** (and rows for DB5A, the PLC tag surface and the generation engine, which it had never listed), and each of the fifteen dashboard sections now names its owning document. Bodies are **retained deliberately** as the internal UX and wireframe record — several sections are cited by name from the mockups and the schema seed-data notes — but they are no longer citable as requirements, and the banner states that the owning document wins on any disagreement. Corrected the stale register count in Related Documents (59 → 99) and removed the pointer to the withdrawn consolidated SRS `.docx`. **Two defects were recorded rather than propagated:** the DB11 roll-gap table still shows five rows for three FM2 stands with duplicated position labels, a residue of the 4 Aug correction; and the roll-adjust line scope is stated three different ways across this file (inventory *FL3 Operator*, DB11 *FL1/FL2 Operator*, DB3 action table *FL3 only*), **all of them wrong or incomplete** — `FR-107`–`FR-109` and all three active-run mockups agree on **FL2 and FL3, not FL1**, and the inventory row has been corrected accordingly. |
| Apr 23, 2026 | Plan team | Initial document created — 10 dashboards defined |
| Apr 24, 2026 | Plan team | Added Dashboard 11 — Roll Adjust; updated DB3 quick actions table and navigation map |
| Apr 24, 2026 | Plan team | Added Dashboard 12 — Rod Checkout (pre-run and mid-run modes); updated inventory, navigation map; full analysis in RodCheckout.md |
| Apr 25, 2026 | Analysis team | Dashboard 9 expanded — added Generate from Specs workflow: two-panel modal wireframe, 5 modal inputs, 7-step algorithm with calculation steps, alloy lookup table (5 alloys), calculation summary chips, warnings/error cases table, Apply & Save lifecycle, Draft status behaviour table, updated Access Control with generate and lookup-table edit roles |
| Apr 25, 2026 | Analysis team | Dashboard 9A added — Pass Schedule List ("All Schedules"): toolbar wireframe, stats strip, table column definitions (8 columns including in-use chip), filter/sort behaviour, choice popup (manual vs generate), Generate from Specs shortcut, empty state, navigation table; Dashboard Inventory and navigation map updated |
| Apr 28, 2026 | MOM — Planning & Shopfloor meeting | **Design Principle 8 added:** fully digital traveler — printing disabled for flat wire; coil labels (DB7) unaffected. Open questions register reference updated to 59 items. Shopfloor mockups and UX walkthrough was well received by the business team; clickable demo / staging access requested. |
| May 21, 2026 | Client feedback | **Edger configuration corrected:** FL1 has no Edger — Edge Set removed from DB2 (FL1 rod check-in), DB3 FL1 (active run monitor), DB9 (pass schedule FL1). FL2 Edgers are at S2 and S3 only (not S1); FM2 now has three 6" stands (S1, S2, S3) — updated in DB5 (FL2 spool check-in), DB9 (pass schedule FL3), and all related component tables. **DB4 Weld Event:** Laser Weld option removed — not viable; Induction Weld only. **DB6 SPC Checkpoint:** "Post DB1" checkpoint type added. **DB7 Output Coil Completion:** Gauge and width display changed from average-measured to target value when in tolerance (no average displayed). **DB9 Pass Schedule:** FL1 schedule wireframe corrected to show FL1-only components (DB1, DB2, FM1); FM2 components now correctly shown only for FL3 hybrid schedules. **DB10 Shift Summary:** Broken into per-machine pages (FL1, FL2, FL3); KPI strip (footage & lbs) now reflects the selected machine. |
| Jul 29, 2026 | Analysis team | **Dashboard 2A added — Rod Pre-Check-in Station (FL1/FL3):** bay-state table (NOT STAGED / PRE-CHECKED-IN / ACTIVE / BLOCKED), three-step pre-check-in wizard with the `PRC008` carry-forward gate, Queue panel implementing `TRV004`/`TRV009`, Mark-as-Welded (`WLD010`), pre-check-out (`ModeP`), field definitions and access control. Traces to SRS §4.2 `PCI001`–`PCI008` — requirements that existed only inside the consolidated SRS `.docx` and had no screen, data model, API or phase owner. Dashboard Inventory and Screen Navigation Map updated. Full analysis in [RodPreCheckin.md](MVP-1/ProjectPlan/Business/Screens/RodPreCheckin.md). |
| Jul 29, 2026 | Analysis team | **Dashboard 2A layout corrected.** Bay cards were clipped by a fixed-height row and their action buttons spilled into the weld-readiness strip; the modal bodies pushed their own footer buttons outside the shell, making them unreachable at shorter viewports. Fixed by auto-sizing the bay row, giving the queue internal scrolling, and adding the required flex `min-height: 0`. Bay facts consolidated to one row and bay alerts shortened to one line (the weld strip already carried the longer wording), which brings the design height to exactly **1024px** — so the shopfloor panel renders at 1:1. |
| Jul 29, 2026 | Client requirement | **Dashboard 2A queue reworked for free processing order + order context.** Planned rod sequence is **not enforced** — the operator may stage in any order; validation is current-order membership and availability only. `Seq` split into **Plan** (planning's intended order) and **Run** (actual staging order, blank until processed) with a neutral `⇅` deviation marker — never a warning, since out-of-order is an allowed choice. Added an **order context header** (line · order no · material spec · `n staged / n available / n on order`); progress counts are required because free ordering removes the implicit "what's left" cue a fixed sequence gave. **Alloy and Temper columns removed** — identical on every row of an order, now stated once in that header; Diameter stays per row per `TRV004`. **Location column removed** — not a `TRV004` field, dependent on the open OI-85, and "bay" already means *payoff position* on this screen. |
| Jul 29, 2026 | Analysis team | **Dashboard 2A cold start + bay symmetry.** Payoff 1 was modelled as an always-`ACTIVE` backdrop, so an empty Payoff 1 (cold start, post-checkout, between orders) crashed the first render and left the screen showing a phantom rod with a live weight, and no queue. Both bays now use one state machine and one renderer: all four states apply to either payoff, the wizard disables a bay for being *occupied* rather than for being bay 1, Payoff 2 can be the running bay after a transition, and weld readiness plus Mark-as-welded handle the no-material case. Empty bays state both cold-start routes — stage here, or go straight to rod check-in. |
| Jul 30, 2026 | Client direction | **Dashboard 2A — planned sequence gated by supervisor authorisation.** Staging a rod other than the one planning expects next now notifies the operator and requires a supervisor override (reason + badge/ID + PIN), rather than proceeding silently. Never a refusal, and later-planned rods stay listed and stageable. Queue `Plan` column gained a green `▸` on the expected rod; `⇅` on `Run` now names the authorising supervisor. Supersedes the free-processing-order behaviour recorded the previous day. |
| Aug 1, 2026 | Client sync (30 Jul call) | **Dashboard 2A — off-schedule becomes an automatic station switch; three other rules change.** A rod whose order is booked on the other rod line is no longer notified-and-authorised: **the screen switches to the correct station** and continues, with no message and no override, at both pre-check-in and check-in (**Q38**, reversing the Jul 29 design; the `OffSchedule*` columns are dropped). The FL1/FL3 toggle therefore stops being an operator-only control that merely relabels the chrome — it must **reload the bays and the queue**, and what happens to a part-completed wizard needs specifying (**Q39**/**Q37**, **F13**). Bay states gained a **welded** row: a welded rod may be un-staged **behind a supervisor override** with a documented reason, going to `HOLD` as a rejection (**Q69**/**Q72**, restoring the control removed on Jul 31); `PRE-CHECKED-IN` no longer implies `INFLAT`, which is now set at check-in (**Q68**); and `BLOCKED` gained its exit — the WIP rejection captures the reason, puts the rod on `HOLD` and releases the bay (**Q36** item 3). Wizard step 1 now validates diameter against a **min/max** tolerance, one of **four** pairs (gauge, width, diameter, ovality) whose values are owed by e-mail, so the alloy map stays mock (**Q35**). Not yet applied here: the order-membership rule is knowingly wrong for a **multi-order rod** (**Q70**/**G22**), pending the sequencing answer (**Q73**). |
| Aug 1, 2026 | Analysis team | **Dashboard 2A — weld-readiness strip removed; every control moves onto the bay it acts on.** The 96px strip between the bays and the queue is **gone**. Its narrative was already duplicated by the cards (weight/percentage on the payoff bar, the four weld states in the bay alert, cold start in the empty-bay text); the one sentence no card carried — *induction-weld tail to head* — became the staged card's alert whenever a rod is running. **Mark as welded** moved to the **staged** card (it *is* the incoming rod — `PCI008` defaulting) with all five disabled-tooltips intact; **Welds this run · N** moved to the **active** card (the run *is* the active bay), which means it is now **absent at cold start rather than disabled** — this supersedes **TC-068e**. **Open active run** was removed from the active card: the run monitor is reachable from the app bar and Line Status, and this station's job is staging the *next* rod. The reclaimed 96px + 12px gap went to the queue, which is **~108px taller (about four more rows)**; the screen is now **three body regions, not four**, and still measures exactly 1024px. Button rules formalised: **at most one primary per card** (the active card deliberately has none), a leading icon for actions and a trailing chevron for the two navigations, never both. Queue head gained a standing **"Rods In Queue"** title — it had been an unlabelled empty row since the order-context line and scan box were removed. **Dashboard 2:** *Acknowledge & Begin Check-in* now returns to **Dashboard 2A**, not Dashboard 3, closing the stage → check-in → stage-next loop. Verified in headless Chrome across six viewport heights and all six station states: 1024px exactly, no overflow, no console errors, and the payoff-transition rule (**TC-068**) confirmed live with Payoff 2 running and Payoff 1 staged. |
| Aug 2, 2026 | Analysis team | **New — Dashboard 5A, the FL2 Spool Queue.** FL2 had no view of its own waiting material: FL1 operators have Dashboard 2A's staging queue, and FL2 has no equivalent because `PCI002` excludes it from staging. Separately, `FR-090` has the operator *scan the FL1-printed label* while **Q27** records the client saying they *"select it by spool number"* — both stand, and only the scan had a screen. And "the spool queue", the phrase `FR-326`, `TC-389`, `RodCheckout.md` and phase 7 all use for where an accepted partial spool goes, had **no table, endpoint, screen or status of that name anywhere**. DB5A is all three. **Two modes over one table:** on opening it lists every spool available for processing irrespective of order; entering a spool number resolves that spool's order **server-side in a single call** and narrows the list to that order's spools, marking the scanned one. The **column set never changes between modes**, and a failed scan **leaves the list unchanged** — losing your place to a mistyped digit is the worst thing a screen like this can do. An **unallocated spool is a valid single-row result, not an error** (planning remainders and supervisor-accepted partials legitimately have no order). Deliberately absent: spool age (`Spool` has no creation timestamp, so it is not queryable), location (`Spool.Location` has no writer), and filter/sort furniture. Read-only — it writes nothing. Reached from DB1's nav strip, More Options, the shift-summary spool tile (whose arrow had gone nowhere since it was drawn) and DB5. Backed by a **new** `GET /spools[?spoolAlpha=]`; before it, `POST /checkin/spool` was the only spool endpoint in the contract, so even DB5's scan validated against nothing. Verified in headless Chrome at five viewport sizes: exactly 1024px, scale 1:1 on the panel, no overflow, no console errors, 44 assertions. **Blocked on OQ-27** (what "available" means), **OI-06** (two unmapped status vocabularies), **OQ-76/OI-02** (identifier and format), and — critically — confirmation that **`Spool.OrderNo` is populated from planning**, without which nothing can be resolved. |
| Aug 4, 2026 | Client direction | **FM2 roller sizes corrected — three stands, `S1` 8″, `S2` 6″, `S3` 6″.** Every FM2 component table, roll-adjust panel and generator formula in this document listed **four** FM2 rows: a separate `8" Roller` above `6" Roller S1 / S2 / S3`. That came from this document's own May 21 2026 change-log entry — *"FM2 now has three 6" stands (S1, S2, S3)"* — which was a **misreading** of the client note. **The 8″ roller is S1**, and there is no fourth stand. Corrected here: DB5's pass-schedule table, the FL2/FL3 component strips, the DB1 line-status component list, the DB11 roll-adjust panel and its override history, and the DB9 generator pseudocode (`FM2 S1 gap = gauge × 1.06`, `S2 = × 1.02`, `S3 = × springback` — the multipliers are unchanged; they move from diameter labels to positions, which fixes the earlier state where three formulas covered four stands and the final stand had none). The mandatory stand is stated as **`S3`**, closing **OI-04** — its two "rival" names, `FM2_6inS2` and `6" S3`, were the same physical stand. Edger placement (S2 and S3) and the stand count are unchanged from May 21. Authoritative source remains [`00-foundations.md`](MVP-1/ProjectPlan/Business/BusinessRules.md) §0.3; decision **D-26**. |
| Aug 11, 2026 | Terminology | **"Transformer line" removed.** The term was used as the precedent for the 2-coils-per-skid packaging rule but is **defined nowhere in this repository or the wider UAL document tree** — it appeared 11 times across 7 files as assumed plant context. The packaging rule itself is unchanged; only the undefined attribution is gone. See `Q4` / `OI-67`, which now ask which existing convention is being inherited. |

---

## [BaseDocuments/ClientCall_2026-07-23_SyncPlan.md](BaseDocuments/ClientCall_2026-07-23_SyncPlan.md)

| Date | Change |
|---|---|
| August 13, 2026 | **Wave W8 marked partly executed.** The *AI-assisted development* decision recorded on this call is now costed, in the new [DevelopmentEffortModel.md](MVP-1/ProjectPlan/Development/DevelopmentEffortModel.md) — MVP-1 development effort **2,242 → 1,486 h**, 4.2 developer-FTE — and cross-referenced from `CapacityAndEffortModel.md`. **W8's other three subjects remain open** across all three targets (priority order, D72, service-based throughput architecture), as does propagation of the AI basis into the programme totals and the staffing decision. Annotated in place per this folder's exception: the two propagation ledgers are **maintained, not frozen**. |
| August 12, 2026 | **Initial document.** Ingested the 23 Jul 2026 meeting summary — sixteen clarifications mapped onto the registers, twelve action items, seven recorded decisions. **One conflict (`C6`) and one scope tension (`C16`) are sent back rather than applied.** Three subjects — drawdown-on-temper, rewind scope, and the flat-wire downtime-reason vocabulary — have **no home in any register** and are raised in §6. Recorded out of chronological order: this meeting predates the 30 Jul and 6 Aug calls but was ingested after both, which is why §1 leads with the precedence problem. |

---

## [BaseDocuments/ClientCall_2026-07-30_SyncPlan.md](BaseDocuments/ClientCall_2026-07-30_SyncPlan.md)

| Date | Change |
|---|---|
| August 11, 2026 | **Moved from `Analysis/` to `BaseDocuments/`**, alongside the 6 Aug ledger, so both sit with the client-call material. Sibling links to `FlatWireOpenQuestions.md` and `FlatWireProcessWalkthrough.md` were repointed to `../Analysis/`; the link to the 6 Aug ledger is unchanged (it moved too). Inbound citations updated in `FlatWireOpenQuestions.md`, `back-matter.md`, `FlatWire_MasterSpecification.md` (§A.2 and §A.7) and `ProjectPlan/00-README.md`. **No content change** — the waves recorded here remain W1–W7 executed, W8 cancelled. Note that `BaseDocuments/` is otherwise a read-only business-input folder; this document and the 6 Aug ledger are the two Nagarro-authored exceptions, recorded as such in `CLAUDE.md` and master spec §A.7. |
| August 1, 2026 | Initial document. Mapped the eleven answers from the 30 Jul 2026 client call onto the `OQ`/`OI`/`G` registers, identified the three reversals of already-delivered decisions and the two decided-but-value-pending items, and sequenced the propagation into eight waves across 41 files plus the SRS `.docx`. Ten items sent back to the client as open, blocking or owed. |
| August 1, 2026 | **W8 cancelled and the consolidated SRS removed from the repository** (project decision). `MVP-1/SRS/Shopfloor_Flat_wireSRS_Consolidated_v3.docx` was `git rm`'d; it remains recoverable from history at commit `6096921`. The docx→markdown pipeline no longer needs rebuilding. Recorded the consequence: the surviving `MVP-1/SRS/Shopfloor_Flat_wireSRS.docx` contains **no pre-check-in content**, so `02-SRS.md`'s `FR-###` set is now the only home for the pre-check-out, `INFLAT`-at-check-in and min/max-`CHK007` rules — and pre-check-out's missing requirement ID (OI-44) is now permanent in the source document. |

---

## [BaseDocuments/ClientCall_2026-08-06_SyncPlan.md](BaseDocuments/ClientCall_2026-08-06_SyncPlan.md)

| Date | Change |
|---|---|
| August 11, 2026 | **Moved from `Analysis/` to `BaseDocuments/`**, alongside the 30 Jul ledger, so both sit with the client-call material — this document's source transcript was already in this folder, and the link to it is now a same-folder reference. Sibling links to `FlatWireOpenQuestions.md`, `FlatWireProcessWalkthrough.md` and `Spool.md` were repointed to `../Analysis/`; the link to the 30 Jul ledger is unchanged (it moved too). Inbound citations updated in `FlatWireOpenQuestions.md`, `back-matter.md`, `FlatWire_MasterSpecification.md` (§A.2 and §A.7) and `ProjectPlan/00-README.md`. **No content change, and no wave closed by the move** — **W3–W8 remain documented and not executed**, with W3/W5/W6 still blocked on `Q97` and `Q99`. `BaseDocuments/` is otherwise a read-only business-input folder; this ledger and the 30 Jul one are the two Nagarro-authored exceptions and are **maintained, not frozen**. |
| August 11, 2026 | **§5 re-pointed after the `Analysis/` → `RequirementDocuments/` consolidation.** The five screens that had no owning requirement document — DB3, DB7, DB8, DB10, DB11 — now have one, and `FlatWireShopfloorDashboards.md` is no longer a requirements source. **W3's paths hold but its section numbers may have shifted** (`RodCheckout.md` gained §7.2, `RocCheckin.md` gained §4.3, and two Related Specifications tables were rewritten to drop links into `Analysis/`), so the targets must be re-read rather than trusted. **W8 now has three owning specifications** that must move with the mockups; when W8 was written, `pause_run.js` and `wip_rejection.js` had no owner. W4–W7 unaffected; **W3/W5/W6 remain blocked on `Q97` and `Q99`.** Six new open items (**OI-98**–**OI-103**) were raised by the consolidation and none blocks a wave. |
| August 6, 2026 | **W2 extended: two gap audits of the generation spec, taking it to v1.4 and then v1.5.** Thirty defects, twelve algorithmic. Recorded against W2 because several were caused by it — propagating the edger physics into a thirteen-step sequence written before edgers existed left five steps consuming quantities no step produced. Largest: the **width chain was never computed**, and with **no edger at the mill exit the final width leaves S3**, so `PSG-D08` governs finished-goods width, not only the FL1 spool's. Eight further client items added (`PSG-D31`–`D35`, `PSG-Q32`–`Q34`), two of them High, and `PSG-D32` is `D8` from this call — Step 12A had split orders against take-up **ratings** rather than the planned multiple, roughly halving the unit count. §6 gains rows 5a–5c. **A separate finding, outside this call's scope:** `FR-381`/`384`/`385`/`386`/`387` contradict the generation spec on five counts of physics; arbitration published as master spec **§10.5** and a `REVIEW.md` Tier 1 callout. |
| August 6, 2026 | Initial document. Extracted eight decisions and eleven action items from the 6 Aug 2026 client call and mapped them onto the `Q`/`OI`/`G`/`PSG` registers. **`Q79` closes** (rod consumption sequence: full → partial → multi-order when welding is involved, validated at pre-check-in and check-in), which **resolves `G22`**. **`OI-13`'s flow is decided** (wire break keeps the same alpha; weld recorded at its footage position) leaving only the persistence target, raised as **`G34`**. Added **`Q96`** (no-weld-customer disposition), **`Q97`** (FM2 dancer modes) with **`G35`**, **`Q98`** (edger roll-forming coefficients) and **`Q99`** (does multi-order-last survive without welding). Recorded the correction that **edging is vertical profiling roll-forming, not knifing**, and that **S3 is a skim pass with no edging** — new generation-spec section plus `PSG-D24`–`D26` and `PSG-Q27`–`Q28`. FM2 stand sizes and edger positions independently corroborated (`Q93`, `G29`); FL1/FL2 output weight multiples corroborated (`Q60`). W1 and W2 executed; W3–W8 sequenced across 20 files and not started. |
| August 12, 2026 | **W6 partly executed — the dancer read surface is authored.** `[PLC]` v1.1 adds `Dancer` on FM1 and `Dancer1`/`Dancer2` on FM2, ordinal per `R6`, all `[PROPOSED]` under new **`PLC-Q18`** with commissioning test **C12**; a `Dancer` lookup table and three seeded rows land in the schema. **Read-only deliberately** — the [23 Jul 2026 meeting](BaseDocuments/ClientCall_2026-07-23_SyncPlan.md) recorded tension control as *machine-driven*, which contradicts `D-28`'s two selectable modes on the question of whether the mode is ever written. Read-only is the half that holds either way. `D7`'s question is now **`Q56`** after the 12 Aug renumbering. |

---

## [LatestDocument/FlatWire_MasterSpecification.md](LatestDocument/FlatWire_MasterSpecification.md)

> This is the one document that kept **two** change logs — an `Appendix B` (`Date | Author | Change`) and a
> later `## Change Log` (`Date | Change`) appended after it. Both are reproduced below, in that order and with
> their own column shapes, because neither superseded the other; entries interleave by date across the two.
> **New entries go in the second table.** The `Appendix B` table-of-contents entry was removed from the
> document in the same pass.

### Appendix B — Change Log

| Date | Author | Change |
|---|---|---|
| 30 Jul 2026 | Consolidation pass | **Footage-to-weight resolved as far as the sources allow, and a new duplication found.** §5.4 gains the weight-derivation subsection: the formula `lb/ft = A × 12ρ` with the round-edge area correction `A = t·w − 0.2146·t²`, the per-alloy `k = 12ρ` constants, reference lb/ft for the line's real cross-sections, and the ruling that **a scalar `LbPerFtFactor` is the wrong shape** and must stay NULL. Density is re-sourced to **`united_db..alloys.alloy_density`**, with its unit **verified as lb/in³** from the PIW formula in `Planning_GetorderminPIW`. §5.12 adds the `alloys` touchpoints plus the **`FlatWireDB..Alloys` view** access pattern — the convention already used by six other databases — and the three type/nullability/join-width mismatches it must absorb. `FR-137`, `FR-332` and `FR-383` re-sourced accordingly; `FR-332a` added to stop Dashboard 7's incorrect `0.069 lb/ft` reaching code. **New issue OI-93:** `AlloyProperty` duplicates four `united_db..alloys` columns, one of which (`Draw_max_reduction`) is the generator's core input held locally only as a provisional seed — while story FW-054 pushes more flat-wire alloy data upstream. **OI-45 rescoped** from "the factor is undefined" to the four sign-offs that actually remain, and a second finding recorded: the ±2 % variance threshold in `FR-153` is unreachable from target dimensions, whose tolerance stack is ±2.6 %. Two malformed table rows in §11.2 (OI-67, OI-69) repaired. Appendix A.10 records the read-only excursion into `ual-database` and what it could not establish. |
| 30 Jul 2026 | Consolidation pass | **Initial creation.** Reconciled every artifact in `Flatwire-planning/` — 19 analysis notes, 18 roadmap/phase files, 8 development-plan documents, 6 schema design docs, 15 DDL/seed scripts, 1 CommonDB script, 27 mockups and 8 shared UI assets, 15 business source documents and 3 SRS revisions — into one self-contained master specification. **Resolutions applied:** the July 26 roadmap and `ShopfloorPlan/*` take precedence over the four April 29–30 documents; the four `REVIEW.md` Tier-1 correctness bugs in `APIContracts.md` are corrected in place (the `/passschedule/generate` worked example recomputed, `RollAdjustTrigger` added to `CheckpointType`, the edge-type vocabulary reduced to one domain set with a display mapping, and the `POST /checkin/rod` body extended with the three `NOT NULL` fields it never sent); the timeline is stated only as 17 Aug → 30 Sep 2026 with the 14 Aug Phase-1 gate; the `Rod`-table divergence is resolved in favour of the DDL and ER document, giving **28 tables** with enforced rod-alpha FKs, and the superseded 21/22-table variant is recorded in §10; the `--fw-*` token prefix is confirmed stale in favour of the `--color-*` semantic system; and Dashboard 2 is fixed as the `- New.html` 6-step wizard with the two earlier layouts listed once as retired. **11 contradictions not present in any existing register were found and recorded** in §11.5. Existing files were not modified. |
| 1 Aug 2026 | Client sync (30 Jul call) | **§11 updated with eleven client answers; body sections deliberately untouched.** Seven open items move — **OI-01** `INFLAT` is set only at check-in (the SRS §4.2 `PCI` data note is superseded; the reqsum / `wip_coil_orders` residual stays open); **OI-07** respecified as **four min/max tolerance pairs** (gauge, width, diameter, ovality) with the values owed by e-mail and nothing to be seeded; **OI-44** pre-check-out needs supervisor approval **only when the rod is welded**, where it is a rejection to `HOLD` — which also settles `WLD011` for the un-staging direction and reverses the 31 Jul removal of that control; **OI-70** WIP rejection releases the blocked staging row; **OI-71** *inverted* — a rod may carry more than one order, so "current order only, else refuse" is wrong for the same-rod successor; **OI-73** *reversed* — off-schedule is no longer an override but an **auto-switch to the correct station**, and the `OffSchedule*` columns are dropped; **OI-74/OI-75** the completion basis is the **customer min/max weight** (the 2,000 lb default is withdrawn) and a short close is an **unplanned stop** on the mill 10-90 pattern, with the mid-run coil break restarting the stop from zero. Four added: **OI-94** (rod scheduled on neither rod line), **OI-95** (multi-order sequencing / MVP scope), **OI-96** (panel resolution — gates Phase 1A against the 14 Aug gate), **OI-97** (bundle gross weight, re-homed from OQ-71). **The body of this document still states the superseded rules** — §4 pre-check-in note, §5 tolerance and staging columns, §6 staging contracts, §7 Dashboard 2A — and is scheduled for wave W6 in [`../BaseDocuments/ClientCall_2026-07-30_SyncPlan.md`](BaseDocuments/ClientCall_2026-07-30_SyncPlan.md). |
| 1 Aug 2026 | Build decision | **DB6, DB8 and DC are dialogs, not screens.** SPC checkpoint, WIP rejection and die change moved out of `dashboard_6_spc_checkpoint.html` / `dashboard_8_wip_rejection.html` / `dashboard_die_change.html` into `spc_checkpoint.js` / `wip_rejection.js` / `die_change.js`, opened as popups over the screen the operator is already on; the three `.html` files remain as launchers so every reference to them still resolves. **No requirement text changed** — the requirements are unaffected by the container. What changed is that each dialog now receives its material context from the caller instead of hard-coding one run, which is what finally lets the pre-check-in rejection path (Q36 item 3) be represented: no run, no footage position, and submitting releases the bay. Two hand-offs the spec already described also become real — a gauge-drift or size-change die change opens the SPC checkpoint it mandates, and an out-of-spec checkpoint's *suspend material* opens the WIP rejection with the failing reading carried over. DB6 gains a **read-only mode** for DB1's "SPC · Last check … · View →", which reviews a recorded checkpoint rather than opening a blank form. New open item **Q43**: DB1's "WIP Rejection**s**" nav item reads as a list screen that has never been specified. |
| 1 Aug 2026 | Build decision | **Pause/Resume redesigned; DB12 becomes a dialog; OI-14 closed at four outcomes.** The fifteen pause reasons were a flat radio list ~1100px tall — the last screen in the suite on radio buttons, and tall enough that the dialog was scaled on every window, taking its 14px labels under the shopfloor floor. They are now glove-sized cards in one column per category and the dialog fits the 1280×1024 panel at 1:1. **Rod Checkout is no longer a pause reason** — it was the only one of fifteen that did not pause, rendered identically to the fourteen that did — and is now the `CheckOutRod` **resume outcome**, which `POST /run/{runId}/resume` and `CK_RunPauseEvent_Outcome` already accepted. That closes **OI-14** and supersedes **FR-262**. **Rod Checkout itself (DB12) moved into `rod_checkout.js`**, so Mode B opens over the pause that raised it with the frozen footage carried over rather than navigating away and losing the pause. Four correctness fixes went with the redesign, all of which the mockup could previously get wrong: the payload now carries **`ReasonCode` + `ReasonCategory`** rather than a display label (`Other` used to overwrite its own code with the note text); **notes are mandatory on `Other`**, matching `CK_RunPauseEvent_NotesOther`; footage comes from the caller rather than a `#footage-val` element that **does not exist on the FL1 monitor**, where the dialog had always shown “Footage —”; and duration reads **h:mm:ss** past an hour instead of reporting a 90-minute stop as “90:00”. New requirements `FR-261a`–`FR-261b` and `FR-266a`–`FR-266b` record the rules; no existing number was reused. |

### Change Log

| Date | Change |
|---|---|
| Aug 6, 2026 | **`CoilTraceability.SpoolAlpha` — the coil → spool edge, which existed nowhere.** `VARCHAR(20)` NULL, `FK_CoilTraceability_Spool` → `Spool.Alpha`, filtered index `IX_CoilTraceability_SpoolAlpha` on `IS NOT NULL`. **`FR-333` requires the chain `rod → spool → coil` and the schema could not produce it:** `CoilOutput` has no spool column, and `RunId` cannot substitute because `SpoolCheckin.RunId` is non-unique and `CoilOutput.RunId` is many-per-run, so the join yields a set of spools rather than the spool. Placed on the range-grained traceability row rather than on `CoilOutput`, since a spool running out mid-coil makes a header column wrong; a separate `SpoolCoilMapping` junction table (the client's May 2026 design) was **rejected** as a footage-less duplicate of an edge this table already owns. `NULL` is correct and common — FL1 standalone and rod-fed FL3 runs have no input spool — so `NOT NULL` was rejected too. Verified on a clean rebuild: spool-fed row accepted, one spool feeding **two** coils accepted (the 1,800 lb → 2 × 900 lb case), one coil spanning **two** spools accepted, a bogus spool rejected by the FK, and `trg_CoilTraceability_NoOverlap` still fires. **Table count unchanged at 27; FKs 42 → 43.** Closes neither `FR-172` (`Spool.ParentRodAlpha` is still singular, so a multi-rod welded spool loses parents) nor `OI-25` (run-cumulative vs coil-local footage frames), on which the attribution's accuracy depends. |
| Aug 6, 2026 | **Die-life columns on `Drawer` (§5 lookup schema).** `Drawer` gains **`LastGrindingFeet`** (`DECIMAL(10,2)` NOT NULL DEFAULT 0) and **`TotalFeetAllowed`** (`DECIMAL(10,2)` NULL), with `CK_Drawer_LastGrindingFeet` (≥ 0) and `CK_Drawer_TotalFeetAllowed` (> 0 when set). Sourced from the `Drawer` sheet of `BaseDocuments/flatwire tables.xlsx` — a **May 2026 proposed design that was never built** — with semantics taken from `DieChangeAndManagement.md` §4.2/§4.4 rather than invented: `LastGrindingFeet` is feet run **since** the last grind (a resettable counter, **not** the reading *at* it, which is what the name suggests), and `TotalFeetAllowed` is the scheduled-life threshold, set lower on a reconditioned die. **No `LastGrindingFeet ≤ TotalFeetAllowed` constraint** — *overdue* is a real state §5.10 displays, not a data error. `TotalFeetAllowed` seeds **NULL**, not an invented limit: **OI-77 / OQ-83** has die-life tracking decided but the thresholds TBD, the same discipline already applied to `AlloyProperty`'s rod-diameter tolerances. **`OI-41` is narrowed, not closed** — `Drawer` is a die-**size** catalogue (13 rows, one per hole diameter), so the counter accumulates against a size rather than a physical tool; registration, condition, status and disposition history are still missing and **Phase 6 still depends on Phase 13**. Nothing maintains the columns automatically either: `DieChangeEvent` identifies its dies by `OldDieSizeIn`/`NewDieSizeIn` decimals with **no `DrawerId` FK**, so run footage cannot be attributed to a `Drawer` row — Maintenance-maintained until that FK or the PLC die counter lands. **Table count unchanged at 27; no new FKs or indexes.** |
| Aug 4, 2026 | **FM2 roller-size correction (client).** FM2 has **three** stands — **S1 = 8", S2 = 6", S3 = 6"** — with edgers at S2 and S3 and S3 final. This is not a digit swap: the repo had modelled a **separate 8" roller upstream of three 6" stands (four components)**, a misreading of the May 21 note recorded in `00-foundations.md` §0.3 as *"three 6-inch stands"*. The 8" roller **is S1**, and `FM2_6inS3` never corresponded to real equipment. New decision **D-26** supersedes **D-21**. Component vocabulary becomes **position-only** — `FM2_S1` / `FM2_S2` / `FM2_S3` — and roll diameter moves into a new **`Stand.RollDiameterIn`** column (12.000 / 8.000 / 6.000 / 6.000), because diameter-in-the-name is what let the error hide for ten weeks. Mapping: `FM2_8in`→`FM2_S1`, `FM2_6inS1`→`FM2_S2`, `FM2_6inS2`→`FM2_S3`; `Stand.Id` 1–4 keep their meaning and Id 5 is removed. `FR-094`, `FR-096`, `FR-297`, `FR-386` and `FR-387` reworded — **`FR-387`'s multipliers are unchanged** (1.06 / 1.02 / springback), they simply move from diameter labels to positions, which incidentally fixes a pre-existing defect where FM2's final stand had no gap formula. `CK_PSC_ComponentName` and `CK_RollOverride_Component` updated; the `Stand` seed drops 5 rows → 4. **Two open items close. `OI-04`** (mandatory stand: `FM2_6inS2` or `6" S3`?) was never a real contradiction — both named the same physical stand, now `FM2_S3`. **`OI-36`** (the final stand has no tag path) is void: the client's published map carries exactly **three** FM2 stations, all `[CONFIRMED]` by observation, so it was complete — the stand with no path was the phantom. PLC stations are renamed to bare position (`FL2.FM2.S1/S2/S3`) pending controls-engineer sign-off as **`PLC-Q04`** / gap **G32**, with the as-published mapping preserved in `[PLC §4.3]`. `PassScheduleGenerationSpec.md` §3.3.5's worked illustration is recomputed at **k = 3**, and §3.3.2's bite condition now states that **S1's larger radius admits ~1.33× the draft** of a 6" stand. |
| Aug 4, 2026 | **HMI/SCADA descoped.** Dashboard 13 (HMI Line Schematic), Dashboard 14 (SCADA Trends) and the Machine View tab on the active run monitor are **withdrawn at client request**. `FR-111`, `FR-112`, `FR-114`, `FR-425`, `FR-440`–`FR-451` and `FR-460`–`FR-470` are marked **withdrawn** — numbers retained, never renumbered — and `FR-113` **reworded**, since it asserted a rule about "the active tab" that outlives the tabs. Both mockups and `HMIAndSCADALayout.md` are deleted. **Descope-ladder rung 7 is removed entirely:** its 67 h stops being *recoverable* effort and becomes *never-planned*, so Phase 5 drops 221 → ~154 h and the programme 3,727 → ~3,660 h, but the ladder loses its largest optional rung and Phase 5 is no longer deferrable. **Nothing structural was removed:** all six run event markers still land on the DB3 traces and no hub event, endpoint, table or column is deleted — every DB13/DB14 reference in the real-time and tag tables was a *consumer* entry, not a row. `Q51`/`OQ-51` is **superseded**, because Dashboard 14 was its answer. §4.21 and §4.22 replaced with withdrawal notices; the approved-screen count drops 22 → **20**. **PLC tag surface consolidated.** The surface existed in six partial, mutually contradictory copies; it now has one home in [`PLCTagSpecification.md`](MVP-1/ProjectPlan/Architecture/PLCTagSpecification.md) (client-facing, `[PLC]`, with its own `PLC-Q##` register) plus `MVP-1/DevelopmentPlan/PLCTagImplementation.md` (internal). This document’s tag-surface section is reduced to a pointer, keeping only what is genuinely its own job. §6.8 keeps only the `INT007` *no new integration layer* sentence. **`OI-36` escalated Medium → Critical** and moved to §11.1: the *write* side already pushes a roll gap and an edge configuration to FM2 6″ S3, for which **no read path exists in any document**, so it blocks the FL2/FL3 push rather than a schematic node — and with DB13 descoped its old *"schematic node"* impact clause is void. §11.5 gains four contradictions surfaced only by reading the six copies as one: speed **target vs limit**, the **edger paths that exist nowhere**, **FM2’s namespace on FL3**, and **units in the path**. |
| Aug 6, 2026 | **Three corrections from the 6 Aug client call.** **(1) `OI-13` is half-closed** — the wire-break *flow* is decided and only the persistence target is missing (gap **G34**). The machine stops, a physical component trips a tag, the system prompts for the stop reason, and on *wire break* it offers **WIP reject** or **weld and continue**. **Weld-and-continue keeps the same alpha** — the wire is still one piece — with the weld recorded at its **footage position** and the material flagged in SCADA; consistent with **Q61** Case 1. Applied to the §3.5 event table, the §4.13 gap note, the §6 traceability table and the **OI-13** register row. The customer-accepts-no-welds branch is new **Q55**. **(2) `D-27`** — the FM2 edgers are **inter-stand** (between S1/S2 and between S2/S3) with **no exit-side edger**, and edging is **vertical profiling roll-forming, not a knifing cut**: nothing is trimmed, width is pushed into length and a slight centre bulge, the first edger converts round to square, and **S3 is a skim pass with no edging**. This gives the generation engine a formula subset it does not have (`PSG-D24`–`D26`, **Q57**) and confirms *“edgers at S2 and S3”* as **entry-side**, leaving the **G29** tag proposals unchanged. **(3) `D-28`** — **FM2 carries two dancers, each with dancer mode and tension mode**, which nothing in the design models and which qualifies `PSM012` (§3.4 glossary amended: the derived-from-speed rule holds **in dancer mode**); open as **Q56**, gap **G35**. Also corroborated without change: **D-26**'s stand sizes, re-stated by the client from the generation spec. §10.2 retitled, since it now carries corrections from three dates. Full record in [`../BaseDocuments/ClientCall_2026-08-06_SyncPlan.md`](BaseDocuments/ClientCall_2026-08-06_SyncPlan.md). |
| Aug 6, 2026 | **`FR-380`–`FR-391` reconciled against the generation specification — new §10.5.** A gap audit of [`PassScheduleGenerationSpec.md`](MVP-2/RequirementDocuments/PassScheduleGenerationSpec.md) (issued as **v1.5**) found that the authority split recorded in `CLAUDE.md` — that document winning on *physics*, this one on *contract shape and arithmetic* — **no longer holds**, because four revisions between 4 and 6 Aug changed the arithmetic itself. **Five contradictions, all in the unsafe direction. `FR-381`:** `D_pre = sqrt(4·t·w/π)` is the **zero-elongation lower bound used as the answer** — `[PSG §3.3.3]` says doing so "produces flat wire that is **under-width**" — and it omits the **round-edge correction**, worth **0.0057″** on the published case, "larger than a typical die increment". **`FR-384`:** snapping to *nearest* violates `R36`/`V39`, which forbid snapping the **final** die below a bounded entry — and since `FR-381` always yields a bounded entry, it violates it on **every** schedule, silently, because the reduction checks test reduction rather than width. **`FR-385`/`FR-387`:** `gauge × springback factor` and the `1.06`/`1.02` multipliers set the gap **above** target where `h₁ = S₀ + F/K` makes the compensation **negative** and **load-dependent**; they also conflate *springback* (a material property) with *mill spring* (a machine stiffness), which `[PSG §3.3.7]` names explicitly as the error that hides the need for mill calibration, carry **no edger term**, and treat `S3` as the gauge stand where `D-27` makes it a **skim**. **`FR-386`:** setting `routeMode = Hybrid` from the aspect-ratio test conflates a **geometric** question with a **metallurgical** one and can select the route with **no intermediate anneal** for material that needs one; its non-hybrid default also bypasses `FM2_S1` and `FM2_S2`, leaving the **skim stand as the only active stand**. **A worked case that fails both documents:** this section's own corrected example — 0.375″ rod to 0.125″ × 0.875″ — has **0.98% of area margin** where the flattening pass needs ~**11% elongation**, so it needs a *larger* rod; the generation spec now rejects it at Step 3A, `FR-383` reads it as a no-draw case. **Contract shape is unaffected** — the envelope, `FR-389`, `FR-390` and `FR-391` all stand — and **the FRs are not rewritten here**; §10.5 records the conflict, adds the row to §10.4, and warns at the head of the FR table that the five formulas must be **rebuilt to the generation spec before `FW-013`**. The 2% no-draw threshold in `FR-383` is a separate case: the generation spec has **not adopted** it (`PSG-D28`) rather than contradicted it. §3.4's **Dancer** entry gains a pointer that **`Q56` and `PSG-Q29` are the same question** — one answer closes both — and that tension **reduces roll separating force**, so it moves the gap as well as the force check. |
| Aug 11, 2026 | **Two mockup filenames normalised — no design change.** `dashboard_3_active_run_v2.html` → **`dashboard_3_active_run.html`**, and `dashboard_2_rod_checkin - New.html` → **`dashboard_2_rod_checkin.html`**. Both suffixes had become misnomers: each marked a revision of a rival layout that no longer exists (the left-rail DB3 screen was withdrawn 1 Aug 2026; the DB2 interim and `- Old.html` layouts were deleted 11 Aug 2026, all recoverable at `2a0426b` / `d79ce78`). **Every reference across the repo was repointed**, and the prose that described the deleted files by these now-reused filenames was rewritten to name the *layout* rather than the *filename* — read "the interim single-page layout" and "the earlier left-rail layout" wherever a retirement is recorded, because the plain filenames now denote the **approved** screens. `- Old.html` remains the only DB2 variant suffix still in use, and it names a deleted file. |
| Aug 11, 2026 | Terminology | **"Transformer line" replaced with "UA's existing coil packaging".** The term was the stated precedent for the 2-coils-per-skid rule but is **defined nowhere in this repository or the wider UAL document tree**. The inheritance claim is kept because it is load-bearing here; the undefined line name is not. The rule is unchanged. `Q4` / `OI-67` now ask which existing convention is actually being inherited. |

---

## [LatestDocument/ProjectPlanPrompt.md](LatestDocument/ProjectPlanPrompt.md)

| Date | Changed By | Description |
|------|-----------|-------------|
| Jul 30, 2026 | Plan team | Created. Generation prompt for the seven-document `MVP-1/ProjectPlan/` set (Vision & Scope · SRS with NFRs folded in · HLD + ER · API contract · Sprint plan/backlog · Test plan + cases · Deployment runbook + rollback), grounded in `LatestDocument/FlatWire_MasterSpecification.md` as primary source with the repo's precedence, traceability, terminology and timeline rules made binding. |

---

## [MVP-1/DBChanges/GapAnalysis.md](MVP-1/ProjectPlan/Database/GapAnalysis.md)

| Date | Change |
|---|---|
| August 12, 2026 | **First issue.** Gap analysis of every artifact in `MVP-1/DBChanges` — the five schema documents, the fifteen SQL scripts, the ER documentation and the WIP-stations script — against the SRS, the HLD, all sixteen requirement specifications, `phase-01c`, `PLCTagImplementation.md` §3 and the `OI-##`/`G##` registers. **Establishes the as-built baseline as 25 tables · 33 FKs · 41 indexes (39 non-clustered + 2 filtered-unique) · 1 procedure · 1 trigger**, counted from the scripts, superseding the four different table counts stated across `phase-01c` (24, 25 and 28 in one document), the root `CLAUDE.md` (24) and the ER documentation footer (27). Findings are classified A–E. **Fifteen are new and unregistered**, the most consequential being: **A1** the FW-001/FW-002 shared-schema migration (8 renames, 2 columns, the `INFLAT` status) has **no script at all** against a 14 Aug Phase-1 gate, while every FlatWireDB table has executable DDL; **D1** the 30 Jul asymmetric-tolerance decision reached `AlloyProperty` only, so `SpcMeasurement.InSpec` — `ABS(actual−target) <= tolerance`, **PERSISTED** — silently stores the symmetrised answer, and `SpcCheckpoint.AllInSpec`, `CoilOutput.GaugeInSpec`/`WidthInSpec` and the print-target-or-measured rule all descend from it; **A5** `FR-075`'s per-write tag audit log is the only persistence sink in `PLCTagImplementation.md` §3 with no DDL reference; **B3**/**B4** the `CoilOutput` weight-basis and approval columns and the `RollOverride` revert columns, both decided requirements (`S-18`–`S-24`, `FR-212`) with nowhere to be recorded — the same class of defect as `G24`; and **C1** `AlloyProperty` is orphaned now that its only child left with MVP-2, so the `CHK007`/`FR-065` tolerance lookup can silently miss. Proposes `OI-107`, `OI-108` and `G37`; **creates no register numbers**. Closes with a *checked and found sound* list — MVP split mechanics, idempotency, seed coverage, the FM2 three-stand model, the `RodStaging` invariants, `CoilTraceability.SpoolAlpha` and the closed `G24` — so verified areas are not rediscovered. |

---

## [MVP-1/DBChanges/Schema/FlatWireSchema_Lookup.md](MVP-1/ProjectPlan/Database/Schema/FlatWireSchema_Lookup.md)

| Date | Change |
|---|---|
| August 6, 2026 | Added **`Drawer.LastGrindingFeet`** (`decimal(10,2)` NOT NULL DEFAULT 0) and **`Drawer.TotalFeetAllowed`** (`decimal(10,2)` NULL) with `CK_Drawer_LastGrindingFeet` and `CK_Drawer_TotalFeetAllowed`, giving the die-life counter and threshold their first home in the schema. Semantics taken from `DieChangeAndManagement.md` §4.2/§4.4, not invented. Source: the `Drawer` sheet of `BaseDocuments/flatwire tables.xlsx` (`Lastgrindingfeet` / `TotalFeet`), a May 2026 proposed design that was never built. **`OQ-83`'s threshold now has a column to land in** — the values themselves are still TBD, so `TotalFeetAllowed` seeds NULL rather than an invented engineering limit (same discipline as this table's rod-diameter tolerances). **`OI-41` is narrowed, not closed**: `Drawer` is a die-size catalogue, so the per-physical-die inventory is still missing and Phase 6 still depends on Phase 13. The columns are added in the `CREATE TABLE` only — an already-deployed `FlatWireDB` needs a teardown + rebuild to pick them up, since `01_Lookup`'s `CREATE` is skipped whenever `Drawer` exists. |
| July 29, 2026 | Documented a gap on `AlloyProperty`: `CHK007` validates **incoming rod diameter** against nominal ± a lookup tolerance, but the only tolerance columns here are `GaugeToleranceDefault`/`WidthToleranceDefault`, which are flat-wire *output* dimensions. No rod-diameter tolerance column exists anywhere in the schema, so `CHK007` is not implementable as written (**Q35**). No DDL change yet — the column shape and owner need confirming first. |
| July 29, 2026 | Added **`PayoffPosition`** with three pinned rows (Payoff1, Payoff2, TraversingTakeup), giving `FlatWireRunDetail.PayoffPositionId` an enforced FK parent and resolving the "payoff modelled three ways" contradiction in `REVIEW.md` #15. Seeded in the DDL rather than the sample-data script because the FK depends on the rows existing. |
| July 26, 2026 | Added `AlloyProperty` lookup (FW-004); added `IsActive` to `SpoolConfiguration`; added unique constraints on `Drawer.Name`, `Edger.Name`, `SpoolConfiguration.Name`; corrected bare `decimal` weights to `decimal(8,2)`. Retargeted to `FlatWireDB`. |
| August 1, 2026 | **`AlloyProperty` tolerances widened to min/max pairs, and two dimensions added (Q35, client 30 Jul 2026).** `GaugeToleranceDefault` / `WidthToleranceDefault` become `…MinusIn` / `…PlusIn` pairs, joined by `RodDiameterToleranceMinusIn` / `…PlusIn` and `RodOvalityMaxIn`. Gauge and width carry their existing symmetric values into both columns; **rod diameter and ovality are NULL until the values arrive by e-mail**. `CK_AlloyProperty_RodDiaTol` makes the diameter band all-or-nothing, written with an explicit `IS NOT NULL` pair after a rebuild showed the naive form accepted half a band (a CHECK constraint accepts UNKNOWN). Ovality moves off the hard-coded `0.003"` in `CheckinImplementationPlan.md`. DDL, seed script and 15 constraint tests re-run clean; table count unchanged at 27. |

---

## [MVP-1/DBChanges/Schema/FlatWireSchema_Mapping.md](MVP-1/ProjectPlan/Database/Schema/FlatWireSchema_Mapping.md)

| Date | Change |
|---|---|
| August 6, 2026 | Added **`CoilTraceability.SpoolAlpha`** (NULL, FK → `Spool.Alpha`) — the coil → spool edge existed nowhere, so `FR-333`'s `rod → spool → coil` chain was unsatisfiable. `RunId` cannot substitute (`SpoolCheckin.RunId` non-unique, `CoilOutput.RunId` many-per-run → the join returns a set). Put on the range-grained row, not `CoilOutput`, so a coil spanning two spools is expressible; a separate `SpoolCoilMapping` junction table was rejected as a weaker duplicate carrying no footage. **Table count unchanged at 27; FKs 42 → 43.** The stated FK total is corrected to the as-built count. |
| August 6, 2026 | `Drawer` gains **`LastGrindingFeet`** and **`TotalFeetAllowed`** — die-life counter and threshold, sourced from the `Drawer` sheet of `BaseDocuments/flatwire tables.xlsx` (a May 2026 proposed design never built) with semantics from `DieChangeAndManagement.md` §4.2/§4.4. **Table count and FK count are unchanged** (28 tables / 37 FKs) — two columns on an existing lookup, no new relationships. `OI-41` (the missing per-physical-die inventory) is narrowed, not closed. |
| July 26, 2026 | Retargeted to `FlatWireDB`. Added 3 production-readiness tables (`AlloyProperty`, `PassScheduleChangeLog`, `RunReading`) → 25 tables, 37 FKs. Added FKs `PassSchedule.Alloy→AlloyProperty`, `PassScheduleChangeLog→PassSchedule`, `CoilOutput.PassScheduleId→PassSchedule`, `RunReading→FlatWireRun`, `Spool.SourceRodAlpha→Rod`. Added component `FM2_6inS3` (May-21-2026 revision). Documented MMS ID / spool-numbering resolutions. See per-domain docs for column-level changes; schema validated on SQL Server 2019. |
| August 12, 2026 | **`Dancer` lookup added** — FM1 carries one dancer, FM2 two, between S1/S2 and S2/S3 (`D-28`). Equipment capability only: `Position`, `Ordinal` (1 = upstream), `SupportsTensionMode`, `DefaultMode`. **No pass schedule column** — whether dancer mode is scheduled or machine-side is unresolved (`OQ-56`), and `PassScheduleComponent` is MVP-2 regardless. Schema is now **28 tables**, 25 of them MVP-1. |

---

## [MVP-1/DBChanges/Schema/FlatWireSchema_Materials.md](MVP-1/ProjectPlan/Database/Schema/FlatWireSchema_Materials.md)

| Date | Change |
|---|---|
| July 29, 2026 | Retired `Rod.StagedPayoffPosition`, `Rod.IsWelded` and `CK_Rod_StagedPayoff`. Pre-check-in staging moved to the new `RodStaging` table (`FlatWireSchema_Runs.md`), which enforces one-rod-per-payoff-bay via filtered unique indexes — an invariant a nullable column pair on `Rod` could not express. Implements SRS §4.2 `PCI001`–`PCI008`. |
| July 26, 2026 | `Rod` kept as FlatWireDB-local master; `TareWeightLb` now computed PERSISTED; added carry-forward (`FootageRunToDate`, `RemainingWeightEstimateLb`), pre-check-in staging (`StagedPayoffPosition`, `IsWelded`), `InventoryType`, audit + `RowVersion`. `Spool` added `SourceRodAlpha`, `OriginRouteMode`, `LineId` CHECK, audit + `RowVersion`. Corrected bare `decimal` weights to `decimal(8,2)`. Retargeted to `FlatWireDB`. |

---

## [MVP-1/DBChanges/Schema/FlatWireSchema_QualityOutput.md](MVP-1/ProjectPlan/Database/Schema/FlatWireSchema_QualityOutput.md)

| Date | Change |
|---|---|
| August 6, 2026 | Added **`CoilTraceability.SpoolAlpha`** (`varchar(20)` NULL, FK → `Spool.Alpha`, filtered index `IX_CoilTraceability_SpoolAlpha`). The coil → spool edge did not exist anywhere in the schema, so `FR-333`'s `rod → spool → coil` chain was not satisfiable: `CoilOutput` has no spool column, and `RunId` cannot substitute because `SpoolCheckin.RunId` is non-unique and `CoilOutput.RunId` is many-per-run, so the join returns a set. Placed on the range-grained row rather than on `CoilOutput` so a coil spanning two spools is expressible, and preferred over a separate `SpoolCoilMapping` junction table (the client's May 2026 proposal), which carries no footage. **Table count unchanged at 27; FK count 42 → 43.** Does not close `FR-172` (singular `Spool.ParentRodAlpha`) or `OI-25` (run-cumulative vs coil-local footage frames). |
| July 29, 2026 | `RodCheckout.Mode` extended with **`ModeP`** (pre-check-out — un-stages a pre-checked-in rod that was never checked in). Added `CK_RodCheckout_ModeP` and `CK_RodCheckout_ModeB` so the per-mode field rules are enforced in the database rather than only stated in prose (`REVIEW.md` #20). Added a modes-compared table. |
| July 26, 2026 | `SpcMeasurement`: added `ToleranceValue`; `Deviation` + `InSpec` now computed PERSISTED. `CoilOutput`: `FootageFt` → `decimal(10,2)`; added `PassScheduleId` + `PassScheduleSnapshot` (OQ-64/NFR013), `NetWeightOverrideLb` + `ScaleWeightLb` (OQ-20/packing), `StagingLocation`, expanded `SkidStatus` domain, audit + `RowVersion`; corrected bare `decimal` weights to `decimal(8,2)`. `CoilTraceability`: overlap now enforced by trigger. `RodCheckout`: `NewRodStatus` CHECK. Retargeted to `FlatWireDB`. |
| August 1, 2026 | **`RodCheckout` gains supervisor-approval columns (gap G24) and the welded Mode P rule (OQ-69, client 30 Jul 2026).** Added `WasWelded`, `ApprovedBy`, `ApprovedAt`, `OverrideReason` — this table previously had **no approval columns at all**, so the OQ-74 and OQ-75 approvals decided on 4 May 2026 had nowhere to be recorded. Added `CK_RodCheckout_WasWelded` (welded removals are Mode P only), `CK_RodCheckout_Approval` (all-or-nothing stamp), `CK_RodCheckout_ModePWelded` (a welded pre-check-out needs supervisor approval, a documented reason and `NewRodStatus='HOLD'` — it is a rejection, not a return) and `CK_RodCheckout_ModeBApproved` (retro-enforcing OQ-74). Sample data gains a Mode P welded row and an approver on the existing Mode B row, which failed the rebuild without one. DDL rebuild clean, 15 constraint tests pass. |

---

## [MVP-1/DBChanges/Schema/FlatWireSchema_Runs.md](MVP-1/ProjectPlan/Database/Schema/FlatWireSchema_Runs.md)

| Date | Change |
|---|---|
| July 31, 2026 | **`Blocked` is now a reachable state — staging commits the `RodStaging` row before the inspection gate.** Q72 items 1–2 decided: `Blocked` stays **derived** (`Status='Staged'` + any inspection column `='Fail'`), and `POST /staging/rod` returns `201` with `state:"Blocked"` instead of `422`-and-write-nothing. A failed bundle is already on the payoff, so writing no row reported an occupied bay as free. **Q72 item 3 — what *releases* a blocked row — is the blocking residual**: `Status` has no value for a WIP-rejection outcome, and a fourth value would move the row outside `UX_RodStaging_Bay`'s filter and free a bay that is not free. From the Dashboard 2A UX review finding F2 (`Analysis/Dashboard2A_UXReview.md`, deleted 1 Aug 2026 — in git history at `2a0426b`). |
| July 29, 2026 | **Free rod processing order.** Planned sequence is not enforced — staging validates only current-order membership and availability. Added **`RodStaging.PlannedSeqno` (int NULL)** and redefined `RodSeqno` as the **actual** processing sequence assigned at pre-check-in, monotonic per line; `PlannedSeqno` snapshots the planned position (same snapshot pattern as pass schedule id/version on the run record). Added `CK_RodStaging_SeqPos` / `CK_RodStaging_PlannedSeqPos`; deliberately **no** constraint relating the two, since a difference is the normal case. Also annotated that `PCI008`'s "enforce sequencing" means *physical weld* sequencing, not planned order (**Q70** partly resolved). |
| July 29, 2026 | Documented that the Dashboard 2A / `GET /payoff/status` **`Blocked`** bay state is **derived** (`Status = 'Staged'` + any inspection column `= 'Fail'`), not a fourth `Status` value — adding one would fall outside the `UX_RodStaging_Bay` filter and free a bay that is still physically occupied. Flagged **Q72**: nothing currently writes such a row, because a failed inspection routes straight to WIP Rejection without committing, so `Blocked` is unreachable in practice; also asks whether `InspectionNotes` should be constraint-enforced when any item fails. No DDL change. |
| July 29, 2026 | Added **`RodStaging`** (pre-check-in / payoff staging) implementing SRS §4.2 `PCI001`–`PCI008`, `WLD010` and `TRV004`; two filtered unique indexes enforce one-rod-per-bay and one-bay-per-rod. `FlatWireRunDetail.PayoffPositionId` now has a real FK parent (`PayoffPosition`, REVIEW.md #15). `WeldEvent`: added `OutgoingPayoffPosition`/`IncomingPayoffPosition` + `CK_WeldEvent_PayoffDiff` so the payoff handover is queryable. `RodCheckin` documented as *consuming* the staged row. |
| July 26, 2026 | Added `RunReading` (sampled gauge profile, G3). `FlatWireRun.FootageFt` → `decimal(10,2)`; added audit + `RowVersion` on `FlatWireRun`. `RodCheckin`: computed `SpcOvalityIn`, added `MmsId`/`MmsStatus`/`ScrapBoxRef`. `SpoolCheckin`: added `MmsId`/`MmsStatus`. `RunPauseEvent`: added `OperatorId`/`ResumedBy`, computed `PauseDurationSeconds`, Notes-when-Other CHECK. `WeldEvent`: fail-reason-required CHECK. `RollOverride`: computed `Delta`, ReasonCode CHECK. `DieChangeEvent`: ReasonCode CHECK. Retargeted to `FlatWireDB`. |
| August 1, 2026 | **`RodStaging` — off-schedule columns dropped, blocked-row release added (client 30 Jul 2026).** `OffScheduleOverride`, `ScheduledLineId`, `CK_RodStaging_OffSched` and `CK_RodStaging_OffSchedLine` are **removed**: a rod booked on the other rod line now triggers an **automatic station switch**, not an override (**Q74**). `CK_RodStaging_Override` reverts to keying on `OutOfSequenceOverride` alone; the shared `OverrideBy`/`OverrideAt`/`OverrideReason` trio is retained for it. Added `UnstageKind` (`PreCheckOut` | `WipRejection`) and `WipRejectionId` so a **WIP rejection can release a blocked bay** — the Q72 item-3 residual, decided 30 Jul: the rejection captures the reason, the rod goes to `HOLD`, and the row leaves `Status='Staged'`. Chosen over a fourth `Rejected` status. `CK_RodStaging_RejectLink` uses `ISNULL(...)` after a rebuild showed the bare comparison admitted a rejection link on a still-staged row. Sample data gains a **blocked bay** (R00047, Staged + Fail). DDL rebuild clean, 15 constraint tests pass, 28 tables. |
| Aug 1, 2026 | **Weld quality now gates the `RodStaging` weld columns. No column changes.** `POST /weldevent` becomes the single weld write (`POST /staging/rod/mark-welded` retired) and sets `IsWelded`/`WeldedAt`/`WeldedBy` in the same transaction as the `WeldEvent` row — **on `WeldQuality = 'Pass'` only**. A failed weld writes the event and leaves the staging row un-welded, because the join did not hold and the line cannot transition through it. `CK_RodStaging_Welded` is **unaffected** — a failure sets none of the three, so the all-or-nothing group still holds. Quality is deliberately **not** mirrored onto `RodStaging`: one join, one quality answer, held on `WeldEvent`. Added a note to `WeldEvent` that a fail-then-remake writes **several rows for one physical join**, that no uniqueness constraint exists or should, and that `CoilTraceability`'s per-weld-boundary footage attribution has no rule for a superseded attempt — **Q24** (certificate) and **Q22** (footage). |

---

## [04-APIContract.md](MVP-1/ProjectPlan/Backend/APIs.md)

| Date | Change |
|---|---|
| August 2, 2026 | Added **`GET /spools[?spoolAlpha=]`** for Dashboard 5A (FL2 Spool Queue). One endpoint, two modes, identical `{ order, spools[] }` shape: without the parameter it lists everything available for processing with a null order; with it, **the backend resolves that spool's order** and returns the order plus that order's spools in one response. Before this, **`POST /checkin/spool` was the only spool endpoint in the contract** — there was no read at all, not even a single-spool lookup, so Dashboard 5's scan field validated against nothing. Four contract points are easy to get wrong and are called out inline: `404` is for an unknown alpha only and an **unallocated spool is a `200` with a null order**; `gaugeIn`/`widthIn` must come from the source FL1 run because `Spool.GaugeIn`/`WidthIn` are set at check-in and are null for every row returned; `sourceRodAlphas` is a list from `CoilTraceability`/`WeldEvent` because `Spool` holds only two single-valued rod FKs; and the `order` block is a cross-database read. **Also recommends an index on `Spool.OrderNo`**, which is unindexed today. The availability rule is `RECEIVED` + `STAGED` as a **proposal** — OQ-27 leaves it undefined. |
| July 31, 2026 | **Failed staging inspection now returns `201 Created` with `state: "Blocked"`, not `422`.** The old behaviour wrote no row at all, so a bundle that failed inspection — already physically on the payoff, since bundles are not unbanded until positioned there — left `GET /payoff/status` reporting the occupied bay as `NotStaged`, Dashboard 2A offering it as "Empty — available", and the next rod stageable into it. It also made the `Blocked` state unreachable despite being implemented across the API enum, the schema and the whole of Dashboard 2A. Staging now commits the row before the inspection gate. `CHK010` is unchanged — no bypass, WIP Rejection is still the only forward path. Closes **Q36** items 1–2; item 3 (*what releases a blocked row*) is the blocking residual, and two consequences are recorded but untraced (`TRV009` traveler class, `Available` projection exclusion). From the Dashboard 2A UX review finding F2 (`Analysis/Dashboard2A_UXReview.md`, deleted 1 Aug 2026 — in git history at `2a0426b`). |
| July 29, 2026 | Added the **Pre-Check-in / Payoff Staging** section: `GET /payoff/status`, `POST /staging/rod`, `POST /staging/rod/unstage`, `POST /staging/rod/mark-welded`, `GET /staging/queue`, plus the `PayoffStateChanged` hub event and `PayoffStagingController`. Extended `GET /rod/{alpha}` with `footageRunToDate`, `remainingWeightEstimateLb`, `stagedPayoffPosition`, `isWelded` — without them the `PRC007` carry-forward gate cannot be enforced. Documented check-in *consuming* the staged row. |
| April 30, 2026 | Original contract set. **Known correctness bugs catalogued in `REVIEW.md` Tier 1** — notably #37 (`RodCheckin` NOT NULL columns the check-in command never sends), the `/passschedule/generate` worked example, a missing `CheckpointType` value, and three edge-type vocabularies. Cross-check before implementing. |
| August 1, 2026 | **30 Jul client decisions applied.** **Wrong station is corrected, not authorised:** the `offSchedule` override object is **removed** from `POST /staging/rod` — the client resolves `scheduledLineId` from `GET /rod/{alpha}` and **switches station**, and a mismatched POST returns `409` with `correctLineId` for the client to switch and re-post (Q38). Out-of-sequence remains the one supervisor-authorised deviation. **`POST /staging/rod` no longer sets `coils.coil_status`** — `INFLAT` is set at check-in (Q68); the reqsum / `wip_coil_orders` half of that side effect is still open. **The blocked-row release is specified:** `POST /wipreject` releases the staging row (`Status → 'Unstaged'`, `UnstageKind = 'WipRejection'`, `WipRejectionId`) and broadcasts `PayoffStateChanged` — the Q36 item-3 residual, and the only thing that clears a `Blocked` bay. **`POST /staging/rod/unstage` gains a weld-dependent approval:** unwelded is operator-only, **welded requires a supervisor override and puts the rod on `HOLD`** because removal means cutting the material (Q69/Q72) — restoring, behind that gate, the control removed on 31 Jul. **`CHK007` becomes a min/max band** read from `AlloyProperty`, currently unseeded pending the values owed by e-mail (Q35). Flagged **G22**: the order-membership `409` is knowingly wrong for a **multi-order rod** (Q70) pending the Q73 sequencing answer, and **Q40** (order scheduled on neither rod line) has no station to switch to and no specified path. |
| August 1, 2026 | **New read endpoint `GET /run/{runId}/weldevents`** — every weld recorded against one run, backing the read-only **Welds this run** dialog on Dashboard 2A (`PCI021`). It replaces a mislabelled "Weld event log" link that navigated to Dashboard 4, which is the weld *capture form* and not a log of anything. Deliberately **not** served by widening `GET /run/active.weldEvents`: that array is a trimmed marker list feeding Dashboard 3's gauge trace, carries no quality/operator/weld type, and Dashboard 2A never calls it. **Read-only by design** — no PUT/PATCH/DELETE counterpart, because reversing a recorded weld in place is `WLD011` and is unspecified. Empty array (not `404`) for a run with no welds yet. Placed in **S3** with Dashboard 2A even though `POST /weldevent` writes its rows in **S4**, so the screen is complete at the phase-4 gate; the S3 stub must return non-empty sample data. Closes `WeldEventPopupPlan.md` **Q-W3** and **withdraws the proposed gap G25**. |
| August 1, 2026 | **Weld quality is captured at the weld, and `POST /staging/rod/mark-welded` is retired.** Dashboard 2A's Mark as welded dialog now captures Pass/Fail with a mandatory reason on Fail (`WLD013`, the same six reasons as Dashboard 4). Quality was the **only** NOT NULL `WeldEvent` column that dialog was missing — it already captured both rod alphas, weld type and footage — so the two screens now compose the same row and **`POST /weldevent` becomes the single weld write**, setting `RodStaging.IsWelded`/`WeldedAt`/`WeldedBy` in the same transaction. This is decision **D-A** in `WeldEventPopupPlan.md` and it closes **Q-W1**; it needs **no schema change**. **The `RodStaging` write is conditional:** a `Pass` sets the weld columns, a **`Fail` writes the `WeldEvent` row and leaves the rod staged and un-welded** — the join did not hold, so flagging it welded would assert a continuous feed that does not exist; the operator remakes the weld. Added the `422` for a missing fail reason and an `isWelded` field on the response so the client never infers bay state from quality. **Consequence — widens `Q13` rather than adding a question:** a fail-then-remake produces several `WeldEvent` rows for one physical join, which is the re-weld-on-certificate question `Q13` already asks; footage attribution across the two boundaries is `Q11`. |
| Aug 13, 2026 | **Demoted below `ProjectPlan/04-APIContract.md` and reconciled to the phase model.** Two API contract documents had coexisted since 4 Aug with no statement of which governed; **the 4 Aug document is now the contract of record** and this one is an elaboration that is not citable — retained because it is 2.6× longer and carries request/response detail, worked examples and project structure the owner does not, all of which should be **migrated into the owner rather than cited from here**. The `S1`–`S5` delivery model is replaced throughout by the roadmap's **14 phases**, sourced from `back-matter.md` Appendix A; the five sprint sections became phase-labelled groups. `/passschedule/**` and `/shiftsummary` are **marked MVP-2 in place**. Correctness fixes: **`RollAdjustTrigger` added to `CheckpointType`** (`POST /rolloverride` already wrote it); the six-member `LineStatus` enum renamed **`LineOperatingState`** and the hub event **`LineStateChanged`** per `[PLC §6]`, with a standing note **not to add a seventh member** since the absence of `Stopped` is deliberate and the mapping is configuration (`PLC-Q01`); `PushPassSchedule`'s first parameter is **`scheduleId`**; `ComponentState` is the three-value enum; `EdgeType` is `Round`/`Square` with display labels mapped at the UI. The hub event list was verified complete at nine events. **Three missing endpoint families — alloy-lookup CRUD, roll-override revert (`OI-32`) and the alert lifecycle (`OI-28`) — are recorded as gaps rather than drafted**, because inventing a shape here would create exactly the second unowned contract this document was demoted for. **`POST /passschedule/generate`'s worked example is deliberately left uncorrected** and is marked as such. |
---

## [MVP-1/ProjectPlan/CapacityAndEffortModel.md](MVP-1/ProjectPlan/Development/CapacityAndEffortModel.md)

| Date | Changed By | Description |
|------|-----------|-------------|
| Aug 13, 2026 | Clean-up | **Resolved-correction narrative removed to this change log, and the story-point cross-check re-based (7,728 to 6,895 words). No hours figure changed.** Four callouts and three parentheticals existed only to narrate the 11 Aug correction of a stale TOTAL row — *"the previous row was wrong twice over"*, *"the TOTAL row had said 3,727"*, *"the base itself was wrong until this pass"*, *"Corrected 11 Aug: W4 read 476 h"* — all of it already in this log. Also removed: the *"figures are NOT apportioned by MVP scope"* callout, which §3b had **made false** (it still claimed 222 / 246 / 209 h were carried whole and overstated MVP-1) and whose one live warning, that the descope ladder's rungs straddle the MVP boundary, is duplicated in §5. Two resolved items were restated as current fact (die inventory; Phase 5 at 154 h, leaving *"one figure that is still soft"*, not two). **The §3 cross-check was materially stale and is now re-based**: it compared *44 shopfloor stories at 184 points* against the retired 3,727 h for **20.3 h/point**. The backlog is **45 stories / 189 points** across three files and the total is 3,660 h, giving **19.4 h/point**, or **2,633 h / 161 points = 16.4** excluding Phase 1. §8's table-count bullet no longer claims the count is unresolved: the build is **25 MVP-1 tables of 28**, so 1C — costed against 22 — is **understated by roughly 17 h**. **The per-stream correction was moved into this log before removal, not discarded** — it was the only record that §1's row had been wrong twice over and that **QA had been understated by its entire contingency share (621 to 701)**. |
| Aug 13, 2026 | Plan team | **Added a *Related Documents* row for [`YieldCostAndScrapSheet.md`](MVP-1/ProjectPlan/Development/YieldCostAndScrapSheet.md) and re-based the dev-sheet row after Phase 12 moved out of it; no figure in this document changed.** The Phase 12 sheet decomposes this document's published **177 h** to the descope ladder's own 33/49/28/67 split, and **both axes reconcile exactly** to §3's discipline columns (44/72/12/26/23) and §5's rung hours — worth recording because it independently confirms those two published tables against each other for the first time. The dev-sheet row now reads **1,397 h / 4.0 developer-FTE excluding Phase 12** (2,114 h hand-coded), or 1,486 h against 2,242 h complete. |
| Aug 13, 2026 | Plan team | **Cross-referenced the new AI-assisted [`DevelopmentEffortModel.md`](MVP-1/ProjectPlan/Development/DevelopmentEffortModel.md); no figure in this document changed.** The team is using **claude.ai**, which invalidates the §2 rate card's hand-coded basis (24 h/screen, 6 h/command endpoint, 4 h/table) for the four build streams. The re-derivation was published as a **separate sheet rather than applied here**, deliberately: roughly twenty files quote this model's totals — the roadmap navigation table, `back-matter.md`'s week grid and gap **G1**, all sixteen phase files' `Effort:` lines, `05-SprintPlanAndBacklog.md` (a near-complete second copy), master spec §9.4/§9.5/§11.1, four READMEs and `ProjectPlanPrompt.md` — so editing the totals in place would have invalidated every one of them at once. **This document remains the base of record**, the new sheet factors its published columns, and QA, BA, contingency, the roster, the §4 capacity decision and the §5 ladder all stay owned here. Added one row to *Related Documents* and bumped `Last Updated` (which also keeps `REVIEW.md` #62 closed). **The AI basis is deliberately NOT propagated into the programme totals or the §7 staffing decision** — that is the larger call, and it is where the 30 Sep conclusion would actually change, since on an AI-assisted basis the MVP-1 programme falls to ~2,322 h and ~7 FTE lands 28 Sep. |
| Aug 11, 2026 | Plan team | **Added §3b — the MVP-1 apportionment, which the model had never carried.** Three scope decisions fixed the boundary: pass schedule **owned outside MVP-1** (Phase 2 leaves whole with its 231 h, permanently), **die inventory and lifecycle outside MVP-1** (the 8 h die table leaves Phase 13 with its screen), and **Phase 9 wholly MVP-1** (222 h intact — DB7/DB7b and `CoilCompletionService` returned). Only phases 11 and 13 needed splitting, and they were split by **re-pricing the carved deliverables from the §2 rate card** and **re-deriving** QA and contingency from the reduced base — not by proportional division, which the descope ladder cannot support. **MVP-1 = 3,292 h / 9.4 FTE.** The weekly grid was recomputed and sums to 3,292 with no leakage. **The per-stream row in §1 was recomputed in the same pass and had been wrong twice over:** it read FE 1,302 · BE 870 · QA 621 · RT 432 · DB 418 · BA 84 = 3,727, both because the total descended from the stale TOTAL row **and** because **QA 621 was the raw QA column with no contingency loaded**, contradicting the sentence directly above it. Corrected to FE 1,211 · BE 840 · QA **701** · RT 408 · DB 404 · BA 96 = 3,660, each carrying its pro-rata contingency at factor 1.146976. **QA moved most — 621 → 701** — having been understated by its entire contingency share, which matters because QA is the stream with the thinnest published roster. *(Recorded here 13 Aug 2026 when the in-document callout was removed; this is now its only record.)* **The base was corrected in the same pass:** §3's TOTAL row said **3,727 h** where its own phase rows sum to **3,660** — the 4 Aug DB13/DB14 descope had updated Phase 5 (221→154 h) and `back-matter.md`, but neither the TOTAL row nor §4's W4 column. W4 is now **409 h / 12.8 FTE**. **The headline finding is that the ~1 FTE recovered lands in W2–W3, already the slackest weeks at 5.3 FTE; W4 and W5 do not move at all and W7 is still 24.5 FTE against three days** — so §7's forced decision stands unchanged, and §5's ladder is worth less on MVP-1 scope because rung 5 bundled the now-MVP-2 Die Management screen. Two soft figures flagged: **Phase 5 is 221 h in §4 and 154 h in §3** (the DB13/DB14 descope reached one and not the other), and Phase 9's 16–32 h reserve is **understated** now that `FR-346`'s packing-station scale weight is in scope. §8's die-inventory risk closed; its table-count risk updated against a verified clean deploy (24 tables / 33 FKs / 1 proc / 1 trigger). |
| Jul 30, 2026 | Plan team | **Re-authored the whole model in hours** at the stated rate **1 dev-day = 8 h**, per request for the complete estimation in hours. Hours are now the **authored unit** and every component is a whole number of hours, so each row and column sums exactly as printed; day figures are derived (hours ÷ 8) and shown only as a reading aid. Grand total **3,727 h (465.9 d)**; Phase 1 **1,027 h**; Phases 2–14 **2,700 h**. Converted the rate card, per-phase table, weekly capacity, gap table, descope ladder, inverse table, calibration targets and §7 options; added per-person capacity columns (**352 h** over the window; 96 h in W0, 40 h/full week, 32 h in W4, 24 h in W7), a stream-share table in §1 (FE is **35%** of all hours), and a new **§4 hour-definition sensitivity** table — on a hands-on-keyboard reading at 6.5 productive h/day the requirement rises from **10.6 to 13.0 FTE**. Whole-hour rounding moved a few derived day figures by ≤0.2 d against the first publication (e.g. Phase 3 23.7→23.8, Phase 9 27.6→27.8; grand total 465.6→465.9 d); the hours are authoritative. **Every conclusion is unchanged** — 10.6 sustained FTE, 10.7 at the Phase-1 gate, 27.2 in W7, a 12% descope ladder, and the same finish dates by team size — which cross-validates both derivations. |
| Jul 30, 2026 | Plan team | **Initial publication — resolves G1 / OI-51.** Defined six delivery streams + an unfilled roster (§1); published a unit-rate card driven off each phase's existing deliverable inventory (§2); derived per-phase effort for all 17 phase specs (§3); built a working-day capacity model showing **10.6 sustained FTE**, a 10.7-FTE Phase-1 gate and an impossible 27.2-FTE W7 (§4); ordered a descope ladder and showed it recovers only **12%**, leaving 9.3 FTE (§5), after correcting an initial double-count in which rungs 1–3 (FW-110/102/101, all Phase-12 stories) were added on top of "Phase 12 entire" rather than being carved out of it; set the Aug-14 gate as the calibration point (§6); and stated the three options the arithmetic forces (§7). Corrected the working-day count to **32 post-gate / 44 total** by deducting **Labor Day (Mon 7 Sep 2026)** and the 3-day W7. Recorded that "thirteen workflow phases" (OI-51) and "14 phases" (G1) are both correct — 1 platform + 13 workflow phases. Documented two costs the inventory omitted (FW-001 rename audit; G2 recovery reserve) and the finding that **epic E01 contains no story for the Angular or .NET scaffold**, which is why the window was believed to fit. |

---

## MVP-1/DevelopmentPlan/CheckinImplementationPlan.md

*Deleted 13 Aug 2026 — heading unlinked because the file no longer exists; recoverable at `1964086`. The section is retained as the audit trail.*

| Date | Changed By | Description |
|------|-----------|-------------|
| Aug 13, 2026 | Clean-up | **Deleted — the record itself is now gone, and its three retained facts were rehomed first.** The entry below says deleting the file was rejected because 26 references cite it; that judgement was reversed the same day. What made the reversal safe is that **all three things cited *from* this document were moved to documents that own them**, rather than left to dangle: **(1) the stub-first delivery contract** → [`ShopfloorPlan/00-foundations.md`](MVP-1/ProjectPlan/Architecture/Architecture.md) **§0.5**, a new section stating the model in full plus the DI-swap mechanism, the canonical fixture set, the switchover criterion and the de-stub debt — `ProjectPlanPrompt.md` cited this file as *the* model and now cites §0.5; **(2) the `0.003″` ovality datum** → restated at each of its four citation sites as **per-alloy reference data, not a constant**, with `AlloyProperty.RodOvalityMaxIn` named as its only home (master spec schema table and `OI-07`, `FlatWireSchema_Lookup.md`, the `01_Lookup` DDL comment, `phase-13`, `Analysis/FlatWireOpenQuestions.md` and `03-HLD-and-ERDiagram.md`); **(3) `G14`** → [`back-matter.md`](MVP-1/ProjectPlan/Development/GapsRegister.md), whose register row now carries the *"the DDL builds four inspection columns NOT NULL plus `SpcM1In`/`SpcM2In`, `POST /checkin/rod` supplies none, so **every check-in insert fails as specified**"* statement, and `REVIEW.md` #6, rewritten from *"inside one doc"* to the live DDL-vs-contract form. **`G14` is still open and still blocks Phase 4** — deleting the document that exposed it does not resolve it, which is exactly what the retained record existed to say. `REVIEW.md` gained a **Tier 5 banner** recording that the six findings' subject documents are gone and that *"banner-ed in both"* closure notes now resolve to the live document named beside them. Full text recoverable at `1964086`. |
| Aug 13, 2026 | Clean-up | **Reduced to a record — the April implementation body was removed (3,435 to 816 words).** Parts 1–6 (Angular scaffold, .NET stub, dummy fixtures, two component specs, API implementation detail) were April-dated instructions for a build now specified by `phase-04`, `phase-08`, `04-APIContract.md` and the requirement documents, and they carried the six `REVIEW.md` Tier 5 defects (#39–#44) that would send a developer to build the wrong thing. **Deleting the file was rejected: 26 references across 13 files cite it**, and three things are cited *from* it rather than about it — so all three are now stated explicitly rather than left inside a deleted body. **(1) The `0.003` inch ovality datum**: the master spec's schema table, `FlatWireSchema_Lookup.md`, `phase-13` and `Analysis/FlatWireOpenQuestions.md` each say `RodOvalityMaxIn` *supersedes the hard-coded value in this document*, so it is recorded here with all four citations. **(2) The stub-first delivery model**, cited by `LatestDocument/ProjectPlanPrompt.md` as *the* model, is stated in a paragraph. **(3) `G14` is still open and still blocks the Phase-4 build** — Part 4 described four inspection items while its own DTO, stub and checklist described three, the DDL builds four NOT NULL plus `SpcM1In`/`SpcM2In`, and `RodStaging` deliberately uses three, so **as specified every check-in insert fails**; deleting the body that exposed it would not have resolved it. Full text recoverable at `1964086`. |
| Aug 13, 2026 | Reconcile-up | **Status corrected from `Ready for implementation`, which it was not.** The document carried all six `REVIEW.md` Tier 5 defects (#39–#44) while telling a developer it was ready — most seriously **#41**, an instruction to *"copy patterns from `checkin-precheckin`"*, a library `00-foundations.md` §0.2 **explicitly forbids as a reference**. Status is now *Draft — superseded in six known respects*, with a banner tabulating each defect against the correct position: `--color-*` not `--fw-*` (**G18**), the 6-step tab wizard not the retired grid/progress-ring layout, `UAController` + `[Authorize]` not bare `ControllerBase`, and the DB seed as the canonical fixture set. Header re-pointed from *Sprint S3* to **Phases 4 and 8**, and from the non-existent `FW-S3-009`/`FW-S3-012` to the real `FW-061`/`FW-082`/`FW-064`. **A seventh, internal defect is called out (`G14`)**: Part 4 describes four inspection items while its own DTO, stub and acceptance checklist describe three — and the as-built DDL builds four NOT NULL plus `SpcM1In`/`SpcM2In`, so **every check-in insert fails as specified**. The body is unrevised and still April 30, 2026. |

---

## MVP-1/DevelopmentPlan/CheckinImplementationPrompt.md

*Deleted 13 Aug 2026 — heading unlinked because the file no longer exists; recoverable at `1964086`. The section is retained as the audit trail.*

| Date | Changed By | Description |
|------|-----------|-------------|
| Aug 13, 2026 | Clean-up | **Deleted alongside its companion.** The entry below says deleting was rejected because 15 references cite this file **as the evidence base**; that judgement was reversed the same day, and the two findings it was the evidence for are now stated by the documents that own the rules rather than by the document that broke them. **`G18`** — `phase-01a` §Review-fixes and the master spec / `02-SRS.md` token callouts now say plainly that **no `--fw-*` token exists, there is no migration to perform, and if the prefix resurfaces from an older commit it is wrong**, which is the operative instruction; the count of occurrences in a deleted April file was never the instruction. **The `[Authorize]` finding** — `04-APIContract.md` §Authorization and master spec §7 now each state that they are the source of record for `UAController` + `[Authorize]`; `phase-01b` keeps the finding in past tense. Full text recoverable at `1964086`. |
| Aug 13, 2026 | Clean-up | **Reduced to a record — the April prompt body was removed (4,292 to 484 words).** This document was written to be **handed to an agent or developer**, and its body was April-dated, unrevised and carrying five Tier 5 defects: 87 occurrences of the retired `--fw-*` tokens against the mockups' `--color-*` (**`G18`**), the retired grid + progress-ring layout rather than the approved 6-step wizard, an instruction to *copy patterns from `checkin-precheckin`* which `00-foundations.md` §0.2 **explicitly forbids as a reference**, three occurrences of bare `ControllerBase` with no `[Authorize]`, and stub fixtures disagreeing with the DB seed. A prompt that produces the wrong build is worse than no prompt. **Deleting the file was rejected: 15 references across 11 files cite it as the evidence base** for `G18` (`back-matter.md`, `phase-01a`) and for the `[Authorize]` finding (`04-APIContract.md`, `phase-01b`, the master spec twice), so the defect table is retained and the citations still resolve. Full text recoverable at `1964086`. |
| Aug 13, 2026 | Gap remediation | **Header block added — the file had none at all.** Every planning document in the repository carries `Project` / `Last Updated` / `Status`; this one carried nothing, which is why a document written explicitly to be *handed to an agent or developer* opened with no indication that it is April-dated and superseded. It now states its status as **Draft, superseded in five known respects**, and carries a banner naming all five with their register ids — the retired `--fw-*` tokens against the mockups' `--color-*` (**G18**), the retired grid + progress-ring layout against the approved 6-step wizard, the instruction to copy `checkin-precheckin` which `00-foundations.md` §0.2 **forbids as a reference**, bare `ControllerBase` without `[Authorize]` against the `UAController` standard, and stub fixtures disagreeing with the DB seed (`REVIEW.md` Tier 5, #39–#44). Its *Sprint S3* framing is marked superseded by the 14-phase model. **The body is unrevised and still April 30, 2026**, which the `Last Updated` line says explicitly rather than implying a content revision that did not happen. |

---

## [MVP-1/ProjectPlan/DevelopmentEffortModel.md](MVP-1/ProjectPlan/Development/DevelopmentEffortModel.md)

| Date | Changed By | Description |
|------|-----------|-------------|
| Aug 13, 2026 | Plan team | **Phase 12 removed to its own sheet; every total re-derived.** Phase 12 (Yield, Cost Ledger & Scrap) is now owned entirely by [`YieldCostAndScrapSheet.md`](MVP-1/ProjectPlan/Development/YieldCostAndScrapSheet.md), which prices it at **story level** against the descope ladder's four rungs — something a single per-phase row here cannot do, and which matters because three of its four stories carry **no requirement specification**. Removing its 89 h AI / 128 h hand-coded cascades through every total: **§2** columns FE 652→**625** · BE 458→**404** · DB 228→**220** · RT 305 (unchanged — Phase 12 has no RT), total 1,643→**1,554** h against 2,368 hand-coded, saved **814 h / 34.4%**; **§3** MVP-1 1,486→**1,397 h** (174.6 d, **4.0** developer-FTE) against 2,242→**2,114 h** (**6.0** FTE), saved **717 h / 33.9%**; **§4** re-based on both sides (FE 1,012→625 · BE 660→404 · DB 340→220 · RT 356→305); **§5** W7 221→**132 h / 5.5 FTE**, the only week affected. **The boundary is stated wherever a total appears, because the sheet no longer covers all of MVP-1 development:** `1,397 + 89 = 1,486 h` is the complete figure, the §3 roll-up prints both lines, and §5 carries an explicit warning that **W7 is understated by 89 h by design** since Phase 12 sits wholly in it. Two second-order effects recorded rather than smoothed over: excluding a **BE-heavy, RT-free** phase *raises* RT's share (15.0% → **19.6%**) and lowers BE's, and BE's saving reads 38.8% here against 37.4% complete because Phase 12's **0.75** is the least favourable BE factor in the plan; and with Phase 12 out, the next-least-compressible phases after 14 are **10 (29.5%)** and **3 (29.8%)**. §1's factor card is unchanged and still carries the 0.75 BE band, which the Phase 12 sheet cites. |
| Aug 13, 2026 | Plan team | **Two FTE cells corrected to the repository's half-up rounding, and the Phase 12 sheet cross-referenced.** §5's W5 and W6 read **6.5** and **5.8**, which are round-half-even values; `CapacityAndEffortModel.md` demonstrably rounds **half-up** (it publishes 210 ÷ 40 = 5.25 as **5.3** and 562 ÷ 40 = 14.05 as **14.1**), and every derived *day* figure in this sheet already followed half-up. W5 is **6.6** (262 ÷ 40 = 6.55) and W6 is **5.9** (234 ÷ 40 = 5.85). No hour figure changed and no conclusion moves. Added a *Related Documents* row for the new [`YieldCostAndScrapSheet.md`](MVP-1/ProjectPlan/Development/YieldCostAndScrapSheet.md), which decomposes this sheet's Phase 12 development aggregate (89 h) to story level. |
| Aug 13, 2026 | Plan team | **Initial publication — development effort re-derived on an AI-assisted (claude.ai) basis.** Not a new assumption: `BaseDocuments/ClientCall_2026-07-23_SyncPlan.md` records *"AI-assisted development to be leveraged wherever practical"* as a client decision filed **recorded, not actioned**, and its propagation **wave W8 named `CapacityAndEffortModel.md` as an unexecuted target** — this sheet executes the effort-model half of W8, and the ledger row is annotated accordingly. Nothing else in the repository carried an AI productivity assumption; `TechStackRecommendation.md` has none. **Scope is the four build streams only — FE, BE, DB, RT.** QA, BA, contingency, the roster, the capacity decision and the descope ladder stay with the capacity model, and §0 says why three of them cannot simply be scaled down: **QA is a proxy for feature volume, not for dev hours** (building the same screens faster does not shrink the test surface), **BA does not compress at all**, and **contingency arguably should rise** because AI-assisted throughput is unmeasured for this team. Method is a **retention-factor card** (§1) applied to the capacity model's *published* discipline columns, so every figure is reproducible by one multiplication and that document stays the base of record. Factors run 0.60–0.62 for screens/endpoints/schema (approved mockups are a complete visual spec; `CoilCheckin` is an exact backend template) to **0.90 for PLC tag push and 1.00 for commissioning**, which needs the controller and the mill. **MVP-1 development = 1,486 h / 185.8 d / 4.2 developer-FTE** against 2,242 h hand-coded (−756 h, −33.7%); both scopes **1,643 h** against 2,496 (−34.2%). **The headline finding is where the saving is not:** RT compresses **14.3%** against FE's 38.3% and is the only stream to *gain* share (14.3% → 18.6%), and **Phase 14 saves 12.5%**, the least of any phase — so **W7 still needs 9.2 developers across three days** before Phase 14's QA and UAT, leaving the capacity model's "UAT cannot share W7" finding intact. Row, column, roll-up and weekly-grid integrity all verified; `1,486 h` is flagged throughout as **not** a programme total. Notes that Phase 5's base is §3's post-descope 120 h and that the roadmap navigation table and master spec §9.5 still carry the pre-descope 221 h. |

---

## [05-SprintPlanAndBacklog.md](MVP-1/ProjectPlan/Development/SprintPlan.md)

| Date | Changed By | Description |
|------|-----------|-------------|
| Aug 13, 2026 | Plan team | **Rewritten whole as the sprint-wise MVP-1 backlog — 35 stories / 147 points to 116 stories / 3,292 h.** Four decisions, each taken deliberately and each with a cost. **(1) Re-derived from the phase specs rather than reorganised.** Every one of the 15 MVP-1 `ShopfloorPlan/` files was walked and its deliverable inventory — `Screens:`, `Components:`, `APIs:`, `## Database Changes`, `## Real-Time Functionality` — cut into stories. **The increase is almost entirely Phase 1**, which the capacity model had flagged as the plan's largest defect: *"there is no story anywhere in the backlog for scaffolding the Angular library, for creating the `FlatWire` .NET solution and its 13 controllers, or for the OPC ingest and `PLCTagService` — Phase 1 is 1,027 h against ~28 points."* **S0 now carries 33 stories**, including `FW-N03`, `FW-N04` and `FW-N05`, the three the model named. **(2) Sized in hours, not points.** Every story carries a **`Rate-card basis:`** line factored from `CapacityAndEffortModel.md` §2, and **every phase closes on its §3b figure exactly** — QA at 20% of the FE/BE/DB/RT base and contingency at 15% of (base+BA+QA) are applied **at phase level, never per story**, because pricing them per story double-counts. All seven stream columns sum without leakage to **3,292 h**. Two phases are deliberate exceptions and are annotated: **Phase 5**, whose QA/contingency do not follow the uplift because the −67 h DB13/DB14 descope was applied to the published total rather than re-derived; and **Phase 14**, which carries no QA uplift because it *is* the QA phase. **The cost of retiring points is real** — ~20 downstream figures lose their source, so *Appendix A — Retired point basis* records the 147 / 189 / 31 / 11 totals, the per-epic split of the 147, the retired epic ids, and the two h/point ratios that lose their denominator. **(3) Even two-week sprints, `S1` starting 24 Aug**, derived by pairing `CapacityAndEffortModel.md` §3b's MVP-1 weekly grid: `S1 = W2+W3` · `S2 = W4+W5` · `S3 = W6+W7`. **The 24 Aug start is what makes the carry-over week visible** — 17–21 Aug falls outside every sprint, which is what §4 already calls it (*"the only slack in the entire plan… the whole recovery budget"*); a 17 Aug start would have absorbed it and reported S1 at 1.2 FTE. It also lands **Phase 3 whole in S1**. The cost is **S3: seven phases in 8 working days at 17.3 FTE**, including the sequential `8→9→10` chain and all of Phase 14. **S3's truncation at 30 Sep is recorded as a choice** — its natural boundary is 2 Oct, and extending it drops the peak to **13.8 FTE**, which is cheaper relief than rungs 1–4 of the descope ladder combined. **(4) Promoted to the single MVP-1 backlog**, superseding `05-SprintPlanAndBacklog.md` §7/§7.2/§7.3; **§11's `FR-###` coverage matrix stays there** and still resolves, because story ids are frozen. **Ids: all 39 previously-published `FW-###` keep their numbers and titles** (~1,010 citations across 93 files, including mockup HTML/JS and four SQL seeds), the `FW-N##` namespace was absorbed, and new work is minted contiguously at **`FW-130`–`FW-201`**. *Appendix B* gives full provenance in five classes. **Three findings surfaced by the derivation and recorded rather than silently fixed:** `phase-13`'s MVP-2 callout claims *"the `FW-N07` table half is also MVP-1"*, which **contradicts its own scope call**, `CapacityAndEffortModel.md` §3b and `CLAUDE.md` — the die inventory table's 8 h left with the screen, so `FW-N07` is wholly MVP-2; **six `FW-N##` stories have no hours anywhere in the capacity model** (`FW-N07`–`FW-N12`) and are listed as **adopted but uncosted** so they are not mistaken for oversights nor assumed to be inside the 3,292 h; and `FW-014` is **subsumed** by `FW-169` for its MVP-1 half, its `PassScheduleChangeLog` sink being MVP-2 — so MVP-1 has an override path with nowhere to log the schedule-level record. Verified: **116 story bodies, no duplicate ids, every previously-published id and all twelve `FW-N##` resolve, every dashboard in `back-matter.md` Appendix B covered, and every relative link opens.** |
| Aug 13, 2026 | Clean-up | **Epic 6 removed and `FW-054` re-homed to Epic 1.** After the upstream deletion earlier the same day, **Epic 6 (".NET Web Application Changes") held exactly one story** — `FW-054` (Alloys — Material Type), MVP-1 Phase 13 reference data — its five siblings having been another team's work. An epic named *"Web App Changes"* containing one alloy story was misleading, and the *"Planning, Scheduling, Web app changes"* label was still surfacing in the sprint crosswalk as though that work lived here. `FW-054` now sits in **E01 — Foundation & Infrastructure, beside `FW-004` (alloy lookup table)**, which is where it belongs; **Epic 6 is deleted**. E01 becomes **8 stories / 33 points** and carries a note that it spans two phases — Phase 1 except `FW-054`, which is Phase 13. **The document total is unchanged at 35 stories / 147 points**, verified against both the story bodies and the Backlog Summary. Consequential fixes: the Sprint 3 crosswalk row now reads *"Nothing in this document"*, matching Sprint 2's shape; the epic-numbering note reads **(E01, E02, E07–E12)** rather than E06–E12; the header records **six epics**; and the effort basis' Phase 13 row attributes to **E01** rather than E06. The surviving mentions of *"Web app changes"* are deliberate — one is the retired Sprint 3's *theme as written* in the crosswalk, which exists so inbound `S3` / `FW-S3-###` citations resolve, and the others record where `FW-054` came from. |
| Aug 13, 2026 | Plan team | **Reduced to shopfloor MVP-1 only — 55 stories / 220 points to 35 / 147.** The document held **three scopes in one point total**, which had already produced wrong figures downstream. **The 14 upstream stories (42 pts) were DELETED** — rod receiving, orders, planning and line scheduling are built by the existing CoilReceiving / Planning / Scheduling teams, so another team's backlog does not belong here, and keeping it had actively misled: those stories still carried `Critical`/`High` priorities and Sprint 2–3 tags. **Epics 3, 4 and 5 went with them, headings included** — they emptied completely, and an empty stub for another team's work would defeat the purpose. **Epic numbering keeps its gaps** (E01, E02, E06–E12); nothing was renumbered, because every citation elsewhere is by story id. **The 6 MVP-2 stories (31 pts) were MOVED** to [`MVP-2/DevelopmentPlan/FlatWireJiraStories-MVP2.md`](MVP-2/DevelopmentPlan/FlatWireJiraStories-MVP2.md) — they are *this* team's work, deferred, and `MVP-2/` is the established home for it. **Deletion was verified safe before it was done:** the bodies are byte-identical to commit **`1964086`**, `05-SprintPlanAndBacklog.md` §7 lists all fourteen by epic with descriptions, and the ids are referenced from `back-matter.md` Appendix C, `phase-04`, the master spec, `01-VisionAndScope.md`, `REVIEW.md` and both API contracts — nothing is orphaned. **The 'never delete' convention this document used to cite does not apply to it:** that convention protects the `FR-###` coverage matrix in `05-SprintPlanAndBacklog.md` **§11**, a different file; this one is the points basis and has no coverage matrix. **Two dependencies survive as explicit notes because they cross the boundary** — `FW-061` depends on the deleted **`FW-020`** for R-series rod alphas (no rod alphas, no rod check-in — the plan's *'Phase 4 has no material'* risk), and `FW-061`/`FW-082`, both `Critical` MVP-1, depend on the moved **`FW-010`**; both nodes are kept in the Critical Path diagram in brackets. **Epics 2 and 6 survive as one-story epics** — `FW-014` spans both scopes and `FW-054` is MVP-1 Phase 13 reference data, which leaves a one-story epic named *'Web App Changes'* as a re-homing candidate. **The effort basis was re-based to MVP-1**: 12 phase rows totalling **2,114 → 1,397 h** (Phase 2's row gone because `FW-010`–`FW-013` left with it; phases 11 and 13 switched to their MVP-1 carves), against 147 points — **14.4 → 9.5 h/point**, or **11.5 → 7.7** excluding Phase 1. Upstream never had an hours row at all, so deleting it removed **42 points and zero hours**, which is why the ratios moved. **Five blocking-question rows were pruned** (`OI-88`, `OQ-2`, `OQ-67`, `OI-49`, `OI-60`) because their only blocked stories left — with a note that this is **not** resolution, and that `OI-60` is the sole tracking home for its subject. **Two scope leaks raised, not fixed:** **`FW-006`** creates the MVP-2-owned `PassSchedule*` tables, making it a **third** both-scopes story alongside `FW-014` and `FW-N07` and absent from every scope table; and **`FW-004`'s title** (*'Alloy lookup table for pass schedule algorithm'*) reads MVP-2 when the lookup is MVP-1 reference data and only its consumer `FW-013` is MVP-2 — flagged so a future scope pass does not sweep it out on the title. `147 + 11 + 31 = 189` shopfloor across three files; **220 and 231 retire as figures.** |
| Aug 13, 2026 | Plan team | **Phase 12's two epics split out, and an effort basis added from the AI-assisted development model.** **Epics 10 and 11 — `FW-100`, `FW-101`, `FW-102`, `FW-110`; 4 stories / 11 points — moved to [`YieldCostAndScrapJiraStories.md`](MVP-1/ProjectPlan/Development/YieldCostAndScrapStories.md)**, mirroring the same day's effort split. **Both epic headings are retained as pointers rather than deleted**, so inbound references from `back-matter.md` Appendix C, the `FR-###` coverage matrix and `ProjectPlan/05-SprintPlanAndBacklog.md` still land — the same reasoning that keeps the MVP-2 rows marked in place. Backlog Summary now reads **55 stories / 220 points** for this document with a second total line at **59 / 231** across both files; the moved epic rows stay in the table, italicised and marked. Shopfloor sizing is **41 / 178 here plus 4 / 11 there**, still 45 / 189 in total. **A new §Effort basis closes the gap this document had by construction — it sized 59 stories and carried no hours**, which is the residual half of `REVIEW.md` #33. It publishes development hours per phase on both bases (hand-coded **2,368 h** → AI-assisted **1,554 h**, excluding Phase 12's 128 → 89) and the points cross-check: **13.3 h/point** hand-coded falling to **8.7** AI-assisted, or **10.8 → 7.1** excluding Phase 1. **Phase 1 has to be excluded, and the reason is a backlog gap this document owns:** E01 is 7 stories / 28 points and all seven are database stories — there is no story anywhere in the 55 for the Angular library scaffold, the `FlatWire` .NET solution, or the OPC ingest and `PLCTagService`, yet Phase 1 is 483 development hours. Two guards are stated rather than left implicit: **per-epic hours are not derivable and must not be invented** (E07 and E08 both feed phases 3–5, and Phase 13 has no epic beyond `FW-054`), and these ratios are **development-only and not comparable** to `CapacityAndEffortModel.md` §3's ~20 h/point, which is all-in — a point nominally covers a full vertical slice including test, so a dev-only ratio understates it. `OQ-10` and `OI-60` rows in the blocking table now mark which of their stories moved. |
| Aug 13, 2026 | Reconcile-up | **Re-baselined to the 14-phase model; every summary figure recounted and wrong.** Timeline moved from the dead *"~10 weeks May–June 2026 / Trial Jul 1 / Production Aug 1"* to **17 Aug – 30 Sep 2026** with the **14 Aug Phase-1 gate** and Q4 2026 production; the priority legend, which keyed `High` and `Medium` to the withdrawn Jul 1 / Aug 1 dates, was re-keyed to the 30 Sep milestone. A **sprint→phase crosswalk** was added so the `S1`–`S5` tags here and the `FW-S3-###` ids cited by the two check-in documents resolve to something — they resolved to nothing in the roadmap. **The crosswalk's most consequential line:** the real-time backbone (`FW-080`–`FW-082`) sat in Sprint 5, *after* the Sprint 4 dashboards that depend on it; the roadmap puts it in **Phase 3**, before them. Scope legend added marking the seven MVP-2 stories, the one spanning both scopes, and the 14 upstream stories — **rows marked in place, never deleted**, per the convention `05-SprintPlanAndBacklog.md` §11 uses. `FW-010`'s acceptance criterion corrected from `IsActive (bool)`, which cannot express **Bypass** vs **Skip** and contradicted `FW-012` in the same epic, and from `Round/Flat` to the `Round`/`Square` domain vocabulary. **Backlog summary recounted from the story rows: 59 stories / 231 points, not 58 / 224** — and `REVIEW.md` #51's proposed correction (67 / 226) was itself overtaken. One story explains all of it: **`FW-124`** (Dashboard 5A, 5 pts, `High`, phase 8) was added to Epic 7 and reached no summary anywhere. |

---

## [FlatWireSchema_Mapping.md](MVP-1/ProjectPlan/Database/Schema/FlatWireSchema_Mapping.md)

| Date | Changed By | Description |
|------|-----------|-------------|
| Aug 13, 2026 | Reconcile-up | **Reconciled to the as-built schema and demoted below the DDL.** A banner states that [`DBChanges/Schema/SQL/`](MVP-1/ProjectPlan/Database/Schema/SQL/) is the schema of record and that this document is the April *analysis*, retained for its reasoning and not for its column types — with the **bare `decimal`** hazard called out as the largest sync risk in the folder: bare `decimal` means `decimal(18,0)`, so regenerating DDL from this document would **round every weight, gauge and measurement to a whole number**. Counts corrected from *"Total tables needed: 22"* to the verified **25 MVP-1 / 28 full** from the clean rebuild of 11 Aug 2026, with a table of what the design gained since April — `PayoffPosition` (**G20**), `RodStaging` (**G19**), `RunReading` (**G3**), the `FlatWireRun` hub of the header/detail split, `Dancer` (**G35**), `AlloyProperty`, and the three MVP-2 `PassSchedule*` tables. **`Rod` is explicitly retained** per `D-04` — `REVIEW.md`'s worklist row telling the reader to drop it is superseded and struck, `G12` having closed on exactly that point. `SpoolCheckin`'s missing owner recorded (created Phase 1C, populated Phase 8). **`RodCheckin`'s NOT NULL conflict verified against the DDL and found narrower than `REVIEW.md` Tier 1 #5 describes**: `InspectionConnectorTag`, `SpcM1In` and `SpcM2In` are genuinely required and unsent, but `SpcOvalityIn` is a **PERSISTED computed column** and can never be supplied — it must not be added to the contract. `united_db` was already absent. |

---

## [MVP-1/ProjectPlan/PLCTagImplementation.md](MVP-1/ProjectPlan/Architecture/PLCCommunication.md)

| Date | Changed By | Description |
|------|-----------|-------------|
| Aug 13, 2026 | Reconcile-up | Section created so the document has a change-log home; **no content change**. Recorded here because `REVIEW.md` #49 flagged the missing sections for the four April documents and this one was missing too. It is also one of the documents deliberately carrying **no MVP-2 marking** — the PLC tag surface is scope-agnostic, and the anti-drift rule (the client-facing `PLCTagSpecification.md` owns every tag path string and this document contains none) is unaffected by the MVP split. |

---

## [MVP-1/ProjectPlan/Development/StaffedSprintPlans.md](MVP-1/ProjectPlan/Development/StaffedSprintPlans.md)

| Date | Changed By | Description |
|------|-----------|-------------|
| Aug 13, 2026 | Plan team | **Re-baselined at three developers, with yield/cost/scrap out of plan — and a sequencing defect corrected in the same pass.** The 2- and 4-developer scenarios were withdrawn (recoverable at `1b8814c`); **Phase 12** — coil yield, cost ledger and scrap — is excluded by direction, taking the plan to **103 stories / 1,396 h** from 107 / 1,485 h.<br><br>**The date does not move: 7 sprints, finish Wed 25 Nov 2026, unchanged.** All 89 h came out of `S6`, which was never the binding sprint — the finish is set by `S7`, which exists because **Phase 14 must have a sprint to itself**. What the removal buys is the plan's only real absorption: `S6` falls from **98 % to 60 %**, 95 free hours immediately before commissioning, with every earlier sprint still at 96–102 %. A new §3 states this outright, because the intuition is the opposite.<br><br>**⚠ The allocator had no barrier on Phase 14, and the published plans were right by accident.** `[SP §6.1]` lists `14` last under *Must be sequential* and phase-14's scope call says *"pull this into a dedicated post-feature-complete window regardless of team size"* — the greedy pass enforced neither. The 3-developer plan isolated Phase 14 only because nothing else fitted, and **the withdrawn 4-developer plan had Phase 14 sharing `S5` with Phases 11, 12 and 13, which the rule forbids**. With 89 h removed the greedy pass would have reported a **6-sprint plan finishing 13 Nov** — twelve days early and not deliverable. The barrier is now explicit and the allocation was regenerated under it.<br><br>**One published figure was wrong in consequence and is corrected:** the *add a 4th developer* lever claimed **30 Oct**, which was the withdrawn plan's barrier-violating date. Under the barrier four developers finish **13 Nov** and five finish **30 Oct**, so **no team size on this start date reaches 30 September** — because the last two sprints are dependency-bound, not effort-bound (at four developers `S5` is 31 % and `S6` 22 %). §1 now says so rather than resting on the 4.0-FTE arithmetic floor.<br><br>**Two notes kept deliberately.** The per-stream split (FE 545 · BE 354 · DB 202 · RT 305) sums to **1,406 h against the allocation's 1,396 h**: `[DE]` publishes a per-phase retention factor *and* a per-stream compression rate, and once Phase 12 is removed they disagree by 10 h because its stream mix compresses differently from its phase factor — the per-phase basis is the one every sprint figure reconciles to. And **`OQ-10` (footage→weight) still matters with yield out of plan**: it is a Phase 9 coil-completion question, printed on every customer label, not a Phase 12 one. |
| Aug 13, 2026 | Plan team | **Created** as sprint plans at 2, 3 and 4 developers, with all 107 development stories allocated — two-week sprints from 24 Aug 2026 on the AI-assisted basis. Superseded the same day by the three-developer re-baseline above. |

---

## [MVP-1/ProjectPlan/Development/TrialRunPlan.md](MVP-1/ProjectPlan/Development/TrialRunPlan.md)

*The cross-document pass that produced this document is recorded once under [Repository-wide](#repository-wide), per this file's rule on cross-document changes. This section carries only what is about the document itself.*

| Date | Changed By | Description |
|------|-----------|-------------|
| Aug 14, 2026 | Plan team | **§1.3 gains *What the four streams are*.** The document leaned heavily on the stream codes — three of §1.3's four findings and the whole of §2.1's staffing shape are stated in them — while glossing them only as *"RT real-time / PLC"* in a single table cell. FE/BE/DB/RT are now defined against `[CE §1]`'s six streams, with QA and BA named as the two not costed in this plan. **`RT` is decomposed to all 127 h** (1A 27 · 1B 61 · Phase 4 11 · Phase 6 7 · Phase 7 5 · `FW-202` 13 · Phase 8 3) and the reason it is a stream at all is stated: it **spans FE and BE**, and splitting a typed hub contract from its client across two owners is how the two drift. The two properties that drive decisions elsewhere are pulled together — **it does not compress** (`[DE §1]` puts OPC ingest and PLC tag push at retention **0.90**, Phase 14 commissioning at **1.00**, against FE's 0.62, which is why `[DE §4]` finds RT's *share* rises under AI assistance while every other stream's falls) and in this trial **it does not spread** (39 of 127 h outside Phase 1). ⚠ Also records the trap `CLAUDE.md` warns about: **`[RT]` and `[DB]` are delivery-stream codes, not document shortcodes** — the real-time specification is `[SIG]` and the database design `[DBD]` for exactly this reason, so `RT` in any table here means hours in a stream and never a file reference. **No total changed.** |
| Aug 14, 2026 | Plan team | **§1.4 completed — the 1B sprint split and Phase 1 all-in.** Two things the deliverable tables did not answer. **(1) 1B is the only sub-phase that crosses a sprint boundary** — 164 h in T1, **28 h in T2** (`FW-150` broadcast loop · `FW-151` `PLCTagService` · the OPC simulator), which are last because they are the only 1B work with nothing downstream waiting on them inside T1. **1A and 1C must finish inside T1** or the whole T2 chain slips: 1A gates every screen, 1C gates every write. The T1 column sums to **395 h**, §2.1's T1 figure — the two reconcile by construction. **(2) Phase 1 all-in is 584 h** (dev 423 · QA 84 · cont. 77) on `[CE §2]`'s uplifts, because every other figure in this plan is **development only** while the hand-coded 1,027 h is all-in — so the two were **not comparable as printed**. The like-for-like saving is **584 against 1,027 = 43 %**, not the 59 % the dev-only figures imply, and the guidance is explicit: **quote 423 h against a roster and 584 h against a budget, never the other way round.** Rounding follows `[CE §3]` — **half-up per cell, total row = the sum of the printed cells** — which puts QA at 84 and contingency at 77 where deriving from 423 directly would give 85/76; same 584, and the cell-sum convention wins so every row and column verifies by hand. |
| Aug 14, 2026 | Plan team | **`FW-203` and `FW-204` minted — the trial plan had two work items with no story identifier.** `[TRP §4]` held **59 numbered stories plus an unnumbered plant-data feed simulator (6 h) and minimal landing route (5 h)**. Both are real deliverables created by trial-scope decisions — the simulator stands in for `FW-N05`, which `[DE §1]` prices at retention **0.90** and calls *"not verifiable without the hardware"*; the landing route exists because removing the line status board took away the target of **every** mockup's back button. **Story identifiers are this repository's join key**, so two blank ones could not be imported, cited or tracked. Both now have full story bodies in `[TB §7]`, **hand-coded at 8 h each** so the published figure factors to the 6 h and 5 h `[TRP §4]` schedules, and `FW-202`'s allocation row was reshaped to `` `FW-202` 67 `` so one expression reads all 61 items rather than needing a special case.<br><br>**Three properties are recorded on each, because all three will look like defects later.** They are **additive to `[CE §3b]`** and folded into no published phase total — `phase-01b`'s and `phase-03`'s reconciliation lines each gained a note saying so. **`FW-203` does not offset `FW-N05`'s 32 h**, which still has to be built for production. And **`FW-204` is temporary**: it retires when `FW-060` ships, so it never enters the MVP-1 baseline at all.<br><br>⚠ **All three trial stories are deliberately excluded from `[SSP §5]`, and the existing workbook build is what proved it should be that way.** Adding content entries for them to `DevelopmentPlanContent.md` made `build_development_plan_xlsx.py` refuse to build — *"has a content entry but no sprint allocation"* — because `[SSP]` is the **full-scope** plan of record at 103 stories / 1,396 h and these are trial scope. The entries were withdrawn rather than the allocation widened; the script now reports `FW-203` and `FW-204` among its excluded stories with the reason stated. **A guard stopped trial-only scope being injected into a full-scope client deliverable**, which is precisely what it is for. |
| Aug 14, 2026 | Plan team | **New §1.4 breaks Phase 1 out at deliverable level, and the removals record renumbers §1.4 → §1.5.** Phase 1 is **423 h, 54 % of the trial**, has not started, and its hard gate was 14 Aug and was not met — so *"the platform is 423 h"* is not an actionable figure. §1.4 now prints **1A/1B/1C on all three bases side by side** (hand-coded `[CE §3]` **1,027 h** · AI-assisted full **484 h** · **trial 423 h**) with every deliverable mapped to its story, and names the **three reductions totalling 61 h**: `FW-138` 35 → 26 h (nine controllers, not thirteen — `PassSchedule`, `PayoffStaging`, `WeldEvent`, `DieChange` and `RollAdjust` are out of trial scope), **`FW-N05` 22 h → a 6 h simulator** (retention **0.90**, *"not verifiable without the hardware"* — deferring it moves 16 h **and** removes the trial's only hardware dependency, with `FW-150`/`FW-151` left unreduced so the real ingest drops in behind the contract), and **`FW-001` 36 h → 0** (the shared-schema renames plus their **40 h impact audit** across `united_db` and the legacy tier — the highest-blast-radius change in the plan, which a trial does not need and production does).<br><br>**Two things the breakdown surfaces.** **`FW-133` is 45 % of 1A and the largest story in the trial** — six shared composite controls that **all six screens depend on**, so it is untrimmable, nothing downstream starts without it, and it is where a wrong 0.62 FE retention factor shows up first. And **`[CE §8]`'s admission that 1C is understated by ~17 h** (costed against 22 tables, built as 25) is **inherited and stated rather than silently corrected** — 1C is carried as *65 h + ~11 h of known understatement*, because fixing it in place would drift `[CE §3b]`.<br><br>⚠ **One reconciliation is recorded rather than resolved: `[DE §2]` and `[SSP §5]` agree on every Phase-1 sub-total to within 1 h and disagree on the stream split by up to 19 h.** `[DE §2]` factors `[CE §3]`'s *phase* columns (1A FE 134 · RT 33 = 167); `[SSP §5]` factors each *story* (1A FE 139 · RT 27 = 166); 1B is `[DE]` BE 121 · RT 95 = 216 against `[SSP]` BE 140 · RT 77 = 217. This is the same two-bases artefact `[SSP §2]` already documents at plan level (*"the per-stream figures sum to 1,406 h, not 1,396 h"*). **This plan uses the story basis throughout**, and the instruction is explicit: quote sub-phase totals freely, **never mix a `[DE §2]` stream cell with a `[SSP §5]` one**. |
| Aug 14, 2026 | Plan team | **§1.3 gains a phase × stream staffing grid** — the phase totals and the stream totals cross-tabulated, on `[CE §3]`'s convention that every row and column sums to its printed total. It exists because a phase total says a block costs 74 h without saying whether it is an Angular or a .NET problem, which is what assigning four or five people actually needs. **Four findings the phase totals hid:** Phase 1 is three unequal problems that genuinely parallelise (1A **139 h pure FE** · 1B **131 BE + 61 RT** · 1C **65 h pure DB**), which is why a fifth developer pays for itself in T1 and nowhere else; **RT is 88 of its 127 h inside Phase 1** and only **39 h across five blocks** thereafter, so the RT owner must take DB or BE work from T2 or idle — `[DE §4]`'s *"RT does not compress"* sharpened to *"and does not spread"*; **`FW-202` is the only BE-heavy block** (29 of 67 h) and is also last and gates Phase 8, so the BE owner's tail is the schedule's tail; and **DB is never the constraint after 1C** — 47 h across seven blocks, none over 15 h. Also records that `FW-081` is dual-tagged **FE/RT** in `[SSP §5]` and is counted wholly as FE, with the alternative split stated so the reading is chosen rather than assumed. **No total changed.** |
| Aug 14, 2026 | Plan team | **Scope reduced on client direction, same day as publication — `DB1` Line Status and `DB2A` Pre-Check-in + weld capture removed. 885 h → 778 h.** New **§1.5** *(published as §1.4 and renumbered when the Phase-1 breakdown took that number)* records what each removal takes with it rather than presenting either as a clean subtraction; the cross-document detail is under [Repository-wide](#repository-wide). In summary: DB1 costs a **minimal landing route** (5 h, since every mockup's back button targeted it), the SPC dialog's **read-only caller**, and **the only consumer of `AlertRaised`/`AlertCleared`**; DB2A + weld **removes two blockers from trial scope** (`G21`, `G26` — both still open for MVP-1) but leaves the trial with **no weld markers, a single-rod spool, and no exercise of the `CoilTraceability` chain behind the welding-wire certificates**, plus **75 test cases out of scope including the contractual weld suite**. Deferred total rose 218 h → **330 h**.<br><br>**Every derived figure was re-derived rather than adjusted:** streams FE 314 · BE 225 · RT 127 · DB 112; sprints T1 395 · T2 240 · T3 143; Phase 1 now **54 %** of the trial. Three developers reach **~1 Oct** on development alone and **a fourth comes within ten hours of closing the whole trial**, so §2.2's recommendation changed from *"staff to five or publish late October"* to **one additional developer, and a second for T1 only**. |
| Aug 14, 2026 | Plan team | **Created** — the client-requested six-screen trial run, shortcode `[TRP]`: four sprints from 17 Aug 2026, **feature-complete Fri 18 Sep, UAT and sign-off 21–30 Sep**. Scope is the six screens (DB2 · DB3-FL1 · DB6 SPC · DB8 WIP · DB5 · DB3-FL2) plus their dependencies, with **every deferral carrying a stated reason** — `FW-001`'s shared-schema renames and `FW-N05`'s real OPC ingest among them. *(Published at 885 h including DB1 and DB2A; reduced to 778 h the same day — see the entry above.)*<br><br>**It is a scoped subset of `[SSP]`, not a replacement** — `[SSP]` remains the staffed plan for all of MVP-1 development at three developers finishing 25 Nov 2026, and this document says so in its own read-first block and in `[SSP]`'s Related Documents. **No figure in `[SSP]`, `[CE]`, `[DE]` or `[RM]` was changed to accommodate it.**<br><br>**§2.2 publishes what each staffing option lands rather than asserting the requested date.** §5 records the two estimate corrections made during drafting, **both upward** — `FW-202` from 52 h to 67 h, and the DB2A block from 24 h to 64 h and then 70 h once it was removed outright, each figure correct against its own baseline and retained so the difference does not read as a discrepancy later. §9 states plainly that the trial proves the screens and not the machine: every line runs `SimulatePLCTagPush` and a simulated OPC feed, so **nothing in it verifies a tag path against a controller** while `G32`/`G33` hold the whole map at `[PROPOSED]`. |

---

## [MVP-1/ProjectPlan/REVIEW.md](MVP-1/ProjectPlan/Development/REVIEW.md)

| Date | Author | Change |
|---|---|---|
| 2026-07-26 | Review | Initial review of `MVP-1/DevelopmentPlan/`; Tiers 1–7 + reconcile-up appendix. Phase 1 split into 1A/1B/1C with the relevant fixes baked in. |
| 2026-08-11 | Review | **Header reissued against the MVP-1/MVP-2 split.** Corrected `Last Updated`, the gaps-register range (`G1–G20` → **`G1–G35`**) and the *"since today is 2026-07-30"* sentence, which had been read as a live statement. Added a scope banner recording the **three standing decisions** — pass schedule owned outside MVP-1, die inventory out with the die change event staying, and **Phase 9 wholly MVP-1** — and marked the two findings those decisions move: **Tier 1 #1** struck as **out of MVP-1 scope** (retained for the owning track, and because `FR-380`–`FR-391` still sit in `02-SRS.md`), and **Tier 2 #13** escalated to an **MVP-1 blocker**, joined by `FR-346`'s second weight-capture point. **No Tier 0 section was added and no register entry was created** — deliberate; the findings live in the remediation plan, not in a new register. |
| 2026-08-06 | Review | **Tier 1 finding #1 escalated: the generate-from-specs *algorithm* is superseded, not only its worked example.** A gap audit of `MVP-2/RequirementDocuments/PassScheduleGenerationSpec.md` (issued as v1.5) found `FR-381`, `FR-384`, `FR-385`, `FR-386` and `FR-387` wrong on the physics — bound-as-answer with no edge correction; snap-to-nearest against `R36`/`V39`; a positive fixed-multiplier roll gap where mill spring is negative and load-dependent; and the finishing-mill test conflated with route selection. Correcting `APIContracts.md`'s example to match them would have hardened the error. Arbitration published as `FlatWire_MasterSpecification.md` **§10.5**; a callout above finding #1 carries the one-line summary of each. **Contract shape is unaffected** — the envelope, `FR-389`, `FR-390` and `FR-391` stand. Note that this makes finding #1 a *symptom*: it stays open, but the fix is to rebuild the formula, not to re-derive the example. |
| Aug 13, 2026 | Reconcile-up | **Its own Appendix worklist executed, and three of the worklist's rows struck as wrong.** A banner at the head records that the reconcile-up ran and that dropping `Rod`, fixing the `/passschedule/generate` example and the *"20 → 22"* table count would each have introduced an error. **Sixteen findings marked closed** with dates — Tier 1 #2/#3/#4/#7, Tier 4 #32/#33/#36/#38, all six of Tier 5, and Tier 7 #50 — and **#51 marked superseded by its own arithmetic**, since it was correct when written and overtaken by `FW-124`. The **Top 5 is re-derived**: the original list is retained as the record of what the 11 Aug review judged urgent, and the current top item is **`G1`**, the capacity escalation, because it is the only one that cannot be closed by editing a document. **New Tier 8 adds six findings** — `FW-124` reaching no summary and the coverage appendix that could not detect it (#57), the `.code-workspace` that had not opened correctly since the folder moved (#58), the two-API-contract ruling (#59), the MVP-2 blind spot (#60), the two missing `ShopfloorPlan/` header blocks (#61) and three untrue document statuses (#62). |
---

## MVP-1/DevelopmentPlan/RodPreCheckinUxReviewPrompt.md

*Deleted 13 Aug 2026 — heading unlinked because the file no longer exists; recoverable at `1964086`. The section is retained as the audit trail.*

| Date | Change | By |
|---|---|---|
| Aug 13, 2026 | **Deleted.** A prompt for a UX review that had already been run — the review output, `Dashboard2A_UXReview.md`, was itself deleted on 1 Aug 2026 once its 17 findings were propagated, and the prompt then described a screen region (*the weld strip*) removed the same day, so it briefed a reviewer on a layout that no longer existed. **Genuinely spent, unlike the three documents reduced to records alongside it:** only three references existed, none needing its body — `REVIEW.md` #60, `MVP-2/DevelopmentPlan/README.md`'s scope table, and the 30 Jul client-call ledger's list of places 1280x1024 is baked in. All three were repointed. Recoverable at `1964086`. | Clean-up |
| 2026-07-30 | Initial version | Claude |

---

## [MVP-1/ProjectPlan/ShopfloorAndRealTimePlan.md](MVP-1/ProjectPlan/Development/Roadmap.md)

| Date | Changed By | Description |
|------|-----------|-------------|
| Aug 11, 2026 | Plan team | **MVP-1 apportioned, Phase 9 returned whole, and two scope questions closed.** The effort model gained **§3b**: MVP-1 is **3,292 h / 9.4 FTE**, derived by re-pricing the carved deliverables from the §2 rate card and **re-deriving** QA and contingency, because the descope ladder straddles the MVP boundary and cannot supply a split. **Phase 2 stays out permanently** (231 h) — pass schedule generation and management are owned by a **separate track**, so `phase-04` was rewritten to treat the schedule as an **external read** (`PassScheduleId` is a documented external reference, like `PlanId`) and the DDL runner no longer claims an incomplete build. **Phase 9 returned to MVP-1 in full** — DB7, DB7b, both coil endpoints, `CoilCompletionService` and `FW-066` — because `CoilOutput`/`CoilTraceability` were MVP-1 with **no writer at all**; the MVP-2 partial file was deleted. **Die inventory and lifecycle left MVP-1 for good**, taking the 8 h die table with the screen, and `D4` was restated at **die-size** level against `Drawer` (`DieChangeAndManagement.md` v2.4 §2.4a) — which also dissolves the Phase 6 → Phase 13 dependency. **Phases 11 and 13 are now 175 h and 143 h.** **The programme finding is unchanged:** the recovered FTE landed in W2–W3, which were never the constraint, and **W7 is still 24.5 FTE against three days**. |
| Aug 11, 2026 | Plan team | **`DevelopmentPlan` divided by MVP scope — and the effort model deliberately was not.** The folder moved into [`../../MVP-1/DevelopmentPlan/`](../) and was split against `MVP-2/`. **Moved whole:** **Phase 2** (with its **231 h** — all four of its stories `FW-010`–`FW-013` and the three `PassSchedule*` tables are deferred) and `PassScheduleGenerationSpecPrompt.md`. **Carved partially:** phase **9** (DB7/DB7b, at section level), phase **11** (DB10, at bullet level) and phase **13** (Die Management, at bullet level) — all verbatim. **Everything cross-cutting stayed:** this index, `00-foundations.md` (which carries a do-not-restate rule), `back-matter.md` with the **G1–G35** register, `REVIEW.md`, the four April split-brain docs, `PLCTagImplementation.md` and `Tools/`. **The hours are NOT apportioned, and that is a decision, not an omission.** Phases 9/11/13 still carry their whole **222 / 246 / 209 h**, so they now **overstate MVP-1**; the partial MVP-2 files carry **no figure at all**. The descope ladder cannot supply one — **rung 5 (99 h) bundles the MVP-2 Die Management screen with the MVP-1 role-assignment UI**, and **rung 6 (105 h) defers four reports that are MVP-1** — so any apportionment would be invented. **3,727 h and 10.6 FTE now mean *both scopes combined*.** **The division also makes one contradiction unavoidable:** Phase 2 is wholly MVP-2 while remaining *"the highest-priority dependency in the plan"* for MVP-1 check-in. And two seams leave MVP-1 work inside a deferred screen — `CoilCompletionService` builds the `CoilTraceability` rows the welding-wire certificates depend on, and the missing die inventory table is costed at 8 h in MVP-1 while its screen is in MVP-2. All references repointed; **zero dangling links**. |
| Aug 11, 2026 | Plan team | **`CoilOutput` and `CoilTraceability` returned to MVP-1** (client direction). They had been split into MVP-2 with the DB7 / DB7b screens hours earlier; that was the wrong boundary. **The coil genealogy those two tables carry is what the welding-wire customer certificates are produced from**, which is an MVP-1 obligation regardless of whether the screen that writes them ships. Moved back **byte-identical** and to their original position in `05_QualityOutput.sql`: both `CREATE TABLE` blocks, 4 FKs, 7 indexes, the **DM010** `trg_CoilTraceability_NoOverlap` trigger, their seed data, and their `FlatWireSchema_QualityOutput.md` sections. **`CoilOutput.PassScheduleId` → `PassSchedule` stayed in MVP-2** as a **fourth** back-reference, since its parent is still deferred — so the unenforced-column exposure between an MVP-1 and an MVP-2 deployment now covers **four** tables, not three. New counts: MVP-1 **24 tables · 33 FKs · 41 indexes · 2 programmability objects**; MVP-2 **3 tables · 10 FKs · 6 indexes · 1 procedure** (`sp_ShiftSummary`). Totals verified unchanged at **27 / 43 / 47**. **The DB7 / DB7b screen artifacts stay in MVP-2** — the mockups, `OutputCoilCompletion.md`, SRS §5.16/§5.17, TC-405–444 and `FW-066`. The lesson is worth keeping: **a deferred screen does not imply deferred persistence.** |
| Aug 11, 2026 | Plan team | **`DBChanges` and `ProjectPlan` divided by MVP scope.** Both moved out of `LatestDocument/` into [`../MVP-1/`](../), then split against `MVP-2/`. **Nothing was reworded, renumbered or re-estimated** — every extracted section is verbatim. **ProjectPlan:** the screen-mapped sections went to [`../MVP-2/ProjectPlan/`](MVP-1/ProjectPlan/) — `02-SRS.md` §5.10/5.16/5.17/5.18/5.19/5.23/5.24, four endpoints from `04-APIContract.md`, five `TC-###` blocks from `06-TestPlanAndTestCases.md`, and eight `FW-###` rows. Cross-cutting content (domain model, §5.0, NFRs, security/roles, HLD+ER, conventions, capacity model, runbook) **stayed in MVP-1 and is cited, never copied.** Three divisions are visible on purpose: `TC-###` ranges are now **discontinuous** (a gap means deferred), the endpoint index still counts **30** while MVP-1 implements **26**, and the eight backlog rows are **marked in place, not removed**, because §11 is the coverage matrix proving every `FR-###` reaches a story. **DBChanges — and this is the part to read before deploying:** 5 of 27 tables, 14 of 43 FKs, 13 of 47 indexes and 2 of 3 programmability objects moved. *⚠ These four figures were **superseded the same day** — `CoilOutput` and `CoilTraceability` returned to MVP-1 (see the entry above), leaving **3 / 10 / 6 / 1**. Retained as the audit trail, not as current counts.* **Three of those FKs sit on MVP-1 tables** (`FlatWireRun`, `RodCheckin`, `SpoolCheckin` → `PassSchedule`) and were routed to MVP-2 so the MVP-1 chain stays deployable; the cost is that `PassScheduleId` is **unenforced, with dangling seed values**, between an MVP-1 and an MVP-2 deployment. MVP-2's runner is ordered **seed-before-constrain** because MVP-1 seeds rows pointing at pass schedules created in MVP-2. `Drawer` could not be divided at all — two of its columns exist only for the deferred Die Management screen. **This division contradicts the screen-scoped principle recorded in the two entries below**, which is why the effort figures still stand: the phase hours were never re-derived. |
| Aug 11, 2026 | Plan team | **`MVP-1/` created to mirror `MVP-2/` — a relocation, not a scope change.** `Mockups/` (root), `LatestDocument/RequirementDocuments/` and `SRS/` moved into [`../MVP-1/`](../) as `Mockups/`, `RequirementDocuments/` and `SRS/`. **`LatestDocument/` no longer holds a `RequirementDocuments/` folder.** Every cross-reference in the repository was repointed and verified — **zero dangling links** — and `build_docx.py`'s source and output paths were updated with it. **No requirement, estimate, phase or scope statement changed.** **`MVP-1/` is not all of MVP-1**, and that is the one thing to know about it: this roadmap and all 17 phase files, the `FR-###` requirement text in `02-SRS.md`, the master specification, the schema and DDL, the open-questions register and the `FW-###` backlog are **all MVP-1 content that stayed where it was**. The folder means *MVP-1 screens and their specs*. [`../MVP-1/README.md`](MVP-1/README.md) records the structure and lists what is deliberately outside it. |
| Aug 11, 2026 | Plan team | **MVP-2 promoted to a top-level folder; the owning specifications followed the screens.** `MVP-1/Mockups/FutureMVP-1/Mockups/` became [`../MVP-2/`](MVP-2/) with `MVP-1/Mockups/`, `RequirementDocuments/` and `MVP-1/SRS/` beneath it. Moved in: `OutputCoilCompletion.md`, `PassScheduleManagement.md`, `ShiftSummary.md`, `PassScheduleGenerationSpec.md` and the client `PassScheduleGenerationSpec.docx`. **`DieChangeAndManagement.md` was split rather than moved** — §4 Die Management became `MVP-2/RequirementDocuments/DieManagement.md`, while §1–3 (the mid-run die change event, **still MVP-1**, Phase 6, `die_change.js`) and §5 (the die-life status vocabulary, deliberately **not** copied across) stay in `MVP-1/RequirementDocuments/` at v2.3. **The per-screen specification count is now 15 in MVP-1 plus 5 in MVP-2** — the "17 specifications" figure in §0.1 and `CLAUDE.md` was corrected. **Four things crossing the boundary are recorded rather than resolved:** `PassScheduleGenerationSpec.md` remains the **authority on generation physics and arithmetic** over `FR-380`–`FR-391` despite being MVP-2 (master spec §10.5); `MVP-2/SRS/PassScheduleGenerationSpec.docx` is the **branding template** `build_docx.py` opens to render every client deliverable *including MVP-1's*, so its `TPL` path was updated and the file must not be deleted as inert; `PassScheduleManagement.md` §3.3–§3.4 is the **only surviving home of the Operations Manager role definition**; and the MVP-1 die change has **no source** for die identity, footage counter or life threshold, nor any way to enforce `D4`, raised as a new **High** open item in the parent's §8. All inbound cross-references across the repository were repointed so **no link dangles**; the mockups still open and render, loading the shared chrome from `../../MVP-1/Mockups/`. **Scope claims elsewhere remain stale on purpose** — the four phase files, `back-matter.md`, the master spec, `02-SRS.md`, `05-SprintPlanAndBacklog.md` and the `FlatWireShopfloorDashboards.md` inventory. |
| Aug 11, 2026 | Plan team | **Seven screens recorded as out of MVP-1 and out of planning.** **DB7** Output Coil Completion · **DB7b** Packing Station · **DB9** Pass Schedule Management · **DB9A** Pass Schedule List · **DB10** Supervisor Shift Summary · **Die Management** · **OEE Dashboard**. Their mockups moved to [`../MVP-2/`](MVP-2/); the [README](MVP-2/README.md) is the record. A second **Scope note** was added at the head of this file. **Nothing else was changed — deliberately.** The phase table, the 3,727 h effort figure and the 10.6-FTE conclusion **all stand**, because the exclusion is **screen-scoped and the phases are not**: Phase 2 also owns the `PassSchedule` tables that rod check-in acknowledges and pushes PLC tags from, Phase 9 also owns `CoilOutput`/`CoilTraceability` genealogy for welding-wire certificates, Phase 11 also owns reporting and certification, and Phase 13 also owns reference data beyond die inventory. Phases 2/9/11/13 total **908 h of 3,727 h**; how much of it the exclusion removes needs a deliberate pass that **has not been taken**, so this is an *input to* **G1** and not a closure of it. Also unchanged and therefore now stale on this point: the four phase files, `back-matter.md`, `FlatWire_MasterSpecification.md` (§2466/§2475 inventory, the *"20 approved screens"* list, the DB7b/OEE sections), `02-SRS.md` (§1094/§1274, §1369/§1378) and the `FW-###` backlog. **Three consequences are open**, listed in the README: how pass schedule records are authored and approved for MVP-1 when DB9/DB9A are gone (Phase 2 is described as gating every check-in phase), how a run records an output coil without DB7, and whether the Phase-6 die change dialog — which **stays** in `MVP-1/Mockups/` — can record against a die with no life record. `DB7b` and `OEE` were never in the fifteen-screen Dashboard Inventory and have no owning requirement document; the other five keep theirs in `MVP-1/RequirementDocuments/`, undeleted and unrewritten. |
| Jul 30, 2026 | Plan team | **Added the capacity & effort model — resolves G1 / OI-51.** New [`CapacityAndEffortModel.md`](MVP-1/ProjectPlan/Development/CapacityAndEffortModel.md): six delivery streams + an unfilled named-owner roster, a published unit-rate card, per-phase effort **in hours** for all 17 phase specs (**3,727 h total**), a working-day capacity model, an ordered descope ladder (which recovers only **12%**), and a calibration checkpoint at the Aug-14 gate. Extended this file's nav table with **Owner (stream)**, **Effort** and **Wk** columns; added the model to §0.1 and flagged `FlatWireJiraStories.md` as superseded *as a schedule*. Stamped `**Owner:**` + `**Effort:**` doc-control blocks onto all 17 phase files (closes `REVIEW.md` #53). Re-baselined `back-matter.md`'s week grid to **W0 = to Aug 14** (hard gate) with **Labor Day Mon 7 Sep** deducted and W7 marked as 3 days — post-gate window is **32 working days**, whole window **44** (closes `REVIEW.md` #31); M1 and a new QA0 moved to the Aug-14 gate. **The model finds the window does not close as scoped:** 10.6 FTE sustained, a 10.7-FTE Phase-1 gate, an impossible 27.2-FTE W7, and a descope ladder that recovers only 12% — escalated as G1's residual for a programme decision. Recorded that OI-51's "thirteen workflow phases" and G1's "14 phases" are **both correct** (1 platform + 13 workflow phases 2–14), and that epic **E01 contains no story for the Angular or .NET scaffold**, which is why the window was believed to fit. |
| Jul 26, 2026 | Plan team | **Renumbered phases to a contiguous 1–14.** After the two upstream phases were removed, the remaining phases were renumbered to close the 3–4 gap: old 5→3, 6→4, 7→5, 8→6, 9→7, 10→8, 11→9, 12→10, 13→11, 14→12, 15→13, 16→14 (Phases 1–2 unchanged). Renamed the 12 phase files to match, and updated every reference — H1 headers, Prev/Next navigation, in-body cross-references, the index nav table, the dependency chain, shared-building-blocks table, roadmap grid, risks/gaps register, and Appendices A/B/C phase columns. Dashboard IDs, story IDs (FW-###), OQ/gap IDs, and stand/line labels were left unchanged. |
| Jul 26, 2026 | Plan team | **Removed Phases 3 & 4 (out of shopfloor scope).** Rod Receiving (former Phase 3) and Order Planning & Line Scheduling (former Phase 4) are handled upstream by the existing CoilReceiving / Planning / Scheduling systems and are not part of the shopfloor implementation. Deleted `phase-03-rod-receiving.md` and `phase-04-order-planning-line-scheduling.md`; reframed their references across the plan as upstream prerequisites (dependency chain, parallelisable/cross-impact notes, roadmap grid W2/W3, M2 milestone, risks, Appendix A rod endpoint); moved their 14 backlog stories (FW-020–022 rod receiving; FW-030/031, FW-040–043, FW-050–055 orders/planning/scheduling, minus FW-054 which stays in Phase 15 admin) out of the Appendix C coverage table (now **44/44 shopfloor stories**). Repaired Prev/Next navigation (Phase 2 ↔ Phase 5) and the Phase 6 entry conditions. Phase numbering kept stable (gap at 3–4) so existing "Phase N" references stay valid. |
| Jul 26, 2026 | Plan team | **Split into per-phase files.** Reorganised the single-file master roadmap into this index plus one file per phase under [`ShopfloorPlan/`](MVP-1/ProjectPlan/Development/Phases/): [`00-foundations.md`](MVP-1/ProjectPlan/Architecture/Architecture.md) (§0.2–0.4 shared context), `phase-01…16-*.md` (one file per phase), and [`back-matter.md`](MVP-1/ProjectPlan/Development/GapsRegister.md) (dependency mapping, overall roadmap, gaps register, appendices, related documents). Phase content copied verbatim; no scope, decisions, or wording changed. This top-level file is now the navigation index and change log. |
| Jul 26, 2026 | Plan team | **Full rewrite.** Replaced the shopfloor-only, layer-organised sprint plan with a workflow-driven master roadmap: Phase 1 Core Platform Setup (Angular/Backend/Database/real-time), then Phases 2–16 as end-to-end vertical slices ordered by the application/user journey. Grounded in the actual `ual-angular`/`ual-api` reference assets (CoilCheckin, shared library, UAController, OPCConnection), the 22-table Flat Wire schema, the full `FlatWire.API` contract, all 14 dashboards, the May 21 equipment corrections, and the OQ register. |
| Jul 26, 2026 | Plan team | **Applied three confirmed decisions:** (1) the Flat Wire UI is a **separate new Angular library** (`flat-wire-shopfloor`); (2) the tables move to a **new `FlatWireDB`** database (DDL retargeted from `united_db`; FW-001 renames stay in the existing scheduling DB); (3) the real-time layer is **not** a copy of the existing `CoilDataHub`/`OPCManagerHub`/`supervisor-monitor-hub` patterns — it is a fresh **industry-standard, fast, optimized** design (added §0.4: strongly-typed MessagePack hub, WebSockets-first, bounded-channel ingest with batching/decimation, NgZone-out + rAF rendering, backplane-ready scale-out), self-contained in `FlatWire.API`. |
| Jul 26, 2026 | Plan team | **Approved mockups adopted as UI source of truth (`MVP-1/Mockups/`), verified against the actual files:** all dashboards already share one `--color-*` semantic token system (`flat-wire-shopfloor.styles.scss`) — the `--fw-*` prefix in older source docs is **stale** (no mockup or stylesheet uses it; there is **no** token migration). Every dashboard already has an approved `--color-*` mockup; **Dashboard 2** was revised to `dashboard_2_rod_checkin.html` — a 6-step tab-wizard (Visual Inspection → Pass Schedule → Pre-run SPC → Die Block → Rolling Mill → Lube & Safety) with tolerance-viz replacing the old progress ring, OK/NG/NA machine inspection, supervisor-override; confirm-bar retained. Updated decision 6, Phase 1A (theme/components), Phase 6 UI; corrected the `--fw-*` narrative; **G18** now tracks the stale-doc correction. |
| Jul 26, 2026 | Plan team | **Frontend reuse policy reversed (confirmed decision).** The Flat Wire UI is now **all-new screens and controls** — no existing Angular feature library (`checkin-precheckin`, `shop-floor-common`, `statistical-process-control`, `wip-rejection`, `slitter-*`, `common-grid`, etc.) is used as a structural/UI/CSS reference. The **only** reuse is the foundational `shared` services (HTTP gateway, login/JWT interceptor, app-config, error handler, correlation-id, logging), consumed so the library plugs into the app shell. Backend reuse plan is unchanged (`CoilCheckin` remains the `FlatWire.API` template; `WipRejection` still extended). Updated §0.2 (Angular note), decision 5, and §1A folder-structure / shared-services rows. Also corrected the working-tree paths to `c:\UAL\ual-angular` / `c:\UAL\ual-api` (dropped the stale `Second-Branch` note). |
| Jul 26, 2026 | Plan team | **Review round applied:** (a) development window reset to **17 Aug → 30 Sep 2026** (~6.5 wks) — roadmap grid, dev/QA/deploy milestones re-dated; original Jul 1 / Aug 1 targets superseded; (b) **rod uses the existing `coils` table** — designed `Rod` table dropped, `FlatWireDB` → **21 tables**, all `Rod.Alpha` refs now cross-DB logical links (schema/relationship/Phase 3/6/9/11 sections updated); (c) **`SlitterInterface`/`slitter-*` removed as references** (UI and real-time); (d) added **Appendix C** (all 58 FW-### → phase, with deferred flags — 58/58 covered); (e) added the **Known Gaps & Issues Register** (G1–G17, prioritised) capturing the plan review. |

---

## [MVP-1/ProjectPlan/ShopfloorPlan/back-matter.md](MVP-1/ProjectPlan/Development/GapsRegister.md)

| Date | Changed By | Description |
|------|-----------|-------------|
| Aug 13, 2026 | Plan team | **Appendix C and Related Documents re-pointed after the Jira backlog was split three ways.** `FlatWireJiraStories.md` was reduced to **shopfloor MVP-1 only (35 stories / 147 points)**; Phase 12's 4 / 11 and MVP-2's 6 / 31 are in sibling files, and the **14 upstream stories were deleted** as another team's backlog. Appendix C's coverage claim stays **45/45** — the mapping is unchanged — but now records **which of the three files each story lives in**, and states that the **59-story / 231-point totals retire** because upstream's 14 / 42 left the repository. The upstream id list is retained with a note that the bodies are **recoverable at commit `1964086`** and that their ids, titles and descriptions remain in `05-SprintPlanAndBacklog.md` §7 — the ids still resolve, and **`FW-061` still depends on `FW-020`**, which is the dependency behind this document's *'if upstream slips, Phase 4 has no material'* risk. Related Documents gained rows for both new files and now cites the backlog's points-vs-hours cross-check (**9.5 h/point** AI-assisted, 7.7 excluding Phase 1). No gap, milestone, week-grid or effort figure in this document changed. |
| Aug 13, 2026 | Gap remediation | **Gap register repaired: two malformed rows completed, two missing entries added.** **`G27` and `G28` had four pipes where every other row has eight** — both were truncated after the Priority cell from the day they were written, so they carried no Area, Impact, Recommended resolution or Status. `G28` is **High** (*FL2 may have no way to record a weld*) and had no recorded disposition at all; both rows are now complete, and `G28`'s resolution states the single client question that settles it and warns against closing it by deleting the disagreeing fixture. **`G26` was cited by [`phase-06`](MVP-1/ProjectPlan/Development/Phases/phase-06-in-run-production-events.md) from 1 Aug 2026 against no register entry** — `WeldEventPopupPlan.md` §5.3 step **S7** said *"Add G25 and G26"* and only the G25 withdrawal was executed, so a phase file cited an id that resolved to nothing for twelve days. **`G25` is registered, reusing the id** the withdrawn weld-history gap released — the register's own note said *"the next free gap ID remains G25"* — for the requirement-coverage defect described under `06-TestPlanAndTestCases.md`. |
| Aug 13, 2026 | Gap remediation | **The MVP-1/MVP-2 split reached the appendices; it had missed them entirely.** `05-SprintPlanAndBacklog.md` §11 and `06-TestPlanAndTestCases.md` were both marked in place on 11 Aug 2026, but this file was not. **Appendix C** gains a **Scope** column separating *what MVP-1 builds* from *what is in the Aug 17 – Sep 30 window* — two axes that had been collapsed into one *"In scope"* value, which is how six MVP-2 stories (`FW-010`–`FW-013`, `FW-068`, `FW-069`) came to be listed as in scope. The closing figure is restated: **44/44 remains true as mapping coverage** — every story has a phase, which is what the appendix proves — but **MVP-1 builds 38 of the 44**, and the two `[NEW]` backlog stories absent from this table (`FW-N07`, `FW-N09`) are named so they are not read as uncovered. **Appendix A** gains a Scope column and the `GET /run/{runId}/weldevents` row (**G26**). **Appendix B** gains one too, and its **`4 Weld Event → 6`** row is struck: Dashboard 4 was retired 1 Aug 2026 and `FW-063` has no screen of its own. Die Management is split from Die Change. The **W6 week row** no longer lists `FW-069` among its backlog. **No row was deleted** — the same convention §11 uses, because deleting them tears holes in the coverage evidence. |
| Aug 13, 2026 | Reconcile-up | **Header block added, and Appendix C's coverage claim corrected from 44/44 to 45/45.** This file and `00-foundations.md` were the only two in `ShopfloorPlan/` with no `Project` / `Last Updated` / `Status` block, though `CLAUDE.md` mandates one and all 17 phase files carry it — leaving the live `G##` register with no modification date a reader could check. **`FW-124` · Dashboard 5A — FL2 Spool Queue** (5 pts, `High`, phase 8) was **missing from Appendix C entirely**; it is built by `phase-08`, specified by `02-SRS.md` §5.3a and owned by `SpoolQueue.md`. The row is added and the closing figure restated — **the 44/44 claim was not a miscount, the row simply was not there**, which is the failure mode a coverage appendix exists to prevent. The *"184-point cross-check basis"* cited in Related Documents is likewise corrected to **189** (45 shopfloor stories), with a note that both it and "58 stories" were correct before `FW-124` and never updated. |
---

## [MVP-1/ProjectPlan/ShopfloorPlan/phase-01-core-platform-setup.md](MVP-1/ProjectPlan/Development/Roadmap.md)

| Date | Changed By | Description |
|------|-----------|-------------|
| Aug 13, 2026 | Gap remediation | **Marked a roll-up index; `1A`/`1B`/`1C` declared authoritative.** This file and the three layer specs were **all** `Status: Ready to build`, with nothing saying which to follow — 107 lines of description maintained in parallel with the 257 lines that supersede them, on the one phase carrying the **14 Aug gate**. `REVIEW.md` records that the layer specs *"bake in the Phase-1-relevant fixes"*, but that was stated only in the review. The status line and a banner now say it in the file itself. **No content was removed** — the roll-up's objective, exit condition and effort roll-up are still the right place to read the cross-layer picture. |

---

## [MVP-1/ProjectPlan/ShopfloorPlan/phase-12-yield-cost-ledger-scrap.md](MVP-1/ProjectPlan/Development/Phases/phase-12-yield-cost-ledger-scrap.md)

| Date | Changed By | Description |
|------|-----------|-------------|
| Aug 13, 2026 | Plan team | **Pointer added to the new [`YieldCostAndScrapSheet.md`](MVP-1/ProjectPlan/Development/YieldCostAndScrapSheet.md); no scope or effort figure in this file changed.** Added a `Phase sheet:` line to the doc-control block and qualified `Status` from a bare *"Ready to build"* to *"Ready to build — but see the specification gap in the phase sheet"*. The qualification is the point: this file's `Scope call` already warned *"Estimate provisional"* against four open items, but not that **`FW-101`, `FW-102` and `FW-110` carry no `FR-` IDs anywhere in `02-SRS.md` and the phase has no owning requirement document** — so its 177 h was priced against four Jira cards rather than a specification. `FW-100`'s formula is specified (`FR-332`/`FR-332a`) but under §5.16, which the SRS maps to DB7 / Phase 9. |

---

## [03-HLD-and-ERDiagram.md §14](MVP-1/ProjectPlan/Architecture/Architecture.md)

| Date | Changed By | Description |
|------|-----------|-------------|
| Aug 13, 2026 | Reconcile-up | **Accepted, fifteen weeks after the build started following it.** Status moved from *Recommendation — Pending Review* to **Accepted**: the `FlatWire` microservice, `FlatWireDB`, SignalR and the extended `OPCConnection` are all in the roadmap and Phase 1 is building them, so the ADR was never actually pending. **Two open points closed.** The database row read *"new `FlatWireDB` **or schema extension**"* — the alternative is struck; it is a **new standalone `FlatWireDB`**, built by `FlatWire_DDL_00_Database.sql`, with `G17`'s cross-database cost (unenforceable logical FKs) named as the price of that choice. The *"five SPC checkpoints vs four `CheckpointType` values"* contradiction (`REVIEW.md` Tier 1 #7) is closed as **not a contradiction**: the five are *physical measurement points* and the enum is *why a checkpoint fired* — different axes that were never meant to map one-to-one. **One reference correction:** the Angular rationale cites *"the shopfloor pattern exists in slitter/furnace modules"*, which must not be read as permission to copy them — `00-foundations.md` §0.2 makes `SlitterInterface` explicitly not a reference. The superseded July 1 trial framing was removed from the summary. |

---

## [MVP-1/ProjectPlan/Tools/README.md](MVP-1/ProjectPlan/Tools/README.md)

| Date | Change |
|---|---|
| Aug 14, 2026 | **Four scripts became five, and the folder's own one-line summary stopped being true.** It opened *"Three render a client deliverable"*; [`build_trial_run_xlsx.py`](MVP-1/ProjectPlan/Tools/build_trial_run_xlsx.py) renders one that is **internal, shared with the client**, so the wording is now *"Four render a deliverable"* and the duplication note reads **three** workbook builders rather than two. A section documents the new script: its ten sheets, its two-source construction, **why the abbreviation guard exists** and the two rules that keep the expansion safe, and a table of all five guards **with the exact message each produced when broken deliberately**.<br><br>⚠ **A callout was added because the summary table's `Writes` column now has an exception.** Every other generator writes into `MVP-1/SRS/`; this one writes into `Development/`, because `MVP-1/SRS/` is where **leakage-guarded** deliverables live and moving the trial-run workbook there would put an unguarded file — one carrying story identifiers, hours and gap identifiers by design — among guarded ones. Also recorded, because it cost a wasted test: **exercising the reconciliation guard means changing a data row, not a footer**, since footer rows are excluded from their own sums on purpose. |
| Aug 13, 2026 | **`build_development_plan_xlsx.py` documented — the fourth script, and the second client workbook builder.** Adds a summary-table row and a full section: what it reads, the seven sheets, the two-source construction, its guards, and the two figures it deliberately withholds. **Three notes are load-bearing.** *(1)* **`Reference title` is a mirror of the plan's own title, not authored prose** — paraphrasing it leaves the drift guard checking nothing, which is exactly what happened on the first build: 60 of 107 entries had hand-typed approximations and the guard rejected every one. *(2)* The two workbook builders **share their helpers by duplication, not import**, so a change to one deliverable's guard cannot silently change the other's. *(3)* Bare `Angular`, `.NET` and `SQL` are **not** blocked by the leakage guard — they name the technology, which the client already knows. Also corrected a stale link label left by the 13 Aug consolidation: the `ClientQuestionsContent.md` reference read `../../RequirementDocuments/…` against a target that is local to this folder. |
| Aug 12, 2026 | **`build_questions_xlsx.py` — Excel no longer repairs the workbook on open, and `Recommended answer` became optional.** Two changes, one a real defect. **(1) The repair.** Excel had been reporting *"Repaired Records: String properties from /xl/worksheets/sheet2.xml"* on every open — four repair logs in `%TEMP%` on 12 Aug alone — and the cause was a **whitespace-only rich-text run**. `render()` splits authored `**bold**` into runs, so two bold spans separated by a single space emit a run containing just `" "`; **openpyxl 3.1.5 writes `xml:space="preserve"` on a whitespace-*edged* run (`" x "`) but omits it on a whitespace-*only* one**, so XML normalisation strips the content and Excel sees an invalid run. Reproduced in isolation against openpyxl directly, so it is an upstream bug, not a data problem. Fixed in our code by `_fold_blank_runs()`, which **never emits such a run** — the whitespace is folded into an adjacent run instead of dropped, so the text is byte-identical (a bold space renders the same as a plain one). The single offending cell was `D27`, the `Q83` die-life decision. Verified by opening the rebuilt workbook through Excel COM: **no new repair log**. The defect was independent of any content change and had been shipping in the client deliverable. **(2)** `Recommended answer` moved from `REQUIRED_OPEN` to the optional set beside `Options` and `Already agreed`, because `Q19` (footage-to-weight) deliberately carries no recommendation; `build()` was also switched to `.get()` for that field, a missing required field having previously been guaranteed by the guard, and the Open Questions caption relaxed from *"each with our recommended answer"* to *"almost all with"*. |
| Aug 6, 2026 | **Flowcharts, and two smaller fixes.** ```` ```mermaid ```` fences were rendering as **source** in the client deliverable — `PassScheduleGenerationSpec.md` has two, §6.1's thirteen-step calculation sequence and §6.2's route tree, and both reached the client as raw mermaid text. They are now **drawn as boxes and lines** out of table cells (see above). Also: **HTML comments are stripped**, since `<!-- TOC -->` was rendering as a literal paragraph on the first page; and the script now **refuses to render onto `MVP-2/SRS/PassScheduleGenerationSpec.docx`**, which is its own branding template — `shutil.copyfile` raises `SameFileError` on that path, so rebuilding *that* file means rendering to a temp path and moving it into place. |
| Aug 4, 2026 | **Created.** `build_docx.py` committed after being re-derived three times — `CLAUDE.md` had recorded the missing render pipeline as a recurring problem, and the scripts it named (`extract.py`, `assemble.py`, `build_docx.py`, `verify_docx.py`, `slim_docx.py`) lived only in a session scratchpad and were lost each time. This one is parameterised over source, output and header title so it is not specific to one document. |
| Aug 13, 2026 | **Documented the two undocumented scripts.** The README covered `build_docx.py` only, while `build_questions_xlsx.py` had been committed and in use since 12 Aug 2026 — the same omission being corrected across the inventories this pass. Adds a three-row summary table at the top (what each reads, writes and fails on), a full section on `build_questions_xlsx.py` including its four fatal guards and the note that **`Recommended answer` and `Why` are optional for exactly one reason** and must not be restored to `REQUIRED_OPEN`, and a section on the new `build_coverage_matrix.py`. |
---

## [MVP-1/ProjectPlan/Tools/build_development_plan_xlsx.py](MVP-1/ProjectPlan/Tools/build_development_plan_xlsx.py)

| Date | Changed By | Description |
|------|-----------|-------------|
| Aug 13, 2026 | Plan team | **Created — the development plan as a client deliverable.** Seven sheets to `MVP-1/SRS/FlatWire_DevelopmentPlan.xlsx`: *Read Me · Delivery Options · Plan by Stage · Sprint Schedule · Work Items · Milestones and Needs · Assumptions and Risks*. Built to the same two-source shape as `build_questions_xlsx.py` — **structure parsed, prose authored** — with `DevelopmentPlanContent.md` as the only home of the client wording and `TaskBreakdown.md` / `StaffedSprintPlans.md` supplying every figure, so a plan change propagates on the next run rather than being retyped.<br><br>**The prose had to be written, not filtered, and the count is why: 68 of the 116 work-item titles trip the leakage guard** — 40 carry code spans, 11 name endpoints, 10 use screen numbers, 7 name database tables. Deleting those does not leave a sentence. The raw material is each story's `So that` clause, **only 4 of which trip the guard**; they were already client language, and every *Delivers* line is built from one. Seeded mechanically from the `As a / I want / So that` triads, then hand-edited — the seeds were a starting point, not the deliverable.<br><br>**Two presentation rules are enforced in code rather than trusted to the author.** Effort is converted to **days** at write time and hours never appear — hours invite a rate conversation. The **unit rate card is never exported**: per-stage and per-item totals are fine, *what a screen costs* is internal pricing mechanics. A small `plain()` pass rewrites internal shorthand in *parsed commentary only* (`Q4` → *the fourth quarter*), never in a figure.<br><br>**Four fatal guards**, coverage and drift keyed on the plan's work items and phases. `LEAKS` gains two patterns over the questions workbook — **library names** and **code identifiers** (anything ending `Service`, `Controller`, `Repository`, `Component`, `Hub`, `Dto`, `Handler`) — chosen because they are what survives a careless content edit. **Verified by deliberate breach**, twice: a repository path, a requirement id, a backlog id, an endpoint, a service name, a library name and a table name were injected into content fields; all seven were named with sheet and cell coordinates, the workbook was deleted and the build exited 1.<br><br>**Three defects found and fixed during verification, all of the same class — parsed structure taken at face value.** The staffed-plan tables carry a **totals row with no sprint label**, which was being counted as a sprint and made the schedule sheet contradict its own headline (11/8/6 sprints against 10/7/5); it is now detected and labelled *Total*. Sprint dates carry **the year on the closing row only**, which is fine read top to bottom and wrong on a milestone sheet where *08 Jan* stands alone and is not 2026; the year is recovered by rolling over when the month goes backwards. And **60 of 107 `Reference title` fields were hand-typed paraphrases**, which the drift guard rejected outright — they are now synced verbatim from the plan, since a paraphrased mirror checks nothing. |

---

## [MVP-1/ProjectPlan/Tools/DevelopmentPlanContent.md](MVP-1/ProjectPlan/Tools/DevelopmentPlanContent.md)

| Date | Changed By | Description |
|------|-----------|-------------|
| Aug 13, 2026 | Plan team | **Created — the client-facing wording of the development plan, and its only home.** Five parts: 15 stages, 107 work items, 4 date levers, 11 milestones and dependencies, 10 assumptions and risks. **Nothing in it is citable as a requirement or a commitment.** It holds prose only — every figure the workbook shows is parsed from the plan of record, so the two cannot drift.<br><br>**107, not 116:** nine backlog stories are QA- or BA-only and carry no build hours. They are not development work and are excluded rather than silently absorbed, which is also why the workbook's effort total is the development figure and not the programme figure.<br><br>**`Reference title` is not authored** — it mirrors the plan's own title so the drift guard can tell a renumbering has not slid prose onto a different work item, and it is never written to the workbook. **`Deferrable` and `Covers` are the only two derived fields**, added so the *can it be deferred* column and the milestone dates come from the content rather than being hard-coded in the generator. |

---

## [MVP-1/ProjectPlan/Tools/build_trial_run_xlsx.py](MVP-1/ProjectPlan/Tools/build_trial_run_xlsx.py)

*The cross-document pass that produced this script is recorded once under [Repository-wide](#repository-wide). This section carries only what is about the script itself.*

| Date | Changed By | Description |
|------|-----------|-------------|
| Aug 14, 2026 | Plan team | **Created** — the trial-run workbook builder, writing ten sheets from `[TRP]` + `[TB]` + `TrialRunContent.md`. **61 work items, 778 hours**, every figure parsed rather than typed.<br><br>**Three parser problems worth recording, because each is the kind of thing that returns.** A helper that found *"the first table under a heading"* could not address the plan's sections that carry **two or three tables before their first sub-heading** — §1.3 alone has three — so it was rewritten to return the **nth** table, bounded by heading depth, with an `expect` assertion matched against the **whole header row** rather than the first cell, since several of the plan's tables deliberately leave the first column heading blank. **Footer rows are the second trap and they cut both ways**: the block table's footer restates the grand total and must be dropped or the sum double-counts to 1,556; the three-bases footer carries the totals the guards check and must be kept — dropping it made `1C` look like the total and raised a bare `StopIteration`. Both are now filtered on a *data* condition rather than on a blank first cell. Third, the plan's **final sprint carries em dashes** for capacity and utilisation because it is testing and sign-off with no development hours — a deliberate dash is not a parse failure and now defaults to zero.<br><br>**The expander and the guard must agree on their word boundary, and for one build they did not.** The expander used `[\w-]`, the guard `\b`, so a hyphen-joined compound like `DB3-FL2` was **flagged but never expanded** — an unfixable failure from the user's side. The expander now uses `\w`, matching the guard. Code identifiers stay safe because they join on word characters and underscores, never hyphens: `PLCTagService`, `FlatWireDbContext` and `sp_GetGaugeTrace` are all untouched, and no hyphen-delimited segment of `run-status-cards` or `chart-tab-strip` is an abbreviation. |

---

## [MVP-1/ProjectPlan/Tools/TrialRunContent.md](MVP-1/ProjectPlan/Tools/TrialRunContent.md)

| Date | Changed By | Description |
|------|-----------|-------------|
| Aug 14, 2026 | Plan team | **Created — the plain-language wording of the trial-run workbook, and its only home.** Four parts: the Read Me narrative, the glossary, **61 work-item entries** and five blocker entries. **Nothing in it is citable as a requirement or a commitment** — it holds prose only, and every figure the workbook shows is parsed from `[TRP]`.<br><br>Each work item carries a **`Work item`** name and a **`Delivers`** line written about the business outcome rather than the implementation, drawn from each story's `So that` clause in `[TB]`. **`Reference title` is not authored** — it mirrors the backlog's own title verbatim so the drift guard can tell that a renumbering has not slid prose onto a different item, and it is never written to the workbook.<br><br>⚠ **Write no abbreviations here.** The workbook expands them at build time and a fatal guard refuses the build if a bare one reaches a cell, so an abbreviation in this file surfaces as a failed build rather than a bad sheet. **The `## Glossary` section is the single exception**, because mapping them is its whole job — and it is the one sheet the guard exempts. |

---

## [MVP-1/ProjectPlan/Tools/build_coverage_matrix.py](MVP-1/ProjectPlan/Tools/build_coverage_matrix.py)

| Date | Changed By | Description |
|------|-----------|-------------|
| Aug 13, 2026 | Gap remediation | **Created — requirement coverage is now measured, not asserted.** Parses the requirement rows of `02-SRS.md` and the case rows of `06-TestPlanAndTestCases.md` and **exits non-zero** if any MVP-1 requirement has neither a test case nor a §10.4 entry. Written to the same shape as the coverage guard in `build_questions_xlsx.py`. Three precedence levels count as covered — **direct** (a §5 case names the requirement in its *FR / Source* column, that column and not the whole row), **indirect** (the SRS `NFR` table names both the requirement and a `TC-###`), **declared** (§10.4 records it with a reason) — on the principle that *a declared hole is a decision and an undeclared one is an accident*, and only the second fails the build. Withdrawn requirements (`~~**FR-###**~~`, four of them) are excluded; **`FR-111` is one, and counting it is how a false positive enters**. Carries an explicit non-goal: `05-SprintPlanAndBacklog.md` §11 has the same range granularity but **no per-requirement data to check against**, so its story assignments are reported as an advisory count that never affects exit status. |

---

## [REVIEW.md](MVP-1/ProjectPlan/Development/REVIEW.md) — *Appendix, WeldEventPopupPlan*

| Date | Change |
|---|---|
| Aug 13, 2026 | **Reduced to a record — the executed change-plan body was removed (4,544 to 910 words).** The plan was applied 1 Aug 2026 with `S7` closing 13 Aug, so its design options, work breakdown and sequencing were instructions for work already done, and leaving them invited re-application. **Deleting the file was rejected:** six references cite it — `APIContracts.md` for decision `D-A`, `REVIEW.md` #62, and gaps **G25**, **G26** and **G28** in `back-matter.md`, which cite it by section and question id. What is retained is exactly what they cite: the `D-A` merge (`POST /weldevent` as the single weld write), the `Q-W1`–`Q-W4` dispositions and the `G25`/`G26` outcomes including the `S7` omission. **Verified on removal that `S1`–`S9` all landed** — including `S6`, `S8` and `S9`, which the plan's own table had left unstruck: `phase-06` carries the dialog, merged write and DB2A entry points, `FW-063` is retitled *Weld Event capture — DB2A dialog*, and `TC-068` was rewritten with `TC-068a`–`TC-068i` added. **One live open question was nearly lost and is now flagged in place: `Q-W2`** (is the post-staging weld offer gated to the sub-3,000 lb window, or shown on every successful staging?) is `Medium` and **exists nowhere else in the repository** — the plan said its `Q-W#` items would be added to `Analysis/FlatWireOpenQuestions.md` but only `Q-W1` and `Q-W4` were ever propagated. It needs registering there; flagged rather than done, that register being outside this clean-up's folder. Full text recoverable at `1964086`. |
| Aug 1, 2026 | Initial plan: retire Mark as Welded, move the weld action onto the payoff card, convert Dashboard 4 to a dialog, offer the weld log after pre-check-in. Four open questions (Q-W1–Q-W4) and two gaps (G25, G26) raised. |
| Aug 1, 2026 | **D-C built; D-D overtaken.** *Mark as welded* moved onto the staged bay card exactly as D-C recommended, rule and tooltips intact. **D-D was overtaken rather than implemented** — it proposed keeping the weld-readiness strip's narrative and dropping only its buttons; in the event the whole 96px band was removed, because the narrative turned out to be duplicated by the bay cards in every branch but one (*induction-weld tail to head*, which moved into the staged card's alert). *Welds this run* went onto the **active** card, which D-D could not have anticipated since it assumed a surviving strip; the consequence is that at cold start the control is **absent rather than disabled**, superseding **TC-068e** and raising **Q83**. Additionally, and proposed by no section of this plan: ***Open active run* was removed** from the active card (`FR-051b`), and Dashboard 2's *Acknowledge & Begin Check-in* now returns to Dashboard 2A rather than Dashboard 3 (`FR-079a`, **Q84**). New requirement IDs `FR-050a`, `FR-051a`, `FR-051b`, `FR-079a`; new test cases TC-068f/g/h/i and TC-079a. |
| Aug 13, 2026 | **Step S7 closed and the stale gap-id instruction retired.** §5.3's **S7** (*"Add G25 and G26 to `back-matter.md`"*) was the one step of this plan never executed: G25 was withdrawn correctly, **G26 was never added**, and `phase-06:45` cited it against nothing for twelve days. Both are now in the register and S7 is struck as done. §4's note *"Do not register G25; the next free gap ID remains G25"* is struck — the id **was** reused on 13 Aug 2026 for the requirement-coverage gap — with a pointer that any `G25` citation dated on or before 12 Aug 2026 means this weld-history row instead. |
---

## [MVP-1/ProjectPlan/Development/YieldCostAndScrapStories.md](MVP-1/ProjectPlan/Development/YieldCostAndScrapStories.md)

| Date | Changed By | Description |
|------|-----------|-------------|
| Aug 13, 2026 | Plan team | **Initial publication — Phase 12's stories split out of [05-SprintPlanAndBacklog.md](MVP-1/ProjectPlan/Development/SprintPlan.md).** Holds **Epic 10** (`FW-100`, `FW-101`, `FW-102`) and **Epic 11** (`FW-110`) — **4 stories / 11 points** — mirroring the effort split into [`YieldCostAndScrapSheet.md`](MVP-1/ProjectPlan/Development/YieldCostAndScrapSheet.md) the same day. Adds a story inventory mapping each story to its **descope rung** with all-in and development hours on both delivery bases, whose per-story dev split (**24/36/20/48** hand-coded → **16/25/15/33** AI) reconciles cell-for-cell against that sheet's rung table. Adds a points cross-check (**8.1 dev-h per point** AI-assisted, 11.6 hand-coded, 11.5 / 16.1 all-in) with an explicit warning **not** to read the closeness to the conventional ~8 h/point as validation, since the points are April-dated and the factors are assumed. **The document carries a §3 stating that it is itself the only requirement text for three of its four stories** — `FW-101`, `FW-102` and `FW-110` have no `FR-` IDs and Phase 12 has no owning specification, so the acceptance criteria are load-bearing in a way no other story file's are. **Two defects raised on `FW-101`. (1)** Its dependencies are `FW-063, FW-090` here and **`FW-095`** in `ProjectPlan/05-SprintPlanAndBacklog.md`; both cannot be right, on a story that is critical-path for the welding-wire certificates. **(2)** Its last two acceptance criteria — the *"Flat Wire"* checkbox and the three field renames — **belong to a different descope rung**: the ladder puts the yield form in **rung 4** while those criteria are written into `FW-101`, which is **rung 3**, so deferring rung 3 as intended would defer two criteria the ladder believes it keeps. Effort is unaffected (same phase); the **descope decision** is not. Three editorial fixes while transcribing: `FW-102`'s duplicated `OI-68` citations consolidated (its AC read *"after OI-68 and OI-68 are decided"*), `FW-110`'s duplicated `OI-83` likewise, and an **`Open Questions: OI-60`** line **added** to `FW-101`, which had none despite `phase-12` listing `OI-60` as one of its blockers. The two stale April epic priorities (*"required by August 1 production"*) are struck rather than deleted. |

---

## [MVP-1/ProjectPlan/YieldCostAndScrapSheet.md](MVP-1/ProjectPlan/Development/YieldCostAndScrapSheet.md)

| Date | Changed By | Description |
|------|-----------|-------------|
| Aug 13, 2026 | Plan team | **§4 gained a callout: rungs 3 and 4 disagree about who owns the yield form.** The ladder labels rung 4 *"footage-based weight + yield form"*, but the *"Flat Wire"* checkbox and the three field renames are written into **`FW-101`'s** acceptance criteria, and `FW-101` is **rung 3** — so deferring rung 3 as the ladder intends would defer two criteria the ladder believes it is keeping. This sheet **continues to price the ladder's split** (rung 3 carries no FE; rung 4 carries the 20 FE for the form), which is the right basis for effort either way since both rungs sit in the same phase and the total is unaffected — it is the **descope decision** that breaks, so the note is filed in §4 rather than §3. No figure changed. Surfaced while splitting the stories into [`YieldCostAndScrapJiraStories.md`](MVP-1/ProjectPlan/Development/YieldCostAndScrapStories.md), which carries the full statement on `FW-101`. |
| Aug 13, 2026 | Plan team | **Promoted to the sole home of Phase 12's figures.** Phase 12 was **removed from [`DevelopmentEffortModel.md`](MVP-1/ProjectPlan/Development/DevelopmentEffortModel.md)** the same day, so the rounding note no longer defers to that sheet's §2 for the 89 h development aggregate — it states which split to quote for which reconciliation (the aggregate FE 27 / BE 54 / DB 8 / RT 0 against the rate card; the rung table against the ladder) and records that the dev sheet now cites 89 h from here rather than the reverse, with its MVP-1 figure re-based to **1,397 h** and `1,397 + 89 = 1,486 h` being complete MVP-1 development. The *Related Documents* row for the dev sheet was rewritten to say its per-phase table **excludes** Phase 12. No figure in this sheet changed. |
| Aug 13, 2026 | Plan team | **Initial publication — a standalone sheet for Phase 12 (Yield, Cost Ledger & Scrap).** Decomposes the phase's published **177 h** to **story level** on both delivery bases, using the descope ladder's own **33 / 49 / 28 / 67** split — which is why the four rungs reconcile to the phase exactly. **Both effort tables close in both directions**: the rung totals equal the ladder's published figures and the five discipline columns equal the phase row's published 44 / 72 / 12 / 26 / 23. Adds the AI-assisted view at **126 h** (28.8% saved, the **second-least-compressible phase** after Phase 14) from `DevelopmentEffortModel.md` §1, noting that its BE **0.75** is the least favourable BE factor in the plan because yield attribution and cost standards are domain reasoning, not boilerplate — Phase 14 resists AI assistance because it needs the mill, Phase 12 because it needs **decisions**. **Two findings that should change how the phase is scheduled, not just how it is sized. (1) Three of its four stories have no requirement specification at all** — `FW-101`, `FW-102` and `FW-110` carry **no `FR-` IDs anywhere in `02-SRS.md`**, and Phase 12 has **no owning requirement document**, being absent from both `MVP-1/RequirementDocuments/` and the Dashboard Inventory; the only requirement text in existence is the Jira acceptance criteria, so the 177 h was priced against four cards. Verified, not assumed — and the scrap requirements that *do* exist (`FR-063`, `FR-066`, `FR-189`, `FR-273`, `FR-292`) govern the **check-in scrap box** and **WIP dispositions**, **not** FW-110's outlet selection, so the phase must not be read as specified because the word "scrap" appears in the SRS. `FW-100`'s formula *is* specified (`FR-332`/`FR-332a`) but under §5.16, which the SRS maps to **DB7 / Phase 9**. **(2) All four stories are blocked, and three blockers are the client's** — `Q10` (Critical, and the one register question deliberately carrying no recommendation), `OI-60`, `OI-68`, `OI-83`. Separates **what is buildable today** (FW-001 field renames, the weight-service structure, the settled `Scrap Box`/`Scrap Skid` vocabulary, the attribution mechanics) from **what no factor makes buildable** (cost-standard values, expected yield per route, the confirmed weight basis). Records that **AI assistance weakens this ladder** — the four rungs recover 126 h instead of 177 h, removing **5.3 FTE from W7 rather than 7.4**. Flags a contradiction to resolve while writing the missing spec: `FW-101`'s dependencies are `FW-063, FW-090` in `FlatWireJiraStories.md` and `FW-095` in `05-SprintPlanAndBacklog.md`. A decompose-then-round artifact is **stated rather than hidden** — FE 26 / DB 9 here against the aggregate's FE 27 / DB 8, differing by 1 h in opposite directions and therefore agreeing on both totals that matter (126 h all-in, 89 h development-only). |

---

## [MVP-1/ProjectPlan/00-README.md](MVP-2/ProjectPlan/00-README.md)

| Date | Changed By | Description |
|------|-----------|-------------|
| Jul 30, 2026 | Plan team | Created as the index to the seven-document project-plan set. Records the read order, the authority map, the precedence chain, the five most-often-misread facts, the four new `PP-##` findings, and what the set deliberately leaves unresolved. |
| Aug 1, 2026 | Client sync | Added §5a recording the seven changes the 30 Jul client call made across this set, three of which **reverse** decisions the set documents, plus the four new open questions. |
| Aug 4, 2026 | Client direction | §3 authority table gains **`[PLC]`** — *"What tag is written to the machine or read from it, and when?"* — pointing at `RequirementDocuments/PLCTagSpecification.md`. **The set is still seven documents:** `[PLC]` is a client-facing companion with its own sign-off sheet and `PLC-Q##` register, not an eighth member, and nothing is renumbered. |

---

## [MVP-1/ProjectPlan/01-VisionAndScope.md](MVP-1/ProjectPlan/Business/VisionAndScope.md)

| Date | Changed By | Description |
|------|-----------|-------------|
| Jul 30, 2026 | Plan team | Initial publication. Vision & Scope derived from `FlatWire_MasterSpecification.md` §1/§2/§9.6/§10/§11, the Capacity & Effort Model and the gaps register. Carries the capacity finding (RISK-01/G1/OI-51) unsoftened, the in/out-of-scope split with consumed interfaces, eleven measurable success criteria written for `[TP]` and `[DR]` to gate on, twelve risks and the Critical open-item tier. Opens the `PP-##` series with the index-count divergence found by counting the DDL. |
| Aug 4, 2026 | Client direction | **HMI/SCADA descoped.** Dashboard 13 (HMI Line Schematic), Dashboard 14 (SCADA Trends) and the Machine View tab on the active run monitor are **withdrawn at client request**. `FR-111`, `FR-112`, `FR-114`, `FR-425`, `FR-440`–`FR-451` and `FR-460`–`FR-470` are marked **withdrawn** — numbers retained, never renumbered — and `FR-113` **reworded**, since it asserted a rule about "the active tab" that outlives the tabs. Both mockups and `HMIAndSCADALayout.md` are deleted. **Descope-ladder rung 7 is removed entirely:** its 67 h stops being *recoverable* effort and becomes *never-planned*, so Phase 5 drops 221 → ~154 h and the programme 3,727 → ~3,660 h, but the ladder loses its largest optional rung and Phase 5 is no longer deferrable. **Nothing structural was removed:** all six run event markers still land on the DB3 traces and no hub event, endpoint, table or column is deleted — every DB13/DB14 reference in the real-time and tag tables was a *consumer* entry, not a row. `Q51`/`OQ-51` is **superseded**, because Dashboard 14 was its answer. In this document: DB13 and DB14 removed from the in-scope dashboard list. |

---

## [MVP-1/ProjectPlan/02-SRS.md](MVP-1/ProjectPlan/Business/BusinessRequirements.md)

| Date | Changed By | Description |
|------|-----------|-------------|
| Aug 2, 2026 | Plan team | Adds **§5.3a — Spool Queue (Dashboard 5A)** with `FR-097`–`FR-099`, taking the total to **366**. A new screen, numbered into the gap between §5.3 and §5.4 rather than appended, so it sits beside the check-in screen it feeds. It exists because FL2 had no view of waiting material (FL1 has DB2A; `PCI002` excludes FL2 from staging) and because `FR-090`'s *scan the label* and **Q27**'s *select it by spool number* both stand while only the first had a screen. It is also the first thing actually named "the spool queue", a phrase `FR-326` and `TC-389` use with nothing behind it. Screen inventory, navigation map and both traceability tables updated; `DB1 --> DB5` becomes `DB1 --> DB5A --> DB5`. |
| Jul 30, 2026 | Plan team | Initial publication. Carries all **363 functional requirements** (`FR-001`–`FR-508`) forward from `FlatWire_MasterSpecification.md` §4 with numbers unchanged, in the same 25 operator-workflow groups, each with screen, source IDs, actors, preconditions, priority, validation, actions, state changes, error paths and real-time events. **NFRs are folded in** — inline `[NFR]` rows in the constraining group plus the §6 register of the ten NFRs that are actually cited, with measurable targets and `TC-###` verification. Adds the domain model with mermaid state machines, the process flows, the UI specification, the role × capability matrix, the external-interface surfaces and a full traceability appendix. Records two new findings: **PP-02** (`NFR001`/`NFR002`/`NFR008` cited nowhere) and **PP-03** (the OEE dashboard has no owning story or phase). |
| Aug 1, 2026 | Client sync (30 Jul call) | **Eight requirements changed, seven added; no existing number reused or renumbered.** FR-045/046/047: **off-schedule is no longer a deviation** — the system auto-selects the correct station, and the override columns are dropped (OQ-38). FR-048: staging **no longer sets `coils.coil_status`** (OQ-68). FR-042/FR-065: diameter validates against a **min/max band**, unseeded pending the values owed by e-mail (OQ-35). FR-044: flagged **knowingly wrong** for a multi-order rod (OQ-70 / G22). Added **FR-052a** (pre-check-out approval depends on the weld; welded needs a supervisor and goes to HOLD), **FR-053a** (a WIP rejection releases a blocked bay — the only thing that does), and **FR-130a–d** (customer min/max weight basis; short close as an unplanned stop on the 10-90 pattern; the spool is run off regardless because FL2 has no stripper; a mid-run coil break restarts the stop from zero). The domain-model note on pre-check-in coil status is **resolved** in favour of `RECEIVED → STAGED`. |
| 1 Aug 2026 | Build decision | **DB6, DB8 and DC are dialogs, not screens.** SPC checkpoint, WIP rejection and die change moved out of `dashboard_6_spc_checkpoint.html` / `dashboard_8_wip_rejection.html` / `dashboard_die_change.html` into `spc_checkpoint.js` / `wip_rejection.js` / `die_change.js`, opened as popups over the screen the operator is already on; the three `.html` files remain as launchers so every reference to them still resolves. **No requirement text changed** — the requirements are unaffected by the container. What changed is that each dialog now receives its material context from the caller instead of hard-coding one run, which is what finally lets the pre-check-in rejection path (Q36 item 3) be represented: no run, no footage position, and submitting releases the bay. Two hand-offs the spec already described also become real — a gauge-drift or size-change die change opens the SPC checkpoint it mandates, and an out-of-spec checkpoint's *suspend material* opens the WIP rejection with the failing reading carried over. DB6 gains a **read-only mode** for DB1's "SPC · Last check … · View →", which reviews a recorded checkpoint rather than opening a blank form. New open item **Q43**: DB1's "WIP Rejection**s**" nav item reads as a list screen that has never been specified. |
| 1 Aug 2026 | Build decision | **Pause/Resume redesigned; DB12 becomes a dialog; OI-14 closed at four outcomes.** The fifteen pause reasons were a flat radio list ~1100px tall — the last screen in the suite on radio buttons, and tall enough that the dialog was scaled on every window, taking its 14px labels under the shopfloor floor. They are now glove-sized cards in one column per category and the dialog fits the 1280×1024 panel at 1:1. **Rod Checkout is no longer a pause reason** — it was the only one of fifteen that did not pause, rendered identically to the fourteen that did — and is now the `CheckOutRod` **resume outcome**, which `POST /run/{runId}/resume` and `CK_RunPauseEvent_Outcome` already accepted. That closes **OI-14** and supersedes **FR-262**. **Rod Checkout itself (DB12) moved into `rod_checkout.js`**, so Mode B opens over the pause that raised it with the frozen footage carried over rather than navigating away and losing the pause. Four correctness fixes went with the redesign, all of which the mockup could previously get wrong: the payload now carries **`ReasonCode` + `ReasonCategory`** rather than a display label (`Other` used to overwrite its own code with the note text); **notes are mandatory on `Other`**, matching `CK_RunPauseEvent_NotesOther`; footage comes from the caller rather than a `#footage-val` element that **does not exist on the FL1 monitor**, where the dialog had always shown “Footage —”; and duration reads **h:mm:ss** past an hour instead of reporting a 90-minute stop as “90:00”. New requirements `FR-261a`–`FR-261b` and `FR-266a`–`FR-266b` record the rules; no existing number was reused. |
| Aug 4, 2026 | Client direction | **FM2 roller-size correction.** FM2 has **three** stands — **S1 = 8″, S2 = 6″, S3 = 6″** — with edgers at S2 and S3 and S3 final. The 8″ roller **is S1**: §3.1's *"three 6″ stands"* (from the May 21 note) had been read as a separate 8″ roller upstream of three 6″ stands, i.e. four components, and `FM2_6inS3` never corresponded to real equipment. **§3.1 rewritten** with the per-stand table; **§3.2 component vocabulary** becomes position-only `FM2_S1` / `FM2_S2` / `FM2_S3`, with roll diameter moved to the new `Stand.RollDiameterIn` column (12.000 / 8.000 / 6.000 / 6.000) so diameter can never again hide inside an identifier. **`FR-094`, `FR-096`, `FR-297`, `FR-386`, `FR-387` reworded; no number reused.** `FR-387`'s multipliers are unchanged (1.06 / 1.02 / springback) — they move from diameter labels to positions, which fixes a pre-existing defect where three formulas covered four stands and the final stand had none. **`OI-04` closed:** SRS §2.7's *"6″ S3"* and the DDL/API/`HMI008` rule *"`FM2_6inS2` must always be Active"* named **the same physical stand**, now `FM2_S3` — the phantom fourth stand created the appearance of a contradiction. **`OI-36` closed:** the client's published tag map carries exactly three FM2 stations, so it was never incomplete; PLC stations are renamed to bare position (`FL2.FM2.S1/S2/S3`) pending sign-off as **`PLC-Q04`**. See decision **D-26** in `[MS §10.2]`. |
| Aug 4, 2026 | Client direction | **HMI/SCADA descoped.** Dashboard 13 (HMI Line Schematic), Dashboard 14 (SCADA Trends) and the Machine View tab on the active run monitor are **withdrawn at client request**. `FR-111`, `FR-112`, `FR-114`, `FR-425`, `FR-440`–`FR-451` and `FR-460`–`FR-470` are marked **withdrawn** — numbers retained, never renumbered — and `FR-113` **reworded**, since it asserted a rule about "the active tab" that outlives the tabs. Both mockups and `HMIAndSCADALayout.md` are deleted. **Descope-ladder rung 7 is removed entirely:** its 67 h stops being *recoverable* effort and becomes *never-planned*, so Phase 5 drops 221 → ~154 h and the programme 3,727 → ~3,660 h, but the ladder loses its largest optional rung and Phase 5 is no longer deferrable. **Nothing structural was removed:** all six run event markers still land on the DB3 traces and no hub event, endpoint, table or column is deleted — every DB13/DB14 reference in the real-time and tag tables was a *consumer* entry, not a row. `Q51`/`OQ-51` is **superseded**, because Dashboard 14 was its answer. §5.21 and §5.22 replaced with withdrawal notices recording what survives and where it went. **Two text fixes still owed here:** `FR-073`/`FR-074` describe the tag batch as *"a single transactional batch"* that *"shall be rolled back"* — machine writes are not transactional and the recovery is a **compensating re-clear** (gap **G16**, which closes on this edit); and `FR-073` says *speed **limits*** where the interface section said *speed **targets***, which is a real ambiguity asked as **`PLC-Q06`** and deliberately left unfixed until it closes. **PLC tag surface consolidated.** The surface existed in six partial, mutually contradictory copies; it now has one home in [`PLCTagSpecification.md`](MVP-1/ProjectPlan/Architecture/PLCTagSpecification.md) (client-facing, `[PLC]`, with its own `PLC-Q##` register) plus `MVP-1/DevelopmentPlan/PLCTagImplementation.md` (internal). This document’s tag-surface section is reduced to a pointer, keeping only what is genuinely its own job. |

---

## [MVP-1/ProjectPlan/03-HLD-and-ERDiagram.md](MVP-1/ProjectPlan/Architecture/Architecture.md)

| Date | Changed By | Description |
|------|-----------|-------------|
| Aug 6, 2026 | DB planning | **`CoilTraceability` gains `SpoolAlpha`** (`varchar(20)` NULL, FK → `Spool.Alpha`, filtered index on `IS NOT NULL`) — the coil → spool edge existed nowhere, leaving `FR-333`'s `rod → spool → coil` chain unsatisfiable. `RunId` cannot substitute: `SpoolCheckin.RunId` is non-unique and `CoilOutput.RunId` is many-per-run, so the join returns a set of spools. Placed on the range-grained row, not `CoilOutput`, so a spool running out mid-coil stays expressible; a separate `SpoolCoilMapping` junction table (the client's May 2026 design) was rejected as a footage-less duplicate. **Counts corrected to the as-built database while here — 27 tables, 43 FKs, 64 non-clustered indexes**; the previous 41/46 were a 30 Jul DDL count that had already drifted. Closes neither `FR-172` (singular `Spool.ParentRodAlpha`) nor `OI-25`. |
| Aug 6, 2026 | DB planning | **`Drawer` gains the two die-life columns** — `LastGrindingFeet DECIMAL(10,2)` NOT NULL DEFAULT 0 (feet run *since* the last grind, a resettable counter, **not** the reading at it) and `TotalFeetAllowed DECIMAL(10,2)` NULL (scheduled life; NULL until **OQ-83** supplies thresholds), with two CHECKs and deliberately **no** `LastGrindingFeet ≤ TotalFeetAllowed` rule, since *overdue* is a displayed state. Semantics from `DieChangeAndManagement.md` §4.2/§4.4; source is the `Drawer` sheet of `BaseDocuments/flatwire tables.xlsx`, a May 2026 design never built. **No change to table, FK or index counts** — two columns on an existing lookup. The §13.3 die-inventory gap row is **narrowed, not closed**: the counter and threshold now have a home, but against a die *size*, not a physical tool, so **OI-41** stands and Phase 6 still depends on Phase 13. |
| Jul 30, 2026 | Plan team | Initial publication. Architecture context and component views, the binding reference-code rules, backend layering and CQRS, the purpose-built real-time pipeline, the Angular library design, and the full data model. **Table count (27), FK count (41) and index count (46) were taken by counting the DDL, not quoted** — the index count corrects the master specification's 44 and is raised as **PP-01**. Publishes an ER overview plus five per-group detailed diagrams covering every table, the complete 41-FK list, the cross-database touchpoints, the PLC/OPC surface, the non-transactional check-in boundary with its compensating-write sequence, and seventeen architecture decisions with rationale. States the `Rod`-table resolution (**D-04**) explicitly and names the two stale documents that say the opposite. |
| Aug 1, 2026 | Client sync (30 Jul call) | **Schema section updated to the as-built DDL.** `RodStaging` loses `OffScheduleOverride` / `ScheduledLineId` and their two CHECK constraints — a rod booked on the other rod line now triggers an **automatic station switch** (OQ-38) — and gains `UnstageKind` / `WipRejectionId` so a **WIP rejection releases a blocked bay** (OQ-36 item 3). `AlloyProperty`'s two single-± tolerance columns become **four min/max pairs** (gauge, width, rod diameter, ovality), with diameter and ovality **NULL pending the values owed by e-mail** (OQ-35) — the `CHK007` missing-column note is resolved in shape but not in data. `coils.coil_status = INFLAT` is written at **check-in only** (OQ-68). Not shown in the ER diagram but added to the DDL: `RodCheckout` gains `WasWelded` and the supervisor approval stamp it never had (**G24**), enforced for Mode B and for a welded Mode P removal. Rebuild verified — `RunAll` clean and idempotent, 27 tables, 15 constraint tests pass. |
| Aug 4, 2026 | Client direction | **HMI/SCADA descoped.** Dashboard 13 (HMI Line Schematic), Dashboard 14 (SCADA Trends) and the Machine View tab on the active run monitor are **withdrawn at client request**. `FR-111`, `FR-112`, `FR-114`, `FR-425`, `FR-440`–`FR-451` and `FR-460`–`FR-470` are marked **withdrawn** — numbers retained, never renumbered — and `FR-113` **reworded**, since it asserted a rule about "the active tab" that outlives the tabs. Both mockups and `HMIAndSCADALayout.md` are deleted. **Descope-ladder rung 7 is removed entirely:** its 67 h stops being *recoverable* effort and becomes *never-planned*, so Phase 5 drops 221 → ~154 h and the programme 3,727 → ~3,660 h, but the ladder loses its largest optional rung and Phase 5 is no longer deferrable. **Nothing structural was removed:** all six run event markers still land on the DB3 traces and no hub event, endpoint, table or column is deleted — every DB13/DB14 reference in the real-time and tag tables was a *consumer* entry, not a row. `Q51`/`OQ-51` is **superseded**, because Dashboard 14 was its answer. The `SCADA multi-pen (DB14)` component row and the `/trends` route are deleted; `sp_GetGaugeTrace` is **unchanged** — it had three consumers and only one was removed. **PLC tag surface consolidated.** The surface existed in six partial, mutually contradictory copies; it now has one home in [`PLCTagSpecification.md`](MVP-1/ProjectPlan/Architecture/PLCTagSpecification.md) (client-facing, `[PLC]`, with its own `PLC-Q##` register) plus `MVP-1/DevelopmentPlan/PLCTagImplementation.md` (internal). This document’s tag-surface section is reduced to a pointer, keeping only what is genuinely its own job. §9.1 (the single-trigger rule) and §10 (the transactional boundary and its compensating-write sequence) are **kept here** — they are architecture, not tag surface, and §10 is the home of **G2**/**OI-39**. |

---

## [MVP-1/ProjectPlan/04-APIContract.md](MVP-1/ProjectPlan/Backend/APIs.md)

| Date | Changed By | Description |
|------|-----------|-------------|
| Jul 30, 2026 | Plan team | Initial publication. Conventions, canonical enums with the three-way mirroring rule, all **30 endpoints** with request/response shapes, validation, side effects, error codes and idempotency, the **`FlatWireHub` contract with all 10 events**, the PLC/OPC surface, the stub-first delivery contract, versioning policy and full traceability. **Corrects the four Tier-1 defects in the published April contract** — the `/passschedule/generate` worked example (`0.3732` / `0.95 %` / `Hybrid`, not `0.265` / `50.1` / `Standalone`), the missing `RollAdjustTrigger` checkpoint type, the component-state boolean, and the three edge-type vocabularies — plus two further corrections (the three `NOT NULL` fields missing from `POST /checkin/rod`, and the `ComponentName`/`PayoffPosition`/`LineState` enum fixes). Records **PP-04**: the hub event count is ten, not nine. |
| Aug 1, 2026 | Client sync (30 Jul call) | **`/staging/**`, `/checkin/rod`, `/checkout` and `/wipreject` updated.** The `offSchedule` override object is **removed** — a rod booked on the other rod line makes the client **switch station** (`scheduledLineId` from `GET /rod/{alpha}`), and a mismatched POST returns `409 WRONG_STATION` with `correctLineId` (Q38). Staging **no longer sets `coils.coil_status`** — `INFLAT` is set at check-in (Q68). A failed inspection now returns **`201` + `state:"Blocked"`** (the 31 Jul contract change, not previously carried here), and **`POST /wipreject` releases the blocked row** — the only thing that clears a `Blocked` bay (Q36 item 3). `POST /staging/rod/unstage` and `POST /checkout` Mode P gain a **weld-dependent supervisor approval** with `HOLD` (Q69/Q72), and `/checkout` gains the approval columns the table never had — retro-enforcing OQ-74 for Mode B (**G24**). `CHK007` becomes a **min/max band**, unseeded pending the values owed by e-mail (Q35). Flagged **G22** (order membership is knowingly wrong for a multi-order rod, Q70/Q73) and **Q40** (an order scheduled on neither rod line has no station to switch to). |
| Aug 4, 2026 | Client direction | **HMI/SCADA descoped.** Dashboard 13 (HMI Line Schematic), Dashboard 14 (SCADA Trends) and the Machine View tab on the active run monitor are **withdrawn at client request**. `FR-111`, `FR-112`, `FR-114`, `FR-425`, `FR-440`–`FR-451` and `FR-460`–`FR-470` are marked **withdrawn** — numbers retained, never renumbered — and `FR-113` **reworded**, since it asserted a rule about "the active tab" that outlives the tabs. Both mockups and `HMIAndSCADALayout.md` are deleted. **Descope-ladder rung 7 is removed entirely:** its 67 h stops being *recoverable* effort and becomes *never-planned*, so Phase 5 drops 221 → ~154 h and the programme 3,727 → ~3,660 h, but the ladder loses its largest optional rung and Phase 5 is no longer deferrable. **Nothing structural was removed:** all six run event markers still land on the DB3 traces and no hub event, endpoint, table or column is deleted — every DB13/DB14 reference in the real-time and tag tables was a *consumer* entry, not a row. `Q51`/`OQ-51` is **superseded**, because Dashboard 14 was its answer. **Zero endpoints and zero hub events deleted** — consumer columns only, and §5.4 renamed from *SCADA event markers* to *Run event markers*. **PLC tag surface consolidated.** The surface existed in six partial, mutually contradictory copies; it now has one home in [`PLCTagSpecification.md`](MVP-1/ProjectPlan/Architecture/PLCTagSpecification.md) (client-facing, `[PLC]`, with its own `PLC-Q##` register) plus `MVP-1/DevelopmentPlan/PLCTagImplementation.md` (internal). This document’s tag-surface section is reduced to a pointer, keeping only what is genuinely its own job. The `PLCTagService` signatures and the two error codes stay here — that is the API contract’s job. Also renamed for clarity: the state enum becomes **`LineOperatingState`** and the hub event **`LineStateChanged`**, so **`LineState`** unambiguously means the machine tag (`[PLC §6]`). |

---

## [MVP-1/ProjectPlan/05-SprintPlanAndBacklog.md](MVP-1/ProjectPlan/Development/SprintPlan.md)

| Date | Changed By | Description |
|------|-----------|-------------|
| Jul 30, 2026 | Plan team | Initial publication. Leads with the **capacity reality check** — 3,727 h against 44 working days, 10.6 FTE sustained, an impossible 27.2-FTE W7 and a 12 % descope ceiling — and states the three programme options with their dates, unsoftened. Publishes the 14-phase delivery model, the six delivery streams with an **unfilled roster**, the unit-rate card, a five-sprint calendar mapped to the authoritative week grid with milestone and QA gates, the phase table with per-phase blockers, the dependency chain with **two corrections to the published graph**, all 12 epics and 44 shopfloor stories with Given/When/Then acceptance criteria, the ordered descope ladder with named sign-offs, Definition of Ready / Done, the risk and gaps registers, and a full `FR-###` → story coverage matrix. Adds **12 `[NEW]` stories** for work the specification requires that no existing story covered — including the Angular and .NET scaffolds absent from epic E01, the pre-check-in station, spool completion, the alert lifecycle, the die master and the stop-transaction popup — each flagged with its rationale rather than folded silently into an existing story. |
| Aug 1, 2026 | Client sync (30 Jul call) | **A new MVP2 candidate, and one scope reduction.** **Multi-order rod (OQ-70/OQ-73) is a candidate for MVP2** — the client confirmed a single rod may carry more than one production order, but the sequencing rule is unanswered (Srikanth is checking his notes) and the deferral itself is still to be confirmed with him. Until it closes, Phase 4's staging validation ships a **knowingly wrong** order-membership refusal (**G22**) — that is a recorded decision, not an oversight. **Scope reduction:** the off-schedule supervisor override is **removed** from Phase 4 (OQ-38) — a rod booked on the other rod line now triggers an automatic station switch, which is less work than the override panel, the five columns and the two constraints it replaces. Offsetting that: Phase 4 gains the auto-switch behaviour on **two** screens (DB2A and DB2), Phase 7 gains the welded pre-check-out approval path, and Phase 8/9 gain the short-close unplanned-stop flow (FR-130a–d). **Blocked on the client:** the four tolerance values owed by e-mail gate `CHK007` in Phase 4, and the panel-resolution answer (**OQ-41**) gates the Phase-1A canvas against the 14 Aug gate. |
| Aug 4, 2026 | Client direction | **HMI/SCADA descoped.** Dashboard 13 (HMI Line Schematic), Dashboard 14 (SCADA Trends) and the Machine View tab on the active run monitor are **withdrawn at client request**. `FR-111`, `FR-112`, `FR-114`, `FR-425`, `FR-440`–`FR-451` and `FR-460`–`FR-470` are marked **withdrawn** — numbers retained, never renumbered — and `FR-113` **reworded**, since it asserted a rule about "the active tab" that outlives the tabs. Both mockups and `HMIAndSCADALayout.md` are deleted. **Descope-ladder rung 7 is removed entirely:** its 67 h stops being *recoverable* effort and becomes *never-planned*, so Phase 5 drops 221 → ~154 h and the programme 3,727 → ~3,660 h, but the ladder loses its largest optional rung and Phase 5 is no longer deferrable. **Nothing structural was removed:** all six run event markers still land on the DB3 traces and no hub event, endpoint, table or column is deleted — every DB13/DB14 reference in the real-time and tag tables was a *consumer* entry, not a row. `Q51`/`OQ-51` is **superseded**, because Dashboard 14 was its answer. **FW-062 keeps its 8 points** — DB13, DB14 and the Machine View had no acceptance criteria of their own in it, and no `FW-###` was ever dedicated to them. The two traceability rows and the `FR-440`–`FR-470` range row are marked withdrawn, and the W4 hours drop from 476 to 409. |

---

## [MVP-1/ProjectPlan/06-TestPlanAndTestCases.md](MVP-1/ProjectPlan/Testing/TestStrategy.md)

| Date | Changed By | Description |
|------|-----------|-------------|
| Jul 30, 2026 | Plan team | Initial publication. Test strategy mapped onto the UAL stack (Jest at 95 %, xUnit + Moq, integration against a seeded `FlatWireDB`, contract tests including the three-way enum mirror, real-time/load, PLC commissioning, UAT), risk-based prioritisation naming seven P1 areas, environments and the SQLCMD build/teardown procedure, entry/exit criteria per level and per gate, and a catalogue of **~250 test cases** grouped to mirror `[SRS §5]` — covering happy path, boundary, negative, permission and real-time behaviour for every group, with the mandatory areas (PLC tag push and the non-dismissible override, all five ITInhibit conditions, weld genealogy end to end, MMS ID lifecycle, the three checkout modes and carry-forward, FL2's `null` trace, FL3 hybrid, recording cadence, reconnect, buildup alerts, SPC and SPC-HOLD, WIP rejection, the role matrix and audit) written explicitly. NFR verification carries measurable targets and **marks four real-time NFRs untestable-until-defined without inventing thresholds** — recording that the QA2 load test as scheduled cannot fail. Adds the UAT scenario scripts in operator language, the PLC commissioning sequence with safety preconditions and abort criteria, defect severity and release gates, and a bidirectional coverage matrix with an explicit list of what is **not** covered and why. |
| Aug 1, 2026 | Client sync (30 Jul call) | **TC-054 rewritten and four cases added.** Off-schedule staging is no longer authorised-and-recorded — the station **auto-switches**, so the old expected result (`OffScheduleOverride=1`, `ScheduledLineId` persisted) asserts columns that no longer exist. Added **TC-054a** (server `409 WRONG_STATION` for a stale client), **TC-054b** (welded pre-check-out needs a supervisor and ends on `HOLD`), **TC-054c** (a WIP rejection clears a blocked bay — the only thing that does), **TC-054d** (asymmetric min/max diameter band, **blocked until the tolerance values arrive**). TC-167's worked example moves off the withdrawn 2,000 lb target to the customer range. |
| Aug 4, 2026 | Client direction | **HMI/SCADA descoped.** Dashboard 13 (HMI Line Schematic), Dashboard 14 (SCADA Trends) and the Machine View tab on the active run monitor are **withdrawn at client request**. `FR-111`, `FR-112`, `FR-114`, `FR-425`, `FR-440`–`FR-451` and `FR-460`–`FR-470` are marked **withdrawn** — numbers retained, never renumbered — and `FR-113` **reworded**, since it asserted a rule about "the active tab" that outlives the tabs. Both mockups and `HMIAndSCADALayout.md` are deleted. **Descope-ladder rung 7 is removed entirely:** its 67 h stops being *recoverable* effort and becomes *never-planned*, so Phase 5 drops 221 → ~154 h and the programme 3,727 → ~3,660 h, but the ladder loses its largest optional rung and Phase 5 is no longer deferrable. **Nothing structural was removed:** all six run event markers still land on the DB3 traces and no hub event, endpoint, table or column is deleted — every DB13/DB14 reference in the real-time and tag tables was a *consumer* entry, not a row. `Q51`/`OQ-51` is **superseded**, because Dashboard 14 was its answer. `TC-138`, `TC-148`, `TC-503` and `TC-515`–`TC-534` are withdrawn (numbers retained); **`TC-139` is reworded** rather than withdrawn, because the always-visible rule survives the tabs. Two anchors moved rather than lapsed: **`NFR005`** loses `TC-534` and is still verified by `TC-604`/`TC-605`, and **`TC-531`** tested SPC control-limit methodology rather than the chart, which still applies to the SPC checkpoint and the gauge-trace report. §8 gains a note that **C1–C11 is reproduced in `[PLC §12]`** for the client while this document stays authoritative for the pass criteria — and that **`C5` needs a step recording which controller(s) were written**, since as written it cannot distinguish the one- and two-controller FL3 topologies (**`PLC-Q08`** / **G30**). |
| Aug 4, 2026 | Client direction | **`ITInhibit` confirmed line-scoped — `TC-017a` added and `C7` tightened.** The client confirmed one interlock tag per line, so **a blocked line blocks only itself**; the plant-level reading, in which an idle FL1 would have stopped a scheduled FL2, is excluded (`Q84`, `[PLC §8.1]`, decision **D15**). `TC-011`–`TC-017` were all single-line and would have passed under either scoping, so **new `TC-017a`** asserts the isolation directly: FL2 running, each of FL1's five conditions set in turn, FL2 unaffected throughout. **`C7` gains the same second line** — as written it exercised one line and could not have detected a plant-level tag. The P1 risk row for `ITInhibit` now names line-scoping alongside the five conditions. **No requirement text changes:** `FR-008`–`FR-010` and `FR-020` were always silent on scope. |
| Aug 13, 2026 | Gap remediation | **Coverage was asserted from a range table; 41 requirements had no test case.** §10.1 mapped SRS **section ranges** to TC ranges (`FR-100 – FR-120 → TC-130 – TC-147`) and the published **96.7 %** was concluded from that mapping — which cannot show that an *individual* requirement was tested, and therefore could not fail. Measured per requirement for the first time: **41 MVP-1 requirements are named by no case, 32 of them `Must`**. §10.2 (*every case names its requirement*) was true, machine-verified, and **the opposite direction** — a suite can name a requirement on every case and still leave requirements untested. **The denominator was wrong too:** **363** was the pre-split MVP-1 + MVP-2 total where `02-SRS.md` now carries **263**, §5.10/§5.18/§5.19/§5.23/§5.24 having left on 11 Aug 2026. Changes: §10.1 is **demoted to a navigation aid** and its five MVP-2 rows struck; §10.4 is split into **10.4.1 knowingly excluded** (a decision, with an owner) and **new 10.4.2**, itemising all 41 with what closes each — including the two that need only a citation added to an existing case (`FR-300` on `TC-377`, `FR-226` on `TC-284`), the one blocked on **`G27`** (`FR-175`, no host), and the one that is **not test-case shaped** (`FR-161`, an architectural quality). **New §10.5 is generated by [`build_coverage_matrix.py`](MVP-1/ProjectPlan/Tools/build_coverage_matrix.py)**, which fails the build on any undeclared hole, and states that *with a case* is not *executable* — `TC-350`–`TC-352` count among the 220 and cannot be run. Registered as gap **`G25`**. **The 41 cases are still owed**; this pass made the number honest, not the coverage complete. |
---

## [MVP-1/ProjectPlan/07-DeploymentRunbookAndRollback.md](MVP-1/ProjectPlan/Operations/Deployment.md)

| Date | Changed By | Description |
|------|-----------|-------------|
| Jul 30, 2026 | Plan team | Initial publication. An executable runbook with numbered steps, exact commands, and a verification after every step: release contents and ordering rationale, environment and configuration matrix, a pre-deployment checklist including the four line-state queries and a **mandatory rollback rehearsal**, and the five-component deployment sequence — `FlatWireDB` via SQLCMD with six post-run verifications (**27 tables, 43 FKs, 64 non-clustered indexes**), the **FW-001 shared-schema renames as a separately approved step** with pre-flight impact check and dependency verification, `FlatWire.API` with the **IIS WebSockets prerequisite**, OPC configuration with tag-push verification **on a stopped line only**, and the Angular bundle. Publishes a fifteen-check smoke suite and a rollback plan with decision criteria and a 10-minute decision limit, per-component procedures in reverse dependency order, an explicit warning that the teardown script **drops everything**, a dedicated section on why the FW-001 renames are the **least-reversible element** and what to do when the reverse fails, an eight-item manual reconciliation list for in-flight runs, open MMS IDs and unlabeled coils, rollback verification, and a communication template. Closes with monitoring, Serilog locations, incident runbooks for the three most likely production failures, an escalation roster left for the release manager to fill, and a command and file reference. |

---

## [MVP-1/README.md](MVP-1/README.md)

| Date | Change |
|---|---|
| Aug 11, 2026 | **Phase 9 returned to MVP-1 in full, and two scope calls were settled against it.** `dashboard_7_coil_completion.html`, `dashboard_7b_packing_station.html` and `OutputCoilCompletion.md` moved back from `MVP-2/`; `02-SRS.md` §5.16/§5.17, `04-APIContract.md` §4.15/§4.16, `06-TestPlanAndTestCases.md` TC-405–429/TC-435–444 and backlog story `FW-066` all restored **verbatim and unrenumbered**; `MVP-2/DevelopmentPlan/ShopfloorPlan/phase-09-mvp2-output-coil-completion.md` deleted. The trigger was that `CoilOutput`/`CoilTraceability` were MVP-1 while their **only** writer, `CoilCompletionService`, was not — leaving a non-overlap trigger guarding rows nothing inserted. **`OutputCoilCompletion.md` reissued at v1.1** with a new §8 owning **DB7b**, which had requirement text but no owning document; four of its eight requirements are mockup-derived and are tagged `[PROPOSED]`. Separately, **pass schedule** (generation and management) and **die inventory/lifecycle** were confirmed owned outside MVP-1 — the mid-run die change event stays, with `D4` restated at die-**size** level against `Drawer`. |
| Aug 11, 2026 | Created. `Mockups/` (root), `LatestDocument/RequirementDocuments/` and `SRS/` moved in; every cross-reference in the repository repointed and verified with **zero dangling links**; `build_docx.py` source and output paths updated. |
| Aug 13, 2026 | **Inventories corrected against the tree, and a stale register count fixed.** `RequirementDocuments/` is **17 files, not 16**, and the *"two are not specifications"* passage is now **three** — `ClientQuestionsContent.md` (12 Aug 2026) is source prose for a generated deliverable and is described as such, since the folder's own trap is that its non-specifications get cited as though they were specifications. `SRS/` is **3 deliverables, not "2 client `.docx`"**, with `FlatWire_ClientQuestions.xlsx` documented as generated output and its renderer named — and the section now says plainly that **two of the three are generated and one is not**, `Shopfloor_Flat_wireSRS.docx` being the delivered SRS itself. `DevelopmentPlan/` is **18 phase files, not 16**. Separately, the *"This folder is not all of MVP-1"* table described the question register as **99 items**; it is **33 open (`Q1`–`Q33`)** and **25 decided (`Q61`–`Q85`)** across two files since the 12 Aug renumbering, and both are now named. |
---

## [MVP-1/RequirementDocuments/ActiveRunMonitor.md](MVP-1/ProjectPlan/Business/Screens/ActiveRunMonitor.md)

| Date | Change |
|---|---|
| Aug 11, 2026 | **First issue.** Created as the owning specification for Dashboard 3, which previously existed only inside the shopfloor dashboard design reference. Carries the trace rules including the FL2 historical-profile behaviour and the August 4 Machine View / View Trends descope, the payoff thresholds with the absolute-weight rule, the per-line action sets, the pause reason vocabulary as codes and categories, the four resume outcomes closing OI-14, and the shift-summary contribution. **Settled the Roll Adjust line scope as FL2 / FL3, not FL1** — four sources disagreed; `FR-107`–`FR-109` and all three active-run mockups agree, and were verified directly. Also added §2.5, the mid-run configuration-change acknowledgement flow, folded in from the operations-manager role document, which was its only home. Screen styling, layout dimensions, scripting detail, endpoint names and internal verification notes removed for the client issue. |

---

## [MVP-1/RequirementDocuments/ClientQuestionsContent.md](MVP-1/ProjectPlan/Tools/ClientQuestionsContent.md)

| Date | Change |
|---|---|
| Aug 12, 2026 | **Cut to 33 open entries and renumbered with the register, and three client edits to the workbook mirrored back into the source.** The 23 withdrawn questions' entries were deleted, the survivors renumbered `Q1`–`Q33`, and the `Answer together with` fields — the only field in this file that carries question numbers — remapped, dropping members that had been withdrawn. **`Q45`'s list lost five of its eight members** to the `PLC-Q##` register and now names the controls session in prose, since a `PLC-Q##` id would fail the leakage scan.<br><br>**The three edits were made by hand in the `.xlsx` and had to come back here or the next build would have reverted them:** `Q4` (skid labelling) **loses its recommendation and its `Why`**, joining footage-to-weight as a question that goes to the client with no answer attached; `Q13` (PLC tags on checkout) loses its `Answer together with`; and `Q24` (staging deviations) has the **supervisor-PIN sentence removed from its question** — that part came from the withdrawn spool-weight question and is now tracked internally as `OI-38`, so the client is no longer asked it. **This required a second tool change:** `Why` was in `REQUIRED_OPEN` in [build_questions_xlsx.py](MVP-1/ProjectPlan/Tools/build_questions_xlsx.py) and a missing required field is fatal, so it moved to the optional set — **for one reason only, that it justifies the recommendation and there is now a question with none.** The workbook was regenerated: 33 open rows, 25 decisions, all four guards passing including the drift guard, which is what proves a renumbering did not move prose onto the wrong question. |

---

## [MVP-1/RequirementDocuments/DieChangeAndManagement.md](MVP-1/ProjectPlan/Business/Screens/DieChangeAndManagement.md)

| Date | Change |
|---|---|
| Apr 2026 | Initial specification — die change event capture, reason codes, conditional quality hold and SPC notice; die management screen, lifecycle operations and life thresholds. |
| May 4, 2026 | Post-die-change resume gate confirmed as a hard block with thread mode permitted; routing corrected for gauge drift and size change. |
| Aug 4, 2026 | **v2.2 — FM2 roller sizes corrected** to three stands, S1 8″ and S2/S3 6″ (the 8″ roller is S1). No die-change requirement affected. |
| Aug 1, 2026 | **Reissued as version 2.0 for client review.** FM2 corrected to three stands with edgers at S2 and S3. The conflicting die-life colour bands between the two screens raised as a client decision instead of being silently reconciled, alongside the band-configurability question. Roll-adjust line applicability flagged as unresolved. Screen styling, navigation targets, interface payloads and mockup-state commentary removed; the gap-analysis framing replaced by the confirmed routing rules it produced. |

---

## [MVP-1/RequirementDocuments/LineStatusOverview.md](MVP-1/ProjectPlan/Business/Screens/LineStatusOverview.md)

| Date | Change |
|---|---|
| Aug 1, 2026 | **First issue.** Consolidated the Line Status Overview requirement from the shopfloor dashboard inventory, the HMI specification and the pre-check-in specification into a document of its own — the screen previously had none. Applied the later decisions: the active pass schedule identity per line card, absolute-weight payoff alerting aligned with the schematic and the staging station, Payoff 2 readiness sourced from the staging record, and navigation to the schematic and trend screens. Raised the line-state vocabulary, alert lifecycle, notification mirroring and panel resolution as client questions. |
| Aug 4, 2026 | **Both header drill-downs removed.** *Open HMI* → Dashboard 13 and *SCADA Trends* → Dashboard 14 are gone with those screens (`FR-425` withdrawn); neither was ever implemented in the mockup, so the wireframe is now accurate rather than aspirational. The board itself is otherwise unchanged — **the five alert rules, the payoff thresholds and every live value it shows are untouched**. The machine tags behind it (line state, speed, live gauge and width, payoff weight, and the component-fault signal that raises the Critical alert) are specified in [`PLCTagSpecification.md`](MVP-1/ProjectPlan/Architecture/PLCTagSpecification.md). |

---

## [MVP-1/RequirementDocuments/OutputCoilCompletion.md](MVP-1/ProjectPlan/Business/Screens/OutputCoilCompletion.md)

| Date | Change |
|---|---|
| Aug 11, 2026 | **v1.1 — Dashboard 7b brought under this document as §8, and the document moved into `MVP-1/`.** Phase 9 was confirmed **wholly MVP-1**, returning DB7, DB7b, `POST /coil/complete`, `GET /coil/{alpha}/label` and `CoilCompletionService` to active scope. DB7b was the only in-scope dashboard with **no owning specification** — its `FR-345`–`FR-352` existed in the SRS but no document owned them — so §8 was written to close that, in preference to a separate `PackingStation.md`, because DB7 and DB7b are one flow and the §6 skid rule spans both. **Four of the eight requirements are mockup-derived and never client-reviewed** (`FR-345`, `FR-346`, `FR-349`, `FR-350`); they are tagged `[PROPOSED]` and flagged as such on the sign-off sheet rather than presented as agreed. **Two new open items:** the **authoritative weight** — a third figure now exists and the label is printed before packing sees the coil — and **staging locations**, which closure must assign and which nothing defines. `OI-98` and `OI-99` restated as packing blockers, not only labelling ones. `A5` superseded; `A6`/`A7` added for the station's scale and printer. Sections 8–11 renumbered 9–12; §4 unmoved, as it is the only externally cited section. |
| Aug 11, 2026 | **First issue.** Created as the owning specification for Dashboard 7, which previously existed only inside the shopfloor dashboard design reference. The source-traceability capture block was folded in from the spool material reference. Carries the May 21 2026 target-value display decision with the reasoning behind it, the two-coil skid rule, the label field set, and the explicit statement that the digital traveler does not make labels digital. **Raised two new open items** — **OI-98** (the odd final coil) and **OI-99** (lot number for a multi-rod coil). Two others were drafted and **withdrawn on checking the requirement text**: the alloy density source and the calculated-versus-weighed question are already settled by `FR-332`, and the genuine remaining weight question is the **dimensional basis**, which is the existing **OI-45** — now cited rather than duplicated. Screen styling, layout dimensions and scripting detail removed for the client issue. |
| Aug 11, 2026 | **v1.2 — "transformer line" removed as an undefined precedent.** §6's skid rule and §7's packaging prompts both cited the transformer line as the convention flat wire inherits. The term is **defined nowhere in this repository or in the wider UAL document tree** — it was used across seven files as assumed plant context, and this was the only client-facing document carrying it. **No requirement changes:** a skid still holds exactly two coreless coils (`FR-335`, §6, D2), and the guided packaging prompts are unchanged. §6 now reads *"consistent with UA's existing coil packaging"* — the inheritance claim is load-bearing here and is kept — and §7's *"in the style already used on the transformer line"* is dropped, since it justified nothing the section does not state itself. `Q4` / `OI-67` now ask the client **which existing convention is being inherited**, which is the question the named line had been standing in for. |

---

## [MVP-1/RequirementDocuments/PartialRodReCheckin.md](MVP-1/ProjectPlan/Business/PartialRodReCheckin.md)

| Date | Change |
|---|---|
| Aug 11, 2026 | **Moved from `Analysis/` to `MVP-1/RequirementDocuments/`**, to sit beside `RodPreCheckin.md` and `RodCheckout.md`, which own its rules. The header now states plainly that this is **not** one of the seventeen client-facing specifications — it has none of their apparatus and nothing in it is citable — because the folder change would otherwise read as promotion to requirement status. Links repointed: the register is now `../../Analysis/`, the two owning specs are same-folder. Inbound citations updated in `CLAUDE.md`, `ShopfloorAndRealTimePlan.md`, `FlatWire_MasterSpecification.md` (§A.2 pointer table → §A.2a), `FlatWireProcessWalkthrough.md`, `FlatWireShopfloorDashboards.md` and `FlatWireOpenQuestions.md`. **Corrected a warning that had become false:** the header claimed the snake_case columns "were never reconciled to the DDL", but all three were delivered on 26 Jul 2026 as `Rod.FootageRunToDate`, `Rod.RemainingWeightEstimateLb` and `Spool.SourceRodAlpha` (with FK, index and seed data), so the warning now carries the proposed → delivered mapping instead, and notes that Jaspreet's schema-impact open item is closed. `Q47` remains open on the payoff-scale question and the full carry-forward sign-off. |
| Aug 11, 2026 | **Consolidated into the owning requirement documents.** The carry-forward gate was already carried by `RodPreCheckin.md` §7; the spool-identity model and the reasoning for two identities rather than one were moved into `RodCheckout.md` §7.2, with the identifiers **corrected to the canonical `R#####` / `SP-#####` forms**. Added a header warning that this file's worked examples use the wrong alpha formats (gap **G14**) and that its snake_case column proposals were never reconciled to the delivered PascalCase schema — both were live traps for anyone building from this file. Retained as the design rationale and the `Q47` audit trail; `Q47` remains open. |

| Date | Changed By | Description |
|------|-----------|-------------|
| Apr 28, 2026 | Analysis Team | Initial document — carry-forward design recommendation, UI flow, spool alpha model, and open items documented |
| May 4, 2026 | Analysis Team — Tim O. review | Status updated to In Progress. Tim confirmed: material in mill will be scrapped; multiple partial spool alphas per rod needed; payoff scale pending plant input (Scott/Bob/Shannon). Full carry-forward design deferred. Q48 and Q50 resolved externally. |

---

## [MVP-1/RequirementDocuments/RocCheckin.md](MVP-1/ProjectPlan/Business/Screens/RocCheckin.md)

| Date | Change |
|---|---|
| Apr 25, 2026 | Initial document — recommended behaviour for "Acknowledge & Begin Check-in" on Dashboard 2 and Dashboard 5. |
| Aug 4, 2026 | **v2.2 — FM2 roller sizes corrected** (three stands: S1 8″, S2 6″, S3 6″; the 8″ roller is S1) and **OI-04 closed**, since its two candidate stands were one stand. See the Document Change History. |
| Aug 1, 2026 | **Reissued as version 2.0 for client review.** Added: automatic line selection; the payoff-selector rule for staged rod; min/max dimensional limits; `INFLAT` committed at check-in; the attribute-lookup and mandatory-confirmation flow; the consumed staging record; FM2's third stand and the FL1 no-edger correction. Consolidated the rules as CI-1 to CI-10, the decisions as D1–D9, and the unresolved points as a client sign-off checklist. Internal gap references and mockup-implementation notes removed. |
| Aug 1, 2026 | **Check-in now ends on the rod pre-check-in station rather than the active run monitor** (Section 3.7). Pressing *Acknowledge & Begin Check-in* completes check-in outright — the records are written, the tags are pushed, the run is open and the rod is drawing — so the destination is a question of what the operator does *next*, not of what check-in still owes. The next task in the continuous-feed cycle is staging the following rod on the idle payoff position, which is the pre-check-in station; returning there closes the cycle (stage → check in → stage the next) on one path rather than landing the operator on a monitor and leaving them to navigate back. The active run monitor is unaffected and remains reachable from the application bar and the line status board. **Raised as Q84 rather than applied silently:** the change assumes operators do not expect to see the run monitor at start-up to confirm the line took the tags and the gauge trace is live. If they do, the previous destination is the correct one and this reverts. |
| Aug 4, 2026 | **§3.6 reduced to a pointer.** This document listed the tag-push payload and was the outlier on two points — whether **edge type** is pushed, and whether speed is a **target** or a **limit**. Both are now settled in one place: edge type is pushed on FL2/FL3 and is not applicable on FL1 (FL1 has no edger), and the speed question is open as **`PLC-Q06`**. What stays here is the acknowledgement sequence, which is this document’s job: **confirm the schedule → write every audit record → push the tags → open the run**. |

---

## [MVP-1/RequirementDocuments/RodCheckout.md](MVP-1/ProjectPlan/Business/Screens/RodCheckout.md)

| Date | Change |
|---|---|
| Apr 24, 2026 | Initial use-case analysis — two scenarios, machine-control behaviour, captured data, open questions. |
| May 4, 2026 | Supervisor approval confirmed for mid-run checkout; partial material held pending disposition; multiple partial identities per rod confirmed. |
| Aug 1, 2026 | **Reissued as version 2.0 for client review.** Added **Mode P** and its weld-dependent approval. Stated the approval record as requirement **RC-4**, correcting an omission in which the May 2026 approvals were agreed but never specified into the design. Proposed requirements RC-1 to RC-6 for the removal cases the SRS does not cover. Restructured as a client deliverable; internal table and column detail removed. |

---

## [MVP-1/RequirementDocuments/RodPreCheckin.md](MVP-1/ProjectPlan/Business/Screens/RodPreCheckin.md)

| Date | Change |
|---|---|
| Jul 29, 2026 | Initial specification: station concept, bay state model, staging validations, traveler queue, three-step wizard, and the two-sequence model. |
| Aug 1, 2026 | **Reissued as version 2.0 for client review.** Applied the July 30, 2026 client decisions: shared coil status set at check-in only; pre-check-out approval determined by weld state, with welded removal a supervisor-authorised rejection to `HOLD`; failed inspection released by WIP rejection; off-schedule staging replaced by automatic line selection; min/max limits for four dimensional attributes; a rod may carry more than one order; bundles are never stacked; no welded-not-checked-in status. Document restructured to client-deliverable form — superseded rules, prior design iterations, internal schema and interface detail, and repository-internal commentary removed. New requirements `PCI009`–`PCI020` proposed for the removal, bay-state, order-resolution and sequence-authorisation rules the SRS does not currently cover. Open items consolidated into Section 11 with a sign-off checklist. New requirement identifiers span `PCI009`–`PCI017`. |
| Aug 1, 2026 | **Rejection captured at the station.** The WIP rejection that releases a blocked position is now raised on this screen rather than reached by navigating to a separate one, so the failing rod and its position are already known and the operator returns to a station that has not lost its state. No rule in this document changed. |
| Aug 1, 2026 | **Version 2.1.** Added **Section 8.1 `PCI021`** — a read-only list of the welds already recorded against the run in progress: time, rod pair, footage at the weld, weld type, operator and quality, with the mandatory failure reason on a failed weld (`WLD013`). Scoped to the **current run** (not the shift, not the coil), available whenever a run exists including while the idle position is empty or blocked, and not editable — correcting a recorded weld remains the unspecified `WLD011`. This replaces a station control labelled *Weld event log*, which navigated to the weld-**recording** form; nothing in the system previously listed the welds made during a run, although each is a certificate traceability input. Also recorded in Section 8 that marking the weld does **not** ask for an operator badge — the operator is taken from the signed-in station session, since `WLD003` requires it recorded, not re-entered, and a keyed value could disagree with the session actually stamped. |
| Aug 1, 2026 | **Version 2.2.** Added **Section 8.1 `PCI022` — weld quality.** Recording a weld now **requires** a Pass/Fail result, with a reason mandatory on Fail (`WLD013`), using the same six reasons as the Weld Event screen so a weld is documented identically wherever it is captured. Neither result is pre-selected: this is the check whose purpose is to make the operator look at the join, and a pre-selected Pass can be confirmed without being read. **A failing weld is recorded but does not mark the rod welded** — the join did not hold, so the rod is not joined to the running rod and the line cannot transition through it; the position keeps reading *not yet welded*, the station states that the last weld failed and why, and the operator remakes it. Previously the station recorded only *that* a weld happened, with no statement of whether it held, while the same join captured on the Weld Event screen did record a quality result — one physical join, documented two ways. Renumbered the run weld list to **8.2**. **Q24 widened** rather than a new question raised: a remade weld leaves more than one record of one physical join, which is the re-weld-on-certificate question Q24 already asks — now reachable both by a weld that breaks mid-run and by one that fails its quality check before anything runs through it. |
| Aug 1, 2026 | **Version 2.3. The station-level weld readiness band is removed and every control moves onto the payoff position it acts on.** The band sat between the two positions and the traveler queue, carrying a sentence of narrative and two buttons. Its narrative was already on the position cards — the weight and percentage on the payoff bar, the weld states in the position's alert line, the cold-start message in the empty-position text — so the band restated at station level what the operator was already reading on the card. Its two controls were station-level for no reason other than where they happened to sit. **Mark as welded now sits on the staged position** (Section 8): that position *is* the incoming rod, so the action carries the rod into the record instead of asking which rod is meant. Its unavailable states now state their reason on the card, which the band never did. **Welds this run now sits on the running position** (Section 8.2), because the run belongs to that position, and carries its count on the control. **Open the active run is withdrawn as a position action** (Section 4.1) — the run screen remains reachable from the application bar and the Line Status board, and this station's purpose is preparing the *next* rod rather than driving the current one. The one instruction the band alone carried, *induction-weld tail to head*, is now stated on the staged position whenever a rod is running. **One consequence needs client confirmation** (**Q83**): with the control on the running position, at cold start there is no position to show it on, so *Welds this run* is **absent** rather than shown-and-greyed as it was before. The traveler queue gained the heading **"Rods In Queue"** (Section 4.5) — the queue head had been an unlabelled empty row since the order-context line was removed, leaving the table with no name. Also recorded: where a position has an obvious next step it is emphasised, and **the running position deliberately emphasises nothing**, because both of its actions are exceptional and emphasising either would read as a recommendation. |

---

## [MVP-1/RequirementDocuments/RollAdjust.md](MVP-1/ProjectPlan/Business/Screens/RollAdjust.md)

| Date | Change |
|---|---|
| Aug 11, 2026 | **First issue.** Created as the owning specification for Dashboard 11, which previously existed only inside the shopfloor dashboard design reference. **The component table was rebuilt rather than transcribed:** the source table had been left internally inconsistent by the August 4 2026 three-stand correction, showing five rows for three stands with `S1` and `S2` each appearing twice and one row labelled *"S2 (final)"* when S3 is the final stand. It is now three rows, position-identified, with roll diameter as data. **Resolved the line scope** rather than carrying it as an open question: the design reference stated it three ways (*FL1/FL2*, *FL3 Operator*, *FL3 only*), but `FR-107`–`FR-109` and all three active-run mockups agree the answer is **FL2 and FL3, not FL1** — verified against the mockups, which show no Roll Adjust control on FL1 and one on each of FL2 and FL3. **Raised OI-103** — no bound exists on a gap change, and the value is written to the machine. Recorded that documents elsewhere still describe this as a standalone screen and that this specification supersedes them on that point. Screen styling, layout dimensions and scripting detail removed for the client issue. |

---

## [MVP-1/RequirementDocuments/SPCCheckpoint.md](MVP-1/ProjectPlan/Business/Screens/SPCCheckpoint.md)

| Date | Change |
|---|---|
| Apr 2026 | Initial specification — purpose, triggers, checkpoint types, measurement rows, tolerance evaluation, the two exit paths, audit stamp. |
| Aug 1, 2026 | **Reissued as version 2.0 for client review.** Corrected the checkpoint type list to the five recorded values and flagged the "Post DB1" selector as unrecorded. Added the May 4, 2026 hard-block decision with thread mode, the roll-adjust and mid-run-change triggers, the checkpoint locations, and the July 30 min/max tolerance decision. Raised the missing checkpoint-to-trigger link and the undistinguished SPC hold as client questions. Screen styling, layout dimensions, colour values and scripting detail removed. |
| Aug 1, 2026 | **Version 2.1 — presented as a dialog.** Raised over the run rather than navigated to; opened directly by a die change that requires it; read-only when reviewing a recorded checkpoint from the line status board. No requirement text changed. |

---

## [MVP-1/RequirementDocuments/Spool.md](MVP-1/ProjectPlan/Business/Spool.md)

| Date | Change |
|---|---|
| Aug 11, 2026 | **Moved from `Analysis/` to `MVP-1/RequirementDocuments/`**, to sit beside the four documents that own its screen content. The header now states plainly that this is **not** one of the seventeen client-facing specifications — it has none of their apparatus — because the folder change would otherwise read as promotion to requirement status; the domain content remains authoritative for *what a spool is*, and the screen content does not. Ten links repointed: the four owning specs and `PassScheduleManagement.md` are now same-folder, the master spec is `../`, and the register, `FlatWirePlan.md` and `FlatWireShopfloorDashboards.md` are `../../Analysis/`. Inbound citations updated in `CLAUDE.md`, `ShopfloorAndRealTimePlan.md`, `FlatWire_MasterSpecification.md` §A.2, `FlatWireProcessWalkthrough.md` and the 6 Aug client-call ledger. **The FM2 banner is unchanged and still applies** — the equipment description below is superseded and must not be built from. |
| Aug 11, 2026 | **Screen content consolidated into the owning requirement documents.** The Dashboard 2 / Dashboard 5 comparison moved to `RocCheckin.md` §4.3 and the Dashboard 7 source-traceability capture to `OutputCoilCompletion.md` §4; both are retained here as the design record under a pointer banner. Flagged the Spool-Related Open Questions section as a **May 2026 snapshot** subordinate to the 99-item register, which it had been silently competing with. The domain reference content — physical form, material flow, the coils-table mapping and the stakeholder concepts — is unchanged and remains authoritative for what a spool is. |

| Date | Changed By | Description |
|------|----------|-------------|
| Apr 24, 2026 | Analysis Team | Initial spool reference document created — comprehensive material flow explanation, system integration, and traceability chain documented |
| Apr 28, 2026 | MOM — Planning & Shopfloor meeting | **Planning Role updated:** alpha generation now at planning time (not execution); stop calculation is system-driven; planner inputs weight only. Three spool-to-order allocation scenarios confirmed (full, partial+stock, multi-order). "Assign as-is" stock option documented. Spool remainder tracking status updated to Decided. Coil capacity (1,000 lb) confirmed in comparison table. |
| May 4, 2026 | Analysis Team — Tim O. review | **Q55 Decided:** Anneal step modifies the existing spool alpha (no new child alpha); traceability maintained through the event log. Re-pass through FL1 not applicable (no capability). **Q57 In Progress:** Unique spool IDs confirmed (like furnace plates); tracking workflow FL1 → furnace → cooling → FL2 confirmed by operator spool number selection; full state machine transitions TBD. **Coil capacity updated** to 1,100 lb (revised from 1,000 lb per Tim O. May 4). |
| Aug 1, 2026 | Client sync (30 Jul call) | **Spool completion is graded on the customer weight range, not TKUP-1 capacity.** 3,500 lb is the equipment ceiling; working spools are ~**1,800 lb** so two ~900 lb finished coils can be cut at FL2, and completion is graded against a customer **min/max weight** (Q60). Recorded the **short-close** rule as an unplanned stop on the mill 10-90 pattern — supervisor override + hold or a concession offer when outside the range, remake last (Q65) — and the hard constraint that **the spool is always run off because FL2 has no spool stripper**. Added the mid-run coil-break rule: the stop is removed and a new stop starts from zero. |

---

## [MVP-1/RequirementDocuments/SpoolCompletionNotification.md](MVP-1/ProjectPlan/Business/Screens/SpoolCompletionNotification.md)

| Date | Change |
|---|---|
| Jul 28, 2026 | Part A recorded — milestone ladder, acknowledge-to-arm behaviour, live update while unacknowledged, supersede in place, non-blocking constraint. |
| Jul 29, 2026 | Part B recorded — stop-transition trigger with dwell filter, latched weight, Yes/No consequences, scale-weight verification, and the client direction that an out-of-tolerance variance is authorised rather than blocked. |
| Aug 1, 2026 | **Reissued as version 2.0 for client review.** **Part C — short close** added, graded against the customer minimum/maximum with offer-before-remake escalation and the always-run-off rule; the mid-run coil-break rule recorded with its footage caution. The assumed 2,000 lb target **withdrawn** in favour of the customer range. Acceptance criteria renumbered as A-1…A-10 and B-1…B-22. Screen styling, layout dimensions, colour tokens, mockup demo affordances and internal component notes removed. |

---

## [MVP-1/RequirementDocuments/SpoolQueue.md](MVP-1/ProjectPlan/Business/Screens/SpoolQueue.md)

| Date | Author | Change |
|---|---|---|
| Aug 13, 2026 | Plan team | **Phase 12 and its four work items removed; three of the four date levers rewritten.** 14 stages and 103 work items. *Defer the yield, cost and scrap work* is no longer a lever — it is already out — and *add a fifth developer* was written against a four-developer baseline that no longer exists. They are replaced by **let the final testing and commissioning stage start early** (the one lever that removes a sprint) and **add a fourth developer**, whose effect is corrected from *four weeks* to *about two* now that Phase 14 is held to its own sprint. The milestone covering every stage no longer lists Phase 12, and the *team is smaller than the work requires* risk was rewritten from three team sizes to one. |
| Aug 13, 2026 | Plan team | **Made scenario-count-agnostic, and coverage re-pointed at the sprint plan rather than the backlog.** The 3-developer re-baseline broke three assumptions at once. **(1)** `SCENARIOS` was the hard-coded tuple `(2, 3, 4)` and the sprint tables were found by fixed section numbers; both now come from the plan — the headline table's rows define the team sizes, sections are located by content, and the allocation matrix's sprint columns are matched by header. Team sizes spelled as words (*"Three developers"*) are accepted, which the old positional `zip` never had to handle; with one team size the sprint and milestone columns drop their *"— N devs"* labels. **(2)** Coverage checked every backlog work item against the content file, so excluding Phase 12 from the plan failed the build with five errors. **The workbook's scope is the sprint plan, not the backlog** — coverage now runs against the allocation matrix, and a backlog story absent from it is an exclusion rather than an error. **It is printed at the end of every build**, because a story dropped by accident and one removed by decision are otherwise indistinguishable. **(3)** Client wording written for three options was rewritten for one, the sheet renamed **Delivery Summary**, and the excluded scope named on both it and the read-me — a client should not have to infer that yield and costing are absent. |
| Aug 2, 2026 | Plan team | v1.1. Two-status rule and single check-in, per client. Section 3.5 rewritten from a four-status proposal to the confirmed *Ready for FL2* / *Checked in* pair plus the exclusivity rule; Section 3.4 gains the two scan-while-occupied outcomes; rules renumbered SQ-7 to SQ-12; confirmed decisions 6 and 7 added; open item 2 reduced to the stored vocabulary only; open item 6 added, since a quality-held spool has no place on a two-status screen. |
| Aug 2, 2026 | Plan team | Initial issue. New screen: Dashboard 5A, the FL2 spool queue. Records the reasons it is being added, the scan-resolves-order behaviour, the four ways a scan can end, and the five items still needing client input. |

---

## [MVP-1/RequirementDocuments/WeldEvent.md](MVP-1/ProjectPlan/Business/Screens/WeldEvent.md)

| Date | Change |
|---|---|
| Apr 2026 | Initial specification — screen purpose, captured data, traceability chain, confirm behaviour, end-to-end flow, welding-wire requirements. |
| Aug 1, 2026 | **Reissued as version 2.0 for client review.** Weld type corrected to induction only. Added the alloy/temper/diameter match as a stated validation, both payoff positions as captured data, and the July 30 decisions (welded-rod release, no welded-not-checked-in status, no stacking, mid-run coil break). Raised the coil-start footage offset (OI-25) and the in-place weld reversal as blocking client questions. Interface payloads, screen navigation targets and internal schema references removed. |
| Aug 1, 2026 | **Version 2.2 — the Weld Event screen is retired.** Dashboard 4 was removed, and the *Log Weld Event* action was removed from the active-run monitors on **all three lines**. The weld is now recorded **only** at the Rod Pre-Check-In station (Dashboard 2A, *Mark as welded*), which since version 2.1 captures the complete record including the mandatory quality result — this is also where the weld physically happens, so it removes a walk to a separate screen. **Two capabilities were not rehoused:** the re-sequenceable rod queue and the traceability chain view (`FR-175`). **And FL2, which has no pre-check-in station, now has no way to record a weld at all** — correct if FL2 only inherits its spool's weld markers, wrong if it joins spool to spool; the shift summary reports FL2 weld events, so confirmation is needed. Both raised as open items. |
| Aug 1, 2026 | **Version 2.1 — weld quality is captured with the weld.** Added Section 4.1 recording that the weld event is captured wherever the weld is made, with the **same mandatory quality result** in every case — there is no lighter path that records a weld without stating whether it held. Recorded that **a failed weld does not complete the join**: where the incoming rod is pre-checked-in, a Fail leaves it staged and un-welded, and the operator remakes the weld, so only a Pass marks the rod welded. Noted that a remade weld therefore leaves more than one record of one physical join, which widens **Q24** (re-welds on the certificate) to cover a weld that fails at capture and not only one that breaks mid-run; footage attribution across the two boundaries remains **Q22**. |

---

## [MVP-1/RequirementDocuments/WipRejection.md](MVP-1/ProjectPlan/Business/Screens/WipRejection.md)

| Date | Change |
|---|---|
| Aug 11, 2026 | **First issue.** Created as the owning specification for Dashboard 8, which previously existed only inside the shopfloor dashboard design reference. Records the 1 Aug 2026 conversion to a dialog and states the five entry paths that required it, since as a screen it could describe only one. Carries the July 30 2026 decision that this rejection is the sole exit from a blocked staging bay, the rule that the pre-check-in path records no footage rather than zero, and the requirement that a failing checkpoint measurement is carried in. **Raised OI-100** — valid rework stages per material state, which had no answer anywhere. Screen styling, layout dimensions and scripting detail removed for the client issue. |

---

## [MVP-2/Analysis/FlatWireOpenQuestions-MVP2.md](MVP-2/Analysis/FlatWireOpenQuestions-MVP2.md)

| Date | Change |
|---|---|
| Aug 11, 2026 | **Created by the MVP scope division of the open-questions register.** Three questions copied **verbatim** from `Analysis/FlatWireOpenQuestions.md`: **OI-88** (the only unambiguously MVP-2 open question), **Q83** (decided; the tracking mechanism is MVP-2, the 60/85 % bands are MVP-1) and **Q62** (decided; spans — the log table is MVP-2, the mid-run alert is MVP-1). **No `Q##` was renumbered and nothing was deleted from the master**, which keeps all 99 rows, all three bodies marked `⚠ MVP-2` in place, and the whole Change Log — so every inbound `OQ-##` reference still resolves there. Recorded here for the reader who expects a fuller file: **the deferred screens raise almost no open questions**, because the questions that sound like theirs are the check-in read boundary and belong to MVP-1. |

---

## [MVP-2/DBChanges/README.md](MVP-2/DBChanges/README.md)

| Date | Change |
|---|---|
| Aug 11, 2026 | **`CoilOutput` and `CoilTraceability` returned to MVP-1.** Their `CREATE TABLE` blocks, 4 FKs, 7 indexes, the DM010 non-overlap trigger, their seed data and their design-doc sections all went back, byte-identical. `CoilOutput.PassScheduleId → PassSchedule` stayed here as a **fourth** back-reference, since its parent is still MVP-2. Counts: **3 tables · 10 FKs · 6 indexes · 1 procedure**. |
| Aug 11, 2026 | Created by dividing `LatestDocument/DBChanges` by MVP scope. **No column definition, constraint or index was altered.** The MVP-1→MVP-2 FKs were routed here so the MVP-1 chain stays deployable, and the runner was ordered seed-before-constrain. |

---

## [MVP-2/DBChanges/Schema/FlatWireSchema_Schedule.md](MVP-2/DBChanges/Schema/FlatWireSchema_Schedule.md)

| Date | Change |
|---|---|
| July 26, 2026 | Added `PassScheduleChangeLog` (OQ-62); added `PassSchedule.ActiveJobId` + `RowVersion`; `PassSchedule.Alloy` FK → `AlloyProperty`; enforced one-Active-per-line+alloy via filtered unique index; added component `FM2_6inS3` + `IsMandatory` + FM1-not-bypassable CHECK (May-21 revision). Retargeted to `FlatWireDB`. |
| August 4, 2026 | **FM2 roller-size correction.** FM2 is three stands — S1 (8"), S2 (6"), S3 (6", final) — not a separate 8" roller plus three 6" stands. `ComponentName` vocabulary renamed to position-only (`FM2_S1`/`FM2_S2`/`FM2_S3`); `FM2_6inS3` withdrawn as never-existent; `CK_PSC_ComponentName` and `CK_RollOverride_Component` updated; roll diameter moved to the new `Stand.RollDiameterIn` column. Closes **OI-04**. |

---

## [MVP-2/DevelopmentPlan/FlatWireJiraStories-MVP2.md](MVP-2/DevelopmentPlan/FlatWireJiraStories-MVP2.md)

| Date | Changed By | Description |
|------|-----------|-------------|
| Aug 13, 2026 | Plan team | **Initial publication — the 6 MVP-2 stories moved out of [05-SprintPlanAndBacklog.md](MVP-1/ProjectPlan/Development/SprintPlan.md)** when that document was reduced to shopfloor MVP-1 only. Holds `FW-010`, `FW-011`, `FW-012`, `FW-013` (Epic 2) and `FW-068`, `FW-069` (Epic 7) — **6 stories / 31 points** — with their **full As a / I want / So that bodies and complete acceptance-criteria checklists**, which is precisely what [`../ProjectPlan/05-Backlog-MVP2.md`](MVP-2/ProjectPlan/05-Backlog-MVP2.md) lacks: that file already carries all six as **condensed one-line rows**, so the two are complementary and neither supersedes the other. **These are moves, not copies** — this file is now the only home for the bodies. **`FW-014` is deliberately not here**, spanning both scopes (MVP-1 trigger, MVP-2 `PassScheduleChangeLog` sink), as `05-Backlog-MVP2.md` already records. **Known-wrong criteria are flagged in callouts rather than edited**, because these are the bodies the MVP-2 team will work from and silently correcting them would hide the disagreement with the governing specifications. Three such flags: **(1)** `FW-013`'s seven algorithm steps must **not** be implemented — build to `PassScheduleGenerationSpec.md` **v1.5**, with a table naming all five errors (the pre-flatten diameter is a lower bound needing the round-edge correction; the pass-count branches are wrong; the final die may not be snapped down; the roll gap sits **below** gauge by a **load-dependent mill-spring** term rather than above it by a fixed alloy springback multiplier — a machine stiffness carried as a material property; and the finishing-mill test is not the route decision). **(2)** `FW-012`'s mandatory-stand criterion names the **retired** pre-`D-26` component `FM2-6\"S2` — the non-bypassable final stand is **`FM2_S3`**, and the story's own component list one row above is already correct, so the criterion contradicts itself. **(3)** `FW-069`'s every figure is blocked by **`OI-101`** (shift boundaries undefined), and all four of its dependencies are MVP-1 stories, making it the one MVP-2 item that cannot start until MVP-1's Phase 6/7 work is done. Also records that **`FW-061` and `FW-082`, both `Critical` MVP-1, depend on `FW-010` here**, and that `FW-006` in MVP-1's Epic 1 also claims the `PassSchedule*` tables this story creates — a both-scopes overlap listed in no scope table. |

---

## [MVP-2/DevelopmentPlan/PassScheduleGenerationSpecPrompt.md](MVP-2/DevelopmentPlan/PassScheduleGenerationSpecPrompt.md)

| Date | Changed By | Description |
|------|----------|-------------|
| Aug 1, 2026 | Analysis Team | Initial prompt created — 13-section client-facing FDD/SRS for pass schedule generation, with provenance tagging rules, drawing/rolling separation, `PSG-*` identifier scheme, and a completion checklist |

---

## [MVP-2/DevelopmentPlan/README.md](MVP-2/DevelopmentPlan/README.md)

| Date | Change |
|---|---|
| Aug 11, 2026 | Created on the MVP scope split of `DevelopmentPlan`. Phase 2 and the generation-spec prompt moved whole; phases 9, 11 and 13 carved partially. **Effort deliberately not apportioned** — the descope ladder does not align with the MVP boundary. |

---

## [MVP-2/DevelopmentPlan/ShopfloorPlan/phase-11-mvp2-shift-summary.md](MVP-2/DevelopmentPlan/ShopfloorPlan/phase-11-mvp2-shift-summary.md)

| Date | Change |
|---|---|
| Aug 11, 2026 | Carved from `phase-11-shift-summary-reporting-certification.md` on the MVP scope split of `DevelopmentPlan`. Bullets copied verbatim; **effort deliberately not apportioned.** |

---

## [MVP-2/DevelopmentPlan/ShopfloorPlan/phase-13-mvp2-die-management.md](MVP-2/DevelopmentPlan/ShopfloorPlan/phase-13-mvp2-die-management.md)

| Date | Change |
|---|---|
| Aug 11, 2026 | Carved from `phase-13-administration-reference-data.md` on the MVP scope split of `DevelopmentPlan`. Bullets copied verbatim; **effort deliberately not apportioned** — the ladder's 99 h rung bundles this screen with the MVP-1 role UI and must not be quoted here. |

---

## [MVP-2/ProjectPlan/00-README.md](MVP-2/ProjectPlan/00-README.md)

| Date | Change |
|---|---|
| Aug 11, 2026 | Created on the MVP scope split of `LatestDocument/ProjectPlan`. |

---

## [MVP-2/ProjectPlan/02-SRS-MVP2.md](MVP-2/ProjectPlan/02-SRS-MVP2.md)

| Date | Change |
|---|---|
| Aug 11, 2026 | Extracted from `02-SRS.md` on the MVP scope split. Sections copied verbatim; identifiers untouched. |

---

## [MVP-2/ProjectPlan/04-APIContract-MVP2.md](MVP-2/ProjectPlan/04-APIContract-MVP2.md)

| Date | Change |
|---|---|
| Aug 11, 2026 | Extracted from `04-APIContract.md` on the MVP scope split. Sections copied verbatim; identifiers untouched. |

---

## [MVP-2/ProjectPlan/05-Backlog-MVP2.md](MVP-2/ProjectPlan/05-Backlog-MVP2.md)

| Date | Change |
|---|---|
| Aug 11, 2026 | Created on the MVP scope split. Rows copied verbatim from `05-SprintPlanAndBacklog.md` §7.2–§7.3; nothing renumbered or re-estimated. |

---

## [MVP-2/ProjectPlan/06-TestCases-MVP2.md](MVP-2/ProjectPlan/06-TestCases-MVP2.md)

| Date | Change |
|---|---|
| Aug 11, 2026 | Extracted from `06-TestPlanAndTestCases.md` on the MVP scope split. Sections copied verbatim; identifiers untouched. |

---

## [MVP-2/README.md](MVP-2/README.md)

| Date | Change |
|---|---|
| Aug 11, 2026 | **Promoted to `MVP-2/` from `MVP-1/Mockups/FutureMVP-1/Mockups/`.** The seven mockups moved to `MVP-2/Mockups/` and their shared-asset paths rewritten to `../../MVP-1/Mockups/`; four owning specifications and one client `.docx` moved in; `DieChangeAndManagement.md` §4 extracted to a new `DieManagement.md`; `build_docx.py`'s branding-template path updated; and all inbound cross-references across the repository repointed so no link dangles. |
| Aug 11, 2026 | Folder created as `MVP-1/Mockups/FutureMVP-1/Mockups/`; the seven mockups moved out of `MVP-1/Mockups/` with `git mv` and recorded as an MVP-1 scope exclusion. |

---

## [MVP-2/RequirementDocuments/DieManagement.md](MVP-2/RequirementDocuments/DieManagement.md)

| Date | Change |
|---|---|
| Aug 11, 2026 | Created by extracting §4 of `DieChangeAndManagement.md` v2.2 into MVP-2 scope. Requirements copied unchanged; the die-life status vocabulary deliberately **not** copied; §6 added to record what MVP-1 still needs from the deferred document, and `OI-12` recorded as dormant rather than closed. |

---

## [MVP-2/RequirementDocuments/PassScheduleManagement.md](MVP-2/RequirementDocuments/PassScheduleManagement.md)

| Date | Change |
|---|---|
| Apr 24, 2026 | Initial integration analysis — acknowledgement gate, role separation, override logging, eight integration questions. |
| Apr 28, 2026 | Attribute-based resolution at check-in and the single unified FL3 hybrid schedule confirmed. |
| May 4, 2026 | Mid-run configuration change flow confirmed, including mandatory operator response and the automatic verification checkpoint; configuration confirmed as recorded at coil creation and excluded from the customer label. |
| Aug 1, 2026 | **Reissued as version 2.0 for client review.** **The generation algorithm carried in earlier versions is removed** — superseded in full by the Pass Schedule Generation Specification, and its pass-count logic was incorrect; only the two management-level constraints on generation are retained here. The eight gap analyses replaced by the confirmed rules they produced (D1–D10), with the unresolved residue carried as PSM-1 to PSM-4. Added the FL1 no-edger and FM2 stand corrections to the schedule content definition, and raised the non-bypassable-stand and edge-type vocabulary conflicts. |

---

## [MVP-2/RequirementDocuments/ShiftSummary.md](MVP-2/RequirementDocuments/ShiftSummary.md)

| Date | Change |
|---|---|
| Aug 11, 2026 | **First issue.** Created as the owning specification for Dashboard 10, which previously existed only inside the shopfloor dashboard design reference. Carries the May 21 2026 per-machine page correction with the reasoning behind it, the downtime grouping and its dependency on coded pause reasons, and the August 2 2026 spool-queue navigation that resolved a link previously drawn to nothing. **Raised OI-101** — shift boundaries are undefined, which blocks every figure on the screen — and **OI-102** (export format). Recorded that the FL2 weld figure has no source while G27/G28 remain open, and carried PP-03 forward as the unowned OEE scope question. Screen styling, layout dimensions and scripting detail removed for the client issue. |
