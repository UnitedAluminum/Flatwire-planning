---
id: FW-233
legacy_id:
title: A host for the /rod/** surface
status: cancelled
status_confirmed: false
status_note: "✅ **CLOSED WITH NO BUILD, 10 Sep 2026 — `P-54` was decided as option 3, re-specify.** This story existed only to host `[API §4.3]` and `§4.20`, and both are now **withdrawn** rather than re-homed: rod and coil receiving are another team's, and so is the data. Its own **`Step-3`** was written for exactly this outcome — *the re-specify branch: close the story, release the hours* — so this is the planned exit, not an abandonment. ✅ **6 h released.** ⚠ **The eight-step plan below is kept and is still worth reading**: `Step-1`'s traceability pack was BUILT and is what established that `[API §4.3]`'s five extra fields are projected from `RodStaging` and therefore already answerable through `GET /payoff/status` (`FW-158`) — the finding that made option 3 the cheap answer rather than the drastic one. ⛔ **Two residuals outlive this story and are NOT closed by it:** **`G129`** — `FR-547` still requires check-in to enforce membership of the allocation set and planning's surface for it is unnamed, which is `FW-226`/`FW-227`'s 46 h — and **`OI-33`**, `planning_routings`' unmapped column mapping, which was half of `Q24`'s station-switching blocker and is now all of it. ⛔ Do not read this closure as discharging either"
owner:
jira:
mvp: 1
phase: "4"
stream: BE
streams: [BE]
priority: high
hours: 0
sprint: S2
depends_on: [FW-138, FW-N04, FW-226]
blocked_by: []
has_plan: true
started:
completed:
---
# FW-233 · A host for the `/rod/**` surface

**Project:** United Aluminum (UAL) — Flat Wire Mill Module
**Last Updated:** September 10, 2026 (restructured into eight virtual steps — §3; **`Step-1` built, §8.1**) — Change history is in [`CHANGELOG.md`](../../../../CHANGELOG.md)
**Document Type:** Implementation plan for a single backlog story
**Status:** ✅ **CLOSED WITH NO BUILD, 10 September 2026** — `P-54` decided as **option 3, re-specify**. Both endpoints are withdrawn, not re-homed; the 6 h is released. `Step-3` was this outcome
**Owner:** Backend (.NET) stream
**Audience:** The developer building `FW-233`, and `[API]`'s owner deciding `P-54`
**Shortcode:** — *(implementation plan, derived from the specifications and the built code; **not citable as a requirement**)*
**Part of:** category [`FS-07` Rod Staging, Check-In, PLC Configuration and Weld Capture](../../FS-07-rod-checkin-plc-config.md) — the parent story for this functional area

---

> **Why this document exists.** Six hours, and **four details decide whether it is right —
> starting with whether it should be built at all.**
>
> **`P-53` was right and left a hole.** *"The service hosts no `/rod/**` surface"* — rod
> receiving is not shopfloor. But `[API §4.3]` and `[API §4.20]` **remain specified with nowhere
> to go**, so four requirements have no reachable endpoint.
> **This story may close with no build.** If `[API]` re-specifies instead of re-homing, the
> hours are released (AC 5). **That is a legitimate outcome, not a failure.**
> **`[TRP]`'s DB2 scans a rod**, so this reaches the trial, not only the contract.
> **`CoilCheckin` covers only the shared-schema half** — it is not the fallback it looks like.
> **It is delivered in eight virtual steps** (§3). ✅ **`Step-1` is built** — the traceability pack
> is §8.1, and ⛔ **it changed the question**: the service `P-54`'s option 2 names as the shared half
> **serves none of it** (§8.1 F1/F2), and `Q24` turns out to be **decided, not open** (F4).

---

## 1. The story

From `[TB §7]` — verbatim:

> ###### FW-233 · A host for the `/rod/**` surface
> **Hours:** 6 h BE · **Priority:** High · **Sprint:** S2 · **Phase:** 4 · **Stream:** BE
>
> > `P-53` withdrew `RodReceivingController` on 25 Aug 2026 — *"the service hosts no `/rod/**`
> > surface"*, rod receiving not being shopfloor — and the withdrawal was right. What it left behind
> > is not: **`[API §4.3]` and `[API §4.20]` remain specified with nowhere to go**, so `FR-042`,
> > `FR-064`, `FR-043`'s carry-forward gate and `Q24`'s station switching have **no endpoint**, and
> > `CoilCheckin`'s `getCheckinCoilInfo` covers only the shared-schema half. `[TRP]`'s DB2 is a trial
> > screen that **scans a rod**.
> >
> > ⚠ **Shell and route wiring only.** `[API §4.20]`'s order set is `FW-226`'s and the check-in reads
> > are their owning stories'. `P-54` names three options and the choice — **re-home or re-specify**
> > — is `[API]`'s, not a plan's.
>
> **Acceptance Criteria:**
> - [ ] `[API]` records the `P-54` outcome first: re-home to a controller, fold into an existing one, or re-specify the two sections away
> - [ ] If re-homed: a controller shell + routes for `[API §4.3]` and `[API §4.20]`, `FlatWireResult<T>`, `[Authorize]`, `401` unauthenticated — **no handler bodies**
> - [ ] `FR-042`, `FR-064`, `FR-043`'s carry-forward gate and `Q24`'s station switching each name a reachable endpoint, or `[API]` records why they do not need one
> - [ ] `[API §3.1]`/`§3.2` updated to match, and the endpoint count restated **there**
> - [ ] ⚠ If `[API]` re-specifies instead, this story closes as a **specification change with no build** and its hours are released
>
> **Rate-card basis:** 1 controller shell **3 h** (`FW-138` §8 basis, as `FW-232`) + route wiring for 3 endpoints ≈ **3 h** = **6 h**. ⚠ **No `[CE §2]` endpoint unit is priced here** — the bodies belong to `FW-226` and the check-in stories
> **Dependencies:** FW-138, FW-N04, **FW-226** *(order set)*
> **Blockers:** ⛔ **`P-54` is `open`** and this is blocked on `[API]`, not on code. It also reaches `[TRP]` — DB2 scans a rod

### 1.1 Out of scope

| Concern | Story |
|---|---|
| `[API §4.20]`'s **order set** bodies | ⛔ **`FW-226`, already costed** |
| The check-in reads' bodies | [`FW-157`](FW-157.md) and the staging stories |
| The `/order/**` host | [`FW-232`](FW-232.md) — sibling finding, same `[API]` owner |
| Rod ingestion into `FlatWireDB` | [`FW-223`](../../FS-15-shared-schema-boundary/BE/FW-223.md) — `sp_IngestRodFromCoils`, a **different** mechanism |
| Rod ↔ order entities | [`FW-240`](../../FS-14-order-allocation-fulfilment/BE/FW-240.md) |

### 1.2 What already exists

Read off the built code on 29 Aug 2026.

| Thing | Where | State |
|---|---|---|
| Controller folders | `FlatWire.API/Controllers/` — fourteen | ⛔ **No `RodReceiving`, and no `Rod`** — `P-53` withdrew it and it was never built |
| `PayoffStagingController` | `Controllers/PayoffStaging/` | ✅ Built — ⚠ **the most likely re-home target** (§2.2) |
| `CheckInController` | `Controllers/CheckIn/` | ✅ Built — the other candidate |
| `IPayoffStagingService` + stub + shell | `FlatWire.Domain/Services/`, `FlatWire.Infrastructure/Services/` | ✅ Built (`FW-140`) |
| `CoilCheckin`'s `getCheckinCoilInfo` | the **shared** service, not this one | ⛔ **Covers NONE of the shared-schema half — measured 10 Sep 2026, §8.1 F1.** Six members, all routing-step and reqsum flags. ⚠ *This row read "covers only the shared-schema half" until `Step-1` measured it* |
| `[API §4.3]`, `[API §4.20]` | `../../../../40-backend/APIs.md` | ⛔ **Specified, with no owning controller** |

**Nothing in this card is cancelled by `D-31`/`D-32`.** ⚠ But `D-32` **did** change what rod
receiving writes — `INFLAT` is now `FlatWireDB`-local only — so any rod endpoint reads local
state (`FR-044`) rather than the shared vocabulary.

---

## 2. The four details

### 2.1 The decision comes first, and it may cancel the build

AC 1 puts `[API]`'s decision **before** any code, and AC 5 says the story may close as *"a
specification change with no build"*. `P-54` names three options:

| Option | Consequence |
|---|---|
| **Re-home** to a new controller | A sixteenth base URL — the same 1A freeze pressure as `FW-232` |
| **Fold** into an existing controller | No new base URL. ⚠ **`PayoffStaging` or `CheckIn`** (§2.2) |
| **Re-specify** the two sections away | ⛔ Then `FR-042`, `FR-064`, `FR-043`'s gate and `Q24` need another home, or `[API]` records why they need none |

⚠ **Do not start with option 1 because it is the tidiest.** `P-53`'s reasoning — rod receiving
is not shopfloor — argues *against* a dedicated rod controller in a shopfloor service, and
option 2 respects it while still giving the four requirements an endpoint.

### 2.2 If it folds, `PayoffStaging` is the better host

Both `CheckInController` and `PayoffStagingController` exist. The rod reads in question serve
**DB2A pre-check-in** — scanning a rod, its carry-forward gate, station switching — which is
staging, not check-in. `RodStaging` is the staging table and `IPayoffStagingService` already
exists.

⚠ **`CheckIn` is the wrong host**: check-in is the *commit*, and these reads happen before it.
Putting them there would make the controller answer for two lifecycle stages.

### 2.3 Four requirements, and they are not equivalent

| Requirement | What it needs |
|---|---|
| `FR-042` | Rod lookup on scan |
| `FR-064` | *(reads with `FR-042`)* |
| **`FR-043`'s carry-forward gate** | ⚠ Its rules live in `RodPreCheckin.md` §7 / `RodCheckout.md` §7.2, and it is tested by `TC-050`/`TC-051`. **`Q12` is still open** on whether a payoff-side scale exists |
| **`Q24`'s station switching** | ~~⚠ Open question~~ → ✅ **`Decided September 8, 2026`**, corrected by `Step-1` (§8.1 F4) — it lives in `Decisions.md` now. **The endpoint need is unchanged.** It still interacts with `G21` — FL1 and FL3 **share** `FL1PO` — and ⛔ has a **second** blocker `P-54` cannot clear: `OI-33` (§8.1 F5) |

⛔ **`G21`'s bay-uniqueness fix constrains station switching.** `STATION_BY_LINE = {FL1:"FL1PO",
FL3:"FL1PO"}`, so "switch station" cannot mean "move to the FL3 payoff" — there is not one.
**Whatever endpoint serves `Q24` must not imply a third bay.**

### 2.4 `CoilCheckin` is not the fallback it looks like

⛔ **Corrected 10 September 2026 by `Step-1`, and the correction is larger than the original
point — read §8.1 F1 and F2.** This section read: *"`CoilCheckin`'s `getCheckinCoilInfo` covers the
**shared-schema half** — the `coils` row."* **Measured against the built code, it covers none of
it.** `CheckinCoilInfo` has six members — `MfgOrderNo`, `SeqNo`, `OpLetter`, `IsDataCopyReqd`,
`IsReqsumReqd`, `IsTravelerUpdateReqd` — so it is a routing-step and reqsum-flags query, not a
material read; and it takes the machine as an **input**, so it cannot serve the one field whose
purpose is to decide the machine. ⚠ **`[TB §7]`'s card and `FW-138` §8 both carry the original
claim**; the card is not on the automatic path, so this is reported rather than swept (§7).

The rest of the section stands and is unaffected: it does **not** carry `RodStaging`'s bay state,
the three inspection results, `IsWelded`, or `Rod.FootageRunToDate` /
`RemainingWeightEstimateLb`, all of which are `FlatWireDB`-local.

⚠ **After `D-32` this gets worse, not better**: nothing in the shared schema now marks a rod as
being on a flattening line (`OI-111`), so the shared half answers less than it used to.
⚠ **`OI-111` "largely closes" since `FR-524` (19 Aug 2026) and this conclusion survives on a
narrower basis** — the station stamp is written **at check-in**, and this read happens before it
(§8.1 F6).

---

## 3. Build order — the eight virtual steps

⛔ **This story is delivered in eight steps, not in one pass.** Each is a **virtual step number**
inside `FW-233` — ⛔ **no step is a separate backlog story, and none gets its own `FW-###`.** The
story's hours, priority, phase, sprint and acceptance criteria are unchanged; only the *order of
delivery* is made explicit, so a developer can take one step, build it, test it, get it reviewed,
and stop.

⛔ **No step is separately costed.** [`[TB §7]`](../../../../60-delivery/TaskBreakdown.md) owns the
**6 h** and the five acceptance criteria, and this board does not re-price or restate them. Where a
step and the card appear to disagree, **report the divergence** — the card is not on the automatic
path.

**Status vocabulary:** ⬜ **Not Started** — ready to pick up now · 🔵 **In Progress** ·
✅ **Completed** · 🔗 **Dependent** — ready once its named steps are done · ⛔ **Blocked** — waiting
on something outside this story.

⚠ **`Dependent` is not `Blocked`.** A dependent step is ready the moment its predecessor merges; a
blocked step needs a register item answered. Keeping them apart is what stops this board reading as
though all eight were stuck on `P-54` — **only `Step-2` is.**

⚠ **The per-step detail is §8**, below §4–§7. It is placed there rather than here so that **§4, §5,
§6 and §7 keep the numbers they have** — this repository does not renumber sections (`CLAUDE.md`).

### 3.1 The board

| Step | Title | Applies when | Depends on | Blocked by | Status |
|---|---|---|---|---|---|
| **Step-1** | The traceability pack — four requirements, their fields, and who can serve them | **Every outcome** (it is the decision's input) | — | — | ✅ **Completed** 10 Sep 2026 — §8.1, **six findings** |
| **Step-2** | `[API]` records the `P-54` outcome | **Every outcome** | Step-1 *(input, not a gate)* | ⛔ **`P-54` is `open`** — `[API]`'s call, not code | ⛔ **Blocked** |
| **Step-3** | ⚠ The **re-specify** branch: close the story, release the hours | **Re-specify only** | Step-2 | ⛔ `P-54`, via Step-2 | 🔗 Dependent |
| **Step-4** | The `Rod` request/response types (`P-52`) | Any **build** outcome | Step-2 | ⛔ `P-54`, via Step-2 | 🔗 Dependent |
| **Step-5** | The service seam — two methods, the stub fixture, the real shell that throws | Any **build** outcome | Step-4 | ⛔ `P-54`, via Step-2 | 🔗 Dependent |
| **Step-6** | The two routes, on the host `Step-2` chose | Any **build** outcome | Step-5 | ⛔ `P-54`, via Step-2 | 🔗 Dependent |
| **Step-7** | `[API §3.1]`/`§3.2` gain the rows, and the endpoint count is restated **there** | Any **build** outcome | Step-6 | ⛔ `P-54`, via Step-2 | 🔗 Dependent |
| **Step-8** | The QA0 walkthrough, the regression baseline and the paper trail | Any **build** outcome | Step-7 | ⛔ `P-54`, via Step-2 | 🔗 Dependent |

⭐ **Eight steps, of which at most seven ever run.** `Step-3` and `Steps 4`–`8` are **mutually
exclusive branches** of the same decision — see §3.2. ⛔ **Nothing renumbers if the branch changes**:
`Step-3` keeps its number whether it runs or not, so every reference written today still resolves.

⚠ **The story's front-matter stays `status: blocked`, and that is correct.** A step board does not
unblock a story; it exposes the one hour of work inside it that was never blocked. ⛔ Do not move
`status:` off `blocked` on the strength of `Step-1` alone — the boards are generated from that field
([`STATUS.md`](../../../../STATUS.md), [`FEATURES.md`](../../../../FEATURES.md)) and it would report
a blocked story as moving.

### 3.2 Which branch you are on

⛔ **`Step-2`'s record routes you, and nothing else does.**

| `Step-2` records | Then build | And skip |
|---|---|---|
| **Fold** into `PayoffStagingController` (`P-54` option 1, and `P-170`'s recommendation) | `Step-4` → `Step-8`. No new base URL, no new service interface | `Step-3` |
| **A new controller** for `/rod/**` (the card's AC 2) | `Step-4` → `Step-8`, ⚠ **plus the 1A base-URL freeze** (`Step-6`) | `Step-3` |
| **Split by source** (`P-54` option 2) | `Step-4` → `Step-8`, with a **narrowed** rod-detail type — see `Step-4` | `Step-3` |
| **Re-specify** the two sections away (`P-54` option 3, card AC 5) | `Step-3`, and **stop** | `Step-4` → `Step-8` |

⚠ **Four outcomes appear in that column, and neither source lists all four.** `P-54` names
re-home-onto-`PayoffStaging`, **split by source**, and re-specify; the card's AC 1 names re-home,
**fold**, and re-specify. **Split by source is only in `P-54`; a dedicated controller is only in the
card.** ⛔ **This plan does not reconcile them** — `[API]` records which set it chose from, which is
why the table above routes on the *outcome* and never on an option number. §7 carries it as an open
item.

### 3.3 Rules that apply to every step

⛔ These are not repeated in §8. They hold for all eight.

- ⛔ **No handler body, in any step.** The bodies belong to [`FW-157`](FW-157.md)/`FW-158` and
  `FW-226` (§1.1). ⚠ **If you write an `if`, you are in another story** — the same line `FW-232`
  §2.1 draws.
- ⛔ **Invent no shape, no route and no controller name that `[API]` has not recorded** (`P-50`,
  `P-54`). *"Do not invent a shape to unblock a screen — that is how a stub becomes a contract
  nobody agreed to."*
- **The conventions are inherited, not re-decided:** `FlatWireResult<T>` (`P-56`), `[Authorize]`
  from the global filter (**never re-declared**, `FW-138` §6.2), class-level base route plus an
  explicit per-action route (`P-04`), models under `FlatWire.Domain/Models/` (`P-52`), and the real
  implementation shipping as a throw named for its owner (`P-64`, `P-169`).
- ⛔ **This plan states no endpoint count, at any step.** `[API]` owns it, for the same reason
  `[DBD §6.2]` owns the object counts.
- ⚠ **The code lands in `Second-Branch/ual-api`** — `API/Domain/FlatWire/FlatWire.sln`. ⛔ **Not
  `c:\UAL\ual-api`, which has no `FlatWire` domain at all**, whatever `CLAUDE.md`'s `../ual-api`
  suggests.
- **Each step records its own outcome in its `Status.` line.** The `CHANGELOG.md` row and the
  regeneration of the two boards are `Step-8`'s, once — not eight times.

---

## 4. Decisions this plan makes

> `P-##` is continuous across the repository; `P-01`–`P-169` precede this story.

### `P-170` — if it folds, it folds into `PayoffStaging`, not `CheckIn`

§2.2. These reads serve pre-check-in staging; check-in is the commit that follows. `RodStaging`
is the staging table and `IPayoffStagingService` already exists, so folding costs no new
service.

⚠ **This is a recommendation into `P-54`, not a substitute for it.** The re-home / fold /
re-specify choice stays `[API]`'s.

### `P-171` — whatever serves `Q24` must not imply a third payoff bay

§2.3. `G21` was fixed on 15 Aug 2026 by FL1 and FL3 **sharing** `FL1PO`, and an endpoint shaped
as *"switch this rod to line FL3's station"* re-opens it at the contract level even with the
index intact.

**Fallback:** if station switching genuinely needs to name a station, it names `FL1PO` for both
lines and the **line** is the varying parameter — never the station.

---

## 5. Verification

**No automated tests** — `[TS §1.2]`. Verified in the QA0 walkthrough.

⚠ **`Step-8` executes this table, top to bottom** — it is the story's verification, not each step's.
The rows a single step can prove on its own are named in that step's own acceptance criteria (§8).

| Check | Expected |
|---|---|
| **Decision recorded first** | `[API]` carries the `P-54` outcome **before** any code (AC 1) |
| Four requirements | `FR-042`, `FR-064`, `FR-043`'s gate and `Q24` each name a **reachable** endpoint, or `[API]` records why not (AC 3) |
| If built: `401` | Unauthenticated → `401`, like every other endpoint |
| If built: envelope | `FlatWireResult<T>` (`P-56`) |
| If built: **no bodies** | Shells throwing, named for their owning story (`P-64`) |
| **No third bay** | Nothing in the contract implies an `FL3PO` (`P-171`, `G21`) |
| `[API]` updated | `§3.1`/`§3.2` match, and **the count is restated there** |
| **No count here** | This plan states no endpoint count |
| If re-specified | The story closes with **hours released**, recorded in `[TB §7]` (AC 5) |
| Regression | The 14 controllers and 22 endpoints unchanged; 61/61 still pass |

---

## 6. Handoff

`FW-226` owns `[API §4.20]`'s order set bodies; [`FW-157`](FW-157.md)
and the staging stories own the check-in reads. [`FW-232`](FW-232.md) is
the sibling finding — **that one's blocker is merely unactioned; this one's `P-54` is `open`**,
which makes this the harder of the two. `[TRP]`'s DB2 scans a rod, so a re-specify outcome
reaches the trial plan and not only the contract.

---

## 7. Open items

| Item | Effect here |
|---|---|
| ⛔ **`P-54`** | `open`. **The story is blocked on `[API]`, not on code**, and may close with no build |
| **`P-53`** | Withdrew `RodReceivingController` — correct, and the cause of this hole |
| **`Q12`** | Still open — whether a payoff-side scale exists. Bears on `FR-043`'s carry-forward gate, and ⛔ **does not block it** (§8.1 C). ⚠ The client answer was owed **10 Sep 2026** |
| **`Q24`** | Station switching. ~~⚠ Open~~ → ✅ **`Decided September 8, 2026`** (§8.1 F4). Constrained by `G21` (`P-171`), and ⛔ **also blocked by `OI-33`** (§8.1 F5) |
| **`G21`** | Index and domain layers fixed 15 Aug 2026 — FL1 and FL3 share `FL1PO`. **No third bay may be implied.** ⚠ **The register does not treat the gap itself as closed** — its FL1/FL3 scope question *"still blocks `FW-007`, `FW-158` and `FW-N01`"*, and `FW-158` is what `Step-5`'s throw names |
| **`OI-111`** | ➕ Raised by `D-32`; ⚠ **"largely closes"** since `FR-524`, 19 Aug 2026. §2.4's conclusion survives on a **narrower** basis — the station stamp is written **at check-in**, and this read happens before it (§8.1 F6) |
| ➕ **`OI-33`** | ⛔ **Raised into this story by `Step-1` (§8.1 F5).** `planning_routings`' column mapping is unmapped, and `[API]`'s own table names `GET /rod/{alpha}` against it — so `orderId` and `scheduledMachineName` stay unanswerable **however `P-54` closes** |
| **§8.1 finding 4** | ✅ **This story closes it**, whichever way `P-54` goes |
| ➕ **The step board** | This plan is delivered in **eight virtual steps** (§3, §8). ⛔ No new backlog id, and `[TB §7]`'s 6 h and five acceptance criteria are unchanged |
| ➕ **AC 1 and `P-54` list different options** | ⚠ **Four outcomes exist across the two**, not three. **Split by source** is only in `P-54`; **a dedicated controller** is only in the card. ⛔ `[API]` reconciles it at `Step-2` — not this plan (§3.2) |
| ➕ **The card prices three endpoints** | ⚠ Its rate basis quotes *"route wiring for 3 endpoints"* and `[API §4.3]` + `§4.20` are **two**. ⛔ **Do not invent a third** — `Q24` needs no endpoint of its own and `POST /rod` is upstream (`P-53`). Report it against `[TB §7]` (`Step-6`) |
| ➕ **`[API §4.3]`'s body has no single owner** | ⚠ §1.1 names `FW-157` *and the staging stories*; the four sibling throws already in `PayoffStagingService` all name **`FW-158`**. `Step-5` names it from `[API]`'s record and **reports the ambiguity rather than choosing** |

---

## 8. The steps in detail

> The board and the branch router are §3. ⛔ **Read §3.3 first** — the rules that apply to every
> step are stated once, there, and are not repeated below.

### Step-1 — The traceability pack: four requirements, their fields, and who can serve them

**Scope.** One table: each of the four requirements, the fields it needs, and which schema half can
serve each field. It is **AC 3's evidence** and `P-54`'s input. ⛔ No code, no shape, no endpoint —
the pack says what is *needed*, never what a response looks like.

**Implementation.**
- One row per requirement — `FR-042`, `FR-064`, **`FR-043`'s carry-forward gate**, **`Q24`'s station
  switching** — carrying the fields it needs from `[API §4.3]`'s five required additions
  (`orderId`, `scheduledMachineName`, `footageRunToDate`, `remainingWeightEstimateLb`,
  `stagedPayoffPosition`/`isWelded`).
- Each field attributed to **`FlatWireDB`-local** or the **shared** schema, and — per field, not per
  requirement — whether `CoilCheckin`'s `getCheckinCoilInfo` can serve it (§2.4).
- ⚠ **Cite, never assert.** The field list is `[API §4.3]`'s, the local/shared split is `[DBD]`'s,
  and `OI-111` is the register's. A plan is derived and **not citable as a requirement**, so a
  number typed here rather than cited is the next contradiction (`CLAUDE.md`).
- ⚠ Carry the three register items that bear on the rows: **`Q12`** (whether a payoff-side scale
  exists — `FR-043`'s gate), **`Q24`** itself, and **`G21`**'s constraint via `P-171`.
- ⚠ **`OI-111` makes the pack worse reading than it would have been in July**, and that is the
  finding: after `D-32` nothing in the shared schema marks a rod as being on a flattening line, so
  the shared half answers **less** than it used to (§2.4).
- The output lands as a table under this step, and is **handed to `[API]` as `P-54`'s input.**

**Dependencies.** ⭐ **None.** This is analysis over documents that already exist, and it is the one
step that does not wait on `P-54`.

**Acceptance criteria.**
- [ ] All four requirements are present, each naming the fields it needs, each field attributed to
      `FlatWireDB`-local or shared, citing `[API §4.3]` and `[DBD]`
- [ ] `CoilCheckin`'s coverage is stated **per field**, not per requirement (§2.4)
- [ ] `Q12`, `Q24`, `G21` and `OI-111` each appear against the rows they actually affect
- [ ] ⛔ No route, no endpoint shape and no field name that is not already in `[API]` appears
      anywhere in the pack (`P-54`)
- [ ] It is recorded as handed to `[API]`, so `Step-2` can be taken against it

**Status.** ✅ **Completed 10 September 2026.** The pack is §8.1 below. ⛔ **It changes what `Step-2`
is choosing between** — see §8.1's findings, four of which were not known when this plan was written
on 29 Aug 2026.

### 8.1 `Step-1` record — the traceability pack, 10 September 2026

> **Every row cites; none asserts.** The field list is [`[API §4.3]`](../../../../40-backend/APIs.md)'s,
> the master column is [`FlatWireSchema_Materials.md`](../../../../30-database/schema/FlatWireSchema_Materials.md)'s
> *(the `Rod` source/master table, which is the only site that states it per column)*, the DDL is
> `FlatWire_DDL_03_Materials.sql`'s, and the register items are
> [`Questions.md`](../../../../90-registers/Questions.md) /
> [`Decisions.md`](../../../../90-registers/Decisions.md) /
> [`Gaps.md`](../../../../90-registers/Gaps.md)'s. ⛔ **No route, no response shape and no field name
> that is not already in `[API §4.3]` or `§4.20` appears anywhere below** (`P-54`).
>
> ⚠ **`CoilCheckin`'s coverage was measured, not read off a document** — from
> `CoilCheckin.Domain/Models/CheckinCoilInfo.cs` and `CoilCheckinController.cs` in the
> `Second-Branch/ual-api` checkout, on 10 Sep 2026. That measurement is finding **F1**, and it is the
> reason this pack matters more than a re-statement of §2.3.

#### A · The four requirements

| Requirement | What it needs | Can any service outside `FlatWire` serve it? |
|---|---|---|
| **`FR-042`** — validate the alpha against the `R#####` series in `coils`, **the measured diameter against a min/max band**, and reject a rod already checked in elsewhere | `alpha` · `diameterIn` · `status` | ⛔ **No.** `diameterIn` is **local** (F3) and *"a rod already checked in elsewhere"* is `Rod.Status`, local since `D-32`. Only `alpha` is shared-mastered |
| **`FR-064`** — validate the rod number against `coils`; reject invalid or non-existent | `alpha` | ⚠ **In principle yes** — `alpha` is shared-mastered (`coils.coil_no`). ⛔ **But not by the named candidate** (F1), and it is the only one of the four that a shared read could satisfy alone |
| **`FR-043`'s carry-forward gate** — where `footageRunToDate > 0`, show footage run, remaining weight, last run reference and prior spool alphas, and offer **only** partial re-check-in | `footageRunToDate` · `remainingWeightEstimateLb` | ⛔ **No.** Both are **local** and both are named by `FR-531` as flat-wire-owned attributes the projection must **leave unchanged** |
| **`Q24`'s station switching** — the screen switches to the rod's scheduled station, **no message, no override** | `scheduledMachineName` *(and `orderId`, which resolves it)* | ⛔ **No**, and F2 is why: the named candidate takes the machine as an **input**. ⚠ Second blocker — `OI-33` (F5) |

#### B · Field by field

| `[API §4.3]` field | Master | Source | `getCheckinCoilInfo` / `getCoilStatus` coverage — **measured** |
|---|---|---|---|
| `alpha` | shared | `coils.coil_no` | ⛔ **Neither returns it** |
| `alloy` | shared | `coils.coil_alloy` → `alloys.alloy` | ⛔ **Neither returns it** |
| `temper` | shared | `coils.coil_temper` | ⛔ **Neither returns it** |
| **`diameterIn`** | ⭐ **local** | the operator's measurement — ⛔ **`coils` has no rod-diameter column at all** | ⛔ **Unservable from the shared schema under any option** (F3) |
| `grossWeightLb` · `netWeightLb` | shared | `coils.coil_gross_wgt` / `coil_net_wgt` | ⛔ **Neither returns them** |
| **`status`** | ⭐ **local** | — (`INFLAT` is `FlatWireDB`-local, `D-32`) | ⚠ `getCoilStatus` returns **a bare `string`** — the *shared* status, which is **not this field** |
| `location` | shared | `coils.storage_section` | ⛔ **Neither returns it** |
| `receivedAt` | shared | `coils.coil_recvd_date` | ⛔ **Neither returns it** |
| **`orderId`** | shared | `planning_routings` | ⛔ **No** — and ⚠ **`OI-33` leaves the column mapping unmapped** (F5) |
| **`scheduledMachineName`** | shared | `planning_routings` | ⛔ **No, and it cannot be** — the candidate takes the machine as an input (F2). `OI-33` also applies |
| **`footageRunToDate`** | ⭐ **local** | — | ⛔ **No.** `FR-531` names it flat-wire-owned |
| **`remainingWeightEstimateLb`** | ⭐ **local** | — | ⛔ **No.** `FR-531` names it flat-wire-owned |
| **`stagedPayoffPosition`** · **`isWelded`** | ⭐ **local** | projected from the `RodStaging` row where `Status='Staged'` — ⛔ **no longer columns on `Rod`** | ⛔ **No.** No service outside `FlatWire` can query `RodStaging` |

⭐ **Nine of the fifteen are `FlatWireDB`-local or unservable from the shared schema; the named
candidate serves none of the six that are shared-mastered.**

#### C · The register items, against the rows they affect

| Item | State | Effect on this pack |
|---|---|---|
| **`Q12`** | `Open`, deferred 8 Sep 2026 | Bears on **`FR-043`'s gate**, and ⛔ **does not block it.** The carry-forward design *"is not dissented from"*; the recommendation split the scale off explicitly — *"do not hold the build for the scale answer."* ⚠ Tim O'Brien is **for** a payoff scale and the answer was owed **Thursday 10 Sep 2026 — today**, from him with Bob S. and Shannon R. |
| **`Q24`** | ✅ **`Decided September 8, 2026`** — in `Decisions.md`, not `Questions.md` | **F4.** The requirement's endpoint need is unchanged; **the question is not open.** ⚠ Its record carries a constraint this plan does not: auto-switching moves the operator between stations **mid-transaction**, and *"the behaviour of a part-completed wizard must be specified"* |
| **`G21`** | Index and domain layers fixed; ⛔ **the register does not treat the gap itself as closed** — its own text says the FL1/FL3 scope question *"still blocks `FW-007`, `FW-158` and `FW-N01`"* | `STATION_BY_LINE = {FL1:"FL1PO", FL3:"FL1PO"}`, **only `FL1PO` seeded, no `FL3PO`** (`G19`/`OI-26`). `UX_RodStaging_Bay` is keyed **`(Station, PayoffPosition)`**, not `MachineName`. ⛔ **`P-171` holds:** nothing may imply a third bay. ⚠ **`FW-158` is the story `Step-5`'s throw forwards to** |
| **`OI-111`** | ⚠ **"largely closes"** per `FR-524` / `[BR §5.26]`, 19 Aug 2026 | **F6.** §2.4's *"nothing in the shared schema marks a rod as being on a flattening line"* now has a **narrower** basis than when it was written: `FR-524` stamps the station on the shared record without altering the status. ⛔ **§2.4's conclusion still holds for this endpoint** — that write happens **at check-in**, and this read happens **at the scan, before it** |
| **`OI-33`** | Open — `[API]`'s own open-items table names **`GET /rod/{alpha}`** against it | **F5.** `orderId` and `scheduledMachineName` resolve from `planning_routings`, whose column mapping is **unmapped**, and `[API §4]` says *"Phase 4 cannot be built without them"* |
| **`OI-117`** | Open | Adjacent, and outside `[API §4.3]`'s field set: `Rod.SupplierHeat` has **no source anywhere**, and the welding-wire certificate chain traces through it |

#### D · Six findings for `P-54`

⛔ **Four of these were not known when this plan was written on 29 Aug 2026**, and two of them bear
directly on whether `P-54`'s options are what they appear to be.

**F1 · ⛔ The named substitute serves none of the shared half — measured, not inferred.**
`getCheckinCoilInfo` returns `CheckinCoilInfo`, which has **six members**: `MfgOrderNo`, `SeqNo`,
`OpLetter`, `IsDataCopyReqd`, `IsReqsumReqd`, `IsTravelerUpdateReqd`. It is a **routing-step and
reqsum-flags query**, keyed `(CoilNo, MachineType)` — not a material-attributes read. `getCoilStatus`
returns a bare `string`. ⛔ **Together they serve none of `[API §4.3]`'s six shared-mastered fields.**
⚠ **This contradicts §2.4 and `FW-138` §8**, which both describe the shared half as covered —
*"alpha, alloy, temper, diameter, weights, status, location, received-at"* — and **`P-54`'s option 2
rests on exactly that claim.** ⚠ **Whether some other shared endpoint serves those six is not
established here**, and establishing it is the precondition for choosing option 2.

**F2 · ⛔ The candidate cannot serve `scheduledMachineName`, because it takes the machine as an
input.** `getCheckinCoilInfo`'s query is `(CoilNo, MachineType)`. `scheduledMachineName` exists to
**decide** which machine the transaction belongs to (`Q24`). Asking that endpoint for it requires
already knowing the answer. ⛔ This is a shape problem, not a coverage gap — no amount of re-homing
fixes it.

**F3 · ⛔ `diameterIn` is `FlatWireDB`-local, and `coils` has no rod-diameter column at all.** The
schema's master column says **local** — the operator's measurement — and records why: the nearest
shared column is `coil_gauge`, *a strip gauge*, and *"reading it as a wire diameter would be a
convention dressed as a fact."* ⛔ **So `FR-042`'s min/max band check — the thing that requirement is
about — is unservable from the shared schema under every option.** ⚠ Independently, the band itself
is unseeded (`OQ-22`/`Q22`), so the check cannot fire yet either way.

**F4 · ⚠ `Q24` is decided, and §2.3 and §7 still call it an open question.** Decided **30 Jul 2026**
(off-schedule is not a deviation: the system selects the station automatically, no message, no
override, at pre-check-in **and** check-in), answered **unqualified on 8 Sep 2026**, and moved to
`Decisions.md` — a decided question in this repository **moves rather than changing state in place**.
✅ **Corrected in §2.3 and §7 by this step.** ⛔ **The endpoint need does not change**:
`scheduledMachineName` still has no host, which is this story's whole subject.

**F5 · ⛔ `P-54` is necessary and not sufficient for two of the fields.** `orderId` and
`scheduledMachineName` both resolve from `planning_routings`, and **`OI-33` leaves its column mapping
unmapped** — with `GET /rod/{alpha}` named against it in `[API]`'s own table, and `[API §4]` stating
that *"Phase 4 cannot be built without them."* ⛔ **Re-homing the endpoint does not make these two
answerable.** `Q24`'s station switching therefore has **two** blockers, only one of which `P-54` can
clear.

**F6 · ⚠ §2.4's `OI-111` basis is narrower than it was, and its conclusion survives.** `FR-524` /
`[BR §5.26]` stamps the rod's shared record with its flattening station **without altering the status
value**, which is why the register says `OI-111` *"largely closes"*. ⛔ **It does not restore anything
to this endpoint**: that write is part of check-in (`FR-519`–`FR-528`), and this read happens at the
scan **before** check-in. So the shared half still answers less than §2.4 assumed — **for a different
reason than §2.4 gives.**

#### E · Handed to `[API]`

⛔ **This pack is `P-54`'s input and decides nothing.** The re-home / fold / split-by-source /
re-specify choice stays `[API]`'s (§3.2, `P-50`, `P-54`). What it establishes for that decision:

- **Nine of fifteen fields are local or unservable from the shared schema** (B), so **splitting by
  source leaves `FlatWire` serving the majority of the response** rather than a residue.
- ⛔ **Option 2's premise needs re-checking before it can be chosen** — F1 and F2. The service it
  names cannot serve the half it is named for.
- ✅ **AC 3 is satisfiable for three of the four requirements by any build outcome.** ⛔ **`Q24`'s
  station switching is not**, until `OI-33` is answered too (F5) — so if `[API]` records *"why it does
  not need an endpoint"* for any of the four, that one is the candidate, and `OI-33` is the reason.

**Status.** ✅ **Completed 10 September 2026.** ⛔ **`Step-2` remains `Blocked`** — this step supplies
the evidence, and `P-54` is still `[API]`'s to record.

---

### Step-2 — `[API]` records the `P-54` outcome

**Scope.** One decision, in writing, in `[API]`. ⛔ **No code** — the card's AC 1 puts this before
any build, and `P-50` forbids a plan inventing the answer.

**Implementation.**
- The outcome is recorded where the contract lives: the *"this endpoint has no host"* warnings at the
  head of `[API §4.3]` and `[API §4.20]` are replaced by the choice, and `[API §3.1]` gains the
  `/rod/**` owner — or both sections go.
- ⚠ **Four outcomes are in play, not three**, and §3.2 sets out which build path each takes.
  ⛔ Do not treat the card's list and `P-54`'s list as the same three.
- `P-170` is this plan's **recommendation into** the decision — if it folds, it folds into
  `PayoffStaging` and not `CheckIn`, because these are pre-check-in reads and check-in is the commit
  that follows (§2.2). ⚠ **It is not a substitute for the decision.**
- ⚠ **Do not start from "a new controller because it is tidiest"** (§2.1). `P-53`'s reasoning — rod
  receiving is not shopfloor — argues *against* a dedicated rod controller in a shopfloor service.
- If the outcome is **split by source**, the record must say which fields FlatWire serves and which
  come from `CoilCheckin` — **per field** (§2.4), because `OI-111` moved that line.

**Dependencies.** **`Step-1`** is its input — the pack is what the decision is taken against.
⚠ **Not a hard gate:** `[API]` may decide without it, and if it does, `Step-1` is still owed for
AC 3.

**Acceptance criteria.**
- [ ] `[API]` carries the outcome **in writing, before any code** (card AC 1)
- [ ] The record names one host, or states that the two sections are withdrawn
- [ ] If split by source: the field-by-field division between FlatWire and `CoilCheckin` is stated
- [ ] ⛔ Nothing is minted in this plan that `[API]` has not recorded (`P-50`, `P-54`)
- [ ] §3.2's router is updated to name the chosen branch, so the next developer reads one path

**Status.** ⛔ **Blocked** on `P-54`, which is `open`. ⚠ **This step is the story's only blocker** —
every other step is dependent, not blocked.

---

### Step-3 — ⚠ The re-specify branch: close the story and release the hours

**Scope.** `[API §4.3]` and `§4.20` withdrawn or rewritten, the four requirements given another home
or a recorded reason they need none, `[TRP]`'s DB2 walkthrough corrected, and the story closed as a
**specification change with no build** (card AC 5). ⛔ **Exclusive with `Steps 4`–`8`** — if this
step runs, none of them does.

**Implementation.**
- `[API §4.3]`/`§4.20` updated, and `[API §3.1]`/`§3.2` then carry **no `/rod/**` owner by decision
  rather than by omission.** ⚠ That distinction is the entire finding this story was raised on —
  record it explicitly, or the hole reappears as a defect the next time someone reads the contract.
- Each of `FR-042`, `FR-064`, `FR-043`'s carry-forward gate and `Q24`'s station switching gets
  another home **or** `[API]`'s recorded reason it needs none (card AC 3). **`Step-1`'s pack is the
  checklist**, which is why that step is owed even on this branch.
- ⚠ **`[TRP]`'s DB2 scans a rod** (§6). A re-specify reaches **the trial plan and not only the
  contract**, so the trial's rod-scan step is changed or removed there.
- The story closes: front-matter `status: cancelled` and `hours: 0` per the precedent
  [`FW-001`](../../FS-03-database-foundation/DB/FW-001.md) and `FW-002` set under `D-32`, with the
  reason in `status_note:`. Then `python tools/build_status.py` **and**
  `python tools/build_features.py`.
- A row in `[TB §7]` recording it as a specification change. ⚠ **`[TB §7]` and the `.xlsx`
  deliverables are not on the automatic path** — the card owns the hours, so **report** the release
  rather than silently re-costing the card.
- A row in [`CHANGELOG.md`](../../../../CHANGELOG.md).

**Dependencies.** **`Step-2`**, and only if its outcome is **re-specify**.

**Acceptance criteria.**
- [ ] Neither `[API §4.3]` nor `§4.20` still stands specified-with-no-host
- [ ] All four requirements name another home or carry `[API]`'s recorded reason (card AC 3)
- [ ] `[TRP]`'s DB2 rod scan reflects the change
- [ ] `[TB §7]` records a specification change and the **6 h released** (card AC 5); front-matter
      follows the `FW-001`/`FW-002` cancellation precedent and both boards are **regenerated, not
      hand-edited**
- [ ] ⛔ `git diff` over `Second-Branch/ual-api` is **empty** — this outcome ships no code

**Status.** 🔗 **Dependent** on `Step-2` · ⚠ **exclusive with `Steps 4`–`8`.**

---

### Step-4 — The `Rod` request/response types

**Scope.** `FlatWire.Domain/Models/Rod/` — the response shapes for `[API §4.3]` and `[API §4.20]`,
and nothing else. ⛔ No controller, no service, no validator, no mapping. Authored here and
**inherited** by whichever story fills the body (`P-52`, as `FW-232` §3 step 4 does it).

**Implementation.**
- Two types: the **rod detail** (`[API §4.3]`) and the **order set** (`[API §4.20]` — `rodAlpha`,
  `netWeightLb`, and `orders[]` of `orderNo`, `relLetter`, `orderSeqNo`, `rodSeqNoInOrder`,
  `allocatedWeightLb`, `rodWeightFrom`, `rodWeightTo`, `pinRole`, `rodKind`, `source`).
- ⚠ **One of the two depends on `Step-2`'s outcome and the other does not.** The order set is the
  same shape under every build outcome. The rod detail is **not**: under **fold** or **a new
  controller** it is `[API §4.3]`'s full body including the five required additions; under **split
  by source** FlatWire serves only the fields it alone can serve and `CoilCheckin` serves the rest
  (§2.4). ⛔ **Do not pick one — read the record.**
- ⭐ **The rod detail carries the order set, not a scalar order.** `[API §4.20]` says so in terms:
  *"`GET /rod/{alpha}` returns the order set too … a consumer reading a scalar order from that
  response is reading a rod that happens to have one"* (`Q70`). **Two types, one composing the
  other** — not two disjoint shapes.
- The five additions are **required, not optional** (`[API §4.3]`'s own table). ⚠ `orderId` is
  `null` for a rod planning has not allocated, and **such a rod cannot be staged** — so the type
  must permit the null and the **refusal belongs to the body**, not to the type.
- `stagedPayoffPosition` and `isWelded` are **projected from the current `RodStaging` row** and are
  ⛔ **no longer columns on `Rod`** — a type that models them as rod columns is wrong even though it
  compiles.
- ⛔ **`P-171`:** no type name, member name or enum value implies a **third payoff bay**.
  `stagedPayoffPosition` names a position on `FL1PO`, which FL1 and FL3 **share**; the **line** is
  what varies (`G21`).

**Dependencies.** **`Step-2`** — its outcome decides the rod detail's field set, and the re-specify
outcome cancels this step outright.

**Acceptance criteria.**
- [ ] Both types exist under `FlatWire.Domain/Models/Rod/` per `P-52`, and the solution compiles
- [ ] The rod detail's field set matches `Step-2`'s record, **field for field** against `[API §4.3]`
- [ ] The order set matches `[API §4.20]`'s worked example, `pinRole`, `rodKind` and `source`
      included
- [ ] The rod detail exposes the **order set**, not a scalar order (`Q70`)
- [ ] `orderId` permits `null`; `stagedPayoffPosition`/`isWelded` are modelled as **projections**
- [ ] ⛔ No member name implies an `FL3PO` (`P-171`, `G21`)
- [ ] ⛔ `git diff` touches `Models/Rod/` only — no validator, no read, no mapping

**Status.** 🔗 **Dependent** on `Step-2`.

---

### Step-5 — The service seam: two methods, the stub fixture, and the real shell that throws

**Scope.** The interface the controller will call, its stub, and its real implementation shipping as
a **throw**. ⛔ No route yet, and **no body ever** in this story.

**Implementation.**
- Under the **fold** outcome (`P-170`): two methods added to `IPayoffStagingService` in
  `FlatWire.Domain/Services/IFlatWireServices.cs`, beside the four already there —
  `GetRodAsync(...)` and `GetRodOrdersAsync(...)`, each returning `Task<FlatWireResult<T>>` with `T`
  from `Step-4`. ⭐ **No new service interface is needed on this path** — that is a large part of why
  `P-170` prefers it.
- Under **a new controller**: the same two methods, on whichever interface `[API]`'s record names.
  ⛔ **Do not mint a new interface unless the record does** (`P-50`).
- `StubPayoffStagingService` gains fixture answers — `[API §4.3]`'s `R00041` worked example and
  `[API §4.20]`'s single-order example — so `useMockData=true` serves a real shape while
  [`FW-061`](../FE/FW-061.md)'s `Step-7` is on its mock path.
- `PayoffStagingService` — the real one — gains **throws**, per `P-64`/`P-169`:
  `NotImplementedException` **naming the story that owns the body**, following the four throws
  already in that file exactly.
- ⚠ **Which story to name is not settled, and this step reports rather than resolves it.** §1.1
  points at [`FW-157`](FW-157.md) *and the staging stories* for the check-in reads; the four sibling
  throws all name **`FW-158`**; and `[API §4.20]`'s order set is **`FW-226`**'s (§1.1). Name each
  throw for the story `[API]`'s record or §1.1 assigns, and if `§4.3`'s body has no single owner,
  **say so in §6 rather than choosing silently.**
- ⛔ **An empty success is forbidden** (`P-169`). A shell returning `success: true` with no data is
  indistinguishable from a working endpoint and would let Phase 1A build against a lie; one that
  throws a named exception fails loudly and names its owner.

**Dependencies.** **`Step-4`** — the methods return its types.

**Acceptance criteria.**
- [ ] Two methods on the interface `[API]`'s record names; the solution compiles and DI resolves
      **both** implementations
- [ ] `useMockData=true` → the stub's fixtures, matching `[API §4.3]`'s and `[API §4.20]`'s worked
      examples
- [ ] `useMockData=false` → a `NotImplementedException` **naming a story**, ⛔ not an empty success
      (`P-169`)
- [ ] The exception text names the body's owner per `[API]`'s record or §1.1, and any ambiguity is
      **recorded in §6**, not settled by choice
- [ ] ⛔ No read, no query, no mapping — the bodies are `FW-157`/`FW-158`/`FW-226`'s

**Status.** 🔗 **Dependent** on `Step-4`.

---

### Step-6 — The two routes, on the host `Step-2` chose

**Scope.** Two `[HttpGet]` actions, their explicit routes, the envelope, and `401` unauthenticated.
⛔ **No logic** (`[SVC §3.2]`).

**Implementation.**
- `GET rod/{alpha}` and `GET rod/{alpha}/orders`: class-level base route plus an **explicit
  per-action route** (`P-04`). ⭐ `PayoffStagingController` **already carries two unrelated
  prefixes** — `/payoff/status` and `/staging/**` — and says so in its own remarks, which is why a
  third is no new pattern and why `P-54` names it option 1.
- Under **a new controller**: `FlatWire.API/Controllers/Rod/RodController.cs : UAController`, as
  `FW-232` §3 builds its shell — ⛔ **and it is a new base URL, so Phase 1A's base-URL freeze
  applies** (`FW-132`, `FW-232` §2.3). A base URL discovered after the freeze is a frontend change,
  not a backend one.
- Each action: inject-and-forward per `P-65`, `return Envelope.From(result)`, `FlatWireResult<T>`
  (`P-56`), and `[ProducesResponseType]` for `200` and **`404 ROD_NOT_FOUND`** (`[API §4.3]`,
  `[API §4.20]`).
- `[Authorize]` is **inherited from the global filter, not re-declared on the class.** ⚠ `P-55`'s
  `UseAuthorization()` between `UseRouting()` and `MapControllers()` is already built, **and it is
  the reason the answer is `401` and not `500`** — endpoint routing requires that middleware for a
  route carrying authorization metadata.
- ⛔ **`P-171`:** the route is `rod/{alpha}`, and **nothing in it names a station.** ⚠ `Q24` is
  served by **`scheduledMachineName` on the response** (`[API §4.3]`: *"this drives navigation …
  the client switches to that station and continues"*), so **station switching needs no endpoint of
  its own** — and adding one shaped as *"switch this rod to line FL3's station"* re-opens `G21` at
  the contract level even with the index intact.
- ⚠ **The card's rate basis prices *"route wiring for 3 endpoints"*, and `[API §4.3]` + `§4.20` are
  two.** ⛔ **Do not resolve that by inventing a third:** `Q24` needs none (above), and `POST /rod`
  is upstream and explicitly not this service's (`[API §3.2]` row 9, `P-53`). **Report the
  divergence against `[TB §7]`** — the card owns the hours and this plan does not re-price them.

**Dependencies.** **`Step-5`** — there must be something to forward to.

**Acceptance criteria.**
- [ ] `GET /api/v1/flatwire/rod/{alpha}` and `.../rod/{alpha}/orders` are reachable on the host the
      record names
- [ ] Unauthenticated → **`401`**, ⛔ not `500` (`P-55`), exactly as the existing endpoints answer
      (`FW-138` §6.2)
- [ ] The envelope is `FlatWireResult<T>` — `data` · `success` · `errors` · `errorCode`, camelCase
      (`P-56`)
- [ ] Authenticated with `useMockData=false` → the named `NotImplementedException` (`P-169`)
- [ ] ⛔ Each action body is a forward and nothing else (`[SVC §3.2]`) — no `if`, no mapping
- [ ] ⛔ Nothing in the routes, the action names or the parameters implies an `FL3PO` (`P-171`)
- [ ] If a new controller: it is inside Phase 1A's base-URL set, or the freeze has not yet happened

**Status.** 🔗 **Dependent** on `Step-5`.

---

### Step-7 — `[API §3.1]`/`§3.2` gain the rows, and the count is restated there

**Scope.** The contract of record catches up with the built route. ⛔ **This plan states no endpoint
count** — `[API]` owns it, as `[DBD §6.2]` owns the object counts.

**Implementation.**
- `[API §3.2]`'s **row 8** (`GET /rod/{alpha}`) reads *"⚠ **none** — `RodReceiving` withdrawn
  25 Aug 2026 (`P-53`); re-homing owed (`P-54`)"*. It names the chosen host instead.
- ⛔ **`[API §4.20]` has no index row at all** — its own warning note says so, and that is a second
  and separate gap meeting on the same endpoint. The row is added.
- ⚠ **`§4.21` is missing an index row too, and that one is `FW-232`'s, not this story's.** ⛔ Do not
  fix it here — **report it**, or the two stories start editing the same table.
- The *"no host"* warnings at the head of `[API §4.3]` and `§4.20`, including *"Do not build it into
  `FlatWire` on the strength of this section"*, are replaced by the record. ⚠ **Those two sentences
  are the reason a developer would refuse to build the route** — leaving them standing above a built
  route is worse than the original hole, because the contract would then contradict the code.
- ⚠ **The count moves in `[API]` and nowhere else.** ⛔ Neither this plan nor `[TB §7]` is its home.

**Dependencies.** **`Step-6`** — there must be a route to index. ⚠ `[API]` *may* write the rows
earlier, but then the contract and the code disagree until `Step-6` lands, which is the exact state
this story exists to end.

**Acceptance criteria.**
- [ ] `[API §3.1]` names the `/rod/**` owner; ⛔ no row still reads "none"
- [ ] `[API §3.2]` carries a row for `§4.3` **and** a row for `§4.20`
- [ ] Both *"no host"* warnings are gone, and what replaces them names the host
- [ ] The endpoint count is restated **in `[API]`**; ⛔ neither this plan nor the card states one
- [ ] `§4.21`'s missing row is **reported to `FW-232`**, not fixed here

**Status.** 🔗 **Dependent** on `Step-6`.

---

### Step-8 — The QA0 walkthrough, the regression baseline and the paper trail

**Scope.** §5's verification table executed, the untouched surface proven untouched, and the record
closed. ⛔ Build outcomes only — `Step-3` closes the re-specify branch itself.

**Implementation.**
- §5's table, top to bottom, against the deployed service — `[DEP §2]`'s `test1`, on the shared
  instance **`DEV00164-001`** (`CLAUDE.md`), not LocalDB.
- The regression: **the other 14 controllers and 22 endpoints answer exactly as before, and 61/61
  still pass** (§5). ⚠ **Re-measure both numbers rather than repeating them** — and note that once
  these routes land, *"22 endpoints"* describes **the untouched ones**, not the service. That is
  precisely why `Step-7` restates the total in `[API]`.
- The **`P-171` audit**, recorded: the route strings, the action signatures **and** the DTO members —
  all three read for anything that implies a third payoff bay.
- The paper trail: §3.1's board updated with what was built **and what was skipped**, the front-matter
  `status:` moved, then `python tools/build_status.py` **and** `python tools/build_features.py`.
  ⛔ Never hand-edit `STATUS.md` or `FEATURES.md`.
- A row in [`CHANGELOG.md`](../../../../CHANGELOG.md) — the only place history lives.
- ⛔ **`[TB §7]` and the `.xlsx` deliverables stay off the automatic path.** Report divergence; do
  not sync them.

**Dependencies.** **`Step-7`**.

**Acceptance criteria.**
- [ ] Every row of §5 is executed and recorded, **including the two "no count here" rows**
- [ ] The other 14 controllers and 22 endpoints are unchanged; 61/61 pass — **measured, not quoted**
- [ ] The `P-171` audit is recorded across routes, signatures and members
- [ ] `CHANGELOG.md` carries the row; both boards are **regenerated** from the front-matter
- [ ] What was **skipped** is written down — the body owners, and every open item this story hands on

**Status.** 🔗 **Dependent** on `Step-7`.
