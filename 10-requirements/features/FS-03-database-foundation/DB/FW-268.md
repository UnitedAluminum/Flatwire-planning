---
id: FW-268
legacy_id:
title: Reconcile ToolingInventoryEdger with the client's edger grid when Q95 returns
status_note: "**DONE 10 September 2026 — `Q95` returned and all five legs were applied.** `EdgeType` is `NOT NULL` again and widened to `varchar(20)` with a **three**-value CHECK (`Natural Round Edge` is excluded — no set is ground for *no edging*); the natural key `UQ_ToolingInventoryEdger_Set (MachineName, SetNumber)` is taken with both columns `NOT NULL`; `GrooveNo` is dropped; `AllocatedPosition (E1/E2)` is added beside `Location`. ⚠ **Re-priced 4 h → 8 h**: the answer was wider than the card assumed, adding a column and a vocabulary split the ACs did not anticipate. ⚠ **`SerialNo` KEPT** — leg 1 is only partly answered (*'I would need to verify with engineering'*), so `G104` stays open on that column alone. ✅ **Object counts UNCHANGED at 47 · 69 · 90**, measured from a teardown-and-deploy on `DEV00164-001`, and every constraint proven by insert."
status: done
status_confirmed: true
owner:
jira:
mvp: 1
phase: "1C"
stream: DB
streams: [DB]
priority: medium
hours: 8
sprint: S2
depends_on: [FW-251]
blocked_by: []
has_plan: true
started:
completed: 2026-09-10
---

# FW-268 - Reconcile ToolingInventoryEdger with the client's edger grid when Q95 returns

> **No implementation plan has been written for this story yet.**
> The card below is the contract from `[TB]`. Before starting, replace this notice
> with the sections in the task template: *What to build* / *Context you need* /
> *Build order* / *Decisions made here* / *Verification* / *Handoff*.

## 1. What to build

**Hours:** ~~4 h~~ **8 h** DB · **Priority:** Medium · **Sprint:** S2 · **Phase:** 1C · **Stream:** DB

> ✅ **`Q95` RETURNED ON 10 SEPTEMBER 2026 AND THIS CARD IS DONE.** Every leg below is applied and
> measured. ⚠ **Re-priced 4 h → 8 h** — the answer was wider than the card assumed: it added a
> column (`AllocatedPosition`), split one vocabulary into two of different sizes, and withdrew the
> life-model arithmetic. ⚠ **One leg is only partly answered**, so `G104` survives on `SerialNo`.

**As a** developer building the Tooling Inventory tab,
**I want** the edger register to carry only fields the client actually has,
**So that** the roll shop is not asked for a serial number that does not exist.

> **The table is already built. This story is the reconciliation, not the build.**
> `ToolingInventoryEdger` and `ToolingInventoryEdgerGauge` landed 6 Sep 2026 with `D-53`, absorbing
> `Edger`. ⭐ **Unlike `FW-259`, the client's grid DID exist and WAS followed** — all fourteen
> columns, in order. This card is the narrower residue: four columns that are **ours** because the
> grid is silent on them (`G104`), and the counted baseline lives only in `[DBD §6.2]`.

**Acceptance Criteria:**
- [x] **`Q95` q1 applied — `SerialNo` KEPT, and this is the one leg still open.** The client:
      *"The edgers are designed by UA and then sent out for production … **This is not to say that
      we can assign a SN, however I would need to verify with engineering**."* So a serial is
      *possible*. ⛔ **The column and its filtered-unique index stay, and stay `[PROPOSED]`** — the
      drop-it branch does **not** fire, and `G104` narrows to this column alone.
      *(Card's original wording:)* **`Q95` q1 applied — `SerialNo`.** If edger sets carry no serial, **drop the column and its
      filtered-unique index** rather than leaving it permanently empty. ⚠ Dropping it moves the
      index count — pair with a recount
- [x] ⭐ **q2 applied — THE NATURAL KEY IS TAKEN: `UQ_ToolingInventoryEdger_Set (MachineName, SetNumber)`.**
      The client identifies a set by an **encoded** number — `400-S` / `401-S` / `402-S` / `400-R` /
      `400-B`, stem = gauge band, suffix = profile — so it is meaningful and **shop-unique**.
      ⚠ **BUILT DIFFERENTLY FROM THIS CARD, AND DELIBERATELY.** The card said make it **filtered**,
      *"since `SetNumber` is nullable, which makes it an index statement and moves the count"*.
      Instead **both key columns were made `NOT NULL`** — the seed already populates both and
      edgers are FL2-only — and the key is a **table `UNIQUE` constraint**. That is count-neutral,
      so ✅ **the index figure did not move**, and it avoids the trap the card's route walks into:
      a `CREATE UNIQUE INDEX` beside a constraint is the exact duplication that moved the figure
      `91 → 90` when `IX_SpoolTraceability_SpoolAlpha` was deleted as *"an exact duplicate"*.
      ⛔ **Do not add a second unique index on these columns.** `Name` and the alpha stay removed.
      *(Card's original wording:)* **q2 applied — `SetNumber` scope, and THE NATURAL KEY ADDED WITH IT.** ⚠ **This is the most
      important leg of the card.** `ToolingInventoryEdger` has **no unique constraint at all** today
      — `Name` (6 Sep) and `EdgerToolAlpha` (7 Sep) were both removed because nothing read them
      (`G104`) — so **two identical rows are possible**. Add `(MachineName, SetNumber)` as `UNIQUE` if the
      set letter is unique per line, or `(SetNumber)` if per shop. ⚠ **Filtered**, since `SetNumber`
      is nullable, which makes it an index statement and **moves the count** — pair with a recount.
      ⛔ **Do not answer it by reinstating `Name` or an alpha**
- [x] **q3 applied — `GrooveNo` DROPPED.** Rolls are identified *"by groove gauge range and
      profile"*, not by position across the roll face, so the column asserted a fact that does not
      exist — which is exactly what this leg said the answer would decide. `(EdgerToolId, GaugeIn)`
      already identifies a groove. ⚠ **Confirm whether the client's *"different grove heights"* is
      exactly `GaugeIn`** before adding any height column; on the present reading they are one
      measure under two names
- [x] ⛔ **q4 applied — AND IT WENT THE WAY THIS CARD FEARED. `.100` IS ARBITRARY.** The client:
      *"**This was an arbitrary number that I entered.** I will need to verify with engineering what
      the actual standard would be. **This could also be variable depending on the groove width
      range**."* So the *"≈ twelve grinds"* reading is **withdrawn as fact** at the DDL, `[DBD]`,
      the schema document and `G77`. ⛔ **No screen may display a remaining-grinds or remaining-life
      figure** until engineering supplies the standard. The column keeps `.100` as shape only.
      ⚠ Whether it belongs on the **groove** rather than the set depends on an answer the client
      does not have — **not moved speculatively**.
      *(Card's original wording:)* **q4 applied — `.100 STD Removal From OD`.** ⚠ **This one is silent if wrong**: the remaining-life
      arithmetic on the tab reads it as *per grind* (`6.00` → `4.75` ≈ twelve). Confirm before any
      screen displays a remaining-grinds figure
- [x] **q5 applied — `EdgeType` IS `NOT NULL` AGAIN, AND THE VOCABULARY SPLIT IN TWO.** A set **is**
      classified by profile: *"edger sets are distinguished from one another by gauge band and
      profile (edge type)"*, *"a set will NOT share different profiles"*, profile *"per set"*. So
      `EdgeType` returns to `NOT NULL` — settling `D-53`'s relaxation limb exactly as `D-53`
      provided for — and widens to `varchar(20)`, because **every new value overflows
      `varchar(10)`** (`Square Edge` 11, `Full Round Edge` 15, `Natural Round Edge` 18).
      ⭐ **`CK_TIE_EdgeType` takes THREE values where `CK_PSC_EdgeType` takes FOUR:**
      `Natural Round Edge` means *no edging*, so no set is ground for it, and the client's own roll
      table carries no such row. ⛔ **The two must not be merged or aligned** — they now differ in
      **cardinality**, which makes the DDL's existing *"do not merge them"* load-bearing.
      ✅ **`CK_PSC_EdgeTypeReq` needed no change**, proven by insert: a *no-edging* schedule leaves
      the `EdgeSet` component `Bypass`, which `CK_PSC_State` already expresses.
      ⚠ **`EdgerType` was NOT folded in** — the client never said the grid's `Type` cell is the edge
      profile, and merging on inference is the `G87` mistake. It stays ours and unreconciled.
      *(Card's original wording:)* **q5 applied — `EdgeType` and `EdgerType`.** If the grid's `Type` cell **is** the edge profile,
      **fold `EdgerType` into `EdgeType`** and drop the spare column. If a set is ground for one profile
      only, return `EdgeType` to `NOT NULL`. ⚠ **Check `CK_PSC_EdgeTypeReq` first** — the schedule
      already carries the requirement. ⚠ **Do not align with `ToolingInventoryDie.DieType`** — same
      grid heading, different meaning
- [x] ⚠ **`[PROPOSED]` restated, not removed wholesale** — `Q95` **is** half-answered on leg 1, so
      `SerialNo` keeps its marker and `EdgerType` keeps its *"ours"* note; `GrooveNo`'s marker went
      with the column
- [x] ✅ **`verify_schema_counts.py` green, and the counts did NOT move: 47 tables · 69 FKs · 90 index
      statements · 1 procedure · 1 trigger**, measured from a **teardown-and-deploy** on
      `DEV00164-001`. ⛔ **Teardown was mandatory, not hygiene:** every script guards its objects
      with `IF NOT EXISTS`, so an incremental `RunAll` would have reported success and changed
      **nothing** — no widened column, no new CHECK values, no dropped `GrooveNo`. ✅ **Sample data
      loaded clean**, and every new constraint was proven by insert: old `Square` rejected;
      `Natural Round Edge` rejected on the tool and **accepted on the schedule**; duplicate key
      rejected; `AllocatedPosition 'E9'` rejected; `Retired` rejected; `Inactive` + `E1` accepted
- [x] **`G104` narrowed to `SerialNo` alone** — the key, `GrooveNo` and `EdgeType` are all resolved,
      and its recorded live consequence (*"two identical rows are possible"*) no longer holds.
      ⛔ **`G77` is NOT closed by this** — the straightener half is untouched, and `G77`'s life-model
      arithmetic was **withdrawn** rather than discharged. ⛔ **`Q95` stays `Open` on leg 1**

**Rate-card basis (§2):** not a rate-card unit — a narrower reconciliation than `FW-259` because the
grid exists and was followed; four columns across four sites with the guard as its test = **4 h**
**Dependencies:** FW-251
**Blockers:** **`Q95`** (the five legs), **`G104`**
