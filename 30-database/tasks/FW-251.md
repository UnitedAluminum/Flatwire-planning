---
id: FW-251
legacy_id:
title: Restate the schema baseline to 40/64/86 and repair the DB cards the die split, the reason codes and the roll set left stale
status: not-started
status_confirmed: true
owner:
jira:
mvp: 1
phase: "1C"
stream: DB
streams: [DB]
priority: high
hours: 8
sprint: S1
depends_on: [FW-152]
blocked_by: []
has_plan: false
started:
completed:
---

# FW-251 - Restate the schema baseline to 40/64/86 and repair the DB cards the die split, the reason codes and the roll set left stale

> **No implementation plan has been written for this story yet.**
> The card below is the contract from `[TB]`. Before starting, replace this notice
> with the sections in the task template: *What to build* / *Context you need* /
> *Build order* / *Decisions made here* / *Verification* / *Handoff*.

## 1. What to build

**Hours:** 8 h DB · **Priority:** High · **Sprint:** S1 · **Phase:** 1C · **Stream:** DB

**As a** developer picking up any Phase-1C card,
**I want** the schema baseline stated once and every DB card describing the schema that exists,
**So that** nobody builds against `Drawer`'s 13 die-size rows, which are no longer there.

> ⛔ **Two schema changes landed on 2 Sep 2026 and neither reached the task cards.** The **die
> split** (`Q91`) added `ToolingInventoryDie` and `DieHistory` and cut `Drawer` to the two draw
> boxes `DB1`/`DB2`; the **client's reason-code lists** then added `DowntimeReason`,
> `WipRejectionReason`, `ItInhibitReason` and `LineDowntimeEvent`. The DDL, the six schema
> documents, `[DBD §6.2]` and `[DEP §4.2]`'s gate were all updated. **The cards were not.**
>
> ✅ **`FW-005` WAS the dangerous one, and it is repaired — 3 Sep 2026.** It said *"`Drawer` seeded
> with the **13 size rows** (`DIE-0210`…`DIE-0340`) … this is what MVP-1's die change validates
> against"* and *"`Drawer.Id 1–13`"*; both were false and a developer following that card would have
> rebuilt the table the split took apart. Both lines are now **struck in place with the reason**,
> rather than deleted, so a developer who had already read them sees what changed. ⚠ **Done as a
> side effect of the roll-set pass** (`D-42`), which added a **twelfth** Lookup table and would
> otherwise have made the card more wrong. The rest of this story still stands.

**Acceptance Criteria:**
- [ ] ⚠ **State no count in this story.** `[DBD §6.2]` is the only site that defines the baseline and it is **already correct** at **40 tables · 64 FKs · 86 index statements · 1 procedure · 1 trigger** (39/62/82 until `ToolingInventoryRollSet` landed on 3 Sep 2026, `D-42`) — `verify_schema_counts.py` passes today. This story repairs the *other* sites, it does not restate the baseline
- [ ] `verify_schema_counts.py`'s **24 advisory `C6` findings** resolved — each either restated by citation or marked as dated audit trail: `MasterSpecification.md:88` and `:1388`, `FW-152.md:282`, `30-database/tasks/Orchestration.md:439` and `:442`, `FW-142.md:243`, `TrialOrchestration.md:366`, `CapacityAndEffortModel.md:433`, `Decisions.md:889`, `tools/deliverables/README.md:42`
- [x] ✅ **`FW-005` re-pointed 3 Sep 2026** — the Lookup group is **twelve** tables: `Stand`, `Drawer` (**two** rows, `DB1`/`DB2`, capped structurally by `CK_Drawer_Name` + `UQ_Drawer_Name`), `ToolingInventoryDie` (**14** rows), **`ToolingInventoryRollSet` (six rows)**, `Edger`, `Dancer`, `Spool`, `AlloyProperty`, `PayoffPosition`, `DowntimeReason`, `WipRejectionReason`, `ItInhibitReason`. Its `Drawer.Id 1–13` line and its *"catalogue the die change validates against"* line are both struck in place, and the rate-card basis is flagged as predating all three scope events
- [ ] **`FW-007` and `FW-171` re-pointed:** the in-run event group is **seven** tables, not five — `DieHistory` and `LineDowntimeEvent` are new — and `DieChangeEvent` carries **`OldDieId` / `NewDieId`**. `FW-171`'s AC 4 read of *"`Drawer` (size, `LastGrindingFeet`, `TotalFeetAllowed`)"* now reads `ToolingInventoryDie`
- [ ] **`FW-176` re-pointed:** `CK_WipRejection_Group` is **dropped** and the group moved onto the `WipRejectionReason` row. ⚠ Record `G79` on the card — **all 72 group values are ours**, assigned on 2 Sep 2026, and the client's sheet has no grouping at all
- [ ] ⚠ **The three reason tables are seeded inline in `01_Lookup`, not in sample data** — 156 rows of **production reference data**, so `RunAll` alone leaves them populated. Any card or document implying they arrive with sample data is corrected. ⚠ `G85` records that `Stand`, `Drawer`, `Dancer` and `Edger` are *still* in the sample-data file and have the same problem — **out of scope here**, and named so it is not read as fixed
- [ ] A **14th** die is seeded because `DC-0001`'s worked example always referenced a 0.2980 die the 13-row catalogue never held. The pre-existing seed defect is recorded on the card, not silently absorbed
- [ ] `verify_schema_counts.py` green afterwards, with `C6`'s advisory count at **0** or every survivor deliberately dated

**Rate-card basis (§2):** not a rate-card unit — a card and count-site repair across ten files with the guard as its test, priced at one script-class deliverable = **8 h**
**Dependencies:** FW-152
**Blockers:** — *(`G79` and `G85` are recorded by this story, not resolved by it)*

---
