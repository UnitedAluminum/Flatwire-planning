---
id: FW-199
legacy_id:
title: Alloy tolerance values and die-life thresholds
status: not-started
status_confirmed: true
owner:
jira:
mvp: 1
phase: "13"
stream: BA
streams: [BA]
priority: critical
hours: 4
sprint: S3
depends_on: []
blocked_by: [OQ-22, OI-12]
has_plan: false
started:
completed:
---

# FW-199 - Alloy tolerance values and die-life thresholds

> **No implementation plan has been written for this story yet.**
> The card below is the contract from `[TB]`. Before starting, replace this notice
> with the sections in the task template: *What to build* / *Context you need* /
> *Build order* / *Decisions made here* / *Verification* / *Handoff*.

## 1. What to build

**Hours:** 4 h BA · **Priority:** Critical · **Sprint:** S3 · **Phase:** 13 · **Stream:** BA

**As a** business analyst,
**I want** the outstanding alloy values chased and the die-life thresholds settled,
**So that** `CHK007` can finally fire.

> ⛔ **`OI-12` ESCALATED TO A LIVE DEFECT ON 2 SEPTEMBER 2026, AND THIS CARD IS ITS OWNER.**
> Until the die split (`Q91`) the two band pairs were harmless — Die Change read `Drawer`'s
> 13-row size catalogue with **60 / 85 %** bands, and Die Management was MVP-2 with **65 / 80 %**.
> The die domain is now **one table in MVP-1**: [`FW-073`](../../50-frontend/tasks/FW-073.md) and
> [`FW-253`](../../50-frontend/tasks/FW-253.md) both read `ToolingInventoryDie.LastGrindingFeet`
> against `TotalFeetAllowed`, **so the same die shows two different warning states depending on
> which screen you open it in.** The old *"only the 60/85 % Die Change bands apply in MVP-1"*
> escape is gone. **Three cards are blocked on this** — `FW-073`, `FW-252`, `FW-253`.
>
> ⛔ **AND THE SEQUENCING IS NOW WRONG: this card is `S3` and two of the three it blocks are
> `S2`.** That is recorded rather than fixed here — moving a story between sprints is a delivery
> decision and `[TB §7.2]`'s sprint cells feed three client `.xlsx` generators.
> [`FW-258`](FW-258.md) surfaces it with the rest of the die domain's return to MVP-1.

**Acceptance Criteria:**
- [ ] ⛔ **OQ-22** closed — width, height, diameter and ovality tolerance values received by e-mail **and** signed off by Process Engineering
- [ ] ⛔ **`OI-12` decided — ONE threshold pair, not two.** 60/85 % or 65/80 % (or a third figure), stated once and cited by both screens. ⚠ Whichever wins, the loser's specification section is corrected rather than left as an alternative
- [ ] Die-life threshold configurability decided (**`OQ-83`** die-life tracking is decided; the threshold is not). ⚠ **`ToolingInventoryDie.TotalFeetAllowed` is seeded `NULL` on purpose** and is `NULL`-able in the schema — *"do not seed an invented limit"* — so the screens must render a die without a threshold, and this card decides whether that stays true in production
- [ ] ⚠ **Per-tool changes the question.** A **reconditioned** die is meant to carry a *lower* limit than a new one, and there is deliberately **no `CHECK` that `LastGrindingFeet <= TotalFeetAllowed`** because *overdue* is a real operating state. Decide whether the threshold is per tool, per size, or per die type
- [ ] **OI-77 / OI-43** progressed — edger profiles and roll spares. ⚠ **`G77`** now records that edger *and straightener* tooling inventory have no schema at all, and the client's Tooling Inventory tab returns **four** tool types (`D-42`). ✅ **`OI-77`'s ROLL half is partly answered** (3 Sep 2026): rolls **are** tracked, `ToolingInventoryRollSet` is built, condition and location are columns, and there is **no footage counter** because a roll is reground to a minimum OD. ⛔ Still owed on it: regrind turnaround, in-house or out, whether *"refurbished"* is the edger's *In Grinding*, and **the entire edger-blade-profile half**
- [ ] Outcomes recorded in the register

**Rate-card basis:** BA / Ops liaison 4 h (§2). ⚠ **Not re-priced** for `OI-12`'s escalation — [`FW-258`](FW-258.md) owns any movement
**Dependencies:** None *(blocks `FW-073`, `FW-252`, `FW-253`)*
**Blockers:** ⛔ **OQ-22 — this also blocks Phase 4.** Tim confirmed the tolerances exist and would send them; he did not have them to hand. **Nothing is seeded until they arrive**, and the Dashboard 2A mock map must not be used as a substitute

---

**Phase 13 reconciliation** — FE `20+12+12+12 = 56` · BE 32 · DB 8 · RT 4 · dev base **100** · BA 4 → QA `0.20 × 100 = 20` → Cont `0.15 × (100+4+20) = 19` → **143 h** ✓ (§3b MVP-1 carve)

---

##### S3 · Phase 14 — Integration Testing, PLC Commissioning & Go-Live

**Spec:** [`phase-14-integration-testing-plc-commissioning-golive.md`](../phases/phase-14-integration-testing-plc-commissioning-golive.md) · **Owner:** QA + BA · **267 h** (QA 112 · RT 40 · BA 40 · FE 16 · BE 16 · DB 8 · cont. 35)

> **Not deferrable — but S3 cannot hold it.** 267 hours alongside Phases 12 and 13 in 8 working days is the single worst compression in the plan. **UAT and stakeholder sign-off cannot begin the day feature work completes.** `CapacityAndEffortModel.md` §7 recommends pulling this into a dedicated post-feature-complete window **regardless of team size**.
>
> **No 20% QA uplift is applied here** — this phase *is* the QA phase, and its QA hours are explicit.

---
