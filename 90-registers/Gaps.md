# Flat Wire Mill — Gaps and Issues Register

**Project:** United Aluminum (UAL) — Flat Wire Mill Module
**Last Updated:** September 5, 2026 (fifth pass — measured, not read) — ⚠ **`G100` NARROWED: the half that blocked `11_` is CLOSED, the half that is left is a trap.** `dbo.OPCModules` on `DEV00164-001` now carries all six columns — added by hand, then captured as [`08_CommonDB_OPCModules_ColumnDrift.sql`](../30-database/scripts/08_CommonDB_OPCModules_ColumnDrift.sql) — and `11_` §4a binds clean. ⛔ **Two findings came out of fixing it.** **(a)** `11_`'s `D11` guard **could never fire**: it shared a batch with §4a, and column names bind at *batch compile* time, so `Msg 207` killed the guard along with the batch. It is now §0, in a batch of its own, and proven to abort before the seed batch is sent. **(b)** Adding the columns **removed an interlock** — `CREATE OR ALTER` on `dbo.GetOPCServerAndTagDetails` used to be impossible and now succeeds, while `OPCModulesIdx` 1-4 are **NULL** in both columns and `OPCInfo` maps them as non-nullable `int`. **Backfill before refreshing that procedure** or `GetOPCInfo` throws for four other modules. ✅ **`G100` is off `FW-238`'s `blocked_by`; `11_` is now gated on `G94`/`FW-236` alone.** ⚠ **No gap minted or closed** — `G100` stays **Open** on its procedure half; new gaps still mint at `G101`+. *(previously September 5, 2026 (fourth pass — `D-49`) — ✅ **`G97` IS RESOLVED.** The flat wire lines use the OPC UA endpoint pair at **`OPCServersIdx` 1 and 2**, and **both lines map to both** as `ConnectionSequence` 1 and 2. ⚠ **Two traps in the answer, both caught by measurement:** the ids do **not** match the names — 1 and 2 are `UAOPC3`/`UAOPC4`, while `UAOPC1`/`UAOPC2` are ids 3 and 4 — and it is a **primary/failover pair**, not a per-line split, so `11_` §4c writes **four** rows. All **37** existing `(module, machine)` pairs carry exactly two endpoints and `GetOPCServerAndTagDetails` returns them `ORDER BY ConnectionSequence`; a one-row mapping would have looked healthy until the primary dropped. ⛔ **`11_` is still gated — by `G100` and `G94`.** ⚠ **`G60` amended**, no gap minted or closed beyond `G97`; new gaps still mint at `G101`+. *(previously September 5, 2026 (third pass — `D-48`) — ✅ **`G97` NARROWED AGAIN and taken off the shared-schema axis: `CommonDB.dbo.OPCServers` is a LOOKUP table and flat wire neither inserts nor updates a row in it.** The endpoint is **selected** from the four that already exist — `UAOPC1`–`UAOPC4`, all `SWToolbox.TOPServer.V6` — and joined through `OPCServerApplicationMapping`, so the open question is a pick from a printed list rather than a specification to obtain. ✅ **The `varchar(56)` sub-risk is WITHDRAWN with the insert that created it** — no URL is written to `OPCServerName`, so no widening and no other team. ⚠ `11_` now writes **four** shared tables, not five, gains two guards and loses `@OPCServerClass`; `16_` reverses **nothing** in `OPCServers`, and its old *"delete the endpoint if nothing maps it"* step is recorded as **wrong**, not merely unnecessary. ⚠ **No gap was minted or closed** — new gaps still mint at `G101`+. *(previously September 5, 2026 (second pass — measured, not read) — ⭐ **`G100` minted by EXECUTING `FW-238` against the live instance: `DEV00164-001`'s `CommonDB` is BEHIND `ual-database` source control, and `11_` would have died on it at its first insert.** `dbo.OPCModules` has four columns there against source control's six — no `OPCEventType`, no `EventDurationSeconds` — and the live `GetOPCServerAndTagDetails` does not select them either, so the instance is internally consistent and simply stale. ⚠ **The script is right and the database is wrong**; a sixth pre-flight guard now says so by name. The other four OPC tables match exactly. ✅ **Four rows CONFIRMED live the same way**: `G60` (0 flat wire tags of 496), `G94` (4 modules, all `21316`; 0 prefixed), `G95` (the `IsSystemErrorTag = 1` INNER JOIN is in the deployed procedure) and `G96`'s per-environment re-check (id **6** free; **no row 5 exists here at all**). ✅ **`G97` NARROWED** — `OPCServers` already holds four `SWToolbox.TOPServer.V6` endpoints, so the class is known and the question shrinks to *which endpoint serves FL1/FL2*; ⚠ but the column is `varchar(56)` and an `opc.tcp://` URL can exceed it. *(previously September 5, 2026 (flat wire OPC registration) — **`G95`–`G99` minted and `G93` WITHDRAWN, all from planning `FW-238`.** `D-45` dropped the `TagKey` column, so `G93`'s shared-schema `ALTER` is gone and `G60` is unblocked on that axis. What replaced it is smaller but sharper: **`G95`** — without a system-error tag row `GetOPCInfo` returns an empty list and the whole line is invisible, a symptom identical to `G60` itself; **`G96`** — the `OPCModules` enum is stale and its apparently-free `5` is in production use by the six slitters, so flat wire takes `6`; **`G97`** — no UA endpoint is recorded anywhere, which gates *running* the seed; **`G98`** — the first real paths off a controller carry a `PLC` element none of the 72 derived paths had, fixed as a values-only edit; **`G99`** — `D-47` answers `PLC-Q08` and FL3 turns out to have no controller of its own, so an FL3 push must write to two of them and nothing is designed for that. ⛔ **`G60` gains `G94` as a prerequisite**: flat wire is the first module to select `ual-api`'s `OPCUAManager`. New gaps now mint at `G101`+. *(previously September 4, 2026 (OPC UA tag identity) — **`G94` minted: `OPCUAManager` has no canonical tag identity, five call sites and four formats, and only the subscribe path is right.** ⚠ **Latent, and MEASURED so rather than inferred** — all four `CommonDB.OPCModules` rows are `21316` (`OPCDA`) on `DEV00164-001`, so the UA manager is selected by nothing and **none of it executes today**; and 0 of 496 `CommonDB.OPCTags` rows carry a prefix, so there is no config to migrate and nothing was configured around the bug. ⛔ **`FW-N05` `P-118` protects us from the notification path but NOT from `ReadTag`/`WriteTag`**, which address namespace **0** while the subscription uses **2** — and `D-44` puts the 72 flat wire tag paths into that same `CommonDB` registration (`G93`), on new hardware for which UA is the likely default. ⚠ The notification handler's real output is the debug form `NodeId="ns=2;s=Tag"`, **verified by loading `OpcLabs.EasyOpcUAPrimitives.dll` 5.84.192**, and it parses back **without throwing** into namespace 0 — so `G58`'s swallow is what has kept this invisible. ✅ **Owned by `FW-236`**, which must land it **before** its own `G58` error-surfacing ACs, or those ACs surface a flood of genuine-but-unexplained write failures. ⚠ **No object count moved, no schema changed, and no effort figure re-derived.** New gaps now mint at `G100`+. *(previously September 4, 2026 — **`G93` minted with `D-44`, and `G60` is amended rather than closed.** `D-44` resolved **`OI-A`** in favour of the `CommonDB` `OPCTags` registration, which discharges `G60`'s *"decide `OI-A` first or the tag rows get written twice"* warning and makes its `OPCTags` half **required rather than conditional** — but the table **cannot key a path by logical name**: `OPCTags` is `OPCTagsIdx · TagName · RequestedUpdateRate · IsSystemErrorTag · IsActive`, and the ecosystem never needed more because `CoolingChamber` reads `opcInfo.Tags[0]` positionally. So a nullable **`TagKey`** column, its read path and a home for `P-74`'s confirmation flag are owed by the **shared-schema team**, and `FW-238` fails on an unaltered table. ⚠ **Additive and nullable only — this does not reopen `D-32`.** New gaps now mint at `G94`+. *(previously September 3, 2026 (pass calculator formulas) — **`G90` minted: the client's round→flat width correlation is undefined over part of its input range, and `[PSG §6.4]`'s nominal product sits three points above the floor.** `D-43`'s `F16` goes complex for `T₂ ≥ D₁/2` and implies negative elongation below ≈57–60% thickness reduction; the worked example runs at 62.5%, and **on the square-edge area basis it falls outside the band entirely**. `[PSG §6.3 Step 2]` solves backwards through this relation, so the guard is not optional. **`G91` minted** — the `C₅`/`C₆`/`k` coefficients are **lookup tables** the client will generate from sample runs, and `[PSG §5]`'s `M8` Process Parameter Master does not exist in `FlatWireDB`; `Q93` asks what they are keyed by before anything is built. **`G74` strengthened** — the five missing `PassScheduleComponent` columns are now engine *outputs*, not just fields on a client screen. *(previously September 3, 2026 (tooling) — **`G87` minted: the fourth Tooling Inventory tool type is built from one sentence.** Dies, edgers and straighteners each arrived as a pictured grid with an ordered column list; roll sets arrived as a single sentence, so every column in `ToolingInventoryRollSet` is ours and is `[PROPOSED]` — `Q92` is the send-back. **`G77` amended** to four tool types, and its *"`Straightener` … appears nowhere in this repository"* claim **struck as stale**: the name reached the delay-code lookup on 2 Sep, just not as equipment. *(previously September 1, 2026 (post-build) — ✅ **[`FW-215`](../40-backend/tasks/FW-215.md) BUILT, and four of the five gaps it owned move.** ✅ **`G70` RESOLVED and measured** — the gate is now *"a line model is hosted"* and both hosts satisfy one seam `ISimLineHost`, so with the `OPCConnection` double mapped `/sim` answers **`401` where it answered `404`**; 17 harness checks drive the double directly. ✅ **`G68` RESOLVED server-side** — `GET /sim/config` is built, and **a read was sufficient**: `[SIM §9.2]`'s *"settable"* is the seed's alone, so **no configuration write was built or is owed**. ✅ **`G73` RESOLVED server-side the day it was raised** — `Paused` and `Idle` are commandable via `POST /run`'s target run state, no seventh route. ⚠ **`G68` and `G73` both leave a residual on the BUILT console**, which binds no `/sim/config` and has no Pause control. ⛔ **`G72` NARROWED, not closed** — the literal is gone and binds to `FlatWireRoles`, but the constants are `"TBD"` and the policy is `FW-145`'s; ownership moves to **`FW-145`**. ⛔ **`G69` is now ARMED**: `Paused` can occur, so its latent display fault is reproducible — the row is unchanged and its severity is not. *(previously September 1, 2026 (second pass) — ⛔ **`G73` registered and `G68` corrected; new gaps now mint at `G74`+.** ⛔ **`G73`: `[SIM §9.2]` specifies a `Pause` control and nothing commands it.** The built surface’s only run lever is a boolean mapping to `Running` / `Stopped`, so **`SimRunState.Paused` and `Idle` can never be commanded** and `[SIM §4.4]`’s run-versus-line distinction — the one `FR-141` turns on — has never been demonstrable; the console has no Pause control at all, and it is not among `G43`’s five greyed items either, so nothing recorded the absence. Owner **`FW-215`**, closed by a target run state on `POST /run`. ⚠ **It arms `G69` rather than resolving it** — a chip fed from `SimLineState.Running` starts showing `Paused` as stopped the moment `Paused` becomes reachable, so the two must be read together. ⛔ **`G68` misquoted `[SIM §9.2]` and the misquote pointed at the wrong endpoint:** *"displayed and settable"* is the **seed’s alone**, `lbPerFt` is *displayed*, and the seed’s settable half is already `POST /run`’s parameter — **so a READ closes `G68`, and a runtime `lbPerFt` setter would decide `Q10` / `OI-45` by the back door against `P-271`.** Its two owed amendments (the `[TB §7]` card, the sixth endpoint) are now **made**; the row stays Open until the endpoint exists *(previously September 1, 2026 — **`G72` registered, and `G68` / `G69` / `G70` are now CARDED.** ⛔ **`G72`: the built simulator control surface guards on `Roles = "Engineer,Admin"`, and `Engineer` is not one of `[SEC §8]`'s six roles** - so it will keep returning `403` after `FW-145` issues the claim, with the same symptom it shows today. It must bind `FW-145`'s **`SimulatorControl`** policy instead. ⚠ **`G68`'s *"no story closes it"* is superseded**: the sixth endpoint `GET /sim/config`, `G70`'s widened gate and shared seam, and `G69`'s vocabulary decision are all on the `FW-215` card as of 1 Sep 2026, and `G69` is its `blocked_by`. The three rows stay **Open** - carded is not built. *(previously August 31, 2026 — **`G70` and `G71` registered, both raised by [`FW-217`](../40-backend/tasks/FW-217.md)'s build and neither visible before the real ingest path was actually exercised.** ⛔ **`G70`: the E2E fixture cannot be steered.** `/sim/**` is mapped only when `FlatWireOpc:SimulateOpcFeed` is *true* and its handlers take `OpcFeedSimulator` concretely - and the `OPCConnection` double needs that flag *false*, because that is what registers `FW-N05`'s real ingest. So the double serves each model's default in-spec run and nothing else, and `FW-213`'s twelve behaviours are built, reachable on the objects and unreachable from outside the process. Owner **`FW-215`**. ⚠ **`G71`: FL2's only load cell has no tag key** - the model reports `TraversingTakeup` and the tag surface carries only `Payoff1Weight` / `Payoff2Weight`, so the weight cannot cross the transport; masked downstream by a second, unrelated hole. *(previously August 31, 2026 — **`G67` registered, and it is a RENUMBERING as much as a new row.** The `stagedWeights` eviction gap was raised on 29 Aug 2026 by four Backend plans and by the de-stubbed `CheckInService.cs` under the id **`G62`** — which this register had already given to the eleven stream/folder mismatches, and which `check_docs.py` enforces. It never reached this file at all, so the most cited new gap of that day was unlookupable. `FW-157`, `FW-172`, `FW-174` and `FW-239` now cite **`G67`**; `G62` is unchanged *(previously August 29, 2026 — **`G66` minted with `D-33`**: `DB-S1` becomes a standalone WinForms executable, so *"the console does not resolve when simulation is off"* stops being a build-time fact and becomes an installation procedure. **Severity Low** — the server-side 404 is the actual control and is unaffected, and the EXE ships only where it is installed rather than to every operator machine inside the shop-floor bundle. ⚠ **`G43`'s recorded figures are restated as superseded**: its *"margin 42 → 16 h"* and `FW-214`'s 15 h both predate the 25 Aug re-baseline (**28 h across three blocks, 12 h of it in T2**) and `D-33`'s **+21 h AI-assisted** — read `[TRP §2.1]` for the current position *(previously August 29, 2026 - **`G61`, `G62` and `G63` minted by [`../tools/check_docs.py`](../tools/check_docs.py)** on its first run over the new task files: 21 blocker citations using the retired `OQ-` prefix, 11 stories whose card and plan folder name different streams, and a circular dependency between `FW-071` and `FW-072`. All three were invisible while the story card and its implementation plan were separate documents.)*)*)*)*)*)*)*)*)*)*)*)*)*)*)*
**Document Type:** Gaps and issues register — the `G##` series
**Status:** Active — the live home of the `G##` register
**Owner:** Delivery lead / programme management
**Audience:** Everyone working on the Flat Wire Mill module
**Shortcode:** `[GAP]`
**Part of:** `ProjectPlan/Development/` — index: [README.md](../DOCUMENTS.md)

> **`G1` is the capacity escalation** and `REVIEW.md` calls it the genuine top item in the plan. Phases cite `G##` as blockers; the `OI-##` register is separate and lives in `../10-requirements/MasterSpecification.md` §11.

---

## Known Gaps & Issues Register

Consolidated review findings, prioritised. **Critical** items should be resolved before the owning phase begins; several are already resolved by the confirmed design decisions.

| ID | Gap / Issue | Area | Priority | Impact if unaddressed | Recommended resolution | Status |
|---|---|---|---|---|---|---|
| **G1** | 14 phases (1 platform + 13 workflow phases 2–14) in a ~6.5-week window (Aug 17–Sep 30) with no capacity/effort model | Planning | **Critical** | Phases miss Sep 30; silent scope loss | Delivered: **[`CapacityAndEffortModel.md`](CapacityAndEffortModel.md)** — six delivery streams + roster, a published unit-rate card, per-phase effort **in hours** for all 17 phase specs (**3,727 h**), a working-day capacity model, an ordered descope ladder, and a calibration checkpoint at the Aug-14 gate. Per-phase **Owner** + **Effort** stamped on every phase file (also closes `REVIEW.md` #53) and surfaced in the roadmap nav table; week grid re-baselined to W0/Aug-14 with Labor Day deducted (closes `REVIEW.md` #31) | ✅ **Resolved (Jul 30 2026) for the model — but the finding is worse than the gap stated.** The plan is **3,727 hours against 32 post-gate working days (44 total = 352 h/person)** = **10.6 FTE sustained**, a **10.7-FTE Phase-1 gate**, and an impossible **27.2 FTE in W7**. G1's own recommended mitigation (defer Phase 12 + non-critical 13) is worth **276 h ≈ 0.8 FTE**; the *full* descope ladder recovers only **12%** (448 h), leaving 9.3 FTE. **Two residuals: (1)** the §1 roster is unfilled, so required-vs-available FTE cannot be computed — programme management must complete it; **(2)** the 30 Sep date now requires an explicit decision (staff to ~11 FTE / move the date / cut below critical path — model §7). The escalation, not the model, is the open item **Apportioned 11 Aug 2026 (model §3b): MVP-1 = 3,292 h / 9.4 FTE.** Two corrections landed with it — §3's TOTAL row had said **3,727 h** where its phase rows sum to **3,660** (the 4 Aug Phase-5 descope never reached it), and §4's W4 column carried Phase 5 at its pre-descope 221 h. **The escalation is unchanged:** the ~1 FTE that MVP-2 deferral recovered fell in W2–W3, which were never the constraint, and **W7 is still 24.5 FTE against three days** |
| **G2** | Check-in spans `FlatWireDB` (run/checkin/SPC) + the shared reqsum / `wip_coil_orders` / `actual_start_date` / `wip_stations.coilno` writes + PLC push — **not one ACID transaction**. ⚠ **Narrowed, not closed, by `D-32` (18 Aug 2026):** ~~existing `coils` `INFLAT`~~ leaves the boundary — `FW-002` is cancelled, so `INFLAT` is a `FlatWireDB`-local `Rod.Status` value and the marker is inside the local transaction. **Four shared writes become three, all into existing columns; the boundary still spans two databases and the PLC** | Architecture / Data | **Critical** | Partial failure → inconsistent state; "atomic rollback" claim invalid | Saga/outbox + compensating PLC clear. ⚠ **The "mirror an `INFLAT` marker into `FlatWireDB`" alternative is no longer a choice — `D-32` has made it the only option**, so `OI-39` narrows to the recovery pattern for the three remaining writes; define incomplete-push recovery. The PLC side of the boundary — what is written, in what order, and what the compensating re-clear has to undo — is specified in [`PLCTagSpecification.md`](../20-architecture/PLCTagSpecification.md) §4.5 and §6.3, and tracked there as a project-owned item (§13.2). **Settle `G30` first** — whether the FL3 push crosses a controller boundary determines whether there are one or two failure domains to compensate | Open — **but the boundary now has a shape (15 Aug 2026, `D-29`).** Under tactical DDD **one aggregate = one transaction**: check-in commits the `FlatWireRun` aggregate locally, and everything outside it — the `coils` `INFLAT` write, `planning_routings`' `actual_start_date`, the PLC push — is a **process manager with explicit compensating actions** rather than an implied ACID scope. That is the saga this gap asks for. **It does not close the gap**: `G30` still decides one failure domain or two |
| **G3** | No table persists raw AGC gauge/width readings, yet FL2 historical profile + Gauge Trace / Cut Traceability reports require them | Data / Schema | **Critical** | FL2 profile + reports have no data source | Add a time-series `RunReading` table (footage, gauge, width, ts, in-spec) + retention/rollup; make it a Phase 1/3 deliverable | ⚠ **Half closed 26 Aug 2026 — the table half is DONE.** `RunReading` is built in `04_Runs` (`RunId`, `FootageFt`, `GaugeIn`, `WidthIn`, `SpeedFPM`, `InSpec`, `ReadingTs`), FK'd in `06`, indexed `(RunId, FootageFt)` as `IX_RunReading_RunId_Footage` in `07`, seeded, and delivered by `FW-007` in Phase 1C — `phase-01c` has recorded it as built since 14 Aug 2026 while this row still read `Open`, and asked for *"the register, not the schema"* to be reconciled. **The retention/rollup policy half is still open and has no owner** — an unbounded 10 Hz time-series table with no defined trim, which is the part that bites in production, not in the build |
| **G4** | Story→phase coverage not provable | Traceability | High | Backlog items silently dropped | Added **Appendix C** (all 58 FW-### → phase + deferred flag) | ✅ Resolved |
| **G5** | Rod source-of-truth ambiguity (new `Rod` table vs `coils`) | Data | High | Conflicting rod status | Use existing `coils` as single source of truth; drop `Rod` table | ✅ Resolved (decision 3). *Note (Jul 29 2026): the DDL/ERD still keep `Rod` as a FlatWireDB-local master under the later "Hybrid foundation" decision — see G12. The two provisional pre-check-in columns that hung off it (`StagedPayoffPosition`, `IsWelded`) are now **retired** in favour of `RodStaging`.* |
| **G6** | Roles (Operator/Ops Mgr/Maintenance/Supervisor/Admin) not confirmed as existing JWT roles vs new. ⚠ *This description lists **five**; `[SEC §8]` has **six** — QA is missing, the same defect the `FW-145` story card carries* | Security | High | Auth may block build | Confirm role/claim mapping in `Login`; add a provisioning story if new | ✅ **Largely resolved (15 Aug 2026)** — the six roles **do** exist as JWT claims, carried on the standard `ClaimTypes.Role`. **No provisioning story is needed**, `RequireRole()`/`[Authorize(Roles=…)]` read them natively, and `UA.Framework.Common`'s `JWTClaims` needs no addition. ⚠ **Residual open:** the six claim **values** are abbreviated or coded rather than `[SEC §8]`'s labels, and the mapping has not been supplied. It gates **verification, not construction** — see `FW-145` §5 and `FW-177` §3.1, where a wrong value fails *closed* in the first and **silent** in the second |
| **G7** | Mid-run checkout supervisor approval relies only on transient SignalR | Reliability | High | Approval lost if no supervisor connected; material stuck locked | Durable pending-approval queue + `Notification` fallback; SignalR = live nudge only | Open |
| **G8** | No data-migration deliverable for legacy `FlatLineSetup`/`FlatLineProcessing` | Data | High | Legacy data stranded on drop | Add mapping + migration + validation + drop-criteria deliverable | Open · ✅ **Owned by `FW-247`** (29 Aug 2026). |
| **G9** | NFRs absent (AGC Hz, concurrent clients, latency, reading retention) | Performance | High | Real-time may not scale/perform | Define NFR targets; hub load test added to **QA2** (N clients × 3 lines × cadence). The **end-to-end latency figure** this gap needs is produced by commissioning test **`C8`** (*"AGC feed reaches the screen"*), reproduced with the rest of the sequence in [`PLCTagSpecification.md`](../20-architecture/PLCTagSpecification.md) §11; the AGC publish rate is asked as **`PLC-Q11`** | Open — load test scheduled |
| **G17** | rod→`coils` multiplies cross-DB logical FKs (every `Rod.Alpha` ref) | Architecture / Data | High | No referential integrity; cross-DB joins for traceability/reports | Consistency checks + indexed alpha on `coils`; consider a replicated view / linked server for report joins | Open |
| **G14** *(format half ✅ **CLOSED AND VERIFIED 26 Aug 2026** — `D-29`: `RodAlpha`, `SpoolAlpha`, `RunAlpha`, `CoilAlpha`, `DieAlpha` are **value objects with validating constructors**, so `ROD-00041` is unrepresentable rather than merely discouraged. ⚠ **It was *"closed by design, unverified"* from 15 to 26 Aug 2026, and that is no longer the position.** The closure rested on `FW-207` AC 4 — ***`RodAlpha("ROD-00041")` must throw*** — with all automated backend tests withdrawn the same day (`[TS §1.2]`), so nothing exercised it. **`FW-141` built the six alphas on 26 Aug and `Program.cs` now constructs exactly that malformed alpha at startup (Development) and *requires* the throw**, so a future relaxation of the pattern breaks the boot with a named reason rather than quietly re-opening the drift `PartialRodReCheckin.md`'s worked examples came from. **No QA0 by-hand demonstration is needed for this half any more.** ⚠ **`G21`'s domain half is NOT in the same position** — see that row: it must reject a second rod **with the DB index absent**, which no deployed environment can show. The footage `DECIMAL`-vs-`INT` half remains open, though `Footage` as a dimensioned quantity narrows it)* | Pre-build data inconsistencies: 3- vs 4-item inspection (+M1/M2 ovality), `R#####` vs `ROD-#####`, `FootageFt` INT vs DECIMAL, `CoilOrderPlanId` vs `PlanId` | Data / Contract | High | Rework if resolved late | Stand up a "Pre-Build Decisions" register with owners; resolve before the owning phase | Open — `RodStaging` and Dashboard 2A deliberately use **3 inspection items** and `R#####`, and do not inherit the 4th item; that scopes the gap to check-in but does not close it. **The check-in half still blocks Phase 4:** the DDL builds four inspection columns NOT NULL plus `SpcM1In`/`SpcM2In` and `POST /checkin/rod` supplies none, so **every check-in insert fails as specified** (`REVIEW.md` Tier 1 #5). *(Rehomed here 13 Aug 2026 from `CheckinImplementationPlan.md`, deleted that day.)* |
| **G10** | Real-time deploy prereqs / MessagePack dependency | Infra | Medium | IIS WebSockets off → transport fallback; new client dep the repo doesn't use | Enable IIS WebSockets (added to deploy); treat MessagePack as **measure-first/optional** — batching+decimation is the real win | Open |
| **G11** | Phases 10/12/13 don't follow the full 8-section template | Doc consistency | Medium | Uneven detail vs stated template | Expand 10/12/13 to the full template | Open |
| ~~**G12**~~ | ~~Source artifacts (DDL, `FlatWireSchema_Mapping.md`, ERD) still say `united_db` and include the dropped `Rod` table~~ | Doc consistency | Medium | Conflicting source-of-truth | — | ✅ **Resolved 11 Aug 2026 — the DDL/ERD were right and the plan documents were stale.** Master-spec **`D-04`** (29 Jul 2026, "Hybrid foundation") **retains `Rod`** as a `FlatWireDB`-local master with **enforced** rod-alpha FKs, superseding `../20-architecture/Architecture.md` §13.1 `D-04` and its 21-table figure; `coils` stays the source of truth for the rod *lifecycle*. A clean teardown-and-rebuild confirms the object set, whose **counts are `[DBD §6.2]`'s and are deliberately not repeated here** — this row published `34 tables · 57 FKs · 69 index statements · 1 procedure · 1 trigger` against a *15 Aug 2026* date until 26 Aug 2026, a pairing that never held: the 34/57 figure is the **22 Aug** deploy, superseded three days later by `Q60`'s `SpoolConfiguration` merge, and `Q89` moved the index count on 26 Aug — `D-31` moved the three `PassSchedule*` tables into MVP-1, so there is no longer a "25 for MVP-1 / 28 full" split. *(This row is exactly the hazard it names: it carried "25 / 33" from 11 Aug and was itself a re-copy source. `[DBD §6.2]` is the baseline — quote it, not this row.)* `../20-architecture/Architecture.md` §13.1 `D-04`, `phase-01b` and `phase-01c` were corrected to match. **`G5` closes with it** — the rod source-of-truth ambiguity is what `D-04` settled |
| **G16** | PLC "rollback" wording — OPC writes are not transactional | Architecture | Medium | Misleads implementers | Reword as compensating writes (re-clear tags) — see G2 | **Resolved for wording (Aug 4 2026)** — the compensating re-clear is now stated normatively in [`PLCTagSpecification.md`](../20-architecture/PLCTagSpecification.md) §4.5, and the four interface sections that carried the misleading "rolled back" phrasing are replaced by pointers to it. **✅ Closed 4 Aug 2026.** The residual is gone: `FR-073` no longer says "a single transactional batch" and `FR-074` no longer says "the batch shall be rolled back" — it now states the compensating re-clear directly. **Nothing in the repo now describes the tag batch as transactional or rolled back.** See also **G30**, which decides whether that re-clear spans one controller or two |
| **G29** | **No edger tag path exists on any line, yet edge type is in the push payload.** Four of five sources list *edge type* among the values written to the PLC at check-in acknowledgement, and FM2 carries edgers at **S2 and S3** — its two 6″ stands (`D-20`/`D-26`). But the only edger-adjacent tag in any published map is **`FL1.EdgeSet.Status.Active`** — on FL1, **the one line with no edger**. S2's and S3's edgers have no activation, status or blade-profile path anywhere. The write side is therefore specified to configure equipment the read side cannot address | Data / Integration | **High** | The FL2/FL3 edge configuration cannot be pushed or verified; an edge-type push either silently no-ops or faults at commissioning, and there is no tag to read back to tell which | Paths are **proposed** from the derived naming grammar in [`PLCTagSpecification.md`](../20-architecture/PLCTagSpecification.md) §2.4 (`FL2.FM2.S2.Edger.Status.Active`, `.Edger.Profile`, and the same for S3) so the client has something concrete to confirm or correct — they are **our invention and marked as such**. Confirm with the controls engineer as **`PLC-Q07`** / **Q28**. Overlaps **OI-77** (blade profiles — the reference data these tags would carry) | Open — raised Aug 4 2026 from the PLC tag-surface consolidation. **Narrowed Aug 4 2026 by the roller-size correction:** the overlap with **OI-36** is gone — that item claimed the same stand had no *roll-gap* path either, and it is now closed, because FM2 has three stands and the published map supplied three stations. **The edger half stands unchanged and is the whole of this gap:** no edger has an activation, status or blade-profile path on any line. **Narrowed again Aug 6 2026 in our favour:** the client confirmed the edgers are **inter-stand** — one between S1 and S2, one between S2 and S3, **none on the exit side** (`D-27`) — which is exactly what *"edgers at S2 and S3"* means read as entry-side, so **the proposed `…S2.Edger.*` / `…S3.Edger.*` paths do not need re-deriving**; only confirmation is outstanding. The same call established that the edger is a **roll-former, not a knife**, so whatever the blade-profile tag turns out to carry is a **roll profile** (see **PSG-D24**, `PSG-D26`) |
| **G30** | **FM2's controller namespace on FL3 is undetermined, and it decides what partial failure means.** Every published tag map addresses the finishing stands as **`FL2.FM2.…`** — including the map headed *"FL1 shown, other lines follow the same pattern"*. FL3 is the hybrid route and pushes **FM1 and FM2 in a single batch on one acknowledgement** (`phase-10:35`). So either FM2 is owned by the FL2 controller and FL3 reaches it through the `FL2.*` namespace — in which case **the FL3 push crosses a controller boundary** — or FL3 has its own `FL3.FM2.*` space and it does not | Architecture / Integration | **High** | A two-controller push is exactly the case where "the batch was rolled back" is least true (**G16**): FM1 can take its tags while FM2 refuses, and the compensating re-clear must then span both. The recovery path is being designed without knowing whether there are one or two failure domains | Answer **`PLC-Q08`** / **Q29** before finalising the Phase-4 compensating-write design. **Commissioning test `C5` cannot settle it as written** — *"one acknowledgement configures FM1 and FM2"* passes under either topology — so `C5` needs a step recording which controller(s) were written. Feeds **G2**/**OI-39** | Open — raised Aug 4 2026 from the PLC tag-surface consolidation |
| **G31** | **Several read tags lost their only consumer when DB13/DB14 were descoped.** The published *"used in"* columns attributed most machine reads to the HMI schematic and the trend charts. With both withdrawn (Aug 4 2026), the per-component **roll-gap and status** tags — `FL1.DB1`/`DB2.Status.IsActive`, `FL1.FM1.RollGap` and `.Status.*`, and all three `FL2.FM2.S*.RollGap`/`.Status` — have **no named surviving consumer**, and `FL1.EdgeSet.Status.IsActive` has none at all (see **G29**). The gauge, width, speed, weight, footage and line-state tags are unaffected — they serve the run monitor, the line status board, the checkout gatekeeper and the spool prompt | Integration / Requirements | Medium | Two opposite errors, both silent: subscribing to tags nothing reads wastes scarce commissioning time on paths no screen will reveal as wrong; dropping one that Dashboard 3's machine-status grid turns out to need is a missing value on the run cockpit found after go-live | Resolve each against **Dashboard 3's kept machine-status grid** (`FR-113`) and the component panel. [`PLCTagSpecification.md`](../20-architecture/PLCTagSpecification.md) §3.5 publishes every affected row as **`[NO REMAINING CONSUMER — confirm before subscribing]`** rather than deleting it, so the decision is visible and reversible | Open — raised Aug 4 2026 from the HMI/SCADA descope |
| **G32** | **The FM2 PLC station names in the spec are ours, not the controller's.** The **4 Aug 2026 roller-size correction** established that FM2 has three stands (`S1` 8″, `S2` 6″, `S3` 6″) and that roll diameter is machine data, not part of an address — so [`PLCTagSpecification.md`](../20-architecture/PLCTagSpecification.md) renames the station segment to bare position: `FL2.FM2.S1/S2/S3`. But the controller's **observed** names are `Stand8`, `Stand6S1` and `Stand6S2`, and those three rows were `[CONFIRMED] by observation` — **the smaller of the two places where the specification departs from a path we have actually seen** (see **G33**, which departs from thirteen) | Integration | **High** | Every FM2 read subscription and every FM2 write resolves against a path string we invented. A wrong path **fails silently** — no exception, no screen change, and a check-in that appears to succeed while the mill keeps its previous gaps | Every affected row is **`[PROPOSED]`**. Answer as **`PLC-Q04`** / **PLC-Q04** before the Phase-4 tag-push build hardens the configuration; **commissioning test `C11`** records the path string the controller actually accepted. **If the rename is rejected, reverting is a three-string edit** — the stand count and the diameters are not affected either way. **The observed originals are no longer in the client document** — it may carry no old/new content — and the register entry that used to hold them was withdrawn on 12 Aug 2026, so **the three names printed in this row are now their only home outside git and [`CHANGELOG.md`](../CHANGELOG.md)**. Do not delete them from here | Open — raised Aug 4 2026 with the roller-size correction. Replaces the closed **OI-36**, which asked the *opposite* question (a stand with no path) and turned out to rest on a stand that does not exist |
| **G33** | **The measure segment of every tag is ours, not the controller's — and it departs from thirteen observed strings.** On 4 Aug 2026 the measure names were respecified: **analogues became a single bare segment** (`.RollGap.Current`→`.RollGap`, `.Gauge.Current`→`.Gauge`, `.Width.Current`→`.Width`, `.Footage.Current`→`.Footage`, `.Die.ActiveDiameter`→`.Diameter`, `.Weight.Lb`→`.Weight`, `Speed.FPM`→`Speed`) and **booleans kept a `Status` group and took an `Is` prefix** (`.Status.Active`→`.Status.IsActive`, `.Status.Fault`→`.Status.IsFaulted`). Element and station segments are untouched. **Thirteen FL1 rows were `[CONFIRMED]` by observation** — every confirmed string on the surface except `FL1.LineState` and `FL1.ITInhibit` — and all thirteen are now `[PROPOSED]` | Integration / Requirements | **High** | **Different in kind from G32, not just larger.** G32 risks three FM2 rows; this risks the measure segment of **every path on all three lines**, so if the reading is wrong **commissioning test `C1` fails across the whole map** rather than on isolated rows. The failure is the same silent one — the write reports success and the line runs on its previous settings (Severity 1). A second-order effect: with `.Lb` and `.FPM` gone **no tag declares a unit**, so nothing in an address can catch a misconfigured scale (**`PLC-Q15`**, raised to High) | Raised as **`PLC-Q05`** / **PLC-Q05** (Critical) and carried on the tag specification's sign-off sheet; the eight rules in `[PLC §4.2]` are what the client confirms, which converts the whole map at once. Every path remains **configuration**, so a correction is a config edit and an app-pool recycle. **`C1` now records the string the controller accepted for every path**, which is what closes this. Watch one semantic: `Diameter` assumes **one** diameter per die block — the old qualifier existed to distinguish the fitted die from a scheduled one, and if the controller exposes both, the qualifier must return | Open — raised Aug 4 2026. **The superseded strings live only in the old→new list printed in this row** — plus git and [`CHANGELOG.md`](../CHANGELOG.md) — since neither client-facing artifact may carry old/new content and the register entry that used to hold them was withdrawn on 12 Aug 2026. Do not compress that list away. ⚠ **The `41` literal is struck (28 Aug 2026, from the `FW-151` review).** It was FL1 15 · FL2 14 · FL3 23 on 4 Aug 2026, before `[PLC]` v1.1 added the dancer rows; the published tables now hold **17 on FL1 and 22 on FL2**, and `FW-144`'s build measured **72 bound paths** (FL1 17 · FL2 22 · FL3 33) on the deployed configuration. **This row is the re-baseline point** — `phase-01b` L192 and `[TB §7]`'s blocker rows still quote `41` and should follow this row, not a leaf plan. **Do not replace it with `72` either**: `P-16` counts the bound map at runtime and the risk is *every* path, not a countable subset. **Widened by the v1.0 reissue the same day:** the `[CONFIRMED]` tag is retired from the specification altogether, so this is no longer a block of unconfirmed rows inside an otherwise-confirmed map — **the whole map is unconfirmed, and the document says so.** That makes **G32** and this gap two named instances of one condition rather than exceptions to a verified baseline, and it makes `C1`'s coverage the only thing that will ever close either |
| **G13** | `slitter-interface` / `CoilDataHub` reference ambiguity | Doc consistency | Low | Wrong pattern copied | Removed as a reference | ✅ Resolved (decision 5) |
| **G15** | No executive summary / 11-stage→phase map | Doc usability | Low | Hard to skim a ~1,250-line doc | Add a one-screen exec summary + process-stage→phase table | Open |
| **G19** | **Pre-check-in was fully specified in the SRS but absent from every other artifact.** `SRS §4.2 PCI001`–`PCI008` (+ `WLD010`, `TRV004`/`TRV009`, §4.18 `PRC001`–`PRC019`) define a dedicated FL1 Pre-Check-In station, yet there was no analysis note, no mockup, no data model, no API, no phase owner — and `10_CommonDB_Insert_WIPStations_FlatWire.sql` D2 **deliberately declined to create the station**. Root cause: `.docx` files are zip containers, so `grep` never reached the requirements; every markdown search for "pre-checkin" returned hits about the *forbidden* `checkin-precheckin` Angular library instead | Requirements / Traceability | High | A `Should`-priority feature that the continuous-feed workflow depends on would have been missed entirely; the Phase-3 "Payoff2 not loaded" alert stays unimplementable | Delivered: `../10-requirements/screens/RodPreCheckin.md`, Dashboard 2A mockup, `RodStaging` table, `/staging/**` endpoints, `PayoffStateChanged`, `FL1PO` station, Phase 4 scope addition | ✅ Resolved (Jul 29 2026) — **but** two items still need business sign-off: `INFLAT`-vs-`STAGED` on staging, and whether pre-check-out needs supervisor approval |
| **G20** | Payoff position modelled **three incompatible ways**: `INT CHECK (1,2)` on rod-fed tables, an FK-style `PayoffPositionId` in `FlatWireRunDetail` pointing at a **table that did not exist**, and the API enum `{Payoff1,Payoff2}` — with FL2's traversing take-up unrepresented (`REVIEW.md` #15) | Data / Schema | Medium | Unenforced FK; no vocabulary for FL2's take-up; "payoff 3" has no meaning | Added the `PayoffPosition` lookup (3 **pinned** Ids: Payoff1, Payoff2, TraversingTakeup) and the real FK on `FlatWireRunDetail`. Rod-fed tables keep `CHECK (1,2)` as a *documented deliberate narrowing* | ✅ Resolved for the data model — `REVIEW.md` #15 stays **partly open**: `TraversingTakeup` has no UI |
| **G21** | **`UX_RodStaging_Bay` does not enforce "one rod per payoff bay" across FL1/FL3.** The filtered unique index is keyed `(LineId, PayoffPosition) WHERE Status = 'Staged'` and `CK_RodStaging_LineId` admits **both** `FL1` and `FL3` — so `(FL1,1)` and `(FL3,1)` are distinct entries. Everything in the design assumes the two lines share **one physical VPS** (`STATION_BY_LINE = {FL1:"FL1PO", FL3:"FL1PO"}` in Dashboard 2A; only `FL1PO` is seeded, no `FL3PO` — G19/OI-26), so **two different rods can be `Staged` on the same physical bay with every constraint satisfied**. Distinct from G20, which settled the payoff-position *vocabulary*, not its *uniqueness scope* | Data / Schema | **High** | The invariant the table exists to defend does not hold; weld genealogy attributes output to whichever row a query picks | Answer **G21** first — the fix is opposite depending on the answer. One station → key the index on the station (`FL1PO`) or a persisted station column. Two stations → seed `FL3PO` and the index is already correct. **Blocks the Phase-4 schema freeze** | ✅ **RESOLVED 15 Aug 2026 — decided as ONE physical station, then fixed in three layers.** **(1) Schema:** `RodStaging` gained a persisted **`[Station] VARCHAR(10) NOT NULL`** and `UX_RodStaging_Bay` was re-keyed `(LineId,PayoffPosition)` → **`([Station],[PayoffPosition]) WHERE [Status]='Staged'`**. **Verified on a live deploy:** an FL3 rod staged at `FL1PO` position 1 is rejected — *"Cannot insert duplicate key row … duplicate key value is (FL1PO, 1)"* — where the old key admitted it as `(FL3,1)` vs `(FL1,1)`. **(2) Domain:** the `RodStaging` **aggregate root** (`D-29`) enforces the same rule as a `BayAlreadyOccupiedRule : IBusinessRule` → `422`, so the index is **belt-and-braces, not the sole defence** — which matters because the invariant is *"one rod per physical station"* and *station* was not a column the table had. ⚠ **This half is closed by design, unverified (15 Aug 2026).** The evidence was `FW-207` AC 6 — reject a second rod on the same station **with the DB index absent** — and **all automated backend tests were withdrawn the same day** (`[TS §1.2]`). Layer (1), the re-keyed index, **is** verified on a live deploy; layer (2) is now asserted. So the practical position is that the *index* is currently the demonstrated defence and the aggregate rule is the designed one. **Do not cite this row as proof of defence-in-depth** until the domain rule is exercised. **(3) UI:** the toggle defect is storied as **`FW-209`** (4 h FE, Phase 4) — it must **reload, not relabel**. `10_CommonDB_Insert_WIPStations_FlatWire.sql` now records that the absence of `FL3PO` is deliberate. *(Originally raised Jul 31 2026 from the Dashboard 2A UX review (`Analysis/Dashboard2A_UXReview.md`, deleted 1 Aug 2026 — in git history at `2a0426b`). **Sharpened Jul 30/Aug 1 2026:** the client confirmed rods are **never stacked** — two rods maximum, one per payoff (**Q71**) — so one-rod-per-bay is definitively the invariant this index must defend, not a modelling assumption. And the off-schedule reversal (**Q24**) makes the system **switch between the FL1 and FL3 stations by itself**, so "are they one station or two" stops being a labelling question and becomes runtime behaviour. **This gap is now the sole tracking home for the question itself** *(its register entry was withdrawn 12 Aug 2026 as ours to answer)*: **our reading is to key the uniqueness on the station**, adding a persisted station column and indexing `(Station, PayoffPosition) WHERE Status = 'Staged'`, which is the fix consistent with the one-VPS answer recorded at **OI-26**. It also carries a **second defect the index does not cover**: the FL1/FL3 toggle on Dashboard 2A relabels the badge, station stamp, queue heading and modal subtitle **without reloading the bays or the queue**, and because the off-schedule check reads the current line, toggling silently reclassifies already-staged rod with no visible change. The toggle should reload, not relabel — that is a defect however the uniqueness question closes |
| **G18** | Source docs (CLAUDE.md / CheckinImplementationPrompt) describe a `--fw-*` design system, but the actual mockups **and** `flat-wire-shopfloor.styles.scss/.css` use `--color-*` semantic tokens (no `--fw-*` anywhere); separately, DB2 was revised to the tab-wizard `- New.html` | UI / Design | Medium | Devs following the stale `--fw-*` docs build wrong token names; DB2 UI must follow the new layout | Correct `--fw-*` → `--color-*` in the source docs; build against the shared `--color-*` stylesheet as-is (no migration needed); ground every dashboard UI section in its `MVP-1/ProjectPlan/Frontend/Mockups/*.html`; DB2 = `- New.html` tab-wizard; a new-layout FL3 check-in variant to follow | Open |
| **G34** | **Wire break now has a decided flow and still no persistence target.** The client specified the whole sequence on 6 Aug 2026 (`OI-13`): the machine stops, a physical component — *"a dancer or something"* — trips a tag, the system prompts **why the machine stopped**, and on *wire break* it offers **WIP reject** or **weld and continue**; weld-and-continue **keeps the same alpha** and records the weld at its **footage position**. What the call did **not** settle is where any of it is stored. `FR-280`–`FR-282` are still marked **"Not deliverable"** in [05-SprintPlanAndBacklog.md](SprintPlan.md), story `FW-N08` is still blocked, and `TC-350`–`TC-352` are still written-and-blocked | Data / Requirements | **High** | The one flow the client has described end to end cannot be built, and the three `WBK` requirements stay unimplementable — now for a narrower reason than before, which makes the gap easy to mistake for closed | Allocate the three writes across existing tables rather than adding one: **`RunPauseEvent`** for the stop and its reason (it already carries a governed reason taxonomy), **`WeldEvent`** for the weld and its footage (it already models induction welds and footage attribution), **`WipRejection`** for the reject branch. The `WBK002` OD verification and `WBK003` defect inspection need a home on whichever row owns the stop. **Decide before the Phase-6 run-event build**; this is **ours to settle, not the client's** | Open — raised Aug 6 2026 from the client call. Replaces the *"no flow"* half of **OI-13**, which is now closed. The no-weld-customer branch is **Q31** |
| **G35** | **FM2's two dancers are unmodelled everywhere, and one of their modes contradicts a stated rule.** The client disclosed on 6 Aug 2026 (`D-28`) that FM2 carries **two dancers**, in the edger positions between S1/S2 and S2/S3, each running in **dancer mode** (compensating speed control) or **tension mode**. **Narrowed 12 Aug 2026.** The `Dancer` lookup now exists with three seeded rows, and a **read-only** tag element is published — `Dancer` on FM1, `Dancer1`/`Dancer2` on FM2, ordinal per `R6`, all `[PROPOSED]` under **`PLC-Q18`** with commissioning test **C12**. **What remains:** no path has been read off a controller; the `Mode` vocabulary is ours; there is still **no `PassScheduleComponent` entry**, deliberately — whether the mode is scheduled or machine-side is unresolved (`OQ-32`), and two client statements disagree. **The third failure mode below is the one still live:** if dancers *remove* rather than *apply* tension, `PSG-D27`'s force substitution may model behaviour the equipment does not have | Data / Schema / Integration | **High** | Three failure modes, and the third is the expensive one. A selectable mode has **nowhere to be persisted** and **nothing to be written to**; and `PSM012` / master spec §3.4 both assert *"tension is derived from speed, never entered manually"*, which is **false in tension mode** — so a rule the pass schedule and the check-in push are built on is wrong for half the operating envelope | Answer **Q32** first: whether the mode is a **pass schedule parameter** or a machine-side setting decides everything downstream. Scheduled → a component row, a DDL column and a place in the acknowledgement push (**one table added to `[DBD §6.2]`'s baseline** — this cell said "table count 27 → 28" until 26 Aug 2026, four baselines stale). Machine-side → a read subscription only, and nothing in the schema moves. Either way the glossary rule must be **qualified by mode**, not deleted — it is correct in dancer mode. The tag paths are **`[PLC]`'s to define**; do not invent one here | Open — raised Aug 6 2026 from the client call |
| **G36** | **Returning Phase 9 to MVP-1 imported three uncosted dependencies, all on the DB7b packing side.** Phase 9 became wholly MVP-1 on 11 Aug 2026 because `CoilOutput`/`CoilTraceability` were MVP-1 with **no writer at all** — `CoilCompletionService` and both coil endpoints had gone to MVP-2, leaving the DM010 non-overlap trigger guarding rows nothing inserted. Bringing the screens back was correct (DB7 owns three operator decisions no headless service can make: the scale override on weight, 1-of-2 vs 2-of-2 on the skid, and suspend on an out-of-spec final SPC) — but **DB7b arrived with three unresolved dependencies that were tolerable only while it was deferred**: (a) ~~`CoilOutput.SkidId` references a skid table nothing names, creates or verifies~~ — **`OI-104` ✅ closed 18 Aug 2026**: it is `united_db..wip_skids` + `proddb..wip_skid_coils`, written by `FW-219` / `FR-509`–`FR-518` and specified in `[INT §8.1]`. **Three dependencies remain, not four**, and the closure has a cost of its own — see **`G44`**; (b) `GET /coil/{alpha}/label` returns a `lotNumber` nothing generates — **`OI-24`**, with **`OI-99`** for the multi-rod case; (c) `FR-346` adds a **physical scale weight at the packing station**, a third weight figure after the calculated value and DB7's operator override, with no rule for which governs the coil record — **`OI-105`**. A fourth, **`OI-106`**, is DB7b's own: closure must assign a staging location and none are defined | Data / Scope | **High** | Skid closure and the coil label cannot be built. `phase-09`'s **222 h assumes the skid table and lot source already exist** and costs neither; its **16–32 h `OQ-10`/`OI-45` reserve is understated**, having been scoped before a second weight-capture point was in scope | Confirm whether the skid table and lot number come from the existing `united_db`/planning schema or must be built, and decide the authoritative weight. If either must be built, Phase 9 needs re-estimating | **Open — raised 11 Aug 2026.** DB7b also had **no owning specification** until the same pass; it is now `OutputCoilCompletion.md` §8 (v1.1), where all four items are carried as `[CLIENT INPUT REQUIRED]` on the client sign-off sheet |
| **G37** | **FL1 spool completion is a fully specified subsystem costed as a 3-hour story, and nothing creates the `SpoolProcessing` row.** [`BusinessRequirements.md`](../10-requirements/BusinessRequirements.md) §5.5 carries **`FR-130`–`FR-155` — 26 requirements**; [`TestCases.md`](../70-testing/TestCases.md) §5.5 carries **`TC-160`–`TC-184` — 25 cases, eleven of them P1**; [`SpoolCompletionNotification.md`](../10-requirements/screens/SpoolCompletionNotification.md) is the owning specification; [`SignalR.md`](../20-architecture/SignalR.md) specifies two hub events; and `spool_notification.js` is a **built mockup component included at `dashboard_3_active_run.html:1396`**. The only build story is **`FW-N02`, 3–4 h RT**, whose acceptance criteria cover the milestone notifications and the machine-stop confirmation and nothing else. Separately and consequently: **`phase-05` routes *Complete Run* to `POST /coil/complete`, which is Phase 9's FL2 output-coil endpoint** (`FW-185` — *"coil alpha, genealogy, skid"*) — **no story anywhere writes the FL1 TKUP-1 `SpoolProcessing` row** | Requirements / Scope | **High** | **Two failures, and the second is the one that stops a demo.** (1) Part B is `Must` and is *"the gate on the spool completion transaction"* — a server-owned prompt state machine (edge-triggered once per stop, configurable dwell, weight latched at the PLC stop timestamp, surviving a browser refresh) plus a scale-vs-calculated weight basis with a ±2 % variance override — priced at 3–4 h. (2) **Without the `SpoolProcessing` write, DB5 has nothing to check in and the FL1→FL2 journey cannot be demonstrated at all.** It also sits inside `FW-062`'s approved mockup, so building Dashboard 3 to spec means building it whether or not it is planned | Re-price and split. **Part B ≈ 52 h AI-assisted** (FE ~20 · BE ~22 · RT ~8 · DB ~2) against ~106 h hand-coded on `[CE §2]`'s rate card — minted as **`FW-202`** and scheduled in [`TrialRunPlan.md`](TrialRunPlan.md) §4. **Part A (`FR-130`–`FR-136`) ≈ 25 h is `Should` and explicitly *"advisory and non-blocking"*** — deferrable. Re-point `FW-N02` at `FW-202` rather than leaving two stories over one subsystem. **Promote `SpoolCompletionPromptDue`/`Resolved` into the published hub contract** — `[SIG]` flags them as absent from it. ⚠ **`FR-137` carries `OQ-10`**: the `lb-per-ft` derivation reads `united_db..alloys.alloy_density` cross-database and the **dimensional basis is undecided**, so `[CE §2]`'s 16–32 h reserve applies here as well as on Phase 9 — `TC-167`/`TC-168` put the same calculation in both | Open — raised 14 Aug 2026 from the six-screen trial-run scoping ([`TrialRunPlan.md`](TrialRunPlan.md) §5.1). **Not a duplicate of `G36`**, which is Phase 9's *coil* completion and its skid/lot/weight dependencies; this is Phase 5/8's *spool* completion, a different transaction on a different line |
| **G38** | **The durable spool-completion prompt has no persistence target anywhere in the schema.** `FR-144` requires `SpoolCompletionPromptDue` to be **server-owned state, persisted against the run**, surviving a browser refresh and **re-delivered on group re-join**; `[SIG §5.2]` promoted it into the published hub contract on 14 Aug 2026 as the **one event that is not fire-and-forget**, and `[SIG §5.6]` tells a client to expect re-delivery on join. `TC-173` is **P1**. **No column exists to hold it.** `FlatWireRun` (`FlatWire_DDL_03_Materials.sql`) has no prompt columns; `SpoolProcessing` has none; nothing in the 25-table MVP-1 schema mentions a prompt. The schema was counted and frozen on 13 Aug (**25 tables · 33 FKs · 41 index statements · 1 procedure · 1 trigger**) and `phase-01b`'s Dependencies line treats convergence with 1C as complete | Data / Schema / Real-time | **High** | **`phase-01b` acceptance criterion 4 cannot be met.** A prompt raised on the `RUNNING → STOPPED` edge is lost on any refresh, screen change or reconnect — and the whole point of `FR-144` is that *"a stopped mill with an unanswered prompt is a spool nobody has committed."* The failure is silent: the event is declared on `IFlatWireClient`, the types compile, and the durability simply never happens. ⚠ **Compounded 15 Aug 2026** — with automated backend tests withdrawn (`[TS §1.2]`), the behaviour now has **neither a column nor a test** | Add the state to `FlatWireRun` (`PromptDueAt`, `PromptLatchedWeightLb`, `PromptPlcStopTs`, `PromptResolvedAt`, `PromptAnswer`) **or** a small `SpoolCompletionPrompt` table keyed on `RunId` — the former is cheaper and matches *"persisted against the run"* literally. **This is a 1C schema delta and it needs a story**; `FW-202` owns spool completion but is Phase 8/9 scope and does not cover 1B's transport obligation. Until it lands, mark criterion 4's durability half **blocked**, not merely untested | ⚠ **Half open — restated 15 Aug 2026, and the halves have different owners.** **The schema half is done:** `FlatWireRun` carries `PromptDueAt`, `PromptPlcStopTs`, `PromptLatchedWeightLb`, `PromptResolvedAt` and `PromptAnswer` with `CK_FlatWireRun_PromptAnswer`, deployed and verified, and `phase-01b` acceptance criterion 4 now reads *"the durability half is now buildable."* **The behavioural half has no owner at all** — persisting to those columns and **re-delivering on group re-join** is a **1B** obligation that `phase-01b` requires to exit, and **no line item in `[TRP §1.4]`'s 1B table covers it**: `FW-080` is the hub, `FW-149` is the typed contract, and `FW-202` is **T3**, two sprints later. Carried as **`[TRP §6]` blocker 5**, needed **inside T1**. ⚠ **The counts in the description above are pre-`D-31`** (25 tables · 33 FKs · 41 indexes); the build is the `[DBD §6.2]` baseline. ⚠ **Compounded again 15 Aug 2026** — with automated backend tests withdrawn the behaviour has a column, no test, and no story. **Not a duplicate of `G37`**, which is that *nothing writes the `SpoolProcessing` row*; this is the *prompt's own state*. Both must close before FL1 spool completion works end to end |
| **G39** | **A simulator built from our own assumptions becomes the de facto specification of machine behaviour, and nothing reconciles it against the real line.** `FW-203` already drives the trial from a synthetic feed, and [`MachineSimulator.md`](../20-architecture/MachineSimulator.md) (`FW-210`–`FW-215`) extends that into a model of FL1/FL2/FL3 that screens, test cases and operator training will all be built against. **Every channel it drives is modelled on an unconfirmed reading:** the `LineStatus` vocabulary is ours (**OI-35**, confirmed only at `C2`); the tag map is `[PROPOSED]` throughout (**G32**, **G33** — the whole map, not isolated rows); the footage→weight basis is undecided and deliberately carries no recommendation (**`Q10`** / **OI-45**); and the cadence has no target, which is `FW-203`'s own listed blocker (**G9** / **OI-34**). Distinct from `G9`: that gap is that the NFR *targets* are undefined, this one is that the **model is unverified even once they are** | Integration / QA | **High** | **The failure is silent and late.** Everything passes against the simulator — screens render, suites go green, operators train — and the divergence surfaces in the October commissioning window, which [`PLCCommunication.md`](../20-architecture/PLCCommunication.md) §4 already flags as *"the worst compression in the schedule"*. A convincing simulator makes this **easier** to forget, not harder: the more believable the model, the more readily its assumptions are mistaken for confirmed behaviour. `[TRP §9]`'s *"the trial proves the screens, not the machine"* is the position, and nothing in the simulator improves it | Two parts, and the first is already delivered. **(a)** `[SIM §5.6]` carries an explicit **assumption table — A1–A10, one row per modelled behaviour**, each naming its basis and the test that confirms it, so no assumption is implicit. A8 and A9 (lateral spread and rolling force are not modelled) are marked permanent simplifications; **A1–A7 are provisional and must be reconciled**. **(b)** Add commissioning step **`C13` — "record the observed value for every simulated channel and diff it against the model"** to [`PLCCommissioning.md`](../70-testing/PLCCommissioning.md), so the reconciliation is a scheduled act with an owner rather than an intention. Without (b) the table in (a) is documentation nobody is required to read | ⚠ **Open, but no longer half-built — updated 15 Aug 2026.** Part (b) landed: **`C13` is now in [`PLCCommissioning.md`](../70-testing/PLCCommissioning.md) §8.3**, and its pass condition is deliberately *that the diff was taken and recorded*, not that it was empty — a test that can pass while finding the model wrong. **The gap stays open because the reconciliation has not happened**, only been scheduled; it closes when `C13` runs in the October window and A1–A7 each carry a verdict. ⚠ **Widened the same day by `FW-218`**: the trial now **steers** the simulated feed (`[TRP §8]` steps 3 and 7 and the interlock's condition 5 are driven through it), which makes the acceptance run reproducible and the model *more* persuasive without making it any more verified. **That is this gap, not a mitigation of it** |
| **G40** | **The trial's acceptance run had no valid pass-schedule pair, and the seed could not hold one.** `[TRP §8]`'s one continuous FL1 → FL2 journey produces a spool under **`PS-1100-FL1-001`** (FL1, **Standalone**, 0.110″ × 0.500″) and checks it into FL2 — but the only **Active** FL2 schedule was **`PS-1100-FL2-001`**, which is **`Hybrid`**, and **`FR-091`** has Dashboard 5 validate the schedule's route mode against the **spool's origin route mode**. A Standalone-origin spool therefore had no schedule it could legally be checked in under. Two further assertions rested on the same error: **`[TRP §8]` step 8's *“0.113″ target”* appears in no seed at all**, and step 9's *“Live shows its empty state”* follows from `FR-120`, which conditions the null-gauge rule on **FL2 in standalone mode** — not on FL2 as a line (`[SIG §5.3]` says the same) | Data / Test fixture | **Critical** | **The trial's headline demonstration could not complete.** Steps 8 and 9 are the FL2 half of the journey and the only proof the six screens are a system rather than six pages. It would have surfaced during T3, against a seed that had been deployed and verified a dozen times — because the defect is in the *combination*, not in any one row | **`PS-1100-FL2-002`** added — 1100 · FL2 · **Standalone** · Active, fed the 0.110″ × 0.500″ round-edge spool FL1 produces, FM2 closing 0.110″ → 0.106″ → 0.103″ → 0.100″ across S1/S2/S3 with the section spreading to 0.560″. ⚠ **`PS-1100-FL2-001` had to be demoted to `Inactive`, and that was not a free choice** — `UX_PassSchedule_OneActivePerLineAlloy` permits exactly **one Active schedule per line + alloy**, so the two cannot coexist. It is kept, not deleted: it is the Hybrid-FL2 coverage case and `RUN-0004` still references it. `[TRP §8]` gains two assertion queries and its steps 8–9 are repointed | ✅ **Resolved 15 Aug 2026** — raised and closed the same day from the trial-run plan review. Verified on a clean teardown-and-deploy: 28 tables · 43 FKs · 1 procedure · 1 trigger, 11 schedules · 77 components, idempotent on re-run. ⚠ **The residual is `OI-47`**, not this row: the hybrid-origin guard itself is still **undefined** (`TC-118` is **P1** and reads *“gate fails until specified”*). The trial no longer walks into it; **Phase 8 still does** |
| **G41** | **`CK_PSC_FM1NotBypassable` is line-blind, so a correct FL2 pass schedule cannot be authored.** The constraint is `CHECK ([ComponentName] <> 'FM1' OR [State] = 'Active')` — commented *“FM1 (12″ flattening mill) is not bypassable (SRS §2.8 equipment table)”* — and it applies to **every** schedule on every line. But `Stand.Id 1` is **`FM1`, `LineId = 'FL1'`**, and an **FL2-standalone** run is fed an already-flattened spool: FL1's 12″ mill is not in that material path at all. So every FL2 schedule must mark `FM1` **Active**, engaging a stand the line does not have. ⚠ **Do not generalise this into *“a component's `Stand.LineId` must equal its schedule's `LineId`”*** — that assertion returns **18 rows** and most are legitimate: an **FL3** schedule drives `FM1` *and* `FM2_S1..S3` because FL3 **is** FL1 feeding FL2, and a **Hybrid FL1** schedule reaches FM2 for the same reason. The defect is the single narrow case | Data / Schema | **High** | The check-in acknowledgement pushes a configuration for a station the line cannot address, and `[PLC]`'s FL2 tag map has no `FM1` entry to receive it. Today it is inert because the trial's FL2 row is stated as pass-through (entry = exit); it stops being inert the moment anything reads component state to decide what to write | Scope the constraint to the lines that have the stand — `FM1` non-bypassable on **FL1 and FL3**, `Skip` permitted on **FL2**. ⚠ **A CHECK cannot express it** (it would have to reach `PassSchedule.LineId` across the row), so it is either a denormalised `LineId` on `PassScheduleComponent` or a validator in the domain model — `FW-207` already owns invariants as `IBusinessRule`. **Do not add a trigger**: `[TRP §8]` publishes *“1 trigger”* as a verified count. Until it closes, `[TRP §8]`'s narrow assertion query documents the one expected row | Open — raised 15 Aug 2026 from the trial-run plan review, while seeding `G40`'s fixture. **Not urgent for the trial** and **not safe to leave for production** · ✅ **Owned by `FW-246`** (29 Aug 2026). ⚠ `FW-246` also carries `G50`, `G52` and `G55`. **Do not generalise `G41`** into a `Stand.LineId` = schedule `LineId` rule. |
| **G42** | **`SpoolProcessing` cannot represent multi-rod genealogy, and the weld descope hides it.** `[TRP §1.5]` records the single-rod spool as a **scope** consequence of the 14 Aug weld removal — *“with no welds the chain is one rod long”* — which is true and is not the whole of it. `SpoolProcessing` carries **`ParentRodAlpha`** and **`SourceRodAlpha`**, two single-rod columns, and the second is documented as the *partial-run* source (`OQ-12`), not a second contributor. **There is no child table.** `CoilTraceability` is **coil**-level, hangs off `CoilOutput`, and is Phase 9's. So when weld capture returns there is still nowhere to record *“this spool came from rods R00041 and R00042, at these footage positions”* | Data / Schema | **High** | **The welding-wire customer certificates are produced from that genealogy** (`NFR012`), and continuous feed through an induction weld is the normal FL1 case, not an edge case — a 1,800 lb spool routinely consumes more than one rod. `FW-202`'s acceptance criterion already reads *“source rods from the run's `WeldEvent` chain”*, plural, against a schema that can hold one | A `SpoolTraceability` child keyed on `SpoolAlpha` — source rod alpha, sequence, start and end footage, weld event reference — mirroring what `CoilTraceability` does for coils. ⚠ **Raise it now, while it is free.** With weld capture out of the trial nothing writes the table, so adding it is a DDL file and a domain type; after weld capture returns it is a migration plus a backfill against certificate data | ⚠ **Open — and client-confirmed 20 Aug 2026, which changes its urgency and its shape.** The [client call](../95-archive/source-documents/ClientCall_2026-08-20_SyncPlan.md) settled that a spool carries **many rods and many orders** (*“spool to A-rods is one to many. In turn, that one A-rod could be on multiple orders as well”*), and Srikanth arrived at this row’s recommended design independently — *“the spool coming out of FL1 should have a separate table … that table should contain the one-to-many relationship”*. **Two additions to the resolution above.** (1) **A weight column per source alpha** — Tim: *“It’s how many pounds are on the spool and how many pounds for each alpha”* — which the child as specified does not carry, and which rests on `Q10` (**`OI-121`**). (2) **A spool↔order sibling**, which this row never mentioned: `SpoolProcessing.OrderNo` is scalar and `SpoolCheckin.OrderId` is scalar `NOT NULL` (**`OI-119`**, **`Q43`**). ⚠ **Both must be `FlatWireDB`-local**, mirroring the `wip_skid_coils` / `wip_coil_orders` **pattern** and not extending those tables — the client offered either, and **`D-32` forbids the shared option**; the `Rod` mirror (`D-04`) is the precedent. ⚠ **The “free” window is now dated: it closes at `S1`, 24 Aug 2026** (**`OI-122`**). Still **not a trial blocker** — the trial demonstrates a single-rod spool by design — and still **an MVP-1 obligation**. ✅ **The SCHEMA half is BUILT — added 27 Aug 2026, because this row still read as though nothing had happened.** `SpoolTraceability` and `SpoolOrder` were deployed and verified on **22 Aug 2026** (with `SpoolStaging` and the `Spool` article), so *"there is no child table"* is no longer true and **the free window has closed**. ⚠ **What is left is the DOMAIN half, and it is `FW-207`'s**: the two entities, their nine `ALTER`-added columns (`P-70`), and ⛔ **the non-overlap invariant with NO trigger behind it** — `SpoolTraceability`'s footage bounds are nullable because the genealogy is weight-primary, so a non-overlap trigger joining on `NULL` **passes silently**, which is why 22 Aug deliberately added none and `[TRP §8]`'s *"1 trigger"* still holds. **The gap does not close until that invariant is built** (`FW-207 §2.1a`, `P-91`); `OI-121`'s per-alpha weight column landed with the table, `OI-119`'s spool↔order sibling is `SpoolOrder`, and `Q10` still gates the printed weight · ✅ **NARROWED 29 Aug 2026, verified against the CODE:** the non-overlap invariant is **built and invoked** — `SpoolSegmentsMustNotOverlapRule` in `FlatWire.Domain/Rules/TraceabilityRules.cs`, called from `SpoolProcessing.cs:188`. `P-91`’s *"the aggregate is the ONLY defence"* is satisfied, and **no story was minted** because none was needed. ⚠ **Still the only defence** — footage is nullable, so no trigger backs it. |
| **G43** | **The trial was specified to demonstrate behaviour it had no way to trigger.** `[TRP §8]`'s acceptance run requires the simulated feed to be **steered while a run is live**: step 3 forces N consecutive out-of-spec readings, step 7 needs a `RUNNING → STOPPED` edge **at a chosen instant** (`TC-171`'s **3 s stop against a 5 s dwell**, `TC-172`'s weight latched at that timestamp), and `FW-205`'s condition 5 can only be reached by **dropping readings**. `FW-203` is a **publisher with no control surface** — `[SIM §1.2]`'s own comparison table marks it ❌ on *“an operator-drivable control surface”* — and `FW-215`'s five endpoints were unscheduled post-trial work depending on `FW-210` and `FW-213`. `[TRP §1.4]`'s callout said of the whole simulator *“None of it is in this plan”*, which was true and left this hole | Test fixture / Integration | **High** | **Three of the acceptance run's ten steps could not be executed.** The only lever without a control surface is a configuration change plus a restart — which destroys the run being demonstrated, and steps 7 and 10 are asserting about a run that survives. It would have surfaced in the UAT window, where there is no room to build anything | **`FW-218`** — three of `[SIM §8.1]`'s five endpoints (`POST /sim/{lineId}/steer`, `DELETE /sim/{lineId}/run`, and `POST /sim/{lineId}/fault` limited to **one** fault, dropped readings), built over `FW-203` rather than over the line model. **18 h base / 11 h AI-assisted**, T2, additive to `[CE §3b]` — four endpoints once `GET /sim/state` joined for the console. It is the **first increment of `FW-215`**, the relationship `FW-203` has to `FW-211`. **`FW-214`, the console `DB-S1`, came with it** (15 h at the time, T2; ⚠ **52 h base / 36 h AI-assisted since `D-33` retargeted it to WinForms**, 29 Aug 2026): an API alone means UAT is executed by an engineer issuing HTTP calls beside the operators while step 7 times a **3 s stop against a 5 s dwell**. ⚠ **The console ships with controls greyed** — Start, the scenario picker, six of the seven fault buttons and the seed — because its own dependency is `FW-215` in full, behind which sit `FW-210` (24 h) and `FW-213` (16 h): **every control live is +50 h AI-assisted and the window does not have it.** Grey, do not delete; each returns as configuration. `[SIM §2.4]` travels with it: **when simulation is off the routes are not registered at all — `404`, not `403`** — and `Engineer`/`Admin` only, never `Operator` | ✅ **Resolved 15 Aug 2026** — raised and closed the same day from the trial-run plan review. Trial **806 → 832 h**, Phase 1 **451 → 462 h**, margin **42 → 16 h**. ⚠ **Both figures are superseded and must not be quoted forward.** The 25 Aug 2026 re-baseline (`[TRP §2.1]`) moved the plan to three resources × 6.5 h/day, where the margin is **28 h across the three blocks** — 4 h T1, **12 h T2**, 12 h T3 — and `D-33` then added **+21 h AI-assisted** to `FW-214` on 29 Aug 2026. Read `[TRP §2.1]`, not this row, for the current position. ⚠ **`G39` applies to `FW-218` unchanged**: a feed we can steer is not a feed we have verified, and a *reproducible* acceptance run is exactly the *“convincing simulator”* that gap warns about |
| **G44** | **The FL2/FL3 run-end write-back is specified and designed but not built, and its three confirmation values are not in anyone's queue.** Until 18 Aug 2026 a completed flat wire coil was written only to `FlatWireDB`, so packing, shipping, certification, cost and yield could not see the finished goods at all — none of `coils`, `wip_coil_orders`, `coil_gen_history`, `coil_cost`, `coil_slit_cuts`, `wip_skids`, `wip_skid_coils` or `wip_log` was written, and not one of them was named anywhere in the repository. That is now specified (`FR-509`–`FR-518`, `[INT §8.1]`) and the procedure is written ([`50_united_db_Proc_FlatWire_CompleteCoilOnSkid.sql`](../Database/Scripts/50_united_db_Proc_FlatWire_CompleteCoilOnSkid.sql)), but **nothing is deployed and nothing has executed against a shared database**. Three values need IT sign-off first — **`Q34`** the eight-character transaction token, **`Q35`** whether `coil_status = 'ONSKID'` is right for finished flat wire, **`Q36`** the sample number and planned operations — plus **`OI-114`**, the cut-record sentinels, which **cannot be settled by copying a template** because the five legacy non-slit writers disagree with each other on `stop_no` and `mfg_order_no` and all five hard-code a `skid_coil_seq_no` flat wire cannot use | Scope / Data | **High** | **`FW-219` is additive to the 3,186 h baseline and is carried in `[TRP §5.3]`**, following the `FW-202` precedent. Two consequences beyond the hours: **`OI-112`** is a live defect this gap surfaced rather than created — `FR-077` sets `wip_stations.coilno` and nothing clears it, against a UNIQUE index, so a line is unusable after its first run — and **`OI-113`** means the shared genealogy and `CoilTraceability` now disagree **by design** on a welded coil's parentage | `FW-219`, `FR-509`–`FR-518`, `[INT §8.1]`, `OI-112`–`OI-114`, `Q34`–`Q36` | **Open — raised 18 Aug 2026**  ⚠ **Three 26 Aug 2026 changes land inside this gap's scope and it is still open:** the coil-identity **cardinality** change (one alpha per source rod), the **23-table rename**, and ⛔ **`Q89`'s decision that every part alpha reaches `proddb..coils`, which REOPENS this write-back** — `FR-512` is deleted and `FR-509`–`FR-518` re-specified. **`OI-113` closes** with it; `OI-112` and `OI-114` remain, and `OI-114` is now known to state something false (`OI-132`). |
| **G45** | **The FL1/FL3 check-in write-back is specified and built but not deployed, and its four confirmation values are not in anyone's queue.** `FR-077` has named four shared writes since it was written — the reqsum into `wip_coil_orders`, `actual_start_date` on `planning_routings` and `routings`, and the `wip_stations.coilno` claim — and **there was no procedure, script or code for any of them**, the same absence `G44` found at the other end of the run. That is now specified (`FR-519`–`FR-528`, `[INT §8.0]`) and three procedures are written ([`FlatWire_CheckInRod`](../Database/Scripts/40_united_db_Proc_FlatWire_CheckInRod.sql), [`FlatWire_ReleaseStation`](../Database/Scripts/60_united_db_Proc_FlatWire_ReleaseStation.sql), [`FlatWire_ReverseReqsum`](../Database/Scripts/70_united_db_Proc_FlatWire_ReverseReqsum.sql)), but **nothing is deployed and nothing has executed against a shared database**. Four values need IT sign-off — **`Q37`** the transaction token, **`Q38`** the WIP-log status value, **`Q39`** stamping the rod's `coils` row, **`Q40`** delete-versus-orphan on the reversal. **⚠ A deployment prerequisite is also unmet**: `FlatWireDB` is on `(localdb)\MSSQLLocalDB` while the shared five are on `DEVUAL-UADEV001\TEST1`, and the one-transaction design **requires co-location** — in the split topology nothing errors, it simply stops being atomic | Scope / Data | **High** | **`FW-220`/`FW-221`/`FW-222` are additive to the 3,186 h baseline** (51 h AI-assisted / 69 h hand-coded, DB only — the BE half is already inside `FW-157`'s 36 h). Two consequences beyond the hours: **`OI-112` closes** — `FlatWire_ReleaseStation` is what makes a line usable after its first run — and **`G2`/`OI-39` narrow**, because the cross-database half of the recovery question is answered by the two databases committing together. **Phase 4's 24–64 h reserve must be re-derived before S2**: its cross-database portion is spent, its PLC portion stands | `FW-220`–`FW-222`, `FR-519`–`FR-528`, `[INT §8.0]`, `OI-115`, `OI-116`, `Q37`–`Q40` | **Open — raised 19 Aug 2026** |
| **G46** | **Six register ids were minted on 19 Aug 2026 and never added to the registers that own them.** `Q37`–`Q40`, `OI-115` and `OI-116` are fully described in **five** documents — this register’s own `G45`, [`Integration.md`](../20-architecture/Integration.md) §8.0, [`BusinessRequirements.md`](../10-requirements/BusinessRequirements.md) §5.26, [`FW-220`’s task plan](../40-backend/tasks/FW-220.md) and `CHANGELOG.md` — and were absent from both [`./Questions.md`](./Questions.md) and master spec §11. The question register meanwhile stated *“there are no numbering holes — `Q1`–`Q36` here”*, so it actively asserted that four cited questions did not exist. **Found 20 Aug 2026** while numbering that call’s questions, which would otherwise have been minted as `Q37`+ and collided | Process / Traceability | **Medium** | **The registers are the join key for the whole repository and a citation that resolves to nothing is worse than one that resolves to the wrong thing** — the reader cannot tell which. Concretely: `OI-115` **blocks building** the FL2 half of `FW-220`, and it sat outside the register that phase owners read. Left alone it would have produced two different `Q37`s in two documents, which is exactly the class of defect the 12 Aug renumbering swept 630 citations to remove. The narrative counts had drifted with it — the register read *“28 of the 33”* against a table of 36, and the priority table omitted `Q36` entirely | **Done in the same pass**: `Q37`–`Q40` registered as section `A14` with recommendations, `OI-115`/`OI-116` added to master spec §11, the index counts and priority table rebuilt from the Quick Reference table (**41 shopfloor + 6 other = 47**, all three tallies now agreeing), and the *“no numbering holes”* paragraph rewritten to say what happened. **The standing rule this gap exists to enforce:** an id is not minted until it is in its owning register — `Q##` in `./Questions.md`, `OI-##` in master spec §11, `G##` here. Citing an id from a gap row or a task plan does **not** create it | ✅ **Resolved 20 Aug 2026** — raised and closed the same day. Retained rather than struck, because it is the only record of why `Q37`–`Q40` carry a 19 Aug provenance and a 20 Aug registration date |
| **G47** | **`FlatWireRun.OrderId` is read as “the order this run produced” and stops being that at an order boundary.** The rod ↔ order design keeps a rod mounted across the boundary, so **one `FlatWireRun` spans two orders** — not a choice but a consequence: `FlatWireRun` is *“one row per check-in event”* and `RodCheckin.RunId` is a single `NOT NULL` column, so closing and reopening the run would require a second check-in, which the client's rule 7 forbids. `OrderId` is a scalar, so it narrows to *“the order at check-in”* | Data / Contract | **High** | Every consumer that joins a run to an order for reporting, scheduling adherence or the shared write-back is wrong at a boundary, and **silently** — there is no error, just an attribution to the wrong order | **Audit the readers, do not widen the column.** Per-order truth belongs on the consumption record and per-output truth is already on `CoilOutput.OrderId`; `FlatWireRun.OrderId` should be *documented* as the check-in order rather than redefined. Widening it to a set would break every existing reader at once instead of the boundary cases | Open — raised 22 Aug 2026 from [`RodOrderAllocation.md`](../95-archive/design-notes/RodOrderAllocation.md) §5. Mirror: **`OI-123`** |
| **G48** | **The spool↔order association has no positional column, so a spool crossing an order boundary cannot say where the boundary is.** It carries a sequence and a planned weight. The client confirmed on 20 Aug that a spool off FL1 may carry two or more orders while **FL2 makes one order at a time** — so FL2 must cut at the boundary, and nothing tells it where. ⚠ **The same table's header also documents a workaround as its design:** its order set is *“read from the shared `planning_routings` rod→order allocation”*, written that way **because the rod↔order table did not exist**. ⚠ **This gap is made concrete, with numbers, in [`RodOrderAllocation_WorkedExamples.md`](../95-archive/design-notes/RodOrderAllocation_WorkedExamples.md) §9** — a rod split 1,500 / 2,500 lb across two orders produces `SP-00006`: one rod, one segment, **two orders, no weld**. Because `CoilOutput.OrderId` is a scalar `NOT NULL`, FL2 must cut at the boundary and yields **two coils below the 800 lb customer minimum out of a good 1,800 lb spool**. §10 then shows the mitigation — placing the boundary at a whole multiple of the coil weight cuts material at risk from 900 lb to 12.64 lb — but it is a **planning input, not an execution path**: §9 and §10 run the identical state machine, so it degrades silently when a rod arrives short or a weld is remade. `Q54` (close the spool at the acknowledgement) would remove the failure outright | Data / Schema | **High** | A two-order spool cannot be finished correctly at FL2, and the welding-wire certificate's per-order attribution has no basis. The derivation half also keeps a **shared-schema read** on the FL1 path that `D-32`'s reasoning would remove | **Add `SpoolWeightFrom` / `SpoolWeightTo`** — half-open, **in pounds**, matching the rod↔order split, because weight is conserved through drawing and rolling and footage is not. Re-point the derivation at the local rod↔order table in the same pass. ⚠ **Raise it now, while it is free** — exactly the `G42` argument: the table was created 22 Aug and **nothing writes it yet**, so today it is two columns; afterwards it is a migration plus a backfill | Open — raised 22 Aug 2026 from [`RodOrderAllocation.md`](../95-archive/design-notes/RodOrderAllocation.md) §6. Mirror: **`OI-124`**, question **`Q54`**. ⚠ **PREMISE CONTRADICTED 3 Sep 2026, STAYS OPEN.** This gap exists to solve a spool that spans an order boundary. Tim O'Brien: *"in both cases the system should require a check-out/check-in of the Rod, and the check-in of a new empty spool. **I do not believe that we want to have multiple orders on a spool.**"* If that is ratified **no spool spans a boundary, the positional column is unnecessary and this gap dissolves.** ⛔ **Not closed:** the 20 Aug basis was a multi-voice client call and this is one voice by e-mail — the situation `ClientCall_2026-08-24_SyncPlan.md` §3.1 flags for `D1`, where a one-person position was accepted only once Bob and Tim agreed on the record. Ratification is owed (audit record [`ClientEmail_2026-09-03_RodOrderAllocation_SyncPlan.md`](../95-archive/source-documents/ClientEmail_2026-09-03_RodOrderAllocation_SyncPlan.md)) |
| **G49** | **Nine decided requirements have no column to land in.** Carried over from the `GapAnalysis.md` audit (its findings **B1**-**B9**) when that document was retired. Each is a requirement that survived client review and is stated in an owning specification, but the schema has nowhere to store its result: the affected screens are **`OutputCoilCompletion`** (DB7/DB7b), **`RollAdjust`** (DB11), **`WipRejection`** (DB8) and **`SpoolCompletionNotification`**. ⚠ **Raised as ONE gap on purpose.** Nine separate rows would be triaged independently and half-forgotten; they share a cause (requirements written against screens, not against tables) and a remedy (one 1C schema delta) | Data / Schema | **High** | Each is a `Must` or `Should` that cannot be satisfied by any deployment of the current schema, and the failure is **silent** - the UI can display the value and simply never persist it. Same class as **`G24`**, which was found and fixed on `RodCheckout`; that one was caught only because someone read the DDL beside the specification | Take the nine-row table from `GapAnalysis.md` at commit `ebd0834` and disposition each against Phase 9, Phase 6 or Phase 1C. Most are single nullable columns; the point is to decide them together and in one DDL change, not to discover them one screen at a time during build | Open - promoted 23 Aug 2026 from the retired `GapAnalysis.md` (**B1**-**B9**). Recoverable at `ebd0834` · ✅ **Owned by `FW-244`** (29 Aug 2026). |
| **G50** | **Three referential-integrity holes: a lookup table nothing references, and two columns modelled as text where a parent exists.** From the retired `GapAnalysis.md` (**C1**, **C2**, **C4**). **(1) `AlloyProperty` is orphaned on the material side** - `Rod.Alloy` and `FlatWireRun.Alloy` are free `varchar` with no FK, so the authoritative alloy list governs `PassSchedule.Alloy` and nothing else, and a typo on a rod is unconstrained. **(2) The payoff position is modelled inconsistently** - some tables FK to `PayoffPosition`, others carry a bare integer. **(3) `PassScheduleComponent.ComponentName` is an enumerating CHECK rather than a reference**, so the component vocabulary is duplicated in a constraint instead of being data | Data / Integrity | **Medium** | A bad alloy string reaches the lb-per-ft derivation and the certificate without ever failing a constraint, which is the same exposure `OI-93` describes from the other end. The payoff inconsistency makes "which bay" queries differ by table | Add the two FKs; decide whether `ComponentName` becomes a lookup. ⚠ **Check the payoff half against `G20` before acting** - `G20` records the payoff being modelled three ways and these may be the same finding | Open - promoted 23 Aug 2026 from the retired `GapAnalysis.md` (**C1**, **C2**, **C4**) · ✅ **Owned by `FW-246`** (29 Aug 2026). |
| **G51** | **`SpcMeasurement.InSpec` stores a wrong answer for an asymmetric tolerance, and stores it on disk.** The column is `AS (CASE WHEN ABS([ActualValue] - [TargetValue]) <= [ToleranceValue] ... END) **PERSISTED**` - a **single symmetric** tolerance - while `AlloyProperty` has carried **min/max pairs** for gauge, width and diameter since 1 Aug 2026 (`Q22`). An asymmetric band is collapsed to one number and the verdict is **written**, not merely computed | Data / Correctness | **High** | A measurement inside an asymmetric band can be persisted as out-of-spec, or outside it as in-spec, and because the column is `PERSISTED` the wrong verdict is **stored** and will be read back by SPC reporting, the certificate and the suspend-material decision on DB6. Recomputing later does not repair rows already written | Replace the single `ToleranceValue` with a minus/plus pair on `SpcMeasurement`, matching what `AlloyProperty` already does, and restate the computed column against both bounds. ⚠ **`APIs.md` cites this as "`GapAnalysis.md` **D1**"** - repoint that citation to `G51` | Open - promoted 23 Aug 2026 from the retired `GapAnalysis.md` (**D1**), which called it the most serious finding in that document · ✅ **Owned by `FW-245`** (29 Aug 2026). ⚠ The column is `PERSISTED`, so **existing rows must be re-evaluated**, not just the expression changed. |
| **G52** | **`PinRole='Sole'` passes the `CHECK` constraint and matches none of the sequence validator's four tiers.** [`FlatWire_DDL_03_Materials.sql:428`](../30-database/sql/FlatWire_DDL_03_Materials.sql) admits `'Sole','PinnedFirst','Free','PinnedLast','PinnedBoth'`, but `RodOrderAllocation.md` §3's `partition(order.rods)` covers only `PinnedFirst`, `Free`, `Free`+`Partial` and `PinnedLast`. ⚠ **`PinnedBoth` is fine** — it is explicitly a member of both the `pinnedFirst` and `pinnedLast` sets. **`Sole` is in neither**, so `minTier` is undefined over a `Sole` row | Data / Correctness | **High** | `FR-546` requires an out-of-tier rod to be **refused**, at both pre-check-in and check-in, with no override. A `Sole` row makes the tier comparison undefined, so the validator either refuses a legitimate single-rod order or admits it by accident — and which one depends on how the implementation happens to treat an empty tier set, not on anything specified. Harmless **only** while a `Sole` order has exactly one rod, and **nothing enforces that**: no constraint ties `PinRole='Sole'` to a one-row order the way `CK_RodOrderAllocation_PinnedBoth` ties `PinnedBoth` to one | Special-case `Sole` explicitly in the validator — a single-rod order is trivially in sequence — **or** add the cross-row constraint that makes a `Sole` row its order's only row, mirroring the `PinnedBoth` rule. Prefer the constraint: it makes the tier question unreachable rather than answered. Covered by `TC-771` once the behaviour is decided | Open — raised 25 Aug 2026 from [`RodOrderAllocation_WorkedExamples.md`](../95-archive/design-notes/RodOrderAllocation_WorkedExamples.md) §12 finding 2, which recorded it without minting an id · ✅ **Owned by `FW-246`** (29 Aug 2026). |
| **G53** | **A welded coil now carries N shared-schema identities, and nothing specifies how they are RENDERED.** `Q88`/`Q89` (26 Aug 2026) decide that each source rod contributes its own alpha and that every one reaches `proddb..coils` — but no artifact says what the **coil label**, the **skid label** or the **welding-wire certificate** prints for a two-rod coil. `OutputCoilCompletion.md` §7.1 lists *"Source rod alphas — every rod in the traceability chain"* and stops there; the DB7 and DB7b mockups render `Source` = `R00041, R00042` — the rod **masters**, comma-joined — which is neither the part alphas nor the `" - "` alpha-join the client's own analysis establishes. ⚠ **`Q87` is the same question at the label layer and was deferred on the 24 Aug call**, so this cannot be closed internally | Requirements / UI | **High** | Blocks Phase 9's label and certificate work — and Phase 11's exit condition is *"cert traceability available before first shipment"*. Two mockup edits are owed, not just a spec line: the `Source` row's **values** change and the traceability panel gains a field per row | `Q87`, `Q88`, `Q89`, `FR-336`, `OutputCoilCompletion.md` §7.1, `phase-09`, `phase-11` | **Open — raised 26 Aug 2026** |
| **G54** | ⛔ **The 26 Aug alpha design has an unbuilt precondition: nothing registers an FL1 segment alpha in `proddb..coils`.** Every mint now passes a **blank** ignore list and relies on `GenerateCoilAlpha`'s own sweep finding prior siblings — which works only for alphas that reach the shared schema. `50_FlatWireDB_Proc_FlatWire_CompleteCoilOnSkid.sql` is the only script that writes `coils`; **no script writes `SpoolTraceability`.** | Database / Integration | **Critical** | **A blank-list mint at FL1 reissues `R00001A` on every spool.** The FL1 rows in `[RodOrderAllocation.md §2.8]` are the design, not current behaviour — so the traces read as though they work. It also keeps `OI-136` live: the shipped procedure's blank list is correct by design and unsafe in fact until this ships | **Build `FW-231`** — register the segment alpha at spool completion (phase-08). It must settle the `coil_status` value (`D-32` bars a new shared one), the **triple-counting** of rod + segments + coils under one six-character root, and mint/insert atomicity. ⚠ **Do not close the design as buildable until this lands** | **Open — gates the whole scheme** (`OI-138`) |
| **G55** | **FL2's spool check-in is constrained to payoff position `1`, while the canonical enum and the pinned lookup row both make FL2's payoff `3`.** [`FlatWire_DDL_04_Runs.sql:684-691`](../30-database/sql/FlatWire_DDL_04_Runs.sql) replaces `CK_SpoolCheckin_PayoffPos` with `CHECK ([PayoffPosition] = 1)` on the stated ground *“FL2 has ONE payoff (client, 21 Aug 2026)”* — which is true and is **not** the same claim as *which* payoff. `PayoffPosition` seeds `1 = Payoff1` (`Equipment='VPS'`, `IsRodFed=1`), `2 = Payoff2` (VPS, rod-fed) and **`3 = TraversingTakeup`** (`Equipment='TraversingTakeup'`, `IsRodFed=0`, `DisplayName='Traversing take-up (FL2)'`), and `CanonicalEnums.cs` documents `TraversingTakeup = 3` as *“FL2's single traversing take-up. FL2 has no two-bay staging.”* So the constraint pins FL2 to a **rod-fed VPS bay**. | Database / Backend | **High** | `FW-179`'s insert must write a value one leg calls wrong: `3` violates the `CHECK`, `1` contradicts the enum, the lookup row and `IsRodFed`. **Nothing catches either** — `SpoolCheckin.PayoffPosition` has **no FK** (only `FlatWireRunDetail` and `RodStaging` do), so the column is an unparented int whose only guard disagrees with the lookup it should point at. Adding the FK later would attach every FL2 spool to `Payoff1` | Decide which is right and change the loser: either widen the `CHECK` to `= 3` (and re-point `[API §4.6a]`, which sends no `payoffPosition` today) or restate `TraversingTakeup`'s pinned id. **Then add `FK_SpoolCheckin_PayoffPosition`**, without which the two legs can drift again silently. Found by running `TC-020` — the *membership* diff passes, because this is a disagreement about **meaning**, not members | Open · ✅ **Owned by `FW-246`** (29 Aug 2026). |
| **G56** | **`TC-020` cannot be run as the three-way diff it is written as, and for two of the fourteen enums it cannot be run at all.** The **TypeScript leg does not exist**: ~~no `flat-wire` library is present in any `ual-angular` checkout (three were searched — `ual-angular`, `Second-Branch/ual-angular`, `ual-angular-latest`)~~ ⚠ **evidence re-based 28 Aug 2026 from the `FW-149` review — a `flat-wire` library IS now present at `Second-Branch/ual-angular/projects/flat-wire`, and the conclusion is unchanged.** ⚠ **Re-based again 31 Aug 2026: the library is gone.** `FW-N03`'s build was reverted, so no `flat-wire` library is present in any `ual-angular` checkout and the struck original evidence reads true once more — across all three re-basings **the conclusion has never changed**. It is `FW-N03`'s **scaffold**: four exports — a placeholder component, the module, the routing module and an auth guard — and **no `.ts` file in that checkout defines `FM2_S1`, `RollAdjustTrigger`, `TraversingTakeup` or `ComponentState`**, nor any hub event or marker name, nor a `MockSignalRService`; `FW-132` and `FW-136` are unbuilt. ⛔ **Read the re-based evidence before closing this**: a re-check that finds the library present could wrongly conclude the leg exists. So the test runs C# ↔ DDL only, which **passes 12 of 14 with zero mismatches**; but **`LineState` and `AlertSeverity` have no DB `CHECK` anywhere** (neither is persisted — `CK_FlatWireRun_Status` is *run* status, a different vocabulary), so their only second leg is the one that does not exist. | Testing / Frontend | **Medium** | The two-way pass is real evidence and should be banked, but `TC-020` reads as a single pass/fail, so banking it looks like claiming the TS leg too — and **`LineState`/`AlertSeverity` are then asserted by nothing at all**. `LineState` is the one carrying a pending rename (`→ LineOperatingState`, `[PLCC §6.3]`) and a deliberate omission (**no `Stopped`**, `PLC-Q01`), which is exactly where a silent divergence costs most | Run and sign off `TC-020` **per leg**, not as one verdict — `FW-147` `P-84` and §6.1 record the C# ↔ DDL leg as **passed 27 Aug 2026**. The TS leg is **owed to `FW-132`** and is that story's to close; until then two enums are unverified by construction, not by omission | Open |
| ~~**G57**~~ | ✅ **RESOLVED 27 Aug 2026 — `SqlSetting:DSN` corrected to `"UA_Connection_String_dev00164001"`.** Verified against the **committed** configuration with no environment override: `GET /api/v1/flatwire/health` returns **`200`** with **`database.reachable: true, latencyMs: 33`** and **zero** *“could not resolve”* entries in the log. ⚠ **This is a per-environment value** — the variable name encodes the server (`dev00164001`), so each environment's `appsettings.{Environment}.json` needs its own, and the sibling services' `"UA_Database_UnitedDB"` is not it (that variable does not exist on these machines). ⚠ **The deferral that hid it is unchanged**: `AddCustomDbContext` still resolves inside its `AddDbContext` lambda and `FW-144`'s `ValidateOnStart` still does not cover the connection string, so **the next wrong value will also boot clean** — that residual is `FW-144`'s and is the reason this row is worth reading after it closed. *(Original text:)* ⛔ **`FlatWire.API`'s committed connection-string configuration cannot resolve, and nothing fails at boot.** [`appsettings.json`](../../Second-Branch/ual-api/API/Domain/FlatWire/FlatWire.API/appsettings.json) sets `SqlSetting:DSN` to **`"DEV00164-001"`** — a **server** name. But `DSN` names the **configuration section holding the connection string**, not a server: `CustomDbContext.ResolveConnectionString` reads `configuration["DEV00164-001" + ":ConnectionString"]`, and no such key exists in any file or environment variable. **Every sibling service sets `"DSN": "UA_Database_UnitedDB"`** — CoilCheckin, Planning and OPCConnection all do — which is an environment-variable name, and the `UA_Connection_String_*` family on the dev machines follows the same convention (`UA_Connection_String_dev00164001__ConnectionString`, which carries the `#CATALOG#` placeholder `CATALOG='FlatWireDB'` substitutes into). | Configuration / Backend | **High** | ⚠ **The service boots clean and looks healthy.** `AddCustomDbContext` resolves the string **inside** the `AddDbContext(options => …)` lambda, so the failure is deferred to the first query rather than raised at startup — which means **every repository, every Dapper call and the whole check-in transaction fail at run time on a service that started without a warning**. `FW-144`'s `ValidateOnStart` covers the OPC and SignalR groups and **not** the connection string, so nothing catches it. | **Set `SqlSetting:DSN` to the name of the variable holding the connection string, per environment** — not to a server. **Measured both ways 27 Aug 2026** by `FW-148`'s new health endpoint: as committed, `GET /api/v1/flatwire/health` returns `database.reachable: false` with *"No connection string at 'DEV00164-001:ConnectionString'"*; with `SqlSetting__DSN=UA_Connection_String_dev00164001` supplied at run time the same check returns **`reachable: true, latencyMs: 19`** against `FlatWireDB` on `dev00164-001`. **The code is correct and the configuration is wrong.** Consider extending `FW-144`'s `P-16` fail-fast to the connection string so the next such value stops the boot — but note `FW-148` deliberately does **not** resolve eagerly (`P-88`), because a health check that cannot start cannot report. | ✅ **Resolved 27 Aug 2026** — raised and closed the same day, found by executing `FW-148` and fixed in `appsettings.json`. **Residual with `FW-144`:** nothing validates the connection string at boot, so the failure mode survives the value |
| **G58** | ⛔ **`OPCConnection` reports no write failure to its caller, so `FR-074` cannot be implemented from the response.** `WriteTag` returns **`200`** with the tag list whatever happened, and neither write path sets `OPCTag.IsGood` — it comes back `null`. `OPCUAManager.WriteTag` (`ual-api/API/Domain/OPCConnection/OPCConnection.OPCUA/OPCUAManager.cs`) catches **every** exception — including the *"Cannot write to tag"* its own read-back check raises — and logs it at **`LogInformation`**; worse, that check compares `Convert.ChangeType(...) == Convert.ChangeType(...)`, two freshly-boxed `object`s, which is **reference equality and therefore false for every numeric tag**, so the verification always reports failure and the failure is always swallowed. `OPCDAManager.WriteTag` verifies correctly with `.Equals` but only logs a `Warning` and returns the read-back tag rather than a status — so **the two managers disagree about what a response means**, and `OPCInfo.ConnectionType` (read from `CommonDB`) decides which one runs. ⚠ **Distinct from `G33`**: that gap is a *wrong path* accepted by the controller; this is a *correct path the controller refused*, reported as success | Integration / Backend | **High** | `FR-074` requires *"if any individual tag write fails, an exception shall be raised"* and there is nothing in the response to raise it on. The audit trail `FR-075` mandates then records `Succeeded` for writes that did not land — so the two things a reviewer would use to reconstruct a machine configuration (`[PLC §11.3]`) both agree, and both are wrong. `TC-085` (**P1**) asserts a mid-batch failure aborts the check-in; **as the contract stands the failure is invisible and `TC-085` cannot fail** | **Two parts, and only the first is FlatWire's.** (1) `FW-151` `P-105` — after a real push, issue **one batched `ReadTag`** on the paths just written, compare, and audit the **read-back** value rather than the HTTP status. One extra round trip per operator action, never on the 10 Hz path. (2) The real fix is **per-tag status on `WriteTag`'s response** (populate `IsGood`, or return a per-tag outcome) plus raising the swallowed failure above `Information` — that is `OPCConnection`'s change, on four other consumers' paths, and **must not be made from `FW-151`**. ⚠ The `==` comparison is a genuine defect worth reporting on its own | **Open — raised 28 Aug 2026** from the `FW-151` review against the built `ual-api` · ✅ **Owned by `FW-236`** (29 Aug 2026). ⚠ **A change in ANOTHER service** — `OPCConnection` sits on four other consumers’ paths, so the response-shape change is theirs to accept. |
| **G59** | **No service identity exists for a PLC write that no operator initiated.** ✅ **Mechanism confirmed by executing `FW-151` on 28 Aug 2026, and it is sharper than "it gets a 401": the call fails BEFORE the network.** `RestClient.SetHeadersAsync` reads the token via `context.HttpContext.GetTokenAsync(...)`, so with no `HttpContext` it dereferences null; `RestClient` catches that and returns `Result.Fail`, and the failure surfaces on the **first** call (`GetOPCInfo`) as `"Object reference not set to an instance of an object."` — **a message that names neither the identity nor the caller**. Measured in `FW-151`'s harness, scenario 9, and **again on the running service on 31 Aug 2026 by [`FW-217`](../40-backend/tasks/FW-217.md)'s build — this time on the READ path**, which is the case the register already called the largest: `NullReferenceException at AuthenticationHttpContextExtensions.GetTokenAsync ← RestClient.SetHeadersAsync`, once per polled line, every tick. ⛔ **That build also settles a question a fixture might otherwise have looked like it answered:** `FW-217`'s test-only `OPCConnection` double answers the two read routes anonymously and **the ingest still never reaches it**, because this failure is on the CLIENT side of the seam. **No test double can mask `G59`, and none should be built to try.** `WriteTagCommandValidator` requires a **non-empty `AccessToken`** and `OPC.MachineId > 0`, and both `GetOPCInfo` and `WriteTag` sit behind `AddCustomMvc`'s global `AuthorizeFilter` — so every call needs a bearer token. Operator-initiated writes have one. **Two of `PLCTagService`'s five operations do not:** `SetITInhibit` is driven by `FW-205`'s watchdog conditions 3–5 (feet data unavailable · invalid · two consecutive recordings missed), which run in an `IHostedService` with no HTTP context, and hold/idle-and-restore can be reached the same way. `CoolingChamber` solves it by logging in with a **badge number** against the `Login` service and carrying the returned JWT, but nothing designates a service account, a badge, or a token lifetime for FlatWire | Integration / Security | **High** | **The interlock is the one write whose whole purpose is to block a line that is running blind**, and without a token it gets `400`/`401` instead of a write — a silent no-op on the safety-shaped operation, discovered when a footage feed drops. `TC-016`/`TC-017a` exercise the interlock's behaviour and neither would reveal an unauthenticated write, because both are written from the operator's side | Decide the identity before `FW-205` is built: a dedicated service badge via the `Login` service (the `CoolingChamber` precedent), or a token minted at check-in and held for the run's duration. ⚠ **Whichever is chosen, the failure must be loud** — `OpcHelper` catches, logs and returns `null`, which is exactly how this becomes invisible. Also settle whether an audit record for a system-initiated write carries a service `OperatorId` or a sentinel, since `AuditEntry.OperatorId` is required (`FW-151` `P-106`) | **Open — raised 28 Aug 2026** from the `FW-151` review against the built `ual-api` · ✅ **Owned by `FW-237`** (29 Aug 2026). ⚠ **The identity half only** (`P-127`). The **token** half stays with commissioning — simulate returns before `GetOPCInfo`, so nothing reaches the network today. |
| **G60** | ⛔ **Nothing registers flat wire with `OPCConnection`, so `GetOPCInfo` cannot succeed and no tag can be written.** Every OPC consumer in `ual-api` calls `GetOPCInfo` first and writes with the `OPCInfo` it returns — `MachineId`, `OPCServers`, `ConnectionType` (which selects the DA or UA manager) and `IsReadonly` are `CommonDB` state and cannot be hand-built, and `WriteTagCommandValidator` rejects `OPC.MachineId <= 0`. Two halves are missing. **(1)** `UAL.Constants.GlobalConstants.OPCModules` has **five** members — `None`, `HoleDetection`, `CoilReceiving`, `HandheldService`, `FurnaceScheduling` — and **no flat wire one**, while `GetConfigurationsQueryValidator` requires `OPCModuleId > 0`, so there is no value to send. **(2)** **None of the eight files in [`30-database/scripts/`](../30-database/scripts/) touches `OPCModules`, `OPCServers`, `OPCTags` or `OPCTagApplicationMapping`** — `10_CommonDB_Insert_WIPStations_FlatWire.sql` registers WIP stations and machines only (`FW-003`), which is what such registration would be *keyed* by, not the registration itself. ⚠ **Distinct from `OI-A`**, which asked *where the tag path strings live*; this asks *whether `OPCConnection` knows the flat wire lines exist at all*, and the answer was no under either resolution of `OI-A`. ✅ **`OI-A` is now CLOSED — `D-44`, 4 Sep 2026: the paths live in the `CommonDB` registration**, so this gap's `OPCTags` half stops being conditional and becomes required. See **`G93`** for what that costs | Integration / Deployment | **High** | **A commissioning blocker, not a build one** — `SimulatePLCTagPush` is `true` in every environment until commissioning and `FW-151`'s simulate branch short-circuits **before** the resolve, so the service builds, boots and demos. The cost is that **the entire resolve-and-write path is dead code until the 40 h Phase-14 window**, which `[PLCC §4]` already calls the worst compression in the schedule, and it is where `C1`/`C11` also have to confirm every path (`G33`) and the FM2 station names (`G32`). Discovering the registration is absent *inside* that window spends commissioning time on data entry | Own it before Phase 14: add the flat wire `OPCModules` member (a shared-constants change, so it needs the owning team) and a **`CommonDB` OPC registration script** beside `10_CommonDB_Insert_WIPStations_FlatWire.sql`, keyed on the `machines` rows `FW-003` creates, covering `OPCModules` · `OPCServers` · `OPCTagApplicationMapping` and — ✅ **`OI-A` resolved toward the `CommonDB` registration (`D-44`, 4 Sep 2026), so `OPCTags` is now required, not conditional** — the 72 tag rows too. ⚠ **The "or the tag rows are written twice" warning is DISCHARGED**: there is one home, and it is this registration. Two consequences follow. **(a)** `FW-144`'s `ConfigurationTagPathResolver` is replaced by a `CommonDbTagPathResolver` behind the unchanged seam, which is what the seam was built for; **(b)** `FW-151` **stops overwriting** `GetOPCInfo`'s `Tags` and starts consuming them, so the *non-tag* half is no longer the only part required. ⛔ **`G93` is now a prerequisite of this row** — `OPCTags` has no logical-name column, so the script cannot write a `TagKey` until the shared table is altered. ⚠ **`10_…` is deliberately outside `FlatWire_Scripts_RunAll.sql`** (irreversible writes to shared tables, run by hand after sign-off) and a registration script belongs in the same category | **Open — raised 28 Aug 2026** from the pre-execution re-review of [`FW-151`](../40-backend/tasks/FW-151.md) · ✅ **Owned by `FW-238`** (29 Aug 2026). ✅ **`OI-A` decided 4 Sep 2026 (`D-44`) — this row's `OPCTags` half is confirmed in scope and the write-twice caveat is gone.** ✅ **`G93` is WITHDRAWN (`D-45`, 5 Sep 2026) and no longer blocks this** — there is no `TagKey` column and none is needed. ⚠ **Amended 5 Sep 2026, and the shape changed in three ways.** **(1)** The seed is **41 rows, not 72** — FL1's 17 + FL2's 22 data tags + **2 system-error rows** (`G95`) — because **FL3 is deliberately not registered** (`D-47` / `G99`): it has no controller of its own, so its tags *are* FL1's and FL2's and machine 127 gets nothing. Do not reconcile that 41 against `FW-144 §8.2`'s **72 bound paths**; both are correct and they count different things. **(2)** The module id is **6**, not the enum's apparently-free `5`, which is in production use by the slitters (`G96`). **(3)** Every path carries a `PLC` element now (`G98`). ⛔ **New prerequisite: `FW-236` / `G94` must merge first.** `ConnectionType = 21317` makes flat wire the first module to select `ual-api`'s `OPCUAManager`, and until `G94`'s identity fix lands `ReadTag` and `WriteTag` address namespace 0 — a different node — so a push writes nowhere. ✅ **`G97` IS RESOLVED (`D-49`, 5 Sep 2026) and no longer blocks this row** — the lines use the endpoint pair at `OPCServersIdx` **1 and 2**, both lines onto both. ⚠ **`D-48` narrows what this row's `OPCServers` half means**: that table is a **lookup**, so the registration **selects** its endpoints and writes only four tables — `OPCModules` · `OPCServerApplicationMapping` (**4** rows, not 2) · `OPCTags` · `OPCTagApplicationMapping`. ⛔ **Two gates remain on *running* it, and neither is about the design**: `G100` (the target `CommonDB` is stale) and `G94` / `FW-236` (which must merge before a `21317` module is activated); [`11_`](../30-database/scripts/11_CommonDB_Insert_OPCRegistration_FlatWire.sql) and its reverse [`16_`](../30-database/scripts/16_CommonDB_Delete_OPCRegistration_FlatWire.sql) are written and abort on their own placeholder until it arrives. |
| **G61** | **21 blocker citations in the backlog use the retired `OQ-` prefix and resolve to nothing.** The `Q##` registers were renumbered in Aug 2026 and every canonical `OQ-##` citation outside the registers was rewritten — but `TaskBreakdown.md` kept **74 occurrences of 21 distinct ids**: `OQ-2`, `OQ-3`, `OQ-4`, `OQ-5`, `OQ-6`, `OQ-10`, `OQ-12`, `OQ-13`, `OQ-14`, `OQ-15`, `OQ-17`, `OQ-18`, `OQ-22`, `OQ-23`, `OQ-24`, `OQ-25`, `OQ-62`, `OQ-67`, `OQ-73`, `OQ-76`, `OQ-79`. They now carry into the task front-matter as `blocked_by` values that name no register entry, so five of the twelve most-blocking items on [`STATUS.md`](../STATUS.md) read *"id not found in any register"*. ⚠ **`OQ-22` is not necessarily `Q22`** — the Aug 2026 retarget was done **by subject**, not by number, so this must be resolved against the old-to-new map in [`CHANGELOG.md`](../CHANGELOG.md) and not by stripping the prefix. | Planning / registers | **High** | Twelve tasks across seven phases cite a blocker nobody can look up. Two of them (`OQ-10` footage-to-weight, `OQ-22` alloy tolerances) are cited as `Critical` elsewhere, so the most consequential unknowns in the build are the ones with no resolvable id | Resolve each of the 21 against `CHANGELOG.md`'s map, rewrite the citation in the backlog card and the task front-matter, and delete the id from `STALE_BLOCKER_IDS` in [`../tools/check_docs.py`](../tools/check_docs.py) as it is fixed. The checker holds the line once the list is empty | **Open** — found 29 Aug 2026 by `check_docs.py` |
| **G62** | **Eleven stories name one stream on their card and have always had their plan in another stream's folder.** `FW-081` and `FW-202` are carded `FE` with plans under `Backend/`; `FW-219`, `FW-220`, `FW-223`, `FW-225`, `FW-229`, `FW-230`, `FW-231` and `FW-243` are carded `DB` with plans under `Backend/`; `FW-249` is carded `BA` with its plan under `Database/`. *(`FW-135`, `FW-136` and `FW-137` are **not** in this set — they are carded `RT` and sit under `Frontend/` deliberately, which the checker allows.)* The disagreement predates the task-file migration; it was invisible while the card and the plan were separate documents and became visible the moment both were read by one tool | Planning / delivery | **Low** | Reporting by stream is wrong for eleven stories, so `[CE]`'s per-stream hour split and any "what is the DB stream carrying?" question are answered from the card while the work happens elsewhere | Decide per story which is right - the card or the placement - then either correct the card's `Stream` or move the task file. Remove the id from `FOLDER_STREAM_EXCEPTIONS` in [`../tools/check_docs.py`](../tools/check_docs.py) as each is settled | **Open** — found 29 Aug 2026 by `check_docs.py` |
| **G63** | **`FW-071` and `FW-072` are a circular dependency, so on a strict reading neither can ever start.** `FW-071` (Pause and Resume dialogs, Phase 6) declares *"the resume dialog depends on **FW-067** and **FW-072** (Phase 7) for two of its four outcomes"*; `FW-072` (Rod checkout dialog, Phase 7) declares `**Dependencies:** FW-134, FW-174, FW-071`. Both are `FE`, both are sized 24 h, and they sit in different phases, which is why the loop was never noticed | Planning / delivery | **Medium** | Every "what can I start now?" calculation excludes both stories permanently, and they are the Pause/Resume and Rod Checkout dialogs - two of the four run-event dialogs. A dependency graph that contains a cycle silently produces a wrong answer rather than an error | **The card's own wording suggests the fix**: `FW-071`'s need for `FW-072` is partial (*"two of its four outcomes"*), not total, so it is a soft dependency and the edge should be dropped from `FW-071.depends_on` with a note - but which edge to cut is a delivery decision, so it is recorded rather than taken here | **Open** — found 29 Aug 2026 by `check_docs.py` |
| **G64** | **Six waves of the 6 Aug client call were never propagated, and they live only in a ledger.** [`ClientCall_2026-08-06_SyncPlan.md`](../95-archive/source-documents/ClientCall_2026-08-06_SyncPlan.md) records `W1` (registers) and `W2` (generation spec) as applied and leaves **`W3`–`W8` unexecuted**: requirement-document edits (`RodPreCheckin` §5.3 / §6.2), `FR-280`–`FR-282` gaining a persistence target, API contract and schema changes, a read-only dancer element on the PLC surface, sprint/test-plan updates that unblock `FW-N08`, and the `pause_run.js` machine-stop prompt chain. Every one is a decision the client has already given us. | Planning / propagation | **High** | Six client answers are agreed and unbuilt, and nothing outside the ledger knows. `FW-N08` (wire break) stays uncosted and blocked on `G34` while `W7` — the wave that unblocks it — sits undone. A ledger is not a tracker: the work is invisible on [`STATUS.md`](../STATUS.md) because no task cites it | Convert each remaining wave into work against the task files it touches, or record per wave why it is not being done. Then archive the ledger. **Do not archive it first** — the waves are the only record | **Open** — raised 29 Aug 2026 during the restructure |
| **G65** | **The welded-coil two-alpha change is a design of record with eight waves unbuilt, and one of them is blocked.** [`WeldedCoilAlpha_2026-08-26_SyncPlan.md`](../95-archive/sync-plans/WeldedCoilAlpha_2026-08-26_SyncPlan.md) applied `S1` and `S2` — the registers and the design — and states plainly: *"Eight waves remain. No DDL run, no requirement text written, no rename swept."* `S1a` is **blocked**. It carries three coupled changes: the client's correction that a welded coil keeps **two alphas, not one**; the `CommonDB` to `PlanningDB` generator cutover; and the `CoilTraceability` to `FlatWire_CoilTraceability` rename | Schema / requirements | **High** | The design of record and the built schema disagree, and the disagreement is in coil genealogy — the chain behind the **welding-wire customer certificates**, which is an MVP-1 obligation. A rename that is designed but not swept is the same class of defect as `G62`, at table scope | Sequence the eight waves against the affected tasks (`FW-186` traceability, `FW-230` segment alpha, `FW-231` alpha registration), resolve `S1a`'s blocker, and run the DDL. Track each wave as it lands | **Open** — raised 29 Aug 2026 during the restructure |
| **G66** | **`DB-S1` stops being unreachable-by-construction and becomes unreachable-by-procedure.** `[SEC §8.8b]` and `[SIM §2.4]` require the simulator control surface to be **absent, not forbidden**, and the console half of that was *"`DB-S1` does not resolve when simulation is off"* — a route that simply did not exist in the bundle. `D-33` (29 Aug 2026) makes the console a **standalone WinForms executable**, and an executable cannot be unregistered. ⚠ **Nothing prevents someone copying the EXE onto a commissioned line’s machine.** | Security / Deployment | **Low** | **The primary control is unaffected and this is why the severity is Low**: with simulation off, `MapFlatWireSimControlSurface` returns before mapping anything, so every `/sim` route is **404** to any caller, and the console shows a lock-out panel rather than a live-looking surface (`[SIM §9.4]`). What is lost is the *distribution* guarantee — previously the tool could not be present on an operator machine, now it merely should not be. ⚠ **On balance `D-33` improves this**: the Angular console shipped to **every** operator machine inside the shop-floor bundle whether or not its route resolved; the EXE ships only where it is installed | Two things, neither of them code. **(1)** `[DEP §1.2a]` names where the console may and may not be installed — keep that list true. **(2)** At commissioning, add *"`FlatWireSimConsole` is not present on this machine"* to the `C1`/`C11` checklist, so the procedure is verified once rather than assumed forever. ⚠ **Do not "fix" this by adding a role check** — `[SEC §8.8b]` is explicit that the 404 is the control and the role policy is the backstop, not the reverse | **Open** — raised 29 Aug 2026 with `D-33` |
| **G67** | **`FW-150`'s `stagedWeights` cache has never had an eviction path, so `PercentRemaining` is computed against the previous rod.** The per-payoff staged weight is cached to spare the 10 Hz path a lookup, and **nothing invalidates the entry when the bay changes** — a restage, a rod checkout that releases a bay, a weld, or the staged-row consumption at check-in all leave the previous rod's denominator in place. The eviction trigger that exists and is unused is **`BayStateChanged`**, which `RodStaging.ConsumeAtCheckIn` already raises. ⚠ **This gap was raised on 29 Aug 2026 as `G62` by four Backend plans and by the de-stubbed `CheckInService.cs`, and never reached this register — while `G62` had already been given to the stream/folder mismatches.** Renumbered to `G67` and registered on 31 Aug 2026; a pre-31-Aug document citing `G62` for `stagedWeights` means this | Backend / real-time | **Medium** | Every consumer of `PercentRemaining` is wrong after any bay change until the run ends — the payoff gauge on Dashboard 1, the post-weld `PayoffWeight` re-broadcast (`FW-172`), and the checkout release path (`FW-174`). It is a **stale-denominator** fault, so it reads as a plausible number rather than an error, and no test that checks for a value rather than the *right* value can see it | Subscribe the cache to `BayStateChanged` and evict the entry for that station and position. `FW-239` AC 2 is the owner and the fix is a **bug fix, not a wiring job** — the card reads as though the handler merely needs connecting. ⚠ Check-in's consumption is one of the triggers, so `FW-157` must not consume a staged row without raising the event | **Open** — raised 29 Aug 2026 by [`FW-239`](../40-backend/tasks/FW-239.md) as `G62`; registered under its own id 31 Aug 2026 |
| **G68** | **The console is required to display two values that no interface carries, and the two specification sections that decide it contradict each other.** `[SIM §9.2]` requires `DB-S1` to show the active **`lbPerFt`** — *displayed* — and the noise **seed** — *"displayed and settable"*. ⚠ **This row read *"displayed and settable"* of BOTH values until 1 Sep 2026, and §9.2 attaches it to the seed alone.** The distinction decides the shape of the fix: a **read** closes this gap, because the seed's settable half is already `POST /sim/{lineId}/run`'s seed parameter (`[SIM §5.7]`), whereas a runtime `lbPerFt` **setter would decide `Q10` / `OI-45` by the back door** against `P-271`. **No configuration write is owed.** And `[SIM §5.3]` insists `lbPerFt` *"must not be buried as a constant"* because it is *"the single most load-bearing unverified number in this model"*. But **nothing on any wire carries either value.** `SimLineState` has **six** fields — line, running, footage, gauge offset, drift per tick, dropped-tick countdown — and `[SIM §9.2]` **itself forbids widening it**; `[SIM §8.1]`'s five endpoints carry no configuration read, the seed appearing only as a *parameter of* `POST /sim/{lineId}/run`; and `HubContracts.cs` declares no such member. ⚠ **And no story closes it**: `[TB §7]`'s **`FW-215`** card is exactly `[SIM §8.1]`'s five endpoints, so the surface that would carry a configuration read is not carded either | Requirements / Integration | **Medium** | **A requirement that cannot be built, in the one place the project decided visibility mattered.** `Q10` / `OI-45` leave the footage-to-weight basis undecided, and `[SIM §5.3]`'s answer was to surface the assumption on screen rather than bury it — so the mitigation for the project's most load-bearing unknown is the thing that has no route. The failure mode is quiet: a console author with no value to bind will render a blank or a `0`, and **a `0` reads as a measured factor** where `Reading` already uses null to mean *not read* | Add **one configuration read** to `FW-215` — a `GET /sim/config` returning the active `lbPerFt`, the seed and the simulation flag — and **amend the `FW-215` card in `[TB §7]`**, which prices five endpoints and would now carry six. ✅ **Both amendments made 1 Sep 2026**; the gap stays open until the endpoint exists. ⚠ **Do not widen `SimLineState`**: `[SIM §9.2]` forbids it, `P-267` fixes its six fields as command echo, and a console polling it would be *a second telemetry path competing with the hub*. Until then **`FW-214`'s `P-290`** renders an explicit *unset* state — never a blank and never a zero | ✅ **RESOLVED server-side 1 Sep 2026 — `GET /sim/config` is BUILT** and carries the active `lbPerFt`, the noise seed and the simulation flag, on `SimConfigState` rather than by widening `SimLineState` (which is untouched at six fields). **`lbPerFt` is nullable and answers null** while it is unset, so the console can render *unset* rather than a `0` that reads as measured (`P-271`). ⛔ **A READ was sufficient, and the gap had said otherwise**: `[SIM §9.2]`'s *"settable"* attaches to the seed alone, and the seed is already `POST /run`'s parameter — **so no configuration write was built and none is owed**, a runtime `lbPerFt` setter being `Q10` / `OI-45` answered by the back door. ✅ **And the SPECIFICATION half closed later the same day** — `[SIM §8.1]` gained `GET /sim/config` as a sixth row, so the endpoint is now built *and* specified, and `[SIM §9.2]`'s note records that a read is sufficient. ⚠ **ONE RESIDUAL REMAINS AND IT IS ON THE CONSOLE**: `FW-214` is built and binds nothing to this endpoint — `P-290` still renders an explicit *unset*, which is correct behaviour against a value it cannot read. A console change is owed before either value reaches a screen. Raised 31 Aug 2026 by [`FW-214`](../50-frontend/tasks/FW-214.md). Owner **`FW-215`** (server, done) / **`[SIM]`** (spec, done) / **`FW-214`** (console, **owed**) |
| **G69** | **`[SIM §9.2]` requires the console to show TWO state chips and the wire carries ONE vocabulary.** The section is explicit - *"state badge (run status **and** line state, per §4.4 - two separate chips)"* - and §4.4's whole point is that they are different things: a mill can stop with the run still `Running`, which is `FR-141`'s edge. **Line state has a source**: `[SIG §5.2]` event 7, `LineStatusEvent { LineId, Status, OrderId, Alpha }`, where `Status` is a `LineState`. **Run status has none.** No hub payload carries a run status at all - `RunStatus` appears nowhere in `HubContracts.cs` or `IFlatWireClient.cs`, and exists only as a `Vocabularies.RunStatus` constant class. The nearest thing on any wire is `SimLineState.Running`, a **bool** - the lossy projection of the simulator's four-state `SimRunState` (`P-274`), and the simulator's run state rather than the business run's | Requirements / Integration | **Medium** | **A specified UI element with no data behind it, on the one distinction `[SIM §4.4]` exists to protect.** The tempting fix is the damaging one: feeding a four-state chip from the bool shows **`Paused` as stopped**, which is exactly the run-versus-line confusion the two chips were separated to prevent - and it fails silently, because a paused line and a stopped line look identical on the panel | **Decide which vocabulary the console is meant to show.** If it is the SIMULATOR's run state, `FW-215` can widen `POST /sim/{lineId}/run`'s reply or add the configuration read `G68` already asks for - ⚠ **not** by widening `SimLineState`, which `[SIM §9.2]` forbids. If it is the BUSINESS run's status, that is a `[SIG §5.2]` event and belongs to `[SIG]`, not to a tool. Until then **`FW-214`'s `P-301`** renders a two-state chip labelled at two states, and `Run: unknown` before any read | ⛔ **Open, and ARMED since 1 Sep 2026 — the severity changed without the row changing.** `FW-215`'s build made `SimRunState.Paused` commandable (`G73`), so the failure this row describes went from **impossible to reproduce** to **live**: until now a four-state chip fed from `SimLineState.Running` could not be caught showing `Paused` as stopped, because `Paused` could not occur. It can now. ⚠ **The decision is still not `FW-215`'s to make** and the story shipped without pre-empting it: `POST /run`'s reply and `GET /sim/config` are both available to carry a run state once someone chooses the vocabulary, and neither widens `SimLineState`. Raised 31 Aug 2026 by [`FW-214`](../50-frontend/tasks/FW-214.md)'s build. Owner **`[SIG]`** or **`FW-214`**, per the resolution above |
| **G70** | **The E2E fixture `FW-217` delivers cannot be steered, because the control surface is gated on the flag the fixture requires to be OFF.** `FW-218`'s `/sim/**` is mapped only when `FlatWireOpc:SimulateOpcFeed` is **true** (`P-38`, `[SIM §2.4]`) and its four handlers take **`OpcFeedSimulator` concretely**, which is registered only in that same branch. The `OPCConnection` double needs `SimulateOpcFeed` **false** - that is what registers `FW-N05`'s real ingest, which is the whole point of the story - so with the double on, `/sim` is a 404 and the models behind it have **no steer, no stop, no fault and no scenario selection**. ⚠ The double therefore serves each model's **default in-spec run** and nothing else. `FW-213`'s twelve scenario and fault behaviours are built and reachable on the objects, and unreachable from outside the process. ⚠ `[SIM §2.4]`'s rule is not violated so much as **out of date**: with the double on the feed IS synthetic, so the condition *"simulation is off"* no longer means what the flag says | Testing / Integration | **Medium** | **An E2E suite can assert that the pipeline carries readings and little else.** It cannot drive `[TRP §8]` step 3's N consecutive out-of-spec readings, cannot exercise `FW-205`'s conditions 3 and 5, and cannot reach `FW-202`'s stop-confirmation edge - each of which has exactly one route, and that route is `/sim`. ⚠ It also leaves `P-304` **unexecuted**: the double's 503-on-a-silent-tick branch is the faithful mapping of a comms drop onto this transport, and nothing can currently arm the drop that would exercise it | **`FW-215`.** Widen the mapping condition to *"the readings come from a line model"* rather than *"`SimulateOpcFeed` is true"*, and give the handlers a seam that both `OpcFeedSimulator` and `OpcConnectionDouble` satisfy. ⛔ **Do not change `OpcFeedSimulator`'s four lever signatures** - they are a built, verified control surface (`P-266`) and `FW-218` is closed. ⚠ **Do not add a second control surface for the double**: `P-39` makes `/sim` a subset of `FW-215` rather than a variant, and a divergent shape strands `FW-214`'s console | ✅ **RESOLVED 1 Sep 2026 by [`FW-215`](../40-backend/tasks/FW-215.md)'s build, and measured rather than asserted.** The mapping condition is now *"a line model is hosted"* — `SimulateOpcFeed` **or** `UseOpcConnectionDouble` (`P-309`) — and the handlers resolve **`ISimLineHost`** (`P-307`), which `OpcFeedSimulator` and `OpcConnectionDouble` both satisfy; the double had **no levers at all** and now has the same seven. **Measured in a fresh process: with the double mapped `/sim` answers `401`, where it answered `404` before.** 17 harness checks drive steer, drop, scenario, fault, all four run states and a model replacement **through the double**, so `P-304`'s 503-on-a-silent-tick branch can now be armed. ⛔ **`OpcFeedSimulator`'s four lever signatures are unchanged** (`P-266`) and **no second control surface was added** (`P-39`). ⚠ **`G59` is untouched and still blocks the end-to-end leg** — steerable is not the same as authenticated. Raised 31 Aug 2026 by [`FW-217`](../40-backend/tasks/FW-217.md)'s build |
| **G71** | **FL2's only load cell has no tag key, so its weight cannot cross the tag surface at all.** `Fl2LineModel.ReadLoadCells` reports one entry at **`PayoffPosition.TraversingTakeup`** - *"FL2 has no two-bay staging"* - and the whole tag surface carries exactly two weight keys, **`Payoff1Weight`** and **`Payoff2Weight`** (`[PLC §5.2.2]`), which `FW-N05`'s mapping lands on `Payoff1` and `Payoff2`. There is no third key and no `TraversingTakeup` arm. ⚠ **Invisible until the real ingest path was actually exercised**: through `FW-211`'s in-process adapter the `Reading` goes straight to the channel carrying `TraversingTakeup`, and only a round trip through the tag map loses it. `FW-217`'s `P-305` answers **null** rather than inventing a fallback | PLC / Data | **Low** | **A modelled instrument that the transport cannot carry**, so FL2's payoff panel is fed by nothing on the real path while it is fed on the simulated one - the exact divergence `P-37` says must not exist between the two feeds. ⚠ **Currently moot downstream and for an unrelated reason**: `FW-150`'s `SendPayoffsAsync` already sends no `PayoffWeight` on FL2, because `PercentRemaining` is non-nullable and `RodStaging` is FL1/FL3 only - so the hole is masked by a second hole rather than by a design | Decide at commissioning whether FL2's take-up load cell is addressed at all, and if it is, add its row to `[PLC §5.2.2]` and its arm to the ingest's mapping and to the double's projection - **three edits that must land together**. ⚠ Do **not** map `Payoff1Weight` to *"the line's only load cell"* as a convenience: that invents a mapping the tag surface does not have, and it would make the fixture prove something the real path cannot do | **Open** - raised 31 Aug 2026 by [`FW-217`](../40-backend/tasks/FW-217.md)'s build. Settled by commissioning tests `C1` / `C11`, which are what read a path off a controller |
| **G72** | **The built simulator control surface guards on a role name that is not in the role matrix, so it will keep denying after the claim is issued.** `SimControlSurface.cs` carries `RequireAuthorization(new AuthorizeAttribute { Roles = "Engineer,Admin" })`, and **`Engineer` is not one of `[SEC §8]`'s six roles** - the column is **`Engineering/Maintenance`**. `[SEC §8.8b]` recorded that correction on 27 Aug 2026 and the code was written from `[SIM §8.4]`'s shorthand instead. ⚠ **`FW-145` does not resolve it by landing**: it registers a named policy - **`SimulatorControl` = `RequireRole(EngineeringMaintenance, Admin)`** - *"registered here, applied elsewhere"*, and a raw `Roles` string does not bind to it. ⛔ **Invisible today for the right reason and the wrong one**: the routes deny because no role claim is issued at all, which is exactly what they will do afterwards | Security / Backend | **Medium** | **A fail-closed surface that stays closed after the fix that was supposed to open it**, and the symptom is identical before and after - a `403` on every call. `FW-218` verified `P-38` both ways (`404` off, `401` on) and could not have caught this: `401` is reached before any role is evaluated. The likely diagnosis when it bites is *"`FW-145` is broken"*, in a story that is closed and correct | **`FW-215`.** Bind **`RequireAuthorization(FlatWirePolicies.SimulatorControl)`** and delete the role strings - one policy name, registered once by `FW-145`, so the spelling lives in `FlatWireRoles` and nowhere else. ⚠ **Do not "fix" it by adding an `Engineer` role**: the matrix's six are `[SEC §8]`'s and a seventh would be invented here. ⛔ **And do not relax it to a bare `[Authorize]`** - that ships *"any authenticated operator may drive the control plane"*, the one thing `[SIM §8.4]` forbids. Verifiable only once `FW-145` issues the claim | ⛔ **NARROWED 1 Sep 2026, NOT CLOSED, and the distinction matters.** `FW-215`'s build **deleted the literal**: the guard now reads `FlatWireRoles.EngineeringMaintenance` / `.Admin`, so the spelling lives in `FlatWireRoles` alone and this surface needs **no edit** when `G6`'s mapping arrives — which is that file's own stated rule, *"policies and group joins bind to the constants, never to literals"*. ⛔ **But those constants are still `"TBD"` and the `SimulatorControl` POLICY is not built** — `FlatWireRoles` forbids building it here in terms (*"`FW-145` owns the POLICIES and this story does not build them"*, `P-75`), so `FW-145` still replaces one line with `RequireAuthorization(FlatWirePolicies.SimulatorControl)`. ✅ **The unmapped state is now announced rather than silent**: the surface logs, at start-up, that it denies every caller and why, on the `FlatWireHub.OnConnectedAsync` precedent — so the `403` cannot be mistaken for a working guard in a review or on a smoke test. **Verifiable only once `FW-145` issues a claim.** Raised 1 Sep 2026 while reconciling the [`FW-215`](../40-backend/tasks/FW-215.md) card. Owner **`FW-145`** |
| **G73** | **`[SIM §9.2]` specifies a `Pause` control and nothing on any route commands it, so two of the model's four run states are unreachable from outside the process.** The section puts **Start / Stop / Pause** on every line panel. The built control surface's only run lever is `OpcFeedSimulator.SetRunning(LineId, bool)`, which maps to `SimRunState.Running` or `SimRunState.Stopped` and nothing else - so **`Paused` and `Idle` can never be commanded**, and `[SIM §4.4]`'s run-versus-line distinction (*"a mill can stop with the run still `Running`"*, `FR-141`'s edge) has **never been demonstrable**. ⚠ **The console does not merely grey it - it has no Pause control at all**, and Pause is not among `G43`'s five greyed items either, so nothing records the absence. ⚠ **`SetRunState(SimRunState)` on `ILineModel` already reaches all four values**; only the HTTP surface narrows them | Requirements / Integration | **Medium** | **The one distinction `[SIM §4.4]` exists to protect cannot be exercised**, so no test and no UAT step can show a paused line reading differently from a stopped one. ⚠ **It also masks `G69` and then un-masks it**: that gap warns a four-state chip fed from `SimLineState.Running` would show *"`Paused` as stopped"* - harmless today precisely because `Paused` cannot occur, and live the moment this is closed. The two must be read together | **`FW-215`.** Carry a **target run state on `POST /sim/{lineId}/run`** - the route is being built anyway and `SetRunState` already accepts all four values. ⛔ **Do not add a seventh route**: `[SIM §8.1]`'s shape is what `FW-214`'s console was built against, and `DELETE /sim/{lineId}/run` must keep being the `Stopped` edge `FW-202` is armed by. ⚠ **Close `G69` first or with it** - making `Paused` reachable without deciding the chip vocabulary is what turns a latent display bug into a visible one | ✅ **RESOLVED server-side 1 Sep 2026, the same day it was raised.** `ISimLineHost.SetRunState` reaches all four values and `POST /sim/{lineId}/run` carries a **target run state**, so `Paused` and `Idle` are commandable and `[SIM §4.4]`'s distinction is demonstrable for the first time. **All four states measured on both hosts**, and a run start can leave a line `Paused`. ⛔ **No seventh route was added** and `DELETE /run` still carries the `Stopped` edge `FW-202` is armed by. ⚠ **RESIDUAL on the CONSOLE**: `FW-214` has **no Pause control at all** — not even a greyed one — so `[SIM §9.2]`'s three-button panel is still two buttons. ⚠ **And this ARMS `G69`**: a chip fed from `SimLineState.Running` can now be caught showing `Paused` as stopped, which it could not be before. Raised 1 Sep 2026 on the second-pass review of the card. Owner **`FW-215`** (server, done) / **`FW-214`** (console, owed) |
| **G74** | **`CK_PSC_ComponentName` offers ONE `EdgeSet` value where a schedule needs TWO edger positions, and the schedule grid is missing five columns the client's screen shows.** The client's 31 Aug 2026 FL2 schedule sequences `E1` (between S1 and S2, `.749 → .740`) and `E2` (between S2 and S3, `.746 → .743`) as **separate steps with different widths**. Two rows *can* be stored — `UQ_PassScheduleComponent_Sequence` is on `(PassScheduleId, Sequence)` — but **both carry the same `ComponentName`**, so position is recoverable only by inferring it from `Sequence`. That breaks the position-only identifier rule `[DDL 02 §3]` states, and `EdgerId` identifies the fitted **tool**, not the **station**. Separately, `PassScheduleComponent` has **no `Operation`** (`DRAW`/`FLAT`/`EDGE`), **no `EntryWidth`/`TargetWidth`**, **no `Reduction`** and **no `TargetSpeed`** — all four are on the client's grid, and the edger rows exist *to change width*. `PassSchedule` has **no anneal-gauge column** although it appears in the filter and header on all three lines | Database / Requirements | **High** | The FL2 and FL3 pass schedules cannot be expressed unambiguously; **the PLC push cannot choose an edger tag path from an ambiguous component name**, compounding **`G29`** — *"no edger tag path exists on any line, yet edge type is in the push payload"* | Replace `'EdgeSet'` with two position values in `CK_PSC_ComponentName`; add `Operation`, `EntryWidth`, `ExitWidth`, `ReductionPct`, `TargetSpeed` to `PassScheduleComponent` and an anneal-gauge field to `PassSchedule`. ⚠ Moves the object baseline — **count from a deploy, never compute**; pair with the `FlatWire_SampleData_Schedule.sql` FL1 edger correction so there is one recount. Answers **OPEN POINT 4(b)** of `FlatWire_DDL_02_Schedule.sql` | **Open** — raised 2 Sep 2026. ⭐ **STRENGTHENED 3 Sep 2026 — there are now named client formulas producing exactly the five missing columns.** `D-43`'s formula set computes `ReductionPct` (`F5`/`F11`/`F12`), `EntryWidth`/`ExitWidth` (`F15`/`F16`/`F19`, and `F20` for the edger's thickness response) and elongation feeding `TargetSpeed` (`F13`) — so the columns are no longer *"fields the client's screen shows"* but **outputs the engine will compute and have nowhere to persist**. The recommended fix is unchanged |
| **G75** | **`PayoffPosition` admits `VPS` but not `TPO`, and cannot tell the two takeups apart.** `CK_PayoffPosition_Equip` allows `'VPS'` and `'TraversingTakeup'`; `CK_PayoffPosition_Code` allows `'Payoff1'`, `'Payoff2'`, `'TraversingTakeup'`. The client's Setup/Handling lists use **`VPS`** throughout FL1/FL3 — a clean match — but **FL2 uses `TPO`** (*Rotate Payoff: TPO*, *Load & Prep Spool: TPO*, *SPC - Payoff: TPO*), which is in neither CHECK; presumably *traversing payoff*, but the third allowed value is a **takeup**, not a payoff. The same lists distinguish **`Takeup-1`** (FL1's intermediate spool) from **`Takeup-2`** (FL2's coreless coil takeup), where the lookup holds one `TraversingTakeup` row | Database | **Medium** | FL2's payoff position has no representable code; setup-time and SPC records that name a takeup cannot say **which** takeup | Confirm with the client what `TPO` denotes and whether the two takeups are two positions on one machine or per-line names (send-back item), then widen both CHECK constraints. **Do not guess `TPO`** | **Open** — raised 2 Sep 2026 |
| **G76** | **Edge type is `Round`/`Square` in the database and `Round Edge`/`Flat Edge` on the order screen. `Flat` is not `Square`.** `CK_PSC_EdgeType` and `CK_Edger_EdgeType` both allow `('Round','Square')`; *Flat Wire Machine - Web Changes* specifies the Order, Quote and IQR **Edge Type** dropdown as *"Round Edge and Flat Edge"*. One vocabulary is wrong. Compounding it, `CK_PSC_EdgeTypeReq` makes `EdgeType` **mandatory on an Active edger row**, yet edge type appears **nowhere on the client's Flattening Line Schedule screen** — so the value the constraint requires has no visible point of entry | Requirements / Database | **Medium** | An order captured as *Flat Edge* cannot be stored against a schedule component that only accepts `Square`; and a schedule cannot be saved without a value the authoring screen never collects | Settle the vocabulary with the client (send-back item), then align both CHECK constraints and the order screen to one pair. Establish where edge type is entered — order, schedule, or the unpictured schedule Edit dialog | **Open** — raised 2 Sep 2026 |
| **G77** | **Edger and straightener tooling inventory have no schema. The die split does not cover them.** The client's Tooling Inventory returns **four** tool types (`D-42`) and this gap owns two of them. **`Edger`** holds five columns (`Id · Name · EdgeType · ToolingSetNo · IsActive`) against a fourteen-column grid — missing `LineId`, `Location`, `P/N`, `Roll Qty`, `STD Removal From OD`, `OD`, `ID`, `Min OD`, `Date of Change`, `Date of Last Grind`; **`Gauge Range` is multi-valued** (`.045, .040, .035` — three grooves on one roll) and needs a child table, not a delimited string; and **`IsActive BIT` is too narrow for three statuses** (`Active`, `In Service`, `In Grinding` — the last is neither). **`Straightener` has no table at all.** ⚠ **The original wording — *"and appears nowhere in this repository"* — is STALE as of 2 Sep 2026** and is struck: the name now has a home in the **delay-code lookup** (`SET31`, `HDL20` and `DWN39`, all *Change Straightener Rolls*, seeded inline by `01_Lookup`, plus two rows in the pause-run mockup). What it still has no home for is **equipment** — which is this gap. ✅ The client's 3 Sep 2026 mail settles what one *is*: *"the straightener rolls are used to straighten the wire in both the x, y, & z axis"*, they are used on **FL1**, the blocks sit on the **entry side of the 12″ mill**, and they are **not** a pass-schedule component — *"they are always used on FL[1], however we did include them in our setup time due to the manual setup required of the operator."* That closes the *what it is* question and leaves the schema question open. ⚠ **The `.134` / `.184` straightener range discrepancy is NOT resolved** — the mail repeats the prose range without addressing the grid. ⚠ The two tool types also imply **different life models**: `Drawer` tracks **footage**, while the edger grid implies a **grind** model (`STD Removal From OD .100`, `OD 6.00`, `Min OD 4.75` — about twelve grinds). Only the footage model is built | Database | **Medium** | Two of the three Tooling Inventory tool types cannot be persisted; a `BIT` cannot express *In Grinding*; and a schedule can never select an edger groove | Decide the grain first — `Edger`'s seed (`EDGE-ROUND-A`) is an edge **configuration** while `ToolingSetNo` and the client's `Set Number` name a **physical set**, the same ambiguity the die split resolved for dies. Then add the columns, a gauge child table and a status column. ⚠ **Ownership is unsettled** — see the Die Management / Tooling Inventory `OI` | **Open** — raised 2 Sep 2026 |
| **G78** | **`wip_stations.PrinterName` is one printer per station and the client's topology needs two on one station.** [`10_CommonDB_Insert_WIPStations_FlatWire.sql`](../30-database/scripts/10_CommonDB_Insert_WIPStations_FlatWire.sql) seeds five stations against four **invented** names — `FL1$PRINT`, `FL2$PRINT`, `FL3$PRINT`, `FWPACK$PRINT`, `CHAR`-padded to exactly 12 (`D6`). The client's 1 Sep 2026 map is: FL1 **Payoff** = SATO · FL1 **Operator Station** = SATO **and** Zebra High-Temp · FL2 Operator Station = SATO (+ a provisioned Zebra, unused — no anneal at FL2). So the FL1 row needs **two printers in one column**, `FL1PO` must stop sharing `FL1$PRINT`, **FL3 has no printer of its own**, and `FWPACK` may be the second FL2 SATO the client is asking Bob about | Integration / Data | **High** | Labels print to the wrong device, or the high-temp label has no route at all. A `CHAR(12)` column cannot hold two printer names, so this is a schema question in a **shared** database, not a seed-value question | Confirm the topology and the device names with Bob S. and Shannon R., then decide whether a second printer is a second `wip_stations` row, a new column, or a flat-wire-side mapping table. ⚠ The high-temp unit is a **ZEBRA**, contradicting `C12`'s *"1 high-temperature + 3 standard ≈ 4 SATO"* and `Q44`'s *"the ones that are output from the mills"* | **Open** — raised 2 Sep 2026 |
| **G79** | **Every `WipRejection.RejectionGroup` value is ours.** The client's WIPREJ sheet is a **flat list of 96 rows with no grouping at all**, 72 of them in scope, while `RejectionGroup` is `NOT NULL` across five groups. All 72 assignments were made by us on 2 Sep 2026 and are recorded as `[PROPOSED]` via `WipRejectionReason.IsProposedGroup = 1` | Data / Requirements | **Medium** | The rejection dialog filters by a taxonomy the client has never seen, and the WIP REJ report (`OI-84`) will group by it. A later client regrouping moves rows the report has already been read against | Send the 72 rows with their proposed groups to Shannon R. to confirm or restrike. `CK_WipRejection_Group` was **dropped** and the composite `FK_WipRejection_Reason` now enforces the (reason, group) pair, so a regrouping is a single lookup update rather than a two-site edit | **Open** — raised 2 Sep 2026 |
| **G80** | **The client's eight IT inhibit reasons and `[PLC §8.2]`'s five set conditions share exactly one.** The overlap is *"no coil or rod is checked in"*. §8.2's other four — no active material-tracking identifier, feet data unavailable, feet data invalid, two or more consecutive recordings missing — appear **nowhere** in the client's list, though feet-data and periodicity conditions **do** appear in the client's own attached screenshots of two live machines. They are not loose prose: they are `FR-008` / `FR-009` with alternate flows `ALT002`–`ALT005` / `DAT009` and **five P1 test cases, `TC-011`–`TC-015`** | Integration / Requirements | **High** | Either four documented inhibit conditions are silently dropped — orphaning two FRs and five P1 test cases that `TC-###` numbering will never renumber — or the machine inhibits on conditions no screen explains | All twelve are seeded in `ItInhibitReason` with a `Source` column (`Client` / `PLC-8.2`), the four spec-only rows **`IsActive = 0`**: present and traceable, evaluated by nothing, deciding nothing. Ask the client directly whether the four still hold | **Open** — raised 2 Sep 2026 |
| **G81** | **`No Qualified Operators Are Logged In` presumes a mechanism that does not exist.** The client marks this IT inhibit reason as applying to flat wire. It requires the **Leadman / Operator / Helper** roles and the **qualification matrix** of the 23 Jul call's `C10`; `[SEC §8]` has six roles, **neither Leadman nor Helper**, and no matrix at all | Security / Requirements | **High** | The inhibit can never be evaluated, so it either never fires or is hard-coded to something else. Compounds `G6`, where the six role **claim values** are still unsupplied | Decide whether the qualification matrix is in MVP-1 at all. If it is, it is a new mechanism — not a new row — and needs a story; if not, this reason must be seeded inactive with the reason recorded | **Open** — raised 2 Sep 2026 |
| **G82** | **Threading scrap must be a WIP rejection and no reason code exists for it.** The client's answer 4 is explicit — *"We will definitely need to record the threading as a WIPREJ/Scrap"* — and **none of the 72 in-scope WIPREJ reasons covers threading**. The footage is meant to come from the Material Loss tab of the 31 Aug mail, whose *Threading Drawblock #1/#2*, *Threading FL1-Stand #1* and *Threading FL2-Stand #1/#2/#3* fields are **all empty**, pending *"the actual footage for these events through testing"* | Requirements / Data | **High** | A mandated transaction has neither a reason code to file it under nor a quantity to file. Threading happens on every setup, so this is routine scrap, not an edge case | Ask for a threading WIPREJ reason — **do not invent one**, since the vocabulary is the client's. The footage values are a trial deliverable and should be tracked as one | **Open** — raised 2 Sep 2026 |
| **G83** | **Four pause reasons lost their vocabulary and gained no successor.** `OperatorBreak`, `ShiftChangeover`, `AwaitingSupervisor` and `SafetyObservation` were in the SRS and the mockup until `D-34`; the client's 72 codes contain **no equivalent**. `SET11 Prior Shift unaccountable` is not shift changeover, and `DWN07 Fire Drill` is not a safety observation | Requirements | **Medium** | An operator taking a break, handing over a shift, waiting on a supervisor or logging a safety observation has no code — so the time lands on `Other` with free text, which is exactly what a delay-code taxonomy exists to prevent, and it will not aggregate in any throughput report | Send the four back to the client and ask for codes. ⚠ **Do not map them onto a nearby code** — a wrong bucket silently corrupts the standard-time arithmetic `D-41` builds on | **Open** — raised 2 Sep 2026 |
| **G84** | **`Return to Stock @ Payoff` is a label type with no transaction behind it.** The client's 1 Sep label list names four types, and this one **has no home anywhere in this repository** — zero matches outside one unrelated sentence. It implies returning unconsumed rod to stock from the FL1 payoff | Requirements | **Medium** | A label is specified for a transaction that is neither designed nor built, and it prints at the FL1 Payoff SATO, which `G78` also shows is unmodelled | Establish whether this is a distinct transaction or the existing rod checkout / partial-rod re-check-in (`Q12`) seen from the payoff. Ask what the label carries and what produces it | **Open** — raised 2 Sep 2026 |
| **G85** | **Equipment reference data sits in the sample-data file, where a production deploy will not run it.** `FlatWire_SampleData_Lookup.sql`'s `Dancer` block says in its own comment *"three rows, and they are **equipment, not sample data**"*. The same is true of the `Stand`, `Drawer` and `Edger` seeds. The three reason-code tables added on 2 Sep 2026 were seeded **inline in `01_Lookup`** for exactly this reason, following `PayoffPosition` — which makes the inconsistency visible rather than fixing it | Database / Deployment | **Medium** | A production deploy runs `RunAll` **without** the sample data, and comes up with no stands, no draw boxes, no edgers and no dancers. Every environment that loads the fixtures looks healthy, so the trial cannot surface it — the same failure shape as empty pass-schedule tables | Move the equipment seeds into `01_Lookup` behind table-level `IF NOT EXISTS` guards and leave only genuine fixtures in the sample-data file. Cheap now; it is a data-loss-shaped bug once anything references those ids | **Open** — raised 2 Sep 2026 |
| **G86** | **The committed plan-generation analysis assigns two operation letters that are already in use.** `Analysis/Planning System/tables.md` (committed 2 Sep 2026, `UADEV-22214`) states the flat wire operation vocabulary as `DRAW`=`D`, `FLATTEN`=`F`, `ANNEAL`=`A`, `EDGE`=`E`. A query over `dbo.c_process_id.process_letter` UNION `dbo.lookups.display_name` (`lookup_category_id = 930`), run 1 Sep 2026, returns only **four unused letters: `F`, `K`, `Q`, `U`**. `F` and `A` hold; **`D` and `E` are taken by other processes.** ⚠ The letters are not decorative — they build the `TempOrderRemainingOperations` route strings (`FA`, `FAF`) that `merging-logic.md` and `merging-logic-sample-data.json` are worked against end to end | Requirements / Database | **High** | Two of the four operation letters cannot be used, so the SMP route strings the plan-generation logic emits are unbuildable as specified. ⚠ **The build is in progress now** — the 24 Aug 2026 call cleared Sushant to code the SMP process, so this is not a latent defect | The two cells are marked **pending** rather than guessed (`K` has been proposed for `DRAW` and carries **no client authority**). Closing needs the client answers tracked as **`OI-27`** (the letter for `DRAW`) and **`OI-143`** (whether `EDGE` is an SMP process at all). ⚠ Do **not** rewrite the `FA` / `FAF` worked examples — they are Flatten/Anneal routes, carry no `D` or `E`, and are correct as written | **Open** — raised 3 Sep 2026 from the [3 Sep client e-mail](../95-archive/source-documents/ClientEmail_2026-09-03_ProcessLetters_SyncPlan.md) |
| **G87** | **The fourth Tooling Inventory tool type is built from one sentence, and every column in it is ours.** Die, Edger and Straightener each reached us as a **screenshot grid with an ordered column list** — that is what *"please include the fields pictured below, in the order pictured"* delivered on 31 Aug 2026. **Roll sets reached us as a single sentence** (3 Sep 2026, `D-42`): *"mill rolls for traceability, 12″ (FL1-S1) 2 roll set, DB1/DB2 Capstans (rolls) current inventory = 2, will be adding a spare and they can be refurbished, 8″, 6″, 6″ rolls for (FL2-S1, FL2-S2, FL2-S3) 2 roll sets."* `ToolingInventoryRollSet` is built against **our reading** of it, cross-referenced to the shape of the other three grids. **Four things are open:** (1) the **column list** itself; (2) whether capstan rolls are the **same tab option** as mill rolls or a fifth — the client named them in one breath but they mount on a `Drawer`, not a `Stand`; (3) whether *"refurbished"* is the edger's **`In Grinding`** under another name, or a distinct operation with its own state; (4) what **`Machine Name`** a capstan roll carries, given `DB1`/`DB2` sit on FL1. ⚠ A fifth ambiguity is in the sentence itself: **"2 roll set(s)" reads both as *two rolls per set* and *two sets per position***, and it appears both ways in one paragraph — the first reading is seeded (`RollQty = 2`, corroborated by the edger grid's `Roll Qty 2`), the second would change the **row count** | Requirements / Database | **Medium** | A built register whose fields the client has never seen. The failure mode is not a crash — it is a Tooling Inventory tab that **looks finished** and turns out to collect the wrong things, after the roll shop has started entering data against it. ⚠ **Distinct from `G77`**, which is that edgers and straighteners have **no** schema; here the schema exists and its **authority** is the problem | Send **`Q92`** — the four questions above, asking for the roll-set grid in the same form as the other three. Until it returns, treat every column as **`[PROPOSED]`**: the DDL comment, the schema document and `[MSP §5]` all say so, and the seed is deliberately **one set per position** rather than a guess at the real inventory. ⚠ **Do not multiply the seed rows** on the two-sets-per-position reading, and **do not add die-life footage columns** by analogy with `ToolingInventoryDie` — the roll life model is **grind to a minimum OD**, which is the distinction `G77` already drew | **Open** — raised 3 Sep 2026 from the [3 Sep tooling e-mail](../95-archive/source-documents/ClientEmail_2026-09-03_Tooling_SyncPlan.md) |
| **G89** | **A customer weld-count specification has no field anywhere — and the client raised this against his own applications.** Tim O'Brien, 3 Sep 2026, asked for the maximum welds allowed in one coil: *"**ASTM standards do not set a limit to the number of welds, or welding across LOT#s/Heats, only across differing alloys, tempers, & drastically different chemistries.** A Customer could have a #ofWelds specification, and **we did not capture this in any fields in the quotes & orders applications.** This may fall under customer note/spec, but the would require the operator to track. We may need to discuss this further as it might need to be captured in a systematic way to prevent rejections due to multiple weld joints."* So the **standards** half of `OI-59` is answered — the constraint is alloy / temper / chemistry, not a count — and a **per-order configurable limit defaulting to unlimited** (that item's existing recommendation) is the right shape. What has no home is the **value** | Requirements / Integration | **Medium** | A customer with a weld-joint limit cannot have it enforced, and the client names the consequence himself: *"rejections due to multiple weld joints."* Operator-tracked in a free-text customer note is not enforceable | Confirm with UA whether the field belongs on the order or the quote, in **their** applications, and whether flat wire should read it or own it. ⚠ **This is a shared-schema question:** `D-32` bars altering the existing schema, so a new order-level column is not ours to add — which is exactly why it needs the discussion Tim asks for | **Open** — raised 3 Sep 2026 (audit record [`ClientEmail_2026-09-03_RodOrderAllocation_SyncPlan.md`](../95-archive/source-documents/ClientEmail_2026-09-03_RodOrderAllocation_SyncPlan.md)) |
| **G88** | **The coil-overage WIPREJ peel-back has no reason code and no requirement.** Tim O'Brien, 3 Sep 2026, choosing between shipping a coil over the customer maximum, mis-attributing the excess to the next order, or keeping the remainder: *"**Cut at 900 lb. and keep the remainder, a WIPREJ would be forced for the overage**, essentially a **peel-back** of the coil to put with in the allowable range."* That confirms the proposed option **and introduces a mechanism nothing specifies.** Measured 3 Sep 2026: **zero** matches for *peel*, *overage* or *overweight* in any reason-code seed, and none of the 72 web-rejection reasons the client supplied on 1 Sep 2026 covers it. Over-target handling exists **only for spools at FL1** — `SpoolCompletionNotification.md`'s M3/M4 ladder — and `OI-74` still lists *"whether the ladder applies to finished coils at TKUP-2"* as open | Requirements / Database | **Medium** | The client has specified the behaviour at a coil boundary and there is no reason code to record it, no requirement to build it and no screen path. A forced WIPREJ that cannot state its reason is not auditable | Add a rejection reason for the coil-overage peel-back and a requirement for the forced WIPREJ at a coil cut; settle it together with `OI-74`'s TKUP-2 half, since both are the same question at the same station. ⚠ **Do not infer the threshold** — Tim's *"keep the remainder"* was our proposal *"below a threshold you set"*, and he did not set one | **Open** — raised 3 Sep 2026 (audit record [`ClientEmail_2026-09-03_RodOrderAllocation_SyncPlan.md`](../95-archive/source-documents/ClientEmail_2026-09-03_RodOrderAllocation_SyncPlan.md)) |
| **G90** | **The client's round→flat width correlation is undefined over part of its input range, and the nominal product sits three points above the floor.** `D-43`'s `F16` — `ωĆ = C₅[(0.7854 D₁²/T₂)(1 − 15.8(1 − 2T₂/D₁)^2.25 (2R/D₁)^−0.82) + 0.1426 D₁(2T₂/D₁)]` — is adopted by `[PSG]` v1.6 as the relation `[PSG §6.3 Step 2]` solves **backwards** for the FM1 entry diameter, and that `Step 9B` iterates on. Two limits, neither stated in the client's document: **(a)** for `T₂ ≥ D₁/2` the term `(1 − 2T₂/D₁)^2.25` raises a **negative base to a fractional power** and the result is complex; **(b)** below ≈ **57–60%** thickness reduction the predicted width implies a cross-section *larger* than the incoming area — negative elongation, which is impossible. Checked across `D₁ ∈ {0.1875, 0.250, 0.3125, 0.375}` × `R ∈ {6.0, 4.0, 3.0}`; at `D₁ = 0.250″, R = 6.0` the crossover is `T₂ = 0.1018″` (**59.3%**). ⚠ **`[PSG §6.4]`'s nominal product runs at 62.5%** — inside, but barely — and ⛔ **on the square-edge area basis the same crossover is 65.1%, which puts it OUTSIDE** (implied elongation **0.982**, against **1.021** on the rounded-edge basis). Which basis applies to footage is `Q93` item 4 | Requirements / Backend | **High** | A backward solve that hands a complex number or a silently unphysical width to `Step 9B`'s iteration, on the engine's **primary** calculation. The `[PSG §12.4]` position that *"the complete calculation framework can proceed now"* holds only if the guard is specified with the relation — and the same limit **falsifies `FW-013`'s acceptance criterion**, whose 0.375″ rod case solves to a `0.3823″` entry with a **−3.93%** rod-adequacy margin | Specify both as validations in `[PSG §8]` — `V46` domain (`T₂ < D₁/2`, **Error**), `V47` implied elongation `≥ 1.0` (**Error**), `V48` predicted width cross-checked against volume conservation on the **rounded-edge** area (**Warning**). ⚠ **Do not clamp or extrapolate** — outside its band the correlation is not merely inaccurate, it is unphysical, and a clamped value would be indistinguishable from a good one. Report *not evaluated*, per `[PSG §8]`'s third outcome | **Open** — raised 3 Sep 2026 (audit record [`ClientEmail_2026-09-02_PassCalculatorFormulas_SyncPlan.md`](../95-archive/source-documents/ClientEmail_2026-09-02_PassCalculatorFormulas_SyncPlan.md)) |
| **G91** | **The three empirical coefficients are LOOKUP TABLES, and `FlatWireDB` has nowhere to put them.** Tim O'Brien, 2 Sep 2026: the placeholders stand *"until, which time we are able to run samples and **generate our own table for the factor to be pulled from**."* So `C₅` (round→flat spread), `C₆` (flat→flat spread) and `k` (edger partition) are not three scalars in a config file — they are three keyed tables that Process Engineering will populate from trial data and revise thereafter. `[PSG §5]` `M8` names a **Process Parameter Master** for exactly this, and **no such object exists**: `01_Lookup` has `AlloyProperty`, which is keyed on alloy alone and carries `MaxReductionPerPass` / `SpringbackFactor` / `LbPerFtFactor` / `DensityLbPerIn3`. A spread factor plausibly varies by alloy **and** stand **and** gauge band — ⛔ **and what it is actually keyed by is unknown** (`Q93` item 5) | Database | **Medium** | `[PSG §12.4]`'s promise that supplying coefficients later is *"a data change, not a code change"* is only true if a table exists to change. Without one the first calibration lands as a code edit or a spreadsheet outside the system, and `PSG-D35` (sanity bounds and revalidation interval) has nothing to attach to — a stale coefficient would then be invisible | **Do not build it yet.** The key is the design, and `Q93` item 5 asks for it; guessing produces a table that has to be dropped. Once the key is known, add the master to `01_Lookup` alongside `AlloyProperty`, with the coefficient, its bounds and its calibration date per `PSG-D35`. ⚠ Moves the object baseline — count from a deploy | **Open** — raised 3 Sep 2026 (audit record [`ClientEmail_2026-09-02_PassCalculatorFormulas_SyncPlan.md`](../95-archive/source-documents/ClientEmail_2026-09-02_PassCalculatorFormulas_SyncPlan.md)) |
| ~~**G22**~~ | ~~**The staging order-membership rule is knowingly wrong until Q73 closes.**~~ **✅ Resolved 6 Aug 2026 — `Q73` answered.** The consumption sequence is a three-tier rule: **full coils → partials → multi-order coils last** when welding is involved, with operators working a **pick list in planned order** and no jumping a multi-order coil to the head of the line; the validation applies at **pre-check-in and check-in**, and it is **MVP1**. The replacement for the wrong rule is now writable: `planning_routings` returns **orders (plural)**, *"belongs to the established order"* becomes membership in an **ordered set**, `RodStaging.OrderId` means **the order this staging is being consumed for**, and the ordering constraint above is enforced on top. **One residual, and it is a branch rather than the rule: `Q73`** — whether multi-order-last also holds when **no welding** is involved. Both readings are on the 6 Aug recording. Build the welding branch now; leave the no-weld branch behind the answer. *(Original text retained below for the audit trail.)* **The staging order-membership rule was knowingly wrong until Q73 closed.** The client confirmed (Jul 30 2026, **Q70**) that a single rod may carry **more than one production order** — finishing order 1 on a 7,000 lb A-rod and starting order 2 on the remainder. Every artifact validates the opposite: staging and check-in refuse any rod whose order differs from the established one, and [RodPreCheckin.md](../10-requirements/screens/RodPreCheckin.md) records *"continuous feed cannot cross an order boundary"* as a consequence to confirm. Both are wrong for the **same-rod successor** | Data / Contract | **High** | The line stops mid-bundle at an order boundary that the material does not have; or the rule is relaxed without a sequencing rule and rods are consumed against the wrong order | ~~Do **not** edit the rule yet — the correct replacement depends on the sequencing answer (**Q73**, Srikanth) and on whether the case is **MVP2**.~~ **`Q73` landed 6 Aug 2026; the rule is now editable** — see the resolution at the head of this row. `planning_routings` returns **orders (plural)**, membership becomes an **ordered set**, and `RodStaging.OrderId` means *the order this staging is being consumed for* | ✅ **Resolved 6 Aug 2026** — raised Aug 1 2026 from the [30 Jul client call](../95-archive/source-documents/ClientCall_2026-07-30_SyncPlan.md), closed by the [6 Aug client call](../95-archive/source-documents/ClientCall_2026-08-06_SyncPlan.md). Residual branch tracked as **Q73**, not here |
| **G23** | **The 1280×1024 shopfloor canvas is an acceptance criterion nobody has confirmed.** All 25+ mockups are authored at 1280×1024, `flat-wire-fit.js` measures and calibrates its 14 px text floor against that box, and [phase-01a](../60-delivery/phases/phase-01a-angular-foundation.md) pins *"fixed 1280×1024 shopfloor canvas"* as acceptance. Tim expects the stocked 1280×1024 panels but will verify with Charles and Juan; we owe him the required **1920×1080** by e-mail (**Q26**) | UI / Design | **High** | 1920×1080 is **1.5× width, 1.05× height** — a re-layout of every screen, not a rescale. An answer arriving after Phase 1 closes lands against a **14 Aug gate** | Send the requirement now; do **not** re-author anything until answered. `data-fit="fill"` already widens to the window, so a wider panel is the cheap direction and the height barely moves | Open — raised Aug 1 2026 |
| **G24** | **Supervisor approvals are decided but unpersisted.** Three decisions require supervisor authorisation — **Q74** mid-run checkout, **Q75** partial-run disposition, and now **Q69** welded pre-check-out — and `RodCheckout` has **no `ApprovedBy`, `ApprovedAt` or `OverrideReason` columns at all**. `RodStaging` has the credential trio; the checkout table does not | Data / Schema | **High** | Every supervisor-gated checkout is unauditable: the system enforces a gate at the UI and stores no evidence that it was passed, by whom, or why | Add `ApprovedBy` / `ApprovedAt` / `OverrideReason` to `RodCheckout` with a constraint tying them (plus `NewRodStatus='HOLD'`) to the welded Mode P case and to Mode B. Settle the **PIN validation source** once for all overrides (**OI-38**) | Open — raised Aug 1 2026 while applying Q69 |
| **G25** | **Requirement coverage was asserted from a range table, and 41 requirements had no test case.** `06-TestPlanAndTestCases.md` §10.1 mapped SRS **section ranges** to TC ranges (`FR-100 – FR-120 → TC-130 – TC-147`) and the coverage percentage was concluded from that mapping. A range mapping cannot show that an *individual* requirement was tested. Measured per requirement on 13 Aug 2026: **41 MVP-1 requirements had no case naming them, 32 of them `Must`** — against a published figure of **96.7 %**. The denominator was wrong too: **363** was the pre-split MVP-1 + MVP-2 total, where `02-SRS.md` now carries **263**. §10.2 (*every case names its requirement*) was true and machine-verified, and is the **opposite** direction — a suite can name a requirement on every case and still leave requirements untested. **`G25` is a reused ID**: it was allocated to a weld-history gap on 1 Aug 2026 and withdrawn the same day before use, with `REVIEW.md` *(appendix)* recording *"the next free gap ID remains G25"* | Traceability / QA | **High** | UAT sign-off (**QA5**, `FW-123`) and the Phase-1 gate suites rest on this matrix. A coverage claim derived from the section headings rather than the cases cannot fail, so it would have been quoted intact to the client at sign-off | Delivered 13 Aug 2026: [`build_coverage_matrix.py`](../tools/deliverables/build_coverage_matrix.py) measures coverage per requirement and **exits non-zero** on any requirement with neither a case nor a §10.4 entry; §10.1 is demoted to a navigation aid; the 41 are itemised in new §10.4.2 with what closes each; §10.5 is generated, not asserted. **The 41 cases are still owed** — this gap tracks writing them. Two need only a citation added to an existing case (`FR-300` on `TC-377`, `FR-226` on `TC-284`); one is blocked on `G27`; one is not test-case shaped (`FR-161`, architectural) | Open — the guard is in place and the number is honest, but **31 of the 41 still need a case authored**. Raised and part-resolved 13 Aug 2026 |
| **G26** | **The merged weld write straddles two phases.** Dashboard 2A's weld control ships in **phase 4**, and the endpoint it calls — `POST /weldevent` — is a **phase 6** deliverable. `POST /staging/rod/mark-welded` was retired on 1 Aug 2026 when the two writes were merged, so there is no phase-4-local endpoint left for the button to call. [`phase-06`](../60-delivery/phases/phase-06-in-run-production-events.md) already states the consequence: *"this phase owns **both** weld entry points — a phase-4 screen depending on a phase-6 endpoint (**G26**); phase 4 ships it against a stub."* | Sequencing | Medium | Phase 4 cannot demonstrate a working weld at its own gate — the button is present and its target does not exist yet. Manageable while it is known; **it is a silent hole if the stub is mistaken for the real write** at the phase-4 acceptance review | Keep the merged write (splitting it back is worse — two endpoints composing one `WeldEvent` row is what the merge removed). **Phase 4 ships against a stub and its acceptance says so explicitly**, and phase 6 de-stubs. Verify at the phase-6 exit, not the phase-4 exit | Open — **raised 1 Aug 2026 and never entered in this register until 13 Aug 2026.** `REVIEW.md` *(appendix)* §5.3 step **S7** carried the instruction *"Add G25 and G26"*; only the G25 withdrawal was executed, so `phase-06:45` cited a gap ID that resolved to nothing for twelve days |
| **G27** | **The weld screen's rod queue and traceability chain have no host.** Dashboard 4 was **retired 1 Aug 2026** (mockup deleted; git history at `2a0426b`) and the weld moved to Dashboard 2A's *Mark as welded* dialog, which captures every field of the `WeldEvent` row. Two things did **not** move: the **re-sequenceable *Rods In Queue*** accordion (drag + undo) and the **traceability chain** strip — completed → outgoing (remaining footage, WELD NOW chip) → incoming (staged) → future rod. The latter is `FR-175`/`FR-484`-adjacent requirement text with no screen behind it.<br><br>**Half-closed 1 Aug 2026 — read the distinction before quoting this gap.** DB2A's traveler queue is now **headed "Rods In Queue"**, which is the same *name* as the retired accordion but **not the same control**: it is a read-only status table with no drag and no undo. The name collision is a trap — the re-sequencing capability is still homeless, and so is the traceability chain. `FR-175` is unchanged. | UI / Requirements | Medium | `FR-175` is requirement text with no screen behind it, so it can be neither built nor tested — it is one of the 41 requirements with no test case (`06-TestPlanAndTestCases.md` §10.4.2, gap **`G25`**), and the only one whose reason is a missing host rather than an unwritten case. The re-sequencing capability disappears silently: nothing errors, the queue simply cannot be reordered | Pick one of three and the test case follows the decision: **rehome** both controls on DB2A · **fold** them into the *Welds this run* dialog · **withdraw `FR-175`**. Do not resolve by renaming — DB2A's read-only table already carries the retired accordion's name, which is what makes this look closed when it is not. **Affects phase 6 / FW-063** | Open — raised 1 Aug 2026 with the Dashboard 4 retirement; half-closed the same day. **Row completed 13 Aug 2026** — it had been truncated at the Priority column since it was written, so it carried no impact, resolution or status |
| **G28** | ✅ **RESOLVED 3 Sep 2026 — FL2 NEVER WELDS, AND FLATTENED MATERIAL CANNOT BE WELDED AT ALL.** Tim O'Brien, on our statement that leftover FL2 material is welded back at FL1: *"the statement is **invalid**. Once the material has been Flattened, it is no longer able to be welded due to a lack of capability at UA."* And on our assumption that welding is induction on the FL1 side only, FL2 cutting and re-threading rather than joining spools: *"**Correct**."* **So the second artifact was the right one:** [WeldEvent.md](../10-requirements/screens/WeldEvent.md) §6's inheritance rule stands and is now normative, and the `dashboard_10_shift_summary.html` fixture was wrong. ⚠ **This gap's own instruction was *"do not resolve by deleting the fixture — that removes the evidence of the disagreement without answering it."* That held while the question was open; it is now answered, so correcting the fixture **is** the resolution.** The FL2 weld tile and event row are removed from all three variants that carried them (the static default, `fl2`, and `"all lines"`); `fl2` gets an empty state on the `fl3` pattern; the FL1 rows are untouched. ⚠ **The fixture was wrong twice over** — it described the FL2 weld as *"Laser, flat–flat"*, a method that appears nowhere else in the repository and contradicts our own induction-only assumption. *Original finding, retained as the audit trail:* **FL2 may have no way to record a weld.** The *Log Weld Event* action was removed from **all four** active-run monitors on 1 Aug 2026 and the weld now lives only at the pre-check-in station — **which FL2 does not have** (`PCI002` excludes FL2 from staging). Two artifacts disagree on whether this matters: `dashboard_10_shift_summary.html` renders **FL2 weld events** (`SP-00029 → SP-00030`, induction, with an "FL2 welds" tile), while [WeldEvent.md](../10-requirements/screens/WeldEvent.md) §6 says a coil completing at FL2 **inherits the spool's** weld markers — i.e. the welds happened upstream on FL1. If the first is right, FL2 has a functional hole; if the second is right, the shift-summary fixture is wrong and should be corrected. **This closed `WeldEventPopupPlan` Q-W4 by decision rather than by answer** | Requirements / UI | **High** | **Exactly one of the two artifacts is wrong and nobody has said which.** If FL2 does weld spool-to-spool, it has no capture path at all and the genealogy chain breaks at the FL2 boundary — which is the chain the **welding-wire customer certificates** are produced from (`CoilTraceability`, `NFR012`). If it does not, a client-facing mockup is showing weld events that can never occur, and the shift-summary fixture teaches operators to expect them | **Ask whether FL2 ever welds spool-to-spool** — one question, and it is the client's to answer, not ours. Yes → FL2 needs a weld entry point, and the natural host is the DB3 FL2 action bar the 1 Aug pass stripped it from. No → correct the `dashboard_10_shift_summary.html` fixture and state the inheritance rule in `WeldEvent.md` §6 as normative rather than descriptive. **Do not resolve by deleting the fixture** — that removes the evidence of the disagreement without answering it | ✅ **Resolved 3 Sep 2026** by client e-mail (audit record [`ClientEmail_2026-09-03_RodOrderAllocation_SyncPlan.md`](../95-archive/source-documents/ClientEmail_2026-09-03_RodOrderAllocation_SyncPlan.md)) — raised 1 Aug 2026 from the Dashboard 4 retirement (`Q-W4`). **Blocks nothing until the FL2 build (phase 8)**, which is where it becomes urgent. **Row completed 13 Aug 2026** — it had been truncated at the Priority column since it was written, so a `High` gap carried no impact, resolution or status |
| **G92** | **The Machine Setup History tab is served by `united_db.dbo.AuditTrail`, and the five new `FlatWireDB` tables cannot feed it.** The Machines Application History tab is `MachineHistory.aspx` over `AuditTrail_GetEntityInfo(@moduleId, @entityId)`, filtered `ModuleId = eModule.Machine AND primary_identifer = machine_idx`, and discriminated by `EntityName` — `'Slitter Set Handling Time'`, `'Mills Material Loss'`. It reads **whole-record XML snapshots** in `OldValue`/`NewValue`, not the tables themselves. Setup/Handling Times and Material Loss now live in **`FlatWireDB`** (`FW-262`), which that tab cannot see, and no `EntityName` exists for either Flat Wire tab. Note also that the legacy pattern buries the **crew size** in a free-text `Comments` string, so it is not queryable even for the Slitter. | Backend / Legacy | **Medium** | The History tab silently shows **nothing** for both Flat Wire tabs — no error, just an empty list, which reads as "no changes yet" rather than "not wired". `RowVersion` on the two `*Standard` tables gives concurrency safety but **no history at all**: there is no effective-dating and no `_hist` table anywhere in the legacy design either. | Decide which of three: the legacy app keeps writing `united_db.dbo.AuditTrail` with two new `EntityName` values (cheapest, and the only option that makes the existing tab work unchanged); or Flat Wire gets its own history table in `FlatWireDB` and the tab is repointed; or the tab is accepted as empty for these two and said so on screen. ⚠ **Tied to `OI-110`** — if that resolves to `united_db`, this gap disappears with it. | Open |
| ~~**G93**~~ | ✅ **WITHDRAWN 5 Sep 2026 — `D-45` removed the requirement, so there is no shared `ALTER` to make and no shared-schema conversation to schedule.** Flat wire registers in `OPCTags` in the same shape as every other module: `TagName` only, no `TagKey`, no confirmation column. The logical-name layer stays where `FW-144` already put it — `TagNames.cs` plus the `appsettings` map behind the unchanged `ITagPathResolver` seam — which is what the ecosystem does anyway (`UnifiedSlitterService` addresses 313 tags with no logical-name column at all). ⚠ **The text below is kept, not deleted**, because it is the record of what the column would have cost and of why the seam exists. ⚠ **If a logical key in the database is ever wanted, `OpcSubModuleTags` is the ecosystem's precedent — a side table, not an `ALTER`** — which needs no shared column and no other team. *(original text follows)* **The `CommonDB` `OPCTags` registration has no logical-name column, so `D-44` cannot be implemented without altering a SHARED table.** `D-44` (4 Sep 2026) closed `OI-A` by putting the 72 flat wire tag paths in the `CommonDB` registration rather than in `appsettings`. The table cannot key them: `OPCTags` is `OPCTagsIdx · TagName · RequestedUpdateRate · IsSystemErrorTag · IsActive`, and `GetOPCServerAndTagDetails` returns a flat `(MachineId, TagName, RequestedUpdateRate)` list keyed only by machine and module — there is **nowhere to put `Gauge`, `ITInhibit` or `Dancer1Position`**. The ecosystem never needed one because no consumer has more than a handful of tags: `CoolingChamber`'s `OpcHelper` takes `opcInfo.Tags[0]` **positionally**, and when the list comes back empty it builds the path from string constants in code — the exact practice `FR-022` forbids. Three things are therefore owed by a team that is not this one: **(1)** a nullable **`TagKey varchar(64)`** on `dbo.OPCTags`; **(2)** that column returned on the read path — either added to `GetOPCServerAndTagDetails`'s third result set (which changes a procedure five modules call) or a new flat-wire-scoped procedure beside it; **(3)** a home for **`P-74`'s confirmation flag**, which `D-44` moves from a per-line config list to a column question. ⚠ **Deriving the key from the path is NOT an option** — `[PLCC §2]`'s first binding rule decouples them precisely so a `PLC-Q05` measure rename stays a values-only edit, and FL2's two dancers need indexed keys the path does not carry | Database / Integration | **High** | **`D-44` is undeliverable until this lands, and `FW-238` is the story that stops.** The fallback is not neutral: leaving the paths in `appsettings` re-opens `OI-A` as *two* homes for one string, which is the **"tag rows get written twice"** failure `G60` names. ⚠ **It compounds `G60` rather than duplicating it** — `G60` is that `OPCConnection` does not know the flat wire lines exist (no `OPCModules` member, no registration script); this is that even once it does, the tag rows cannot be addressed by meaning. Both land in the same **40 h Phase-14 commissioning window** `[PLCC §4]` already calls the worst compression in the schedule, and `C1` cannot start until both are closed | Raise the column and the read-path change with the shared-schema owning team **before Phase 14 opens**, alongside `G60`'s `GlobalConstants.OPCModules` member — they are one conversation with one team and should not be taken twice. ⚠ **Additive and nullable only**: every existing consumer must be unaffected, which is what makes this a safe alteration and is why it does **not** reopen `D-32` (that binds the `united_db` `coils` / scheduling schema, and `CommonDB` registration writes were always in scope — `D-32` (a)). ⚠ **Ship the column before the rows**: `FW-238`'s registration script writes `TagKey` values, so it fails on an unaltered table. ⚠ **Do not work around it by parsing `TagName`** — see the gap text | ✅ **WITHDRAWN 5 Sep 2026 by `D-45`** — the `TagKey` column is not built, so there is no shared `ALTER`, no procedure change and no other team on the Phase-14 critical path. `FW-238` proceeds without it and dropped `G93` from its `blocked_by` the same day. ⚠ **The Impact and Recommended-resolution cells are left as written**: they are the reasoning `D-45` overturned, kept as audit trail rather than as current guidance. ⚠ **`G60` still stands on its own** — this row's *"sequence with `G60`"* applied to the column conversation only. Raised 4 Sep 2026 with `D-44`, from the resolution of `OI-A` |
| **G95** | ⛔ **`GetOPCInfo` returns an EMPTY LIST for a line that has no system-error tag row, so registering every data tag is not enough to make a line reachable.** `GetOPCServerAndTagDetails`'s **first** result set — the `OPCInfo` header carrying `MachineId`, `ConnectionType`, `IsReadonly` and the servers — is an INNER JOIN through `OPCTagApplicationMapping` to `OPCTags` filtered `IsActive = 1 AND IsSystemErrorTag = 1` (`GetOPCServerAndTagDetails.sql:41-42`). No such row, no header — and `ContextRepository` builds the servers and tags off that header, so **the whole line disappears**. ⚠ **The symptom is indistinguishable from `G60` itself** — an empty list either way — which is how it would have survived a full 39-row registration and surfaced only at `C1`. ⚠ The same result set joins `CommonDB.Machines`, which filters `STATUS = 1`: a `machines` row in any other status vanishes the same silent way | Integration / Database | **High** | **Found before the seed rather than inside the 40 h Phase-14 window**, which is the whole value of it. Unaddressed it would have cost a commissioning session to diagnose, because nothing in the failure distinguishes *"not registered"* from *"registered without a system-error tag"* | ✅ **Resolved in the seed.** [`11_CommonDB_Insert_OPCRegistration_FlatWire.sql`](../30-database/scripts/11_CommonDB_Insert_OPCRegistration_FlatWire.sql) writes **two** `IsSystemErrorTag = 1` rows — `FL1.PLC._System._Error` (machine 125) and `FL2.PLC._System._Error` (126), both supplied by the client 5 Sep 2026 and now recorded in `[PLC §5.2.1]` / `[PLC §5.2.2]`. The script's verification block asserts a system-error row per line, and a pre-flight guard asserts both `machines` rows are `status = 1` | ✅ **Resolved — raised and closed 5 Sep 2026**, from reading `GetOPCServerAndTagDetails` while planning `FW-238`. The row stays as the reason those two tags exist |
| **G96** | ⛔ **`UAL.Constants.GlobalConstants.OPCModules` is not the register of OPC module ids, and it is already stale — `5` looks free and is in production use.** The enum stops at `FurnaceScheduling = 4`. But `UnifiedSlitterService` runs **all six slitters on `OpcModuleId: 5`** (`appsettings.{D72,N36,R36,R48,U30,U36}.json:7` in `ual-window-service`) and does **not** consume the `UAL.Constants` package, so it never registered its module there. Separately `G94` measured only **four** `OPCModules` rows on `DEV00164-001`. **The enum, the slitter config and the live table give three different pictures**, and only the live table is authoritative | Integration / Deployment | **High** | Taking `5` from the enum as *"the next free value"* would collide with production slitter data. And `OPCModulesIdx` is an `IDENTITY`, so an unforced insert lands wherever the seed happens to be — a compiled enum value and a runtime identity silently diverging is the worst shape this could take, because nothing would report it | ✅ **Flat wire takes `OPCModuleId = 6`**, clear of all three pictures, forced with `SET IDENTITY_INSERT` and a pre-flight `RAISERROR` if `6` is occupied. `FlatWireOpc:OpcModuleId` is set to `6`. ⚠ **Re-check the free id per environment before seeding** — `test1`, staging and production can differ and the enum will not warn you. If `UAL.Constants` is ever updated, add **`Slitter = 5`** alongside `FlatWire = 6` so it stops lying | ✅ **Resolved — raised and closed 5 Sep 2026** while planning `FW-238`. ⚠ The enum *being stale* is worked around, not fixed; adding the two members is optional and owned by `UAL.Constants` |
| ~~**G97**~~ | ✅ **RESOLVED — `D-49`, 5 Sep 2026.** *(Original: no OPC UA server endpoint was recorded for the flat wire lines, so the registration script could not be run.)* **The lines connect through the endpoint pair at `OPCServersIdx` 1 and 2**, and **both lines map to both**, `ConnectionSequence` 1 and 2. ⚠ **Two traps in the answer, both measured on `DEV00164-001` the same day.** **(a) The ids do not match the names** — `OPCServersIdx` 1 and 2 are **`UAOPC3`/`UAOPC4`**; `UAOPC1`/`UAOPC2` are ids **3 and 4**, a different pair serving three other modules. **(b) It is a pair, not a split** — `11_` `D9` had been asking whether FL1 sat behind one endpoint and FL2 behind another, and that was the wrong shape of question | Integration | **High** | **It gated *running* `11_`, never writing it, and that gate is now lifted.** ✅ The script carries real values and no placeholder. ⚠ **What the answer cost was two corrections rather than a fill-in-the-blank**: §4c goes from **2** rows to **4**, and the endpoint is addressed **by id** rather than by name — had the name path survived, *"1 and 2"* would have been read as `UAOPC1`/`UAOPC2` and pointed the lines at the wrong pair, with no error at any layer. ⛔ **A one-row-per-line mapping would have looked like a working registration until the primary endpoint dropped** — all **37** existing `(module, machine)` pairs carry exactly two, and `GetOPCServerAndTagDetails` returns them `ORDER BY MachineId, ConnectionSequence` | ✅ **Done in `11_`** — two guards (both ids must exist; they must differ), §4b prints the endpoint **names** so a reader sees which rows 1 and 2 are on that instance, §4c `CROSS JOIN`s the two lines with the two endpoints, and two fault queries catch a line without a full pair and a duplicated `ConnectionSequence`. ⛔ **Re-check `OPCServersIdx` per environment, exactly as for the module id (`G96`)** — the numbering is an artefact of insertion order, not a convention, so the right numbers elsewhere may be the wrong pair. ⚠ **One assumption left that measurement cannot settle**: `11_` makes id **1** the primary, taking *"1 and 2"* in the order given — but **Hole Detection, the only other module on this pair, uses id 2 as its primary**. Flipping two `DECLARE`s in §1 is the whole change. ⚠ **The namespace index is NOT closed by this** — `DefaultNamespaceIndex = 2` is still a compile-time assumption (`11_` `D8`), and it only ever travelled with this row | ✅ **RESOLVED 5 Sep 2026 — `D-49`.** Raised 5 Sep 2026 while planning `FW-238`; narrowed the same day by measurement, again by `D-48`, and answered by client direction. ⚠ **`11_` is still gated — by `G100` and `G94`, not by this row** |
| **G98** | ✅ **Every flat wire tag path was missing a `PLC` element, and the first real paths off a controller proved it.** `[PLC]`'s derived grammar produced `FL1.AGC.Gauge`, `FL1.Speed`, `FL2.FM2.S1.RollGap` — line, element, measure. The two system-error paths the client supplied on 5 Sep 2026 are **`FL1.PLC._System._Error`** and **`FL2.PLC._System._Error`**, carrying a `PLC` segment after the line that nothing in the derivation had. The ecosystem is unanimous the same way: all **313** of `UnifiedSlitterService`'s paths are `{Machine}.PLC.{…}` (`D72.PLC.LineSpeed`, `R48iQ.PLC.Running`), and `CoolingChamber` addresses `Cooling.PLC.CR.C{n}.NumberOfLoadsPresent` | Architecture / Integration | **High** | Had this surfaced at `C1` instead, **all 72 paths would have failed identically** — which reads as a connectivity fault rather than an addressing one, and would have burned commissioning time on the network. It is also the first evidence *against* a derived path that `PLC-Q02` has produced | ✅ **Applied 5 Sep 2026, and it cost only a values edit** — precisely the property `[PLCC §2]`'s key/value decoupling and `[PLC §4]`'s convention-first structure were built to buy. **No logical name moved and no code changed.** Three sites: the 39 FL1/FL2 values in `FlatWire.API/appsettings.json`; `[PLC §4.1]`'s grammar plus R2/R3 and every path in `§5.2` (now v1.3); and the seed rows in `11_`. ⚠ **FL3's 33 config values were deliberately left alone** — rewriting them to `FL3.PLC.*` would encode a namespace `D-47` says does not exist; they move when `G99` re-points them | ✅ **Resolved — raised and closed 5 Sep 2026** (`D-46`). ⚠ The rest of `[PLC §5.2]` is still derivation and not observation — `G33` and `PLC-Q02` are untouched by this |
| **G99** | ⛔ **FL3 has no controller of its own, so an FL3 push must write to TWO controllers — and nothing is built or specified for that.** `D-47` (5 Sep 2026) answers `PLC-Q08` in favour of option 1: FM1 and the die blocks belong to the FL1 controller, FM2 to the FL2 controller, and there is no `FL3.PLC.*` namespace on any machine. Four consequences, none designed: **(1)** `PLCTagService.ResolveOpcInfoAsync` resolves **one** `OPCInfo` per `LineId` from `Lines:{line}:MachineId`, so FL3 needs two resolutions and two `WriteTag` batches — `[PLC §7.1]`'s *"one acknowledgement pushes all FM1 and FM2 tags in a single batch"* is superseded for FL3. **(2)** The compensating re-clear in `FailAsync` then spans **two failure domains**, and `[PLC §7.5]` — which already warns it is a re-clear and not a rollback — has never specified the FL1-commits / FL2-fails case. **(3)** `ITInhibit` is one tag per line (`[PLC §8.1]`), so blocking FL3 means setting **both** `FL1.PLC.ITInhibit` and `FL2.PLC.ITInhibit`; setting one blocks nothing. **(4)** FL3's 33 configured paths must be re-pointed into the FL1 and FL2 namespaces | Backend / Architecture | **High** | **FL3 cannot run.** FL1 and FL2 are unaffected and their registration is unblocked, but until this is designed an FL3 check-in has no defined push, no defined recovery and no working interlock. ⚠ The commissioning test is **blind to it**: *"one acknowledgement configures FM1 and FM2"* passes under either topology, which is how `PLC-Q08` stayed open so long | **Fail loudly rather than half-work, until it is designed.** `Lines:FL3:MachineId` stays `0` so `FW-151`'s tripwire fails every FL3 push **by name**; `11_` does not register machine 127 and its verification block asserts 127 is absent. Then design it: the two-batch push, two-domain compensation, the `ITInhibit` pair, and the re-pointing of the 33 paths. ⚠ **Rewrite the commissioning test** to prove both controllers were written and that a partial failure re-clears both. ⚠ **Do not "fix" this by seeding 127** — that makes an FL3 push reach a controller nobody designed the write for | **Open — raised 5 Sep 2026** with `D-47`, from planning `FW-238`. Owns FL3's machine interface; does not block FL1/FL2 |
| **G94** | ⛔ **`OPCUAManager` has no canonical tag identity: five call sites, four formats, and only the subscribe path is right.** `OPCTag.GetOpcUaTagName()` (`ual-api/API/Common/UA.APIDTO/OPC/OPCTag.cs:86`) returns `ns=2;s={TagName}`, and `Subscribe` (`OPCUAManager.cs:247`) uses it — but `ReadTag` (`:207`) and `WriteTag` (`:359`) pass the **bare** name, which QuickOPC parses as namespace **0**, a different node; `GetSystemErrorTagValue` (`:554`) hand-builds the same literal a fifth time; and the notification handler (`:670`) publishes `e.Arguments.NodeDescriptor.ToString()`, whose real output is the debug form `NodeId="ns=2;s=Tag"` — **verified by loading `OpcLabs.EasyOpcUAPrimitives.dll` 5.84.192 and invoking the conversion**, and fed back to a UA API that string parses **without throwing** as namespace 0 with the whole literal as the identifier. `OPCDAManager` is correct (`:679` uses `ItemDescriptor.ItemId`), so **the two managers publish different identities on the same event**. ⚠ **Distinct from `G58`**: that is the write read-back at `:362` reported as success; this is *which node was addressed at all*, and `G58`'s swallow is what has kept it invisible | Integration / Backend | **Medium** — latent | ⚠ **Latent, and measured so rather than assumed — 4 Sep 2026 on `DEV00164-001`:** all four `CommonDB.OPCModules` rows are `21316` (`OPCDA`), so `OPCUAManager` is selected by nothing and **no part of this executes today**. It bites on the first module set to `21317` (`OPCUA`), and three things then fail silently: the SignalR contract (`OPCManagerHub.cs:161-162` sends the decorated name to `transferOPCdataForMachine{id}` on `Clients.All`), the cooling-chamber popup's exact full-path match (`cooling-chamber-popup.component.ts:121`), and **alarm history plus snooze** — `Alarms_InsertAlarmOnHistory.sql:37` joins `OT.Tag_Name = @tagName`, so `@alarmRuleId` is `NULL` and the `IF @alarmRuleId IS NOT NULL` gate at `:39` skips the insert with **no row and no error**. ⛔ **The Flat Wire exposure is `D-44`**, which puts the 72 flat wire tag paths into that same `CommonDB` registration (`G93`), while FL1/FL2/FL3 are new hardware for which UA is the likely default — so `FW-N05` `P-118`'s decision to poll rather than subscribe protects us from `:670` but **not** from `:207`/`:359`. And nothing pins the format: `OPCConnection` has **zero** tests and no Angular spec feeds a prefixed name, so no regression would be caught | **Canonicalise, and sequence it against `G58`.** (1) Bare `TagName` becomes the domain and wire identity, with the `ns=2;s=` form applied only at the QuickOPC boundary: correlate notifications on the **`object state` overload** of `SubscribeDataChange` — it exists in the pinned 5.84.192 and `e.Arguments.State` reads it back — instead of re-stringifying a descriptor; correct `:207`/`:359` to `ns=2`; collapse `:554`'s duplicate literal; and make `UpdateOPCInfoTags`' lookup (`:702`, ordinal) case-insensitive to match `OPCTag`'s own `Equals` (`OPCTag.cs:125-175`). (2) ⛔ **Land it BEFORE `G58`'s error surfacing** — `FW-236` AC 3/4 lift the swallow that hides the failed `ns=0` write, so lifting that first would surface a flood of genuine-but-unexplained failures. ⚠ **A test seam is required, not optional**: `Read`, `WriteValue` and `SubscribeDataChange` are **extension methods** on `IEasyUAClient` (which declares only `ReadMultiple`/`WriteMultiple`/`SubscribeMultipleMonitoredItems`), so `Mock<IEasyUAClient>` cannot intercept them and a narrow first-party adapter is the only route to a test. ⚠ `DefaultNamespaceIndex = 2` is a **compile-time constant** (`OPCTag.cs:14`) and no repository can prove every deployed server exposes tags in namespace 2; making it per-`OPCServer` configuration is the follow-up, and only `[PLC]`'s `C1`/`C11` settle it | **Open — raised 4 Sep 2026** from the `FW-236` review of `OPCUAManager` against the built `ual-api` · ✅ **Owned by `FW-236`** (4 Sep 2026). ⚠ **A change in ANOTHER service** — `OPCConnection` sits on four consumers' paths (Alarms, CoolingChamber, FurnaceScheduler, FlatWire), so it is theirs to accept: the same boundary `G58` and `FW-N05` both draw. ✅ **Priority is measured, not inferred** — 0 of 4 modules on UA, and 0 of 496 `CommonDB.OPCTags` rows carry a prefix, so there is no config to migrate |
| **G100** | ⚠ **`DEV00164-001`'s `CommonDB` is BEHIND `ual-database` source control, and the flat wire registration is the first thing to need the difference.** ✅ **THE HALF THAT BLOCKED `11_` IS CLOSED, 5 Sep 2026:** the two columns were added on `DEV00164-001` by hand and the change is captured as [`08_CommonDB_OPCModules_ColumnDrift.sql`](../30-database/scripts/08_CommonDB_OPCModules_ColumnDrift.sql), so `dbo.OPCModules` now carries all six and `11_` §4a binds clean — re-measured the same day with a `SET NOEXEC ON` compile pass that returns **zero** messages. ⛔ **THE OTHER HALF STANDS AND THE ROW STAYS OPEN:** the deployed `dbo.GetOPCServerAndTagDetails` still does not select the two columns, so the instance is still behind — it just no longer blocks this project. *(Original measurement, 5 Sep 2026, on the instance `[DEP §2]` names as the **`test1` database server**: `dbo.OPCModules` **had four** columns — `OPCModulesIdx` · `ModuleName` · `IsReadOnly` · `ConnectionType` — while `ual-database/Databases/CommonDB/Tables/OPCModules/CreateTable.sql` *(sibling repo, not linkable from here)* declares **six**, adding `OPCEventType` and `EventDurationSeconds`. The live `GetOPCServerAndTagDetails` does not select those two either, while the source-controlled copy does (`:26-27`) — so **the instance is internally consistent and simply stale**, which is why nothing has ever failed on it. ⚠ **`11_` is CORRECT and the database is wrong** — the script inserts all six because source control declares all six. ✅ **Scope measured, not assumed:** the other four tables (`OPCTags`, `OPCTagApplicationMapping`, `OPCServers`, `OPCServerApplicationMapping`) were compared column by column the same day and **match exactly**, so `OPCModules` is the only drifted **table** of the five.)* ⛔ **What is left is not a table.** The procedure half carries a hazard the column fix created — see the resolution cell | Database / Deployment | **High** | **`11_` would have died mid-transaction on Msg 207 `Invalid column name 'OPCEventType'`** — at section 4a, the *first* insert, so nothing is written and the failure is clean. But the message reads as **a typo in the script**, not as a stale database, which sends the reader to the wrong file. ⚠ **It would have surfaced inside the 40 h Phase-14 window**, on the environment `[DEP]` nominates for `test1`, on top of `G97` and `G94` — the compression `[PLCC §4]` already calls the worst in the schedule. ⚠ **And it is invisible to every existing check**: `verify_schema_counts.py` covers `FlatWireDB`, not `CommonDB`, and `11_`'s five original guards all pass on this instance. ⛔ **And so was the sixth guard — it could not fire.** It was written into the **same batch** as §4a, and SQL Server binds column names for an existing table at **batch compile** time, so `Msg 207` killed the batch — guard included — before its first line ran. Measured on `DEV00164-001` while the columns were still missing: `Msg 207` and nothing else, the guard's `PRINT` never appeared. `SET NOEXEC ON` does not suppress it either. ⛔ **A NEW impact, created by the fix itself: adding the columns REMOVED AN INTERLOCK.** While they were absent, `CREATE OR ALTER` on `dbo.GetOPCServerAndTagDetails` was *impossible* — it binds columns at create time and failed on `Msg 207`. It now succeeds, and `OPCModulesIdx` **1-4** hold **NULL** in both columns, which `UA.APIDTO.OPCInfo` maps as **non-nullable `int`** through Dapper (`ContextRepository.cs:41`, `gr.Read<OPCInfo>()`). So refreshing that procedure **alone** now makes `GetOPCInfo` **throw for `Hole Detection`, `CoilReceiving`, `Handheld Service` and `Furnace Scheduling`** — four modules this project does not own | ✅ **Fixed on `test1` and captured as a script, 5 Sep 2026** — [`08_CommonDB_OPCModules_ColumnDrift.sql`](../30-database/scripts/08_CommonDB_OPCModules_ColumnDrift.sql), guarded per column and idempotent, so it reports *present* on an instance that is already level. It adds **nullable** columns to match source control exactly and **writes no row**. ✅ **`11_`'s guard now works** — moved to **§0**, a batch of its own ahead of the seed, and proven: on a simulated stale instance it prints the table shape, aborts by name, and the seed batch is **never sent** (sqlcmd exits 1, no `Msg 207`). `:on error exit` was deliberately **not** used — it is SQLCMD-mode-only and `11_` is run by hand, so under sqlcmd pass **`-b`**. ⛔ **BEFORE ANYONE REFRESHES `dbo.GetOPCServerAndTagDetails`, BACKFILL `OPCModulesIdx` 1-4** — see the impact cell; the values are not this project's to invent and no current `CommonDB` is recorded to read them from (`[DEP §2]` fills in a database server for `test1` only). ⛔ **Never "refresh" the table by running `ual-database`'s `OPCModules/CreateTable.sql`** — it opens with `DROP TABLE IF EXISTS`. Since `09_` ran it now fails on `Msg 3726` against the two FKs, which is luck, not a design. ⚠ **Re-check per environment**, exactly as `G96` requires for the module id — `test1`, staging and production can each be at a different revision | **Open — raised 5 Sep 2026** by *executing* `FW-238`'s plan against the live instance rather than reading it, and **NARROWED the same day by executing it again**: the table half is closed on `DEV00164-001`, the procedure half is not. ✅ **No longer blocks `FW-238`** — it is off that story's `blocked_by`, because flat wire reaches the PLC through `WriteTag`, not the SignalR hub, so the two columns the stale procedure withholds are ones it never reads (`11_` `D6`). ⛔ **`11_` remains gated on `G94`/`FW-236` alone.** ⚠ **Owned by whoever owns `CommonDB` provisioning, not by `FW-238`** — the story's own artifact is correct. ⚠ **Not split into a new id**: the procedure half was already inside this row's original remediation text, so it is narrowed here rather than re-minted. New gaps still mint at `G101`+ |
